local _CoreGui = game:GetService("CoreGui")
local _TweenService = game:GetService("TweenService")
local _RunService = game:GetService("RunService")
local _Players = game:GetService("Players")
local _UserInputService = game:GetService("UserInputService")
local _SoundService = game:GetService("SoundService")
local _HttpService = game:GetService("HttpService")

local runtimeEnv = (getgenv and getgenv()) or _G
runtimeEnv.__FOB_AUTOMINE_CFRAME_EXPERIMENT = true
runtimeEnv.__FOB_AUTOMINE_FORCE_CFRAME = true
local localPlayer = _Players.LocalPlayer
local _SCRIPT_SESSION_START_TICK = tick()
local AUTOMINE_ONLY_MODE = false
local EXPERIMENTAL_AUTOMINE_CFRAME = true
local FORCE_AUTOMINE_CFRAME_DEFAULT = true

-- Clear any stale one-tab toggle so the full hub always loads in this build.
runtimeEnv.__FOB_AUTOMINE_ONLY = nil

local sessionToken = tostring(os.clock()) .. "_" .. tostring(math.random(1000, 9999))
runtimeEnv._FOB_GUI_ONLY_TOKEN = sessionToken

local function cleanupExistingGui()
    local roots = {_CoreGui}
    if localPlayer then
        local playerGui = localPlayer:FindFirstChild("PlayerGui")
        if playerGui then
            table.insert(roots, playerGui)
        end
    end
    for _, root in ipairs(roots) do
        local existing = root and root:FindFirstChild("FOBGuiOnlyRoot")
        if existing then
            pcall(function()
                existing:Destroy()
            end)
        end
        local overlay = root and root:FindFirstChild("FobGuiOnlyFpsPingOverlay")
        if overlay then
            pcall(function()
                overlay:Destroy()
            end)
        end
        local legacyAutoMine = root and root:FindFirstChild("WildWestAutoMineGui")
        if legacyAutoMine then
            pcall(function()
                legacyAutoMine:Destroy()
            end)
        end
        if root then
            for _, child in ipairs(root:GetChildren()) do
                if child:IsA("ScreenGui") and child.Name:match("^DropdownGui_") then
                    pcall(function()
                        child:Destroy()
                    end)
                end
            end
        end
    end
    for _, folderName in ipairs({"AutoMineRecordedPathViz", "AutoMinePathViz", "AutoMinePathAvoiderViz"}) do
        local folder = workspace:FindFirstChild(folderName)
        if folder then
            pcall(function()
                folder:Destroy()
            end)
        end
    end
end

cleanupExistingGui()

local function isSessionActive()
    return runtimeEnv._FOB_GUI_ONLY_TOKEN == sessionToken
end

local hubGui = Instance.new("ScreenGui")
hubGui.Name = "FOBGuiOnlyRoot"
hubGui.ResetOnSpawn = false
hubGui.IgnoreGuiInset = true
hubGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
hubGui.DisplayOrder = 50

do
    local parented = pcall(function()
        hubGui.Parent = _CoreGui
    end)
    if not parented and localPlayer then
        hubGui.Parent = localPlayer:WaitForChild("PlayerGui")
    end
end

runtimeEnv._FOB_GUI_ONLY_ROOT = hubGui

local DEFAULTS = {
    notificationsEnabled = true,
    fpsPingStatsEnabled = false,
    guiTransparency = 0,
    demoToggle = false,
    demoSlider = 40,
    demoDropdown = "Default",
    demoInput = "FOB GUI Only",
    dynamiteTpEnabled = false,
    dynamiteStyle = "Blatant",
    dynamiteTarget = "Vault",
    dynamiteSpectateEnabled = false,
    dynamiteMarkPlayersEnabled = false,
    dynamiteSelectedPlayer = "No Players",
    infiniteStaminaEnabled = false,
    safeTeleportEnabled = false,
    noJumpCooldownEnabled = false,
    noFallDamageEnabled = false,
    noRagdollEnabled = false,
    autoBreakFreeEnabled = false,
    autoGetUpEnabled = false,
    equipWhileSwimmingEnabled = false,
    silentAimEnabled = false,
    silentAimBodyTarget = "Head",
    silentAimMode = "FOV",
    silentAimVisibilityCheck = false,
    silentAimTargetPriority = {
        Players = true,
        NPCs = false,
        Entities = false,
    },
    silentAimAccuracy = 50,
    silentAimDistanceLimit = 1000,
    gunFovEnabled = false,
    gunFovShape = "Circle",
    gunFovRadius = 200,
    gunSnaplineEnabled = false,
    fastProjectileEnabled = false,
    wallBangEnabled = false,
    noSpreadEnabled = false,
    noRecoilEnabled = false,
    noFireCooldownEnabled = false,
    fastReloadEnabled = false,
    instantLassoChargeEnabled = false,
    lassoSilentAimEnabled = false,
    lassoSnaplineEnabled = false,
    lassoNoRopeLimitEnabled = false,
    fasterLassoEnabled = false,
    playerEspEnabled = false,
    playerEspMethods = {
        Text = true,
        Box = false,
        Tracer = false,
        Figure = false,
    },
    staffNotifyEnabled = false,
    staffKickEnabled = false,
    pathRecordNodeSpacing = 9,
    pathPlayerAvoiderEnabled = false,
    pathPlayerAvoiderDistance = 45,
    pathPlayerAvoiderShowCorridor = false,
    pathPlayerAvoiderMethod = "Corridor",
    pathPlayerAvoiderAction = "Movement",
    pathPlayerAvoiderRadius = 40,
    pathPlayerAvoiderShowRadius = true,
    pathHopOnDeathEnabled = false,
    discordAutomineWebhookTrackingEnabled = false,
    discordAutomineWebhookUrl = "",
    discordAutomineWebhookOptions = {
        ["Track earnings"] = true,
        ["Track flags"] = false,
    },
}

local S = {
    notificationsEnabled = DEFAULTS.notificationsEnabled,
    fpsPingStatsEnabled = DEFAULTS.fpsPingStatsEnabled,
    guiTransparency = DEFAULTS.guiTransparency,
    demoToggle = DEFAULTS.demoToggle,
    demoSlider = DEFAULTS.demoSlider,
    demoDropdown = DEFAULTS.demoDropdown,
    demoInput = DEFAULTS.demoInput,
    dynamiteTpEnabled = DEFAULTS.dynamiteTpEnabled,
    dynamiteStyle = DEFAULTS.dynamiteStyle,
    dynamiteTarget = DEFAULTS.dynamiteTarget,
    dynamiteSpectateEnabled = DEFAULTS.dynamiteSpectateEnabled,
    dynamiteMarkPlayersEnabled = DEFAULTS.dynamiteMarkPlayersEnabled,
    dynamiteSelectedPlayer = DEFAULTS.dynamiteSelectedPlayer,
    infiniteStaminaEnabled = DEFAULTS.infiniteStaminaEnabled,
    safeTeleportEnabled = DEFAULTS.safeTeleportEnabled,
    noJumpCooldownEnabled = DEFAULTS.noJumpCooldownEnabled,
    noFallDamageEnabled = DEFAULTS.noFallDamageEnabled,
    noRagdollEnabled = DEFAULTS.noRagdollEnabled,
    autoBreakFreeEnabled = DEFAULTS.autoBreakFreeEnabled,
    autoGetUpEnabled = DEFAULTS.autoGetUpEnabled,
    equipWhileSwimmingEnabled = DEFAULTS.equipWhileSwimmingEnabled,
    silentAimEnabled = DEFAULTS.silentAimEnabled,
    silentAimBodyTarget = DEFAULTS.silentAimBodyTarget,
    silentAimMode = DEFAULTS.silentAimMode,
    silentAimVisibilityCheck = DEFAULTS.silentAimVisibilityCheck,
    silentAimTargetPriority = {
        Players = DEFAULTS.silentAimTargetPriority.Players,
        NPCs = DEFAULTS.silentAimTargetPriority.NPCs,
        Entities = DEFAULTS.silentAimTargetPriority.Entities,
    },
    silentAimAccuracy = DEFAULTS.silentAimAccuracy,
    silentAimDistanceLimit = DEFAULTS.silentAimDistanceLimit,
    gunFovEnabled = DEFAULTS.gunFovEnabled,
    gunFovShape = DEFAULTS.gunFovShape,
    gunFovRadius = DEFAULTS.gunFovRadius,
    gunSnaplineEnabled = DEFAULTS.gunSnaplineEnabled,
    fastProjectileEnabled = DEFAULTS.fastProjectileEnabled,
    wallBangEnabled = DEFAULTS.wallBangEnabled,
    noSpreadEnabled = DEFAULTS.noSpreadEnabled,
    noRecoilEnabled = DEFAULTS.noRecoilEnabled,
    noFireCooldownEnabled = DEFAULTS.noFireCooldownEnabled,
    fastReloadEnabled = DEFAULTS.fastReloadEnabled,
    instantLassoChargeEnabled = DEFAULTS.instantLassoChargeEnabled,
    lassoSilentAimEnabled = DEFAULTS.lassoSilentAimEnabled,
    lassoSnaplineEnabled = DEFAULTS.lassoSnaplineEnabled,
    lassoNoRopeLimitEnabled = DEFAULTS.lassoNoRopeLimitEnabled,
    fasterLassoEnabled = DEFAULTS.fasterLassoEnabled,
    playerEspEnabled = DEFAULTS.playerEspEnabled,
    playerEspMethods = {
        Text = DEFAULTS.playerEspMethods.Text,
        Box = DEFAULTS.playerEspMethods.Box,
        Tracer = DEFAULTS.playerEspMethods.Tracer,
        Figure = DEFAULTS.playerEspMethods.Figure,
    },
    staffNotifyEnabled = DEFAULTS.staffNotifyEnabled,
    staffKickEnabled = DEFAULTS.staffKickEnabled,
    pathRecordNodeSpacing = DEFAULTS.pathRecordNodeSpacing,
    pathPlayerAvoiderEnabled = DEFAULTS.pathPlayerAvoiderEnabled,
    pathPlayerAvoiderDistance = DEFAULTS.pathPlayerAvoiderDistance,
    pathPlayerAvoiderShowCorridor = DEFAULTS.pathPlayerAvoiderShowCorridor,
    pathPlayerAvoiderMethod = DEFAULTS.pathPlayerAvoiderMethod,
    pathPlayerAvoiderAction = DEFAULTS.pathPlayerAvoiderAction,
    pathPlayerAvoiderRadius = DEFAULTS.pathPlayerAvoiderRadius,
    pathPlayerAvoiderShowRadius = DEFAULTS.pathPlayerAvoiderShowRadius,
    pathHopOnDeathEnabled = DEFAULTS.pathHopOnDeathEnabled,
    discordAutomineWebhookTrackingEnabled = DEFAULTS.discordAutomineWebhookTrackingEnabled,
    discordAutomineWebhookUrl = DEFAULTS.discordAutomineWebhookUrl,
    discordAutomineWebhookOptions = {
        ["Track earnings"] = DEFAULTS.discordAutomineWebhookOptions["Track earnings"],
        ["Track flags"] = DEFAULTS.discordAutomineWebhookOptions["Track flags"],
    },
    keybinds = {
        dynamiteMark = Enum.KeyCode.F,
    },
}

local guiToggleKeyCode = Enum.KeyCode.E
local guiToggleKeyName = "E"
local panicKeyCode = nil
local panicKeyName = "none"

local PLAYER_ESP_METHOD_ORDER = {"Text", "Box", "Tracer", "Figure"}
local GUN_FOV_SHAPE_ORDER = {"Circle", "Hexagon", "Square", "Triangle"}
local GUN_AIM_MODE_ORDER = {"FOV", "Closest"}
local GUN_TARGET_PRIORITY_ORDER = {"Players", "NPCs", "Entities"}
local PATH_PLAYER_AVOIDER_METHOD_ORDER = {"Corridor", "Radius Field"}
local PATH_PLAYER_AVOIDER_ACTION_ORDER = {"Movement", "Hop"}
local AUTOMINE_WEBHOOK_TRACK_OPTION_ORDER = {"Track earnings", "Track flags"}

local function clonePlayerEspMethods(source)
    local cloned = {
        Text = false,
        Box = false,
        Tracer = false,
        Figure = false,
    }

    if type(source) == "table" then
        for _, methodName in ipairs(PLAYER_ESP_METHOD_ORDER) do
            cloned[methodName] = source[methodName] == true
        end
    end

    if not cloned.Text and not cloned.Box and not cloned.Tracer and not cloned.Figure then
        cloned.Text = true
    end

    return cloned
end

local function cloneGunTargetPriority(source)
    local cloned = {
        Players = false,
        NPCs = false,
        Entities = false,
    }

    if type(source) == "table" then
        for _, priorityName in ipairs(GUN_TARGET_PRIORITY_ORDER) do
            cloned[priorityName] = source[priorityName] == true
        end
    end

    if not cloned.Players and not cloned.NPCs and not cloned.Entities then
        cloned.Players = true
    end

    return cloned
end

local function cloneAutomineWebhookOptions(source)
    local cloned = {
        ["Track earnings"] = true,
        ["Track flags"] = false,
    }

    if type(source) == "table" then
        for _, optionName in ipairs(AUTOMINE_WEBHOOK_TRACK_OPTION_ORDER) do
            if source[optionName] ~= nil then
                cloned[optionName] = source[optionName] == true
            end
        end
    end

    if not cloned["Track earnings"] and not cloned["Track flags"] then
        cloned["Track earnings"] = true
    end

    return cloned
end

local function normalizePathPlayerAvoiderMethod(value)
    local candidate = tostring(value or DEFAULTS.pathPlayerAvoiderMethod)
    for _, methodName in ipairs(PATH_PLAYER_AVOIDER_METHOD_ORDER) do
        if string.lower(candidate) == string.lower(methodName) then
            return methodName
        end
    end
    return DEFAULTS.pathPlayerAvoiderMethod
end

local function normalizePathPlayerAvoiderAction(value)
    local candidate = tostring(value or DEFAULTS.pathPlayerAvoiderAction)
    for _, actionName in ipairs(PATH_PLAYER_AVOIDER_ACTION_ORDER) do
        if string.lower(candidate) == string.lower(actionName) then
            return actionName
        end
    end
    return DEFAULTS.pathPlayerAvoiderAction
end

S.playerEspMethods = clonePlayerEspMethods(S.playerEspMethods)
S.silentAimTargetPriority = cloneGunTargetPriority(S.silentAimTargetPriority)
S._rowTweenState = setmetatable({}, {__mode = "k"})
S._dynamiteMarkedPlayers = {}
S._dynamiteMarkVisuals = {}
S._staffCache = {}
S._staffRoles = {
    Tester = true,
    ["Clothing Designer"] = true,
    Contributor = true,
    Developer = true,
    ["Co-Founders"] = true,
    Holder = true,
}
S._staffGroupId = 3755133
S._espLeaderboardStateMap = nil

local _CLICK_SOUND = "rbxassetid://100906385041190"
local _HOVER_SOUND = "rbxassetid://92708987611847"
local _NOTIF_SOUND = "rbxassetid://127119919079045"

local _NOTIF_TYPES = {
    info = {
        color = Color3.fromRGB(50, 170, 255),
        iconId = "rbxthumb://type=Asset&id=3209694625&w=150&h=150",
        iconColor = Color3.fromRGB(50, 170, 255),
        title = "Info",
    },
    warning = {
        color = Color3.fromRGB(255, 190, 50),
        iconId = "rbxthumb://type=Asset&id=11745872952&w=150&h=150",
        iconColor = Color3.fromRGB(255, 200, 50),
        title = "Warning",
    },
    error = {
        color = Color3.fromRGB(255, 50, 50),
        iconId = "rbxthumb://type=Asset&id=14563958719&w=150&h=150",
        iconColor = Color3.fromRGB(255, 60, 60),
        title = "Error",
    },
    success = {
        color = Color3.fromRGB(50, 200, 80),
        iconId = "rbxthumb://type=Asset&id=9754130800&w=150&h=150",
        iconColor = Color3.fromRGB(60, 220, 90),
        title = "Success",
    },
}

local _NOTIF_WIDTH = 420
local _NOTIF_HEIGHT = 72
local _NOTIF_GAP = 10
local _NOTIF_RIGHT_MARGIN = 24
local _NOTIF_BOTTOM_MARGIN = 24
local _NOTIF_DURATION = 5
local _NOTIF_CIRCLE_BG = "rbxthumb://type=Asset&id=1141086161&w=150&h=150"

local _UTILS = {}
local _allUiSyncs = {}
local _allToggleSyncs = {}
local _allSliderSyncs = {}
local _allDropdownSyncs = {}
local _allTextSyncs = {}
local _buttonRegistry = {}
local _currentRegistryTab = nil
local _rowLayoutCounter = 0
local _activeNotifications = {}
local _dropdownFrames = {}
local _allDropdownGuis = {}
local _extraCleanupGuis = {}
local _captureStackCount = 0
local destroyed = false
local storageLoaded = false

local function registerUiSync(store, fn)
    table.insert(store, fn)
    table.insert(_allUiSyncs, fn)
    return fn
end

local function beginKeyCapture()
    _captureStackCount = _captureStackCount + 1
end

local function endKeyCapture()
    _captureStackCount = math.max(0, _captureStackCount - 1)
end

local function isCapturingKey()
    return _captureStackCount > 0
end

function _UTILS._playSound(soundId, volume)
    if destroyed or not isSessionActive() then
        return
    end
    local sound = Instance.new("Sound")
    sound.SoundId = soundId
    sound.Volume = volume or 0.5
    sound.Parent = _SoundService
    sound:Play()
    sound.Ended:Connect(function()
        pcall(function()
            sound:Destroy()
        end)
    end)
end

function _UTILS._applySoundsToButton(button)
    if not button:IsA("GuiButton") then
        return
    end
    button.MouseEnter:Connect(function()
        if destroyed then
            return
        end
        _UTILS._playSound(_HOVER_SOUND)
    end)
    button.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._playSound(_CLICK_SOUND)
    end)
end

local _glitchSymbols = {"@", "!", "9", "6", "=", "<", "?", "$", "&"}
local _activeDecode = 0

function _UTILS._animateNotifText(label, targetText)
    if not label then
        return
    end
    local currentStr = ""
    for i = 1, #targetText do
        if destroyed or not isSessionActive() or not label.Parent then
            return
        end
        local char = targetText:sub(i, i)
        if char == " " then
            currentStr = currentStr .. char
            label.Text = currentStr
        else
            label.Text = currentStr .. _glitchSymbols[math.random(1, #_glitchSymbols)]
            task.wait(0.04)
            currentStr = currentStr .. char
            label.Text = currentStr
        end
    end
end

function _UTILS._animateTabTitle(label, targetText)
    if not label then
        return
    end
    _activeDecode = _activeDecode + 1
    local currentDecode = _activeDecode
    local currentStr = ""
    for i = 1, #targetText do
        if destroyed or not isSessionActive() or not label.Parent then
            return
        end
        if currentDecode ~= _activeDecode then
            label.Text = targetText
            return
        end
        local char = targetText:sub(i, i)
        if char == " " then
            currentStr = currentStr .. char
            label.Text = currentStr
        else
            label.Text = currentStr .. _glitchSymbols[math.random(1, #_glitchSymbols)]
            task.wait(0.08)
            if currentDecode ~= _activeDecode then
                label.Text = targetText
                return
            end
            currentStr = currentStr .. char
            label.Text = currentStr
        end
    end
end

function _UTILS._createParticleBurst(parent)
    if destroyed or not parent or not parent.Parent then
        return
    end
    local mouse = localPlayer and localPlayer:GetMouse()
    if not mouse then
        return
    end
    local absPos = parent.AbsolutePosition
    local absSize = parent.AbsoluteSize
    local relativeX = mouse.X - absPos.X
    local relativeY = mouse.Y - absPos.Y
    if relativeX < 0 or relativeY < 0 or relativeX > absSize.X or relativeY > absSize.Y then
        relativeX = absSize.X / 2
        relativeY = absSize.Y / 2
    end
    for _ = 1, 10 do
        local square = Instance.new("Frame")
        square.Size = UDim2.new(0, 6, 0, 6)
        square.Position = UDim2.new(0, relativeX, 0, relativeY)
        square.AnchorPoint = Vector2.new(0.5, 0.5)
        square.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
        square.BackgroundTransparency = 0
        square.BorderSizePixel = 0
        square.ZIndex = (parent.ZIndex or 1) + 4
        square.Parent = parent
        Instance.new("UICorner", square).CornerRadius = UDim.new(1, 0)
        local direction = Vector2.new(math.random(-10, 10), math.random(-10, 10))
        if direction.Magnitude <= 0 then
            direction = Vector2.new(1, 0)
        else
            direction = direction.Unit
        end
        local distance = math.random(30, 60)
        local duration = 0.4
        square:TweenPosition(
            UDim2.new(0, relativeX + direction.X * distance, 0, relativeY + direction.Y * distance),
            Enum.EasingDirection.Out.Name,
            Enum.EasingStyle.Quad.Name,
            duration,
            true
        )
        _TweenService:Create(square, TweenInfo.new(duration), {BackgroundTransparency = 1}):Play()
        game:GetService("Debris"):AddItem(square, duration + 0.1)
    end
end

local function keyNameToCode(name)
    local normalized = type(name) == "string" and name:lower() or ""
    if type(name) ~= "string" or normalized == "" or normalized == "none" then
        return nil
    end
    local ok, result = pcall(function()
        return Enum.KeyCode[name]
    end)
    if ok then
        return result
    end
    return nil
end

local function getStorageId()
    local hwid = ""
    pcall(function()
        if gethwid then
            hwid = tostring(gethwid() or "")
        end
    end)
    if hwid == "" then
        pcall(function()
            hwid = tostring(game:GetService("RbxAnalyticsService"):GetClientId() or "")
        end)
    end
    if hwid == "" then
        hwid = tostring(localPlayer and localPlayer.UserId or "default")
    end
    return hwid:gsub("[^%w%-%_]", "_")
end

local storageFolderApisAvailable = type(isfolder) == "function" and type(makefolder) == "function"
local storageReadAvailable = type(readfile) == "function" and type(isfile) == "function"
local storageWriteAvailable = type(writefile) == "function"
local storageDeleteAvailable = type(delfile) == "function"
local storageEnabled = storageReadAvailable and storageWriteAvailable
local CONFIG_SYSTEM = {
    AUTOSAVE_NAME = "Autosave from last session",
    AUTOSAVE_FILE_KEY = "__autosave_last_session",
    FOLDER = "InfinitesHub/AutoMineConfigs",
    selectedConfigName = nil,
    autoLoadConfigName = nil,
    savedConfigs = {},
    autosaveLoadedOnStartup = false,
    autoLoadLoadedOnStartup = false,
}
local storageFileName = "automine_config_meta_" .. getStorageId() .. ".json"
local storagePath = storageFolderApisAvailable and ("InfinitesHub/" .. storageFileName) or storageFileName

local function ensureStorageFolder()
    if not storageFolderApisAvailable then
        return
    end
    if not isfolder("InfinitesHub") then
        makefolder("InfinitesHub")
    end
end

function ensureConfigStorageFolder()
    if not storageFolderApisAvailable then
        return
    end
    ensureStorageFolder()
    if not isfolder(CONFIG_SYSTEM.FOLDER) then
        makefolder(CONFIG_SYSTEM.FOLDER)
    end
end

function trimConfigName(rawValue)
    return (tostring(rawValue or ""):gsub("^%s+", ""):gsub("%s+$", ""))
end

function sanitizeConfigFileKey(rawValue)
    local cleaned = trimConfigName(rawValue)
    cleaned = cleaned:gsub("[/\\:%*%?\"<>|]", "_")
    cleaned = cleaned:gsub("%s+", "_")
    cleaned = cleaned:gsub("[^%w%-%_]", "_")
    cleaned = cleaned:gsub("_+", "_")
    return cleaned ~= "" and cleaned or "config"
end

function normalizeSavedConfigNames(source)
    local result = {}
    local seen = {}

    if type(source) == "table" then
        for _, rawName in ipairs(source) do
            local trimmed = trimConfigName(rawName)
            if trimmed ~= "" and trimmed ~= CONFIG_SYSTEM.AUTOSAVE_NAME and not seen[trimmed] then
                seen[trimmed] = true
                table.insert(result, trimmed)
            end
        end
    end

    table.sort(result, function(a, b)
        return a:lower() < b:lower()
    end)

    return result
end

function getConfigFilePath(name)
    local fileKey = tostring(name) == CONFIG_SYSTEM.AUTOSAVE_NAME and CONFIG_SYSTEM.AUTOSAVE_FILE_KEY or sanitizeConfigFileKey(name)
    local fileName = fileKey .. ".cfg"
    if storageFolderApisAvailable then
        return CONFIG_SYSTEM.FOLDER .. "/" .. fileName
    end
    return fileName
end

function getSelectedConfigName()
    local selected = trimConfigName(CONFIG_SYSTEM.selectedConfigName)
    if selected == "" or selected == "None" then
        return nil
    end
    return selected
end

function getAutoLoadConfigName()
    local selected = trimConfigName(CONFIG_SYSTEM.autoLoadConfigName)
    if selected == "" or selected == "None" then
        return nil
    end
    return selected
end

local function serializeState()
    local payload = {
        notificationsEnabled = S.notificationsEnabled,
        fpsPingStatsEnabled = S.fpsPingStatsEnabled,
        guiTransparency = S.guiTransparency,
        demoToggle = S.demoToggle,
        demoSlider = S.demoSlider,
        demoDropdown = S.demoDropdown,
        demoInput = S.demoInput,
        dynamiteTpEnabled = S.dynamiteTpEnabled,
        dynamiteStyle = S.dynamiteStyle,
        dynamiteTarget = S.dynamiteTarget,
        dynamiteSpectateEnabled = S.dynamiteSpectateEnabled,
        dynamiteMarkPlayersEnabled = S.dynamiteMarkPlayersEnabled,
        dynamiteSelectedPlayer = S.dynamiteSelectedPlayer,
        infiniteStaminaEnabled = S.infiniteStaminaEnabled,
        safeTeleportEnabled = S.safeTeleportEnabled,
        noJumpCooldownEnabled = S.noJumpCooldownEnabled,
        noFallDamageEnabled = S.noFallDamageEnabled,
        noRagdollEnabled = S.noRagdollEnabled,
        autoBreakFreeEnabled = S.autoBreakFreeEnabled,
        autoGetUpEnabled = S.autoGetUpEnabled,
        equipWhileSwimmingEnabled = S.equipWhileSwimmingEnabled,
        silentAimEnabled = S.silentAimEnabled,
        silentAimBodyTarget = S.silentAimBodyTarget,
        silentAimMode = S.silentAimMode,
        silentAimVisibilityCheck = S.silentAimVisibilityCheck,
        silentAimTargetPriority = cloneGunTargetPriority(S.silentAimTargetPriority),
        silentAimAccuracy = S.silentAimAccuracy,
        silentAimDistanceLimit = S.silentAimDistanceLimit,
        gunFovEnabled = S.gunFovEnabled,
        gunFovShape = S.gunFovShape,
        gunFovRadius = S.gunFovRadius,
        gunSnaplineEnabled = S.gunSnaplineEnabled,
        fastProjectileEnabled = S.fastProjectileEnabled,
        wallBangEnabled = S.wallBangEnabled,
        noSpreadEnabled = S.noSpreadEnabled,
        noRecoilEnabled = S.noRecoilEnabled,
        noFireCooldownEnabled = S.noFireCooldownEnabled,
        fastReloadEnabled = S.fastReloadEnabled,
        instantLassoChargeEnabled = S.instantLassoChargeEnabled,
        lassoSilentAimEnabled = S.lassoSilentAimEnabled,
        lassoSnaplineEnabled = S.lassoSnaplineEnabled,
        lassoNoRopeLimitEnabled = S.lassoNoRopeLimitEnabled,
        fasterLassoEnabled = S.fasterLassoEnabled,
        playerEspEnabled = S.playerEspEnabled,
        playerEspMethods = clonePlayerEspMethods(S.playerEspMethods),
        staffNotifyEnabled = S.staffNotifyEnabled,
        staffKickEnabled = S.staffKickEnabled,
        pathRecordNodeSpacing = S.pathRecordNodeSpacing,
        pathPlayerAvoiderEnabled = S.pathPlayerAvoiderEnabled,
        pathPlayerAvoiderDistance = S.pathPlayerAvoiderDistance,
        pathPlayerAvoiderShowCorridor = S.pathPlayerAvoiderShowCorridor,
        pathPlayerAvoiderMethod = S.pathPlayerAvoiderMethod,
        pathPlayerAvoiderAction = S.pathPlayerAvoiderAction,
        pathPlayerAvoiderRadius = S.pathPlayerAvoiderRadius,
        pathPlayerAvoiderShowRadius = S.pathPlayerAvoiderShowRadius,
        pathHopOnDeathEnabled = S.pathHopOnDeathEnabled,
        discordAutomineWebhookTrackingEnabled = S.discordAutomineWebhookTrackingEnabled,
        discordAutomineWebhookUrl = S.discordAutomineWebhookUrl,
        discordAutomineWebhookOptions = cloneAutomineWebhookOptions(S.discordAutomineWebhookOptions),
        guiToggleKey = guiToggleKeyName,
        panicKey = panicKeyName,
        keybinds = {},
    }
    for bindKey, keyCode in pairs(S.keybinds) do
        if keyCode and typeof(keyCode) == "EnumItem" then
            payload.keybinds[bindKey] = keyCode.Name
        end
    end
    return payload
end

function applySerializedState(data)
    if type(data) ~= "table" then
        return false
    end
    if type(data.notificationsEnabled) == "boolean" then
        S.notificationsEnabled = data.notificationsEnabled
    end
    if type(data.fpsPingStatsEnabled) == "boolean" then
        S.fpsPingStatsEnabled = data.fpsPingStatsEnabled
    end
    if type(data.guiTransparency) == "number" then
        S.guiTransparency = math.clamp(math.floor(data.guiTransparency + 0.5), 0, 100)
    end
    if type(data.demoToggle) == "boolean" then
        S.demoToggle = data.demoToggle
    end
    if type(data.demoSlider) == "number" then
        S.demoSlider = math.clamp(math.floor(data.demoSlider + 0.5), 0, 100)
    end
    if type(data.demoDropdown) == "string" and data.demoDropdown ~= "" then
        S.demoDropdown = data.demoDropdown
    end
    if type(data.demoInput) == "string" then
        S.demoInput = data.demoInput
    end
    if type(data.dynamiteTpEnabled) == "boolean" then
        S.dynamiteTpEnabled = data.dynamiteTpEnabled
    end
    if type(data.dynamiteStyle) == "string" and data.dynamiteStyle ~= "" then
        S.dynamiteStyle = data.dynamiteStyle
    end
    if type(data.dynamiteTarget) == "string" and data.dynamiteTarget ~= "" then
        S.dynamiteTarget = data.dynamiteTarget
    end
    if type(data.dynamiteSpectateEnabled) == "boolean" then
        S.dynamiteSpectateEnabled = data.dynamiteSpectateEnabled
    end
    if type(data.dynamiteMarkPlayersEnabled) == "boolean" then
        S.dynamiteMarkPlayersEnabled = data.dynamiteMarkPlayersEnabled
    end
    if type(data.dynamiteSelectedPlayer) == "string" and data.dynamiteSelectedPlayer ~= "" then
        S.dynamiteSelectedPlayer = data.dynamiteSelectedPlayer
    end
    if type(data.infiniteStaminaEnabled) == "boolean" then
        S.infiniteStaminaEnabled = data.infiniteStaminaEnabled
    end
    if type(data.safeTeleportEnabled) == "boolean" then
        S.safeTeleportEnabled = data.safeTeleportEnabled
    end
    if type(data.noJumpCooldownEnabled) == "boolean" then
        S.noJumpCooldownEnabled = data.noJumpCooldownEnabled
    end
    if type(data.noFallDamageEnabled) == "boolean" then
        S.noFallDamageEnabled = data.noFallDamageEnabled
    end
    if type(data.noRagdollEnabled) == "boolean" then
        S.noRagdollEnabled = data.noRagdollEnabled
    end
    if type(data.autoBreakFreeEnabled) == "boolean" then
        S.autoBreakFreeEnabled = data.autoBreakFreeEnabled
    end
    if type(data.autoGetUpEnabled) == "boolean" then
        S.autoGetUpEnabled = data.autoGetUpEnabled
    end
    if type(data.equipWhileSwimmingEnabled) == "boolean" then
        S.equipWhileSwimmingEnabled = data.equipWhileSwimmingEnabled
    end
    if type(data.silentAimEnabled) == "boolean" then
        S.silentAimEnabled = data.silentAimEnabled
    end
    if type(data.silentAimBodyTarget) == "string" and data.silentAimBodyTarget ~= "" then
        S.silentAimBodyTarget = data.silentAimBodyTarget
    end
    if type(data.silentAimMode) == "string" and data.silentAimMode ~= "" then
        S.silentAimMode = data.silentAimMode
    end
    if type(data.silentAimVisibilityCheck) == "boolean" then
        S.silentAimVisibilityCheck = data.silentAimVisibilityCheck
    end
    if type(data.silentAimTargetPriority) == "table" then
        S.silentAimTargetPriority = cloneGunTargetPriority(data.silentAimTargetPriority)
    end
    if type(data.silentAimAccuracy) == "number" then
        S.silentAimAccuracy = math.clamp(math.floor(data.silentAimAccuracy + 0.5), 1, 100)
    end
    if type(data.silentAimDistanceLimit) == "number" then
        S.silentAimDistanceLimit = math.clamp(math.floor(data.silentAimDistanceLimit + 0.5), 10, 20000)
    end
    if type(data.gunFovEnabled) == "boolean" then
        S.gunFovEnabled = data.gunFovEnabled
    end
    if type(data.gunFovShape) == "string" and data.gunFovShape ~= "" then
        S.gunFovShape = data.gunFovShape
    end
    if type(data.gunFovRadius) == "number" then
        S.gunFovRadius = math.clamp(math.floor(data.gunFovRadius + 0.5), 10, 2000)
    end
    if type(data.gunSnaplineEnabled) == "boolean" then
        S.gunSnaplineEnabled = data.gunSnaplineEnabled
    end
    if type(data.fastProjectileEnabled) == "boolean" then
        S.fastProjectileEnabled = data.fastProjectileEnabled
    end
    if type(data.wallBangEnabled) == "boolean" then
        S.wallBangEnabled = data.wallBangEnabled
    end
    if type(data.noSpreadEnabled) == "boolean" then
        S.noSpreadEnabled = data.noSpreadEnabled
    end
    if type(data.noRecoilEnabled) == "boolean" then
        S.noRecoilEnabled = data.noRecoilEnabled
    end
    if type(data.noFireCooldownEnabled) == "boolean" then
        S.noFireCooldownEnabled = data.noFireCooldownEnabled
    end
    if type(data.fastReloadEnabled) == "boolean" then
        S.fastReloadEnabled = data.fastReloadEnabled
    end
    if type(data.instantLassoChargeEnabled) == "boolean" then
        S.instantLassoChargeEnabled = data.instantLassoChargeEnabled
    end
    if type(data.lassoSilentAimEnabled) == "boolean" then
        S.lassoSilentAimEnabled = data.lassoSilentAimEnabled
    end
    if type(data.lassoSnaplineEnabled) == "boolean" then
        S.lassoSnaplineEnabled = data.lassoSnaplineEnabled
    end
    if type(data.lassoNoRopeLimitEnabled) == "boolean" then
        S.lassoNoRopeLimitEnabled = data.lassoNoRopeLimitEnabled
    end
    if type(data.fasterLassoEnabled) == "boolean" then
        S.fasterLassoEnabled = data.fasterLassoEnabled
    end
    if type(data.playerEspEnabled) == "boolean" then
        S.playerEspEnabled = data.playerEspEnabled
    end
    if type(data.playerEspMethods) == "table" then
        S.playerEspMethods = clonePlayerEspMethods(data.playerEspMethods)
    end
    if type(data.staffNotifyEnabled) == "boolean" then
        S.staffNotifyEnabled = data.staffNotifyEnabled
    end
    if type(data.staffKickEnabled) == "boolean" then
        S.staffKickEnabled = data.staffKickEnabled
    end
    if type(data.pathRecordNodeSpacing) == "number" then
        S.pathRecordNodeSpacing = math.clamp(math.floor((data.pathRecordNodeSpacing * 10) + 0.5) / 10, 1, 10)
    end
    if type(data.pathPlayerAvoiderEnabled) == "boolean" then
        S.pathPlayerAvoiderEnabled = data.pathPlayerAvoiderEnabled
    end
    if type(data.pathPlayerAvoiderDistance) == "number" then
        S.pathPlayerAvoiderDistance = math.clamp(math.floor(data.pathPlayerAvoiderDistance + 0.5), 1, 200)
    end
    if type(data.pathPlayerAvoiderShowCorridor) == "boolean" then
        S.pathPlayerAvoiderShowCorridor = data.pathPlayerAvoiderShowCorridor
    end
    if type(data.pathPlayerAvoiderMethod) == "string" then
        S.pathPlayerAvoiderMethod = normalizePathPlayerAvoiderMethod(data.pathPlayerAvoiderMethod)
    end
    if type(data.pathPlayerAvoiderAction) == "string" then
        S.pathPlayerAvoiderAction = normalizePathPlayerAvoiderAction(data.pathPlayerAvoiderAction)
    end
    if type(data.pathPlayerAvoiderRadius) == "number" then
        S.pathPlayerAvoiderRadius = math.clamp(math.floor(data.pathPlayerAvoiderRadius + 0.5), 5, 200)
    end
    if type(data.pathPlayerAvoiderShowRadius) == "boolean" then
        S.pathPlayerAvoiderShowRadius = data.pathPlayerAvoiderShowRadius
    end
    if type(data.pathHopOnDeathEnabled) == "boolean" then
        S.pathHopOnDeathEnabled = data.pathHopOnDeathEnabled
    end
    if type(data.discordAutomineWebhookTrackingEnabled) == "boolean" then
        S.discordAutomineWebhookTrackingEnabled = data.discordAutomineWebhookTrackingEnabled
    end
    if type(data.discordAutomineWebhookUrl) == "string" then
        S.discordAutomineWebhookUrl = data.discordAutomineWebhookUrl
    end
    if type(data.discordAutomineWebhookOptions) == "table" then
        S.discordAutomineWebhookOptions = cloneAutomineWebhookOptions(data.discordAutomineWebhookOptions)
    end
    if type(data.guiToggleKey) == "string" then
        local code = keyNameToCode(data.guiToggleKey)
        if code then
            guiToggleKeyCode = code
            guiToggleKeyName = data.guiToggleKey
        end
    end
    if type(data.panicKey) == "string" then
        local code = keyNameToCode(data.panicKey)
        if code then
            panicKeyCode = code
            panicKeyName = data.panicKey
        else
            panicKeyCode = nil
            panicKeyName = "none"
        end
    end
    S.keybinds = {}
    if type(data.keybinds) == "table" then
        for bindKey, keyName in pairs(data.keybinds) do
            if type(bindKey) == "string" and type(keyName) == "string" then
                local code = keyNameToCode(keyName)
                if code then
                    S.keybinds[bindKey] = code
                end
            end
        end
    end
    if not S.keybinds.dynamiteMark then
        S.keybinds.dynamiteMark = Enum.KeyCode.F
    end
    return true
end

local function resetStateToDefaults()
    S.notificationsEnabled = DEFAULTS.notificationsEnabled
    S.fpsPingStatsEnabled = DEFAULTS.fpsPingStatsEnabled
    S.guiTransparency = DEFAULTS.guiTransparency
    S.demoToggle = DEFAULTS.demoToggle
    S.demoSlider = DEFAULTS.demoSlider
    S.demoDropdown = DEFAULTS.demoDropdown
    S.demoInput = DEFAULTS.demoInput
    S.dynamiteTpEnabled = DEFAULTS.dynamiteTpEnabled
    S.dynamiteStyle = DEFAULTS.dynamiteStyle
    S.dynamiteTarget = DEFAULTS.dynamiteTarget
    S.dynamiteSpectateEnabled = DEFAULTS.dynamiteSpectateEnabled
    S.dynamiteMarkPlayersEnabled = DEFAULTS.dynamiteMarkPlayersEnabled
    S.dynamiteSelectedPlayer = DEFAULTS.dynamiteSelectedPlayer
    S.infiniteStaminaEnabled = DEFAULTS.infiniteStaminaEnabled
    S.safeTeleportEnabled = DEFAULTS.safeTeleportEnabled
    S.noJumpCooldownEnabled = DEFAULTS.noJumpCooldownEnabled
    S.noFallDamageEnabled = DEFAULTS.noFallDamageEnabled
    S.noRagdollEnabled = DEFAULTS.noRagdollEnabled
    S.autoBreakFreeEnabled = DEFAULTS.autoBreakFreeEnabled
    S.autoGetUpEnabled = DEFAULTS.autoGetUpEnabled
    S.equipWhileSwimmingEnabled = DEFAULTS.equipWhileSwimmingEnabled
    S.silentAimEnabled = DEFAULTS.silentAimEnabled
    S.silentAimBodyTarget = DEFAULTS.silentAimBodyTarget
    S.silentAimMode = DEFAULTS.silentAimMode
    S.silentAimVisibilityCheck = DEFAULTS.silentAimVisibilityCheck
    S.silentAimTargetPriority = cloneGunTargetPriority(DEFAULTS.silentAimTargetPriority)
    S.silentAimAccuracy = DEFAULTS.silentAimAccuracy
    S.silentAimDistanceLimit = DEFAULTS.silentAimDistanceLimit
    S.gunFovEnabled = DEFAULTS.gunFovEnabled
    S.gunFovShape = DEFAULTS.gunFovShape
    S.gunFovRadius = DEFAULTS.gunFovRadius
    S.gunSnaplineEnabled = DEFAULTS.gunSnaplineEnabled
    S.fastProjectileEnabled = DEFAULTS.fastProjectileEnabled
    S.wallBangEnabled = DEFAULTS.wallBangEnabled
    S.noSpreadEnabled = DEFAULTS.noSpreadEnabled
    S.noRecoilEnabled = DEFAULTS.noRecoilEnabled
    S.noFireCooldownEnabled = DEFAULTS.noFireCooldownEnabled
    S.fastReloadEnabled = DEFAULTS.fastReloadEnabled
    S.instantLassoChargeEnabled = DEFAULTS.instantLassoChargeEnabled
    S.lassoSilentAimEnabled = DEFAULTS.lassoSilentAimEnabled
    S.lassoSnaplineEnabled = DEFAULTS.lassoSnaplineEnabled
    S.lassoNoRopeLimitEnabled = DEFAULTS.lassoNoRopeLimitEnabled
    S.fasterLassoEnabled = DEFAULTS.fasterLassoEnabled
    S.playerEspEnabled = DEFAULTS.playerEspEnabled
    S.playerEspMethods = clonePlayerEspMethods(DEFAULTS.playerEspMethods)
    S.staffNotifyEnabled = DEFAULTS.staffNotifyEnabled
    S.staffKickEnabled = DEFAULTS.staffKickEnabled
    S.pathRecordNodeSpacing = DEFAULTS.pathRecordNodeSpacing
    S.pathPlayerAvoiderEnabled = DEFAULTS.pathPlayerAvoiderEnabled
    S.pathPlayerAvoiderDistance = DEFAULTS.pathPlayerAvoiderDistance
    S.pathPlayerAvoiderShowCorridor = DEFAULTS.pathPlayerAvoiderShowCorridor
    S.pathPlayerAvoiderMethod = DEFAULTS.pathPlayerAvoiderMethod
    S.pathPlayerAvoiderAction = DEFAULTS.pathPlayerAvoiderAction
    S.pathPlayerAvoiderRadius = DEFAULTS.pathPlayerAvoiderRadius
    S.pathPlayerAvoiderShowRadius = DEFAULTS.pathPlayerAvoiderShowRadius
    S.pathHopOnDeathEnabled = DEFAULTS.pathHopOnDeathEnabled
    S.discordAutomineWebhookTrackingEnabled = DEFAULTS.discordAutomineWebhookTrackingEnabled
    S.discordAutomineWebhookUrl = DEFAULTS.discordAutomineWebhookUrl
    S.discordAutomineWebhookOptions = cloneAutomineWebhookOptions(DEFAULTS.discordAutomineWebhookOptions)
    S.keybinds = {
        dynamiteMark = Enum.KeyCode.F,
    }
    guiToggleKeyCode = Enum.KeyCode.E
    guiToggleKeyName = "E"
    panicKeyCode = nil
    panicKeyName = "none"
end

function saveConfigState(name)
    if not storageEnabled then
        return false
    end
    local trimmedName = trimConfigName(name)
    if trimmedName == "" then
        return false
    end
    local ok = pcall(function()
        ensureConfigStorageFolder()
        writefile(getConfigFilePath(trimmedName), _HttpService:JSONEncode(serializeState()))
    end)
    return ok
end

function loadConfigState(name, resetBeforeApply)
    if not storageReadAvailable then
        return false
    end
    local trimmedName = trimConfigName(name)
    if trimmedName == "" then
        return false
    end
    local okRead, raw = pcall(function()
        local filePath = getConfigFilePath(trimmedName)
        if not isfile(filePath) then
            return nil
        end
        return readfile(filePath)
    end)
    if not okRead or type(raw) ~= "string" or raw == "" then
        return false
    end
    local okDecode, data = pcall(function()
        return _HttpService:JSONDecode(raw)
    end)
    if not okDecode or type(data) ~= "table" then
        return false
    end
    if resetBeforeApply ~= false then
        resetStateToDefaults()
    end
    return applySerializedState(data)
end

function listConfigs()
    local names = {}
    local seen = {}

    for _, savedName in ipairs(normalizeSavedConfigNames(CONFIG_SYSTEM.savedConfigs)) do
        if not seen[savedName] then
            seen[savedName] = true
            table.insert(names, savedName)
        end
    end

    if type(listfiles) == "function" and storageFolderApisAvailable then
        pcall(function()
            ensureConfigStorageFolder()
            for _, filePath in ipairs(listfiles(CONFIG_SYSTEM.FOLDER)) do
                local rawName = tostring(filePath):match("([^/\\]+)%.cfg$")
                if rawName and rawName ~= CONFIG_SYSTEM.AUTOSAVE_FILE_KEY then
                    local displayName = rawName:gsub("_+", "_")
                    if displayName ~= "" and not seen[displayName] then
                        seen[displayName] = true
                        table.insert(names, displayName)
                    end
                end
            end
        end)
    end

    table.sort(names, function(a, b)
        return a:lower() < b:lower()
    end)
    return names
end

local function savePersistentState()
    if not storageEnabled then
        return false
    end
    local payload = {
        selectedConfigName = getSelectedConfigName(),
        autoLoadConfigName = getAutoLoadConfigName(),
        savedConfigs = normalizeSavedConfigNames(CONFIG_SYSTEM.savedConfigs),
    }
    local ok = pcall(function()
        ensureStorageFolder()
        writefile(storagePath, _HttpService:JSONEncode(payload))
    end)
    return ok
end

local function loadPersistentState()
    CONFIG_SYSTEM.selectedConfigName = nil
    CONFIG_SYSTEM.autoLoadConfigName = nil
    CONFIG_SYSTEM.savedConfigs = {}
    CONFIG_SYSTEM.autosaveLoadedOnStartup = false
    CONFIG_SYSTEM.autoLoadLoadedOnStartup = false

    if not storageReadAvailable then
        return false
    end

    local ok, raw = pcall(function()
        if not isfile(storagePath) then
            return nil
        end
        return readfile(storagePath)
    end)
    if not ok or not raw or raw == "" then
        return false
    end

    local decodedOk, data = pcall(function()
        return _HttpService:JSONDecode(raw)
    end)
    if not decodedOk or type(data) ~= "table" then
        return false
    end

    CONFIG_SYSTEM.savedConfigs = normalizeSavedConfigNames(data.savedConfigs)
    if type(data.selectedConfigName) == "string" and trimConfigName(data.selectedConfigName) ~= "" then
        CONFIG_SYSTEM.selectedConfigName = trimConfigName(data.selectedConfigName)
    end
    if type(data.autoLoadConfigName) == "string" and trimConfigName(data.autoLoadConfigName) ~= "" then
        CONFIG_SYSTEM.autoLoadConfigName = trimConfigName(data.autoLoadConfigName)
    end

    local autoLoadName = getAutoLoadConfigName()
    if autoLoadName then
        CONFIG_SYSTEM.selectedConfigName = autoLoadName
        CONFIG_SYSTEM.autoLoadLoadedOnStartup = loadConfigState(autoLoadName, false) == true
    elseif getSelectedConfigName() == CONFIG_SYSTEM.AUTOSAVE_NAME then
        CONFIG_SYSTEM.autosaveLoadedOnStartup = loadConfigState(CONFIG_SYSTEM.AUTOSAVE_NAME, false) == true
    end

    return true
end

local function clearPersistentState()
    if storageDeleteAvailable and storageReadAvailable then
        local ok = pcall(function()
            if isfile(storagePath) then
                delfile(storagePath)
            end
        end)
        return ok
    end
    return savePersistentState()
end

local pendingPersist = false
local function schedulePersist()
    if pendingPersist then
        return
    end
    pendingPersist = true
    task.delay(0.2, function()
        pendingPersist = false
        if not destroyed and isSessionActive() then
            storageLoaded = savePersistentState() or storageLoaded
            if getSelectedConfigName() == CONFIG_SYSTEM.AUTOSAVE_NAME then
                saveConfigState(CONFIG_SYSTEM.AUTOSAVE_NAME)
            end
        end
    end)
end

storageLoaded = loadPersistentState()

local overlayRoot = Instance.new("Frame")
overlayRoot.Name = "OverlayRoot"
overlayRoot.Size = UDim2.new(1, 0, 1, 0)
overlayRoot.BackgroundTransparency = 1
overlayRoot.BorderSizePixel = 0
overlayRoot.ZIndex = 100
overlayRoot.Parent = hubGui

local hubWrapper = Instance.new("Frame")
hubWrapper.Name = "HubWrapper"
hubWrapper.Size = UDim2.new(0, 600, 0, 380)
hubWrapper.Position = UDim2.new(0.5, 0, 0.5, 0)
hubWrapper.AnchorPoint = Vector2.new(0.5, 0.5)
hubWrapper.BackgroundTransparency = 1
hubWrapper.BorderSizePixel = 0
hubWrapper.Visible = false
hubWrapper.Parent = hubGui

local hubMain = Instance.new("CanvasGroup")
hubMain.Name = "HubMain"
hubMain.Size = UDim2.new(1, 0, 1, 0)
hubMain.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
hubMain.BorderSizePixel = 0
hubMain.ClipsDescendants = true
hubMain.GroupTransparency = S.guiTransparency / 100
hubMain.Parent = hubWrapper
Instance.new("UICorner", hubMain).CornerRadius = UDim.new(0, 12)

local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0, 140, 1, 0)
sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
sidebar.BorderSizePixel = 0
sidebar.ZIndex = 2
sidebar.Parent = hubMain

local sidebarScroll = Instance.new("ScrollingFrame")
sidebarScroll.Size = UDim2.new(1, 0, 1, -50)
sidebarScroll.Position = UDim2.new(0, 0, 0, 50)
sidebarScroll.BackgroundTransparency = 1
sidebarScroll.BorderSizePixel = 0
sidebarScroll.ScrollBarThickness = 3
sidebarScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
sidebarScroll.CanvasSize = UDim2.new(0, 0, 0, 250)
sidebarScroll.ZIndex = 2
sidebarScroll.Parent = sidebar

local hubLogo = Instance.new("TextButton")
hubLogo.Name = "HomeTabBtn"
hubLogo.Size = UDim2.new(1, 0, 0, 40)
hubLogo.Position = UDim2.new(0, 0, 0, 10)
hubLogo.BackgroundTransparency = 1
hubLogo.Text = ""
hubLogo.AutoButtonColor = false
hubLogo.Parent = sidebar

local homeIcon = Instance.new("ImageLabel")
homeIcon.Size = UDim2.new(0, 16, 0, 16)
homeIcon.Position = UDim2.new(0, 15, 0.5, -8)
homeIcon.BackgroundTransparency = 1
homeIcon.Image = "rbxthumb://type=Asset&id=11347112419&w=420&h=420"
homeIcon.ImageColor3 = Color3.fromRGB(0, 150, 255)
homeIcon.Parent = hubLogo

local homeLblText = Instance.new("TextLabel")
homeLblText.Size = UDim2.new(1, -40, 1, 0)
homeLblText.Position = UDim2.new(0, 38, 0, 0)
homeLblText.BackgroundTransparency = 1
homeLblText.TextColor3 = Color3.fromRGB(0, 150, 255)
homeLblText.Text = "Home"
homeLblText.Font = Enum.Font.GothamBold
homeLblText.TextSize = 13
homeLblText.TextXAlignment = Enum.TextXAlignment.Left
homeLblText.Parent = hubLogo

local sidebarActiveHighlight = Instance.new("Frame")
sidebarActiveHighlight.Name = "ActiveTabHighlight"
sidebarActiveHighlight.Size = UDim2.new(1, 0, 0, 35)
sidebarActiveHighlight.Position = UDim2.new(0, 0, 0, 10)
sidebarActiveHighlight.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
sidebarActiveHighlight.BorderSizePixel = 0
sidebarActiveHighlight.Visible = false
sidebarActiveHighlight.ZIndex = 1
sidebarActiveHighlight.Parent = sidebarScroll

local tabButtons = {}
local tabContents = {}
local tabTitles = {}
local activeTab = nil

local _TAB_ICON_BLUE = Color3.fromRGB(0, 150, 255)
local _TAB_SLIDE_TWEEN = TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

local _TAB_ICON_MAP = {
    AUTOMINE = {asset = "https://create.roblox.com/store/asset/78007610361363/Robot-icon", size = 17},
    WEAPONS = {asset = "https://create.roblox.com/store/asset/130295580/BlazingSword", size = 16},
    PLAYER = {asset = "https://create.roblox.com/store/asset/11984980825/User-White-Icon", size = 16},
    VISUALS = {asset = "https://create.roblox.com/store/asset/6523858422/Eye-Icon", size = 17},
    GAME = {asset = "https://create.roblox.com/store/asset/11887653913/World-Icon", size = 17},
    SETTINGS = {asset = "https://create.roblox.com/store/asset/6720824672/SettingIconWhite", size = 17},
    KEYBINDS = {asset = "https://create.roblox.com/store/asset/129163184749578/edit", size = 17},
}

local function _extractTabIconAssetId(value)
    if type(value) ~= "string" or value == "" then
        return nil
    end
    return value:match("^rbxassetid://(%d+)$") or value:match("asset/(%d+)") or value:match("(%d+)")
end

local function _normalizeTabIconAsset(value)
    local id = _extractTabIconAssetId(value)
    if not id then
        return nil
    end
    return string.format("rbxthumb://type=Asset&id=%s&w=150&h=150", id)
end

local function createTabButton(name, yPos)
    local tabBtnFrame = Instance.new("TextButton")
    tabBtnFrame.Size = UDim2.new(1, 0, 0, 35)
    tabBtnFrame.Position = UDim2.new(0, 0, 0, yPos)
    tabBtnFrame.BackgroundTransparency = 1
    tabBtnFrame.BorderSizePixel = 0
    tabBtnFrame.Text = ""
    tabBtnFrame.AutoButtonColor = false
    tabBtnFrame.ZIndex = 2
    tabBtnFrame.Parent = sidebarScroll
    _UTILS._applySoundsToButton(tabBtnFrame)

    local iconConfig = _TAB_ICON_MAP[name]
    local tabIconAsset = _normalizeTabIconAsset(iconConfig and iconConfig.asset or iconConfig)
    local indicator
    if tabIconAsset then
        local iconHolder = Instance.new("Frame")
        iconHolder.Size = UDim2.new(0, 18, 0, 18)
        iconHolder.Position = UDim2.new(0, 12, 0.5, -9)
        iconHolder.BackgroundTransparency = 1
        iconHolder.ZIndex = 3
        iconHolder.Parent = tabBtnFrame

        local icon = Instance.new("ImageLabel")
        local iconSize = iconConfig and iconConfig.size or 16
        icon.Size = UDim2.new(0, iconSize, 0, iconSize)
        icon.AnchorPoint = Vector2.new(0.5, 0.5)
        icon.Position = UDim2.new(0.5, 0, 0.5, 0)
        icon.BackgroundTransparency = 1
        icon.Image = tabIconAsset
        icon.ImageColor3 = _TAB_ICON_BLUE
        icon.ScaleType = Enum.ScaleType.Fit
        icon.ZIndex = 3
        icon.Parent = iconHolder
        indicator = icon
    else
        local icon = Instance.new("Frame")
        icon.Size = UDim2.new(0, 12, 0, 12)
        icon.Position = UDim2.new(0, 15, 0.5, -6)
        icon.BackgroundColor3 = _TAB_ICON_BLUE
        icon.BorderSizePixel = 0
        icon.ZIndex = 3
        icon.Parent = tabBtnFrame
        indicator = icon
    end

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -35, 1, 0)
    label.Position = UDim2.new(0, 35, 0, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(190, 190, 190)
    label.Text = name
    label.Font = Enum.Font.GothamMedium
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 3
    label.Parent = tabBtnFrame

    return tabBtnFrame, indicator, label
end

local function createContentArea()
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, -140, 1, 0)
    content.Position = UDim2.new(0, 140, 0, 0)
    content.BackgroundTransparency = 1
    content.ClipsDescendants = true
    content.ZIndex = 25
    content.Visible = false
    content.Parent = hubMain
    return content
end

local function createTabBackground(parentContentArea)
    local bgContainer = Instance.new("Frame")
    bgContainer.Size = UDim2.new(1, 0, 1, 0)
    bgContainer.BackgroundTransparency = 1
    bgContainer.BorderSizePixel = 0
    bgContainer.ClipsDescendants = true
    bgContainer.Parent = parentContentArea

    local glow = Instance.new("Frame")
    glow.Size = UDim2.new(1, 0, 0, 60)
    glow.BackgroundTransparency = 0.6
    glow.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
    glow.BorderSizePixel = 0
    glow.Parent = bgContainer

    local grad = Instance.new("UIGradient")
    grad.Rotation = 90
    grad.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.6),
        NumberSequenceKeypoint.new(1, 1),
    })
    grad.Parent = glow

    local wm = Instance.new("Frame")
    wm.Size = UDim2.new(0, 400, 0, 400)
    wm.Position = UDim2.new(1, -100, 1, -50)
    wm.AnchorPoint = Vector2.new(0.5, 0.5)
    wm.Rotation = 45
    wm.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    wm.BorderSizePixel = 0
    wm.Parent = bgContainer

    local im = Instance.new("Frame")
    im.Size = UDim2.new(0, 370, 0, 370)
    im.Position = UDim2.new(0.5, 0, 0.5, 0)
    im.AnchorPoint = Vector2.new(0.5, 0.5)
    im.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    im.BorderSizePixel = 2
    im.BorderColor3 = Color3.fromRGB(24, 24, 24)
    im.Parent = wm

    local dotGrid = Instance.new("Frame")
    dotGrid.Size = UDim2.new(1, 0, 1, 0)
    dotGrid.BackgroundTransparency = 1
    dotGrid.Parent = bgContainer

    for x = 1, 6 do
        for y = 1, 5 do
            local dot = Instance.new("Frame")
            dot.Size = UDim2.new(0, 2, 0, 2)
            dot.Position = UDim2.new(x / 7, 0, y / 6, 0)
            dot.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
            dot.BorderSizePixel = 0
            dot.Parent = dotGrid
        end
    end
end

local function createTabLayout(parentContentArea)
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -40, 0, 50)
    title.Position = UDim2.new(0, 25, 0, 15)
    title.BackgroundTransparency = 1
    title.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    title.Text = ""
    title.Font = Enum.Font.GothamBold
    title.TextSize = 24
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.ZIndex = 50
    title.Parent = parentContentArea

    local clipWrapper = Instance.new("CanvasGroup")
    clipWrapper.Size = UDim2.new(1, -50, 1, -80)
    clipWrapper.Position = UDim2.new(0, 25, 0, 65)
    clipWrapper.BackgroundTransparency = 1
    clipWrapper.BorderSizePixel = 0
    clipWrapper.Parent = parentContentArea

    local scrollList = Instance.new("ScrollingFrame")
    scrollList.Size = UDim2.new(1, 0, 1, 0)
    scrollList.BackgroundTransparency = 1
    scrollList.BorderSizePixel = 0
    scrollList.ScrollBarThickness = 4
    scrollList.ClipsDescendants = true
    scrollList.AutomaticCanvasSize = Enum.AutomaticSize.Y
    scrollList.Parent = clipWrapper

    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 8)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Parent = scrollList

    return title, scrollList
end

local function switchTab(tabName)
    if activeTab == tabName then
        return
    end
    activeTab = tabName
    for name, content in pairs(tabContents) do
        content.Visible = (name == tabName)
    end
    local activeData = tabButtons[tabName]
    if activeData and tabName ~= "Home" then
        if not sidebarActiveHighlight.Visible then
            sidebarActiveHighlight.Position = activeData.btn.Position
            sidebarActiveHighlight.Size = activeData.btn.Size
            sidebarActiveHighlight.Visible = true
        else
            _TweenService:Create(sidebarActiveHighlight, _TAB_SLIDE_TWEEN, {
                Position = activeData.btn.Position,
                Size = activeData.btn.Size,
            }):Play()
        end
    else
        sidebarActiveHighlight.Visible = false
    end
    for name, data in pairs(tabButtons) do
        local isActive = (name == tabName)
        if data.indicator:IsA("ImageLabel") then
            _TweenService:Create(data.indicator, TweenInfo.new(0.2), {
                ImageColor3 = isActive and Color3.new(1, 1, 1) or _TAB_ICON_BLUE,
            }):Play()
        else
            _TweenService:Create(data.indicator, TweenInfo.new(0.2), {
                BackgroundColor3 = isActive and Color3.new(1, 1, 1) or _TAB_ICON_BLUE,
            }):Play()
        end
        _TweenService:Create(data.label, TweenInfo.new(0.2), {
            TextColor3 = isActive and Color3.new(1, 1, 1) or Color3.new(0.8, 0.8, 0.8),
        }):Play()
    end
end

local function animateCurrentTitle(tabName)
    local titleTextMap = {
        Home = "Dashboard",
        AUTOMINE = "Automine",
        WEAPONS = "Weapons",
        PLAYER = "Player",
        VISUALS = "Visuals",
        GAME = "Game",
        SETTINGS = "Settings",
        KEYBINDS = "Keybinds",
    }
    local title = tabTitles[tabName]
    if title then
        task.spawn(function()
            _UTILS._animateTabTitle(title, titleTextMap[tabName] or tabName)
        end)
    end
end

local function createRow(name)
    local rowBtn = Instance.new("TextButton")
    rowBtn.Name = name
    rowBtn.Size = UDim2.new(1, 0, 0, 38)
    rowBtn.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
    rowBtn.BorderSizePixel = 0
    rowBtn.Text = ""
    rowBtn.AutoButtonColor = false
    _UTILS._applySoundsToButton(rowBtn)
    _rowLayoutCounter = _rowLayoutCounter + 1
    rowBtn.LayoutOrder = _rowLayoutCounter

    local diamond = Instance.new("Frame")
    diamond.Size = UDim2.new(0, 6, 0, 6)
    diamond.Position = UDim2.new(0, 10, 0.5, 0)
    diamond.AnchorPoint = Vector2.new(0.5, 0.5)
    diamond.Rotation = 45
    diamond.BackgroundColor3 = Color3.new(1, 1, 1)
    diamond.BorderSizePixel = 0
    diamond.Visible = false
    diamond.Parent = rowBtn

    local icon = Instance.new("Frame")
    icon.Size = UDim2.new(0, 14, 0, 14)
    icon.Position = UDim2.new(0, 24, 0.5, -7)
    icon.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    icon.BorderSizePixel = 0
    icon.Parent = rowBtn
    Instance.new("UICorner", icon).CornerRadius = UDim.new(0, 2)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0, 180, 1, 0)
    label.Position = UDim2.new(0, 50, 0, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.new(0.8, 0.8, 0.8)
    label.Text = name
    label.Font = Enum.Font.GothamMedium
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextTruncate = Enum.TextTruncate.AtEnd
    label.Parent = rowBtn

    local topLine = Instance.new("Frame")
    topLine.Size = UDim2.new(0, 0, 0, 2)
    topLine.BackgroundColor3 = Color3.new(1, 1, 1)
    topLine.BorderSizePixel = 0
    topLine.Parent = rowBtn

    local bottomLine = Instance.new("Frame")
    bottomLine.Size = UDim2.new(0, 0, 0, 2)
    bottomLine.Position = UDim2.new(0, 0, 1, -2)
    bottomLine.BackgroundColor3 = Color3.new(1, 1, 1)
    bottomLine.BorderSizePixel = 0
    bottomLine.Parent = rowBtn

    return rowBtn, icon, label, topLine, bottomLine, diamond
end

local function applyHoverState(btn, icon, label, topLine, bottomLine, diamond, isToggle, getToggleState)
    local function updateVisuals(hovered)
        local toggled = isToggle and getToggleState and getToggleState()
        local active = hovered or toggled
        _TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = active and Color3.fromRGB(35, 35, 35) or Color3.fromRGB(24, 24, 24),
        }):Play()
        _TweenService:Create(icon, TweenInfo.new(0.2), {
            BackgroundColor3 = active and Color3.new(1, 1, 1) or Color3.fromRGB(0, 150, 255),
        }):Play()
        _TweenService:Create(label, TweenInfo.new(0.2), {
            TextColor3 = active and Color3.new(1, 1, 1) or Color3.new(0.8, 0.8, 0.8),
        }):Play()
        local targetLineSize = active and UDim2.new(1, 0, 0, 2) or UDim2.new(0, 0, 0, 2)
        local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
        _TweenService:Create(topLine, tweenInfo, {Size = targetLineSize}):Play()
        _TweenService:Create(bottomLine, tweenInfo, {Size = targetLineSize}):Play()
        diamond.Visible = active
    end

    btn.MouseEnter:Connect(function()
        if destroyed then
            return
        end
        updateVisuals(true)
    end)
    btn.MouseLeave:Connect(function()
        if destroyed then
            return
        end
        updateVisuals(false)
    end)
    btn.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        task.delay(0.05, function()
            if not destroyed then
                updateVisuals(true)
            end
        end)
    end)

    return updateVisuals
end

local function registerButtonEntry(entry)
    table.insert(_buttonRegistry, entry)
end

local function createButtonRow(name, parentList, onClickFn, opts)
    opts = opts or {}
    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    local updateVis = applyHoverState(row, icon, label, topLine, bottomLine, diamond, false)

    if opts.iconColor then
        icon.BackgroundColor3 = opts.iconColor
    end
    if opts.labelColor then
        label.TextColor3 = opts.labelColor
    end

    row.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(row)
        if onClickFn then
            onClickFn()
        end
        updateVis(true)
    end)

    if not opts.skipRegistry and _currentRegistryTab then
        registerButtonEntry({
            name = name,
            tab = _currentRegistryTab,
            type = "button",
            onClickFn = onClickFn,
        })
    end

    return row, updateVis, label, icon, diamond, topLine, bottomLine
end

local function createToggleRow(name, parentList, getStateFn, onClickFn)
    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    local updateVis = applyHoverState(row, icon, label, topLine, bottomLine, diamond, true, getStateFn)
    local toggleAnimating = false
    local toggleAnimToken = 0

    local textContainer = Instance.new("Frame")
    textContainer.Size = UDim2.new(0, 50, 1, 0)
    textContainer.Position = UDim2.new(1, -65, 0, 0)
    textContainer.BackgroundTransparency = 1
    textContainer.ClipsDescendants = true
    textContainer.Parent = row

    local offLabel = Instance.new("TextLabel")
    offLabel.Size = UDim2.new(1, 0, 1, 0)
    offLabel.BackgroundTransparency = 1
    offLabel.TextColor3 = Color3.fromRGB(120, 120, 120)
    offLabel.Text = "OFF"
    offLabel.Font = Enum.Font.GothamMedium
    offLabel.TextSize = 13
    offLabel.TextXAlignment = Enum.TextXAlignment.Right
    offLabel.Parent = textContainer

    local onLabel = Instance.new("TextLabel")
    onLabel.Size = UDim2.new(1, 0, 1, 0)
    onLabel.Position = UDim2.new(1, 0, 0, 0)
    onLabel.BackgroundTransparency = 1
    onLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
    onLabel.Text = "ON"
    onLabel.Font = Enum.Font.GothamMedium
    onLabel.TextSize = 13
    onLabel.TextXAlignment = Enum.TextXAlignment.Right
    onLabel.Parent = textContainer

    local function syncLabels(forceInstant)
        if toggleAnimating and not forceInstant then
            updateVis(false)
            return
        end
        if getStateFn() then
            offLabel.Position = UDim2.new(-1, 0, 0, 0)
            onLabel.Position = UDim2.new(0, 0, 0, 0)
        else
            offLabel.Position = UDim2.new(0, 0, 0, 0)
            onLabel.Position = UDim2.new(1, 0, 0, 0)
        end
        updateVis(false)
    end

    syncLabels()
    registerUiSync(_allToggleSyncs, syncLabels)

    if _currentRegistryTab then
        registerButtonEntry({
            name = name,
            tab = _currentRegistryTab,
            type = "toggle",
            getStateFn = getStateFn,
            onClickFn = onClickFn,
        })
    end

    row.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(row)
        onClickFn()
        toggleAnimating = true
        toggleAnimToken = toggleAnimToken + 1
        local myAnimToken = toggleAnimToken
        local slideInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
        if getStateFn() then
            _TweenService:Create(offLabel, slideInfo, {Position = UDim2.new(-1, 0, 0, 0)}):Play()
            _TweenService:Create(onLabel, slideInfo, {Position = UDim2.new(0, 0, 0, 0)}):Play()
        else
            _TweenService:Create(offLabel, slideInfo, {Position = UDim2.new(0, 0, 0, 0)}):Play()
            _TweenService:Create(onLabel, slideInfo, {Position = UDim2.new(1, 0, 0, 0)}):Play()
        end
        updateVis(true)
        task.defer(function()
            pcall(syncAllUi)
        end)
        task.delay(0.31, function()
            if destroyed or myAnimToken ~= toggleAnimToken then
                return
            end
            toggleAnimating = false
            syncLabels(true)
        end)
    end)

    return row, updateVis, syncLabels, label, offLabel, onLabel
end

local function createSliderRow(name, parentList, initialValue, onValueChanged, getStateFn, minValue, maxValue, opts)
    local smin = minValue or 0
    local smax = maxValue or 100
    local span = math.max(1, smax - smin)
    opts = opts or {}
    local decimals = math.max(0, math.floor(tonumber(opts.decimals) or 0))
    local step = tonumber(opts.step)
    if not step or step <= 0 then
        step = decimals > 0 and (10 ^ (-decimals)) or 1
    end
    local textBoxWidth = math.max(30, math.floor(tonumber(opts.textBoxWidth) or (decimals > 0 and 42 or 30)))
    local trackWidth = math.max(72, math.floor(tonumber(opts.trackWidth) or 158))
    local trackRightInset = math.max(trackWidth + 8, math.floor(tonumber(opts.trackRightInset) or 170))
    local labelWidth = math.max(72, math.floor(tonumber(opts.labelWidth) or math.max(96, 140 - math.max(0, textBoxWidth - 30))))

    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    icon.Visible = false
    label.Position = UDim2.new(0, 20 + textBoxWidth + 8, 0, 0)
    label.Size = UDim2.new(0, labelWidth, 1, 0)

    local textBox = Instance.new("TextBox")
    textBox.Size = UDim2.new(0, textBoxWidth, 0, 22)
    textBox.Position = UDim2.new(0, 20, 0.5, -11)
    textBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    textBox.BorderSizePixel = 1
    textBox.BorderColor3 = Color3.fromRGB(45, 45, 45)
    textBox.TextColor3 = Color3.new(1, 1, 1)
    textBox.Font = Enum.Font.GothamMedium
    textBox.TextSize = 12
    textBox.ClearTextOnFocus = false
    textBox.Parent = row
    Instance.new("UICorner", textBox).CornerRadius = UDim.new(0, 4)

    local track = Instance.new("Frame")
    track.Size = UDim2.new(0, trackWidth, 0, 14)
    track.Position = UDim2.new(1, -trackRightInset, 0.5, -7)
    track.BackgroundTransparency = 1
    track.Parent = row

    local trackLayout = Instance.new("UIListLayout")
    trackLayout.FillDirection = Enum.FillDirection.Horizontal
    trackLayout.VerticalAlignment = Enum.VerticalAlignment.Center
    trackLayout.SortOrder = Enum.SortOrder.LayoutOrder
    trackLayout.Padding = UDim.new(0, 2)
    trackLayout.Parent = track

    local tickCount = 40
    local tickFrames = {}
    for i = 1, tickCount do
        local tickFrame = Instance.new("Frame")
        tickFrame.LayoutOrder = i
        tickFrame.Size = UDim2.new(0, 2, 0, 2)
        tickFrame.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        tickFrame.BorderSizePixel = 0
        tickFrame.Parent = track
        tickFrames[i] = tickFrame
    end

    local currentVal = nil
    local function formatSliderValue(val)
        if decimals > 0 then
            return string.format("%." .. tostring(decimals) .. "f", val)
        end
        return tostring(math.floor(val + 0.5))
    end

    local function normalizeSliderValue(val)
        val = tonumber(val) or smin
        val = math.floor((val / step) + 0.5) * step
        val = math.clamp(val, smin, smax)
        if decimals > 0 then
            local formatted = tonumber(string.format("%." .. tostring(decimals) .. "f", val))
            if formatted then
                val = formatted
            end
        else
            val = math.floor(val + 0.5)
        end
        return val
    end

    local function updateVal(val, suppressCallback)
        val = normalizeSliderValue(val)
        if currentVal == val then
            textBox.Text = formatSliderValue(val)
            return
        end
        currentVal = val
        textBox.Text = formatSliderValue(val)
        local pct = (val - smin) / span
        local activeCount = math.floor(pct * (tickCount - 1)) + 1
        for i = 1, tickCount do
            local tickSize
            local tickColor
            if i < activeCount then
                tickSize = UDim2.new(0, 2, 0, 8)
                tickColor = Color3.new(0.8, 0.8, 0.8)
            elseif i == activeCount then
                tickSize = UDim2.new(0, 2, 0, 14)
                tickColor = Color3.new(1, 1, 1)
            else
                tickSize = UDim2.new(0, 2, 0, 2)
                tickColor = Color3.fromRGB(60, 60, 60)
            end
            _TweenService:Create(tickFrames[i], TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Size = tickSize,
                BackgroundColor3 = tickColor,
            }):Play()
        end
        if onValueChanged and not suppressCallback then
            onValueChanged(val)
        end
    end

    updateVal(initialValue or smin, true)

    textBox.FocusLost:Connect(function()
        if destroyed then
            return
        end
        local num = tonumber(textBox.Text)
        if num then
            updateVal(num, false)
        else
            textBox.Text = formatSliderValue(currentVal or smin)
        end
    end)

    local interact = Instance.new("TextButton")
    interact.Size = UDim2.new(1, -70, 1, 0)
    interact.Position = UDim2.new(0, 70, 0, 0)
    interact.BackgroundTransparency = 1
    interact.Text = ""
    interact.Parent = row

    local isDragging = false
    local function updateFromMouse(input)
        local trackX = track.AbsolutePosition.X
        local trackW = track.AbsoluteSize.X
        local pct = math.clamp((input.Position.X - trackX) / trackW, 0, 1)
        updateVal(smin + pct * span, false)
    end

    interact.InputBegan:Connect(function(input)
        if destroyed then
            return
        end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = true
            updateFromMouse(input)
        end
    end)

    _UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = false
        end
    end)

    _UserInputService.InputChanged:Connect(function(input)
        if destroyed then
            return
        end
        if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateFromMouse(input)
        end
    end)

    row.MouseEnter:Connect(function()
        if destroyed then
            return
        end
        _TweenService:Create(row, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 35, 35)}):Play()
        _TweenService:Create(label, TweenInfo.new(0.2), {TextColor3 = Color3.new(1, 1, 1)}):Play()
        local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
        _TweenService:Create(topLine, tweenInfo, {Size = UDim2.new(1, 0, 0, 2)}):Play()
        _TweenService:Create(bottomLine, tweenInfo, {Size = UDim2.new(1, 0, 0, 2)}):Play()
        diamond.Visible = true
    end)

    row.MouseLeave:Connect(function()
        if destroyed then
            return
        end
        _TweenService:Create(row, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(24, 24, 24)}):Play()
        _TweenService:Create(label, TweenInfo.new(0.2), {TextColor3 = Color3.new(0.8, 0.8, 0.8)}):Play()
        local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
        _TweenService:Create(topLine, tweenInfo, {Size = UDim2.new(0, 0, 0, 2)}):Play()
        _TweenService:Create(bottomLine, tweenInfo, {Size = UDim2.new(0, 0, 0, 2)}):Play()
        diamond.Visible = false
    end)

    registerUiSync(_allSliderSyncs, function()
        local value = getStateFn and getStateFn() or currentVal or initialValue or smin
        updateVal(value, true)
    end)

    if _currentRegistryTab then
        registerButtonEntry({
            name = name,
            tab = _currentRegistryTab,
            type = "slider",
            defaultVal = initialValue,
        })
    end

    return row, updateVal, function()
        return currentVal
    end, label, textBox
end

local function hideAllDropdowns()
    for _, frame in ipairs(_dropdownFrames) do
        frame.Visible = false
    end
    for _, dropGui in ipairs(_allDropdownGuis) do
        if dropGui then
            dropGui.Enabled = false
        end
    end
end

local function createDraggableDropdown(titleText)
    local dropGui = Instance.new("ScreenGui")
    dropGui.Name = "DropdownGui_" .. titleText
    dropGui.ResetOnSpawn = false
    dropGui.IgnoreGuiInset = true
    dropGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    dropGui.DisplayOrder = (hubGui.DisplayOrder or 50) + 20
    dropGui.Enabled = false
    local ok = pcall(function()
        dropGui.Parent = hubGui.Parent or _CoreGui
    end)
    if not ok and localPlayer then
        dropGui.Parent = localPlayer:WaitForChild("PlayerGui")
    end

    local dropFrame = Instance.new("Frame")
    dropFrame.Size = UDim2.new(0, 240, 0, 220)
    dropFrame.Position = UDim2.new(0.5, -120, 0.5, -110)
    dropFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
    dropFrame.BorderSizePixel = 1
    dropFrame.BorderColor3 = Color3.fromRGB(60, 60, 60)
    dropFrame.ZIndex = 100
    dropFrame.ClipsDescendants = true
    dropFrame.Parent = dropGui
    Instance.new("UICorner", dropFrame).CornerRadius = UDim.new(0, 4)

    local dropHeader = Instance.new("Frame")
    dropHeader.Size = UDim2.new(1, 0, 0, 26)
    dropHeader.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    dropHeader.BorderSizePixel = 0
    dropHeader.ZIndex = 101
    dropHeader.Parent = dropFrame
    Instance.new("UICorner", dropHeader).CornerRadius = UDim.new(0, 4)

    local dropHeaderBlock = Instance.new("Frame")
    dropHeaderBlock.Size = UDim2.new(1, 0, 0, 4)
    dropHeaderBlock.Position = UDim2.new(0, 0, 1, -4)
    dropHeaderBlock.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    dropHeaderBlock.BorderSizePixel = 0
    dropHeaderBlock.ZIndex = 101
    dropHeaderBlock.Parent = dropHeader

    local dropTitle = Instance.new("TextLabel")
    dropTitle.Size = UDim2.new(1, -30, 1, 0)
    dropTitle.Position = UDim2.new(0, 10, 0, 0)
    dropTitle.BackgroundTransparency = 1
    dropTitle.TextColor3 = Color3.new(1, 1, 1)
    dropTitle.Text = titleText
    dropTitle.Font = Enum.Font.GothamMedium
    dropTitle.TextSize = 12
    dropTitle.TextXAlignment = Enum.TextXAlignment.Left
    dropTitle.ZIndex = 102
    dropTitle.Parent = dropHeader

    local closeDropBtn = Instance.new("TextButton")
    closeDropBtn.Size = UDim2.new(0, 26, 0, 26)
    closeDropBtn.Position = UDim2.new(1, -26, 0, 0)
    closeDropBtn.BackgroundTransparency = 1
    closeDropBtn.TextColor3 = Color3.new(0.6, 0.6, 0.6)
    closeDropBtn.Text = "\226\156\149"
    closeDropBtn.Font = Enum.Font.GothamMedium
    closeDropBtn.TextSize = 12
    closeDropBtn.ZIndex = 102
    closeDropBtn.Parent = dropHeader
    _UTILS._applySoundsToButton(closeDropBtn)

    local searchBox = Instance.new("TextBox")
    searchBox.Size = UDim2.new(1, -16, 0, 24)
    searchBox.Position = UDim2.new(0, 8, 0, 30)
    searchBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    searchBox.BorderSizePixel = 1
    searchBox.BorderColor3 = Color3.fromRGB(50, 50, 50)
    searchBox.TextColor3 = Color3.new(1, 1, 1)
    searchBox.PlaceholderText = "Search..."
    searchBox.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
    searchBox.Text = ""
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 12
    searchBox.TextXAlignment = Enum.TextXAlignment.Left
    searchBox.ClearTextOnFocus = false
    searchBox.ZIndex = 101
    searchBox.Parent = dropFrame
    Instance.new("UICorner", searchBox).CornerRadius = UDim.new(0, 4)

    local dropList = Instance.new("ScrollingFrame")
    dropList.Size = UDim2.new(1, 0, 1, -60)
    dropList.Position = UDim2.new(0, 0, 0, 58)
    dropList.BackgroundTransparency = 1
    dropList.BorderSizePixel = 0
    dropList.ScrollBarThickness = 2
    dropList.AutomaticCanvasSize = Enum.AutomaticSize.Y
    dropList.ClipsDescendants = true
    dropList.ZIndex = 101
    dropList.Parent = dropFrame

    local dropLayout = Instance.new("UIListLayout")
    dropLayout.SortOrder = Enum.SortOrder.Name
    dropLayout.Parent = dropList

    local function normalizeDropdownSearchText(text)
        local normalized = tostring(text or ""):lower()
        normalized = normalized:gsub("^%s+", "")
        normalized = normalized:gsub("^%[x%]%s*", "")
        normalized = normalized:gsub("^\226\156\147%s*", "")
        return normalized
    end

    local function dropdownTextMatchesQuery(haystackText, queryText)
        local normalizedHaystack = normalizeDropdownSearchText(haystackText)
        if normalizedHaystack == "" then
            return false
        end
        if normalizedHaystack:find(queryText, 1, true) then
            return true
        end
        local compactHaystack = normalizedHaystack:gsub("%s+", "")
        local compactQuery = tostring(queryText or ""):gsub("%s+", "")
        return compactQuery ~= "" and compactHaystack:find(compactQuery, 1, true) ~= nil
    end

    searchBox:GetPropertyChangedSignal("Text"):Connect(function()
        local query = normalizeDropdownSearchText(searchBox.Text)
        for _, child in pairs(dropList:GetChildren()) do
            if child:IsA("TextButton") then
                if query == "" then
                    child.Visible = true
                else
                    local found = false
                    local haystacks = {
                        child:GetAttribute("SearchText"),
                        normalizeDropdownSearchText(child.Name),
                        normalizeDropdownSearchText(child.Text),
                    }
                    for _, haystack in ipairs(haystacks) do
                        if dropdownTextMatchesQuery(haystack, query) then
                            found = true
                            break
                        end
                    end
                    if not found then
                        for _, desc in pairs(child:GetChildren()) do
                            if desc:IsA("TextLabel") and dropdownTextMatchesQuery(desc.Text, query) then
                                found = true
                                break
                            end
                        end
                    end
                    child.Visible = found
                end
            end
        end
    end)

    local ddDragging = false
    local ddDragInput, ddDragStart, ddStartPos
    local ddTargetRot, ddCurrentRot, ddLastMouseX = 0, 0, nil

    dropFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            ddDragging = true
            ddDragStart = input.Position
            ddStartPos = dropFrame.Position
        end
    end)

    _UserInputService.InputChanged:Connect(function(input)
        if (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) and ddDragging then
            ddDragInput = input
        end
    end)

    _UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            ddDragging = false
        end
    end)

    _RunService.RenderStepped:Connect(function(dt)
        if dropGui.Enabled then
            if ddDragging and ddDragInput then
                local delta = ddDragInput.Position - ddDragStart
                local dest = UDim2.new(ddStartPos.X.Scale, ddStartPos.X.Offset + delta.X, ddStartPos.Y.Scale, ddStartPos.Y.Offset + delta.Y)
                dropFrame.Position = dropFrame.Position:Lerp(dest, math.min(dt * 20, 1))
                if ddLastMouseX then
                    ddTargetRot = math.clamp(-(ddDragInput.Position.X - ddLastMouseX) * 0.7, -15, 15)
                end
                ddLastMouseX = ddDragInput.Position.X
            else
                ddLastMouseX = nil
                ddTargetRot = 0
            end
            ddCurrentRot = ddCurrentRot + (ddTargetRot - ddCurrentRot) * math.min(dt * 10, 1)
            dropFrame.Rotation = ddCurrentRot
        end
    end)

    local function positionDropdown(anchorGuiObject)
        local camera = workspace.CurrentCamera
        local viewport = camera and camera.ViewportSize or Vector2.new(1280, 720)
        local padding = 10
        local frameWidth = dropFrame.AbsoluteSize.X > 0 and dropFrame.AbsoluteSize.X or 240
        local frameHeight = dropFrame.AbsoluteSize.Y > 0 and dropFrame.AbsoluteSize.Y or 220
        local defaultX = math.floor((viewport.X - frameWidth) * 0.5)
        local defaultY = math.floor((viewport.Y - frameHeight) * 0.5)

        if typeof(anchorGuiObject) == "Instance" and anchorGuiObject:IsA("GuiObject") then
            local anchorPos = anchorGuiObject.AbsolutePosition
            local anchorSize = anchorGuiObject.AbsoluteSize
            local targetX = anchorPos.X + anchorSize.X + 12
            local targetY = anchorPos.Y
            local maxX = math.max(padding, viewport.X - frameWidth - padding)
            local maxY = math.max(padding, viewport.Y - frameHeight - padding)

            if targetX > maxX then
                targetX = anchorPos.X - frameWidth - 12
            end

            targetX = math.clamp(targetX, padding, maxX)
            targetY = math.clamp(targetY, padding, maxY)
            dropFrame.Position = UDim2.fromOffset(targetX, targetY)
            return
        end

        dropFrame.Position = UDim2.fromOffset(defaultX, defaultY)
    end

    local function showDropdown(anchorGuiObject)
        searchBox.Text = ""
        positionDropdown(anchorGuiObject)
        dropGui.Enabled = true
    end

    local function hideDropdown()
        dropGui.Enabled = false
    end

    closeDropBtn.MouseButton1Click:Connect(hideDropdown)
    table.insert(_allDropdownGuis, dropGui)
    table.insert(_extraCleanupGuis, dropGui)

    return dropFrame, dropList, searchBox, showDropdown, hideDropdown
end

local function createDropdownRow(name, parentList, options, getValueFn, setValueFn)
    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    local updateVis = applyHoverState(row, icon, label, topLine, bottomLine, diamond, false)

    local function resolveOptions()
        if type(options) == "function" then
            local ok, resolvedOptions = pcall(options)
            if ok and type(resolvedOptions) == "table" then
                return resolvedOptions
            end
            return {}
        end

        return type(options) == "table" and options or {}
    end

    local openBg = Instance.new("Frame")
    openBg.Size = UDim2.new(0, 100, 0, 22)
    openBg.Position = UDim2.new(1, -115, 0.5, -11)
    openBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    openBg.BorderSizePixel = 0
    openBg.Parent = row
    Instance.new("UICorner", openBg).CornerRadius = UDim.new(0, 4)

    local openLabel = Instance.new("TextLabel")
    openLabel.Size = UDim2.new(1, 0, 1, 0)
    openLabel.BackgroundTransparency = 1
    openLabel.TextColor3 = Color3.new(1, 1, 1)
    openLabel.Font = Enum.Font.GothamMedium
    openLabel.TextSize = 11
    openLabel.TextTruncate = Enum.TextTruncate.AtEnd
    openLabel.Parent = openBg

    local _, optionList, _, showDropdownGui, hideDropdownGui = createDraggableDropdown(name)

    local function syncLabel()
        local value = tostring(getValueFn() or "none")
        openLabel.Text = "\226\137\161 " .. value
    end

    local function refreshOptions()
        for _, child in ipairs(optionList:GetChildren()) do
            if child:IsA("TextButton") then
                child:Destroy()
            end
        end
        for index, optionValue in ipairs(resolveOptions()) do
            local optionText = tostring(optionValue)
            local button = Instance.new("TextButton")
            button.Name = optionText
            button:SetAttribute("SearchText", optionText)
            button.Size = UDim2.new(1, 0, 0, 24)
            button.LayoutOrder = index
            button.BackgroundColor3 = tostring(getValueFn()) == optionText and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(24, 24, 24)
            button.BorderSizePixel = 0
            button.Text = tostring(getValueFn()) == optionText and ("  \226\156\147 " .. optionText) or ("  " .. optionText)
            button.TextColor3 = Color3.new(1, 1, 1)
            button.Font = Enum.Font.Gotham
            button.TextSize = 12
            button.TextXAlignment = Enum.TextXAlignment.Left
            button.AutoButtonColor = false
            button.ZIndex = 102
            button.Parent = optionList
            Instance.new("UICorner", button).CornerRadius = UDim.new(0, 2)
            _UTILS._applySoundsToButton(button)

            button.MouseButton1Click:Connect(function()
                if destroyed then
                    return
                end
                _UTILS._createParticleBurst(button)
                setValueFn(optionText)
                pcall(syncAllUi)
                syncLabel()
                refreshOptions()
                hideDropdownGui()
            end)

            button.MouseEnter:Connect(function()
                if tostring(getValueFn()) ~= optionText then
                    button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                end
            end)

            button.MouseLeave:Connect(function()
                if tostring(getValueFn()) ~= optionText then
                    button.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
                end
            end)
        end
    end

    local function showDropdown()
        hideAllDropdowns()
        syncLabel()
        refreshOptions()
        showDropdownGui(row)
    end

    registerUiSync(_allDropdownSyncs, syncLabel)
    syncLabel()

    row.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(row)
        updateVis(true)
        showDropdown()
    end)

    if _currentRegistryTab then
        registerButtonEntry({
            name = name,
            tab = _currentRegistryTab,
            type = "special",
            onClickFn = showDropdown,
        })
    end

    return row, syncLabel, label, openLabel
end

local function createMultiSelectDropdownRow(name, parentList, options, getSelectedFn, toggleValueFn, getLabelTextFn)
    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    local updateVis = applyHoverState(row, icon, label, topLine, bottomLine, diamond, false)

    local function resolveOptions()
        if type(options) == "function" then
            local ok, resolvedOptions = pcall(options)
            if ok and type(resolvedOptions) == "table" then
                return resolvedOptions
            end
            return {}
        end

        return type(options) == "table" and options or {}
    end

    local openBg = Instance.new("Frame")
    openBg.Size = UDim2.new(0, 100, 0, 22)
    openBg.Position = UDim2.new(1, -115, 0.5, -11)
    openBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    openBg.BorderSizePixel = 0
    openBg.Parent = row
    Instance.new("UICorner", openBg).CornerRadius = UDim.new(0, 4)

    local openLabel = Instance.new("TextLabel")
    openLabel.Size = UDim2.new(1, 0, 1, 0)
    openLabel.BackgroundTransparency = 1
    openLabel.TextColor3 = Color3.new(1, 1, 1)
    openLabel.Font = Enum.Font.GothamMedium
    openLabel.TextSize = 11
    openLabel.TextTruncate = Enum.TextTruncate.AtEnd
    openLabel.Parent = openBg

    local _, optionList, _, showDropdownGui = createDraggableDropdown(name)

    local function syncLabel()
        local labelText = getLabelTextFn and getLabelTextFn() or "none"
        openLabel.Text = "\226\137\161 " .. tostring(labelText)
    end

    local function refreshOptions()
        for _, child in ipairs(optionList:GetChildren()) do
            if child:IsA("TextButton") then
                child:Destroy()
            end
        end

        local selected = type(getSelectedFn) == "function" and getSelectedFn() or {}
        for index, optionValue in ipairs(resolveOptions()) do
            local optionText = tostring(optionValue)
            local isSelected = type(selected) == "table" and selected[optionText] == true
            local button = Instance.new("TextButton")
            button.Name = optionText
            button:SetAttribute("SearchText", optionText)
            button.Size = UDim2.new(1, 0, 0, 24)
            button.LayoutOrder = index
            button.BackgroundColor3 = isSelected and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(24, 24, 24)
            button.BorderSizePixel = 0
            button.Text = isSelected and ("  \226\156\147 " .. optionText) or ("  " .. optionText)
            button.TextColor3 = Color3.new(1, 1, 1)
            button.Font = Enum.Font.Gotham
            button.TextSize = 12
            button.TextXAlignment = Enum.TextXAlignment.Left
            button.AutoButtonColor = false
            button.ZIndex = 102
            button.Parent = optionList
            Instance.new("UICorner", button).CornerRadius = UDim.new(0, 2)
            _UTILS._applySoundsToButton(button)

            button.MouseButton1Click:Connect(function()
                if destroyed then
                    return
                end
                _UTILS._createParticleBurst(button)
                toggleValueFn(optionText)
                pcall(syncAllUi)
                refreshOptions()
                syncLabel()
            end)

            button.MouseEnter:Connect(function()
                if not isSelected then
                    button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                end
            end)

            button.MouseLeave:Connect(function()
                if not isSelected then
                    button.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
                end
            end)
        end
    end

    local function showDropdown()
        hideAllDropdowns()
        syncLabel()
        refreshOptions()
        showDropdownGui(row)
    end

    registerUiSync(_allDropdownSyncs, syncLabel)
    syncLabel()

    row.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(row)
        updateVis(true)
        showDropdown()
    end)

    if _currentRegistryTab then
        registerButtonEntry({
            name = name,
            tab = _currentRegistryTab,
            type = "special",
            onClickFn = showDropdown,
        })
    end

    return row, syncLabel, label, openLabel
end

local function createInputRow(name, parentList, getValueFn, setValueFn, placeholder)
    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    applyHoverState(row, icon, label, topLine, bottomLine, diamond, false)
    icon.BackgroundColor3 = Color3.fromRGB(130, 90, 255)

    local textBox = Instance.new("TextBox")
    textBox.Size = UDim2.new(0, 180, 0, 24)
    textBox.Position = UDim2.new(1, -195, 0.5, -12)
    textBox.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    textBox.BorderSizePixel = 1
    textBox.BorderColor3 = Color3.fromRGB(45, 45, 45)
    textBox.TextColor3 = Color3.new(1, 1, 1)
    textBox.PlaceholderText = placeholder or ""
    textBox.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
    textBox.Text = tostring(getValueFn() or "")
    textBox.Font = Enum.Font.GothamMedium
    textBox.TextSize = 11
    textBox.TextXAlignment = Enum.TextXAlignment.Left
    textBox.ClearTextOnFocus = false
    textBox.ClipsDescendants = true
    textBox.Parent = row
    Instance.new("UICorner", textBox).CornerRadius = UDim.new(0, 4)

    local textPadding = Instance.new("UIPadding")
    textPadding.PaddingLeft = UDim.new(0, 6)
    textPadding.Parent = textBox

    row.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(row)
        textBox:CaptureFocus()
    end)

    textBox.FocusLost:Connect(function()
        if destroyed then
            return
        end
        setValueFn(textBox.Text)
    end)

    registerUiSync(_allTextSyncs, function()
        textBox.Text = tostring(getValueFn() or "")
    end)

    return row, textBox, label
end

local function createSectionHeader(text, parentList)
    local header = Instance.new("TextLabel")
    header.Size = UDim2.new(1, 0, 0, 28)
    header.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    header.BorderSizePixel = 0
    header.TextColor3 = Color3.fromRGB(0, 150, 255)
    header.Text = "  " .. text
    header.Font = Enum.Font.GothamBold
    header.TextSize = 13
    header.TextXAlignment = Enum.TextXAlignment.Left
    _rowLayoutCounter = _rowLayoutCounter + 1
    header.LayoutOrder = _rowLayoutCounter
    header.Parent = parentList
    Instance.new("UICorner", header).CornerRadius = UDim.new(0, 4)
    return header
end

local function createCollapsibleSection(sectionName, parentList, options)
    options = type(options) == "table" and options or {}
    local collapsedToggleText = type(options.collapsedToggleText) == "string" and options.collapsedToggleText or "+"
    local expandedToggleText = type(options.expandedToggleText) == "string" and options.expandedToggleText or "-"
    local onRemove = type(options.onRemove) == "function" and options.onRemove or nil

    local headerBtn = Instance.new("TextButton")
    headerBtn.Size = UDim2.new(1, 0, 0, 38)
    headerBtn.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    headerBtn.BorderSizePixel = 0
    headerBtn.Text = ""
    headerBtn.AutoButtonColor = false
    _rowLayoutCounter = _rowLayoutCounter + 1
    headerBtn.LayoutOrder = _rowLayoutCounter
    headerBtn.Parent = parentList
    _UTILS._applySoundsToButton(headerBtn)
    Instance.new("UICorner", headerBtn).CornerRadius = UDim.new(0, 4)

    local headerIcon = Instance.new("Frame")
    headerIcon.Size = UDim2.new(0, 14, 0, 14)
    headerIcon.Rotation = 45
    headerIcon.Position = UDim2.new(0, 24, 0.5, -7)
    headerIcon.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    headerIcon.BorderSizePixel = 0
    headerIcon.Parent = headerBtn
    Instance.new("UICorner", headerIcon).CornerRadius = UDim.new(0, 3)

    local headerLabel = Instance.new("TextLabel")
    headerLabel.Size = UDim2.new(1, onRemove and -108 or -80, 1, 0)
    headerLabel.Position = UDim2.new(0, 50, 0, 0)
    headerLabel.BackgroundTransparency = 1
    headerLabel.TextColor3 = Color3.fromRGB(0, 150, 255)
    headerLabel.Text = sectionName
    headerLabel.Font = Enum.Font.GothamBold
    headerLabel.TextSize = 14
    headerLabel.TextXAlignment = Enum.TextXAlignment.Left
    headerLabel.Parent = headerBtn

    local arrow = Instance.new("TextLabel")
    arrow.Size = UDim2.new(0, 30, 1, 0)
    arrow.Position = UDim2.new(1, -40, 0, 0)
    arrow.BackgroundTransparency = 1
    arrow.TextColor3 = Color3.fromRGB(150, 150, 150)
    arrow.Text = collapsedToggleText
    arrow.Font = Enum.Font.GothamBold
    arrow.TextSize = 16
    arrow.Parent = headerBtn

    local ignoreHeaderToggleUntil = 0
    local removeButton = nil
    if onRemove then
        removeButton = Instance.new("TextButton")
        removeButton.Size = UDim2.new(0, 24, 1, 0)
        removeButton.Position = UDim2.new(1, -72, 0, 0)
        removeButton.BackgroundTransparency = 1
        removeButton.BorderSizePixel = 0
        removeButton.TextColor3 = Color3.fromRGB(255, 90, 90)
        removeButton.Text = "-"
        removeButton.Font = Enum.Font.GothamBold
        removeButton.TextSize = 18
        removeButton.AutoButtonColor = false
        removeButton.Parent = headerBtn
        _UTILS._applySoundsToButton(removeButton)

        removeButton.MouseButton1Click:Connect(function()
            if destroyed then
                return
            end
            ignoreHeaderToggleUntil = os.clock() + 0.2
            _UTILS._createParticleBurst(removeButton)
            onRemove()
        end)

        removeButton.MouseEnter:Connect(function()
            if destroyed then
                return
            end
            _TweenService:Create(removeButton, TweenInfo.new(0.15), {TextColor3 = Color3.new(1, 1, 1)}):Play()
        end)

        removeButton.MouseLeave:Connect(function()
            if destroyed then
                return
            end
            _TweenService:Create(removeButton, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(255, 90, 90)}):Play()
        end)
    end

    local headerTopLine = Instance.new("Frame")
    headerTopLine.Size = UDim2.new(0, 0, 0, 2)
    headerTopLine.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    headerTopLine.BorderSizePixel = 0
    headerTopLine.Parent = headerBtn

    local headerBotLine = Instance.new("Frame")
    headerBotLine.Size = UDim2.new(0, 0, 0, 2)
    headerBotLine.Position = UDim2.new(0, 0, 1, -2)
    headerBotLine.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    headerBotLine.BorderSizePixel = 0
    headerBotLine.Parent = headerBtn

    local sectionContainer = Instance.new("Frame")
    sectionContainer.Size = UDim2.new(1, 0, 0, 0)
    sectionContainer.BackgroundTransparency = 1
    sectionContainer.BorderSizePixel = 0
    sectionContainer.ClipsDescendants = true
    _rowLayoutCounter = _rowLayoutCounter + 1
    sectionContainer.LayoutOrder = _rowLayoutCounter
    sectionContainer.Parent = parentList

    local sectionList = Instance.new("Frame")
    sectionList.Size = UDim2.new(1, 0, 0, 0)
    sectionList.BackgroundTransparency = 1
    sectionList.BorderSizePixel = 0
    sectionList.AutomaticSize = Enum.AutomaticSize.Y
    sectionList.Parent = sectionContainer

    local sectionLayout = Instance.new("UIListLayout")
    sectionLayout.Padding = UDim.new(0, 8)
    sectionLayout.SortOrder = Enum.SortOrder.LayoutOrder
    sectionLayout.Parent = sectionList

    local sectionIsExpanded = false
    sectionLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        if sectionIsExpanded then
            sectionContainer.Size = UDim2.new(1, 0, 0, sectionLayout.AbsoluteContentSize.Y)
        end
    end)

    headerBtn.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        if os.clock() < ignoreHeaderToggleUntil then
            return
        end
        _UTILS._createParticleBurst(headerBtn)
        sectionIsExpanded = not sectionIsExpanded
        if sectionIsExpanded then
            local targetH = sectionLayout.AbsoluteContentSize.Y
            arrow.Text = expandedToggleText
            _TweenService:Create(sectionContainer, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
                Size = UDim2.new(1, 0, 0, targetH)
            }):Play()
            _TweenService:Create(headerIcon, TweenInfo.new(0.2), {BackgroundColor3 = Color3.new(1, 1, 1)}):Play()
            _TweenService:Create(arrow, TweenInfo.new(0.2), {TextColor3 = Color3.new(1, 1, 1)}):Play()
        else
            arrow.Text = collapsedToggleText
            _TweenService:Create(sectionContainer, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {
                Size = UDim2.new(1, 0, 0, 0)
            }):Play()
            _TweenService:Create(headerIcon, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 150, 255)}):Play()
            _TweenService:Create(arrow, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(150, 150, 150)}):Play()
        end
    end)

    headerBtn.MouseEnter:Connect(function()
        if destroyed then
            return
        end
        _TweenService:Create(headerBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(28, 28, 28)}):Play()
        _TweenService:Create(headerLabel, TweenInfo.new(0.2), {TextColor3 = Color3.new(1, 1, 1)}):Play()
        local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
        _TweenService:Create(headerTopLine, tweenInfo, {Size = UDim2.new(1, 0, 0, 2)}):Play()
        _TweenService:Create(headerBotLine, tweenInfo, {Size = UDim2.new(1, 0, 0, 2)}):Play()
    end)

    headerBtn.MouseLeave:Connect(function()
        if destroyed then
            return
        end
        _TweenService:Create(headerBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(18, 18, 18)}):Play()
        _TweenService:Create(headerLabel, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(0, 150, 255)}):Play()
        local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
        _TweenService:Create(headerTopLine, tweenInfo, {Size = UDim2.new(0, 0, 0, 2)}):Play()
        _TweenService:Create(headerBotLine, tweenInfo, {Size = UDim2.new(0, 0, 0, 2)}):Play()
    end)

    return sectionList, headerLabel, removeButton
end

local function getDynamitePlayerOptions()
    local playerNames = {}
    for _, player in ipairs(_Players:GetPlayers()) do
        if player ~= localPlayer then
            table.insert(playerNames, player.Name)
        end
    end
    table.sort(playerNames)
    if #playerNames == 0 then
        table.insert(playerNames, "No Players")
    end
    return playerNames
end

local function createPathSlotSectionUi(parentPage, slotIndex, nextCustomOrderFn)
    local function refreshPathManagerUi()
        local refreshButtons = getCallableGlobal("refreshPathManagerButtons")
        if type(refreshButtons) == "function" then
            refreshButtons()
        end
    end

    local function refreshPathVisualsUi()
        local refreshVisuals = getCallableGlobal("refreshPathVisuals")
        if type(refreshVisuals) == "function" then
            refreshVisuals()
        end
    end

    local slotHost = nextCustomOrderFn(Instance.new("Frame"))
    slotHost.Name = "PathSectionHost" .. tostring(slotIndex)
    slotHost.Size = UDim2.new(1, 0, 0, 0)
    slotHost.AutomaticSize = Enum.AutomaticSize.Y
    slotHost.BackgroundTransparency = 1
    slotHost.BorderSizePixel = 0
    slotHost.Parent = parentPage

    local slotHostLayout = Instance.new("UIListLayout")
    slotHostLayout.Padding = UDim.new(0, 0)
    slotHostLayout.SortOrder = Enum.SortOrder.LayoutOrder
    slotHostLayout.Parent = slotHost
    slotHostLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(updateAutofarmLayout)

    local slotList, slotHeaderLabel, slotRemoveButton = createCollapsibleSection("Path " .. tostring(slotIndex), slotHost, {
        onRemove = function()
            if removePathSection(slotIndex) then
                refreshPathManagerUi()
                updateAutofarmLayout()
            end
        end,
    })
    local slotUi = {
        host = slotHost,
        headerLabel = slotHeaderLabel,
        removeSectionButton = slotRemoveButton,
    }

    local _, savedPathSync = createDropdownRow(
        "Saved Path",
        slotList,
        function()
            local savedNames = getSavedPathNamesOrdered()
            if #savedNames == 0 then
                return {"none"}
            end
            return savedNames
        end,
        function()
            return getPathSlotSelection(slotIndex, true) or "none"
        end,
        function(value)
            local nextPathName = tostring(value) == "none" and nil or tostring(value)
            setPathSlotSelection(slotIndex, nextPathName, {syncSelectedPath = true})
            setPathDeleteConfirmSlot(nil)
            savePathsToStorage(false)
            refreshPathManagerUi()
            refreshPathVisualsUi()
        end
    )
    slotUi.savedPathSync = savedPathSync

    local _, _, pathPlaySync = createToggleRow(
        "Play Path",
        slotList,
        function()
            return isPathPlaybackBusy() and (normalizePathSlotIndex(pathState.sequenceStartSlotIndex) or 1) == slotIndex
        end,
        function()
            togglePathLoopFromControl("button", slotIndex)
        end
    )
    slotUi.pathPlaySync = pathPlaySync

    local _, _, alwaysPlaySync = createToggleRow(
        "Always Play Path",
        slotList,
        function()
            return pathState.alwaysPlayEnabled and (normalizePathSlotIndex(pathState.sequenceStartSlotIndex) or 1) == slotIndex
        end,
        function()
            local playableSlotIndex = getPlayablePathSlotIndex(slotIndex)
            if not playableSlotIndex then
                notifyUser("Paths", "Pick a saved path first.", 4)
                return
            end

            local sameSlotEnabled = pathState.alwaysPlayEnabled
                and (normalizePathSlotIndex(pathState.sequenceStartSlotIndex) or 1) == playableSlotIndex
            setPathSequenceStartSlotIndex(playableSlotIndex)
            setFocusedPathSlotIndex(playableSlotIndex, true)
            pathState.alwaysPlayEnabled = not sameSlotEnabled
            if pathState.alwaysPlayEnabled and not autoMineEnabled then
                activeFarmMode = "path"
                local ok, started = xpcall(function()
                    return startPathPlaybackForSlot(playableSlotIndex, "alwaysplay")
                end, debug.traceback)
                if not ok then
                    autoMineEnabled = false
                    activeFarmMode = nil
                    setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
                    pushDebug(started)
                elseif not started then
                    autoMineEnabled = false
                    activeFarmMode = nil
                end
            end
            savePathsToStorage(true)
            refreshPathManagerUi()
        end
    )
    slotUi.alwaysPlaySync = alwaysPlaySync

    local recordButton, _, recordLabel = createButtonRow("Record Path", slotList, function()
        setFocusedPathSlotIndex(slotIndex, false)
        if pathState.editorVisible and not pathState.editingSavedPath and pathState.focusedPathSlotIndex == slotIndex then
            closePathEditor()
            return
        end
        beginNewPathRecording(slotIndex)
        refreshPathEditorButtons()
        refreshPathManagerUi()
    end, {skipRegistry = true})
    slotUi.recordButton = recordButton
    slotUi.recordLabel = recordLabel

    local _, _, editPathSync = createToggleRow(
        "Edit Path",
        slotList,
        function()
            return pathState.editorVisible and pathState.editingSavedPath and (normalizePathSlotIndex(pathState.focusedPathSlotIndex) or 1) == slotIndex
        end,
        function()
            if pathState.editorVisible and pathState.editingSavedPath and (normalizePathSlotIndex(pathState.focusedPathSlotIndex) or 1) == slotIndex then
                closePathEditor()
                return
            end

            local selectedPathName = getPathSlotSelection(slotIndex, false)
            if not selectedPathName or not pathState.paths[selectedPathName] then
                notifyUser("Paths", "Pick a saved path first.", 4)
                return
            end
            beginPathEditing(selectedPathName, slotIndex)
            refreshPathEditorButtons()
            refreshPathManagerUi()
        end
    )
    slotUi.editPathSync = editPathSync

    local deleteButton, _, deleteLabel = createButtonRow("Delete Path", slotList, function()
        local selectedPathName = getPathSlotSelection(slotIndex, false)
        if not selectedPathName then
            notifyUser("Paths", "Pick a saved path first.", 4)
            return
        end

        setFocusedPathSlotIndex(slotIndex, true)
        if isPathDeleteConfirmArmed(slotIndex) then
            setPathDeleteConfirmSlot(nil)
            deleteSelectedPath(slotIndex)
            refreshPathManagerUi()
            return
        end

        setPathDeleteConfirmSlot(slotIndex)
        refreshPathManagerUi()
        task.delay(3, function()
            if isPathDeleteConfirmArmed(slotIndex) then
                setPathDeleteConfirmSlot(nil)
                refreshPathManagerUi()
            end
        end)
    end, {skipRegistry = true})
    slotUi.deleteButton = deleteButton
    slotUi.deleteLabel = deleteLabel

    return slotUi
end

local function ensureValidDynamiteSelectedPlayer()
    local options = getDynamitePlayerOptions()
    if #options == 0 then
        S.dynamiteSelectedPlayer = "No Players"
        return S.dynamiteSelectedPlayer
    end
    if not table.find(options, S.dynamiteSelectedPlayer) then
        S.dynamiteSelectedPlayer = options[1]
    end
    return S.dynamiteSelectedPlayer
end

local function getTwwEntityPlayersFolder()
    local entities = workspace:FindFirstChild("WORKSPACE_Entities")
    return entities and entities:FindFirstChild("Players") or nil
end

local function getTwwModelRoot(model)
    if not model then
        return nil
    end
    return model:FindFirstChild("Body") or model:FindFirstChild("HumanoidRootPart") or model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart", true)
end

function getDynamiteMarkedPlayers()
    if type(S._dynamiteMarkedPlayers) ~= "table" then
        S._dynamiteMarkedPlayers = {}
    end
    return S._dynamiteMarkedPlayers
end

function destroyDynamiteMarkVisual(playerName)
    if type(S._dynamiteMarkVisuals) ~= "table" then
        S._dynamiteMarkVisuals = {}
        return
    end

    local visuals = S._dynamiteMarkVisuals[playerName]
    if not visuals then
        return
    end

    for _, instance in pairs(visuals) do
        if typeof(instance) == "Instance" then
            pcall(function()
                instance:Destroy()
            end)
        end
    end
    S._dynamiteMarkVisuals[playerName] = nil
end

function clearAllDynamiteMarkedPlayers()
    for _, playerName in ipairs(getDynamiteMarkedPlayers()) do
        destroyDynamiteMarkVisual(playerName)
    end
    S._dynamiteMarkedPlayers = {}
end

function removeDynamiteMarkedPlayer(playerName)
    local order = getDynamiteMarkedPlayers()
    for index = #order, 1, -1 do
        if order[index] == playerName then
            table.remove(order, index)
        end
    end
    destroyDynamiteMarkVisual(playerName)
end

function getTwwPlayerFromTarget(target)
    local playersFolder = getTwwEntityPlayersFolder()
    local current = target
    while current and current ~= playersFolder do
        if current.Parent == playersFolder then
            return _Players:FindFirstChild(current.Name)
        end
        current = current.Parent
    end
    return nil
end

function getAliveTwwTargetData(playerName)
    local player = type(playerName) == "Instance" and playerName or _Players:FindFirstChild(tostring(playerName or ""))
    if not player or player == localPlayer then
        return nil
    end

    local character = getWorldCharacterForPlayer(player)
    local rootPart = getTwwModelRoot(character)
    if character and rootPart and isCharacterAlive(character) then
        return player, character, rootPart
    end

    return nil
end

function refreshDynamiteMarkVisuals()
    if type(S._dynamiteMarkVisuals) ~= "table" then
        S._dynamiteMarkVisuals = {}
    end

    local activeVisuals = {}
    if not (S.dynamiteTarget == "Player" and S.dynamiteMarkPlayersEnabled) then
        for playerName in pairs(S._dynamiteMarkVisuals) do
            destroyDynamiteMarkVisual(playerName)
        end
        return
    end

    for index, playerName in ipairs(getDynamiteMarkedPlayers()) do
        local player, character, rootPart = getAliveTwwTargetData(playerName)
        if player and character and rootPart then
            activeVisuals[player.Name] = true
            local visuals = S._dynamiteMarkVisuals[player.Name]
            if type(visuals) ~= "table" or not visuals.highlight or not visuals.highlight.Parent then
                visuals = {}

                local highlight = Instance.new("Highlight")
                highlight.Name = "FobDynamiteMark"
                highlight.FillColor = Color3.fromRGB(255, 58, 58)
                highlight.FillTransparency = 0.55
                highlight.OutlineColor = Color3.fromRGB(255, 215, 215)
                highlight.OutlineTransparency = 0
                highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                highlight.Adornee = character
                highlight.Parent = character
                visuals.highlight = highlight

                local billboard = Instance.new("BillboardGui")
                billboard.Name = "FobDynamiteMarkBillboard"
                billboard.AlwaysOnTop = true
                billboard.MaxDistance = 10000
                billboard.Size = UDim2.new(0, 40, 0, 28)
                billboard.StudsOffset = Vector3.new(0, 4.2, 0)
                billboard.Adornee = rootPart
                billboard.Parent = character
                visuals.billboard = billboard

                local label = Instance.new("TextLabel")
                label.Size = UDim2.new(1, 0, 1, 0)
                label.BackgroundTransparency = 1
                label.Font = Enum.Font.GothamBold
                label.TextScaled = true
                label.TextStrokeTransparency = 0
                label.TextColor3 = Color3.fromRGB(255, 70, 70)
                label.Parent = billboard
                visuals.label = label

                S._dynamiteMarkVisuals[player.Name] = visuals
            end

            if visuals.highlight then
                visuals.highlight.Adornee = character
                visuals.highlight.Parent = character
            end
            if visuals.billboard then
                visuals.billboard.Adornee = rootPart
                visuals.billboard.Parent = character
            end
            if visuals.label then
                visuals.label.Text = tostring(index)
            end
        end
    end

    for playerName in pairs(S._dynamiteMarkVisuals) do
        if not activeVisuals[playerName] then
            destroyDynamiteMarkVisual(playerName)
        end
    end
end

function pruneDynamiteMarkedPlayers()
    local order = getDynamiteMarkedPlayers()
    local index = 1
    local changed = false
    while index <= #order do
        local player, character = getAliveTwwTargetData(order[index])
        if not player or not character then
            destroyDynamiteMarkVisual(order[index])
            table.remove(order, index)
            changed = true
        else
            index = index + 1
        end
    end
    if changed then
        schedulePersist()
        refreshDynamiteMarkVisuals()
    end
end

function destroyPathPlayerAvoiderVisual()
    if type(pathState) ~= "table" then
        return
    end

    local visuals = pathState.playerAvoiderVisual
    if type(visuals) == "table" then
        for _, instance in pairs(visuals) do
            if typeof(instance) == "Instance" then
                pcall(function()
                    instance:Destroy()
                end)
            end
        end
    end

    pathState.playerAvoiderVisual = nil
end

function clearPathPlayerAvoiderDetection()
    if type(pathState) ~= "table" then
        return
    end

    pathState.playerAvoiderLastScanAt = 0
    pathState.playerAvoiderLastResult = nil
    pathState.playerAvoiderBlockedPlayerName = nil
    pathState.playerAvoiderInternalMove = false
    destroyPathPlayerAvoiderVisual()
    if type(clearPathPlayerAvoiderCorridorVisualization) == "function" then
        clearPathPlayerAvoiderCorridorVisualization()
    end
end

local function shouldPathHopOnDeath()
	return isPathHopOnDeathEnabled()
		and (
			pathState.playbackActive
			or pathState.farmPathsEnabled
			or pathState.alwaysPlayEnabled
			or pathState.resumePathPlaybackPending
			or (autoMineEnabled and activeFarmMode == "path")
		)
end

local function disconnectPathDeathHopConnection()
	if pathDeathHopDiedConnection then
		pcall(function()
			pathDeathHopDiedConnection:Disconnect()
		end)
	end
	pathDeathHopDiedConnection = nil
	pathDeathHopHumanoid = nil
	pathDeathHopBoundName = nil
end

local function getPathHopOnDeathHumanoid()
	local playersFolder = getTwwEntityPlayersFolder()
	local worldCharacter = playersFolder and playersFolder:FindFirstChild(localPlayer.Name) or getWorldCharacterForPlayer(localPlayer)
	if worldCharacter then
		local worldHumanoid = worldCharacter:FindFirstChild("Humanoid") or worldCharacter:FindFirstChildOfClass("Humanoid")
		if worldHumanoid then
			return worldHumanoid, worldCharacter
		end
		if playersFolder and worldCharacter.Parent == playersFolder then
			return nil, worldCharacter
		end
	end

	local customCharacter = getCustomCharacter()
	if customCharacter then
		local customHumanoid = customCharacter:FindFirstChild("Humanoid") or customCharacter:FindFirstChildOfClass("Humanoid")
		if customHumanoid then
			return customHumanoid, customCharacter
		end
	end

	local defaultCharacter = localPlayer and localPlayer.Character
	if defaultCharacter then
		return getHumanoid(defaultCharacter), defaultCharacter
	end

	return nil, nil
end

local function handlePathHopOnDeathTriggered()
	if not shouldPathHopOnDeath() or serverHopInProgress or serverHopTaskQueued then
		return
	end
	local now = os.clock()
	if (now - (tonumber(lastPathDeathHopAt) or 0)) < 2 then
		return
	end
	lastPathDeathHopAt = now
	requestPathServerHop("Character died, hopping servers...", getPathCycleRestartSlotIndex(), true, "death")
end

function refreshPathDeathHopConnection()
	if not shouldPathHopOnDeath() then
		disconnectPathDeathHopConnection()
		return
	end

	local humanoid, ownerCharacter = getPathHopOnDeathHumanoid()
	if not humanoid or humanoid.Health <= 0 then
		disconnectPathDeathHopConnection()
		return
	end

	if humanoid == pathDeathHopHumanoid and pathDeathHopDiedConnection then
		return
	end

	disconnectPathDeathHopConnection()
	pathDeathHopHumanoid = humanoid
	pathDeathHopBoundName = ownerCharacter and ownerCharacter:GetFullName() or humanoid:GetFullName()
	pathDeathHopDiedConnection = humanoid.Died:Connect(function()
		if humanoid ~= pathDeathHopHumanoid then
			return
		end
		handlePathHopOnDeathTriggered()
	end)
end

function updatePathPlayerAvoiderVisual(player, character, rootPart)
    if not (type(pathState) == "table" and player and character and rootPart and isPathPlayerAvoiderEnabled()) then
        clearPathPlayerAvoiderDetection()
        return
    end

    local visuals = pathState.playerAvoiderVisual
    if type(visuals) ~= "table" or not visuals.highlight or not visuals.highlight.Parent then
        visuals = {}

        local highlight = Instance.new("Highlight")
        highlight.Name = "FobPathPlayerAvoider"
        highlight.FillColor = Color3.fromRGB(255, 156, 64)
        highlight.FillTransparency = 0.5
        highlight.OutlineColor = Color3.fromRGB(255, 220, 152)
        highlight.OutlineTransparency = 0
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.Adornee = character
        highlight.Parent = character
        visuals.highlight = highlight

        local billboard = Instance.new("BillboardGui")
        billboard.Name = "FobPathPlayerAvoiderBillboard"
        billboard.AlwaysOnTop = true
        billboard.MaxDistance = 10000
        billboard.Size = UDim2.new(0, 38, 0, 30)
        billboard.StudsOffset = Vector3.new(0, 4.4, 0)
        billboard.Adornee = rootPart
        billboard.Parent = character
        visuals.billboard = billboard

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamBold
        label.TextScaled = true
        label.TextStrokeTransparency = 0
        label.TextColor3 = Color3.fromRGB(255, 171, 77)
        label.Text = "!"
        label.Parent = billboard
        visuals.label = label

        pathState.playerAvoiderVisual = visuals
    end

    if visuals.highlight then
        visuals.highlight.Adornee = character
        visuals.highlight.Parent = character
    end
    if visuals.billboard then
        visuals.billboard.Adornee = rootPart
        visuals.billboard.Parent = character
    end
    if visuals.label then
        visuals.label.Text = "!"
    end

    pathState.playerAvoiderBlockedPlayerName = player.Name
end

function getPriorityDynamiteTargetName()
    if S.dynamiteTarget == "Player" and S.dynamiteMarkPlayersEnabled then
        pruneDynamiteMarkedPlayers()
        local order = getDynamiteMarkedPlayers()
        if #order > 0 then
            return order[1]
        end
    end
    return ensureValidDynamiteSelectedPlayer()
end

function toggleHoveredDynamitePlayerMark()
    if destroyed or not isSessionActive() or not S.dynamiteMarkPlayersEnabled or S.dynamiteTarget ~= "Player" then
        return
    end

    local mouse = localPlayer and localPlayer:GetMouse()
    local hoveredPlayer = getTwwPlayerFromTarget(mouse and mouse.Target)
    if not hoveredPlayer or hoveredPlayer == localPlayer then
        return
    end

    local livePlayer = getAliveTwwTargetData(hoveredPlayer.Name)
    if not livePlayer then
        spawnNotification("That player is not alive right now.", "warning", true)
        return
    end

    local order = getDynamiteMarkedPlayers()
    local existingIndex = table.find(order, hoveredPlayer.Name)
    if existingIndex then
        table.remove(order, existingIndex)
        destroyDynamiteMarkVisual(hoveredPlayer.Name)
        refreshDynamiteMarkVisuals()
        spawnNotification("Removed " .. hoveredPlayer.Name .. " from the dynamite mark list.", "info", true)
        return
    end

    table.insert(order, hoveredPlayer.Name)
    refreshDynamiteMarkVisuals()
    spawnNotification("Marked " .. hoveredPlayer.Name .. " as target #" .. tostring(#order) .. ".", "success", true)
end

function getWorldCharacterForPlayer(player)
    if not player then
        return nil
    end

    local playersFolder = getTwwEntityPlayersFolder()
    return (playersFolder and playersFolder:FindFirstChild(player.Name)) or player.Character
end

function isPlayerEspMethodEnabled(methodName)
    return type(S.playerEspMethods) == "table" and S.playerEspMethods[tostring(methodName)] == true
end

function setPlayerEspMethodEnabled(methodName, isEnabled)
    local nextMethods = clonePlayerEspMethods(S.playerEspMethods)
    nextMethods[tostring(methodName)] = isEnabled == true
    S.playerEspMethods = clonePlayerEspMethods(nextMethods)
end

function togglePlayerEspMethod(methodName)
    local methodKey = tostring(methodName or "")
    if methodKey == "" then
        return
    end

    local nextValue = not isPlayerEspMethodEnabled(methodKey)
    local enabledCount = 0
    if nextValue == false then
        for _, otherMethod in ipairs(PLAYER_ESP_METHOD_ORDER) do
            if otherMethod ~= methodKey and isPlayerEspMethodEnabled(otherMethod) then
                enabledCount = enabledCount + 1
            end
        end
        if enabledCount == 0 then
            nextValue = true
        end
    end

    setPlayerEspMethodEnabled(methodKey, nextValue)
end

function getPlayerEspMethodLabel()
    local enabledMethods = {}
    for _, methodName in ipairs(PLAYER_ESP_METHOD_ORDER) do
        if isPlayerEspMethodEnabled(methodName) then
            table.insert(enabledMethods, methodName)
        end
    end

    if #enabledMethods == 0 then
        return "Text"
    end
    if #enabledMethods == 1 then
        return enabledMethods[1]
    end

    return enabledMethods[1] .. " +" .. tostring(#enabledMethods - 1)
end

function isGunTargetPriorityEnabled(priorityName)
    return type(S.silentAimTargetPriority) == "table" and S.silentAimTargetPriority[tostring(priorityName)] == true
end

function setGunTargetPriorityEnabled(priorityName, isEnabled)
    local nextPriority = cloneGunTargetPriority(S.silentAimTargetPriority)
    nextPriority[tostring(priorityName)] = isEnabled == true
    S.silentAimTargetPriority = cloneGunTargetPriority(nextPriority)
end

function toggleGunTargetPriority(priorityName)
    local priorityKey = tostring(priorityName or "")
    if priorityKey == "" then
        return
    end

    local nextValue = not isGunTargetPriorityEnabled(priorityKey)
    local enabledCount = 0
    if nextValue == false then
        for _, otherPriority in ipairs(GUN_TARGET_PRIORITY_ORDER) do
            if otherPriority ~= priorityKey and isGunTargetPriorityEnabled(otherPriority) then
                enabledCount = enabledCount + 1
            end
        end
        if enabledCount == 0 then
            nextValue = true
        end
    end

    setGunTargetPriorityEnabled(priorityKey, nextValue)
end

function getGunTargetPriorityLabel()
    local enabledPriorities = {}
    for _, priorityName in ipairs(GUN_TARGET_PRIORITY_ORDER) do
        if isGunTargetPriorityEnabled(priorityName) then
            table.insert(enabledPriorities, priorityName)
        end
    end

    if #enabledPriorities == 0 then
        return "Players"
    end
    if #enabledPriorities == 1 then
        return enabledPriorities[1]
    end

    return enabledPriorities[1] .. " +" .. tostring(#enabledPriorities - 1)
end

function resolveTwwLeaderboardStateMap(forceRefresh)
    if type(S._espLeaderboardStateMap) == "table" and not forceRefresh then
        return S._espLeaderboardStateMap
    end

    S._espLeaderboardStateMap = nil

    local leaderboard = nil
    local modulesTable = type(resolveGameModules) == "function" and resolveGameModules(forceRefresh) or nil
    if isTable(modulesTable) then
        leaderboard = rawget(modulesTable, "leaderboard") or modulesTable.leaderboard
    end

    if not isTable(leaderboard) then
        local globalModule = rawget(_G, "__AutoMineGlobalModule")
        if isTable(globalModule) and type(globalModule.LoadModule) == "function" then
            local okLoad, loadedModule = pcall(function()
                return globalModule:LoadModule("Leaderboard")
            end)
            if okLoad and isTable(loadedModule) then
                leaderboard = loadedModule
            end
        end
    end

    if not isTable(leaderboard) or type(leaderboard.UpdatePlayers) ~= "function" then
        return nil
    end
    if type(debug) ~= "table" or type(debug.getupvalues) ~= "function" then
        return nil
    end

    local okOuter, outerUpvalues = pcall(debug.getupvalues, leaderboard.UpdatePlayers)
    if not okOuter or type(outerUpvalues) ~= "table" then
        return nil
    end

    local stateHolder = outerUpvalues[2]
    if type(stateHolder) ~= "table" or type(stateHolder.new) ~= "function" then
        return nil
    end

    local okInner, innerUpvalues = pcall(debug.getupvalues, stateHolder.new)
    if not okInner or type(innerUpvalues) ~= "table" or type(innerUpvalues[1]) ~= "table" then
        return nil
    end

    S._espLeaderboardStateMap = innerUpvalues[1]
    return S._espLeaderboardStateMap
end

function getTwwPlayerStateInfo(player)
    local stateMap = resolveTwwLeaderboardStateMap(false)
    local stateEntry = stateMap and stateMap[player]
    local stateObject = stateEntry and stateEntry.StateObject
    local stateInfo = stateObject and stateObject.State
    if type(stateInfo) == "table" then
        return stateInfo
    end

    local character = getWorldCharacterForPlayer(player)
    local roleValue = character and character:FindFirstChild("Role", true)
    local criminalValue = character and character:FindFirstChild("CriminalStatus", true)
    if roleValue or criminalValue then
        return {
            Role = roleValue and roleValue:IsA("StringValue") and roleValue.Value or nil,
            CriminalStatus = criminalValue and criminalValue:IsA("StringValue") and criminalValue.Value or nil,
        }
    end

    return nil
end

function getPlayerEspRoleColor(player)
    local stateInfo = getTwwPlayerStateInfo(player)
    if type(stateInfo) == "table" then
        local criminalStatus = tostring(stateInfo.CriminalStatus or "")
        if criminalStatus ~= "" then
            if criminalStatus == "Menace" then
                return Color3.fromRGB(255, 38, 38), "Outlaw"
            end
            if criminalStatus == "Wanted" then
                return Color3.fromRGB(225, 92, 38), "Outlaw"
            end
            return Color3.fromRGB(150, 18, 18), "Outlaw"
        end

        local roleName = tostring(stateInfo.Role or "")
        if roleName == "Sheriff" then
            return Color3.fromRGB(26, 92, 214), "Law"
        end
        if roleName == "Mayor" then
            return Color3.fromRGB(255, 200, 0), "Law"
        end
        if roleName == "Citizen" then
            return Color3.fromRGB(215, 210, 188), "Law"
        end
        if roleName == "Law" then
            return Color3.fromRGB(0, 41, 117), "Law"
        end
        if roleName == "Outlaw" then
            return Color3.fromRGB(216, 74, 52), "Outlaw"
        end
        if roleName == "Skulled" then
            return Color3.fromRGB(145, 14, 14), "Outlaw"
        end
    end

    if player and player.Team and player.Team.Name == "Lawmen" then
        return Color3.fromRGB(26, 92, 214), "Law"
    end
    if player and player.Team and string.find(string.lower(player.Team.Name), "outlaw", 1, true) then
        return Color3.fromRGB(216, 74, 52), "Outlaw"
    end
    return Color3.new(0.996078, 0.952941, 0.733333), "Law"
end

local function getTwwDynamiteTargetCFrame()
    local mode = tostring(S.dynamiteTarget or "Vault")
    if mode == "Tunnel" then
        return CFrame.new(649.74, 20.53, -512.49), true
    elseif mode == "Player" then
        local playersFolder = getTwwEntityPlayersFolder()
        local targetModel = playersFolder and playersFolder:FindFirstChild(getPriorityDynamiteTargetName())
        local targetRoot = getTwwModelRoot(targetModel)
        if targetRoot then
            return targetRoot.CFrame, true
        end
        return nil, false
    end
    return CFrame.new(634.87, 21.95, -545.13), true
end

local function getDynamiteSpectateTargetData()
    local targetName = getPriorityDynamiteTargetName()
    local _, character, rootPart = getAliveTwwTargetData(targetName)
    if not character then
        return nil
    end
    local humanoid = character:FindFirstChild("Humanoid") or character:FindFirstChildOfClass("Humanoid")
    local focusPart = character:FindFirstChild("Head") or character:FindFirstChild("UpperTorso") or rootPart or getTwwModelRoot(character)
    return character, humanoid, rootPart or getTwwModelRoot(character), focusPart
end

function S._buildDynamiteSpectateIgnoreList(targetCharacter)
    local ignoreList = {}
    local entitiesFolder = workspace:FindFirstChild("WORKSPACE_Entities")
    if entitiesFolder then
        ignoreList[#ignoreList + 1] = entitiesFolder
    end
    local playersFolder = getTwwEntityPlayersFolder()
    if playersFolder then
        for _, character in ipairs(playersFolder:GetChildren()) do
            if character and character ~= targetCharacter then
                ignoreList[#ignoreList + 1] = character
            end
        end
    end
    if targetCharacter then
        ignoreList[#ignoreList + 1] = targetCharacter
    end
    local localWorldCharacter = getWorldCharacterForPlayer(localPlayer)
    if localWorldCharacter and localWorldCharacter ~= targetCharacter then
        ignoreList[#ignoreList + 1] = localWorldCharacter
    end
    return ignoreList
end

function S._stepDynamiteSpectateZoomSpring(dt)
    local minDistance = 3
    local maxDistance = 24
    local goal = math.clamp(tonumber(S._dynamiteSpectateTargetDistance) or tonumber(S._dynamiteSpectateDistance) or 8, minDistance, maxDistance)
    local currentDistance = math.clamp(tonumber(S._dynamiteSpectateDistance) or goal, minDistance, maxDistance)
    local currentVelocity = tonumber(S._dynamiteSpectateZoomVelocity) or 0
    local stepDt = math.max(tonumber(dt) or 0, 1 / 240)
    local omega = 4.5 * 2 * math.pi
    local distanceDelta = goal - currentDistance
    local omegaDt = omega * stepDt
    local decay = math.exp(-omegaDt)
    local nextDistance = goal + (currentVelocity * stepDt - distanceDelta * (omegaDt + 1)) * decay
    local nextVelocity = ((distanceDelta * omega - currentVelocity) * omegaDt + currentVelocity) * decay
    if nextDistance < minDistance then
        nextDistance = minDistance
        nextVelocity = 0
    elseif nextDistance > maxDistance then
        nextDistance = maxDistance
        nextVelocity = 0
    end
    S._dynamiteSpectateDistance = nextDistance
    S._dynamiteSpectateZoomVelocity = nextVelocity
    return nextDistance
end

function S._resolveDynamiteSpectatePopper(camera, desiredCFrame, focusPosition, targetCharacter)
    if not (camera and desiredCFrame and typeof(focusPosition) == "Vector3") then
        return desiredCFrame
    end
    local desiredDistance = (desiredCFrame.Position - focusPosition).Magnitude
    local cutoff = 0
    pcall(function()
        camera.CFrame = desiredCFrame
        camera.Focus = CFrame.new(focusPosition)
        cutoff = tonumber(camera:GetLargestCutoffDistance(S._buildDynamiteSpectateIgnoreList(targetCharacter))) or 0
    end)
    if math.abs(desiredDistance - (tonumber(S._dynamiteSpectateLastZoomLevel) or 0)) > 0.001 then
        S._dynamiteSpectateLastPopAmount = 0
    end
    local lastPopAmount = tonumber(S._dynamiteSpectateLastPopAmount) or 0
    if cutoff < lastPopAmount then
        cutoff = lastPopAmount
    end
    if cutoff > 0 then
        desiredCFrame = desiredCFrame + desiredCFrame.LookVector * cutoff
        S._dynamiteSpectateLastPopAmount = math.max(cutoff - 0.3, 0)
    else
        S._dynamiteSpectateLastPopAmount = 0
    end
    S._dynamiteSpectateLastZoomLevel = desiredDistance
    return desiredCFrame
end

local function restoreDynamiteSpectateCamera()
    if not S._dynamiteSpectateCameraActive then
        return
    end
    if S._dynamiteSpectateRenderConnection then
        S._dynamiteSpectateRenderConnection:Disconnect()
        S._dynamiteSpectateRenderConnection = nil
    end
    if S._dynamiteSpectateInputConnection then
        S._dynamiteSpectateInputConnection:Disconnect()
        S._dynamiteSpectateInputConnection = nil
    end
    if S._dynamiteSpectateInputBeganConnection then
        S._dynamiteSpectateInputBeganConnection:Disconnect()
        S._dynamiteSpectateInputBeganConnection = nil
    end
    if S._dynamiteSpectateInputEndedConnection then
        S._dynamiteSpectateInputEndedConnection:Disconnect()
        S._dynamiteSpectateInputEndedConnection = nil
    end
    local camera = workspace.CurrentCamera
    if not camera then
        return
    end
    local localCharacter = localPlayer and localPlayer.Character
    local humanoid = localCharacter and localCharacter:FindFirstChildOfClass("Humanoid")
    local rootPart = getRootPart(localCharacter)
    pcall(function()
        if S._dynamiteSpectatePreDragMouseBehavior ~= nil then
            _UserInputService.MouseBehavior = S._dynamiteSpectatePreDragMouseBehavior
            S._dynamiteSpectatePreDragMouseBehavior = nil
        else
            _UserInputService.MouseBehavior = Enum.MouseBehavior.Default
        end
        if S._dynamiteSpectatePreDragMouseIconEnabled ~= nil then
            _UserInputService.MouseIconEnabled = S._dynamiteSpectatePreDragMouseIconEnabled
            S._dynamiteSpectatePreDragMouseIconEnabled = nil
        end
        if S._dynamiteSpectatePreviousType then
            camera.CameraType = S._dynamiteSpectatePreviousType
        else
            camera.CameraType = Enum.CameraType.Custom
        end
        if S._dynamiteSpectatePreviousSubject then
            camera.CameraSubject = S._dynamiteSpectatePreviousSubject
        else
            local localWorldCharacter = getWorldCharacterForPlayer(localPlayer) or localCharacter
            local localWorldHumanoid = localWorldCharacter and (localWorldCharacter:FindFirstChild("Humanoid") or localWorldCharacter:FindFirstChildOfClass("Humanoid"))
            local localWorldRoot = getTwwModelRoot(localWorldCharacter) or rootPart
            camera.CameraSubject = localWorldHumanoid or humanoid or localWorldRoot or camera.CameraSubject
        end
        if typeof(S._dynamiteSpectatePreviousCFrame) == "CFrame" then
            camera.CFrame = S._dynamiteSpectatePreviousCFrame
        end
    end)
    S._dynamiteSpectatePreviousType = nil
    S._dynamiteSpectatePreviousSubject = nil
    S._dynamiteSpectatePreviousCFrame = nil
    S._dynamiteSpectateLastMousePos = nil
    S._dynamiteSpectateRotateHeld = false
    S._dynamiteSpectateLastPopAmount = 0
    S._dynamiteSpectateLastZoomLevel = nil
    S._dynamiteSpectateZoomVelocity = 0
    S._dynamiteSpectateCameraActive = false
end

local function updateDynamiteSpectateCamera()
    if not (S.dynamiteSpectateEnabled and S.dynamiteTarget == "Player") then
        restoreDynamiteSpectateCamera()
        return
    end
    local camera = workspace.CurrentCamera
    if not camera then
        restoreDynamiteSpectateCamera()
        return
    end
    if not S._dynamiteSpectateCameraActive then
        S._dynamiteSpectatePreviousType = camera.CameraType
        S._dynamiteSpectatePreviousSubject = camera.CameraSubject
        S._dynamiteSpectatePreviousCFrame = camera.CFrame
    end
    if not S._dynamiteSpectateInputBeganConnection then
        S._dynamiteSpectateInputBeganConnection = _UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if destroyed or not isSessionActive() or gameProcessed then
                return
            end
            if not (S.dynamiteSpectateEnabled and S.dynamiteTarget == "Player") then
                return
            end
            if input.UserInputType == Enum.UserInputType.MouseButton2 then
                S._dynamiteSpectateRotateHeld = true
                S._dynamiteSpectateLastMousePos = nil
                S._dynamiteSpectatePreDragMouseBehavior = _UserInputService.MouseBehavior
                S._dynamiteSpectatePreDragMouseIconEnabled = _UserInputService.MouseIconEnabled
                pcall(function()
                    _UserInputService.MouseBehavior = Enum.MouseBehavior.LockCurrentPosition
                    _UserInputService.MouseIconEnabled = false
                end)
            end
        end)
    end
    if not S._dynamiteSpectateInputEndedConnection then
        S._dynamiteSpectateInputEndedConnection = _UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton2 then
                S._dynamiteSpectateRotateHeld = false
                S._dynamiteSpectateLastMousePos = nil
                pcall(function()
                    if S._dynamiteSpectatePreDragMouseBehavior ~= nil then
                        _UserInputService.MouseBehavior = S._dynamiteSpectatePreDragMouseBehavior
                    else
                        _UserInputService.MouseBehavior = Enum.MouseBehavior.Default
                    end
                    if S._dynamiteSpectatePreDragMouseIconEnabled ~= nil then
                        _UserInputService.MouseIconEnabled = S._dynamiteSpectatePreDragMouseIconEnabled
                    else
                        _UserInputService.MouseIconEnabled = true
                    end
                end)
                S._dynamiteSpectatePreDragMouseBehavior = nil
                S._dynamiteSpectatePreDragMouseIconEnabled = nil
            end
        end)
    end
    if not S._dynamiteSpectateInputConnection then
        S._dynamiteSpectateInputConnection = _UserInputService.InputChanged:Connect(function(input, gameProcessed)
            if destroyed or not isSessionActive() or gameProcessed then
                return
            end
            if not (S.dynamiteSpectateEnabled and S.dynamiteTarget == "Player") then
                return
            end
            if input.UserInputType == Enum.UserInputType.MouseWheel then
                local zoomDelta = tonumber(input.Position.Z) or 0
                local currentGoal = tonumber(S._dynamiteSpectateTargetDistance) or tonumber(S._dynamiteSpectateDistance) or 8
                S._dynamiteSpectateTargetDistance = math.clamp(currentGoal - zoomDelta * 1.75, 3, 24)
            elseif input.UserInputType == Enum.UserInputType.MouseMovement and S._dynamiteSpectateRotateHeld == true then
                local delta = input.Delta
                S._dynamiteSpectateYaw = (tonumber(S._dynamiteSpectateYaw) or 0) + delta.X * 0.00375
                S._dynamiteSpectatePitch = math.clamp((tonumber(S._dynamiteSpectatePitch) or math.rad(8)) + delta.Y * 0.00285, math.rad(-65), math.rad(65))
            end
        end)
    end
    if not S._dynamiteSpectateRenderConnection then
        S._dynamiteSpectateRenderConnection = _RunService.RenderStepped:Connect(function(dt)
            if destroyed or not isSessionActive() then
                restoreDynamiteSpectateCamera()
                return
            end
            if not (S.dynamiteSpectateEnabled and S.dynamiteTarget == "Player") then
                restoreDynamiteSpectateCamera()
                return
            end
            local liveCharacter, _, liveRootPart, liveFocusPart = getDynamiteSpectateTargetData()
            if not liveCharacter or not liveRootPart then
                return
            end
            local liveCamera = workspace.CurrentCamera
            if not liveCamera then
                return
            end
            local focusPosition = (liveFocusPart or liveRootPart).Position + Vector3.new(0, 0.35, 0)
            local distance = tonumber(S._dynamiteSpectateDistance)
            local yaw = tonumber(S._dynamiteSpectateYaw)
            local pitch = tonumber(S._dynamiteSpectatePitch)
            if not distance or not yaw or not pitch then
                local defaultOffset = (-liveRootPart.CFrame.LookVector * 8) + Vector3.new(0, 2.6, 0)
                local currentOffset = liveCamera.CFrame.Position - focusPosition
                if currentOffset.Magnitude < 3 or currentOffset.Magnitude > 30 then
                    currentOffset = defaultOffset
                end
                distance = math.clamp(currentOffset.Magnitude, 3, 24)
                yaw = math.atan2(currentOffset.Z, currentOffset.X)
                pitch = math.asin(math.clamp(currentOffset.Y / math.max(distance, 0.001), -1, 1))
                if not pitch or pitch ~= pitch then
                    pitch = math.rad(8)
                end
                S._dynamiteSpectateDistance = distance
                S._dynamiteSpectateTargetDistance = distance
                S._dynamiteSpectateYaw = yaw
                S._dynamiteSpectatePitch = pitch
                S._dynamiteSpectateZoomVelocity = 0
            end
            distance = S._stepDynamiteSpectateZoomSpring(dt)
            local orbitOffset = Vector3.new(
                math.cos(pitch) * math.cos(yaw),
                math.sin(pitch),
                math.cos(pitch) * math.sin(yaw)
            ) * distance
            local cameraPosition = focusPosition + orbitOffset
            pcall(function()
                liveCamera.CameraType = Enum.CameraType.Scriptable
                liveCamera.CFrame = S._resolveDynamiteSpectatePopper(liveCamera, CFrame.lookAt(cameraPosition, focusPosition), focusPosition, liveCharacter)
                liveCamera.Focus = CFrame.new(focusPosition)
            end)
        end)
    end
    S._dynamiteSpectateCameraActive = true
end

_Players.PlayerAdded:Connect(function()
    ensureValidDynamiteSelectedPlayer()
    S._espLeaderboardStateMap = nil
    pruneDynamiteMarkedPlayers()
    for _, syncFn in ipairs(_allDropdownSyncs) do
        pcall(syncFn)
    end
end)

_Players.PlayerRemoving:Connect(function(player)
    if player and S.dynamiteSelectedPlayer == player.Name then
        ensureValidDynamiteSelectedPlayer()
        schedulePersist()
    end
    if player then
        removeDynamiteMarkedPlayer(player.Name)
        S._staffCache[player.UserId] = nil
    end
    S._espLeaderboardStateMap = nil
    clearPlayerEspEntry(player)
    refreshDynamiteMarkVisuals()
    for _, syncFn in ipairs(_allDropdownSyncs) do
        pcall(syncFn)
    end
end)

do
local dynamiteActionAccumulator = 0
local dynamiteMarkAccumulator = 0
_RunService.Heartbeat:Connect(function(dt)
    if destroyed or not isSessionActive() then
        restoreDynamiteSpectateCamera()
        return
    end
    local frameDt = tonumber(dt) or 0.016
    dynamiteActionAccumulator = dynamiteActionAccumulator + frameDt
    dynamiteMarkAccumulator = dynamiteMarkAccumulator + frameDt
    if S.dynamiteTarget == "Player" and S.dynamiteMarkPlayersEnabled and dynamiteMarkAccumulator >= 0.25 then
        dynamiteMarkAccumulator = 0
        pruneDynamiteMarkedPlayers()
    end
    updateDynamiteSpectateCamera()
    if dynamiteActionAccumulator < 0.05 then
        return
    end
    dynamiteActionAccumulator = 0
    if not S.dynamiteTpEnabled then
        return
    end

    local playersFolder = getTwwEntityPlayersFolder()
    if not playersFolder then
        return
    end

    local localModel = playersFolder:FindFirstChild(localPlayer and localPlayer.Name or "")
    local localRoot = getTwwModelRoot(localModel)
    local destCFrame, targetFound = getTwwDynamiteTargetCFrame()
    if not localRoot then
        return
    end

    if not (S.dynamiteTpEnabled and targetFound and destCFrame) then
        return
    end

    if S.dynamiteStyle == "Silent" then
        local ignoreFolder = workspace:FindFirstChild("Ignore")
        if not ignoreFolder then
            return
        end
        for _, obj in ipairs(ignoreFolder:GetChildren()) do
            if obj.Name == "Dynamite" and obj:IsA("Model") then
                local root = getTwwModelRoot(obj)
                if root then
                    pcall(function()
                        obj:PivotTo(destCFrame)
                        root.AssemblyLinearVelocity = Vector3.new(0, 0.001, 0)
                        root.AssemblyAngularVelocity = Vector3.zero
                    end)
                end
            end
        end
    else
        for _, child in ipairs(localModel:GetChildren()) do
            if child.Name == "Dynamite" and child:IsA("Model") then
                local root = getTwwModelRoot(child)
                if root then
                    pcall(function()
                        child:PivotTo(destCFrame)
                        root.AssemblyLinearVelocity = Vector3.new(0, 0.001, 0)
                        root.AssemblyAngularVelocity = Vector3.zero
                    end)
                end
            end
        end
    end
end)
end

local function _repositionNotifications()
    for index, entry in ipairs(_activeNotifications) do
        local stackIndex = #_activeNotifications - index
        local targetY = -(_NOTIF_BOTTOM_MARGIN + stackIndex * (_NOTIF_HEIGHT + _NOTIF_GAP))
        _TweenService:Create(entry.container, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
            Position = UDim2.new(1, -_NOTIF_RIGHT_MARGIN, 1, targetY),
        }):Play()
    end
end

local function _removeNotification(entry)
    for index, activeEntry in ipairs(_activeNotifications) do
        if activeEntry == entry then
            table.remove(_activeNotifications, index)
            break
        end
    end
    _repositionNotifications()
end

local function spawnNotification(message, notifType, force, duration, messageUpdater)
    if destroyed or not isSessionActive() then
        return
    end
    if not force and not S.notificationsEnabled then
        return
    end
    notifType = notifType or "info"
    message = tostring(message or ("Press " .. guiToggleKeyName .. " to hide GUI"))
    local typeConfig = _NOTIF_TYPES[notifType] or _NOTIF_TYPES.info

    local notifContainer = Instance.new("Frame")
    notifContainer.Size = UDim2.new(0, _NOTIF_WIDTH, 0, _NOTIF_HEIGHT)
    notifContainer.AnchorPoint = Vector2.new(1, 1)
    notifContainer.BackgroundColor3 = Color3.fromRGB(25, 28, 35)
    notifContainer.BackgroundTransparency = 1
    notifContainer.BorderSizePixel = 0
    notifContainer.ClipsDescendants = true
    notifContainer.Active = true
    notifContainer.ZIndex = 180
    notifContainer.Parent = overlayRoot
    Instance.new("UICorner", notifContainer).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(50, 55, 65)
    stroke.Transparency = 1
    stroke.Parent = notifContainer

    local thisEntry = {container = notifContainer}
    table.insert(_activeNotifications, thisEntry)
    local startY = -_NOTIF_BOTTOM_MARGIN - 25
    notifContainer.Position = UDim2.new(1, -_NOTIF_RIGHT_MARGIN, 1, startY)
    _repositionNotifications()

    local circleBg = Instance.new("ImageLabel")
    circleBg.Size = UDim2.new(0, 48, 0, 48)
    circleBg.Position = UDim2.new(0, 12, 0.5, -24)
    circleBg.BackgroundTransparency = 1
    circleBg.BorderSizePixel = 0
    circleBg.Image = _NOTIF_CIRCLE_BG
    circleBg.ImageColor3 = typeConfig.color
    circleBg.ImageTransparency = 1
    circleBg.ScaleType = Enum.ScaleType.Fit
    circleBg.ZIndex = 181
    circleBg.Parent = notifContainer
    Instance.new("UICorner", circleBg).CornerRadius = UDim.new(1, 0)

    local circleGlow = Instance.new("UIStroke")
    circleGlow.Color = typeConfig.color
    circleGlow.Thickness = 2
    circleGlow.Transparency = 1
    circleGlow.Parent = circleBg

    local iconImage = Instance.new("ImageLabel")
    iconImage.Size = UDim2.new(0, 22, 0, 22)
    iconImage.Position = UDim2.new(0.5, -11, 0.5, -11)
    iconImage.BackgroundTransparency = 1
    iconImage.BorderSizePixel = 0
    iconImage.Image = typeConfig.iconId
    iconImage.ImageColor3 = typeConfig.iconColor
    iconImage.ImageTransparency = 1
    iconImage.ScaleType = Enum.ScaleType.Fit
    iconImage.ZIndex = 182
    iconImage.Parent = circleBg

    local typeTitleLabel = Instance.new("TextLabel")
    typeTitleLabel.Size = UDim2.new(1, -85, 0, 20)
    typeTitleLabel.Position = UDim2.new(0, 72, 0, 8)
    typeTitleLabel.BackgroundTransparency = 1
    typeTitleLabel.Text = typeConfig.title
    typeTitleLabel.TextColor3 = typeConfig.color
    typeTitleLabel.Font = Enum.Font.GothamBold
    typeTitleLabel.TextSize = 14
    typeTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    typeTitleLabel.TextYAlignment = Enum.TextYAlignment.Center
    typeTitleLabel.TextTransparency = 1
    typeTitleLabel.ZIndex = 183
    typeTitleLabel.Parent = notifContainer

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -85, 0, _NOTIF_HEIGHT - 32)
    titleLabel.Position = UDim2.new(0, 72, 0, 28)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = message
    titleLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    titleLabel.Font = Enum.Font.Gotham
    titleLabel.TextSize = 13
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.TextYAlignment = Enum.TextYAlignment.Top
    titleLabel.TextTransparency = 1
    titleLabel.TextWrapped = true
    titleLabel.ZIndex = 183
    titleLabel.Parent = notifContainer

    local timerTrack = Instance.new("Frame")
    timerTrack.Size = UDim2.new(1, -10, 0, 3)
    timerTrack.Position = UDim2.new(0, 5, 1, -6)
    timerTrack.BackgroundTransparency = 1
    timerTrack.BorderSizePixel = 0
    timerTrack.ZIndex = 184
    timerTrack.Parent = notifContainer

    local timerBar = Instance.new("Frame")
    timerBar.Size = UDim2.new(1, 0, 1, 0)
    timerBar.Position = UDim2.new(0, 0, 0, 0)
    timerBar.BackgroundColor3 = typeConfig.color
    timerBar.BorderSizePixel = 0
    timerBar.BackgroundTransparency = 1
    timerBar.ZIndex = 185
    timerBar.Parent = timerTrack
    Instance.new("UICorner", timerBar).CornerRadius = UDim.new(0, 2)

    local hoverPaused = false
    local notificationDuration = math.max(0.25, tonumber(duration) or _NOTIF_DURATION)
    local timerRemaining = notificationDuration
    local timerStarted = false
    local timerConnection = nil
    local dismissing = false

    local function updateTimerBar()
        local fillPercent = 0
        if notificationDuration > 0 then
            fillPercent = math.clamp(timerRemaining / notificationDuration, 0, 1)
        end
        timerBar.Size = UDim2.new(fillPercent, 0, 1, 0)
    end

    local function stopTimerLoop()
        if timerConnection then
            timerConnection:Disconnect()
            timerConnection = nil
        end
    end

    local function dismissNotification()
        if dismissing then
            return
        end
        dismissing = true
        stopTimerLoop()
        if destroyed or not notifContainer.Parent then
            if notifContainer.Parent then
                notifContainer:Destroy()
            end
            _removeNotification(thisEntry)
            return
        end
        local fadeOffset = -30
        local myIndex
        for index, entry in ipairs(_activeNotifications) do
            if entry == thisEntry then
                myIndex = index
                break
            end
        end
        if myIndex and #_activeNotifications > 1 and myIndex < #_activeNotifications then
            fadeOffset = 30
        end
        local currentPosition = notifContainer.Position
        _TweenService:Create(notifContainer, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {
            BackgroundTransparency = 1,
            Position = UDim2.new(currentPosition.X.Scale, currentPosition.X.Offset, currentPosition.Y.Scale, currentPosition.Y.Offset + fadeOffset),
        }):Play()
        _TweenService:Create(stroke, TweenInfo.new(0.35), {Transparency = 1}):Play()
        _TweenService:Create(circleBg, TweenInfo.new(0.3), {ImageTransparency = 1}):Play()
        _TweenService:Create(circleGlow, TweenInfo.new(0.3), {Transparency = 1}):Play()
        _TweenService:Create(iconImage, TweenInfo.new(0.3), {ImageTransparency = 1}):Play()
        _TweenService:Create(typeTitleLabel, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
        _TweenService:Create(titleLabel, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
        _TweenService:Create(timerBar, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        task.wait(0.5)
        _removeNotification(thisEntry)
        if notifContainer.Parent then
            notifContainer:Destroy()
        end
    end

    notifContainer.MouseEnter:Connect(function()
        hoverPaused = true
    end)

    notifContainer.MouseLeave:Connect(function()
        hoverPaused = false
    end)

    notifContainer.AncestryChanged:Connect(function(_, parent)
        if not parent then
            stopTimerLoop()
        end
    end)

    _UTILS._playSound(_NOTIF_SOUND, 3.5)

    task.spawn(function()
        if destroyed or not notifContainer.Parent or dismissing then
            return
        end
        _TweenService:Create(notifContainer, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
            BackgroundTransparency = 0,
        }):Play()
        _TweenService:Create(stroke, TweenInfo.new(0.4), {Transparency = 0.5}):Play()
        _TweenService:Create(circleBg, TweenInfo.new(0.4), {ImageTransparency = 0.65}):Play()
        _TweenService:Create(circleGlow, TweenInfo.new(0.4), {Transparency = 0.3}):Play()
        _TweenService:Create(iconImage, TweenInfo.new(0.4), {ImageTransparency = 0}):Play()
        typeTitleLabel.TextTransparency = 0
        titleLabel.TextTransparency = 0
        task.spawn(function()
            _UTILS._animateNotifText(typeTitleLabel, typeConfig.title)
        end)
        task.spawn(function()
            _UTILS._animateNotifText(titleLabel, message)
        end)
        _TweenService:Create(timerBar, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()
        task.wait(0.3)
        if not timerBar.Parent or dismissing then
            return
        end
        timerStarted = true
        updateTimerBar()
        timerConnection = _RunService.Heartbeat:Connect(function(dt)
            if destroyed or dismissing or not notifContainer.Parent then
                stopTimerLoop()
                return
            end
            if type(messageUpdater) == "function" then
                local okText, updatedMessage = pcall(messageUpdater)
                if okText then
                    updatedMessage = tostring(updatedMessage or message)
                    if updatedMessage ~= message then
                        message = updatedMessage
                        titleLabel.Text = updatedMessage
                    end
                end
            end
            if not timerStarted or hoverPaused then
                return
            end
            timerRemaining = math.max(0, timerRemaining - dt)
            updateTimerBar()
            if timerRemaining <= 0 then
                task.spawn(dismissNotification)
            end
        end)
    end)
end

function evaluateTwwStaffPlayer(player)
    if not player or player == localPlayer then
        return false
    end

    local cached = S._staffCache[player.UserId]
    if cached ~= nil then
        return cached
    end

    local roleName = nil
    local okRole = pcall(function()
        roleName = player:GetRoleInGroup(S._staffGroupId)
    end)

    if okRole and roleName and S._staffRoles[roleName] then
        cached = {
            roleName = roleName,
        }
    else
        cached = false
    end

    S._staffCache[player.UserId] = cached
    return cached
end

function checkTwwStaffPlayer(player)
    if not player or player == localPlayer then
        return
    end

    task.spawn(function()
        local staffData = evaluateTwwStaffPlayer(player)
        if not staffData then
            return
        end

        if S.staffNotifyEnabled then
            spawnNotification("STAFF ALERT: " .. player.Name .. " (" .. tostring(staffData.roleName or "Staff") .. ") is in your server.", "warning", true)
        end
        if pathState and pathState.staffHopEnabled then
            local resumeSlotIndex = getPathCycleRestartSlotIndex()
            if requestPathServerHop("Staff detected, hopping servers...", resumeSlotIndex, true, "staff") then
                return
            end
        end
        if S.staffKickEnabled then
            localPlayer:Kick("Staff detected in your server: " .. player.Name .. " (" .. tostring(staffData.roleName or "Staff") .. ")")
        end
    end)
end

function scanCurrentTwwStaff(mode)
    for _, player in ipairs(_Players:GetPlayers()) do
        if player ~= localPlayer then
            local staffData = evaluateTwwStaffPlayer(player)
            if staffData then
                if mode == "notify" then
                    spawnNotification("STAFF ALERT: " .. player.Name .. " (" .. tostring(staffData.roleName or "Staff") .. ") is in your server.", "warning", true)
                    if pathState and pathState.staffHopEnabled then
                        local resumeSlotIndex = getPathCycleRestartSlotIndex()
                        if requestPathServerHop("Staff detected, hopping servers...", resumeSlotIndex, true, "staff") then
                            return
                        end
                    end
                elseif mode == "kick" then
                    localPlayer:Kick("Staff detected in your server: " .. player.Name .. " (" .. tostring(staffData.roleName or "Staff") .. ")")
                    return
                end
            end
        end
    end
end

_Players.PlayerAdded:Connect(function(player)
    checkTwwStaffPlayer(player)
end)

task.spawn(function()
    for _, player in ipairs(_Players:GetPlayers()) do
        if player ~= localPlayer then
            task.spawn(function()
                evaluateTwwStaffPlayer(player)
            end)
        end
    end
end)

local activeTabName = "Home"
local AUTOMINEBtn, AUTOMINEIndicator, AUTOMINELabel
local clearPathVisualization
local clearRecordedPathVisuals
local stopAutoMine
local refreshPathVisuals
local refreshPathManagerButtons
local setSavedPathDropdownOpen
local UI_COLORS
local ensureWorkingPathStartNode
local getPathRecordNodeSpacing
local setPathRecordNodeSpacing
local MINE_RANGE = 7
local PATH_RECORD_NODE_SPACING = 9
local MINE_AURA_RANGE = 15

do
-- Automine runtime ported from automine_gui.lua
local Players = game:GetService("Players")
local PathfindingService = game:GetService("PathfindingService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")

local CoreGui = nil
pcall(function()
	CoreGui = game:GetService("CoreGui")
end)

local VirtualInputManager = nil
pcall(function()
	VirtualInputManager = game:GetService("VirtualInputManager")
end)

LocalPlayer = Players.LocalPlayer

GUI_NAME = "WildWestAutoMineGui"
PICKAXE_NAME = "BasicPickaxe"
STORED_PICKAXE_NAME = "LoadoutItem/BasicPickaxe"
PICKAXE_EQUIP_REQUEST_COOLDOWN = 0.45
PICKAXE_LOADOUT_REQUEST_COOLDOWN = 0.9
AUTO_MINE_INTERVAL = 0.5
RESCAN_INTERVAL = 2
TOGGLE_DEBOUNCE = 0.25
DEFAULT_AUTOFARM_KEY = Enum.KeyCode.F
DEFAULT_GUI_TOGGLE_KEY = Enum.KeyCode.RightShift
MINE_RANGE = 7
MOVE_TIMEOUT = 2.5
WAYPOINT_REACH_DISTANCE = 4
WALK_PROGRESS_TIMEOUT = 2.25
PATH_WAYPOINT_SPACING = 2
PATH_SEGMENT_DISTANCE = 110
PATH_REPLAN_ATTEMPTS = 8
RECORDED_PATH_FOLDER_NAME = "AutoMineRecordedPathViz"
PATH_RECORD_NODE_SPACING = 9
PATH_RECORD_NODE_SIZE = Vector3.new(0.85, 0.85, 0.85)
PATH_RECORD_SELL_DISTANCE = 48
PATH_SELL_APPROACH_DISTANCE = 5
PATH_SPAWN_SELECTOR_MIN_MATCHES = 2
PATH_SPAWN_SELECTOR_WAIT_TIMEOUT = 6
PATH_STORAGE_FOLDER = "WildWestAutoMine"
PATH_STORAGE_FILE_PREFIX = "paths"
TELEPORT_SCRIPT_PATH_FILE_PREFIX = "teleport_script_path"
TELEPORT_SCRIPT_EMBEDDED_FALLBACK_PATH = "C:\\Users\\Yonis\\Downloads\\dundun\\bestversionautomine_work (2) (1).txt"
TELEPORT_QUEUE_RETRY_ATTEMPTS = 60
TELEPORT_QUEUE_RETRY_DELAY = 1
PICKUP_RANGE = 6
AUTO_PICKUP_MAX_DISTANCE = 45
PICKUP_HOLD_SECONDS = 0.9
AUTO_LAST_SLOT_ORE_DISCARD_SCAN_INTERVAL = 0.18
AUTO_LAST_SLOT_ORE_DISCARD_COOLDOWN = 0.7
AUTO_LAST_SLOT_ORE_DISCARD_CONFIRM_TIMEOUT = 0.45
SELL_HOLD_SECONDS = 0.8
VISUALIZER_FOLDER_NAME = "AutoMinePathViz"
PLAYER_AVOIDER_VISUALIZER_FOLDER_NAME = "AutoMinePathAvoiderViz"
VISUALIZER_HEIGHT = 0.2
MOVE_DIRECTION_DEADZONE = 0.18
MOVE_SIDE_DEADZONE = 0.2
TWEEN_MOVE_SPEED = 20
TWEEN_SPEED_MIN = 1
TWEEN_SPEED_MAX = 90
MINE_AURA_MULTIPLIER = 1.0
TWEEN_GROUND_SCAN_HEIGHT = 30
TWEEN_GROUND_CLEARANCE = 3.5
RUBBERBAND_RETRY_DELAY = 0.6
RUBBERBAND_NOTIFY_COOLDOWN = 1.2
RUBBERBAND_BACKTRACK_DISTANCE = 3.25
RUBBERBAND_STEP_DEVIATION_DISTANCE = 5.5
PATH_PLAYER_AVOIDER_SCAN_INTERVAL = 0.1
PATH_PLAYER_AVOIDER_CORRIDOR_RADIUS = 32.5
PATH_PLAYER_AVOIDER_LAYER_TOLERANCE = 7
PATH_PLAYER_AVOIDER_WAIT_STEP = 0.08
PATH_PLAYER_AVOIDER_BACKUP_TRIGGER_DISTANCE = 9
PATH_PLAYER_AVOIDER_BACKUP_STEP_DISTANCE = 4.75
PATH_PLAYER_AVOIDER_EMERGENCY_DISTANCE = 13.5
PATH_PLAYER_AVOIDER_VIZ_REFRESH_INTERVAL = 0.12
PATH_PLAYER_AVOIDER_RADIUS_TOUCH_MARGIN = 2.75
PATH_PLAYER_AVOIDER_RADIUS_MIN_SEGMENTS = 24
PATH_PLAYER_AVOIDER_RADIUS_MAX_SEGMENTS = 60
AUTOMINE_INPUT_LOCK_ACTION_NAME = "FobAutoMineInputLock"
MAX_MINE_NETWORK_REACH = 65
MINE_AURA_RANGE = 15
TOOLINFO_GC_SCAN_BUDGET = 0.015
TOOLINFO_DIRECT_SCAN_BUDGET = 0.03
AURA_FORCE_SCAN_INTERVAL = 6
PATH_VIZ_REFRESH_INTERVAL = 0.75
PATH_VIZ_TARGET_EPSILON = 2
INTERACT_KEY = Enum.KeyCode.E
INTERACT_SECONDARY_KEY = Enum.KeyCode.F
SERVER_HOP_PAGE_LIMIT = 4
SERVER_HOP_PAGE_SIZE = 100

-- If the game uses a numbered hotbar to equip the pickaxe, set this to the slot key.
-- Example: Enum.KeyCode.One, Enum.KeyCode.Two, etc.
PICKAXE_HOTBAR_KEY = nil
HOTBAR_KEYS_TO_TRY = {
	Enum.KeyCode.One,
	Enum.KeyCode.Two,
	Enum.KeyCode.Three,
	Enum.KeyCode.Four,
	Enum.KeyCode.Five,
}

ORE_OPTIONS = {
	{ id = "Coal", label = "Coal" },
	{ id = "Copper", label = "Copper" },
	{ id = "Zinc", label = "Zinc" },
	{ id = "Iron", label = "Iron" },
	{ id = "Silver", label = "Silver" },
	{ id = "Gold", label = "Gold" },
	{ id = "Limestone", label = "Limestone" },
	{ id = "Quartz", label = "Quartz" },
	{ id = "CoalVein", label = "Coal Vein" },
	{ id = "CopperVein", label = "Copper Vein" },
	{ id = "ZincVein", label = "Zinc Vein" },
	{ id = "IronVein", label = "Iron Vein" },
	{ id = "SilverVein", label = "Silver Vein" },
	{ id = "GoldVein", label = "Gold Vein" },
}

ORE_LABELS = {}
for _, oreOption in ipairs(ORE_OPTIONS) do
	ORE_LABELS[oreOption.id] = oreOption.label
end

INVENTORY_ORE_OPTIONS = {
	{ id = "Diamond", label = "Diamond" },
	{ id = "Emerald", label = "Emerald" },
	{ id = "Sapphire", label = "Sapphire" },
	{ id = "Ruby", label = "Ruby" },
	{ id = "GoldOre", label = "Gold Ore" },
	{ id = "SilverOre", label = "Silver Ore" },
	{ id = "QuartzOre", label = "Quartz Ore" },
	{ id = "Limestone", label = "Limestone" },
	{ id = "IronOre", label = "Iron Ore" },
	{ id = "ZincOre", label = "Zinc Ore" },
	{ id = "CopperOre", label = "Copper Ore" },
	{ id = "CoalOre", label = "Coal Ore" },
}

INVENTORY_ORE_LABELS = {}
for _, oreOption in ipairs(INVENTORY_ORE_OPTIONS) do
	INVENTORY_ORE_LABELS[oreOption.id] = oreOption.label
end

KEYCODE_DISPLAY_NAMES = {
	[Enum.KeyCode.LeftShift] = "Left Shift",
	[Enum.KeyCode.RightShift] = "Right Shift",
	[Enum.KeyCode.LeftControl] = "Left Ctrl",
	[Enum.KeyCode.RightControl] = "Right Ctrl",
	[Enum.KeyCode.Return] = "Enter",
	[Enum.KeyCode.Backquote] = "Backquote",
}

AUTO_PICKUP_OPTIONS = {
	{ id = "CoalOre", label = "Coal Ore" },
	{ id = "CopperOre", label = "Copper Ore" },
	{ id = "ZincOre", label = "Zinc Ore" },
	{ id = "IronOre", label = "Iron Ore" },
	{ id = "SilverOre", label = "Silver Ore" },
	{ id = "GoldOre", label = "Gold Ore" },
	{ id = "QuartzOre", label = "Quartz Ore" },
	{ id = "Limestone", label = "Limestone" },
	{ id = "Diamond", label = "Uncut Diamond" },
	{ id = "Emerald", label = "Uncut Emerald" },
	{ id = "Sapphire", label = "Uncut Sapphire" },
	{ id = "Ruby", label = "Uncut Ruby" },
	{ id = "Amethyst", label = "Uncut Amethyst" },
}

AUTO_PICKUP_LABELS = {}
for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
	AUTO_PICKUP_LABELS[pickupOption.id] = pickupOption.label
end

autoMineEnabled = false
autoMineThread = nil
autoMineToken = 0
cachedCharacter = nil
cachedHumanoid = nil
cachedPickaxe = nil
lastPickaxeEquipRequestAt = 0
lastPickaxeEquipRequestId = nil
lastPickaxeLoadoutRequestAt = 0
lastPickaxeLoadoutRequestId = nil
lastRescanAt = 0
cachedEquipRemote = nil
lastEquipRemoteScanAt = 0
cachedMineRemote = nil
cachedMineRemotes = nil
lastMineRemoteScanAt = 0
cachedAutoSellTargets = nil
lastAutoSellTargetScanAt = 0
cachedGameModules = nil
cachedMainModule = nil
cachedSyncedTime = nil
lastModuleScanAt = 0
cachedToolInfoOwner = nil
lastToolInfoOwnerScanAt = 0
cachedDirectToolInfo = nil
lastDirectToolInfoScanAt = 0
lastMineAuraNetworkHitAt = 0
lastMineAuraDebugAt = 0
lastAuraForceScanAt = 0
lastAuraProbeAt = 0
auraCapabilitiesReported = false
screenGui = nil
frame = nil
headerSubtitleLabel = nil
statusLabel = nil
debugLabel = nil
movementDebugLabel = nil
toggleButton = nil
oreDropdownButton = nil
oreDropdownFrame = nil
tweenButton = nil
movementModeDropdownFrame = nil
movementModeLegitButton = nil
movementModeTweenButton = nil
movementModeCFrameButton = nil
mineAuraButton = nil
autoPickupButton = nil
tweenSpeedBox = nil
guiKeybindButton = nil
autoFarmKeybindButton = nil
settingsHintLabel = nil
tabButtons = {}
tabPages = {}
playerUi = {}
oreOptionButtons = {}
pickupWhitelistButton = nil
pickupWhitelistDropdownButton = nil
pickupWhitelistDropdownFrame = nil
pickupWhitelistOptionButtons = {}
autofarmPageRef = nil
autoFarmCardFrame = nil
movementCardFrame = nil
pickupCardFrame = nil
liveFeedCardFrame = nil
autoSellCardFrame = nil
autoSellButton = nil
autoSellThresholdLabel = nil
autoSellInventoryLabel = nil
autoSellSliderHitbox = nil
autoSellSliderFill = nil
autoSellSliderKnob = nil
serverHopButton = nil
serverHopStatusLabel = nil
lastToggleAt = 0
ignoreToggleUntil = 0
debugLines = {}
debugCooldowns = {}
visualizerFolder = nil
playerAvoiderVisualizerFolder = nil
lastPathVizAt = 0
lastPathVizTarget = nil
lastPathVizCount = 0
lastPlayerAvoiderVizAt = 0
lastPlayerAvoiderVizSignature = nil
lastMoveCommand = Vector3.zero
lastMoveCommandSource = "idle"
currentTargetOre = nil
lastRootSamplePosition = nil
lastRootSampleAt = 0
lastRubberBandNoticeAt = 0
movementMode = FORCE_AUTOMINE_CFRAME_DEFAULT and "cframe" or "legit"
tweenModeEnabled = false
mineAuraEnabled = false
autoPickupEnabled = false
tweenMoveSpeed = TWEEN_MOVE_SPEED
activeMoveTween = nil
auraOriginalParts = {}
autoMineLockedControls = nil
autoMineLockedHumanoid = nil
autoMineLockedWalkSpeed = nil
autoMineLockedUseJumpPower = nil
autoMineLockedJumpPower = nil
autoMineLockedJumpHeight = nil
autoMineLockedAutoRotate = nil
autoMineInputLockBound = false
activeTabName = "Autofarm"
pendingKeybindAction = nil
oreDropdownOpen = false
movementModeDropdownOpen = false
autoFarmKeybind = DEFAULT_AUTOFARM_KEY
guiToggleKeybind = DEFAULT_GUI_TOGGLE_KEY
autoPickupWhitelistEnabled = false
pickupWhitelistDropdownOpen = false
autoSellEnabled = false
autoSellThreshold = 0
autoSellSliderDragging = false
serverHopEnabled = false
serverHopInProgress = false
serverHopTaskQueued = false
playerState = {
	infiniteStaminaEnabled = S.infiniteStaminaEnabled,
	safeTeleportEnabled = S.safeTeleportEnabled,
	noJumpCooldownEnabled = S.noJumpCooldownEnabled,
	noFallDamageEnabled = S.noFallDamageEnabled,
	noRagdollEnabled = S.noRagdollEnabled,
	autoBreakFreeEnabled = S.autoBreakFreeEnabled,
	autoGetUpEnabled = S.autoGetUpEnabled,
	equipWhileSwimmingEnabled = S.equipWhileSwimmingEnabled,
	displayStamina = 100,
	hookedNetwork = nil,
	originalNetworkFireServer = nil,
	networkFireServerWrapper = nil,
	stateHooks = {},
	hookRefreshActive = false,
	jumpHookHumanoid = nil,
	jumpHookConnections = {},
	breakFreeOwner = nil,
	breakFreeHook = nil,
	hookedSafeTeleportHandler = nil,
	originalSafeTeleportPlatformUpdate = nil,
	safeTeleportPlatformWrapper = nil,
	originalSafeTeleportReplicateCharacters = nil,
	safeTeleportReplicateWrapper = nil,
	hookedSafeTeleportFlagsMetatable = nil,
	originalSafeTeleportFlagsNewIndex = nil,
	safeTeleportFlagsNewIndexWrapper = nil,
	hookedSafeTeleportRagdollOwner = nil,
	originalSafeTeleportRagdoll = nil,
	safeTeleportRagdollWrapper = nil,
	hookedSwimOwner = nil,
	originalSwimCanSwitchToItem = nil,
	swimCanSwitchWrapper = nil,
	originalSwimUpdate = nil,
	swimUpdateWrapper = nil,
}
playerEspState = {
    renderConnection = nil,
    entries = {},
    warnedUnavailable = false,
}
LASSO_SILENT_AIM_MAX_DISTANCE = 200
LASSO_FAST_THROW_MULTIPLIER = 3.5
LASSO_FAST_HIT_CONFIRM_DELAY = 0.12
gunFeatureState = {
    renderConnection = nil,
    warnedAimUnavailable = false,
    currentTargetInfo = nil,
    currentLassoTargetInfo = nil,
    lastProjectileTravelTime = nil,
    fovCircle = nil,
    fovSquare = nil,
    fovPolygonLines = {},
    snapline = nil,
    lassoSnapline = nil,
    trackedFastProjectileBullets = {},
    hookedCamModule = nil,
    originalCamAddRecoil = nil,
    camAddRecoilWrapper = nil,
    originalCamVibrate = nil,
    camVibrateWrapper = nil,
    hookedGunItemModule = nil,
    originalGunCanShoot = nil,
    gunCanShootWrapper = nil,
    hookedProjectileHandler = nil,
    originalProjectileRaycast = nil,
	projectileRaycastWrapper = nil,
	fastReloadItem = nil,
	fastReloadOriginalPlayTrack = nil,
	fastReloadWrapper = nil,
    fastReloadHooks = {},
    lassoChargeOverrides = {},
    lassoSilentAimHooks = {},
    lassoParams = nil,
    originalLassoMaxLength = nil,
}

GUN_RELOAD_TRACK_NAMES = {
    LoadStart = true,
    Load = true,
    LoadEnd = true,
    Reload = true,
    ReloadStart = true,
    ReloadEnd = true,
}

function applyStoredStateToRuntime()
    if type(playerState) == "table" then
        playerState.infiniteStaminaEnabled = S.infiniteStaminaEnabled == true
        playerState.safeTeleportEnabled = S.safeTeleportEnabled == true
        playerState.noJumpCooldownEnabled = S.noJumpCooldownEnabled == true
        playerState.noFallDamageEnabled = S.noFallDamageEnabled == true
        playerState.noRagdollEnabled = S.noRagdollEnabled == true
        playerState.autoBreakFreeEnabled = S.autoBreakFreeEnabled == true
        playerState.autoGetUpEnabled = S.autoGetUpEnabled == true
        playerState.equipWhileSwimmingEnabled = S.equipWhileSwimmingEnabled == true
    end

    _G.__AutoMineNoRecoilEnabled = S.noRecoilEnabled == true

    S.playerEspMethods = clonePlayerEspMethods(S.playerEspMethods)
    S.silentAimTargetPriority = cloneGunTargetPriority(S.silentAimTargetPriority)
    if type(S.keybinds) ~= "table" then
        S.keybinds = {}
    end
    if not S.keybinds.dynamiteMark then
        S.keybinds.dynamiteMark = Enum.KeyCode.F
    end
    if S.silentAimBodyTarget ~= "Torso" then
        S.silentAimBodyTarget = "Head"
    end
    if not table.find(GUN_AIM_MODE_ORDER, tostring(S.silentAimMode or "")) then
        S.silentAimMode = "FOV"
    end
    S.silentAimVisibilityCheck = S.silentAimVisibilityCheck == true
    if not table.find(GUN_FOV_SHAPE_ORDER, tostring(S.gunFovShape or "")) then
        S.gunFovShape = "Circle"
    end
    S.instantLassoChargeEnabled = S.instantLassoChargeEnabled == true
    S.lassoSilentAimEnabled = S.lassoSilentAimEnabled == true
    S.lassoSnaplineEnabled = S.lassoSnaplineEnabled == true
    S.lassoNoRopeLimitEnabled = S.lassoNoRopeLimitEnabled == true
    S.fasterLassoEnabled = S.fasterLassoEnabled == true
    S.silentAimAccuracy = math.clamp(math.floor((tonumber(S.silentAimAccuracy) or 50) + 0.5), 1, 100)
    S.silentAimDistanceLimit = math.clamp(math.floor((tonumber(S.silentAimDistanceLimit) or 1000) + 0.5), 10, 20000)
    S.gunFovRadius = math.clamp(math.floor((tonumber(S.gunFovRadius) or 200) + 0.5), 10, 2000)
    S.pathRecordNodeSpacing = math.clamp(math.floor(((tonumber(S.pathRecordNodeSpacing) or DEFAULTS.pathRecordNodeSpacing) * 10) + 0.5) / 10, 1, 10)
    S.pathPlayerAvoiderEnabled = S.pathPlayerAvoiderEnabled == true
    S.pathPlayerAvoiderDistance = math.clamp(math.floor((tonumber(S.pathPlayerAvoiderDistance) or DEFAULTS.pathPlayerAvoiderDistance) + 0.5), 1, 200)
    S.pathPlayerAvoiderShowCorridor = S.pathPlayerAvoiderShowCorridor == true
    S.pathPlayerAvoiderMethod = normalizePathPlayerAvoiderMethod(S.pathPlayerAvoiderMethod)
    S.pathPlayerAvoiderAction = normalizePathPlayerAvoiderAction(S.pathPlayerAvoiderAction)
    S.pathPlayerAvoiderRadius = math.clamp(math.floor((tonumber(S.pathPlayerAvoiderRadius) or DEFAULTS.pathPlayerAvoiderRadius) + 0.5), 5, 200)
	S.pathPlayerAvoiderShowRadius = S.pathPlayerAvoiderShowRadius ~= false
	S.pathHopOnDeathEnabled = S.pathHopOnDeathEnabled == true
	S.discordAutomineWebhookTrackingEnabled = S.discordAutomineWebhookTrackingEnabled == true
	S.discordAutomineWebhookUrl = tostring(S.discordAutomineWebhookUrl or "")
	S.discordAutomineWebhookOptions = cloneAutomineWebhookOptions(S.discordAutomineWebhookOptions)
	if type(isDiscordAutomineWebhookUrlValid) == "function" and not isDiscordAutomineWebhookUrlValid(S.discordAutomineWebhookUrl) then
		S.discordAutomineWebhookTrackingEnabled = false
	end
	PATH_RECORD_NODE_SPACING = S.pathRecordNodeSpacing

    if type(pathState) == "table" then
        pathState.playerAvoiderEnabled = S.pathPlayerAvoiderEnabled
        pathState.playerAvoiderDistance = S.pathPlayerAvoiderDistance
        pathState.playerAvoiderShowCorridor = S.pathPlayerAvoiderShowCorridor
        pathState.playerAvoiderMethod = S.pathPlayerAvoiderMethod
        pathState.playerAvoiderAction = S.pathPlayerAvoiderAction
        pathState.playerAvoiderRadius = S.pathPlayerAvoiderRadius
        pathState.playerAvoiderShowRadius = S.pathPlayerAvoiderShowRadius
        pathState.hopOnDeathEnabled = S.pathHopOnDeathEnabled
        if not pathState.playerAvoiderEnabled and type(clearPathPlayerAvoiderDetection) == "function" then
            clearPathPlayerAvoiderDetection()
        elseif ((pathState.playerAvoiderMethod == "Corridor" and not pathState.playerAvoiderShowCorridor) or (pathState.playerAvoiderMethod == "Radius Field" and not pathState.playerAvoiderShowRadius))
            and type(clearPathPlayerAvoiderCorridorVisualization) == "function" then
            clearPathPlayerAvoiderCorridorVisualization()
        end
    end

    if S.playerEspEnabled then
        ensurePlayerEspRenderLoop()
    else
        for _, entry in pairs(playerEspState.entries) do
            hidePlayerEspEntry(entry)
        end
    end

    if not (S.dynamiteTarget == "Player" and S.dynamiteMarkPlayersEnabled) then
        clearAllDynamiteMarkedPlayers()
    else
        pruneDynamiteMarkedPlayers()
    end

    if type(ensureGunAimRenderLoop) == "function" and type(clearGunAimDrawings) == "function" then
        if hasGunAimVisualsEnabled() then
            ensureGunAimRenderLoop()
        else
            clearGunAimDrawings()
        end
    end
end

serverHopStatusText = "Off. Path sells stay in the current server."
serverHopStatusColor = nil
lastInventoryFilledSlots = 0
lastInventoryTotalSlots = 0
cachedInventorySlotsFrame = nil
lastAutoDiscardScanAt = 0
lastAutoDiscardAttemptAt = 0
lastAutoDiscardItemKey = nil
suppressAutoFarmHotkey = false
activeFarmMode = nil
pathDeathHopHumanoid = nil
pathDeathHopDiedConnection = nil
pathDeathHopBoundName = nil
lastPathDeathHopAt = 0
automineWebhookState = type(runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE) == "table" and runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE or {}
automineWebhookState.totalEarned = tonumber(automineWebhookState.totalEarned) or 0
runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE = automineWebhookState
automineWebhookServerStartedAt = os.clock()
selectedOreNames = {
	Coal = true,
}
selectedPickupNames = {}
pathUi = {
	subTabButtons = {},
	savedPathButtons = {},
	editorNodeParts = {},
	editorNodeHandles = {},
	editorOreMarkers = {},
}
settingsUi = {}
MAX_PATH_PLAYBACK_SLOTS = 4
pathState = {
	subTab = "AutofarmSettings",
	selectedPathName = nil,
	pathSlots = {nil, nil, nil, nil},
	pathSectionCount = 1,
	focusedPathSlotIndex = 1,
	sequenceStartSlotIndex = 1,
	pathDropdownOpen = false,
	recording = false,
	editorVisible = false,
	deleteMode = false,
	editNodePlacementEnabled = false,
	editingSavedPath = false,
	playLoopEnabled = false,
	playbackActive = false,
	alwaysPlayEnabled = false,
	farmPathsEnabled = false,
	staffHopEnabled = false,
	playbackSlotIndex = nil,
	playbackNodeIndex = 1,
	playbackOreIndex = 1,
	playbackPathName = nil,
	currentPath = nil,
	entryPathingPending = false,
	playbackSellPending = false,
	selectedNodeIndex = nil,
	draggingNodeIndex = nil,
	draggingNodeAxis = nil,
	dragLastPointerPosition = nil,
	sellNodePickMode = false,
	sellNodeDirty = true,
	resumePathPlaybackPending = false,
	pendingPathNameCapture = false,
	markOreKey = Enum.KeyCode.F,
	sellNodeIndex = nil,
	storageChecked = false,
	storageSupported = false,
	storageMissing = {},
	storageNoticeShown = false,
	runtimeFolder = nil,
	lastRecordedNodePosition = nil,
	deleteConfirmSlotIndex = nil,
	spawnPlaybackSequenceId = 0,
	spawnPlaybackPending = false,
	spawnPlaybackAwaitingRespawn = false,
	spawnPlaybackPendingSlotIndex = nil,
	spawnPlaybackPendingPathName = nil,
	spawnPlaybackPendingSpawnName = nil,
	spawnPlaybackReadySlotIndex = nil,
	spawnPlaybackReadyPathName = nil,
	spawnPlaybackReadySpawnName = nil,
	playerAvoiderEnabled = S.pathPlayerAvoiderEnabled == true,
	playerAvoiderDistance = math.clamp(math.floor((tonumber(S.pathPlayerAvoiderDistance) or DEFAULTS.pathPlayerAvoiderDistance) + 0.5), 1, 200),
	playerAvoiderShowCorridor = S.pathPlayerAvoiderShowCorridor == true,
	playerAvoiderMethod = normalizePathPlayerAvoiderMethod(S.pathPlayerAvoiderMethod),
	playerAvoiderAction = normalizePathPlayerAvoiderAction(S.pathPlayerAvoiderAction),
	playerAvoiderRadius = math.clamp(math.floor((tonumber(S.pathPlayerAvoiderRadius) or DEFAULTS.pathPlayerAvoiderRadius) + 0.5), 5, 200),
	playerAvoiderShowRadius = S.pathPlayerAvoiderShowRadius ~= false,
	playerAvoiderLastScanAt = 0,
	playerAvoiderLastResult = nil,
	playerAvoiderBlockedPlayerName = nil,
	playerAvoiderVisual = nil,
	playerAvoiderInternalMove = false,
	hopOnDeathEnabled = S.pathHopOnDeathEnabled == true,
	workingPath = {
		nodes = {},
		ores = {},
		manualSellNodeIndex = nil,
		spawnName = nil,
	},
	paths = {},
}

getPathRecordNodeSpacing = function()
	return math.clamp(tonumber(S.pathRecordNodeSpacing) or PATH_RECORD_NODE_SPACING or DEFAULTS.pathRecordNodeSpacing, 1, 10)
end

local pendingPathSettingsPersist = false
function schedulePathSettingsPersist()
	if pendingPathSettingsPersist then
		return
	end
	pendingPathSettingsPersist = true
	task.delay(0.2, function()
		pendingPathSettingsPersist = false
		if type(savePathsToStorage) == "function" then
			pcall(savePathsToStorage, false)
		end
	end)
end

setPathRecordNodeSpacing = function(value, skipPersistence)
	local nextValue = math.clamp(math.floor(((tonumber(value) or getPathRecordNodeSpacing()) * 10) + 0.5) / 10, 1, 10)
	if math.abs(getPathRecordNodeSpacing() - nextValue) <= 0.001 then
		return
	end
	S.pathRecordNodeSpacing = nextValue
	PATH_RECORD_NODE_SPACING = nextValue
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function isPathPlayerAvoiderEnabled()
	return pathState.playerAvoiderEnabled == true
end

function getPathPlayerAvoiderDistance()
	return math.clamp(math.floor((tonumber(pathState.playerAvoiderDistance) or DEFAULTS.pathPlayerAvoiderDistance) + 0.5), 1, 200)
end

function getPathPlayerAvoiderMethod()
	return normalizePathPlayerAvoiderMethod(pathState.playerAvoiderMethod)
end

function getPathPlayerAvoiderAction()
	return normalizePathPlayerAvoiderAction(pathState.playerAvoiderAction)
end

function getPathPlayerAvoiderRadius()
	return math.clamp(math.floor((tonumber(pathState.playerAvoiderRadius) or DEFAULTS.pathPlayerAvoiderRadius) + 0.5), 5, 200)
end

function isPathPlayerAvoiderShowCorridorEnabled()
	return pathState.playerAvoiderShowCorridor == true
end

function isPathPlayerAvoiderShowRadiusEnabled()
	return pathState.playerAvoiderShowRadius ~= false
end

function isPathHopOnDeathEnabled()
	return pathState.hopOnDeathEnabled == true
end

function setPathPlayerAvoiderEnabled(value, skipPersistence)
	local nextValue = value == true
	pathState.playerAvoiderEnabled = nextValue
	S.pathPlayerAvoiderEnabled = nextValue
	if not nextValue and type(clearPathPlayerAvoiderDetection) == "function" then
		clearPathPlayerAvoiderDetection()
	end
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathPlayerAvoiderMethod(value, skipPersistence)
	local nextValue = normalizePathPlayerAvoiderMethod(value)
	if getPathPlayerAvoiderMethod() == nextValue then
		return
	end
	pathState.playerAvoiderMethod = nextValue
	S.pathPlayerAvoiderMethod = nextValue
	if type(clearPathPlayerAvoiderDetection) == "function" then
		clearPathPlayerAvoiderDetection()
	end
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathPlayerAvoiderAction(value, skipPersistence)
	local nextValue = normalizePathPlayerAvoiderAction(value)
	if getPathPlayerAvoiderAction() == nextValue then
		return
	end
	pathState.playerAvoiderAction = nextValue
	S.pathPlayerAvoiderAction = nextValue
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathPlayerAvoiderShowCorridorEnabled(value, skipPersistence)
	local nextValue = value == true
	if isPathPlayerAvoiderShowCorridorEnabled() == nextValue then
		return
	end
	pathState.playerAvoiderShowCorridor = nextValue
	S.pathPlayerAvoiderShowCorridor = nextValue
	if not nextValue and type(clearPathPlayerAvoiderCorridorVisualization) == "function" then
		clearPathPlayerAvoiderCorridorVisualization()
	end
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathPlayerAvoiderShowRadiusEnabled(value, skipPersistence)
	local nextValue = value == true
	if isPathPlayerAvoiderShowRadiusEnabled() == nextValue then
		return
	end
	pathState.playerAvoiderShowRadius = nextValue
	S.pathPlayerAvoiderShowRadius = nextValue
	if not nextValue and type(clearPathPlayerAvoiderCorridorVisualization) == "function" then
		clearPathPlayerAvoiderCorridorVisualization()
	end
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathPlayerAvoiderDistance(value, skipPersistence)
	local nextValue = math.clamp(math.floor((tonumber(value) or getPathPlayerAvoiderDistance()) + 0.5), 1, 200)
	if getPathPlayerAvoiderDistance() == nextValue then
		return
	end
	pathState.playerAvoiderDistance = nextValue
	S.pathPlayerAvoiderDistance = nextValue
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathPlayerAvoiderRadius(value, skipPersistence)
	local nextValue = math.clamp(math.floor((tonumber(value) or getPathPlayerAvoiderRadius()) + 0.5), 5, 200)
	if getPathPlayerAvoiderRadius() == nextValue then
		return
	end
	pathState.playerAvoiderRadius = nextValue
	S.pathPlayerAvoiderRadius = nextValue
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function setPathHopOnDeathEnabled(value, skipPersistence)
	local nextValue = value == true
	if isPathHopOnDeathEnabled() == nextValue then
		return
	end
	pathState.hopOnDeathEnabled = nextValue
	S.pathHopOnDeathEnabled = nextValue
	if type(refreshPathDeathHopConnection) == "function" then
		refreshPathDeathHopConnection()
	end
	if skipPersistence then
		return
	end
	schedulePersist()
	schedulePathSettingsPersist()
end

function normalizePathSlotIndex(slotIndex)
	local numericIndex = tonumber(slotIndex)
	if not numericIndex then
		return nil
	end
	numericIndex = math.floor(numericIndex + 0.5)
	if numericIndex < 1 or numericIndex > MAX_PATH_PLAYBACK_SLOTS then
		return nil
	end
	return numericIndex
end

function ensurePathSlotState()
	if type(pathState.pathSlots) ~= "table" then
		pathState.pathSlots = {}
	end
	for slotIndex = 1, MAX_PATH_PLAYBACK_SLOTS do
		local slotName = pathState.pathSlots[slotIndex]
		pathState.pathSlots[slotIndex] = type(slotName) == "string" and slotName or nil
	end
	pathState.pathSectionCount = math.clamp(math.floor(tonumber(pathState.pathSectionCount) or 1), 1, MAX_PATH_PLAYBACK_SLOTS)
	pathState.focusedPathSlotIndex = normalizePathSlotIndex(pathState.focusedPathSlotIndex) or 1
	pathState.sequenceStartSlotIndex = normalizePathSlotIndex(pathState.sequenceStartSlotIndex) or 1
	pathState.focusedPathSlotIndex = math.clamp(pathState.focusedPathSlotIndex, 1, pathState.pathSectionCount)
	pathState.sequenceStartSlotIndex = math.clamp(pathState.sequenceStartSlotIndex, 1, pathState.pathSectionCount)
	pathState.deleteConfirmSlotIndex = normalizePathSlotIndex(pathState.deleteConfirmSlotIndex)
	pathState.spawnPlaybackPendingSlotIndex = normalizePathSlotIndex(pathState.spawnPlaybackPendingSlotIndex)
	pathState.spawnPlaybackReadySlotIndex = normalizePathSlotIndex(pathState.spawnPlaybackReadySlotIndex)
	pathState.playbackSlotIndex = normalizePathSlotIndex(pathState.playbackSlotIndex)
end

function getPathSlotSelection(slotIndex, allowMissingPath)
	ensurePathSlotState()
	local normalizedSlotIndex = normalizePathSlotIndex(slotIndex)
	if not normalizedSlotIndex then
		return nil
	end
	local slotName = pathState.pathSlots[normalizedSlotIndex]
	if type(slotName) ~= "string" or slotName == "" then
		return nil
	end
	if allowMissingPath or pathState.paths[slotName] ~= nil then
		return slotName
	end
	return nil
end

function getPlayablePathSlotIndex(startSlotIndex)
	ensurePathSlotState()
	local slotIndex = normalizePathSlotIndex(startSlotIndex) or 1
	for index = slotIndex, pathState.pathSectionCount do
		if getPathSlotSelection(index, false) then
			return index
		end
	end
	return nil
end

function getNextPlayablePathSlotIndex(currentSlotIndex)
	ensurePathSlotState()
	local slotIndex = normalizePathSlotIndex(currentSlotIndex)
	if not slotIndex then
		return getPlayablePathSlotIndex(1)
	end
	for index = slotIndex + 1, pathState.pathSectionCount do
		if getPathSlotSelection(index, false) then
			return index
		end
	end
	return nil
end

function getPathSequenceStartSlotIndex()
	ensurePathSlotState()
	return getPlayablePathSlotIndex(pathState.sequenceStartSlotIndex) or getPlayablePathSlotIndex(1)
end

function setFocusedPathSlotIndex(slotIndex, syncSelectedPath)
	ensurePathSlotState()
	pathState.focusedPathSlotIndex = normalizePathSlotIndex(slotIndex) or 1
	if syncSelectedPath ~= false then
		pathState.selectedPathName = getPathSlotSelection(pathState.focusedPathSlotIndex, false)
	end
end

function setPathSequenceStartSlotIndex(slotIndex)
	ensurePathSlotState()
	pathState.sequenceStartSlotIndex = normalizePathSlotIndex(slotIndex) or 1
	ensurePathSlotState()
end

function setPathSectionCount(sectionCount)
	ensurePathSlotState()
	pathState.pathSectionCount = math.clamp(math.floor(tonumber(sectionCount) or 1), 1, MAX_PATH_PLAYBACK_SLOTS)
	ensurePathSlotState()
end

local function remapPathSlotIndexAfterRemoval(slotIndex, removedSlotIndex, remainingCount)
	local normalizedSlotIndex = normalizePathSlotIndex(slotIndex)
	if not normalizedSlotIndex then
		return nil
	end
	if normalizedSlotIndex < removedSlotIndex then
		return normalizedSlotIndex
	end
	if normalizedSlotIndex > removedSlotIndex then
		return normalizedSlotIndex - 1
	end
	if remainingCount < 1 then
		return nil
	end
	return math.clamp(removedSlotIndex, 1, remainingCount)
end

function removePathSection(slotIndex)
	ensurePathSlotState()
	local removedSlotIndex = normalizePathSlotIndex(slotIndex)
	if not removedSlotIndex or removedSlotIndex > pathState.pathSectionCount then
		return false
	end
	if pathState.pathSectionCount <= 1 then
		notifyUser("Paths", "Keep at least one path section.", 4)
		return false
	end
	if isPathPlaybackBusy() or pathState.spawnPlaybackPending or pathState.spawnPlaybackAwaitingRespawn then
		notifyUser("Paths", "Stop path farming before removing a path section.", 4)
		return false
	end
	if pathState.editorVisible or pathState.recording then
		notifyUser("Paths", "Close the path editor before removing a path section.", 4)
		return false
	end

	local previousCount = pathState.pathSectionCount
	for index = removedSlotIndex, previousCount - 1 do
		pathState.pathSlots[index] = pathState.pathSlots[index + 1]
	end
	pathState.pathSlots[previousCount] = nil

	local remainingCount = previousCount - 1
	pathState.pathSectionCount = remainingCount
	pathState.focusedPathSlotIndex = remapPathSlotIndexAfterRemoval(pathState.focusedPathSlotIndex, removedSlotIndex, remainingCount) or 1
	pathState.sequenceStartSlotIndex = remapPathSlotIndexAfterRemoval(pathState.sequenceStartSlotIndex, removedSlotIndex, remainingCount) or 1
	pathState.spawnPlaybackPendingSlotIndex = remapPathSlotIndexAfterRemoval(pathState.spawnPlaybackPendingSlotIndex, removedSlotIndex, remainingCount)
	pathState.spawnPlaybackReadySlotIndex = remapPathSlotIndexAfterRemoval(pathState.spawnPlaybackReadySlotIndex, removedSlotIndex, remainingCount)
	pathState.playbackSlotIndex = remapPathSlotIndexAfterRemoval(pathState.playbackSlotIndex, removedSlotIndex, remainingCount)
	pathState.deleteConfirmSlotIndex = nil

	ensurePathSlotState()
	setFocusedPathSlotIndex(pathState.focusedPathSlotIndex, true)
	savePathsToStorage(false)
	return true
end

function setPathSlotSelection(slotIndex, pathName, options)
	options = options or {}
	ensurePathSlotState()
	local normalizedSlotIndex = normalizePathSlotIndex(slotIndex)
	if not normalizedSlotIndex then
		return false
	end

	local normalizedPathName = type(pathName) == "string" and pathName or nil
	if normalizedPathName and pathState.paths[normalizedPathName] == nil then
		normalizedPathName = nil
	end
	pathState.pathSlots[normalizedSlotIndex] = normalizedPathName
	if normalizedSlotIndex == (normalizePathSlotIndex(pathState.spawnPlaybackPendingSlotIndex) or 0) and normalizedPathName == nil then
		cancelPendingPathSpawnPlayback(true)
	end
	if normalizedSlotIndex == (normalizePathSlotIndex(pathState.spawnPlaybackReadySlotIndex) or 0) and normalizedPathName == nil then
		clearPathSpawnReadyState()
	end
	setFocusedPathSlotIndex(normalizedSlotIndex, options.syncSelectedPath ~= false)
	if options.updateStartSlot and normalizedPathName then
		setPathSequenceStartSlotIndex(normalizedSlotIndex)
	end
	return true
end

function getPathSlotSpawnName(slotIndex)
	local selectedPathName = getPathSlotSelection(slotIndex, false)
	local selectedPath = type(selectedPathName) == "string" and pathState.paths[selectedPathName] or nil
	return resolvePathSpawnOptionValue(selectedPath and selectedPath.spawnName)
end

function clearPathReferences(pathName)
	if type(pathName) ~= "string" or pathName == "" then
		return
	end
	ensurePathSlotState()
	for slotIndex = 1, MAX_PATH_PLAYBACK_SLOTS do
		if pathState.pathSlots[slotIndex] == pathName then
			pathState.pathSlots[slotIndex] = nil
		end
	end
	if pathState.selectedPathName == pathName then
		pathState.selectedPathName = nil
	end
end

function isPathDeleteConfirmArmed(slotIndex)
	return normalizePathSlotIndex(pathState.deleteConfirmSlotIndex) == normalizePathSlotIndex(slotIndex)
end

function setPathDeleteConfirmSlot(slotIndex)
	pathState.deleteConfirmSlotIndex = normalizePathSlotIndex(slotIndex)
end

ensurePathSlotState()

PATH_SPAWN_PLACEHOLDER_TEXT = "Choose Spawn"
PATH_SPAWN_WAIT_INTERVAL = 0.25
PATH_RESET_RETRY_INTERVAL = 1.5
PATH_RESET_COOLDOWN_NOTICE_DELAY = 1.25
PATH_RESPAWN_RETRY_INTERVAL = 1.75
PATH_RESPAWN_CONFIRM_TIMEOUT = 6
PATH_ALLOWED_SPAWN_ORDER = {
	"Bronze",
	"Delores",
	"Tribal",
	"WindmillCamp",
	"CanyonCamp",
	"HowlingPeak",
	"Dorado",
}
PATH_ALLOWED_SPAWN_LOOKUP = {}
for _, spawnName in ipairs(PATH_ALLOWED_SPAWN_ORDER) do
	PATH_ALLOWED_SPAWN_LOOKUP[spawnName] = true
end

function normalizePathSpawnName(rawValue)
	local trimmed = string.gsub(tostring(rawValue or ""), "^%s+", "")
	trimmed = string.gsub(trimmed, "%s+$", "")
	if trimmed == "" or safeLower(trimmed) == "none" then
		return nil
	end
	return trimmed
end

function isAllowedPathSpawnName(spawnName)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	return normalizedSpawnName ~= nil and PATH_ALLOWED_SPAWN_LOOKUP[normalizedSpawnName] == true
end

function getPathSpawnDisplayName(pathData)
	return getPathSpawnOptionText(pathData and pathData.spawnName)
end

function getWorkingPathSpawnName()
	return resolvePathSpawnOptionValue(pathState.workingPath and pathState.workingPath.spawnName)
end

function getWorkingPathSpawnDisplayName()
	return getPathSpawnDisplayName(pathState.workingPath)
end

function getSelectedPathSpawnName()
	return getPathSlotSpawnName(pathState.focusedPathSlotIndex)
end

local FALLBACK_PATH_SPAWN_INFO = {
	Bronze = { Name = "Bronze City" },
	Delores = { Name = "Delores' Ranch" },
	Tribal = { Name = "The Reservation" },
	WindmillCamp = { Name = "Windmill Camp" },
	CanyonCamp = { Name = "Outlaw's Perch" },
	HowlingPeak = { Name = "Howling Peak" },
	Dorado = { Name = "Puerto Dorado" },
}

local function mergePathSpawnInfoInto(target, source)
	if not isTable(target) or not isTable(source) then
		return
	end

	for spawnName, info in pairs(source) do
		if type(spawnName) == "string" and spawnName ~= "" and PATH_ALLOWED_SPAWN_LOOKUP[spawnName] == true then
			if isTable(info) then
				target[spawnName] = info
			else
				target[spawnName] = { Name = tostring(info or spawnName) }
			end
		end
	end
end

function resolveSharedDataModule(forceRefresh)
	local cache = rawget(_G, "__AutoMineSharedDataModule")
	if isTable(cache) then
		return cache
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end

	local sharedData = isTable(globalModule) and (rawget(globalModule, "SharedData") or globalModule.SharedData) or nil
	if not isTable(sharedData) and isTable(globalModule) and type(globalModule.LoadModule) == "function" then
		for _, loadArgs in ipairs({
			{ "SharedData", true },
			{ "SharedData" },
			{ "Data.SharedData", true },
			{ "Data.SharedData" },
		}) do
			local okLoad, loadedSharedData = pcall(function()
				return globalModule:LoadModule(table.unpack(loadArgs))
			end)
			if okLoad and isTable(loadedSharedData) then
				sharedData = loadedSharedData
				globalModule.SharedData = loadedSharedData
				break
			end
		end
	end

	if not isTable(sharedData) then
		local sharedModules = ReplicatedStorage:FindFirstChild("SharedModules")
		local dataFolder = sharedModules and sharedModules:FindFirstChild("Data")
		local sharedDataModuleScript = dataFolder and dataFolder:FindFirstChild("SharedData")
		if sharedDataModuleScript and sharedDataModuleScript:IsA("ModuleScript") then
			local okRequire, requiredSharedData = pcall(require, sharedDataModuleScript)
			if okRequire and isTable(requiredSharedData) then
				sharedData = requiredSharedData
				if isTable(globalModule) then
					globalModule.SharedData = requiredSharedData
				end
			end
		end
	end

	if isTable(sharedData) then
		_G.__AutoMineSharedDataModule = sharedData
		return sharedData
	end

	return nil
end

function getPathSpawnInfoTable()
	local mergedSpawnInfo = {}
	mergePathSpawnInfoInto(mergedSpawnInfo, FALLBACK_PATH_SPAWN_INFO)

	local okSharedData, sharedData = pcall(function()
		return resolveSharedDataModule(false)
	end)
	local spawnInfo = okSharedData and isTable(sharedData) and (rawget(sharedData, "SpawnInfo") or sharedData.SpawnInfo) or nil
	if isTable(spawnInfo) then
		mergePathSpawnInfoInto(mergedSpawnInfo, spawnInfo)
	end

	if next(mergedSpawnInfo) ~= nil then
		return mergedSpawnInfo
	end
	return FALLBACK_PATH_SPAWN_INFO
end

function getPathSpawnOptionText(spawnName)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	if not normalizedSpawnName or not isAllowedPathSpawnName(normalizedSpawnName) then
		return PATH_SPAWN_PLACEHOLDER_TEXT
	end

	local spawnInfo = getPathSpawnInfoTable()
	local info = isTable(spawnInfo) and spawnInfo[normalizedSpawnName] or nil
	local displayName = isTable(info) and tostring(rawget(info, "Name") or info.Name or normalizedSpawnName) or normalizedSpawnName
	return displayName
end

function resolvePathSpawnOptionValue(rawValue)
	local normalizedValue = normalizePathSpawnName(rawValue)
	if not normalizedValue then
		return nil
	end

	local spawnInfo = getPathSpawnInfoTable()
	if isTable(spawnInfo) and isTable(spawnInfo[normalizedValue]) then
		return normalizedValue
	end

	local loweredValue = safeLower(normalizedValue)
	for spawnName, info in pairs(spawnInfo or {}) do
		if type(spawnName) == "string" then
			local displayName = isTable(info) and tostring(rawget(info, "Name") or info.Name or spawnName) or spawnName
			local optionText = getPathSpawnOptionText(spawnName)
			if loweredValue == safeLower(spawnName)
				or loweredValue == safeLower(displayName)
				or loweredValue == safeLower(optionText) then
				return spawnName
			end
		end
	end

	local extractedKey = string.match(normalizedValue, "%(([^()]+)%)%s*$")
	return normalizePathSpawnName(extractedKey or normalizedValue)
end

function getPathSpawnOptions()
	local spawnInfo = getPathSpawnInfoTable()
	local options = {}
	for _, spawnName in ipairs(PATH_ALLOWED_SPAWN_ORDER) do
		if isTable(spawnInfo[spawnName]) then
			table.insert(options, getPathSpawnOptionText(spawnName))
		end
	end
	return options
end

function isPathSpawnSelectorGuiObjectVisible(guiObject)
	if not guiObject or not guiObject.Parent then
		return false
	end

	local current = guiObject
	while current do
		if current:IsA("GuiObject") then
			local okVisible, isVisible = pcall(function()
				return current.Visible
			end)
			if okVisible and isVisible == false then
				return false
			end
		elseif current:IsA("ScreenGui") then
			local okEnabled, isEnabled = pcall(function()
				return current.Enabled
			end)
			if okEnabled and isEnabled == false then
				return false
			end
		end
		current = current.Parent
	end

	return true
end

function findPathSpawnSelectorGui()
	if not localPlayer then
		return nil, nil, nil
	end

	local playerGui = localPlayer:FindFirstChild("PlayerGui")
	if not playerGui then
		return nil, nil, nil
	end

	for _, child in ipairs(playerGui:GetChildren()) do
		if child:IsA("ScreenGui") then
			local screen = child:FindFirstChild("Screen")
			local background = child:FindFirstChild("Background")
			local acceptButton = screen and screen:FindFirstChild("AcceptButton")
			local sideBar = screen and screen:FindFirstChild("SideBar")
			local body = sideBar and sideBar:FindFirstChild("Body")
			local scrollingFrame = body and body:FindFirstChild("ScrollingFrame")
			if screen and background and acceptButton and scrollingFrame then
				return child, screen, scrollingFrame
			end
		end
	end

	return nil, nil, nil
end

function getPathSpawnSelectorMatchCount()
	local guiRoot, screen, scrollingFrame = findPathSpawnSelectorGui()
	if not guiRoot or not screen or not scrollingFrame then
		return 0
	end

	if not isPathSpawnSelectorGuiObjectVisible(screen) then
		return 0
	end
	if not isPathSpawnSelectorGuiObjectVisible(scrollingFrame) then
		return 0
	end

	local matchCount = 0
	for _, button in ipairs(scrollingFrame:GetChildren()) do
		if button:IsA("ImageButton") and PATH_ALLOWED_SPAWN_LOOKUP[button.Name] == true and isPathSpawnSelectorGuiObjectVisible(button) then
			matchCount += 1
		end
	end

	return matchCount
end

function isOnPathSpawnSelectorScreen(_character)
	return getPathSpawnSelectorMatchCount() >= PATH_SPAWN_SELECTOR_MIN_MATCHES
end

function setWorkingPathSpawnName(spawnName)
	local normalizedSpawnName = resolvePathSpawnOptionValue(spawnName)
	if type(pathState.workingPath) ~= "table" then
		pathState.workingPath = {
			nodes = {},
			ores = {},
			manualSellNodeIndex = nil,
			spawnName = nil,
		}
	end
	pathState.workingPath.spawnName = normalizedSpawnName
	refreshPathEditorButtons()
end

AUTO_SELL_SHOP_TYPES = {
	GunShop = true,
	GeneralShop = true,
	ManorGeneralShop = true,
	FortHenryShop = true,
	FortSpencerShop = true,
	FortHighlandsShop = true,
}
AUTO_SELL_OBJECT_ACTION_DURATION = 0.55
AUTO_SELL_OBJECT_ACTION_CONFIRM_TIMEOUT = 0.8
AUTO_SELL_OBJECT_ACTION_NETWORK_ID_WAIT = 0.25
AUTO_PICKUP_OBJECT_ACTION_DURATION = 0.55
AUTO_PICKUP_OBJECT_ACTION_CONFIRM_TIMEOUT = 0.8
AUTO_PICKUP_INSTANT_CONFIRM_TIMEOUT = 0.22
AUTO_SELL_SHOP_ATTRIBUTE_NAMES = { "ActionGroupType", "ObjectActionType", "ActionType", "ShopType" }
AUTO_PICKUP_ACTION_ATTRIBUTE_NAMES = { "ActionGroupType", "ObjectActionType", "ActionType", "Type" }
AUTO_SELL_SHOP_NAME_HINTS = {
	"generalshop",
	"general store",
	"gunshop",
	"gun shop",
	"sell loot",
	"buy guns",
	"fort henry clerk",
	"fort spencer clerk",
	"fort highlands",
	"nokoda",
	"clerk",
	"merchant",
	"vendor",
}
movementKeyState = {
	[Enum.KeyCode.W] = false,
	[Enum.KeyCode.A] = false,
	[Enum.KeyCode.S] = false,
	[Enum.KeyCode.D] = false,
	[Enum.KeyCode.LeftShift] = false,
}
interactKeyHeld = false
autoSellObjectActionActivationId = 0

function safeLower(value)
	return string.lower(tostring(value or ""))
end

function formatKeyCodeName(keyCode)
	if KEYCODE_DISPLAY_NAMES[keyCode] then
		return KEYCODE_DISPLAY_NAMES[keyCode]
	end

	return keyCode and keyCode.Name or "None"
end

function getOreLabel(oreName)
	return ORE_LABELS[oreName] or tostring(oreName or "Unknown")
end

function isOreNameSelected(oreName)
	return selectedOreNames[oreName] == true
end

function getSelectedOreNamesOrdered()
	local results = {}

	for _, oreOption in ipairs(ORE_OPTIONS) do
		if selectedOreNames[oreOption.id] then
			table.insert(results, oreOption.id)
		end
	end

	return results
end

function hasSelectedOres()
	return next(selectedOreNames) ~= nil
end

function getSelectedOreSummary()
	local oreNames = getSelectedOreNamesOrdered()
	if #oreNames == 0 then
		return "No ores selected"
	end

	if #oreNames == 1 then
		return getOreLabel(oreNames[1])
	end

	if #oreNames == 2 then
		return getOreLabel(oreNames[1]) .. ", " .. getOreLabel(oreNames[2])
	end

	return string.format("%s, %s +%d", getOreLabel(oreNames[1]), getOreLabel(oreNames[2]), #oreNames - 2)
end

function getOreGroupName(oreDeposit)
	local parent = oreDeposit and oreDeposit.Parent
	return parent and parent.Name or nil
end

function getOreDisplayName(oreDeposit)
	local groupName = getOreGroupName(oreDeposit)
	if groupName then
		return getOreLabel(groupName)
	end

	local oreNames = getSelectedOreNamesOrdered()
	if #oreNames == 1 then
		return getOreLabel(oreNames[1])
	end

	return "selected ores"
end

function getPickupLabel(itemName)
	return AUTO_PICKUP_LABELS[itemName] or tostring(itemName or "Unknown")
end

function getSelectedPickupNamesOrdered()
	local results = {}

	for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
		if selectedPickupNames[pickupOption.id] then
			table.insert(results, pickupOption.id)
		end
	end

	return results
end

function getSelectedPickupSummary()
	local itemNames = getSelectedPickupNamesOrdered()
	if #itemNames == 0 then
		return "No pickup filter"
	end

	if #itemNames == 1 then
		return getPickupLabel(itemNames[1])
	end

	if #itemNames == 2 then
		return getPickupLabel(itemNames[1]) .. ", " .. getPickupLabel(itemNames[2])
	end

	return string.format("%s, %s +%d", getPickupLabel(itemNames[1]), getPickupLabel(itemNames[2]), #itemNames - 2)
end

function getDropItemTypeName(dropItem)
	if not dropItem then
		return nil
	end

	local pickupItem = dropItem:FindFirstChild("PickupItem", true)
	local candidates = {
		dropItem:GetAttribute("ItemType"),
		dropItem:GetAttribute("ItemName"),
		dropItem:GetAttribute("Type"),
		dropItem.Name,
	}

	if pickupItem then
		table.insert(candidates, pickupItem:GetAttribute("ItemType"))
		table.insert(candidates, pickupItem:GetAttribute("ItemName"))
		table.insert(candidates, pickupItem:GetAttribute("Type"))

		for _, childName in ipairs({ "ItemType", "ItemName", "Type", "ItemId" }) do
			local valueObject = pickupItem:FindFirstChild(childName)
			if valueObject and valueObject:IsA("StringValue") then
				table.insert(candidates, valueObject.Value)
			end
		end
	end

	for _, candidate in ipairs(candidates) do
		if type(candidate) == "string" and AUTO_PICKUP_LABELS[candidate] then
			return candidate
		end
	end

	local fallbackName = tostring(dropItem.Name or "")
	local loweredFallback = safeLower(fallbackName)
	for itemName in pairs(AUTO_PICKUP_LABELS) do
		if string.find(loweredFallback, safeLower(itemName), 1, true) then
			return itemName
		end
	end

	return dropItem.Name
end

function isAutoPickupDropAllowed(dropItem)
	if not autoPickupWhitelistEnabled then
		return true
	end

	local itemName = getDropItemTypeName(dropItem)
	return itemName ~= nil and selectedPickupNames[itemName] == true
end

function getInventorySlotsFrame()
	if cachedInventorySlotsFrame and cachedInventorySlotsFrame.Parent then
		return cachedInventorySlotsFrame
	end

	local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
	if not playerGui then
		cachedInventorySlotsFrame = nil
		return nil
	end

	local inventoryUI = playerGui:FindFirstChild("InventoryUI") or playerGui:FindFirstChild("InventoryUI", true)
	local inventory = inventoryUI and inventoryUI:FindFirstChild("Inventory")
	local container = inventory and inventory:FindFirstChild("Container")
	local body = container and container:FindFirstChild("Body")
	local page = body and body:FindFirstChild("Page")
	local scrollingFrame = page and page:FindFirstChild("ScrollingFrame")

	if not scrollingFrame then
		for _, descendant in ipairs(playerGui:GetDescendants()) do
			if descendant:IsA("ScrollingFrame") and descendant.Name == "ScrollingFrame" then
				local slotTemplate = descendant:FindFirstChild("SlotTemplate") or descendant:FindFirstChild("SlotTemplate", true)
				local containerFrame = slotTemplate and slotTemplate:FindFirstChild("Container")
				local viewportFrame = containerFrame and containerFrame:FindFirstChild("ViewportFrame")
				if viewportFrame then
					scrollingFrame = descendant
					break
				end
			end
		end
	end

	cachedInventorySlotsFrame = scrollingFrame
	return cachedInventorySlotsFrame
end

function getViewportFrameItemName(viewportFrame)
	if not viewportFrame then
		return nil
	end

	for _, child in ipairs(viewportFrame:GetChildren()) do
		local isVisualItem = not child:IsA("Camera")
			and not child:IsA("UIAspectRatioConstraint")
			and not child:IsA("UISizeConstraint")
			and not child:IsA("UIScale")
			and not child:IsA("UIPadding")
			and not child:IsA("UICorner")
			and not child:IsA("UIStroke")
			and not child:IsA("UIGradient")
		if isVisualItem then
			return child.Name
		end
	end

	return nil
end

function getInventorySlotCounts()
	local scrollingFrame = getInventorySlotsFrame()
	if not scrollingFrame then
		return 0, 0
	end

	local filledSlots = 0
	local totalSlots = 0

	for _, child in ipairs(scrollingFrame:GetDescendants()) do
		local container = child:FindFirstChild("Container")
		local viewportFrame = container and container:FindFirstChild("ViewportFrame")
		if viewportFrame and child.Name == "SlotTemplate" then
			totalSlots += 1
			if getViewportFrameItemName(viewportFrame) then
				filledSlots += 1
			end
		end
	end

	return filledSlots, totalSlots
end

function getOrderedInventorySlotFrames()
	local scrollingFrame = getInventorySlotsFrame()
	if not scrollingFrame then
		return {}
	end

	local slotFrames = {}
	for _, descendant in ipairs(scrollingFrame:GetDescendants()) do
		local container = descendant:FindFirstChild("Container")
		local viewportFrame = container and container:FindFirstChild("ViewportFrame")
		if viewportFrame and descendant.Name == "SlotTemplate" then
			table.insert(slotFrames, descendant)
		end
	end

	table.sort(slotFrames, function(left, right)
		local leftOrder = tonumber(left.LayoutOrder) or 0
		local rightOrder = tonumber(right.LayoutOrder) or 0
		if leftOrder ~= rightOrder then
			return leftOrder < rightOrder
		end
		return tostring(left:GetFullName()) < tostring(right:GetFullName())
	end)

	return slotFrames
end

function normalizeInventoryItemLookupName(rawName)
	if type(rawName) ~= "string" or rawName == "" then
		return nil
	end

	local normalized = safeLower(rawName):gsub("[^%w]", "")
	if normalized == "" then
		return nil
	end

	return normalized
end

function resolveSharedPlayerItemRecordByName(rawName, forceRefresh)
	local normalized = normalizeInventoryItemLookupName(rawName)
	if not normalized then
		return nil, nil
	end

	local playerItems = resolveSharedPlayerItems(forceRefresh)
	if not isTable(playerItems) then
		return nil, nil
	end

	for itemType, sharedItemData in pairs(playerItems) do
		if isTable(sharedItemData) then
			local nameCandidates = {
				itemType,
				rawget(sharedItemData, "Display") or sharedItemData.Display,
				rawget(sharedItemData, "Name") or sharedItemData.Name,
			}
			for _, candidateName in ipairs(nameCandidates) do
				if type(candidateName) == "string" and normalized == safeLower(candidateName):gsub("[^%w]", "") then
					return itemType, sharedItemData
				end
			end
		end
	end

	return nil, nil
end

function getSharedInventoryDiscardFlags(sharedItemData)
	if not isTable(sharedItemData) then
		return false, false
	end

	local isLoot = (rawget(sharedItemData, "IsLoot") or sharedItemData.IsLoot) == true
	local droppable = rawget(sharedItemData, "Droppable")
	if droppable == nil then
		droppable = sharedItemData.Droppable
	end
	local canBeDiscarded = rawget(sharedItemData, "CanBeDiscarded")
	if canBeDiscarded == nil then
		canBeDiscarded = sharedItemData.CanBeDiscarded
	end

	local hasDiscardAction = false
	local validActions = rawget(sharedItemData, "ValidActions") or sharedItemData.ValidActions
	if isTable(validActions) then
		for key, value in pairs(validActions) do
			local actionName = nil
			if type(key) == "string" and value then
				actionName = key
			elseif type(value) == "string" then
				actionName = value
			end
			local normalizedAction = actionName and safeLower(actionName) or nil
			if normalizedAction and (string.find(normalizedAction, "drop", 1, true) or string.find(normalizedAction, "throw", 1, true) or string.find(normalizedAction, "discard", 1, true)) then
				hasDiscardAction = true
				break
			end
		end
	end

	return droppable == true or canBeDiscarded == true or hasDiscardAction, isLoot
end

function isOreInventoryType(itemType)
	return type(itemType) == "string" and INVENTORY_ORE_LABELS[itemType] ~= nil
end

function getInventoryOreLabel(itemType)
	return INVENTORY_ORE_LABELS[itemType] or tostring(itemType or "Unknown")
end

function getInventoryDiscardDisplayName(discardState)
	if not isTable(discardState) then
		return "item"
	end

	local itemType = rawget(discardState, "itemType") or discardState.itemType
	local sharedItemData = rawget(discardState, "sharedItemData") or discardState.sharedItemData
	local displayName = isTable(sharedItemData) and (rawget(sharedItemData, "Display") or sharedItemData.Display) or nil
	if type(displayName) == "string" and displayName ~= "" then
		return displayName
	end

	if isOreInventoryType(itemType) then
		return getInventoryOreLabel(itemType)
	end

	local visualName = rawget(discardState, "visualName") or discardState.visualName
	if type(visualName) == "string" and visualName ~= "" then
		return visualName
	end

	return tostring(itemType or "item")
end

function getOrderedInventoryItems(inventoryContainer)
	local items = isTable(inventoryContainer) and (rawget(inventoryContainer, "Items") or inventoryContainer.Items) or nil
	if not isTable(items) then
		return {}
	end

	local orderedItems = {}
	for key, inventoryItem in pairs(items) do
		if isTable(inventoryItem) then
			table.insert(orderedItems, {
				key = key,
				numericKey = tonumber(key),
				item = inventoryItem,
			})
		end
	end

	table.sort(orderedItems, function(left, right)
		if left.numericKey and right.numericKey and left.numericKey ~= right.numericKey then
			return left.numericKey < right.numericKey
		end
		if left.numericKey ~= nil and right.numericKey == nil then
			return true
		end
		if right.numericKey ~= nil and left.numericKey == nil then
			return false
		end
		return tostring(left.key) < tostring(right.key)
	end)

	return orderedItems
end

function getLastInventorySlotOreState(forceRefresh)
	local playerData = resolvePlayerDataModule(forceRefresh)
	local playerItems = resolveSharedPlayerItems(forceRefresh)
	local inventoryContainer = getResolvedInventoryContainer(playerData)
	if not (isTable(playerData) and isTable(playerItems) and isTable(inventoryContainer)) then
		return nil, "inventory container unavailable"
	end

	local orderedItems = getOrderedInventoryItems(inventoryContainer)
	local filledSlots = #orderedItems
	local totalSlots = tonumber(rawget(inventoryContainer, "Size") or inventoryContainer.Size) or 0
	if totalSlots <= 0 then
		return nil, "inventory size unavailable"
	end
	if filledSlots < totalSlots then
		return nil, string.format("waiting for last slot (%d/%d)", filledSlots, totalSlots)
	end

	local candidateEntry = orderedItems[#orderedItems]
	local inventoryItem = nil
	if isTable(candidateEntry) then
		inventoryItem = candidateEntry.item
	end

	local itemType = isTable(inventoryItem) and (rawget(inventoryItem, "Type") or inventoryItem.Type) or nil
	if not isOreInventoryType(itemType) then
		return nil, "last slot item not ore: " .. tostring(itemType or "nil")
	end

	local sharedItemData = rawget(playerItems, itemType) or playerItems[itemType]
	local isDiscardable, isLoot = getSharedInventoryDiscardFlags(sharedItemData)
	if not isTable(inventoryItem) or not isDiscardable or isLoot ~= true then
		return nil, "last slot ore blocked: " .. tostring(itemType)
	end

	local itemId = isTable(inventoryItem) and (rawget(inventoryItem, "Id") or inventoryItem.Id) or nil
	local visualName = nil
	local slotFrames = getOrderedInventorySlotFrames()
	local lastSlot = slotFrames[#slotFrames]
	if lastSlot then
		local container = lastSlot:FindFirstChild("Container")
		local viewportFrame = container and container:FindFirstChild("ViewportFrame")
		visualName = getViewportFrameItemName(viewportFrame)
	end

	return {
		slotFrame = lastSlot,
		visualName = visualName,
		itemType = itemType,
		itemId = itemId,
		inventoryItem = inventoryItem,
		sharedItemData = sharedItemData,
		isLoot = isLoot,
		filledSlots = filledSlots,
		totalSlots = totalSlots,
	}, nil
end

function hasRemainingPathOresToMine(currentPath, nodeIndex)
	if not isTable(currentPath) then
		return false
	end

	local currentNodeIndex = math.max(1, tonumber(nodeIndex) or 1)
	for _, oreTarget in ipairs(currentPath.ores or {}) do
		local oreNodeIndex = tonumber(oreTarget.nodeIndex) or 0
		if oreNodeIndex >= currentNodeIndex then
			local resolvedOre = resolveSavedOreTarget(oreTarget)
			local oreRemaining = getOreRemainingValue(resolvedOre)
			if resolvedOre and oreRemaining and (tonumber(oreRemaining.Value) or 0) > 0 then
				return true
			end
		end
	end

	return false
end

function shouldPreserveLastSlotOreForSell(discardState)
	if activeFarmMode ~= "path" or not pathState.playbackActive or not pathState.playbackSellPending then
		return false
	end

	if isTable(discardState) and (rawget(discardState, "isLoot") or discardState.isLoot) ~= true then
		return false
	end

	local currentPath = pathState.currentPath
	if not isTable(currentPath) then
		return false
	end

	if hasRemainingPathOresToMine(currentPath, pathState.playbackNodeIndex) then
		return false
	end

	return isPathPlaybackSellReady()
end

function waitForLastSlotOreDiscard(beforeSummary, discardedState, timeoutSeconds)
	local deadline = os.clock() + math.max(0.1, tonumber(timeoutSeconds) or AUTO_LAST_SLOT_ORE_DISCARD_CONFIRM_TIMEOUT)
	while os.clock() < deadline do
		task.wait(0.05)

		local afterSummary = getSellableLootSummary(false) or getSellableLootSummary(true)
		if isTable(beforeSummary) and isTable(afterSummary) and wasSellableLootReduced(beforeSummary, afterSummary) then
			return true
		end

		local latestState = getLastInventorySlotOreState(false) or getLastInventorySlotOreState(true)
		if not latestState then
			return true
		end
		if latestState.itemType ~= discardedState.itemType then
			return true
		end
		if type(discardedState.itemId) == "string"
			and type(latestState.itemId) == "string"
			and latestState.itemId ~= discardedState.itemId then
			return true
		end
	end

	return false
end

function collectInventoryDiscardActionNames(sharedItemData)
	local actionNames = {}
	local seen = {}
	local function isDiscardOnlyActionName(value)
		local normalizedValue = safeLower(value)
		if normalizedValue == "" then
			return false
		end
		if string.find(normalizedValue, "drop", 1, true) or string.find(normalizedValue, "throw", 1, true) then
			return false
		end
		return string.find(normalizedValue, "discard", 1, true)
			or string.find(normalizedValue, "destroy", 1, true)
			or string.find(normalizedValue, "remove", 1, true)
			or string.find(normalizedValue, "delete", 1, true)
	end

	local function pushActionName(value)
		if type(value) ~= "string" or value == "" or seen[value] or not isDiscardOnlyActionName(value) then
			return
		end
		seen[value] = true
		table.insert(actionNames, value)
	end

	local validActions = isTable(sharedItemData) and (rawget(sharedItemData, "ValidActions") or sharedItemData.ValidActions) or nil
	if isTable(validActions) then
		for key, value in pairs(validActions) do
			if type(key) == "string" and value then
				pushActionName(key)
			elseif type(value) == "string" then
				pushActionName(value)
			end
		end
	end

	pushActionName("DiscardItem")
	pushActionName("DiscardInventoryItem")
	pushActionName("DestroyInventoryItem")
	pushActionName("RemoveInventoryItem")
	pushActionName("DeleteInventoryItem")
	pushActionName("DestroyItem")
	pushActionName("RemoveItem")
	return actionNames
end

function collectKeywordMethodNames(owner, baseNames)
	local methodNames = {}
	local seen = {}
	local function pushMethodName(name)
		if type(name) ~= "string" or name == "" or seen[name] then
			return
		end
		seen[name] = true
		table.insert(methodNames, name)
	end

	for _, name in ipairs(baseNames or {}) do
		pushMethodName(name)
	end

	if not isTable(owner) then
		return methodNames
	end

	for key, value in pairs(owner) do
		if type(key) == "string" and type(value) == "function" then
			local normalizedKey = safeLower(key)
			if string.find(normalizedKey, "discard", 1, true)
				or string.find(normalizedKey, "destroy", 1, true)
				or string.find(normalizedKey, "remove", 1, true)
				or string.find(normalizedKey, "delete", 1, true) then
				pushMethodName(key)
			end
		end
	end

	return methodNames
end

function getPlayerCharacterItemObject(playerCharacter, itemType)
	if not isTable(playerCharacter) or type(itemType) ~= "string" or itemType == "" then
		return nil
	end

	local getItem = rawget(playerCharacter, "GetItem") or playerCharacter.GetItem
	if type(getItem) ~= "function" then
		return nil
	end

	local okItem, itemObject = pcall(getItem, playerCharacter, itemType)
	if okItem and isTable(itemObject) then
		return itemObject
	end

	return nil
end

function tryDiscardInventoryItemViaKnownActions(discardState, playerData, inventoryContainer, inventoryItem, beforeSummary)
	if not (isTable(discardState) and isTable(playerData) and isTable(inventoryContainer) and isTable(inventoryItem)) then
		return false, "discard state unavailable"
	end

	local itemType = rawget(discardState, "itemType") or discardState.itemType
	local sharedItemData = rawget(discardState, "sharedItemData") or discardState.sharedItemData
	local inventoryItemId = rawget(inventoryItem, "Id") or inventoryItem.Id
	local inventoryContainerId = rawget(inventoryContainer, "Id") or inventoryContainer.Id
	local playerCharacterModule = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
	local playerItemObject = getPlayerCharacterItemObject(playerCharacterModule, itemType)
	local actionNames = collectInventoryDiscardActionNames(sharedItemData)
	local triedMethods = {}
	local lastFailure = nil

	local function confirmDiscard()
		return waitForLastSlotOreDiscard(beforeSummary, discardState, AUTO_LAST_SLOT_ORE_DISCARD_CONFIRM_TIMEOUT)
	end

	local function tryMethodCall(owner, methodName, ...)
		if not isTable(owner) then
			return false
		end

		local method = rawget(owner, methodName) or owner[methodName]
		if type(method) ~= "function" or triedMethods[method] then
			return false
		end
		triedMethods[method] = true

		local packedArgs = table.pack(...)
		local okCall, callResult = pcall(function()
			return method(owner, table.unpack(packedArgs, 1, packedArgs.n))
		end)
		if okCall then
			if confirmDiscard() then
				return true
			end
			return false
		end

		lastFailure = tostring(callResult)
		return false
	end

	local function tryNetworkActivate(actionName, ...)
		if not isTable(playerItemObject) then
			return false
		end

		local packedArgs = table.pack(...)
		local okCall, sent = pcall(function()
			return callToolNetworkActivate(playerItemObject, actionName, table.unpack(packedArgs, 1, packedArgs.n))
		end)
		if okCall and sent and confirmDiscard() then
			return true
		end
		if not okCall then
			lastFailure = tostring(sent)
		end
		return false
	end

	for _, actionName in ipairs(actionNames) do
		if tryNetworkActivate(actionName, inventoryItemId) then
			return true, "item.NetworkActivate(" .. tostring(actionName) .. ", itemId)"
		end
		if tryNetworkActivate(actionName, inventoryContainerId, inventoryItemId) then
			return true, "item.NetworkActivate(" .. tostring(actionName) .. ", containerId, itemId)"
		end
		if tryNetworkActivate(actionName, inventoryItem) then
			return true, "item.NetworkActivate(" .. tostring(actionName) .. ", item)"
		end
		if tryNetworkActivate(actionName) then
			return true, "item.NetworkActivate(" .. tostring(actionName) .. ")"
		end
	end

	for _, target in ipairs({
		{ label = "item", owner = playerItemObject },
		{ label = "inventoryItem", owner = inventoryItem },
		{ label = "inventoryContainer", owner = inventoryContainer },
		{ label = "playerData", owner = playerData },
	}) do
		local methodNames = collectKeywordMethodNames(target.owner, actionNames)
		for _, methodName in ipairs(methodNames) do
			if tryMethodCall(target.owner, methodName, inventoryItem) then
				return true, target.label .. "." .. tostring(methodName) .. "(item)"
			end
			if tryMethodCall(target.owner, methodName, inventoryItemId) then
				return true, target.label .. "." .. tostring(methodName) .. "(itemId)"
			end
			if tryMethodCall(target.owner, methodName, inventoryContainerId, inventoryItemId) then
				return true, target.label .. "." .. tostring(methodName) .. "(containerId, itemId)"
			end
			if tryMethodCall(target.owner, methodName, itemType) then
				return true, target.label .. "." .. tostring(methodName) .. "(itemType)"
			end
			if tryMethodCall(target.owner, methodName) then
				return true, target.label .. "." .. tostring(methodName) .. "()"
			end
		end
	end

	return false, lastFailure or "no discard action succeeded"
end

function tryAutoDiscardLastSlotOre(character, activeToken)
	if not autoMineEnabled or activeToken ~= autoMineToken then
		return false
	end

	local now = os.clock()
	if (now - lastAutoDiscardScanAt) < AUTO_LAST_SLOT_ORE_DISCARD_SCAN_INTERVAL then
		return false
	end
	lastAutoDiscardScanAt = now

	local discardState, discardReason = getLastInventorySlotOreState(false)
	if not discardState then
		discardState, discardReason = getLastInventorySlotOreState(true)
	end
	if not discardState then
		pushDebugRateLimited("last_slot_discard_probe", "last-slot discard: " .. tostring(discardReason or "not ready"), 2)
		return false
	end
	if shouldPreserveLastSlotOreForSell(discardState) then
		pushDebugRateLimited(
			"last_slot_discard_preserve",
			"last-slot discard: keeping final " .. tostring(getInventoryDiscardDisplayName(discardState)) .. " for sell",
			2
		)
		return false
	end

	local itemType = discardState.itemType
	local discardKey = tostring(itemType) .. ":" .. tostring(discardState.itemId or discardState.visualName or "slot")
	if lastAutoDiscardItemKey == discardKey and (now - lastAutoDiscardAttemptAt) < AUTO_LAST_SLOT_ORE_DISCARD_COOLDOWN then
		return false
	end

	local playerData = resolvePlayerDataModule(false) or resolvePlayerDataModule(true)
	local inventoryContainer = getResolvedInventoryContainer(playerData)
	local inventoryItem = discardState.inventoryItem
	if not isTable(playerData) or not isTable(inventoryContainer) or not isTable(inventoryItem) then
		return false
	end

	local inventoryItemId = rawget(inventoryItem, "Id") or inventoryItem.Id
	local inventoryContainerId = rawget(inventoryContainer, "Id") or inventoryContainer.Id
	if type(inventoryItemId) ~= "string" or inventoryItemId == "" then
		return false
	end

	lastAutoDiscardAttemptAt = now
	lastAutoDiscardItemKey = discardKey

	local lootSummaryBefore = getSellableLootSummary(false) or getSellableLootSummary(true)
	local discardLabel = getInventoryDiscardDisplayName(discardState)
	pushDebugRateLimited(
		"last_slot_discard_attempt",
		string.format(
			"last-slot discard attempt: %s (%d/%d)",
			tostring(discardLabel),
			tonumber(discardState.filledSlots) or 0,
			tonumber(discardState.totalSlots) or 0
		),
		1.5
	)

	setStatus("Discarding " .. discardLabel, Color3.fromRGB(255, 190, 120))
	clearMovementKeys()
	setInteractKeyHeld(false)
	endExperimentalCFrameTravel(character)

	local discardOk, discardSource = tryDiscardInventoryItemViaKnownActions(
		discardState,
		playerData,
		inventoryContainer,
		inventoryItem,
		lootSummaryBefore
	)
	if discardOk then
		pushDebug("discarded last-slot ore via " .. tostring(discardSource))
		return true
	end

	pushDebugRateLimited("last_slot_discard_action_failed", "last-slot discard action failed: " .. tostring(discardSource), 2)
	pushDebugRateLimited("last_slot_discard_failed", "last-slot discard failed for " .. tostring(discardLabel), 2)
	return false
end

function getInventoryItemStackAmount(inventoryItem)
	if not isTable(inventoryItem) then
		return 1
	end

	local amount = tonumber(
		rawget(inventoryItem, "Amount")
			or inventoryItem.Amount
			or rawget(inventoryItem, "Count")
			or inventoryItem.Count
			or rawget(inventoryItem, "Quantity")
			or inventoryItem.Quantity
			or rawget(inventoryItem, "StackSize")
			or inventoryItem.StackSize
			or 1
	)
	if not amount or amount < 1 then
		return 1
	end

	return amount
end

function resolveSharedPlayerItemSellMetadata(forceRefresh)
	local cache = rawget(_G, "__AutoMineSharedPlayerItemSellMetadata")
	if isTable(cache) then
		return cache
	end

	local sharedData = resolveSharedDataModule(forceRefresh)
	if not isTable(sharedData) then
		return nil
	end

	local playerItemsRoot = rawget(sharedData, "PlayerItems") or sharedData.PlayerItems
	if not isTable(playerItemsRoot) then
		return nil
	end

	local metadata = {}
	for _, categoryData in pairs(playerItemsRoot) do
		if isTable(categoryData) then
			local items = rawget(categoryData, "Items") or categoryData.Items
			local categoryIsLoot = (rawget(categoryData, "IsLoot") or categoryData.IsLoot) == true
			local categoryVipValueMultiplier = tonumber(rawget(categoryData, "VIPValueMultiplier") or categoryData.VIPValueMultiplier) or 0
			if isTable(items) then
				for itemType, itemData in pairs(items) do
					if type(itemType) == "string" and isTable(itemData) then
						metadata[itemType] = {
							isLoot = ((rawget(itemData, "IsLoot") or itemData.IsLoot) == true) or categoryIsLoot,
							value = tonumber(rawget(itemData, "Value") or itemData.Value) or 0,
							stackSellSize = tonumber(rawget(itemData, "StackSellSize") or itemData.StackSellSize) or 1,
							vipValueMultiplier = tonumber(rawget(itemData, "VIPValueMultiplier") or itemData.VIPValueMultiplier) or categoryVipValueMultiplier,
							display = rawget(itemData, "Display") or itemData.Display or itemType,
						}
					end
				end
			end
		end
	end

	_G.__AutoMineSharedPlayerItemSellMetadata = metadata
	return metadata
end

function playerHasVipSellBonus(forceRefresh)
	local replicatedState = resolveGlobalReplicatedState(forceRefresh)
	if not isTable(replicatedState) then
		return false
	end

	local state = rawget(replicatedState, "State") or replicatedState.State
	if not isTable(state) then
		return false
	end

	if (rawget(state, "VIP") or state.VIP) == true or (rawget(state, "IsVIP") or state.IsVIP) == true then
		return true
	end

	local ownedGamePasses = rawget(state, "OwnedGamePasses") or state.OwnedGamePasses
	if isTable(ownedGamePasses) then
		for key, value in pairs(ownedGamePasses) do
			if value then
				local loweredKey = safeLower(key)
				if loweredKey == "vip" or string.find(loweredKey, "vip", 1, true) then
					return true
				end
			end
		end
	end

	return false
end

function formatMoneyAmount(value)
	local rounded = math.max(0, math.floor((tonumber(value) or 0) + 0.5))
	local text = tostring(rounded)
	local sign = ""
	if string.sub(text, 1, 1) == "-" then
		sign = "-"
		text = string.sub(text, 2)
	end

	while true do
		local formatted, substitutions = string.gsub(text, "^(-?%d+)(%d%d%d)", "%1,%2")
		text = formatted
		if substitutions == 0 then
			break
		end
	end

	return sign .. text
end

function getSellableLootSummary(forceRefresh)
	local filledSlots, totalSlots = getInventorySlotCounts()
	local playerData = resolvePlayerDataModule(forceRefresh)
	local playerItems = resolveSharedPlayerItems(forceRefresh)
	if not isTable(playerData) or not isTable(playerItems) then
		return nil
	end

	local inventoryContainer = getResolvedInventoryContainer(playerData)
	if not isTable(inventoryContainer) then
		return nil
	end

	local items = rawget(inventoryContainer, "Items") or inventoryContainer.Items
	if not isTable(items) then
		return nil
	end

	local sellMetadata = resolveSharedPlayerItemSellMetadata(forceRefresh)
	local hasVipSellBonus = playerHasVipSellBonus(forceRefresh)
	local lootEntryCount = 0
	local lootUnitCount = 0
	local lootValueTotal = 0
	for _, inventoryItem in pairs(items) do
		if isTable(inventoryItem) then
			local itemType = rawget(inventoryItem, "Type") or inventoryItem.Type
			local sharedItemData = type(itemType) == "string" and (rawget(playerItems, itemType) or playerItems[itemType]) or nil
			local itemSellMetadata = type(itemType) == "string" and isTable(sellMetadata) and (rawget(sellMetadata, itemType) or sellMetadata[itemType]) or nil
			local isLoot = (isTable(sharedItemData) and ((rawget(sharedItemData, "IsLoot") or sharedItemData.IsLoot) == true))
				or (isTable(itemSellMetadata) and ((rawget(itemSellMetadata, "isLoot") or itemSellMetadata.isLoot) == true))
			if isLoot then
				lootEntryCount += 1
				local stackAmount = getInventoryItemStackAmount(inventoryItem)
				lootUnitCount += stackAmount

				local baseValue = tonumber(
					(isTable(sharedItemData) and (rawget(sharedItemData, "Value") or sharedItemData.Value))
						or (isTable(itemSellMetadata) and (rawget(itemSellMetadata, "value") or itemSellMetadata.value))
						or 0
				) or 0
				local stackSellSize = tonumber(
					(isTable(sharedItemData) and (rawget(sharedItemData, "StackSellSize") or sharedItemData.StackSellSize))
						or (isTable(itemSellMetadata) and (rawget(itemSellMetadata, "stackSellSize") or itemSellMetadata.stackSellSize))
						or 1
				) or 1
				if stackSellSize < 1 then
					stackSellSize = 1
				end

				local itemValueTotal = baseValue * (stackAmount / stackSellSize)
				local vipValueMultiplier = tonumber(
					(isTable(itemSellMetadata) and (rawget(itemSellMetadata, "vipValueMultiplier") or itemSellMetadata.vipValueMultiplier))
						or (isTable(sharedItemData) and (rawget(sharedItemData, "VIPValueMultiplier") or sharedItemData.VIPValueMultiplier))
						or 0
				) or 0
				if hasVipSellBonus and vipValueMultiplier > 0 then
					itemValueTotal = itemValueTotal * (1 + vipValueMultiplier)
				end
				lootValueTotal += itemValueTotal
			end
		end
	end

	return {
		hasLoot = lootEntryCount > 0,
		lootEntryCount = lootEntryCount,
		lootUnitCount = lootUnitCount,
		lootValueTotal = lootValueTotal,
		filledSlots = filledSlots,
		totalSlots = totalSlots,
	}
end

function wasSellableLootReduced(beforeSummary, afterSummary)
	if not (isTable(beforeSummary) and isTable(afterSummary)) then
		return false
	end

	local beforeEntryCount = tonumber(rawget(beforeSummary, "lootEntryCount") or beforeSummary.lootEntryCount) or 0
	local afterEntryCount = tonumber(rawget(afterSummary, "lootEntryCount") or afterSummary.lootEntryCount) or 0
	if afterEntryCount < beforeEntryCount then
		return true
	end

	local beforeUnitCount = tonumber(rawget(beforeSummary, "lootUnitCount") or beforeSummary.lootUnitCount) or beforeEntryCount
	local afterUnitCount = tonumber(rawget(afterSummary, "lootUnitCount") or afterSummary.lootUnitCount) or afterEntryCount
	if afterUnitCount < beforeUnitCount then
		return true
	end

	local beforeValueTotal = tonumber(rawget(beforeSummary, "lootValueTotal") or beforeSummary.lootValueTotal) or beforeUnitCount
	local afterValueTotal = tonumber(rawget(afterSummary, "lootValueTotal") or afterSummary.lootValueTotal) or afterUnitCount
	if afterValueTotal < beforeValueTotal then
		return true
	end

	local beforeHasLoot = rawget(beforeSummary, "hasLoot")
	if beforeHasLoot == nil then
		beforeHasLoot = beforeSummary.hasLoot
	end
	local afterHasLoot = rawget(afterSummary, "hasLoot")
	if afterHasLoot == nil then
		afterHasLoot = afterSummary.hasLoot
	end

	return beforeHasLoot == true and afterHasLoot == false
end

function getSoldLootValue(beforeSummary, afterSummary)
	if not isTable(beforeSummary) then
		return 0
	end

	local beforeValueTotal = tonumber(rawget(beforeSummary, "lootValueTotal") or beforeSummary.lootValueTotal) or 0
	local afterValueTotal = tonumber((isTable(afterSummary) and (rawget(afterSummary, "lootValueTotal") or afterSummary.lootValueTotal)) or 0) or 0
	return math.max(0, beforeValueTotal - afterValueTotal)
end

function setAutoSellThreshold(value, totalSlotsHint)
	local totalSlots = totalSlotsHint or lastInventoryTotalSlots or 0
	if totalSlots > 0 then
		autoSellThreshold = math.clamp(math.floor((value or 1) + 0.5), 1, totalSlots)
	else
		autoSellThreshold = math.max(1, math.floor((value or 1) + 0.5))
	end
end

function updateAutoSellThresholdFromPointer(pointerX)
	if not autoSellSliderHitbox then
		return
	end

	local _, totalSlots = getInventorySlotCounts()
	if totalSlots > 0 then
		lastInventoryTotalSlots = totalSlots
	end
	totalSlots = math.max(totalSlots or lastInventoryTotalSlots or 0, 1)
	local absolutePosition = autoSellSliderHitbox.AbsolutePosition
	local absoluteSize = autoSellSliderHitbox.AbsoluteSize
	local width = math.max(absoluteSize.X, 1)
	local ratio = math.clamp((pointerX - absolutePosition.X) / width, 0, 1)
	local threshold = math.floor((ratio * math.max(totalSlots - 1, 0)) + 1.5)
	setAutoSellThreshold(threshold, totalSlots)
end

function resolveAutoSellThreshold()
	local filledSlots, totalSlots = getInventorySlotCounts()
	lastInventoryFilledSlots = filledSlots
	lastInventoryTotalSlots = totalSlots

	if totalSlots > 0 then
		if autoSellThreshold <= 0 then
			autoSellThreshold = totalSlots
		else
			setAutoSellThreshold(autoSellThreshold, totalSlots)
		end
	end

	return filledSlots, totalSlots, autoSellThreshold
end

function isAutoSellThresholdReached()
	local filledSlots, totalSlots, threshold = resolveAutoSellThreshold()
	if not autoSellEnabled or totalSlots <= 0 or threshold <= 0 then
		return false, filledSlots, totalSlots, threshold
	end

	return filledSlots >= threshold, filledSlots, totalSlots, threshold
end

function isTable(value)
	return type(value) == "table"
end

function pushUniqueTable(list, seen, value)
	if isTable(value) and not seen[value] then
		seen[value] = true
		table.insert(list, value)
	end
end

function getEnvironmentTables()
	local list = {}
	local seen = {}

	if type(getgenv) == "function" then
		local ok, genv = pcall(getgenv)
		if ok then
			pushUniqueTable(list, seen, genv)
		end
	end

	if type(getrenv) == "function" then
		local ok, renv = pcall(getrenv)
		if ok then
			pushUniqueTable(list, seen, renv)
		end
	end

	pushUniqueTable(list, seen, _G)

	local okShared, sharedTable = pcall(function()
		return shared
	end)
	if okShared then
		pushUniqueTable(list, seen, sharedTable)
	end

	return list
end

local hasPlrCharacterModule
local resolveGameModules
local resolveGlobalNetwork

function hasGetEquippedItem(owner)
	return isTable(owner) and type(owner.GetEquippedItem) == "function"
end

function looksLikeToolInfo(toolInfo)
	if not isTable(toolInfo) then
		return false
	end

	local networkActivate = rawget(toolInfo, "NetworkActivate") or toolInfo.NetworkActivate
	return type(networkActivate) == "function"
end

function tryGetToolInfoFromOwner(owner)
	if not hasGetEquippedItem(owner) then
		return nil
	end

	local ok, toolInfo = pcall(function()
		return owner:GetEquippedItem()
	end)

	if ok and looksLikeToolInfo(toolInfo) then
		return toolInfo
	end

	return nil
end

function pushUniqueOwner(list, seen, owner)
	if hasGetEquippedItem(owner) and not seen[owner] then
		seen[owner] = true
		table.insert(list, owner)
	end
end

function collectOwnerCandidates(root, owners, ownerSeen, recurseSeen, depth)
	if depth <= 0 or not isTable(root) or recurseSeen[root] then
		return
	end

	recurseSeen[root] = true
	pushUniqueOwner(owners, ownerSeen, root)

	local directKeys = {
		"plrCharacter",
		"PlrCharacter",
		"character",
		"Character",
		"playerCharacter",
		"PlayerCharacter",
		"RepCharHandler",
		"repCharHandler",
		"Modules",
		"modules",
		"mainModule",
		"MainModule",
	}

	for _, key in ipairs(directKeys) do
		local value = rawget(root, key) or root[key]
		if hasGetEquippedItem(value) then
			pushUniqueOwner(owners, ownerSeen, value)
		elseif isTable(value) then
			collectOwnerCandidates(value, owners, ownerSeen, recurseSeen, depth - 1)
		end
	end
end

function collectGcOwnerCandidates(owners, ownerSeen)
	if type(getgc) ~= "function" then
		return
	end

	local ok, gcObjects = pcall(getgc, true)
	if not ok or type(gcObjects) ~= "table" then
		return
	end

	local deadline = os.clock() + TOOLINFO_GC_SCAN_BUDGET
	for _, object in ipairs(gcObjects) do
		if os.clock() > deadline then
			break
		end

		if isTable(object) then
			pushUniqueOwner(owners, ownerSeen, object)
			local plrCharacter = rawget(object, "plrCharacter") or object.plrCharacter
			pushUniqueOwner(owners, ownerSeen, plrCharacter)
		end
	end
end

function resolveToolInfoOwner(forceRefresh)
	if cachedToolInfoOwner and hasGetEquippedItem(cachedToolInfoOwner) then
		local cachedToolInfo = tryGetToolInfoFromOwner(cachedToolInfoOwner)
		if cachedToolInfo then
			return cachedToolInfoOwner, cachedToolInfo
		end
		cachedToolInfoOwner = nil
	end

	local now = os.clock()
	if not forceRefresh and (now - lastToolInfoOwnerScanAt) < RESCAN_INTERVAL then
		return nil
	end
	lastToolInfoOwnerScanAt = now

	local owners = {}
	local ownerSeen = {}
	local recurseSeen = {}

	local modulesTable = resolveGameModules(forceRefresh)
	if hasPlrCharacterModule(modulesTable) then
		pushUniqueOwner(owners, ownerSeen, modulesTable.plrCharacter)
	end

	resolveGlobalNetwork(forceRefresh)
	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if isTable(globalModule) then
		pushUniqueOwner(owners, ownerSeen, rawget(globalModule, "PlayerCharacter") or globalModule.PlayerCharacter)
		pushUniqueOwner(owners, ownerSeen, rawget(globalModule, "playerCharacter") or globalModule.playerCharacter)
		pushUniqueOwner(owners, ownerSeen, rawget(globalModule, "RepCharHandler") or globalModule.RepCharHandler)
		pushUniqueOwner(owners, ownerSeen, rawget(globalModule, "repCharHandler") or globalModule.repCharHandler)
	end

	if isTable(cachedMainModule) then
		collectOwnerCandidates(cachedMainModule, owners, ownerSeen, recurseSeen, 3)
	end

	for _, root in ipairs(getEnvironmentTables()) do
		collectOwnerCandidates(root, owners, ownerSeen, recurseSeen, 3)
	end

	collectGcOwnerCandidates(owners, ownerSeen)

	for _, owner in ipairs(owners) do
		local toolInfo = tryGetToolInfoFromOwner(owner)
		if toolInfo then
			cachedToolInfoOwner = owner
			return owner, toolInfo
		end
	end

	return nil
end

hasPlrCharacterModule = function(modulesTable)
	if not isTable(modulesTable) then
		return false
	end

	local plrCharacter = rawget(modulesTable, "plrCharacter") or modulesTable.plrCharacter
	return isTable(plrCharacter) and type(plrCharacter.GetEquippedItem) == "function"
end

function looksLikePlayerCharacterModule(candidate)
    if not isTable(candidate) then
        return false
    end

    return type(rawget(candidate, "GetEquippedItem") or candidate.GetEquippedItem) == "function"
        or type(rawget(candidate, "CanSwitchToItem") or candidate.CanSwitchToItem) == "function"
        or type(rawget(candidate, "CanGetUp") or candidate.CanGetUp) == "function"
        or type(rawget(candidate, "ToggleSelfRagdoll") or candidate.ToggleSelfRagdoll) == "function"
        or type(rawget(candidate, "Ragdoll") or candidate.Ragdoll) == "function"
        or rawget(candidate, "BreakFreePerc") ~= nil
end

function looksLikePlayerDataModule(candidate)
	if not isTable(candidate) then
		return false
	end

	local setLoadout = rawget(candidate, "SetLoadout") or candidate.SetLoadout
	local getLoadout = rawget(candidate, "GetLoadout") or candidate.GetLoadout
	local getContainer = rawget(candidate, "GetContainer") or candidate.GetContainer
	local inventoryContainer = rawget(candidate, "InventoryContainer") or candidate.InventoryContainer
	return type(setLoadout) == "function"
		or (type(getLoadout) == "function" and (type(getContainer) == "function" or isTable(inventoryContainer)))
end

function extractSyncedTime(source)
	if not isTable(source) then
		return nil
	end

	local syncedTime = rawget(source, "SyncedTime") or source.SyncedTime
	if isTable(syncedTime) and type(syncedTime.GetTime) == "function" then
		return syncedTime
	end

	return nil
end

resolveGameModules = function(forceRefresh)
	if cachedGameModules and hasPlrCharacterModule(cachedGameModules) then
		return cachedGameModules
	end

	local now = os.clock()
	if not forceRefresh and (now - lastModuleScanAt) < RESCAN_INTERVAL then
		return nil
	end

	lastModuleScanAt = now

	for _, root in ipairs(getEnvironmentTables()) do
		local directMain = rawget(root, "mainModule") or root.mainModule
		local directMainAlt = rawget(root, "MainModule") or root.MainModule

		local candidates = {
			rawget(root, "Modules") or root.Modules,
			rawget(root, "modules") or root.modules,
			directMain and (rawget(directMain, "Modules") or directMain.Modules),
			directMainAlt and (rawget(directMainAlt, "Modules") or directMainAlt.Modules),
			root,
		}

		for _, candidate in ipairs(candidates) do
			if hasPlrCharacterModule(candidate) then
				cachedGameModules = candidate
				cachedMainModule = rawget(candidate, "mainModule")
					or candidate.mainModule
					or rawget(candidate, "MainModule")
					or candidate.MainModule
					or directMain
					or directMainAlt
				return cachedGameModules
			end
		end
	end

	return nil
end

function resolveSyncedTime(forceRefresh)
	if cachedSyncedTime and type(cachedSyncedTime.GetTime) == "function" then
		return cachedSyncedTime
	end

	local modulesTable = resolveGameModules(forceRefresh)
	if modulesTable then
		local moduleMain = rawget(modulesTable, "mainModule")
			or modulesTable.mainModule
			or rawget(modulesTable, "MainModule")
			or modulesTable.MainModule
		if moduleMain then
			cachedMainModule = moduleMain
		end
	end

	local mainSynced = extractSyncedTime(cachedMainModule)
	if mainSynced then
		cachedSyncedTime = mainSynced
		return cachedSyncedTime
	end

	for _, root in ipairs(getEnvironmentTables()) do
		local synced = extractSyncedTime(root)
			or extractSyncedTime(rawget(root, "mainModule") or root.mainModule)
			or extractSyncedTime(rawget(root, "MainModule") or root.MainModule)
		if synced then
			cachedSyncedTime = synced
			return cachedSyncedTime
		end
	end

	local sharedModules = ReplicatedStorage:FindFirstChild("SharedModules")
	local globalModuleScript = sharedModules and sharedModules:FindFirstChild("Global")
	if globalModuleScript and globalModuleScript:IsA("ModuleScript") then
		local okRequire, globalModule = pcall(require, globalModuleScript)
		if okRequire and isTable(globalModule) then
			local synced = extractSyncedTime(globalModule)
			if not synced and type(globalModule.LoadModule) == "function" then
				local okLoad, syncedModule = pcall(function()
					return globalModule:LoadModule("SyncedTime")
				end)
				if okLoad and isTable(syncedModule) and type(syncedModule.GetTime) == "function" then
					synced = syncedModule
				end
			end
			if synced then
				cachedSyncedTime = synced
				return cachedSyncedTime
			end
		end
	end

	return nil
end

function getSyncedNetworkTime()
	local syncedTime = resolveSyncedTime(false)
	if syncedTime and type(syncedTime.GetTime) == "function" then
		local ok, value = pcall(function()
			return syncedTime:GetTime()
		end)
		if ok and type(value) == "number" then
			return value
		end
	end

	return 0
end

function isStoredPickaxeName(name)
	if type(name) ~= "string" or name == "" then
		return false
	end

	local lowered = safeLower(tostring(name)):gsub("\\", "/")
	return string.find(lowered, "loadoutitem/", 1, true) ~= nil
		and string.find(lowered, "pickaxe", 1, true) ~= nil
end

function isEquippedPickaxeName(name)
	if type(name) ~= "string" or name == "" then
		return false
	end

	local lowered = safeLower(tostring(name)):gsub("\\", "/")
	return string.find(lowered, "loadoutitem/", 1, true) == nil
		and string.find(lowered, "pickaxe", 1, true) ~= nil
end

function matchesPickaxeName(instance)
	local name = typeof(instance) == "Instance" and instance.Name or instance
	if type(name) ~= "string" or name == "" then
		return false
	end

	return string.find(safeLower(tostring(name)):gsub("\\", "/"), "pickaxe", 1, true) ~= nil
end

function stripStoredPickaxePrefix(name)
	if type(name) ~= "string" or name == "" then
		return nil
	end

	local normalized = tostring(name):gsub("\\", "/")
	local lowered = safeLower(normalized)
	local prefix = "loadoutitem/"
	if string.sub(lowered, 1, #prefix) == prefix then
		return string.sub(normalized, #prefix + 1)
	end

	return normalized
end

function appendPickaxeNameHint(hints, seen, value)
	if type(value) ~= "string" or value == "" then
		return
	end

	local normalized = tostring(value):gsub("\\", "/")
	if not matchesPickaxeName(normalized) then
		return
	end

	local function addHint(name)
		if type(name) == "string" and name ~= "" and not seen[name] then
			seen[name] = true
			table.insert(hints, name)
		end
	end

	local baseName = stripStoredPickaxePrefix(normalized) or normalized
	local strippedItemName = type(baseName) == "string" and string.gsub(baseName, "Item$", "") or baseName
	addHint(baseName)
	addHint("LoadoutItem/" .. baseName)
	if strippedItemName ~= baseName then
		addHint(strippedItemName)
		addHint("LoadoutItem/" .. strippedItemName)
	end
	if string.sub(baseName, -4) ~= "Item" then
		addHint(baseName .. "Item")
	end
	if type(strippedItemName) == "string" and strippedItemName ~= "" and string.sub(strippedItemName, -4) ~= "Item" then
		addHint(strippedItemName .. "Item")
	end
end

function collectPickaxeNameHints(...)
	local hints = {}
	local seen = {}
	local visitedTables = {}

	local function scanValue(value)
		if value == nil then
			return
		end

		if type(value) == "string" then
			appendPickaxeNameHint(hints, seen, value)
			return
		end

		if typeof(value) == "Instance" then
			appendPickaxeNameHint(hints, seen, value.Name)
			return
		end

		if not isTable(value) or visitedTables[value] then
			return
		end

		visitedTables[value] = true
		for _, key in ipairs({
			"Type",
			"Id",
			"Name",
			"DisplayName",
			"ItemType",
			"VariantOf",
			"Model",
			"ModelName",
			"WorldModel",
			"WorldModelName",
			"ViewModel",
			"ViewModelName",
		}) do
			scanValue(rawget(value, key) or value[key])
		end

		scanValue(rawget(value, "ItemData") or value.ItemData)
	end

	for index = 1, select("#", ...) do
		scanValue(select(index, ...))
	end

	return hints
end

function getBestCharacterPickaxeModel(character, nameMatcher)
	if not character then
		return nil
	end

	local bestPickaxe = nil
	local bestScore = 0
	for _, child in ipairs(character:GetChildren()) do
		if (child:IsA("Model") or child:IsA("Tool")) and (nameMatcher == nil or nameMatcher(child.Name)) then
			local score = getPickaxeTextScore(child.Name)
			if score > bestScore then
				bestScore = score
				bestPickaxe = child
			end
		end
	end

	return bestPickaxe
end

function getBackpackPickaxeTool(player)
	player = player or localPlayer or LocalPlayer
	if not player then
		return nil
	end

	local backpack = player:FindFirstChildOfClass("Backpack")
	if not backpack then
		return nil
	end

	local bestTool = nil
	local bestScore = 0
	for _, child in ipairs(backpack:GetChildren()) do
		if child:IsA("Tool") and matchesPickaxeName(child.Name) then
			local score = getPickaxeTextScore(child.Name)
			if score > bestScore then
				bestScore = score
				bestTool = child
			end
		end
	end

	return bestTool
end

function setStatus(text, color)
	if statusLabel then
		pcall(function()
			statusLabel.Text = text
			if color then
				statusLabel.TextColor3 = color
			end
		end)
	end
end

function setLastMoveCommand(direction, source)
	lastMoveCommand = direction or Vector3.zero
	lastMoveCommandSource = source or "unknown"
end

function sendKeyState(keyCode, isDown)
	if not VirtualInputManager then
		return false
	end

	local ok = pcall(function()
		VirtualInputManager:SendKeyEvent(isDown, keyCode, false, game)
	end)

	return ok
end

function setMovementKey(keyCode, isDown)
	if movementKeyState[keyCode] == isDown then
		return
	end

	movementKeyState[keyCode] = isDown
	sendKeyState(keyCode, isDown)
end

function clearMovementKeys()
	setMovementKey(Enum.KeyCode.W, false)
	setMovementKey(Enum.KeyCode.A, false)
	setMovementKey(Enum.KeyCode.S, false)
	setMovementKey(Enum.KeyCode.D, false)
	setMovementKey(Enum.KeyCode.LeftShift, false)
end

local AUTO_MINE_INPUT_LOCK_ITEMS = {
	Enum.PlayerActions.CharacterForward,
	Enum.PlayerActions.CharacterBackward,
	Enum.PlayerActions.CharacterLeft,
	Enum.PlayerActions.CharacterRight,
	Enum.PlayerActions.CharacterJump,
	Enum.KeyCode.W,
	Enum.KeyCode.A,
	Enum.KeyCode.S,
	Enum.KeyCode.D,
	Enum.KeyCode.Space,
	Enum.KeyCode.Up,
	Enum.KeyCode.Down,
	Enum.KeyCode.Left,
	Enum.KeyCode.Right,
}

local function areAutoMineMovementKeysHeld()
	return movementKeyState[Enum.KeyCode.W]
		or movementKeyState[Enum.KeyCode.A]
		or movementKeyState[Enum.KeyCode.S]
		or movementKeyState[Enum.KeyCode.D]
		or movementKeyState[Enum.KeyCode.LeftShift]
end

local function getLocalPlayerControls()
	local playerScripts = LocalPlayer and LocalPlayer:FindFirstChild("PlayerScripts")
	if not playerScripts then
		return nil
	end

	local playerModule = playerScripts:FindFirstChild("PlayerModule")
	if not playerModule then
		return nil
	end

	local okRequire, moduleResult = pcall(require, playerModule)
	if not okRequire or not moduleResult or type(moduleResult.GetControls) ~= "function" then
		return nil
	end

	local okControls, controls = pcall(moduleResult.GetControls, moduleResult)
	if not okControls then
		return nil
	end

	return controls
end

local function restoreAutoMineMovementHumanoid()
	local humanoid = autoMineLockedHumanoid
	if humanoid then
		pcall(function()
			if autoMineLockedWalkSpeed ~= nil then
				humanoid.WalkSpeed = autoMineLockedWalkSpeed
			end
		end)
		pcall(function()
			if autoMineLockedAutoRotate ~= nil then
				humanoid.AutoRotate = autoMineLockedAutoRotate
			end
		end)
		pcall(function()
			if autoMineLockedUseJumpPower ~= nil then
				humanoid.UseJumpPower = autoMineLockedUseJumpPower
			end
		end)
		pcall(function()
			if autoMineLockedJumpPower ~= nil then
				humanoid.JumpPower = autoMineLockedJumpPower
			end
		end)
		pcall(function()
			if autoMineLockedJumpHeight ~= nil then
				humanoid.JumpHeight = autoMineLockedJumpHeight
			end
		end)
	end

	autoMineLockedHumanoid = nil
	autoMineLockedWalkSpeed = nil
	autoMineLockedUseJumpPower = nil
	autoMineLockedJumpPower = nil
	autoMineLockedJumpHeight = nil
	autoMineLockedAutoRotate = nil
end

local function releaseAutoMineManualMovementLock()
	if autoMineInputLockBound and ContextActionService then
		pcall(function()
			ContextActionService:UnbindAction(AUTOMINE_INPUT_LOCK_ACTION_NAME)
		end)
	end
	autoMineInputLockBound = false

	if autoMineLockedControls and type(autoMineLockedControls.Enable) == "function" then
		pcall(autoMineLockedControls.Enable, autoMineLockedControls)
	end
	autoMineLockedControls = nil

	restoreAutoMineMovementHumanoid()
end

local function ensureAutoMineManualMovementLock(character, humanoid)
	if not autoMineEnabled then
		releaseAutoMineManualMovementLock()
		return
	end

	if ContextActionService and movementMode ~= "legit" and not autoMineInputLockBound then
		local okBind = pcall(function()
			ContextActionService:BindActionAtPriority(
				AUTOMINE_INPUT_LOCK_ACTION_NAME,
				function()
					return Enum.ContextActionResult.Sink
				end,
				false,
				Enum.ContextActionPriority.High.Value,
				table.unpack(AUTO_MINE_INPUT_LOCK_ITEMS)
			)
		end)
		autoMineInputLockBound = okBind == true
	end

	if movementMode ~= "legit" then
		if not autoMineLockedControls then
			autoMineLockedControls = getLocalPlayerControls()
			if autoMineLockedControls and type(autoMineLockedControls.Disable) == "function" then
				pcall(autoMineLockedControls.Disable, autoMineLockedControls)
			end
		end

		if humanoid and autoMineLockedHumanoid ~= humanoid then
			restoreAutoMineMovementHumanoid()
			autoMineLockedHumanoid = humanoid
			autoMineLockedWalkSpeed = humanoid.WalkSpeed
			autoMineLockedAutoRotate = humanoid.AutoRotate
			autoMineLockedUseJumpPower = humanoid.UseJumpPower
			autoMineLockedJumpPower = humanoid.JumpPower
			autoMineLockedJumpHeight = humanoid.JumpHeight
		end

		if humanoid then
			pcall(function()
				humanoid.WalkSpeed = 0
				humanoid.AutoRotate = false
				humanoid.Jump = false
				if humanoid.UseJumpPower then
					humanoid.JumpPower = 0
				else
					humanoid.JumpHeight = 0
				end
				humanoid:Move(Vector3.zero)
			end)
		end
		return
	end

	if autoMineInputLockBound and ContextActionService then
		pcall(function()
			ContextActionService:UnbindAction(AUTOMINE_INPUT_LOCK_ACTION_NAME)
		end)
	end
	autoMineInputLockBound = false

	if autoMineLockedControls and type(autoMineLockedControls.Enable) == "function" then
		pcall(autoMineLockedControls.Enable, autoMineLockedControls)
	end
	autoMineLockedControls = nil
	restoreAutoMineMovementHumanoid()

	if humanoid and not areAutoMineMovementKeysHeld() then
		pcall(function()
			humanoid.Jump = false
			humanoid:Move(Vector3.zero)
		end)
	end
end

function setInteractKeyHeld(isDown)
	if interactKeyHeld == isDown then
		return
	end

	interactKeyHeld = isDown
	sendKeyState(INTERACT_KEY, isDown)
end

function cancelActiveMoveTween()
	if activeMoveTween then
		pcall(function()
			activeMoveTween:Cancel()
		end)
		activeMoveTween = nil
	end
end

function clampTweenSpeed(value)
	return math.clamp(value, TWEEN_SPEED_MIN, TWEEN_SPEED_MAX)
end

function setTweenSpeed(value, skipPersistence)
	local nextValue = clampTweenSpeed(value)
	local changed = math.abs((tonumber(tweenMoveSpeed) or TWEEN_MOVE_SPEED) - nextValue) > 0.001
	tweenMoveSpeed = nextValue
	if tweenSpeedBox then
		tweenSpeedBox.Text = tostring(math.floor(tweenMoveSpeed + 0.5))
	end
	if changed and not skipPersistence and type(schedulePathSettingsPersist) == "function" then
		schedulePathSettingsPersist()
	end
end

function getEffectiveCFrameSpeed()
	local sliderSpeed = math.max(tonumber(tweenMoveSpeed) or TWEEN_MOVE_SPEED, 1)
	if sliderSpeed <= 10 then
		return sliderSpeed
	end
	if sliderSpeed <= 40 then
		return 10 + ((sliderSpeed - 10) * 1.25)
	end
	return 47.5 + ((sliderSpeed - 40) * 1.55)
end

function restoreMineAura()
	for part, original in pairs(auraOriginalParts) do
		if part and part.Parent then
			pcall(function()
				part.Size = original.Size
			end)
		end
		auraOriginalParts[part] = nil
	end
end

function applyMineAuraToPickaxe(pickaxe)
	if not mineAuraEnabled or not pickaxe then
		return
	end

	for _, descendant in ipairs(pickaxe:GetDescendants()) do
		if descendant:IsA("BasePart") then
			local partName = safeLower(descendant.Name)
			if string.find(partName, "hitbox", 1, true) ~= nil then
				if not auraOriginalParts[descendant] then
					auraOriginalParts[descendant] = {
						Size = descendant.Size,
					}
				end

				pcall(function()
					local baseSize = auraOriginalParts[descendant].Size
					descendant.Size = Vector3.new(
						baseSize.X * MINE_AURA_MULTIPLIER,
						baseSize.Y * MINE_AURA_MULTIPLIER,
						baseSize.Z * MINE_AURA_MULTIPLIER
					)
				end)
			end
		end
	end
end

function pushDebug(text)
	local line = tostring(text)
	if debugLines[1] == line then
		return
	end

	warn("[AutoMine] " .. line)

	table.insert(debugLines, 1, line)
	while #debugLines > 3 do
		table.remove(debugLines)
	end

	if debugLabel then
		pcall(function()
			debugLabel.Text = table.concat(debugLines, "\n")
		end)
	end
end

function pushDebugRateLimited(key, text, interval)
	local now = os.clock()
	local throttle = interval or 1
	local lastTime = debugCooldowns[key] or 0
	if (now - lastTime) < throttle then
		return
	end

	debugCooldowns[key] = now
	pushDebug(text)
end

function notifyUser(title, text, duration)
    local textProvider = type(text) == "function" and text or nil
    local notifyText = textProvider and tostring(textProvider() or "") or tostring(text or "")
    local notifyTitle = tostring(title or "Automine")
    if type(spawnNotification) == "function" then
        local lowered = safeLower(notifyTitle)
        local notifType = "info"
        if string.find(lowered, "warn", 1, true) then
            notifType = "warning"
        elseif string.find(lowered, "error", 1, true) then
            notifType = "error"
        elseif string.find(lowered, "success", 1, true) then
            notifType = "success"
        end
        local updater = nil
        if textProvider then
            updater = function()
                return notifyTitle .. ": " .. tostring(textProvider() or notifyText)
            end
        end
        spawnNotification(notifyTitle .. ": " .. notifyText, notifType, true, duration, updater)
    else
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = notifyTitle,
                Text = notifyText,
                Duration = duration or 4,
            })
        end)
    end
    pushDebug((title and (title .. ": ") or "") .. tostring(text or ""))
end

function notifyRubberBandDetected()
	local now = os.clock()
	if (now - lastRubberBandNoticeAt) < RUBBERBAND_NOTIFY_COOLDOWN then
		return
	end

	lastRubberBandNoticeAt = now
	if type(spawnNotification) == "function" then
		spawnNotification("Rubber band detected.", "warning", true, 3)
	else
		notifyUser("Warning", "Rubber band detected.", 3)
	end
end

function handlePathPlaybackRubberBand(character, targetPosition)
	cancelActiveMoveTween()
	clearMovementKeys()
	setInteractKeyHeld(false)
	if movementMode == "cframe" and type(endExperimentalCFrameTravel) == "function" then
		endExperimentalCFrameTravel(character)
	end
	local closestNodeIndex = nil
	local closestNodeDistance = nil
	local rootPart = getRootPart(character)
	if rootPart and pathState.currentPath then
		closestNodeIndex, closestNodeDistance = getClosestPlaybackPathNodeIndex(pathState.currentPath, rootPart.Position)
		if closestNodeIndex then
			pathState.playbackNodeIndex = closestNodeIndex
			pathState.entryPathingPending = true
		end
	end
	setStatus("Rubber band detected", Color3.fromRGB(255, 190, 120))
	pushDebugRateLimited(
		"path_rubberband",
		"rubber band detected while pathing to "
			.. (typeof(targetPosition) == "Vector3" and string.format("(%.1f, %.1f, %.1f)", targetPosition.X, targetPosition.Y, targetPosition.Z) or "node"),
		1.25
	)
	if closestNodeIndex then
		pushDebugRateLimited(
			"path_rubberband_resync",
			string.format("rubber band resync -> node %d (%.1f studs)", closestNodeIndex, tonumber(closestNodeDistance) or -1),
			1.25
		)
	end
	notifyRubberBandDetected()
	task.wait(RUBBERBAND_RETRY_DELAY)
end

function getCallableGlobal(name)
	local direct = rawget(_G, name)
	if type(direct) == "function" then
		return direct
	end

	if type(getgenv) == "function" then
		local ok, environment = pcall(getgenv)
		if ok and type(environment) == "table" and type(environment[name]) == "function" then
			return environment[name]
		end
	end

	return nil
end

function ensurePathStorageSupport(showNotice)
	if pathState.storageChecked then
		if showNotice and not pathState.storageSupported and not pathState.storageNoticeShown then
			notifyUser("Path Storage", "Missing executor file APIs: " .. table.concat(pathState.storageMissing, ", "), 6)
			pathState.storageNoticeShown = true
		end
		return pathState.storageSupported
	end

	local missing = {}
	for _, functionName in ipairs({ "writefile", "readfile", "isfile", "makefolder" }) do
		if not getCallableGlobal(functionName) then
			table.insert(missing, functionName)
		end
	end

	if not getCallableGlobal("listfiles") then
		table.insert(missing, "listfiles")
	end

	pathState.storageChecked = true
	pathState.storageMissing = missing
	pathState.storageSupported = #missing == 0 or (#missing == 1 and missing[1] == "listfiles")

	if showNotice and not pathState.storageSupported and not pathState.storageNoticeShown then
		notifyUser("Path Storage", "Missing executor file APIs: " .. table.concat(pathState.storageMissing, ", "), 6)
		pathState.storageNoticeShown = true
	end

	return pathState.storageSupported
end

function sanitizePathStorageKey(rawValue)
	local cleaned = tostring(rawValue or "local")
	cleaned = string.gsub(cleaned, "[^%w_%-]", "_")
	cleaned = string.gsub(cleaned, "_+", "_")
	return cleaned ~= "" and cleaned or "local"
end

function getPathStorageFilePath()
	local deviceKey = nil
	local hwidFunction = getCallableGlobal("gethwid")
	if hwidFunction then
		local ok, value = pcall(hwidFunction)
		if ok and value then
			deviceKey = value
		end
	end

	if not deviceKey then
		deviceKey = tostring(LocalPlayer.UserId)
	end

	local fileName = string.format("%s_%s.json", PATH_STORAGE_FILE_PREFIX, sanitizePathStorageKey(deviceKey))
	return PATH_STORAGE_FOLDER .. "\\" .. fileName
end

function vectorToData(vector)
	return {
		x = math.floor(vector.X * 100 + 0.5) / 100,
		y = math.floor(vector.Y * 100 + 0.5) / 100,
		z = math.floor(vector.Z * 100 + 0.5) / 100,
	}
end

function vectorFromData(data)
	if type(data) ~= "table" then
		return nil
	end

	return Vector3.new(tonumber(data.x) or 0, tonumber(data.y) or 0, tonumber(data.z) or 0)
end

function normalizePathNodeIndex(index, nodeCount)
	local numericIndex = tonumber(index)
	if not numericIndex then
		return nil
	end

	numericIndex = math.floor(numericIndex)
	if numericIndex < 1 or numericIndex > math.max(tonumber(nodeCount) or 0, 0) then
		return nil
	end

	return numericIndex
end

function getManualPathSellNodeIndex(pathData)
	local nodes = pathData and pathData.nodes or {}
	return normalizePathNodeIndex(pathData and pathData.manualSellNodeIndex, #nodes)
end

function getPathNodePosition(node)
	if typeof(node) == "Vector3" then
		return node
	end
	if type(node) == "table" then
		if typeof(node.position) == "Vector3" then
			return node.position
		end
		local nestedPosition = vectorFromData(node.position)
		if nestedPosition then
			return nestedPosition
		end
	end
	return nil
end

function getClosestPlaybackPathNodeIndex(pathData, worldPosition)
	if type(pathData) ~= "table" or typeof(worldPosition) ~= "Vector3" then
		return nil, nil
	end

	local nodes = pathData.nodes or {}
	local bestNodeIndex = nil
	local bestDistance = nil
	local flatWorldPosition = Vector3.new(worldPosition.X, 0, worldPosition.Z)
	for nodeIndex, node in ipairs(nodes) do
		local nodePosition = getPathNodePosition(node)
		if nodePosition then
			local flatNodePosition = Vector3.new(nodePosition.X, 0, nodePosition.Z)
			local distance = (flatWorldPosition - flatNodePosition).Magnitude
			if not bestDistance or distance < bestDistance then
				bestDistance = distance
				bestNodeIndex = nodeIndex
			end
		end
	end

	return bestNodeIndex, bestDistance
end

function setPathNodePosition(pathData, nodeIndex, position)
	if not pathData or not position then
		return false
	end

	local currentNode = pathData.nodes and pathData.nodes[nodeIndex]
	if not currentNode then
		return false
	end

	if typeof(currentNode) == "Vector3" then
		pathData.nodes[nodeIndex] = position
		return true
	end

	if type(currentNode) == "table" then
		currentNode.position = position
		return true
	end

	pathData.nodes[nodeIndex] = position
	return true
end

function serializePathData(pathData)
	local serialized = {
		nodes = {},
		ores = {},
		manualSellNodeIndex = getManualPathSellNodeIndex(pathData),
		spawnName = normalizePathSpawnName(pathData and pathData.spawnName),
	}

	for _, node in ipairs(pathData.nodes or {}) do
		local nodePosition = getPathNodePosition(node)
		if nodePosition then
			table.insert(serialized.nodes, {
				position = vectorToData(nodePosition),
			})
		end
	end

	for _, ore in ipairs(pathData.ores or {}) do
		table.insert(serialized.ores, {
			groupName = ore.groupName,
			position = vectorToData(ore.position),
			nodeIndex = ore.nodeIndex,
		})
	end

	return serialized
end

function deserializePathData(pathData)
	local result = {
		nodes = {},
		ores = {},
		manualSellNodeIndex = nil,
		spawnName = normalizePathSpawnName(pathData and pathData.spawnName),
	}

	for _, node in ipairs(pathData and pathData.nodes or {}) do
		local position = getPathNodePosition(node) or vectorFromData(node)
		if position then
			table.insert(result.nodes, {
				position = position,
			})
		end
	end

	for _, ore in ipairs(pathData and pathData.ores or {}) do
		local position = vectorFromData(ore.position)
		if position then
			table.insert(result.ores, {
				groupName = ore.groupName,
				position = position,
				nodeIndex = tonumber(ore.nodeIndex) or 0,
			})
		end
	end

	result.manualSellNodeIndex = normalizePathNodeIndex(pathData and pathData.manualSellNodeIndex, #result.nodes)

	return result
end

function getSavedPathNamesOrdered()
	local names = {}
	for pathName in pairs(pathState.paths) do
		table.insert(names, pathName)
	end
	table.sort(names, function(left, right)
		return string.lower(left) < string.lower(right)
	end)
	return names
end

function savePathsToStorage(showNotice)
	if not ensurePathStorageSupport(showNotice) then
		return false
	end

	ensurePathSlotState()
	local serializedPaths = {}
	for pathName, pathData in pairs(pathState.paths) do
		serializedPaths[pathName] = serializePathData(pathData)
	end

	local serializedPathSlots = {}
	for slotIndex = 1, MAX_PATH_PLAYBACK_SLOTS do
		serializedPathSlots[slotIndex] = getPathSlotSelection(slotIndex, true)
	end

	local payload = {
		version = 4,
		settings = {
			selectedPathName = pathState.selectedPathName,
			pathSlots = serializedPathSlots,
			pathSectionCount = pathState.pathSectionCount,
			focusedPathSlotIndex = pathState.focusedPathSlotIndex,
			sequenceStartSlotIndex = pathState.sequenceStartSlotIndex,
			alwaysPlayEnabled = pathState.alwaysPlayEnabled,
			farmPathsEnabled = pathState.farmPathsEnabled == true,
			staffHopEnabled = pathState.staffHopEnabled == true,
			playerAvoiderEnabled = pathState.playerAvoiderEnabled == true,
			playerAvoiderDistance = getPathPlayerAvoiderDistance(),
			playerAvoiderShowCorridor = pathState.playerAvoiderShowCorridor == true,
			playerAvoiderMethod = getPathPlayerAvoiderMethod(),
			playerAvoiderAction = getPathPlayerAvoiderAction(),
			playerAvoiderRadius = getPathPlayerAvoiderRadius(),
			playerAvoiderShowRadius = pathState.playerAvoiderShowRadius ~= false,
			hopOnDeathEnabled = pathState.hopOnDeathEnabled == true,
			playLoopEnabled = false,
			resumePathPlaybackPending = pathState.resumePathPlaybackPending == true,
			markOreKey = pathState.markOreKey and pathState.markOreKey.Name or DEFAULT_AUTOFARM_KEY.Name,
			recordNodeSpacing = getPathRecordNodeSpacing(),
			movementMode = normalizeMovementMode(movementMode),
			tweenMoveSpeed = clampTweenSpeed(tweenMoveSpeed),
			serverHopEnabled = serverHopEnabled == true,
		},
		paths = serializedPaths,
	}

	local makefolderFunction = getCallableGlobal("makefolder")
	local writefileFunction = getCallableGlobal("writefile")
	pcall(makefolderFunction, PATH_STORAGE_FOLDER)
	local ok = false
	local err = nil
	ok, err = pcall(writefileFunction, getPathStorageFilePath(), HttpService:JSONEncode(payload))

	if not ok then
		notifyUser("Path Storage", "Failed to save paths: " .. tostring(err), 5)
		return false
	end

	return true
end

function loadPathsFromStorage()
	if not ensurePathStorageSupport(false) then
		return false
	end

	local isfileFunction = getCallableGlobal("isfile")
	local readfileFunction = getCallableGlobal("readfile")
	local filePath = getPathStorageFilePath()
	local okExists, exists = pcall(isfileFunction, filePath)
	if not okExists or not exists then
		return false
	end

	local okRead, contents = pcall(readfileFunction, filePath)
	if not okRead or type(contents) ~= "string" or contents == "" then
		return false
	end

	local okDecode, payload = pcall(function()
		return HttpService:JSONDecode(contents)
	end)
	if not okDecode or type(payload) ~= "table" then
		notifyUser("Path Storage", "Saved path data could not be read.", 5)
		return false
	end

	pathState.paths = {}
	for pathName, pathData in pairs(payload.paths or {}) do
		pathState.paths[pathName] = deserializePathData(pathData)
	end

	local settings = payload.settings or {}
	ensurePathSlotState()
	pathState.selectedPathName = nil
	pathState.pathSlots = {nil, nil, nil, nil}
	pathState.pathSectionCount = 1
	pathState.focusedPathSlotIndex = 1
	pathState.sequenceStartSlotIndex = 1
	pathState.alwaysPlayEnabled = false
	pathState.farmPathsEnabled = false
	pathState.staffHopEnabled = false
	pathState.playerAvoiderEnabled = S.pathPlayerAvoiderEnabled == true
	pathState.playerAvoiderDistance = math.clamp(math.floor((tonumber(S.pathPlayerAvoiderDistance) or DEFAULTS.pathPlayerAvoiderDistance) + 0.5), 1, 200)
	pathState.playerAvoiderShowCorridor = S.pathPlayerAvoiderShowCorridor == true
	pathState.playerAvoiderMethod = normalizePathPlayerAvoiderMethod(S.pathPlayerAvoiderMethod)
	pathState.playerAvoiderAction = normalizePathPlayerAvoiderAction(S.pathPlayerAvoiderAction)
	pathState.playerAvoiderRadius = math.clamp(math.floor((tonumber(S.pathPlayerAvoiderRadius) or DEFAULTS.pathPlayerAvoiderRadius) + 0.5), 5, 200)
	pathState.playerAvoiderShowRadius = S.pathPlayerAvoiderShowRadius ~= false
	pathState.hopOnDeathEnabled = S.pathHopOnDeathEnabled == true
	pathState.playLoopEnabled = false
	pathState.resumePathPlaybackPending = false
	if type(settings.markOreKey) == "string" and Enum.KeyCode[settings.markOreKey] then
		pathState.markOreKey = Enum.KeyCode[settings.markOreKey]
	end
	if type(settings.recordNodeSpacing) == "number" then
		setPathRecordNodeSpacing(settings.recordNodeSpacing, true)
	end
	if type(settings.movementMode) == "string" then
		setMovementMode(settings.movementMode, "storage", true)
	end
	if type(settings.tweenMoveSpeed) == "number" then
		setTweenSpeed(settings.tweenMoveSpeed, true)
	end
	if isTable(settings.pathSlots) then
		for slotIndex = 1, MAX_PATH_PLAYBACK_SLOTS do
			local slotName = settings.pathSlots[slotIndex]
			if type(slotName) == "string" and pathState.paths[slotName] ~= nil then
				pathState.pathSlots[slotIndex] = slotName
			end
		end
	else
		local selectedPathName = settings.selectedPathName
		if type(selectedPathName) == "string" and pathState.paths[selectedPathName] ~= nil then
			pathState.pathSlots[1] = selectedPathName
		end
	end
	if type(settings.pathSectionCount) == "number" then
		pathState.pathSectionCount = settings.pathSectionCount
	else
		local highestConfiguredSlot = 1
		for slotIndex = MAX_PATH_PLAYBACK_SLOTS, 1, -1 do
			if type(pathState.pathSlots[slotIndex]) == "string" then
				highestConfiguredSlot = slotIndex
				break
			end
		end
		pathState.pathSectionCount = highestConfiguredSlot
	end
	pathState.focusedPathSlotIndex = normalizePathSlotIndex(settings.focusedPathSlotIndex) or 1
	pathState.sequenceStartSlotIndex = normalizePathSlotIndex(settings.sequenceStartSlotIndex) or 1
	pathState.farmPathsEnabled = settings.farmPathsEnabled == true
	pathState.staffHopEnabled = settings.staffHopEnabled == true
	pathState.playerAvoiderEnabled = settings.playerAvoiderEnabled == true
	pathState.playerAvoiderDistance = math.clamp(math.floor((tonumber(settings.playerAvoiderDistance) or pathState.playerAvoiderDistance or DEFAULTS.pathPlayerAvoiderDistance) + 0.5), 1, 200)
	if type(settings.playerAvoiderShowCorridor) == "boolean" then
		pathState.playerAvoiderShowCorridor = settings.playerAvoiderShowCorridor
	end
	pathState.playerAvoiderMethod = normalizePathPlayerAvoiderMethod(settings.playerAvoiderMethod or pathState.playerAvoiderMethod)
	pathState.playerAvoiderAction = normalizePathPlayerAvoiderAction(settings.playerAvoiderAction or pathState.playerAvoiderAction)
	pathState.playerAvoiderRadius = math.clamp(math.floor((tonumber(settings.playerAvoiderRadius) or pathState.playerAvoiderRadius or DEFAULTS.pathPlayerAvoiderRadius) + 0.5), 5, 200)
	if type(settings.playerAvoiderShowRadius) == "boolean" then
		pathState.playerAvoiderShowRadius = settings.playerAvoiderShowRadius
	end
	pathState.hopOnDeathEnabled = settings.hopOnDeathEnabled == true
	S.pathPlayerAvoiderEnabled = pathState.playerAvoiderEnabled
	S.pathPlayerAvoiderDistance = pathState.playerAvoiderDistance
	S.pathPlayerAvoiderShowCorridor = pathState.playerAvoiderShowCorridor
	S.pathPlayerAvoiderMethod = pathState.playerAvoiderMethod
	S.pathPlayerAvoiderAction = pathState.playerAvoiderAction
	S.pathPlayerAvoiderRadius = pathState.playerAvoiderRadius
	S.pathPlayerAvoiderShowRadius = pathState.playerAvoiderShowRadius
	S.pathHopOnDeathEnabled = pathState.hopOnDeathEnabled
	serverHopEnabled = settings.serverHopEnabled == true
	ensurePathSlotState()
	setFocusedPathSlotIndex(pathState.focusedPathSlotIndex, true)

	return true
end

function getGuiParent()
	if CoreGui then
		return CoreGui
	end

	return LocalPlayer:WaitForChild("PlayerGui")
end

function getVisualizerFolder()
	if visualizerFolder and visualizerFolder.Parent then
		return visualizerFolder
	end

	local folder = workspace:FindFirstChild(VISUALIZER_FOLDER_NAME)
	if folder and folder:IsA("Folder") then
		visualizerFolder = folder
		return folder
	end

	folder = Instance.new("Folder")
	folder.Name = VISUALIZER_FOLDER_NAME
	folder.Parent = workspace
	visualizerFolder = folder
	return folder
end

clearPathVisualization = function()
	local folder = getVisualizerFolder()
	folder:ClearAllChildren()
	lastPathVizTarget = nil
	lastPathVizCount = 0
end

local function findPathPlayerAvoiderVisualizerFolder()
	if playerAvoiderVisualizerFolder and playerAvoiderVisualizerFolder.Parent then
		return playerAvoiderVisualizerFolder
	end

	local folder = workspace:FindFirstChild(PLAYER_AVOIDER_VISUALIZER_FOLDER_NAME)
	if folder and folder:IsA("Folder") then
		playerAvoiderVisualizerFolder = folder
		return folder
	end

	return nil
end

function getPathPlayerAvoiderVisualizerFolder()
	local folder = findPathPlayerAvoiderVisualizerFolder()
	if folder then
		return folder
	end

	folder = Instance.new("Folder")
	folder.Name = PLAYER_AVOIDER_VISUALIZER_FOLDER_NAME
	folder.Parent = workspace
	playerAvoiderVisualizerFolder = folder
	return folder
end

function clearPathPlayerAvoiderCorridorVisualization()
	local folder = findPathPlayerAvoiderVisualizerFolder()
	if folder then
		folder:ClearAllChildren()
	end
	lastPlayerAvoiderVizAt = 0
	lastPlayerAvoiderVizSignature = nil
end

local function createPathPlayerAvoiderCorridorSegment(fromPosition, toPosition, segmentWidth, color)
	local delta = toPosition - fromPosition
	local distance = delta.Magnitude
	if distance <= 0.05 then
		return nil
	end

	local segment = Instance.new("Part")
	segment.Name = "AvoiderCorridor"
	segment.Anchored = true
	segment.CanCollide = false
	segment.CanQuery = false
	segment.CanTouch = false
	segment.Material = Enum.Material.Neon
	segment.Color = color
	segment.Transparency = 0.78
	segment.Size = Vector3.new(math.max(1.2, tonumber(segmentWidth) or 1), 0.18, distance)
	segment.CFrame = CFrame.lookAt(
		fromPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0),
		toPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	) * CFrame.new(0, 0, -distance * 0.5)
	segment.Parent = getPathPlayerAvoiderVisualizerFolder()
	return segment
end

local function createPathPlayerAvoiderRadiusSegment(fromPosition, toPosition, color, segmentIndex)
	local delta = toPosition - fromPosition
	local distance = delta.Magnitude
	if distance <= 0.05 then
		return nil
	end

	local folder = getPathPlayerAvoiderVisualizerFolder()
	local segmentName = type(segmentIndex) == "number" and ("AvoiderRadius_" .. tostring(segmentIndex)) or "AvoiderRadius"
	local segment = type(segmentIndex) == "number" and folder:FindFirstChild(segmentName) or nil
	if not (segment and segment:IsA("Part")) then
		segment = Instance.new("Part")
		segment.Name = segmentName
		segment.Anchored = true
		segment.CanCollide = false
		segment.CanQuery = false
		segment.CanTouch = false
		segment.Material = Enum.Material.Neon
		segment.Parent = folder
	end

	segment.Color = color
	segment.Transparency = 0.2
	segment.Size = Vector3.new(0.26, 0.16, distance)
	segment.CFrame = CFrame.lookAt(
		fromPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0),
		toPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	) * CFrame.new(0, 0, -distance * 0.5)
	segment.Parent = folder
	return segment
end

function trimPathPlayerAvoiderRadiusSegments(usedCount)
	local folder = findPathPlayerAvoiderVisualizerFolder()
	if not folder then
		return
	end

	for _, child in ipairs(folder:GetChildren()) do
		if child and child:IsA("Part") then
			local segmentIndex = string.match(child.Name, "^AvoiderRadius_(%d+)$")
			if segmentIndex and tonumber(segmentIndex) and tonumber(segmentIndex) > (tonumber(usedCount) or 0) then
				child:Destroy()
			end
		end
	end
end

local function samplePathPlayerAvoiderRadiusVisualizationPoint(rootPosition, originFloorPosition, radius, angle, character)
	local samplePosition = rootPosition + Vector3.new(
		math.cos(angle) * radius,
		0,
		math.sin(angle) * radius
	)
	local alignedPosition = getGroundAlignedPathPosition(samplePosition, character)
	if alignedPosition then
		return alignedPosition
	end
	if typeof(originFloorPosition) == "Vector3" then
		return Vector3.new(samplePosition.X, originFloorPosition.Y, samplePosition.Z)
	end
	return samplePosition
end

function updatePathPlayerAvoiderRadiusVisualization(rootPosition, character, blocker)
	if not (isPathPlayerAvoiderEnabled() and getPathPlayerAvoiderMethod() == "Radius Field" and isPathPlayerAvoiderShowRadiusEnabled() and pathState.playbackActive) then
		clearPathPlayerAvoiderCorridorVisualization()
		return
	end
	if typeof(rootPosition) ~= "Vector3" then
		clearPathPlayerAvoiderCorridorVisualization()
		return
	end

	local radius = getPathPlayerAvoiderRadius()
	local segmentCount = math.clamp(math.floor((radius * 0.35) + 16 + 0.5), 18, 40)
	local now = os.clock()
	local localCharacter = character or getCustomCharacter() or cachedCharacter
	local originFloorPosition = getGroundAlignedPathPosition(rootPosition, localCharacter) or rootPosition
	local signature = table.concat({
		"radius",
		math.floor(originFloorPosition.X * 2 + 0.5),
		math.floor(originFloorPosition.Y * 2 + 0.5),
		math.floor(originFloorPosition.Z * 2 + 0.5),
		radius,
		segmentCount,
		blocker and "1" or "0",
	}, ":")
	if signature == lastPlayerAvoiderVizSignature and (now - lastPlayerAvoiderVizAt) < PATH_PLAYER_AVOIDER_VIZ_REFRESH_INTERVAL then
		return
	end

	lastPlayerAvoiderVizAt = now
	lastPlayerAvoiderVizSignature = signature

	local ringColor = blocker and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(245, 245, 245)
	local folder = getPathPlayerAvoiderVisualizerFolder()
	local firstPoint = nil
	local previousPoint = nil
	local usedSegments = 0
	for index = 0, segmentCount - 1 do
		local angle = (index / segmentCount) * math.pi * 2
		local nextPoint = samplePathPlayerAvoiderRadiusVisualizationPoint(rootPosition, originFloorPosition, radius, angle, localCharacter)
		if previousPoint and nextPoint then
			usedSegments += 1
			createPathPlayerAvoiderRadiusSegment(previousPoint, nextPoint, ringColor, usedSegments)
		end
		if not firstPoint then
			firstPoint = nextPoint
		end
		previousPoint = nextPoint
	end
	if previousPoint and firstPoint then
		usedSegments += 1
		createPathPlayerAvoiderRadiusSegment(previousPoint, firstPoint, ringColor, usedSegments)
	end
	trimPathPlayerAvoiderRadiusSegments(usedSegments)

	local marker = folder:FindFirstChild("AvoiderOrigin")
	if not (marker and marker:IsA("Part")) then
		marker = Instance.new("Part")
		marker.Name = "AvoiderOrigin"
		marker.Shape = Enum.PartType.Ball
		marker.Size = Vector3.new(0.42, 0.42, 0.42)
		marker.Anchored = true
		marker.CanCollide = false
		marker.CanQuery = false
		marker.CanTouch = false
		marker.Material = Enum.Material.Neon
		marker.Parent = folder
	end
	marker.Color = ringColor
	marker.Transparency = 0.22
	marker.Position = originFloorPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	marker.Parent = folder
end

function updatePathPlayerAvoiderCorridorVisualization(rootPosition, segments, blocker)
	if not (isPathPlayerAvoiderEnabled() and getPathPlayerAvoiderMethod() == "Corridor" and isPathPlayerAvoiderShowCorridorEnabled() and pathState.playbackActive) then
		clearPathPlayerAvoiderCorridorVisualization()
		return
	end
	if typeof(rootPosition) ~= "Vector3" or type(segments) ~= "table" or #segments == 0 then
		clearPathPlayerAvoiderCorridorVisualization()
		return
	end

	local firstSegment = segments[1]
	local lastSegment = segments[#segments]
	if not firstSegment or not lastSegment then
		clearPathPlayerAvoiderCorridorVisualization()
		return
	end

	local now = os.clock()
	local signature = table.concat({
		math.floor(rootPosition.X * 2 + 0.5),
		math.floor(rootPosition.Y * 2 + 0.5),
		math.floor(rootPosition.Z * 2 + 0.5),
		#segments,
		math.floor(firstSegment.finish.X * 2 + 0.5),
		math.floor(firstSegment.finish.Z * 2 + 0.5),
		math.floor(lastSegment.finish.X * 2 + 0.5),
		math.floor(lastSegment.finish.Z * 2 + 0.5),
		blocker and "1" or "0",
	}, ":")
	if signature == lastPlayerAvoiderVizSignature and (now - lastPlayerAvoiderVizAt) < PATH_PLAYER_AVOIDER_VIZ_REFRESH_INTERVAL then
		return
	end

	clearPathPlayerAvoiderCorridorVisualization()
	lastPlayerAvoiderVizAt = now
	lastPlayerAvoiderVizSignature = signature

	local corridorColor = blocker and Color3.fromRGB(255, 116, 72) or Color3.fromRGB(255, 166, 64)
	local segmentWidth = PATH_PLAYER_AVOIDER_CORRIDOR_RADIUS * 2
	for _, segment in ipairs(segments) do
		createPathPlayerAvoiderCorridorSegment(segment.start, segment.finish, segmentWidth, corridorColor)
	end

	local marker = Instance.new("Part")
	marker.Name = "AvoiderOrigin"
	marker.Shape = Enum.PartType.Ball
	marker.Size = Vector3.new(0.55, 0.55, 0.55)
	marker.Anchored = true
	marker.CanCollide = false
	marker.CanQuery = false
	marker.CanTouch = false
	marker.Material = Enum.Material.Neon
	marker.Color = corridorColor
	marker.Transparency = 0.4
	marker.Position = rootPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	marker.Parent = getPathPlayerAvoiderVisualizerFolder()
end

function createPathMarker(position, color, size)
	local marker = Instance.new("Part")
	marker.Name = "Point"
	marker.Shape = Enum.PartType.Ball
	marker.Size = size or Vector3.new(0.72, 0.72, 0.72)
	marker.Anchored = true
	marker.CanCollide = false
	marker.CanQuery = false
	marker.CanTouch = false
	marker.Material = Enum.Material.Neon
	marker.Color = color
	marker.Transparency = 0.42
	marker.Position = position + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	marker.Parent = getVisualizerFolder()
	return marker
end

function createPathSegment(fromPosition, toPosition, color)
	local delta = toPosition - fromPosition
	local distance = delta.Magnitude
	if distance <= 0.05 then
		return nil
	end

	local segment = Instance.new("Part")
	segment.Name = "Segment"
	segment.Anchored = true
	segment.CanCollide = false
	segment.CanQuery = false
	segment.CanTouch = false
	segment.Material = Enum.Material.Neon
	segment.Color = color
	segment.Transparency = 0.58
	segment.Size = Vector3.new(0.16, 0.16, distance)
	segment.CFrame = CFrame.lookAt(
		fromPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0),
		toPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	) * CFrame.new(0, 0, -distance * 0.5)
	segment.Parent = getVisualizerFolder()
	return segment
end

function visualizePathPoints(points, targetPosition)
	if pathState.playbackActive then
		return
	end

	local now = os.clock()
	if targetPosition and lastPathVizTarget and (now - lastPathVizAt) < PATH_VIZ_REFRESH_INTERVAL then
		local delta = (targetPosition - lastPathVizTarget).Magnitude
		if delta <= PATH_VIZ_TARGET_EPSILON and math.abs(#points - lastPathVizCount) <= 1 then
			return
		end
	end

	clearPathVisualization()
	lastPathVizAt = now
	lastPathVizTarget = targetPosition
	lastPathVizCount = #points

	local previousPosition = nil
	for index, point in ipairs(points) do
		local pointColor = index == 1 and Color3.fromRGB(80, 180, 255) or Color3.fromRGB(255, 220, 90)
		createPathMarker(point, pointColor)
		if previousPosition then
			createPathSegment(previousPosition, point, Color3.fromRGB(255, 220, 90))
		end
		previousPosition = point
	end

	if targetPosition then
		createPathMarker(targetPosition, Color3.fromRGB(110, 255, 140), Vector3.new(0.92, 0.92, 0.92))
		if previousPosition then
			createPathSegment(previousPosition, targetPosition, Color3.fromRGB(110, 255, 140))
		end
	end
end

function getRecordedPathFolder()
	if pathState.runtimeFolder and pathState.runtimeFolder.Parent then
		return pathState.runtimeFolder
	end

	local folder = workspace:FindFirstChild(RECORDED_PATH_FOLDER_NAME)
	if folder and folder:IsA("Folder") then
		pathState.runtimeFolder = folder
		return folder
	end

	folder = Instance.new("Folder")
	folder.Name = RECORDED_PATH_FOLDER_NAME
	folder.Parent = workspace
	pathState.runtimeFolder = folder
	return folder
end

clearRecordedPathVisuals = function()
	for _, marker in pairs(pathUi.editorNodeParts) do
		if marker and marker.Parent then
			marker:Destroy()
		end
	end
	for _, handle in ipairs(pathUi.editorNodeHandles) do
		if handle and handle.Parent then
			handle:Destroy()
		end
	end
	for _, marker in ipairs(pathUi.editorOreMarkers) do
		if marker and marker.Parent then
			marker:Destroy()
		end
	end
	local folder = getRecordedPathFolder()
	folder:ClearAllChildren()
	pathUi.editorNodeParts = {}
	pathUi.editorNodeHandles = {}
	pathUi.editorOreMarkers = {}
end

function createRecordedPathSegment(fromPosition, toPosition, color)
	local delta = toPosition - fromPosition
	local distance = delta.Magnitude
	if distance <= 0.05 then
		return nil
	end

	local segment = Instance.new("Part")
	segment.Name = "RecordedSegment"
	segment.Anchored = true
	segment.CanCollide = false
	segment.CanQuery = false
	segment.CanTouch = false
	segment.Material = Enum.Material.Neon
	segment.Color = color
	segment.Transparency = 0.62
	segment.Size = Vector3.new(0.14, 0.14, distance)
	segment.CFrame = CFrame.lookAt(
		fromPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0),
		toPosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	) * CFrame.new(0, 0, -distance * 0.5)
	segment.Parent = getRecordedPathFolder()
	return segment
end

function clonePathData(pathData)
	local cloned = {
		nodes = {},
		ores = {},
		manualSellNodeIndex = getManualPathSellNodeIndex(pathData),
		spawnName = normalizePathSpawnName(pathData and pathData.spawnName),
	}

	for _, node in ipairs(pathData and pathData.nodes or {}) do
		local nodePosition = getPathNodePosition(node)
		if nodePosition then
			table.insert(cloned.nodes, {
				position = nodePosition,
			})
		end
	end

	for _, ore in ipairs(pathData and pathData.ores or {}) do
		table.insert(cloned.ores, {
			groupName = ore.groupName,
			position = ore.position,
			nodeIndex = ore.nodeIndex,
		})
	end

	return cloned
end

function getClosestPathNodeIndex(position, nodes)
	local bestIndex = nil
	local bestDistance = nil

	for index, node in ipairs(nodes or pathState.workingPath.nodes) do
		local nodePosition = getPathNodePosition(node)
		if nodePosition then
			local distance = (nodePosition - position).Magnitude
			if not bestDistance or distance < bestDistance then
				bestDistance = distance
				bestIndex = index
			end
		end
	end

	return bestIndex, bestDistance or math.huge
end

function getConfiguredGeneralShopTarget()
	local geometry = workspace:FindFirstChild("WORKSPACE_Geometry")
	local region = geometry and geometry:FindFirstChild("REGION_Plains")
	local bronzeCity = region and region:FindFirstChild("BronzeCity")
	local buildings = bronzeCity and bronzeCity:FindFirstChild("Buildings")
	local generalStore = buildings and buildings:FindFirstChild("GeneralStoreNew")
	local storeBuilding = generalStore and generalStore:FindFirstChild("GeneralStoreBuilding")
	local props = storeBuilding and storeBuilding:FindFirstChild("Props")
	local decor = props and props:FindFirstChild("Decor")
	local counter = decor and decor:FindFirstChild("Counter")
	local part = counter and counter:FindFirstChild("Part")
	local shopGui = part and part:FindFirstChild("GeneralShop")

	if part and part:IsA("BasePart") and shopGui and shopGui:IsA("BillboardGui") then
		return part, shopGui
	end

	return nil, nil
end

function recomputeWorkingPathSellNode()
	pathState.sellNodeIndex = nil

	local manualSellNodeIndex = getManualPathSellNodeIndex(pathState.workingPath)
	if manualSellNodeIndex then
		pathState.workingPath.manualSellNodeIndex = manualSellNodeIndex
		pathState.sellNodeIndex = manualSellNodeIndex
		pathState.sellNodeDirty = false
		return pathState.sellNodeIndex
	end

	pathState.workingPath.manualSellNodeIndex = nil
	local forceRefresh = (not cachedAutoSellTargets)
		or (#cachedAutoSellTargets == 0 and (os.clock() - lastAutoSellTargetScanAt) >= 3)
	local _, _, bestIndex, bestDistance = getBestSellTargetForPath(pathState.workingPath, nil, forceRefresh)
	if bestIndex and bestDistance and bestDistance <= PATH_RECORD_SELL_DISTANCE then
		pathState.sellNodeIndex = bestIndex
	end
	pathState.sellNodeDirty = false

	return pathState.sellNodeIndex
end

function truncateWorkingPathFromIndex(startIndex)
	if not startIndex or startIndex < 1 then
		return
	end

	for index = #pathState.workingPath.nodes, startIndex, -1 do
		table.remove(pathState.workingPath.nodes, index)
	end

	for index = #pathState.workingPath.ores, 1, -1 do
		if (pathState.workingPath.ores[index].nodeIndex or 0) >= startIndex then
			table.remove(pathState.workingPath.ores, index)
		end
	end

	if pathState.workingPath.manualSellNodeIndex and pathState.workingPath.manualSellNodeIndex >= startIndex then
		pathState.workingPath.manualSellNodeIndex = nil
	end

	pathState.sellNodeDirty = true
	recomputeWorkingPathSellNode()
end

function getOreDepositFromInstance(instance)
	local oreDepositsGetter = rawget(_G, "getOreDepositsFolder")
	local oreDeposits = oreDepositsGetter and oreDepositsGetter() or nil
	if not oreDeposits or typeof(instance) ~= "Instance" then
		return nil
	end

	local cursor = instance
	while cursor and cursor ~= workspace do
		if cursor.Parent and cursor.Parent.Parent == oreDeposits then
			return cursor
		end
		cursor = cursor.Parent
	end

	return nil
end

function resolveSavedOreTarget(oreTarget)
	if not oreTarget or not oreTarget.groupName or not oreTarget.position then
		return nil
	end

	local oreDepositsGetter = rawget(_G, "getOreDepositsFolder")
	local orePositionGetter = rawget(_G, "getOrePosition")
	local oreAvailableGetter = rawget(_G, "isOreAvailable")
	local oreDeposits = oreDepositsGetter and oreDepositsGetter() or nil
	local oreFolder = oreDeposits and oreDeposits:FindFirstChild(oreTarget.groupName)
	if not oreFolder or not orePositionGetter or not oreAvailableGetter then
		return nil
	end

	local bestOre = nil
	local bestDistance = nil
	for _, oreDeposit in ipairs(oreFolder:GetChildren()) do
		if oreAvailableGetter(oreDeposit) then
			local orePosition = orePositionGetter(oreDeposit)
			if orePosition then
				local distance = (orePosition - oreTarget.position).Magnitude
				if not bestDistance or distance < bestDistance then
					bestDistance = distance
					bestOre = oreDeposit
				end
			end
		end
	end

	if bestDistance and bestDistance <= 18 then
		return bestOre
	end

	return nil
end

function getCustomCharacter()
	local character = LocalPlayer.Character
	if character and character.Parent then
		return character
	end

	local entities = workspace:FindFirstChild("WORKSPACE_Entities")
	local playersFolder = entities and entities:FindFirstChild("Players")
	if not playersFolder then
		return nil
	end

	local exactMatch = playersFolder:FindFirstChild(LocalPlayer.Name)
	if exactMatch and exactMatch:FindFirstChildOfClass("Humanoid") then
		return exactMatch
	end

	for _, model in ipairs(playersFolder:GetChildren()) do
		if model:IsA("Model") and model:FindFirstChildOfClass("Humanoid") then
			local loweredName = safeLower(model.Name)
			if loweredName == safeLower(LocalPlayer.Name) or loweredName == tostring(LocalPlayer.UserId) then
				return model
			end
		end
	end

	for _, model in ipairs(playersFolder:GetChildren()) do
		if model:IsA("Model") and model:FindFirstChildOfClass("Humanoid") then
			return model
		end
	end

	return nil
end

function getHumanoid(character)
	return character and character:FindFirstChildOfClass("Humanoid") or nil
end

function getRootPart(character)
	if not character then
		return nil
	end

	return character:FindFirstChild("HumanoidRootPart")
		or character:FindFirstChild("UpperTorso")
		or character:FindFirstChild("Torso")
		or character:FindFirstChild("Head")
		or character:FindFirstChildWhichIsA("BasePart")
end

function getCharacterHumanoid(character)
    if not character then
        return nil
    end

    return character:FindFirstChildOfClass("Humanoid")
end

function isCharacterAlive(character)
    local humanoid = getCharacterHumanoid(character)
    return humanoid and humanoid.Health > 0
end

function getPlayerEspEntry(player)
    local entry = playerEspState.entries[player]
    if entry then
        return entry
    end

    local okDrawing = type(Drawing) == "table" and type(Drawing.new) == "function"
    if not okDrawing then
        if not playerEspState.warnedUnavailable then
            playerEspState.warnedUnavailable = true
            spawnNotification("Drawing API unavailable. Player ESP could not start.", "warning", true)
        end
        return nil
    end

    entry = {
        text = Drawing.new("Text"),
        box = Drawing.new("Square"),
        tracer = Drawing.new("Line"),
        figureLines = {},
    }

    entry.text.Center = true
    entry.text.Outline = true
    entry.text.Size = 13
    entry.text.Font = 2
    entry.text.Visible = false

    entry.box.Filled = false
    entry.box.Thickness = 1.6
    entry.box.Visible = false

    entry.tracer.Thickness = 1.4
    entry.tracer.Visible = false

    for index = 1, 6 do
        local line = Drawing.new("Line")
        line.Thickness = 1.3
        line.Visible = false
        entry.figureLines[index] = line
    end

    playerEspState.entries[player] = entry
    return entry
end

function hidePlayerEspEntry(entry)
    if not entry then
        return
    end

    if entry.text then
        entry.text.Visible = false
    end
    if entry.box then
        entry.box.Visible = false
    end
    if entry.tracer then
        entry.tracer.Visible = false
    end
    if type(entry.figureLines) == "table" then
        for _, line in ipairs(entry.figureLines) do
            if line then
                line.Visible = false
            end
        end
    end
end

function clearPlayerEspEntry(player)
    local entry = playerEspState.entries[player]
    if not entry then
        return
    end

    local function removeDrawingObject(object)
        if object and type(object.Remove) == "function" then
            pcall(function()
                object:Remove()
            end)
        end
    end

    removeDrawingObject(entry.text)
    removeDrawingObject(entry.box)
    removeDrawingObject(entry.tracer)
    if type(entry.figureLines) == "table" then
        for _, line in ipairs(entry.figureLines) do
            removeDrawingObject(line)
        end
    end

    playerEspState.entries[player] = nil
end

function clearAllPlayerEspEntries()
    for player in pairs(playerEspState.entries) do
        clearPlayerEspEntry(player)
    end
end

function getModelScreenBounds(camera, model)
    if not camera or not model or not model:IsA("Model") then
        return nil
    end

    local okBounds, cf, size = pcall(function()
        return model:GetBoundingBox()
    end)
    if not okBounds or not cf or not size then
        return nil
    end

    local half = size * 0.5
    local corners = {
        cf * Vector3.new(half.X, half.Y, half.Z),
        cf * Vector3.new(-half.X, half.Y, half.Z),
        cf * Vector3.new(half.X, -half.Y, half.Z),
        cf * Vector3.new(-half.X, -half.Y, half.Z),
        cf * Vector3.new(half.X, half.Y, -half.Z),
        cf * Vector3.new(-half.X, half.Y, -half.Z),
        cf * Vector3.new(half.X, -half.Y, -half.Z),
        cf * Vector3.new(-half.X, -half.Y, -half.Z),
    }

    local minX, minY = math.huge, math.huge
    local maxX, maxY = -math.huge, -math.huge
    local onScreen = false

    for _, corner in ipairs(corners) do
        local projected, visible = camera:WorldToViewportPoint(corner)
        if visible then
            onScreen = true
        end
        minX = math.min(minX, projected.X)
        minY = math.min(minY, projected.Y)
        maxX = math.max(maxX, projected.X)
        maxY = math.max(maxY, projected.Y)
    end

    if not onScreen or minX == math.huge or minY == math.huge then
        return nil
    end

    return minX, minY, maxX, maxY
end

function getCharacterFigureSegments(character)
    if not character then
        return {}
    end

    local torso = character:FindFirstChild("UpperTorso") or character:FindFirstChild("Torso") or character:FindFirstChild("LowerTorso")
    local lowerTorso = character:FindFirstChild("LowerTorso") or torso
    local head = character:FindFirstChild("Head")
    local leftArm = character:FindFirstChild("LeftHand")
        or character:FindFirstChild("LeftLowerArm")
        or character:FindFirstChild("LeftUpperArm")
        or character:FindFirstChild("Left Arm")
    local rightArm = character:FindFirstChild("RightHand")
        or character:FindFirstChild("RightLowerArm")
        or character:FindFirstChild("RightUpperArm")
        or character:FindFirstChild("Right Arm")
    local leftLeg = character:FindFirstChild("LeftFoot")
        or character:FindFirstChild("LeftLowerLeg")
        or character:FindFirstChild("LeftUpperLeg")
        or character:FindFirstChild("Left Leg")
    local rightLeg = character:FindFirstChild("RightFoot")
        or character:FindFirstChild("RightLowerLeg")
        or character:FindFirstChild("RightUpperLeg")
        or character:FindFirstChild("Right Leg")

    local segments = {}
    local function addSegment(partA, partB)
        if partA and partB then
            table.insert(segments, {partA.Position, partB.Position})
        end
    end

    addSegment(head, torso)
    addSegment(torso, lowerTorso)
    addSegment(torso, leftArm)
    addSegment(torso, rightArm)
    addSegment(lowerTorso, leftLeg)
    addSegment(lowerTorso, rightLeg)

    return segments
end

function updatePlayerEsp()
    if not S.playerEspEnabled or destroyed or not isSessionActive() then
        for _, entry in pairs(playerEspState.entries) do
            hidePlayerEspEntry(entry)
        end
        return
    end

    local camera = workspace.CurrentCamera
    if not camera then
        return
    end

    local localCharacter = getWorldCharacterForPlayer(localPlayer) or localPlayer.Character
    local localRoot = getRootPart(localCharacter)
    local tracerStart = Vector2.new(camera.ViewportSize.X * 0.5, camera.ViewportSize.Y - 24)
    local showText = isPlayerEspMethodEnabled("Text")
    local showBox = isPlayerEspMethodEnabled("Box")
    local showTracer = isPlayerEspMethodEnabled("Tracer")
    local showFigure = isPlayerEspMethodEnabled("Figure")
    if not (showText or showBox or showTracer or showFigure) then
        for _, entry in pairs(playerEspState.entries) do
            hidePlayerEspEntry(entry)
        end
        return
    end
    if localRoot then
        local localProjected, localVisible = camera:WorldToViewportPoint(localRoot.Position)
        if localVisible then
            tracerStart = Vector2.new(localProjected.X, localProjected.Y)
        end
    end

    for _, player in ipairs(_Players:GetPlayers()) do
        if player ~= localPlayer then
            local entry = getPlayerEspEntry(player)
            if entry then
                hidePlayerEspEntry(entry)

                local character = getWorldCharacterForPlayer(player)
                local rootPart = getRootPart(character)
                if character and rootPart and isCharacterAlive(character) then
                    local rootScreen, rootVisible = camera:WorldToViewportPoint(rootPart.Position)
                    local head = character:FindFirstChild("Head") or rootPart
                    local headScreen, headVisible = camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.85, 0))
                    local distance = localRoot and math.floor((rootPart.Position - localRoot.Position).Magnitude + 0.5) or 0
                    local espColor = getPlayerEspRoleColor(player)

                    if showText and headVisible then
                        entry.text.Text = string.format("%s [%d studs]", player.Name, distance)
                        entry.text.Position = Vector2.new(headScreen.X, headScreen.Y)
                        entry.text.Color = espColor
                        entry.text.Visible = true
                    end

                    if showBox then
                        local minX, minY, maxX, maxY = getModelScreenBounds(camera, character)
                        if minX and maxX and maxX > minX and maxY > minY then
                            entry.box.Position = Vector2.new(minX, minY)
                            entry.box.Size = Vector2.new(maxX - minX, maxY - minY)
                            entry.box.Color = espColor
                            entry.box.Visible = true
                        end
                    end

                    if showTracer and rootVisible then
                        entry.tracer.From = tracerStart
                        entry.tracer.To = Vector2.new(rootScreen.X, rootScreen.Y)
                        entry.tracer.Color = espColor
                        entry.tracer.Visible = true
                    end

                    if showFigure then
                        local segments = getCharacterFigureSegments(character)
                        for index, line in ipairs(entry.figureLines) do
                            local segment = segments[index]
                            if line and segment then
                                local pointA, visibleA = camera:WorldToViewportPoint(segment[1])
                                local pointB, visibleB = camera:WorldToViewportPoint(segment[2])
                                if visibleA and visibleB then
                                    line.From = Vector2.new(pointA.X, pointA.Y)
                                    line.To = Vector2.new(pointB.X, pointB.Y)
                                    line.Color = espColor
                                    line.Visible = true
                                else
                                    line.Visible = false
                                end
                            elseif line then
                                line.Visible = false
                            end
                        end
                    end
                end
            end
        end
    end
end

function ensurePlayerEspRenderLoop()
    if playerEspState.renderConnection then
        return
    end

    local renderAccumulator = 0
    playerEspState.renderConnection = _RunService.RenderStepped:Connect(function(dt)
        if destroyed or not isSessionActive() then
            if playerEspState.renderConnection then
                playerEspState.renderConnection:Disconnect()
                playerEspState.renderConnection = nil
            end
            clearAllPlayerEspEntries()
            return
        end
        renderAccumulator = renderAccumulator + (tonumber(dt) or 0.016)
        if renderAccumulator < (1 / 30) then
            return
        end
        renderAccumulator = 0
        updatePlayerEsp()
    end)
end

function hasGunFeatureHooksEnabled()
    return playerState.equipWhileSwimmingEnabled
        or S.silentAimEnabled
        or S.fastProjectileEnabled
        or S.noSpreadEnabled
        or S.wallBangEnabled
        or S.noRecoilEnabled
        or S.noFireCooldownEnabled
        or S.fastReloadEnabled
        or S.instantLassoChargeEnabled
        or S.lassoSilentAimEnabled
        or S.lassoNoRopeLimitEnabled
        or S.fasterLassoEnabled
end

function hasGunAimVisualsEnabled()
    return (S.silentAimEnabled and (S.gunFovEnabled or S.gunSnaplineEnabled))
        or (S.lassoSilentAimEnabled and S.lassoSnaplineEnabled)
end

function getGunAimCursorPosition()
    local camera = workspace.CurrentCamera
    if not camera then
        return Vector2.new(0, 0)
    end

    local okMouse, mouseLocation = pcall(function()
        return _UserInputService:GetMouseLocation()
    end)
    if okMouse and typeof(mouseLocation) == "Vector2" then
        return mouseLocation
    end

    return Vector2.new(camera.ViewportSize.X * 0.5, camera.ViewportSize.Y * 0.5)
end

function getSilentAimTargetPart(character)
    if not character then
        return nil
    end

    local preferTorso = tostring(S.silentAimBodyTarget or "Head") == "Torso"
    if preferTorso then
        return character:FindFirstChild("UpperTorso")
            or character:FindFirstChild("Torso")
            or character:FindFirstChild("HumanoidRootPart")
            or getRootPart(character)
    end

    return character:FindFirstChild("Head")
        or character:FindFirstChild("UpperTorso")
        or character:FindFirstChild("Torso")
        or getRootPart(character)
end

function getSilentAimPriorityOrder()
    local enabledOrder = {}
    for _, priorityName in ipairs(GUN_TARGET_PRIORITY_ORDER) do
        if isGunTargetPriorityEnabled(priorityName) then
            table.insert(enabledOrder, priorityName)
        end
    end
    if #enabledOrder == 0 then
        table.insert(enabledOrder, "Players")
    end
    return enabledOrder
end

function isSilentAimModelAlive(model)
    if not model or not model:IsA("Model") then
        return false
    end

    local humanoid = getCharacterHumanoid(model)
    if humanoid then
        return humanoid.Health > 0
    end

    return getSilentAimTargetPart(model) ~= nil
end

function buildSilentAimTargetInfo(kind, model, player)
    if not isSilentAimModelAlive(model) then
        return nil
    end

    local targetPart = getSilentAimTargetPart(model)
    if not targetPart then
        return nil
    end

    return {
        kind = kind,
        player = player,
        character = model,
        part = targetPart,
        position = targetPart.Position,
    }
end

function getSilentAimTargetsByPriority()
    local groupedTargets = {
        Players = {},
        NPCs = {},
        Entities = {},
    }

    for _, player in ipairs(_Players:GetPlayers()) do
        if player ~= localPlayer then
            local _, character = getAliveTwwTargetData(player)
            local candidate = buildSilentAimTargetInfo("Players", character, player)
            if candidate then
                table.insert(groupedTargets.Players, candidate)
            end
        end
    end

    local entitiesFolder = workspace:FindFirstChild("WORKSPACE_Entities")
    local npcsFolder = entitiesFolder and entitiesFolder:FindFirstChild("NPCs")
    if npcsFolder then
        for _, model in ipairs(npcsFolder:GetChildren()) do
            local candidate = buildSilentAimTargetInfo("NPCs", model, nil)
            if candidate then
                table.insert(groupedTargets.NPCs, candidate)
            end
        end
    end

    local animalsFolder = entitiesFolder and entitiesFolder:FindFirstChild("Animals")
    if animalsFolder then
        for _, model in ipairs(animalsFolder:GetChildren()) do
            local candidate = buildSilentAimTargetInfo("Entities", model, nil)
            if candidate then
                table.insert(groupedTargets.Entities, candidate)
            end
        end
    end

    return groupedTargets
end

function isSilentAimLineOfSightClear(targetPart, targetCharacter)
    if not S.silentAimVisibilityCheck then
        return true
    end
    if S.wallBangEnabled then
        return true
    end

    local camera = workspace.CurrentCamera
    if not camera or not targetPart then
        return false
    end

    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude

    local ignoreList = {}
    local localCharacter = getWorldCharacterForPlayer(localPlayer) or localPlayer.Character
    if localCharacter then
        table.insert(ignoreList, localCharacter)
    end
    local ignoreFolder = workspace:FindFirstChild("Ignore")
    if ignoreFolder then
        table.insert(ignoreList, ignoreFolder)
    end
    params.FilterDescendantsInstances = ignoreList

    local direction = targetPart.Position - camera.CFrame.Position
    local result = workspace:Raycast(camera.CFrame.Position, direction, params)
    if not result then
        return true
    end

    return targetCharacter and result.Instance and result.Instance:IsDescendantOf(targetCharacter)
end

function getSilentAimTargetInfo()
    local camera = workspace.CurrentCamera
    if not camera then
        return nil
    end

    local cursorPosition = getGunAimCursorPosition()
    local maxDistance = math.clamp(tonumber(S.silentAimDistanceLimit) or 1000, 10, 20000)
    local fovRadius = math.clamp(tonumber(S.gunFovRadius) or 200, 10, 2000)
    local useFovMode = tostring(S.silentAimMode or "FOV") == "FOV"
    local groupedTargets = getSilentAimTargetsByPriority()

    for _, priorityName in ipairs(getSilentAimPriorityOrder()) do
        local bestTarget = nil
        local bestScore = nil
        for _, candidate in ipairs(groupedTargets[priorityName] or {}) do
            local targetPart = candidate.part
            local targetCharacter = candidate.character
            local worldDistance = (targetPart.Position - camera.CFrame.Position).Magnitude
            if worldDistance <= maxDistance then
                local screenPoint, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                if onScreen and isSilentAimLineOfSightClear(targetPart, targetCharacter) then
                    local screenVector = Vector2.new(screenPoint.X, screenPoint.Y)
                    local screenDistance = (screenVector - cursorPosition).Magnitude
                    if (not useFovMode) or screenDistance <= fovRadius then
                        local candidateScore = useFovMode and screenDistance or worldDistance
                        if not bestTarget or not bestScore or candidateScore < bestScore then
                            candidate.screenPosition = screenVector
                            candidate.screenDistance = screenDistance
                            candidate.worldDistance = worldDistance
                            candidate.position = targetPart.Position
                            bestTarget = candidate
                            bestScore = candidateScore
                        end
                    end
                end
            end
        end
        if bestTarget then
            return bestTarget
        end
    end

    return nil
end

function getCurrentGunSharedData(forceRefresh)
    local gunItem = getCurrentEquippedGunItem(forceRefresh)
    if not isTable(gunItem) then
        return nil, nil
    end

    local sharedData = rawget(gunItem, "SharedData") or gunItem.SharedData
    if isTable(sharedData) then
        return gunItem, sharedData
    end

    return gunItem, nil
end

function getCurrentProjectileType(projectileEntry)
    local projectileType = nil

    if type(debug) == "table" and type(debug.getstack) == "function" then
        local okStack, stackValues = pcall(function()
            return debug.getstack(3)
        end)
        if okStack and type(stackValues) == "table" and type(stackValues[2]) == "string" then
            if string.find(stackValues[2], "Projectile", 1, true) ~= nil then
                projectileType = stackValues[2]
            end
        end
    end

    if projectileType then
        return projectileType
    end

    local entry = isTable(projectileEntry) and projectileEntry or nil
    local arrowType = safeLower(entry and entry.arrowType)
    local ammoType = safeLower(entry and entry.ammoType)
    if string.find(arrowType, "arrow", 1, true) ~= nil or string.find(ammoType, "arrow", 1, true) ~= nil then
        return "ArrowProjectile"
    end
    if string.find(ammoType, "tomahawk", 1, true) ~= nil then
        return "TomahawkProjectile"
    end

    return "Projectile"
end

function solveProjectileTravel(origin, speed, targetPosition, gravityMagnitude)
    if typeof(origin) ~= "Vector3" or typeof(targetPosition) ~= "Vector3" then
        return nil, nil
    end

    local projectileSpeed = tonumber(speed)
    if not projectileSpeed or projectileSpeed <= 0 then
        return nil, nil
    end

    local displacement = targetPosition - origin
    local horizontalDisplacement = Vector3.new(displacement.X, 0, displacement.Z)
    local horizontalDistance = horizontalDisplacement.Magnitude
    if horizontalDistance <= 0.001 then
        local distance = displacement.Magnitude
        if distance <= 0.001 then
            return Vector3.new(0, 1, 0), 0
        end
        local directTime = distance / projectileSpeed
        return displacement.Unit * projectileSpeed, directTime
    end

    local travelTime = horizontalDistance / projectileSpeed
    if travelTime <= 0 then
        return nil, nil
    end

    local verticalVelocity = (displacement.Y + (0.5 * gravityMagnitude * travelTime * travelTime)) / travelTime
    local flatVelocity = horizontalDisplacement.Unit * projectileSpeed
    return Vector3.new(flatVelocity.X, verticalVelocity, flatVelocity.Z), travelTime
end

function buildProjectileBasis(direction)
    if typeof(direction) ~= "Vector3" or direction.Magnitude <= 0.001 then
        return Vector3.new(0, 0, -1), Vector3.new(1, 0, 0), Vector3.new(0, 1, 0)
    end

    local forward = direction.Unit
    local referenceUp = math.abs(forward.Y) > 0.99 and Vector3.new(1, 0, 0) or Vector3.new(0, 1, 0)
    local right = forward:Cross(referenceUp)
    if right.Magnitude <= 0.001 then
        right = forward:Cross(Vector3.new(0, 0, 1))
    end
    right = right.Unit
    local up = right:Cross(forward)
    if up.Magnitude <= 0.001 then
        up = Vector3.new(0, 1, 0)
    else
        up = up.Unit
    end

    return forward, right, up
end

function randomConeVector(randomObject, direction, maxAngle)
    local coneAngle = math.max(tonumber(maxAngle) or 0, 0)
    local forward, right, up = buildProjectileBasis(direction)
    if coneAngle <= 0 then
        return forward
    end

    local rng = randomObject
    if typeof(rng) ~= "Random" then
        rng = Random.new()
    end

    local theta = rng:NextNumber(0, coneAngle)
    local phi = rng:NextNumber(0, math.pi * 2)
    local sinTheta = math.sin(theta)
    local candidate = (forward * math.cos(theta))
        + (right * math.cos(phi) * sinTheta)
        + (up * math.sin(phi) * sinTheta)

    if candidate.Magnitude <= 0.001 then
        return forward
    end

    return candidate.Unit
end

function getSpreadVectorCompensation(seed, direction, maxAngle)
    local forward = buildProjectileBasis(direction)
    local rngSeed = tonumber(seed) or 0
    local randomized = randomConeVector(Random.new(rngSeed), forward, maxAngle)
    local compensated = forward - (randomized - forward)
    if compensated.Magnitude <= 0.001 then
        return forward
    end
    return compensated.Unit
end

function getProjectileAccuracyState(sharedData, projectileEntry)
    local data = isTable(sharedData) and sharedData or {}
    local entry = isTable(projectileEntry) and projectileEntry or {}

    local projectileAccuracy = tonumber(rawget(data, "ProjectileAccuracy") or data.ProjectileAccuracy) or 1
    if tostring(entry.ammoType or "") == "ShotgunRound" then
        projectileAccuracy = projectileAccuracy * (tonumber(rawget(data, "ShotgunRoundAccuracy") or data.ShotgunRoundAccuracy) or 1)
    end

    local projectileCount = tonumber(rawget(data, "NumProjectiles") or data.NumProjectiles) or 1
    local maxSpreadAngle = projectileCount > 1 and math.rad(10) or math.rad(12.5)
    local spreadAngle = maxSpreadAngle * math.clamp(1 - projectileAccuracy, 0, 1)

    return projectileAccuracy, projectileCount, spreadAngle
end

function applySilentAimToProjectileEntry(projectileEntry, targetInfo, projectileType, sharedData)
    if not isTable(projectileEntry) or not targetInfo then
        return false
    end

    local origin = getProjectileEntryOrigin(projectileEntry)
    if typeof(origin) ~= "Vector3" then
        return false
    end

    local data = isTable(sharedData) and sharedData or {}
    local projectileName = tostring(projectileType or "Projectile")
    local projectilePower = tonumber(rawget(data, "ProjectilePower") or data.ProjectilePower)
        or tonumber(projectileEntry.projectileSpeed)
        or tonumber(projectileEntry.speed)
        or tonumber(projectileEntry.power)
        or 1
    if projectileName == "TomahawkProjectile" then
        projectilePower = 200
    end

    local gravity = rawget(data, "Gravity") or data.Gravity
    if typeof(gravity) ~= "Vector3" then
        gravity = Vector3.new(0, -32, 0)
    end
    if projectileName == "TomahawkProjectile" then
        gravity = Vector3.new(0, -150, 0)
    end

    local gravityMultiplier = tonumber(rawget(data, "GravityMultiplier") or data.GravityMultiplier)
    if gravityMultiplier then
        gravity = gravity * gravityMultiplier
    end

    if type(projectileEntry.power) == "number" and type(projectileEntry.drawStart) == "number" then
        local drawPower = math.clamp(projectileEntry.power, 0, 1)
        if tostring(projectileEntry.arrowType or "") == "ExplosiveArrow" then
            local maxPower = tonumber(rawget(data, "MaxPower") or data.MaxPower)
            if maxPower then
                projectilePower = math.max(maxPower, drawPower)
            end
        else
            local minPower = tonumber(rawget(data, "MinPower") or data.MinPower)
            if minPower then
                projectilePower = math.max(minPower, drawPower)
            else
                projectilePower = math.max(projectilePower, drawPower)
            end
        end
    end

    local solvedVelocity, travelTime = solveProjectileTravel(origin, projectilePower, targetInfo.position, math.abs(gravity.Y))
    if typeof(solvedVelocity) ~= "Vector3" or solvedVelocity.Magnitude <= 0.001 then
        return false
    end

    local aimDirection = solvedVelocity.Unit
    local _, _, spreadAngle = getProjectileAccuracyState(data, projectileEntry)
    local changed = false

    if projectileName == "ArrowProjectile" then
        local directVector = targetInfo.position - origin
        if directVector.Magnitude > 0.001 then
            projectileEntry.direction = directVector.Unit
            changed = true
        end
        if type(projectileEntry.power) == "number" or projectileEntry.power == nil then
            projectileEntry.power = 1
            changed = true
        end
    else
        local nextDirection = aimDirection
        if projectileName ~= "TomahawkProjectile" then
            nextDirection = getSpreadVectorCompensation(projectileEntry.seed, aimDirection, spreadAngle)
        end
        projectileEntry.direction = nextDirection
        changed = true
        if projectileName ~= "TomahawkProjectile" and (type(projectileEntry.accuracy) == "number" or projectileEntry.accuracy == nil) then
            projectileEntry.accuracy = 1
            changed = true
        end
    end

    local appliedDirection = typeof(projectileEntry.direction) == "Vector3" and projectileEntry.direction or aimDirection
    if typeof(projectileEntry.velocity) == "Vector3" then
        local velocityMagnitude = projectileEntry.velocity.Magnitude
        if velocityMagnitude <= 0.001 then
            velocityMagnitude = math.max(projectilePower, 1)
        end
        projectileEntry.velocity = appliedDirection * velocityMagnitude
        changed = true
    end
    if type(projectileEntry.speed) == "number" then
        projectileEntry.speed = math.max(projectileEntry.speed, projectilePower)
        changed = true
    end
    if type(projectileEntry.projectileSpeed) == "number" then
        projectileEntry.projectileSpeed = math.max(projectileEntry.projectileSpeed, projectilePower)
        changed = true
    end
    if type(projectileEntry.power) == "number" and type(projectileEntry.drawStart) ~= "number" then
        projectileEntry.power = 1
        changed = true
    end

    gunFeatureState.lastProjectileTravelTime = travelTime
    return changed
end

function applyNoSpreadToProjectileEntry(projectileEntry, projectileType, sharedData)
    if not isTable(projectileEntry) then
        return false
    end

    local projectileName = tostring(projectileType or "Projectile")
    if projectileName == "TomahawkProjectile" or projectileName == "ArrowProjectile" then
        return false
    end

    local direction = typeof(projectileEntry.direction) == "Vector3" and projectileEntry.direction or nil
    if (not direction or direction.Magnitude <= 0.001) and typeof(projectileEntry.velocity) == "Vector3" and projectileEntry.velocity.Magnitude > 0.001 then
        direction = projectileEntry.velocity.Unit
    end
    if not direction or direction.Magnitude <= 0.001 then
        return false
    end

    local _, _, spreadAngle = getProjectileAccuracyState(sharedData, projectileEntry)
    projectileEntry.direction = getSpreadVectorCompensation(projectileEntry.seed, direction.Unit, spreadAngle)
    projectileEntry.accuracy = 1
    if type(projectileEntry.spread) == "number" then
        projectileEntry.spread = 0
    end
    return true
end

function applyFastProjectileToEntry(projectileEntry, targetInfo)
    if not isTable(projectileEntry) then
        return false
    end

    local direction = typeof(projectileEntry.direction) == "Vector3" and projectileEntry.direction or nil
    if (not direction or direction.Magnitude <= 0.001) and typeof(projectileEntry.velocity) == "Vector3" and projectileEntry.velocity.Magnitude > 0.001 then
        direction = projectileEntry.velocity.Unit
    end
    if (not direction or direction.Magnitude <= 0.001) and targetInfo and typeof(targetInfo.position) == "Vector3" then
        local origin = getProjectileEntryOrigin(projectileEntry)
        if typeof(origin) == "Vector3" then
            local toTarget = targetInfo.position - origin
            if toTarget.Magnitude > 0.001 then
                direction = toTarget.Unit
            end
        end
    end

    if typeof(direction) == "Vector3" and direction.Magnitude > 0.001 then
        if typeof(projectileEntry.direction) ~= "Vector3" or projectileEntry.direction.Magnitude <= 0.001 then
            projectileEntry.direction = direction.Unit
            return true
        end
    end

    return false
end

function getFastProjectileBulletIdList(bulletIds)
    local normalized = {}
    if type(bulletIds) == "table" then
        for _, bulletId in ipairs(bulletIds) do
            if bulletId ~= nil then
                table.insert(normalized, bulletId)
            end
        end
        if #normalized == 0 then
            for _, bulletId in pairs(bulletIds) do
                if bulletId ~= nil then
                    table.insert(normalized, bulletId)
                end
            end
        end
    elseif bulletIds ~= nil then
        table.insert(normalized, bulletIds)
    end
    return normalized
end

function isTrackedFastProjectileBullet(bulletId)
    return type(gunFeatureState.trackedFastProjectileBullets) == "table"
        and bulletId ~= nil
        and gunFeatureState.trackedFastProjectileBullets[bulletId] == true
end

function untrackFastProjectileBullet(bulletId)
    if type(gunFeatureState.trackedFastProjectileBullets) == "table" and bulletId ~= nil then
        gunFeatureState.trackedFastProjectileBullets[bulletId] = nil
    end
end

function trackFastProjectileBullets(bulletIds)
    if type(gunFeatureState.trackedFastProjectileBullets) ~= "table" then
        gunFeatureState.trackedFastProjectileBullets = {}
    end

    for _, bulletId in ipairs(getFastProjectileBulletIdList(bulletIds)) do
        gunFeatureState.trackedFastProjectileBullets[bulletId] = true
        task.delay(5, function()
            untrackFastProjectileBullet(bulletId)
        end)
    end
end

function resolveFastProjectileHitPosition(origin, targetPosition, targetPart)
    if typeof(targetPosition) ~= "Vector3" then
        return targetPart and targetPart.Position or origin
    end
    if typeof(origin) ~= "Vector3" or not targetPart then
        return targetPosition
    end

    local okRay, hitPosition = pcall(function()
        local hitPart, position = workspace:FindPartOnRayWithWhitelist(
            Ray.new(origin, targetPosition - origin),
            { targetPart }
        )
        if hitPart and typeof(position) == "Vector3" then
            return position
        end
        return targetPosition
    end)
    if okRay and typeof(hitPosition) == "Vector3" then
        return hitPosition
    end

    return targetPosition
end

function resolveFastProjectileTargetReference(network, targetInfo, playerTarget)
    local targetPart = targetInfo and targetInfo.part
    if not targetPart then
        return nil
    end

    local targetReference = nil
    if network and type(network.GetReference) == "function" then
        pcall(function()
            targetReference = network:GetReference(targetPart, "CharacterPart")
        end)
    end
    if targetReference ~= nil then
        return targetReference
    end

    if playerTarget then
        return nil
    end

    return targetPart
end

function getFastProjectileTargetFlags(targetInfo)
    local targetPart = targetInfo and targetInfo.part
    local targetCharacter = targetInfo and targetInfo.character
    local playerTarget = false
    local animalTarget = false
    local npcTarget = false

    if targetCharacter then
        playerTarget = _Players:GetPlayerFromCharacter(targetCharacter) ~= nil
    end

    if targetPart and targetPart.Parent then
        local entities = workspace:FindFirstChild("WORKSPACE_Entities")
        local animals = entities and entities:FindFirstChild("Animals")
        local npcs = entities and entities:FindFirstChild("NPCs")
        if animals then
            animalTarget = targetPart:IsDescendantOf(animals)
        end
        if npcs then
            npcTarget = targetPart:IsDescendantOf(npcs)
        end
    end

    return playerTarget, animalTarget, npcTarget
end

function estimateFastProjectileTravelTime(projectileEntry, targetInfo)
    local travelTime = tonumber(gunFeatureState.lastProjectileTravelTime)
    if travelTime and travelTime >= 0 then
        return math.clamp(travelTime, 0.01, 2)
    end

    local origin = getProjectileEntryOrigin(projectileEntry)
    local targetPosition = targetInfo and targetInfo.position
    if typeof(origin) ~= "Vector3" or typeof(targetPosition) ~= "Vector3" then
        return 0.03
    end

    local speed = tonumber(projectileEntry and projectileEntry.projectileSpeed)
        or tonumber(projectileEntry and projectileEntry.speed)
        or (typeof(projectileEntry and projectileEntry.velocity) == "Vector3" and projectileEntry.velocity.Magnitude)
        or tonumber(projectileEntry and projectileEntry.power)
        or 10000

    if speed <= 0 then
        speed = 10000
    end

    return math.clamp((targetPosition - origin).Magnitude / speed, 0.01, 2)
end

function scheduleFastProjectileFinalEvent(targetInfo, projectileEntry, bulletIds)
    local bulletIdList = getFastProjectileBulletIdList(bulletIds)
    local targetPart = targetInfo and targetInfo.part
    if #bulletIdList == 0 or not targetPart or not targetPart:IsA("BasePart") then
        return
    end

    trackFastProjectileBullets(bulletIdList)

    local origin = getProjectileEntryOrigin(projectileEntry)
    local targetPosition = targetInfo.position or targetPart.Position
    local targetCFrame = targetPart.CFrame
    local hitPosition = resolveFastProjectileHitPosition(origin, targetPosition, targetPart)
    local direction = typeof(projectileEntry and projectileEntry.direction) == "Vector3" and projectileEntry.direction or nil
    if (not direction or direction.Magnitude <= 0.001) and typeof(projectileEntry and projectileEntry.velocity) == "Vector3" then
        direction = projectileEntry.velocity
    end
    if (not direction or direction.Magnitude <= 0.001) and typeof(origin) == "Vector3" and typeof(targetPosition) == "Vector3" then
        direction = targetPosition - origin
    end
    if typeof(direction) ~= "Vector3" or direction.Magnitude <= 0.001 then
        direction = Vector3.FromNormalId(Enum.NormalId.Front)
    else
        direction = direction.Unit
    end
    local speed = tonumber(projectileEntry and projectileEntry.projectileSpeed)
        or tonumber(projectileEntry and projectileEntry.speed)
        or tonumber(projectileEntry and projectileEntry.power)
        or 1
    if speed <= 0 then
        speed = 1
    end
    local velocity = typeof(projectileEntry and projectileEntry.velocity) == "Vector3" and projectileEntry.velocity or nil
    if typeof(velocity) ~= "Vector3" or velocity.Magnitude <= 0.001 then
        velocity = direction * speed
    end

    local playerTarget, animalTarget, npcTarget = getFastProjectileTargetFlags(targetInfo)
    local travelTime = estimateFastProjectileTravelTime(projectileEntry, targetInfo)
    local npcResolvedTarget = nil
    if npcTarget then
        npcResolvedTarget = { targetPart.Parent and targetPart.Parent.Parent or targetPart.Parent, targetPart.Name }
    end
    local animalResolvedTarget = animalTarget and targetPart or nil
    local targetMaterialName = targetPart.Material.Name

    task.spawn(function()
        task.wait(travelTime)

        local network = playerState.hookedNetwork or resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
        local fireServer = type(playerState.originalNetworkFireServer) == "function" and playerState.originalNetworkFireServer
            or (network and network.FireServer)
        if not network or type(fireServer) ~= "function" then
            for _, bulletId in ipairs(bulletIdList) do
                untrackFastProjectileBullet(bulletId)
            end
            return
        end

        local targetReference = resolveFastProjectileTargetReference(network, targetInfo, playerTarget)
        if targetReference == nil and playerTarget then
            for _ = 1, 6 do
                task.wait(0.05)
                targetReference = resolveFastProjectileTargetReference(network, targetInfo, true)
                if targetReference ~= nil then
                    break
                end
            end
        end
        if targetReference == nil and playerTarget then
            for _, bulletId in ipairs(bulletIdList) do
                untrackFastProjectileBullet(bulletId)
            end
            return
        end

        local objectHit = targetCFrame:PointToObjectSpace(hitPosition)
        local frontNormal = targetCFrame:VectorToObjectSpace(Vector3.FromNormalId(Enum.NormalId.Front))
        local directionObject = targetCFrame:VectorToObjectSpace(velocity.Unit)
        local syncedTime = getSyncedNetworkTime()
        local resolvedTarget = targetReference
        if npcTarget then
            resolvedTarget = npcResolvedTarget
        elseif animalTarget then
            resolvedTarget = animalResolvedTarget
        end

        for _, bulletId in ipairs(bulletIdList) do
            pcall(fireServer, network, "ProjectileEvent", bulletId, syncedTime, "Final", resolvedTarget, objectHit, frontNormal, directionObject, hitPosition, Vector3.FromNormalId(Enum.NormalId.Front), targetMaterialName)
            task.defer(function()
                pcall(fireServer, network, "RemoveProjectile", bulletId)
            end)
            untrackFastProjectileBullet(bulletId)
        end
    end)
end

function shouldSuppressTrackedFastProjectileRemote(remoteName, packedArgs)
    if remoteName ~= "ProjectileEvent" and remoteName ~= "RemoveProjectile" then
        return false
    end

    if type(packedArgs) ~= "table" then
        return false
    end

    if isTrackedFastProjectileBullet(packedArgs[1]) then
        return true
    end

    return remoteName == "ProjectileEvent"
        and packedArgs[3] == "Final"
        and packedArgs[4] == nil
end

function collectProjectileEntries(payload, depth, seen, results)
    if not isTable(payload) or (depth or 0) > 4 then
        return results or {}
    end

    results = results or {}
    seen = seen or {}
    if seen[payload] then
        return results
    end
    seen[payload] = true

    if looksLikeProjectileEntry(payload) then
        table.insert(results, payload)
    end

    for _, value in pairs(payload) do
        if isTable(value) then
            collectProjectileEntries(value, (depth or 0) + 1, seen, results)
        end
    end

    return results
end

function resolveCameraModule(forceRefresh)
    local modulesTable = resolveGameModules(forceRefresh)
    if isTable(modulesTable) then
        local camModule = rawget(modulesTable, "cam") or modulesTable.cam
        if isTable(camModule) and (type(camModule.AddRecoil) == "function" or type(camModule.Vibrate) == "function") then
            return camModule
        end
    end

    local globalModule = rawget(_G, "__AutoMineGlobalModule")
    if not isTable(globalModule) then
        resolveGlobalNetwork(forceRefresh)
        globalModule = rawget(_G, "__AutoMineGlobalModule")
    end
    if isTable(globalModule) and type(globalModule.LoadModule) == "function" then
        local okLoad, loadedModule = pcall(function()
            return globalModule:LoadModule("Camera")
        end)
        if okLoad and isTable(loadedModule) and (type(loadedModule.AddRecoil) == "function" or type(loadedModule.Vibrate) == "function") then
            return loadedModule
        end
    end

    return nil
end

function resolveGunItemModule(forceRefresh)
    local modulesTable = resolveGameModules(forceRefresh)
    if isTable(modulesTable) then
        local gunItem = rawget(modulesTable, "gunitem") or modulesTable.gunitem
        if isTable(gunItem) and type(gunItem.CanShoot) == "function" then
            return gunItem
        end
    end

    local globalModule = rawget(_G, "__AutoMineGlobalModule")
    if not isTable(globalModule) then
        resolveGlobalNetwork(forceRefresh)
        globalModule = rawget(_G, "__AutoMineGlobalModule")
    end
    if isTable(globalModule) and type(globalModule.LoadModule) == "function" then
        local okLoad, loadedModule = pcall(function()
            return globalModule:LoadModule("GunItem")
        end)
        if okLoad and isTable(loadedModule) and type(loadedModule.CanShoot) == "function" then
            return loadedModule
        end
    end

    return nil
end

function resolveProjectileHandlerModule(forceRefresh)
    local modulesTable = resolveGameModules(forceRefresh)
    if isTable(modulesTable) then
        local projectileHandler = rawget(modulesTable, "projHandler") or modulesTable.projHandler
        if isTable(projectileHandler) and type(projectileHandler.Raycast) == "function" then
            return projectileHandler
        end
    end

    local globalModule = rawget(_G, "__AutoMineGlobalModule")
    if not isTable(globalModule) then
        resolveGlobalNetwork(forceRefresh)
        globalModule = rawget(_G, "__AutoMineGlobalModule")
    end
    if isTable(globalModule) and type(globalModule.LoadModule) == "function" then
        local okLoad, loadedHandler = pcall(function()
            return globalModule:LoadModule("ProjectileHandler")
        end)
        if okLoad and isTable(loadedHandler) and type(loadedHandler.Raycast) == "function" then
            return loadedHandler
        end
    end

    local sharedModules = ReplicatedStorage:FindFirstChild("SharedModules")
    local worldFolder = sharedModules and sharedModules:FindFirstChild("World")
    local handlerModule = worldFolder and worldFolder:FindFirstChild("ProjectileHandler")
    if handlerModule and handlerModule:IsA("ModuleScript") then
        local okRequire, requiredHandler = pcall(require, handlerModule)
        if okRequire and isTable(requiredHandler) and type(requiredHandler.Raycast) == "function" then
            return requiredHandler
        end
    end

    return nil
end

function isGunItemTool(toolInfo)
    if not isTable(toolInfo) then
        return false
    end

    if rawget(toolInfo, "IsGunItem") == true or toolInfo.IsGunItem == true then
        return true
    end

    local sharedData = rawget(toolInfo, "SharedData") or toolInfo.SharedData
    return isTable(sharedData) and (
        type(sharedData.ProjectilePower) == "number"
        or type(sharedData.ProjectileAccuracy) == "number"
        or type(sharedData.MaxPower) == "number"
        or sharedData.AmmoType ~= nil
    )
end

function isLassoSharedData(sharedData)
    if not isTable(sharedData) then
        return false
    end

    return type(rawget(sharedData, "LassoChargeTime") or sharedData.LassoChargeTime) == "number"
        or type(rawget(sharedData, "LassoStayTime") or sharedData.LassoStayTime) == "number"
end

function hasLassoTextHint(source)
    if not isTable(source) then
        return false
    end

    for _, key in ipairs({
        "Type",
        "Name",
        "DisplayName",
        "ItemType",
        "Model",
        "ModelName",
        "EquipmentType",
    }) do
        if string.find(safeLower(rawget(source, key) or source[key]), "lasso", 1, true) ~= nil then
            return true
        end
    end

    return false
end

function isLassoItemTool(toolInfo)
    if not isTable(toolInfo) then
        return false
    end

    local isOfType = rawget(toolInfo, "IsOfType") or toolInfo.IsOfType
    if type(isOfType) == "function" then
        local okMatch, matches = pcall(function()
            return toolInfo:IsOfType("Lasso")
        end)
        if okMatch and matches == true then
            return true
        end
    end

    local sharedData = rawget(toolInfo, "SharedData") or toolInfo.SharedData
    if isLassoSharedData(sharedData) then
        return true
    end

    local itemData = getResolvedItemData(toolInfo)
    if isTable(itemData) then
        local itemSharedData = rawget(itemData, "SharedData") or itemData.SharedData
        if isLassoSharedData(itemSharedData) or hasLassoTextHint(itemData) then
            return true
        end
    end

    return hasLassoTextHint(toolInfo)
end

function getCurrentEquippedGunItem(forceRefresh)
    local playerCharacter = resolvePlayerCharacterModule(forceRefresh)
    if not hasGetEquippedItem(playerCharacter) then
        return nil
    end

    local okEquipped, equippedItem = pcall(function()
        return playerCharacter:GetEquippedItem()
    end)
    if okEquipped and isGunItemTool(equippedItem) then
        return equippedItem
    end

    return nil
end

function getCurrentEquippedLassoItem(forceRefresh)
    local playerCharacter = resolvePlayerCharacterModule(forceRefresh)
    if not hasGetEquippedItem(playerCharacter) then
        return nil, nil
    end

    local okEquipped, equippedItem = pcall(function()
        return playerCharacter:GetEquippedItem()
    end)
    if okEquipped and isLassoItemTool(equippedItem) then
        local sharedData = rawget(equippedItem, "SharedData") or equippedItem.SharedData
        if isTable(sharedData) then
            return equippedItem, sharedData
        end
        return equippedItem, nil
    end

    return nil, nil
end

function collectLassoSharedDataCandidates(forceRefresh)
    local candidates = {}
    local seen = {}

    local function tryAdd(sharedData)
        if isTable(sharedData) and not seen[sharedData] then
            seen[sharedData] = true
            table.insert(candidates, sharedData)
        end
    end

    local _, equippedSharedData = getCurrentEquippedLassoItem(forceRefresh)
    tryAdd(equippedSharedData)

    local playerItems = resolveSharedPlayerItems(forceRefresh)
    if isTable(playerItems) then
        for itemType, entry in pairs(playerItems) do
            if isTable(entry) then
                if isLassoSharedData(entry) then
                    tryAdd(entry)
                elseif string.find(safeLower(itemType), "lasso", 1, true) ~= nil then
                    tryAdd(entry)
                else
                    local entrySharedData = rawget(entry, "SharedData") or entry.SharedData
                    if isLassoSharedData(entrySharedData) or isLassoItemTool(entry) then
                        tryAdd(entrySharedData)
                    end
                end
            end
        end
    end

    return candidates
end

function getLassoAimTargetPart(character)
    if not character then
        return nil
    end

    return character:FindFirstChild("UpperTorso")
        or character:FindFirstChild("Torso")
        or character:FindFirstChild("HumanoidRootPart")
        or character:FindFirstChild("Head")
        or getRootPart(character)
end

function buildLassoSilentAimTargetInfo(kind, model, player)
    if not isSilentAimModelAlive(model) then
        return nil
    end

    local targetPart = getLassoAimTargetPart(model)
    if not targetPart then
        return nil
    end

    return {
        kind = kind,
        player = player,
        character = model,
        part = targetPart,
        position = targetPart.Position,
    }
end

function getLassoSilentAimTargetInfo()
    local camera = workspace.CurrentCamera
    if not camera then
        return nil
    end

    local cursorPosition = getGunAimCursorPosition()
    local maxDistance = math.min(
        math.clamp(tonumber(S.silentAimDistanceLimit) or 1000, 10, 20000),
        LASSO_SILENT_AIM_MAX_DISTANCE
    )

    local function findBestTarget(candidates)
        local bestTarget = nil
        local bestScore = nil

        for _, candidate in ipairs(candidates) do
            local targetPart = candidate.part
            local targetCharacter = candidate.character
            local worldDistance = (targetPart.Position - camera.CFrame.Position).Magnitude
            if worldDistance <= maxDistance then
                local screenPoint, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                if onScreen and isSilentAimLineOfSightClear(targetPart, targetCharacter) then
                    local screenVector = Vector2.new(screenPoint.X, screenPoint.Y)
                    local screenDistance = (screenVector - cursorPosition).Magnitude
                    if not bestTarget or not bestScore or screenDistance < bestScore then
                        candidate.screenPosition = screenVector
                        candidate.screenDistance = screenDistance
                        candidate.worldDistance = worldDistance
                        candidate.position = targetPart.Position
                        bestTarget = candidate
                        bestScore = screenDistance
                    end
                end
            end
        end

        return bestTarget
    end

    local playerTargets = {}
    for _, player in ipairs(_Players:GetPlayers()) do
        if player ~= localPlayer then
            local _, character = getAliveTwwTargetData(player)
            local candidate = buildLassoSilentAimTargetInfo("Players", character, player)
            if candidate then
                table.insert(playerTargets, candidate)
            end
        end
    end
    local bestPlayerTarget = findBestTarget(playerTargets)
    if bestPlayerTarget then
        return bestPlayerTarget
    end

    local npcTargets = {}
    local entitiesFolder = workspace:FindFirstChild("WORKSPACE_Entities")
    local npcsFolder = entitiesFolder and entitiesFolder:FindFirstChild("NPCs")
    if npcsFolder then
        for _, model in ipairs(npcsFolder:GetChildren()) do
            local candidate = buildLassoSilentAimTargetInfo("NPCs", model, nil)
            if candidate then
                table.insert(npcTargets, candidate)
            end
        end
    end

    return findBestTarget(npcTargets)
end

function solveLassoSilentAimVelocity(origin, speed, targetInfo)
    if typeof(origin) ~= "Vector3" or type(speed) ~= "number" or speed <= 0.001 or not targetInfo then
        return nil
    end

    local targetPosition = targetInfo.position
    if typeof(targetPosition) ~= "Vector3" then
        return nil
    end

    local distance = (targetPosition - origin).Magnitude
    local travelTime = distance > 0.001 and (distance / speed) or 0
    local targetPart = targetInfo.part
    if targetPart and travelTime and travelTime > 0 then
        local assemblyVelocity = targetPart.AssemblyLinearVelocity
        if typeof(assemblyVelocity) == "Vector3" and assemblyVelocity.Magnitude > 0.05 then
            targetPosition = targetPosition + (assemblyVelocity * math.min(travelTime, 1.25))
        end
    end

    local directVector = targetPosition - origin
    if directVector.Magnitude > 0.001 then
        return directVector.Unit * speed
    end

    return nil
end

function shouldDelayFasterLassoHit(hitPart)
    if not (S.fasterLassoEnabled and typeof(hitPart) == "Instance") then
        return false
    end

    local entitiesFolder = workspace:FindFirstChild("WORKSPACE_Entities")
    local playersFolder = entitiesFolder and entitiesFolder:FindFirstChild("Players")
    if playersFolder and hitPart:IsDescendantOf(playersFolder) then
        return true
    end

    local npcsFolder = entitiesFolder and entitiesFolder:FindFirstChild("NPCs")
    return npcsFolder and hitPart:IsDescendantOf(npcsFolder) or false
end

function getProjectileEntryOrigin(entry)
    if typeof(entry.origin) == "Vector3" then
        return entry.origin
    end
    if typeof(entry.position) == "Vector3" then
        return entry.position
    end

    local camera = workspace.CurrentCamera
    return camera and camera.CFrame.Position or nil
end

function looksLikeProjectileEntry(entry)
    if not isTable(entry) then
        return false
    end

    return typeof(entry.origin) == "Vector3"
        or typeof(entry.direction) == "Vector3"
        or typeof(entry.velocity) == "Vector3"
        or type(entry.accuracy) == "number"
        or type(entry.ammoType) == "string"
        or type(entry.arrowType) == "string"
        or type(entry.power) == "number"
end

function processGunProjectileRemote(remoteName, packedArgs)
    if remoteName ~= "InitProjectiles" then
        return packedArgs
    end
    if not (S.silentAimEnabled or S.fastProjectileEnabled or S.noSpreadEnabled) then
        return packedArgs
    end

    local gunItem, sharedData = getCurrentGunSharedData(false)
    if not sharedData then
        gunItem, sharedData = getCurrentGunSharedData(true)
    end

    local targetInfo = nil
    gunFeatureState.lastProjectileTravelTime = nil
    if S.silentAimEnabled then
        local hitRoll = math.random(1, 100)
        local hitChance = math.clamp(tonumber(S.silentAimAccuracy) or 50, 1, 100)
        if hitRoll <= hitChance then
            targetInfo = getSilentAimTargetInfo()
        end
        if not targetInfo and not (S.fastProjectileEnabled or S.noSpreadEnabled) then
            return packedArgs
        end
    end

    local appliedSilentAim = false
    local trackedFastProjectileEntry = nil
    for index = 1, packedArgs.n do
        if isTable(packedArgs[index]) then
            local projectileEntries = collectProjectileEntries(packedArgs[index], 0, {}, {})
            for _, projectileEntry in ipairs(projectileEntries) do
                local projectileType = getCurrentProjectileType(projectileEntry)
                local silentAimChanged = false

                if targetInfo then
                    silentAimChanged = applySilentAimToProjectileEntry(projectileEntry, targetInfo, projectileType, sharedData)
                    appliedSilentAim = appliedSilentAim or silentAimChanged
                end

                if S.fastProjectileEnabled then
                    applyFastProjectileToEntry(projectileEntry, targetInfo)
                    if not trackedFastProjectileEntry and targetInfo and silentAimChanged then
                        trackedFastProjectileEntry = projectileEntry
                    end
                end

                if S.noSpreadEnabled and not silentAimChanged then
                    applyNoSpreadToProjectileEntry(projectileEntry, projectileType, sharedData)
                end
            end
        end
    end

    if S.fastProjectileEnabled and targetInfo and appliedSilentAim and trackedFastProjectileEntry and packedArgs[3] ~= nil then
        scheduleFastProjectileFinalEvent(targetInfo, trackedFastProjectileEntry, packedArgs[3])
    end

    if targetInfo and appliedSilentAim then
        gunFeatureState.currentTargetInfo = targetInfo
    elseif not hasGunAimVisualsEnabled() then
        gunFeatureState.currentTargetInfo = nil
    end

    return packedArgs
end

function hideGunAimDrawings()
    local drawings = {
        gunFeatureState.fovCircle,
        gunFeatureState.fovSquare,
        gunFeatureState.snapline,
        gunFeatureState.lassoSnapline,
    }
    for _, drawingObject in ipairs(drawings) do
        if drawingObject then
            drawingObject.Visible = false
        end
    end
    for _, line in ipairs(gunFeatureState.fovPolygonLines or {}) do
        if line then
            line.Visible = false
        end
    end
end

function clearGunAimDrawings()
    hideGunAimDrawings()

    if gunFeatureState.renderConnection then
        gunFeatureState.renderConnection:Disconnect()
        gunFeatureState.renderConnection = nil
    end

    local drawings = {
        "fovCircle",
        "fovSquare",
        "snapline",
        "lassoSnapline",
    }
    for _, key in ipairs(drawings) do
        local drawingObject = gunFeatureState[key]
        if drawingObject then
            pcall(function()
                drawingObject:Remove()
            end)
        end
        gunFeatureState[key] = nil
    end

    if type(gunFeatureState.fovPolygonLines) == "table" then
        for _, line in ipairs(gunFeatureState.fovPolygonLines) do
            if line then
                pcall(function()
                    line:Remove()
                end)
            end
        end
    end
    gunFeatureState.fovPolygonLines = {}
    gunFeatureState.currentTargetInfo = nil
    gunFeatureState.currentLassoTargetInfo = nil
end

function ensureGunAimDrawings()
    if type(Drawing) ~= "table" or type(Drawing.new) ~= "function" then
        if not gunFeatureState.warnedAimUnavailable then
            gunFeatureState.warnedAimUnavailable = true
            spawnNotification("Drawing API unavailable. Aim visuals could not start.", "warning", true)
        end
        return false
    end

    if not gunFeatureState.fovCircle then
        gunFeatureState.fovCircle = Drawing.new("Circle")
        gunFeatureState.fovCircle.Filled = false
        gunFeatureState.fovCircle.Thickness = 1.6
        gunFeatureState.fovCircle.Color = Color3.new(1, 1, 1)
        gunFeatureState.fovCircle.Visible = false
    end

    if not gunFeatureState.fovSquare then
        gunFeatureState.fovSquare = Drawing.new("Square")
        gunFeatureState.fovSquare.Filled = false
        gunFeatureState.fovSquare.Thickness = 1.6
        gunFeatureState.fovSquare.Color = Color3.new(1, 1, 1)
        gunFeatureState.fovSquare.Visible = false
    end

    if not gunFeatureState.snapline then
        gunFeatureState.snapline = Drawing.new("Line")
        gunFeatureState.snapline.Thickness = 1.5
        gunFeatureState.snapline.Color = Color3.new(1, 1, 1)
        gunFeatureState.snapline.Visible = false
    end

    if not gunFeatureState.lassoSnapline then
        gunFeatureState.lassoSnapline = Drawing.new("Line")
        gunFeatureState.lassoSnapline.Thickness = 1.5
        gunFeatureState.lassoSnapline.Color = Color3.new(1, 1, 1)
        gunFeatureState.lassoSnapline.Visible = false
    end

    if type(gunFeatureState.fovPolygonLines) ~= "table" then
        gunFeatureState.fovPolygonLines = {}
    end
    while #gunFeatureState.fovPolygonLines < 6 do
        local polygonLine = Drawing.new("Line")
        polygonLine.Thickness = 1.6
        polygonLine.Color = Color3.new(1, 1, 1)
        polygonLine.Visible = false
        table.insert(gunFeatureState.fovPolygonLines, polygonLine)
    end

    return true
end

function getGunFovPolygonSides(shapeName)
    local shape = tostring(shapeName or "Circle")
    if shape == "Triangle" then
        return 3
    end
    if shape == "Hexagon" then
        return 6
    end
    return 0
end

function updateGunAimVisuals()
    if not hasGunAimVisualsEnabled() then
        hideGunAimDrawings()
        gunFeatureState.currentTargetInfo = nil
        gunFeatureState.currentLassoTargetInfo = nil
        return
    end
    if not ensureGunAimDrawings() then
        return
    end

    local cursor = getGunAimCursorPosition()
    local radius = math.clamp(tonumber(S.gunFovRadius) or 200, 10, 2000)
    local shapeName = tostring(S.gunFovShape or "Circle")
    local targetInfo = S.silentAimEnabled and getSilentAimTargetInfo() or nil
    local lassoTargetInfo = (S.lassoSilentAimEnabled and S.lassoSnaplineEnabled) and getLassoSilentAimTargetInfo() or nil
    gunFeatureState.currentTargetInfo = targetInfo
    gunFeatureState.currentLassoTargetInfo = lassoTargetInfo

    hideGunAimDrawings()

    if S.gunFovEnabled then
        if shapeName == "Square" and gunFeatureState.fovSquare then
            gunFeatureState.fovSquare.Position = Vector2.new(cursor.X - radius, cursor.Y - radius)
            gunFeatureState.fovSquare.Size = Vector2.new(radius * 2, radius * 2)
            gunFeatureState.fovSquare.Color = Color3.new(1, 1, 1)
            gunFeatureState.fovSquare.Visible = true
        else
            local polygonSides = getGunFovPolygonSides(shapeName)
            if polygonSides > 0 then
                local startAngle = -math.pi * 0.5
                local points = {}
                for index = 1, polygonSides do
                    local angle = startAngle + ((index - 1) * (math.pi * 2 / polygonSides))
                    points[index] = Vector2.new(
                        cursor.X + math.cos(angle) * radius,
                        cursor.Y + math.sin(angle) * radius
                    )
                end
                for index, line in ipairs(gunFeatureState.fovPolygonLines) do
                    if line and index <= polygonSides then
                        local nextIndex = (index % polygonSides) + 1
                        line.From = points[index]
                        line.To = points[nextIndex]
                        line.Color = Color3.new(1, 1, 1)
                        line.Visible = true
                    elseif line then
                        line.Visible = false
                    end
                end
            elseif gunFeatureState.fovCircle then
                gunFeatureState.fovCircle.Position = cursor
                gunFeatureState.fovCircle.Radius = radius
                gunFeatureState.fovCircle.Color = Color3.new(1, 1, 1)
                gunFeatureState.fovCircle.NumSides = 64
                gunFeatureState.fovCircle.Visible = true
            end
        end
    end

    if S.gunSnaplineEnabled and targetInfo and gunFeatureState.snapline then
        gunFeatureState.snapline.From = cursor
        gunFeatureState.snapline.To = targetInfo.screenPosition
        gunFeatureState.snapline.Color = Color3.new(1, 1, 1)
        gunFeatureState.snapline.Visible = true
    end

    if S.lassoSilentAimEnabled and S.lassoSnaplineEnabled and lassoTargetInfo and gunFeatureState.lassoSnapline then
        gunFeatureState.lassoSnapline.From = cursor
        gunFeatureState.lassoSnapline.To = lassoTargetInfo.screenPosition
        gunFeatureState.lassoSnapline.Color = Color3.new(1, 1, 1)
        gunFeatureState.lassoSnapline.Visible = true
    end
end

function ensureGunAimRenderLoop()
    if not hasGunAimVisualsEnabled() then
        clearGunAimDrawings()
        return
    end
    if gunFeatureState.renderConnection then
        return
    end

    gunFeatureState.renderConnection = _RunService.RenderStepped:Connect(function()
        if destroyed or not isSessionActive() or not hasGunAimVisualsEnabled() then
            clearGunAimDrawings()
            return
        end
        updateGunAimVisuals()
    end)
end

function restoreEquipWhileSwimmingHooks()
    local owner = playerState.hookedSwimOwner
    if isTable(owner) then
        if playerState.originalSwimCanSwitchToItem and owner.CanSwitchToItem == playerState.swimCanSwitchWrapper then
            owner.CanSwitchToItem = playerState.originalSwimCanSwitchToItem
        end
        if playerState.originalSwimUpdate and owner.Update == playerState.swimUpdateWrapper then
            owner.Update = playerState.originalSwimUpdate
        end
    end

    playerState.hookedSwimOwner = nil
    playerState.originalSwimCanSwitchToItem = nil
    playerState.swimCanSwitchWrapper = nil
    playerState.originalSwimUpdate = nil
    playerState.swimUpdateWrapper = nil
end

function ensureEquipWhileSwimmingHooks()
    if not playerState.equipWhileSwimmingEnabled then
        return
    end

    local owner = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
    if not isTable(owner) then
        return
    end
    if owner ~= playerState.hookedSwimOwner then
        restoreEquipWhileSwimmingHooks()
        playerState.hookedSwimOwner = owner
    end

    local canSwitch = rawget(owner, "CanSwitchToItem") or owner.CanSwitchToItem
    if type(canSwitch) == "function" and canSwitch ~= playerState.swimCanSwitchWrapper then
        playerState.originalSwimCanSwitchToItem = canSwitch
        playerState.swimCanSwitchWrapper = function(self, ...)
            if playerState.equipWhileSwimmingEnabled and type(self) == "table" then
                self.Swimming = false
            end
            return playerState.originalSwimCanSwitchToItem(self, ...)
        end
        owner.CanSwitchToItem = playerState.swimCanSwitchWrapper
    end

    local updateFn = rawget(owner, "Update") or owner.Update
    if type(updateFn) == "function" and updateFn ~= playerState.swimUpdateWrapper then
        playerState.originalSwimUpdate = updateFn
        playerState.swimUpdateWrapper = function(self, ...)
            if playerState.equipWhileSwimmingEnabled and type(self) == "table" then
                self.Swimming = false
            end
            return playerState.originalSwimUpdate(self, ...)
        end
        owner.Update = playerState.swimUpdateWrapper
    end
end

function setNoRecoilGuardState(isEnabled)
    _G.__AutoMineNoRecoilEnabled = isEnabled == true
end

function getNoRecoilHookRegistry()
    local registry = rawget(_G, "__AutoMineNoRecoilHookRegistry")
    if type(registry) ~= "table" then
        registry = {}
        _G.__AutoMineNoRecoilHookRegistry = registry
    end
    return registry
end

function shouldBypassNoRecoilGuard()
    if type(debug) ~= "table" or type(debug.getconstants) ~= "function" then
        return false
    end

    local okConstants, constants = pcall(function()
        return debug.getconstants(2)
    end)
    return okConstants and type(constants) == "table" and table.find(constants, "StackSize") ~= nil
end

function makeNoRecoilGuardedWrapper(originalFn)
    return function(...)
        if shouldBypassNoRecoilGuard() then
            return originalFn(...)
        end
        if rawget(_G, "__AutoMineNoRecoilEnabled") == true then
            return nil
        end
        return originalFn(...)
    end
end

function restoreNoRecoilHooks(forceRestore)
    local camModule = gunFeatureState.hookedCamModule
    if forceRestore and isTable(camModule) then
        local registry = getNoRecoilHookRegistry()
        local entry = registry[camModule]
        if type(entry) == "table" then
            if type(entry.originalAddRecoil) == "function" and camModule.AddRecoil == entry.wrapperAddRecoil then
                camModule.AddRecoil = entry.originalAddRecoil
            end
            if type(entry.originalVibrate) == "function" and camModule.Vibrate == entry.wrapperVibrate then
                camModule.Vibrate = entry.originalVibrate
            end
            registry[camModule] = nil
        else
            if gunFeatureState.originalCamAddRecoil and camModule.AddRecoil == gunFeatureState.camAddRecoilWrapper then
                camModule.AddRecoil = gunFeatureState.originalCamAddRecoil
            end
            if gunFeatureState.originalCamVibrate and camModule.Vibrate == gunFeatureState.camVibrateWrapper then
                camModule.Vibrate = gunFeatureState.originalCamVibrate
            end
        end
    end

    gunFeatureState.hookedCamModule = nil
    gunFeatureState.originalCamAddRecoil = nil
    gunFeatureState.camAddRecoilWrapper = nil
    gunFeatureState.originalCamVibrate = nil
    gunFeatureState.camVibrateWrapper = nil
end

function resetStaleNoRecoilHookRegistry()
    local registry = rawget(_G, "__AutoMineNoRecoilHookRegistry")
    if type(registry) ~= "table" then
        return
    end

    for camModule, entry in pairs(registry) do
        if isTable(camModule) and type(entry) == "table" then
            if type(entry.originalAddRecoil) == "function" and camModule.AddRecoil == entry.wrapperAddRecoil then
                camModule.AddRecoil = entry.originalAddRecoil
            end
            if type(entry.originalVibrate) == "function" and camModule.Vibrate == entry.wrapperVibrate then
                camModule.Vibrate = entry.originalVibrate
            end
        end
    end

    _G.__AutoMineNoRecoilHookRegistry = {}
end

resetStaleNoRecoilHookRegistry()

function ensureNoRecoilHooks()
    if not S.noRecoilEnabled then
        return
    end

    local camModule = resolveCameraModule(false) or resolveCameraModule(true)
    if not isTable(camModule) then
        return
    end
    if camModule ~= gunFeatureState.hookedCamModule then
        restoreNoRecoilHooks()
        gunFeatureState.hookedCamModule = camModule
    end

    local registry = getNoRecoilHookRegistry()
    local entry = registry[camModule]
    if type(entry) ~= "table" then
        entry = {}
        registry[camModule] = entry
    end

    local addRecoil = rawget(camModule, "AddRecoil") or camModule.AddRecoil
    if type(addRecoil) == "function" then
        if addRecoil ~= entry.wrapperAddRecoil then
            entry.originalAddRecoil = addRecoil
            entry.wrapperAddRecoil = makeNoRecoilGuardedWrapper(addRecoil)
            camModule.AddRecoil = entry.wrapperAddRecoil
        end
        gunFeatureState.originalCamAddRecoil = entry.originalAddRecoil
        gunFeatureState.camAddRecoilWrapper = entry.wrapperAddRecoil
    end

    local vibrate = rawget(camModule, "Vibrate") or camModule.Vibrate
    if type(vibrate) == "function" then
        if vibrate ~= entry.wrapperVibrate then
            entry.originalVibrate = vibrate
            entry.wrapperVibrate = makeNoRecoilGuardedWrapper(vibrate)
            camModule.Vibrate = entry.wrapperVibrate
        end
        gunFeatureState.originalCamVibrate = entry.originalVibrate
        gunFeatureState.camVibrateWrapper = entry.wrapperVibrate
    end
end

function restoreNoFireCooldownHook()
    local gunModule = gunFeatureState.hookedGunItemModule
    if isTable(gunModule) and gunFeatureState.originalGunCanShoot and gunModule.CanShoot == gunFeatureState.gunCanShootWrapper then
        gunModule.CanShoot = gunFeatureState.originalGunCanShoot
    end

    gunFeatureState.hookedGunItemModule = nil
    gunFeatureState.originalGunCanShoot = nil
    gunFeatureState.gunCanShootWrapper = nil
end

function ensureNoFireCooldownHook()
    if not S.noFireCooldownEnabled then
        return
    end

    local gunModule = resolveGunItemModule(false) or resolveGunItemModule(true)
    if not isTable(gunModule) then
        return
    end
    if gunModule ~= gunFeatureState.hookedGunItemModule then
        restoreNoFireCooldownHook()
        gunFeatureState.hookedGunItemModule = gunModule
    end

    local canShoot = rawget(gunModule, "CanShoot") or gunModule.CanShoot
    if type(canShoot) == "function" and canShoot ~= gunFeatureState.gunCanShootWrapper then
        gunFeatureState.originalGunCanShoot = canShoot
        gunFeatureState.gunCanShootWrapper = function(...)
            if S.noFireCooldownEnabled and not S.fastReloadEnabled then
                return true
            end
            return gunFeatureState.originalGunCanShoot(...)
        end
        gunModule.CanShoot = gunFeatureState.gunCanShootWrapper
    end
end

function appendUniqueIgnore(ignoreList, instance)
    if not ignoreList or not instance or table.find(ignoreList, instance) then
        return
    end
    table.insert(ignoreList, instance)
end

function restoreWallBangHook()
    local projectileHandler = gunFeatureState.hookedProjectileHandler
    if isTable(projectileHandler)
        and gunFeatureState.originalProjectileRaycast
        and projectileHandler.Raycast == gunFeatureState.projectileRaycastWrapper then
        projectileHandler.Raycast = gunFeatureState.originalProjectileRaycast
    end

    gunFeatureState.hookedProjectileHandler = nil
    gunFeatureState.originalProjectileRaycast = nil
    gunFeatureState.projectileRaycastWrapper = nil
end

function ensureWallBangHook()
    if not S.wallBangEnabled then
        return
    end

    local projectileHandler = resolveProjectileHandlerModule(false) or resolveProjectileHandlerModule(true)
    if not isTable(projectileHandler) then
        return
    end
    if projectileHandler ~= gunFeatureState.hookedProjectileHandler then
        restoreWallBangHook()
        gunFeatureState.hookedProjectileHandler = projectileHandler
    end

    local raycastFn = rawget(projectileHandler, "Raycast") or projectileHandler.Raycast
    if type(raycastFn) == "function" and raycastFn ~= gunFeatureState.projectileRaycastWrapper then
        gunFeatureState.originalProjectileRaycast = raycastFn
        gunFeatureState.projectileRaycastWrapper = function(...)
            local packedArgs = table.pack(...)
            if S.wallBangEnabled and type(packedArgs[1]) == "table" and packedArgs[1].IsOwner and type(packedArgs[4]) == "table" then
                appendUniqueIgnore(packedArgs[4], workspace.Terrain)
                appendUniqueIgnore(packedArgs[4], workspace:FindFirstChild("WORKSPACE_Geometry"))
                appendUniqueIgnore(packedArgs[4], workspace:FindFirstChild("WORKSPACE_Interactables"))
                appendUniqueIgnore(packedArgs[4], workspace:FindFirstChild("Ignore"))
            end
            return gunFeatureState.originalProjectileRaycast(table.unpack(packedArgs, 1, packedArgs.n))
        end
        projectileHandler.Raycast = gunFeatureState.projectileRaycastWrapper
    end
end

function isGunReloadTrack(trackName)
    if GUN_RELOAD_TRACK_NAMES[tostring(trackName or "")] then
        return true
    end

    local lowered = safeLower(trackName)
    return lowered:find("reload", 1, true) ~= nil or lowered:find("load", 1, true) ~= nil
end

function restoreFastReloadHook()
    local hooks = type(gunFeatureState.fastReloadHooks) == "table" and gunFeatureState.fastReloadHooks or {}
    for gunItem, hook in pairs(hooks) do
        if isTable(gunItem)
            and type(hook) == "table"
            and type(hook.originalPlayTrack) == "function"
            and gunItem.PlayTrack == hook.wrapper then
            gunItem.PlayTrack = hook.originalPlayTrack
        end
    end

    gunFeatureState.fastReloadItem = nil
    gunFeatureState.fastReloadOriginalPlayTrack = nil
    gunFeatureState.fastReloadWrapper = nil
    gunFeatureState.fastReloadHooks = {}
end

function restoreInstantLassoChargeHooks()
    local overrides = type(gunFeatureState.lassoChargeOverrides) == "table" and gunFeatureState.lassoChargeOverrides or {}
    for sharedData, originalState in pairs(overrides) do
        if isTable(sharedData) and type(originalState) == "table" then
            sharedData.LassoChargeTime = originalState.chargeTime
        end
    end

    gunFeatureState.lassoChargeOverrides = {}
end

function ensureInstantLassoChargeHooks()
    if not S.instantLassoChargeEnabled then
        return
    end

    if type(gunFeatureState.lassoChargeOverrides) ~= "table" then
        gunFeatureState.lassoChargeOverrides = {}
    end

    local candidates = collectLassoSharedDataCandidates(false)
    if #candidates == 0 then
        candidates = collectLassoSharedDataCandidates(true)
    end

    for _, sharedData in ipairs(candidates) do
        local originalState = gunFeatureState.lassoChargeOverrides[sharedData]
        if type(originalState) ~= "table" then
            originalState = {
                chargeTime = rawget(sharedData, "LassoChargeTime"),
            }
            if originalState.chargeTime == nil then
                originalState.chargeTime = sharedData.LassoChargeTime
            end
            gunFeatureState.lassoChargeOverrides[sharedData] = originalState
        end
        sharedData.LassoChargeTime = 0.001
    end
end

function restoreLassoSilentAimHooks()
    local hooks = type(gunFeatureState.lassoSilentAimHooks) == "table" and gunFeatureState.lassoSilentAimHooks or {}
    for lassoItem, hook in pairs(hooks) do
        if isTable(lassoItem)
            and type(hook) == "table"
            and type(hook.originalNetworkActivate) == "function"
            and lassoItem.NetworkActivate == hook.wrapper then
            lassoItem.NetworkActivate = hook.originalNetworkActivate
        end
        if isTable(lassoItem)
            and type(hook) == "table"
            and type(hook.originalOnHit) == "function"
            and lassoItem.OnHit == hook.onHitWrapper then
            lassoItem.OnHit = hook.originalOnHit
        end
    end

    gunFeatureState.lassoSilentAimHooks = {}
end

function ensureLassoSilentAimHook()
    if not (S.lassoSilentAimEnabled or S.fasterLassoEnabled) then
        return
    end

    local lassoItem = getCurrentEquippedLassoItem(false) or getCurrentEquippedLassoItem(true)
    if not isTable(lassoItem) then
        return
    end

    if type(gunFeatureState.lassoSilentAimHooks) ~= "table" then
        gunFeatureState.lassoSilentAimHooks = {}
    end

    local networkActivate = rawget(lassoItem, "NetworkActivate") or lassoItem.NetworkActivate
    if type(networkActivate) ~= "function" then
        return
    end

    local hook = gunFeatureState.lassoSilentAimHooks[lassoItem]
    if type(hook) ~= "table" then
        hook = {}
        gunFeatureState.lassoSilentAimHooks[lassoItem] = hook
    end

    if networkActivate ~= hook.wrapper then
        hook.originalNetworkActivate = networkActivate
        hook.wrapper = function(self, ...)
            local packedArgs = table.pack(...)
            local localVelocityOverride = nil
            local localPlatformVelocity = Vector3.zero
            local localLassoCircle = nil
            if tostring(packedArgs[1] or "") == "ThrowLasso" then
                local throwVelocity = typeof(packedArgs[3]) == "Vector3" and packedArgs[3] or nil
                local platformVelocity = typeof(packedArgs[4]) == "Vector3" and packedArgs[4] or Vector3.zero
                local lassoCircle = rawget(self, "LassoCircle") or self.LassoCircle
                local origin = lassoCircle and lassoCircle.Position or nil
                local throwSpeed = throwVelocity and throwVelocity.Magnitude or nil
                if typeof(origin) == "Vector3" and throwSpeed and throwSpeed > 0.001 then
                    local networkVelocity = throwVelocity
                    local visualVelocity = throwVelocity

                    if S.lassoSilentAimEnabled then
                        local targetInfo = getLassoSilentAimTargetInfo()
                        if targetInfo then
                            local aimedVelocity = solveLassoSilentAimVelocity(origin, throwSpeed, targetInfo)
                            if typeof(aimedVelocity) == "Vector3" and aimedVelocity.Magnitude > 0.001 then
                                networkVelocity = aimedVelocity
                                visualVelocity = aimedVelocity
                            end
                        end
                    end

                    if S.fasterLassoEnabled and typeof(visualVelocity) == "Vector3" and visualVelocity.Magnitude > 0.001 then
                        visualVelocity = visualVelocity.Unit * (throwSpeed * LASSO_FAST_THROW_MULTIPLIER)
                    end

                    if typeof(networkVelocity) == "Vector3" and networkVelocity.Magnitude > 0.001 then
                        packedArgs[3] = networkVelocity
                    end

                    if typeof(visualVelocity) == "Vector3" and visualVelocity.Magnitude > 0.001 then
                        localVelocityOverride = visualVelocity
                        localPlatformVelocity = platformVelocity
                        localLassoCircle = lassoCircle
                    end
                end
            end

            local results = table.pack(hook.originalNetworkActivate(self, table.unpack(packedArgs, 1, packedArgs.n)))

            if localLassoCircle and localLassoCircle:IsA("BasePart") and typeof(localVelocityOverride) == "Vector3" then
                localLassoCircle.Velocity = localVelocityOverride + localPlatformVelocity
                local bodyGyro = localLassoCircle:FindFirstChild("BodyGyro")
                if bodyGyro then
                    bodyGyro.CFrame = CFrame.new(localLassoCircle.Position, localLassoCircle.Position + localVelocityOverride)
                        * CFrame.Angles(0, -1.5707963267948966, 0)
                end
            end

            return table.unpack(results, 1, results.n)
        end
        lassoItem.NetworkActivate = hook.wrapper
    end

    local onHit = rawget(lassoItem, "OnHit") or lassoItem.OnHit
    if type(onHit) == "function" and onHit ~= hook.onHitWrapper then
        hook.originalOnHit = onHit
        hook.onHitWrapper = function(self, hitPart)
            if not shouldDelayFasterLassoHit(hitPart) then
                return hook.originalOnHit(self, hitPart)
            end
            if self.FastLassoHitPending then
                return
            end

            self.FastLassoHitPending = true
            local lassoCircle = rawget(self, "LassoCircle") or self.LassoCircle
            local impactCFrame = lassoCircle and lassoCircle.CFrame or nil
            local wasAnchored = lassoCircle and lassoCircle.Anchored or false
            local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)

            if lassoCircle and lassoCircle:IsA("BasePart") and typeof(impactCFrame) == "CFrame" then
                lassoCircle.Anchored = true
                lassoCircle.CFrame = impactCFrame
                lassoCircle.Velocity = Vector3.zero
                local bodyForce = lassoCircle:FindFirstChild("BodyForce")
                if bodyForce then
                    bodyForce.Force = Vector3.zero
                end
                if network then
                    pcall(network.FireServer, network, "UpdateThrownLasso", impactCFrame)
                end
            end

            task.delay(LASSO_FAST_HIT_CONFIRM_DELAY, function()
                self.FastLassoHitPending = false

                local currentCircle = rawget(self, "LassoCircle") or self.LassoCircle
                if currentCircle and currentCircle:IsA("BasePart") and typeof(impactCFrame) == "CFrame" then
                    currentCircle.CFrame = impactCFrame
                    if network then
                        pcall(network.FireServer, network, "UpdateThrownLasso", impactCFrame)
                    end
                end

                if not (self.IsEquipped and self.IsThrowingLasso) then
                    if currentCircle and currentCircle:IsA("BasePart") then
                        currentCircle.Anchored = wasAnchored
                    end
                    return
                end

                hook.originalOnHit(self, hitPart)
            end)
        end
        lassoItem.OnHit = hook.onHitWrapper
    end
end

function restoreLassoNoRopeLimitHook()
    local params = gunFeatureState.lassoParams
    if isTable(params) then
        params.LassoMaxLength = gunFeatureState.originalLassoMaxLength
    end

    gunFeatureState.lassoParams = nil
    gunFeatureState.originalLassoMaxLength = nil
end

function ensureLassoNoRopeLimitHook()
    if not S.lassoNoRopeLimitEnabled then
        return
    end

    local params = resolveGlobalParams(false) or resolveGlobalParams(true)
    if not isTable(params) then
        return
    end

    if params ~= gunFeatureState.lassoParams then
        restoreLassoNoRopeLimitHook()
        gunFeatureState.lassoParams = params
        gunFeatureState.originalLassoMaxLength = rawget(params, "LassoMaxLength")
        if gunFeatureState.originalLassoMaxLength == nil then
            gunFeatureState.originalLassoMaxLength = params.LassoMaxLength
        end
    end

    params.LassoMaxLength = 1000000
end

function ensureFastReloadHook()
    if not S.fastReloadEnabled then
        return
    end

    local gunItem = getCurrentEquippedGunItem(false) or getCurrentEquippedGunItem(true)
    if not isTable(gunItem) then
        gunFeatureState.fastReloadItem = nil
        gunFeatureState.fastReloadOriginalPlayTrack = nil
        gunFeatureState.fastReloadWrapper = nil
        return
    end

    local playTrack = rawget(gunItem, "PlayTrack") or gunItem.PlayTrack
    if type(playTrack) ~= "function" then
        gunFeatureState.fastReloadItem = nil
        gunFeatureState.fastReloadOriginalPlayTrack = nil
        gunFeatureState.fastReloadWrapper = nil
        return
    end

    if type(gunFeatureState.fastReloadHooks) ~= "table" then
        gunFeatureState.fastReloadHooks = {}
    end

    local hook = gunFeatureState.fastReloadHooks[gunItem]
    if type(hook) ~= "table" then
        hook = {}
        gunFeatureState.fastReloadHooks[gunItem] = hook
    end

    if playTrack ~= hook.wrapper then
        hook.originalPlayTrack = playTrack
        hook.wrapper = function(self, trackName, ...)
            local packedArgs = table.pack(...)
            local isReloadTrack = S.fastReloadEnabled and isGunReloadTrack(trackName)
            if isReloadTrack and type(packedArgs[packedArgs.n]) == "number" then
                packedArgs[packedArgs.n] = packedArgs[packedArgs.n] * 10
            end
            local result = hook.originalPlayTrack(self, trackName, table.unpack(packedArgs, 1, packedArgs.n))
            if isReloadTrack and tostring(trackName) == "LoadStart" then
                task.spawn(function()
                    pcall(function()
                        self:StopTrack(trackName, 0.05)
                    end)
                end)
            end
            return result
        end
        gunItem.PlayTrack = hook.wrapper
    end

    gunFeatureState.fastReloadItem = gunItem
    gunFeatureState.fastReloadOriginalPlayTrack = hook.originalPlayTrack
    gunFeatureState.fastReloadWrapper = hook.wrapper
end

function restoreGunFeatureHooks()
    setNoRecoilGuardState(false)
    restoreNoFireCooldownHook()
    restoreWallBangHook()
    restoreFastReloadHook()
    restoreInstantLassoChargeHooks()
    restoreLassoSilentAimHooks()
    restoreLassoNoRopeLimitHook()
    restoreEquipWhileSwimmingHooks()
    clearGunAimDrawings()
end

function ensureGunFeatureHooks()
    setNoRecoilGuardState(S.noRecoilEnabled == true)

    if playerState.equipWhileSwimmingEnabled then
        ensureEquipWhileSwimmingHooks()
    else
        restoreEquipWhileSwimmingHooks()
    end

    if S.noRecoilEnabled then
        ensureNoRecoilHooks()
    end

    if S.noFireCooldownEnabled then
        ensureNoFireCooldownHook()
    else
        restoreNoFireCooldownHook()
    end

    if S.wallBangEnabled then
        ensureWallBangHook()
    else
        restoreWallBangHook()
    end

    if S.fastReloadEnabled then
        ensureFastReloadHook()
    else
        restoreFastReloadHook()
    end

    if S.instantLassoChargeEnabled then
        ensureInstantLassoChargeHooks()
    else
        restoreInstantLassoChargeHooks()
    end

    if S.lassoSilentAimEnabled or S.fasterLassoEnabled then
        ensureLassoSilentAimHook()
    else
        restoreLassoSilentAimHooks()
    end

    if S.lassoNoRopeLimitEnabled then
        ensureLassoNoRopeLimitHook()
    else
        restoreLassoNoRopeLimitHook()
    end

    if hasGunAimVisualsEnabled() then
        ensureGunAimRenderLoop()
    else
        clearGunAimDrawings()
    end
end

function runGunFeaturePulse()
    if playerState.equipWhileSwimmingEnabled then
        ensureEquipWhileSwimmingHooks()
    end
    if S.noRecoilEnabled then
        ensureNoRecoilHooks()
    end
    if S.noFireCooldownEnabled then
        ensureNoFireCooldownHook()
    end
    if S.wallBangEnabled then
        ensureWallBangHook()
    end
    if S.fastReloadEnabled then
        ensureFastReloadHook()
    end
    if S.instantLassoChargeEnabled then
        ensureInstantLassoChargeHooks()
    end
    if S.lassoSilentAimEnabled or S.fasterLassoEnabled then
        ensureLassoSilentAimHook()
    end
    if S.lassoNoRopeLimitEnabled then
        ensureLassoNoRopeLimitHook()
    end
end

function getEquippedPickaxe(character)
	return getBestCharacterPickaxeModel(character, isEquippedPickaxeName)
end

function getStoredPickaxe(character)
	return getBestCharacterPickaxeModel(character, isStoredPickaxeName)
end

function findPickaxe(character)
	return getEquippedPickaxe(character) or getStoredPickaxe(character) or getBackpackPickaxeTool()
end

function getResolvedItemData(candidate)
	if not isTable(candidate) then
		return nil
	end

	local itemData = rawget(candidate, "ItemData") or candidate.ItemData
	if isTable(itemData) then
		return itemData
	end

	return candidate
end

function getPickaxeTextScore(value)
	if type(value) ~= "string" then
		return 0
	end

	local normalized = safeLower(value):gsub("[^%w]", "")
	if normalized == "" then
		return 0
	end
	local tier = tonumber(string.match(normalized, "tier(%d+)pickaxe"))
	if tier ~= nil then
		return 20 + tier
	end
	if string.find(normalized, "basicpickaxe", 1, true) ~= nil then
		return 21
	end
	if string.find(normalized, "pickaxe", 1, true) ~= nil then
		return 8
	end
	if string.find(normalized, "pickax", 1, true) ~= nil then
		return 7
	end
	return 0
end

function resolveSharedItemData(playerItems, item)
	item = getResolvedItemData(item)
	if not isTable(playerItems) or not isTable(item) then
		return nil
	end

	local itemType = rawget(item, "Type") or item.Type
	if type(itemType) ~= "string" or itemType == "" then
		return nil
	end

	return rawget(playerItems, itemType) or playerItems[itemType]
end

function scorePickaxeItemCandidate(item, playerItems)
	item = getResolvedItemData(item)
	if not isTable(item) then
		return 0
	end

	local bestScore = 0
	local function scanSource(source)
		if not isTable(source) then
			return
		end

		for _, key in ipairs({
			"Type",
			"Id",
			"Name",
			"DisplayName",
			"ItemType",
			"VariantOf",
			"Model",
			"ModelName",
			"WorldModel",
			"WorldModelName",
			"ViewModel",
			"ViewModelName",
		}) do
			local score = getPickaxeTextScore(rawget(source, key) or source[key])
			if score > bestScore then
				bestScore = score
			end
		end

		if rawget(source, "IsPickaxe") == true or source.IsPickaxe == true then
			bestScore = math.max(bestScore, 9)
		end
		if rawget(source, "Pickaxe") == true or source.Pickaxe == true then
			bestScore = math.max(bestScore, 9)
		end
		if rawget(source, "MinePower") ~= nil or source.MinePower ~= nil
			or rawget(source, "MiningPower") ~= nil or source.MiningPower ~= nil
			or rawget(source, "PickaxePower") ~= nil or source.PickaxePower ~= nil then
			bestScore = math.max(bestScore, 7)
		end
		local pickaxeTier = tonumber(rawget(source, "PickaxeTier") or source.PickaxeTier)
		if pickaxeTier ~= nil then
			bestScore = math.max(bestScore, 20 + pickaxeTier)
		end
	end

	scanSource(item)
	scanSource(resolveSharedItemData(playerItems, item))
	return bestScore
end

function isLikelyPickaxeItem(item, playerItems)
	return scorePickaxeItemCandidate(item, playerItems) > 0
end

function getPlayerCharacterItemData(playerCharacter, methodName)
	if not isTable(playerCharacter) then
		return nil
	end

	local method = rawget(playerCharacter, methodName) or playerCharacter[methodName]
	if type(method) ~= "function" then
		return nil
	end

	local okResult, result = pcall(method, playerCharacter)
	if not okResult then
		return nil
	end

	return getResolvedItemData(result)
end

function getCurrentEquippedItemData(playerCharacter)
	return getPlayerCharacterItemData(playerCharacter, "GetEquippedItemData")
		or getPlayerCharacterItemData(playerCharacter, "GetEquippedItem")
end

function getVisualEquippedItemData(playerCharacter)
	return getPlayerCharacterItemData(playerCharacter, "GetVisualEquippedItemData")
		or getResolvedItemData(rawget(playerCharacter, "NextItemToEquip") or playerCharacter.NextItemToEquip)
		or getResolvedItemData(rawget(playerCharacter, "SwitchItem") or playerCharacter.SwitchItem)
		or getCurrentEquippedItemData(playerCharacter)
end

function getResolvedInventoryContainer(playerData)
	if not isTable(playerData) then
		return nil
	end

	local container = rawget(playerData, "InventoryContainer") or playerData.InventoryContainer
	if isTable(container) then
		return container
	end

	local getContainer = rawget(playerData, "GetContainer") or playerData.GetContainer
	if type(getContainer) ~= "function" then
		return nil
	end

	local okContainer, resolvedContainer = pcall(getContainer, playerData, "Inventory")
	if okContainer and isTable(resolvedContainer) then
		playerData.InventoryContainer = resolvedContainer
		return resolvedContainer
	end

	return nil
end

function getResolvedLoadoutTable(playerData, playerCharacter)
	if isTable(playerData) then
		local getLoadout = rawget(playerData, "GetLoadout") or playerData.GetLoadout
		if type(getLoadout) == "function" then
			local okLoadout, loadout = pcall(getLoadout, playerData)
			if okLoadout and isTable(loadout) then
				return loadout
			end
		end
	end

	if isTable(playerCharacter) then
		local state = rawget(playerCharacter, "State") or playerCharacter.State
		local loadout = isTable(state) and (rawget(state, "Loadout") or state.Loadout) or nil
		if isTable(loadout) then
			return loadout
		end
	end

	return nil
end

function resolveInventoryItemById(playerData, itemId)
	if not isTable(playerData) or type(itemId) ~= "string" or itemId == "" then
		return nil
	end

	local getInventoryItemWithId = rawget(playerData, "GetInventoryItemWithId") or playerData.GetInventoryItemWithId
	if type(getInventoryItemWithId) == "function" then
		local okItem, inventoryItem = pcall(getInventoryItemWithId, playerData, itemId)
		if okItem and isTable(inventoryItem) then
			return inventoryItem
		end
	end

	local inventoryContainer = getResolvedInventoryContainer(playerData)
	if not isTable(inventoryContainer) then
		return nil
	end

	local getItem = rawget(inventoryContainer, "GetItem") or inventoryContainer.GetItem
	if type(getItem) == "function" then
		local okItem, inventoryItem = pcall(getItem, inventoryContainer, itemId)
		if okItem and isTable(inventoryItem) then
			return inventoryItem
		end
	end

	local itemsById = rawget(inventoryContainer, "ItemsById") or inventoryContainer.ItemsById
	if isTable(itemsById) then
		local inventoryItem = rawget(itemsById, itemId) or itemsById[itemId]
		if isTable(inventoryItem) then
			return inventoryItem
		end
	end

	return nil
end

function findPickaxeLoadoutItem(playerData, playerCharacter, playerItems)
	local loadout = getResolvedLoadoutTable(playerData, playerCharacter)
	if not isTable(loadout) then
		return nil
	end

	local bestItem = nil
	local bestSlotType = nil
	local bestSlotIndex = nil
	local bestScore = 0

	for slotType, slots in pairs(loadout) do
		if isTable(slots) then
			for slotIndex, assignedItem in pairs(slots) do
				local score = scorePickaxeItemCandidate(assignedItem, playerItems)
				if score > bestScore then
					local itemId = isTable(assignedItem) and (rawget(assignedItem, "Id") or assignedItem.Id) or nil
					local resolvedItem = resolveInventoryItemById(playerData, itemId) or getResolvedItemData(assignedItem)
					if isTable(resolvedItem) then
						bestScore = score
						bestItem = resolvedItem
						bestSlotType = tostring(slotType)
						bestSlotIndex = tonumber(slotIndex) or slotIndex
					end
				end
			end
		end
	end

	return bestItem, bestSlotType, bestSlotIndex
end

function findInventoryPickaxeItem(playerData, playerItems, preferredItem)
	local preferredId = isTable(preferredItem) and (rawget(preferredItem, "Id") or preferredItem.Id) or nil
	local preferredInventoryItem = resolveInventoryItemById(playerData, preferredId)

	local inventoryContainer = getResolvedInventoryContainer(playerData)
	if not isTable(inventoryContainer) then
		return isLikelyPickaxeItem(preferredInventoryItem, playerItems) and preferredInventoryItem or nil
	end

	local items = rawget(inventoryContainer, "Items") or inventoryContainer.Items
	if not isTable(items) then
		return isLikelyPickaxeItem(preferredInventoryItem, playerItems) and preferredInventoryItem or nil
	end

	local bestItem = nil
	local bestScore = scorePickaxeItemCandidate(preferredInventoryItem, playerItems)
	if bestScore > 0 then
		bestItem = preferredInventoryItem
	end
	for _, inventoryItem in pairs(items) do
		local score = scorePickaxeItemCandidate(inventoryItem, playerItems)
		if score > bestScore then
			bestScore = score
			bestItem = inventoryItem
		end
	end

	return bestItem
end

function getPickaxeEquipmentType(item, playerItems, playerData, playerCharacter)
	item = getResolvedItemData(item)
	if not isTable(item) then
		return nil
	end

	local sharedData = resolveSharedItemData(playerItems, item)
	local equipmentType = rawget(item, "EquipmentType") or item.EquipmentType
		or (isTable(sharedData) and (rawget(sharedData, "EquipmentType") or sharedData.EquipmentType))
	if type(equipmentType) == "string" and equipmentType ~= "" then
		return equipmentType
	end

	local _, slotType = findPickaxeLoadoutItem(playerData, playerCharacter, playerItems)
	return slotType
end

function findPreferredLoadoutSlot(playerData, slotType, preferredItemId)
	if type(slotType) ~= "string" or slotType == "" then
		return nil
	end

	local loadout = getResolvedLoadoutTable(playerData)
	local slots = isTable(loadout) and loadout[slotType] or nil
	if not isTable(slots) then
		return 1
	end

	local matchingIndex = nil
	local openIndex = nil
	local fallbackIndex = nil

	for slotIndex, assignedItem in pairs(slots) do
		local numericIndex = tonumber(slotIndex) or slotIndex
		if type(numericIndex) == "number" then
			if fallbackIndex == nil or numericIndex < fallbackIndex then
				fallbackIndex = numericIndex
			end

			local assignedId = isTable(assignedItem) and (rawget(assignedItem, "Id") or assignedItem.Id) or nil
			if type(preferredItemId) == "string" and assignedId == preferredItemId then
				matchingIndex = numericIndex
			elseif not assignedItem and (openIndex == nil or numericIndex < openIndex) then
				openIndex = numericIndex
			end
		end
	end

	return matchingIndex or openIndex or fallbackIndex or 1
end

function hasPendingLoadoutSync(playerData)
	local replicatedState = resolveGlobalReplicatedState(false) or resolveGlobalReplicatedState(true)
	if isTable(replicatedState) then
		local state = rawget(replicatedState, "State") or replicatedState.State
		local recent = isTable(state) and (rawget(state, "RecentlyAddedToLoadout") or state.RecentlyAddedToLoadout) or nil
		if type(recent) == "table" and next(recent) ~= nil then
			return true
		end
	end

	if isTable(playerData) then
		local localRecent = rawget(playerData, "LocalRecentlyAddedToLoadout") or playerData.LocalRecentlyAddedToLoadout
		if type(localRecent) == "table" and next(localRecent) ~= nil then
			return true
		end
	end

	return false
end

function getInteractablesFolder()
	return workspace:FindFirstChild("WORKSPACE_Interactables") or workspace:FindFirstChild("WORKSPACE_Interactbles")
end

function getOreDepositsFolder()
	local interactables = getInteractablesFolder()
	local mining = interactables and interactables:FindFirstChild("Mining")
	return mining and mining:FindFirstChild("OreDeposits") or nil
end

function getSelectedOreFolders()
	local oreDeposits = getOreDepositsFolder()
	if not oreDeposits then
		return {}
	end

	local oreFolders = {}
	for _, oreOption in ipairs(ORE_OPTIONS) do
		if selectedOreNames[oreOption.id] then
			local oreFolder = oreDeposits:FindFirstChild(oreOption.id)
			if oreFolder then
				table.insert(oreFolders, oreFolder)
			end
		end
	end

	return oreFolders
end

function getDroppedItemsFolder()
	local interactables = getInteractablesFolder()
	return interactables and interactables:FindFirstChild("DroppedItems") or nil
end

function getOreRemainingValue(oreDeposit)
	local depositInfo = oreDeposit and oreDeposit:FindFirstChild("DepositInfo")
	local oreRemaining = depositInfo and depositInfo:FindFirstChild("OreRemaining")
	if oreRemaining and oreRemaining:IsA("NumberValue") then
		return oreRemaining
	end

	return nil
end

function getOreHealthValues(oreDeposit)
	local oreRemaining = getOreRemainingValue(oreDeposit)
	if not oreRemaining then
		return nil, nil
	end

	local currentValue = tonumber(oreRemaining.Value)
	if currentValue == nil then
		return nil, nil
	end

	local roundedCurrent = math.max(0, math.floor(currentValue + 0.5))
	local depositInfo = oreDeposit and oreDeposit:FindFirstChild("DepositInfo")
	local maxValue = nil

	if depositInfo then
		for _, childName in ipairs({ "MaxOreRemaining", "MaxHealth", "MaxOreHealth" }) do
			local child = depositInfo:FindFirstChild(childName)
			if child and child:IsA("NumberValue") then
				maxValue = tonumber(child.Value)
				break
			end
		end
	end

	if type(maxValue) == "number" then
		maxValue = math.max(roundedCurrent, math.floor(maxValue + 0.5))
	end

	return roundedCurrent, maxValue
end

function formatOreHealthText(oreDeposit, includeLabel)
	local currentValue, maxValue = getOreHealthValues(oreDeposit)
	if currentValue == nil then
		return includeLabel == false and "nil" or "HP: ?"
	end

	local healthText = maxValue and string.format("%d/%d", currentValue, maxValue) or tostring(currentValue)
	if includeLabel == false then
		return healthText
	end

	return "HP: " .. healthText
end

function getOreBasePart(oreDeposit)
	if not oreDeposit then
		return nil
	end

	local rockBase = oreDeposit:FindFirstChild("RockBase")
	if rockBase and rockBase:IsA("BasePart") then
		return rockBase
	end

	local rockOre = oreDeposit:FindFirstChild("RockOre")
	if rockOre and rockOre:IsA("BasePart") then
		return rockOre
	end

	return oreDeposit:FindFirstChildWhichIsA("BasePart", true)
end

function getOreRenderParts(oreDeposit)
	local oreParts = {}
	local seen = {}

	local function tryAdd(part)
		if part and part:IsA("BasePart") and not seen[part] then
			seen[part] = true
			table.insert(oreParts, part)
		end
	end

	tryAdd(oreDeposit and oreDeposit:FindFirstChild("RockBase"))
	tryAdd(oreDeposit and oreDeposit:FindFirstChild("RockOre"))
	tryAdd(getOreBasePart(oreDeposit))

	return oreParts
end

function getOrePosition(oreDeposit)
	local orePart = getOreBasePart(oreDeposit)
	if orePart then
		return orePart.Position
	end

	local ok, pivot = pcall(function()
		return oreDeposit:GetPivot()
	end)

	if ok then
		return pivot.Position
	end

	return nil
end

function isOreAvailable(oreDeposit)
	local oreRemaining = getOreRemainingValue(oreDeposit)
	return oreRemaining and oreRemaining.Value > 0
end

_G.getOreDepositsFolder = getOreDepositsFolder
_G.getOrePosition = getOrePosition
_G.isOreAvailable = isOreAvailable

function getDistanceToPosition(character, position)
	local rootPart = getRootPart(character)
	if not rootPart or not position then
		return math.huge
	end

	return (rootPart.Position - position).Magnitude
end

function getDistanceToOre(character, oreDeposit)
	return getDistanceToPosition(character, getOrePosition(oreDeposit))
end

function getDroppedItemBasePart(dropItem)
	if not dropItem then
		return nil
	end

	local pickupItem = dropItem:FindFirstChild("PickupItem", true)
	if pickupItem then
		local adornee = pickupItem:IsA("BillboardGui") and pickupItem.Adornee or nil
		if adornee and adornee:IsA("BasePart") then
			return adornee
		end

		local parent = pickupItem.Parent
		if parent and parent:IsA("BasePart") then
			return parent
		end
	end

	local dropCollider = dropItem:FindFirstChild("DropCollider", true)
	if dropCollider and dropCollider:IsA("BasePart") then
		return dropCollider
	end

	return dropItem:FindFirstChildWhichIsA("BasePart", true)
end

function isDroppedItemDisabled(dropItem)
	if not dropItem then
		return true
	end

	local pickupItem = dropItem:FindFirstChild("PickupItem", true)
	if not pickupItem then
		return false
	end

	local disabled = pickupItem:FindFirstChild("Disabled")
	if disabled and disabled:IsA("BoolValue") then
		return disabled.Value
	end

	return false
end

function findNearestDroppedItem(character, maxDistance)
	local droppedItems = getDroppedItemsFolder()
	local rootPart = getRootPart(character)
	if not droppedItems or not rootPart then
		return nil, nil
	end

	local nearestItem = nil
	local nearestDistance = nil
	local limit = maxDistance or math.huge

	for _, dropItem in ipairs(droppedItems:GetChildren()) do
		if not isDroppedItemDisabled(dropItem) and isAutoPickupDropAllowed(dropItem) then
			local part = getDroppedItemBasePart(dropItem)
			if part then
				local distance = (rootPart.Position - part.Position).Magnitude
				if distance <= limit and (not nearestDistance or distance < nearestDistance) then
					nearestDistance = distance
					nearestItem = dropItem
				end
			end
		end
	end

	return nearestItem, nearestDistance
end

function facePosition(character, targetPosition)
	local rootPart = getRootPart(character)
	if not rootPart or not targetPosition then
		return false
	end

	local lookAt = Vector3.new(targetPosition.X, rootPart.Position.Y, targetPosition.Z)
	if (lookAt - rootPart.Position).Magnitude < 0.05 then
		return true
	end

	local ok = pcall(function()
		rootPart.CFrame = CFrame.lookAt(rootPart.Position, lookAt)
	end)

	return ok
end

function getCharacterGroundPoint(character, rootPart)
	local activeRoot = rootPart or getRootPart(character)
	if not activeRoot then
		return nil
	end

	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.FilterDescendantsInstances = {
		getRecordedPathFolder(),
		getVisualizerFolder(),
		findPathPlayerAvoiderVisualizerFolder(),
	}
	if character then
		table.insert(raycastParams.FilterDescendantsInstances, character)
	end

	local scanOrigin = activeRoot.Position + Vector3.new(0, 0.75, 0)
	local ray = workspace:Raycast(scanOrigin, Vector3.new(0, -12, 0), raycastParams)
	if ray and ray.Position and ray.Normal and ray.Normal.Y >= 0.25 then
		return ray.Position
	end

	local groundAligner = rawget(_G, "getGroundAlignedPathPosition")
	if type(groundAligner) == "function" then
		return groundAligner(activeRoot.Position, character)
	end

	return nil
end

function getCharacterGroundClearance(character, rootPart)
	local activeRoot = rootPart or getRootPart(character)
	if not activeRoot then
		return math.max(TWEEN_GROUND_CLEARANCE - 0.08, 2.4)
	end

	local groundPoint = getCharacterGroundPoint(character, activeRoot)
	if groundPoint then
		local clearance = activeRoot.Position.Y - groundPoint.Y
		if clearance == clearance and clearance > 0.25 then
			return math.clamp(clearance, 1.6, 4.5)
		end
	end

	return math.max(TWEEN_GROUND_CLEARANCE - 0.08, 2.4)
end

function getPathFollowGroundClearance(character, rootPart)
	local currentClearance = getCharacterGroundClearance(character, rootPart)
	return math.clamp(currentClearance, 1.85, math.max(2.45, TWEEN_GROUND_CLEARANCE - 0.7))
end

function getPathFollowSurfaceTarget(character, rootPart, nextXZ, flatDirection, stepDistance, fallbackPosition)
	if not nextXZ then
		return fallbackPosition
	end

	local activeRoot = rootPart or getRootPart(character)
	local clearanceOffset = getPathFollowGroundClearance(character, activeRoot)
	local probePosition = Vector3.new(nextXZ.X, activeRoot and activeRoot.Position.Y or nextXZ.Y, nextXZ.Z)
	local stepGround = getGroundAlignedPathPosition(probePosition, character)
	local fallbackY = typeof(fallbackPosition) == "Vector3" and fallbackPosition.Y or probePosition.Y
	if not stepGround then
		stepGround = Vector3.new(nextXZ.X, fallbackY, nextXZ.Z)
	end

	local desiredGroundY = stepGround.Y
	if flatDirection and flatDirection.Magnitude > 0.001 then
		local lookAheadDistance = math.min(math.max((tonumber(stepDistance) or 0.5) * 1.15, 1.15), 3.6)
		local lookAheadProbe = probePosition + (flatDirection.Unit * lookAheadDistance)
		local aheadGround = getGroundAlignedPathPosition(lookAheadProbe, character)
		if aheadGround and aheadGround.Y > desiredGroundY then
			desiredGroundY = math.max(desiredGroundY, aheadGround.Y - 0.08)
		end
	end

	local desiredStepY = desiredGroundY + clearanceOffset
	if activeRoot then
		local horizontalStep = (
			Vector3.new(nextXZ.X, 0, nextXZ.Z) - Vector3.new(activeRoot.Position.X, 0, activeRoot.Position.Z)
		).Magnitude
		local verticalDelta = desiredStepY - activeRoot.Position.Y
		local verticalStep = math.max(0.3, math.min((horizontalStep * 1.15) + 0.45, 5.25))
		desiredStepY = activeRoot.Position.Y + math.clamp(verticalDelta, -verticalStep, verticalStep)
	end

	return Vector3.new(nextXZ.X, desiredStepY, nextXZ.Z)
end

function getSafeTweenTargetPosition(character, targetPosition)
	local rootPart = getRootPart(character)
	if not rootPart or not targetPosition then
		return targetPosition
	end

	local groundAligner = rawget(_G, "getGroundAlignedPathPosition")
	if type(groundAligner) == "function" then
		local grounded = groundAligner(targetPosition, character)
		if grounded then
			local clearanceOffset = getCharacterGroundClearance(character, rootPart)
			return Vector3.new(targetPosition.X, grounded.Y + clearanceOffset, targetPosition.Z)
		end
	end

	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.FilterDescendantsInstances = {
		character,
		getRecordedPathFolder(),
		getVisualizerFolder(),
		findPathPlayerAvoiderVisualizerFolder(),
	}

	local scanOrigin = Vector3.new(targetPosition.X, targetPosition.Y + TWEEN_GROUND_SCAN_HEIGHT, targetPosition.Z)
	local ray = workspace:Raycast(scanOrigin, Vector3.new(0, -(TWEEN_GROUND_SCAN_HEIGHT * 2), 0), raycastParams)
	if ray and ray.Position and ray.Normal and ray.Normal.Y >= 0.25 then
		local minY = ray.Position.Y + getCharacterGroundClearance(character, rootPart)
		return Vector3.new(targetPosition.X, math.max(targetPosition.Y, minY), targetPosition.Z)
	end

	return targetPosition
end

local function projectPointOntoSegment(point, segmentStart, segmentFinish)
	local segmentDelta = segmentFinish - segmentStart
	local segmentLengthSquared = segmentDelta:Dot(segmentDelta)
	if segmentLengthSquared <= 1e-4 then
		return segmentStart, 0, (point - segmentStart).Magnitude, 0
	end

	local alpha = math.clamp((point - segmentStart):Dot(segmentDelta) / segmentLengthSquared, 0, 1)
	local projectedPoint = segmentStart + (segmentDelta * alpha)
	return projectedPoint, alpha, (point - projectedPoint).Magnitude, math.sqrt(segmentLengthSquared)
end

local function buildPathPlayerAvoiderSegments(rootPosition, currentTargetPosition)
	local segments = {}
	if typeof(rootPosition) ~= "Vector3" then
		return segments
	end

	local lookAheadDistance = math.max(1, getPathPlayerAvoiderDistance())
	local remainingDistance = lookAheadDistance
	local traveledDistance = 0
	local currentPath = pathState.currentPath
	local playbackNodeIndex = math.max(1, tonumber(pathState.playbackNodeIndex) or 1)
	local segmentStart = rootPosition

	local function appendSegment(segmentEnd, nodeIndex)
		if typeof(segmentEnd) ~= "Vector3" or remainingDistance <= 0 then
			return false
		end

		local segmentDelta = segmentEnd - segmentStart
		local segmentLength = segmentDelta.Magnitude
		if segmentLength <= 0.05 then
			segmentStart = segmentEnd
			return false
		end

		local segmentDistance = math.min(segmentLength, remainingDistance)
		local cappedFinish = segmentLength > segmentDistance
			and (segmentStart + (segmentDelta.Unit * segmentDistance))
			or segmentEnd

		table.insert(segments, {
			start = segmentStart,
			finish = cappedFinish,
			length = segmentDistance,
			nodeIndex = nodeIndex,
			distanceStart = traveledDistance,
		})

		traveledDistance += segmentDistance
		remainingDistance -= segmentDistance
		segmentStart = cappedFinish
		return remainingDistance <= 0
	end

	if typeof(currentTargetPosition) == "Vector3" and appendSegment(currentTargetPosition, playbackNodeIndex) then
		return segments
	end

	if not currentPath or type(currentPath.nodes) ~= "table" then
		return segments
	end

	for nodeIndex = playbackNodeIndex + 1, #currentPath.nodes do
		local nodePosition = getPathNodePosition(currentPath.nodes[nodeIndex])
		if nodePosition and appendSegment(nodePosition, nodeIndex) then
			break
		end
	end

	return segments
end

local function buildPathPlayerAvoiderRetreatSegments(rootPosition)
	local segments = {}
	if typeof(rootPosition) ~= "Vector3" then
		return segments
	end

	local currentPath = pathState.currentPath
	if not currentPath or type(currentPath.nodes) ~= "table" then
		return segments
	end

	local remainingDistance = math.max(PATH_PLAYER_AVOIDER_BACKUP_STEP_DISTANCE, 1)
	local traveledDistance = 0
	local playbackNodeIndex = math.max(1, tonumber(pathState.playbackNodeIndex) or 1)
	local segmentStart = rootPosition

	local function appendSegment(segmentEnd, nodeIndex)
		if typeof(segmentEnd) ~= "Vector3" or remainingDistance <= 0 then
			return false
		end

		local segmentDelta = segmentEnd - segmentStart
		local segmentLength = segmentDelta.Magnitude
		if segmentLength <= 0.05 then
			segmentStart = segmentEnd
			return false
		end

		local segmentDistance = math.min(segmentLength, remainingDistance)
		local cappedFinish = segmentLength > segmentDistance
			and (segmentStart + (segmentDelta.Unit * segmentDistance))
			or segmentEnd

		table.insert(segments, {
			start = segmentStart,
			finish = cappedFinish,
			length = segmentDistance,
			nodeIndex = nodeIndex,
			distanceStart = traveledDistance,
		})

		traveledDistance += segmentDistance
		remainingDistance -= segmentDistance
		segmentStart = cappedFinish
		return remainingDistance <= 0
	end

	for nodeIndex = playbackNodeIndex - 1, 1, -1 do
		local nodePosition = getPathNodePosition(currentPath.nodes[nodeIndex])
		if nodePosition and appendSegment(nodePosition, nodeIndex) then
			break
		end
	end

	return segments
end

local function samplePathPlayerAvoiderPoint(segments, sampleDistance)
	if type(segments) ~= "table" or #segments == 0 then
		return nil
	end

	local remaining = math.max(0, tonumber(sampleDistance) or 0)
	for _, segment in ipairs(segments) do
		local segmentLength = tonumber(segment.length) or 0
		if segmentLength > 0.05 then
			if remaining <= segmentLength then
				local direction = segment.finish - segment.start
				if direction.Magnitude <= 0.05 then
					return segment.finish
				end
				return segment.start + (direction.Unit * remaining)
			end
			remaining -= segmentLength
		end
	end

	return segments[#segments].finish
end

local function isPathPlayerAvoiderLineClear(originPosition, targetPosition, localCharacter, targetCharacter)
	if typeof(originPosition) ~= "Vector3" or typeof(targetPosition) ~= "Vector3" then
		return false
	end

	local direction = targetPosition - originPosition
	if direction.Magnitude <= 0.05 then
		return true
	end

	local ignoreInstances = {
		localCharacter,
		targetCharacter,
		getRecordedPathFolder(),
		getVisualizerFolder(),
		findPathPlayerAvoiderVisualizerFolder(),
	}
	local ignoreFolder = workspace:FindFirstChild("Ignore")
	if ignoreFolder then
		table.insert(ignoreInstances, ignoreFolder)
	end

	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.FilterDescendantsInstances = ignoreInstances

	local result = workspace:Raycast(originPosition, direction, raycastParams)
	if not result then
		return true
	end

	return targetCharacter and result.Instance and result.Instance:IsDescendantOf(targetCharacter)
end

local function isPathPlayerAvoiderCandidateReachable(localCharacter, candidateCharacter, candidateRootPart, projectedPoint)
	if not (localCharacter and candidateCharacter and candidateRootPart and typeof(projectedPoint) == "Vector3") then
		return false
	end

	local pathFloor = getGroundAlignedPathPosition(projectedPoint, localCharacter) or projectedPoint
	local playerFloor = getGroundAlignedPathPosition(candidateRootPart.Position, candidateCharacter) or candidateRootPart.Position
	if math.abs(playerFloor.Y - pathFloor.Y) > PATH_PLAYER_AVOIDER_LAYER_TOLERANCE then
		return false
	end

	local rayOrigin = pathFloor + Vector3.new(0, 1.8, 0)
	local rayTarget = candidateRootPart.Position + Vector3.new(0, 1.4, 0)
	return isPathPlayerAvoiderLineClear(rayOrigin, rayTarget, localCharacter, candidateCharacter)
end

local function findPathPlayerAvoiderRadiusTarget(character)
	if not (isPathPlayerAvoiderEnabled() and pathState.playbackActive) then
		return nil
	end

	local rootPart = getRootPart(character)
	if not rootPart then
		return nil
	end

	local radius = getPathPlayerAvoiderRadius()
	local localFloor = getGroundAlignedPathPosition(rootPart.Position, character) or rootPart.Position
	local bestCandidate = nil
	for _, player in ipairs(_Players:GetPlayers()) do
		if player ~= localPlayer then
			local candidateCharacter = getWorldCharacterForPlayer(player)
			local candidateRootPart = getTwwModelRoot(candidateCharacter) or getRootPart(candidateCharacter)
			if candidateCharacter and candidateRootPart and isCharacterAlive(candidateCharacter) then
				local flatDelta = Vector3.new(
					candidateRootPart.Position.X - rootPart.Position.X,
					0,
					candidateRootPart.Position.Z - rootPart.Position.Z
				)
				local horizontalDistance = flatDelta.Magnitude
				local rootSize = candidateRootPart.Size
				local touchMargin = math.max(PATH_PLAYER_AVOIDER_RADIUS_TOUCH_MARGIN, ((rootSize.X + rootSize.Z) * 0.25))
				if horizontalDistance <= (radius + touchMargin) then
					local candidateFloor = getGroundAlignedPathPosition(candidateRootPart.Position, candidateCharacter) or candidateRootPart.Position
					local verticalGap = math.abs(candidateFloor.Y - localFloor.Y)
					if verticalGap <= math.max(PATH_PLAYER_AVOIDER_LAYER_TOLERANCE, 10) then
						local localDistance = (candidateRootPart.Position - rootPart.Position).Magnitude
						if not bestCandidate or horizontalDistance < bestCandidate.horizontalDistance or (math.abs(horizontalDistance - bestCandidate.horizontalDistance) <= 0.1 and localDistance < bestCandidate.localDistance) then
							bestCandidate = {
								player = player,
								character = candidateCharacter,
								rootPart = candidateRootPart,
								projectedPoint = candidateFloor,
								horizontalDistance = horizontalDistance,
								localDistance = localDistance,
								action = localDistance <= PATH_PLAYER_AVOIDER_BACKUP_TRIGGER_DISTANCE and "backup" or "wait",
							}
						end
					end
				end
			end
		end
	end

	return bestCandidate
end

local function findPathPlayerAvoiderTarget(character, currentTargetPosition)
	if not (isPathPlayerAvoiderEnabled() and pathState.playbackActive and pathState.currentPath) then
		return nil
	end

	if getPathPlayerAvoiderMethod() == "Radius Field" then
		return findPathPlayerAvoiderRadiusTarget(character)
	end

	local rootPart = getRootPart(character)
	if not rootPart then
		return nil
	end

	local segments = buildPathPlayerAvoiderSegments(rootPart.Position, currentTargetPosition)
	local flatForwardDirection = nil
	if typeof(currentTargetPosition) == "Vector3" then
		flatForwardDirection = Vector3.new(
			currentTargetPosition.X - rootPart.Position.X,
			0,
			currentTargetPosition.Z - rootPart.Position.Z
		)
	end
	if (not flatForwardDirection or flatForwardDirection.Magnitude <= 0.001) and #segments > 0 then
		local firstSegment = segments[1]
		flatForwardDirection = Vector3.new(
			firstSegment.finish.X - firstSegment.start.X,
			0,
			firstSegment.finish.Z - firstSegment.start.Z
		)
	end
	if #segments == 0 then
		return nil
	end

	local bestEmergencyCandidate = nil
	local bestCandidate = nil
	for _, player in ipairs(_Players:GetPlayers()) do
		if player ~= localPlayer then
			local candidateCharacter = getWorldCharacterForPlayer(player)
			local candidateRootPart = getTwwModelRoot(candidateCharacter) or getRootPart(candidateCharacter)
			if candidateCharacter and candidateRootPart and isCharacterAlive(candidateCharacter) then
				local localDistance = (candidateRootPart.Position - rootPart.Position).Magnitude
				if localDistance <= PATH_PLAYER_AVOIDER_EMERGENCY_DISTANCE
					and isPathPlayerAvoiderCandidateReachable(character, candidateCharacter, candidateRootPart, rootPart.Position) then
					local flatToPlayer = Vector3.new(
						candidateRootPart.Position.X - rootPart.Position.X,
						0,
						candidateRootPart.Position.Z - rootPart.Position.Z
					)
					local relativeDot = 0
					if flatForwardDirection and flatForwardDirection.Magnitude > 0.001 and flatToPlayer.Magnitude > 0.001 then
						relativeDot = flatForwardDirection.Unit:Dot(flatToPlayer.Unit)
					end
					local emergencyAction = relativeDot < -0.1 and "advance" or "backup"
					if not bestEmergencyCandidate or localDistance < bestEmergencyCandidate.localDistance then
						bestEmergencyCandidate = {
							player = player,
							character = candidateCharacter,
							rootPart = candidateRootPart,
							projectedPoint = rootPart.Position,
							alongDistance = 0,
							lateralDistance = 0,
							localDistance = localDistance,
							action = emergencyAction,
						}
					end
				end

				for _, segment in ipairs(segments) do
					local projectedPoint, alpha, lateralDistance, segmentLength = projectPointOntoSegment(
						candidateRootPart.Position,
						segment.start,
						segment.finish
					)
					if lateralDistance <= PATH_PLAYER_AVOIDER_CORRIDOR_RADIUS then
						local segmentDirection = segment.finish - segment.start
						if segmentLength > 0.05 and not (alpha <= 0.001 and (candidateRootPart.Position - segment.start):Dot(segmentDirection.Unit) < -0.75) then
							local alongDistance = (segment.distanceStart or 0) + (segment.length * alpha)
							if alongDistance <= getPathPlayerAvoiderDistance()
								and isPathPlayerAvoiderCandidateReachable(character, candidateCharacter, candidateRootPart, projectedPoint) then
								if not bestCandidate
									or alongDistance < bestCandidate.alongDistance
									or (math.abs(alongDistance - bestCandidate.alongDistance) <= 0.1 and lateralDistance < bestCandidate.lateralDistance) then
									bestCandidate = {
										player = player,
										character = candidateCharacter,
										rootPart = candidateRootPart,
										projectedPoint = projectedPoint,
										alongDistance = alongDistance,
										lateralDistance = lateralDistance,
										localDistance = localDistance,
										action = localDistance <= PATH_PLAYER_AVOIDER_BACKUP_TRIGGER_DISTANCE and "backup" or "wait",
									}
								end
							end
						end
					end
				end
			end
		end
	end

	if bestEmergencyCandidate then
		return bestEmergencyCandidate
	end

	return bestCandidate
end

function getPathPlayerAvoiderTarget(character, currentTargetPosition)
	if not (type(pathState) == "table" and isPathPlayerAvoiderEnabled() and pathState.playbackActive) then
		clearPathPlayerAvoiderDetection()
		return nil
	end

	local rootPart = getRootPart(character)
	if not rootPart then
		if type(clearPathPlayerAvoiderCorridorVisualization) == "function" then
			clearPathPlayerAvoiderCorridorVisualization()
		end
		destroyPathPlayerAvoiderVisual()
		pathState.playerAvoiderBlockedPlayerName = nil
		return nil
	end

	local avoidMethod = getPathPlayerAvoiderMethod()
	local finalResult = nil
	local now = os.clock()
	local cachedResult = pathState.playerAvoiderLastResult
	if cachedResult and (now - (tonumber(pathState.playerAvoiderLastScanAt) or 0)) < PATH_PLAYER_AVOIDER_SCAN_INTERVAL then
		finalResult = cachedResult
	else
		finalResult = findPathPlayerAvoiderTarget(character, currentTargetPosition)
		pathState.playerAvoiderLastScanAt = now
		pathState.playerAvoiderLastResult = finalResult
	end

	if avoidMethod == "Radius Field" and type(updatePathPlayerAvoiderRadiusVisualization) == "function" then
		updatePathPlayerAvoiderRadiusVisualization(rootPart.Position, character, finalResult)
	elseif type(updatePathPlayerAvoiderCorridorVisualization) == "function" then
		local liveSegments = buildPathPlayerAvoiderSegments(rootPart.Position, currentTargetPosition)
		updatePathPlayerAvoiderCorridorVisualization(rootPart.Position, liveSegments, finalResult)
	elseif type(clearPathPlayerAvoiderCorridorVisualization) == "function" then
		clearPathPlayerAvoiderCorridorVisualization()
	end

	if finalResult then
		updatePathPlayerAvoiderVisual(finalResult.player, finalResult.character, finalResult.rootPart)
	else
		destroyPathPlayerAvoiderVisual()
		pathState.playerAvoiderBlockedPlayerName = nil
	end

	return finalResult
end

local function getPathPlayerAvoiderRetreatTarget(character, rootPart, currentTargetPosition)
	if not rootPart then
		return nil
	end

	local retreatTarget = samplePathPlayerAvoiderPoint(
		buildPathPlayerAvoiderRetreatSegments(rootPart.Position),
		PATH_PLAYER_AVOIDER_BACKUP_STEP_DISTANCE
	)
	if retreatTarget then
		return retreatTarget
	end

	local forwardSegments = buildPathPlayerAvoiderSegments(rootPart.Position, currentTargetPosition)
	for _, segment in ipairs(forwardSegments) do
		local segmentDelta = segment.finish - segment.start
		if segmentDelta.Magnitude > 0.05 then
			local fallbackTarget = rootPart.Position - (segmentDelta.Unit * PATH_PLAYER_AVOIDER_BACKUP_STEP_DISTANCE)
			local groundedTarget = getGroundAlignedPathPosition(fallbackTarget, character)
			return groundedTarget or fallbackTarget
		end
	end

	return nil
end

local function getPathPlayerAvoiderAdvanceTarget(rootPart, currentTargetPosition)
	if not rootPart then
		return nil
	end
	return samplePathPlayerAvoiderPoint(
		buildPathPlayerAvoiderSegments(rootPart.Position, currentTargetPosition),
		PATH_PLAYER_AVOIDER_BACKUP_STEP_DISTANCE
	)
end

local function cancelPathPlayerAvoiderMining(character, pickaxe)
	currentTargetOre = nil
	setInteractKeyHeld(false)
	if pickaxe and pickaxe:IsA("Tool") and pickaxe.Parent == character then
		pcall(function()
			pickaxe:Deactivate()
		end)
		return
	end

	local equippedPickaxe = character and getEquippedPickaxe(character)
	if equippedPickaxe and equippedPickaxe:IsA("Tool") then
		pcall(function()
			equippedPickaxe:Deactivate()
		end)
	end
end

local function movePathPlayerAvoiderTarget(character, humanoid, targetPosition, activeToken)
	if not (character and typeof(targetPosition) == "Vector3") then
		return false
	end

	pathState.playerAvoiderInternalMove = true
	local ok, moved = pcall(function()
		if movementMode == "legit" then
			return moveTowardsPosition(
				character,
				humanoid or getHumanoid(character),
				targetPosition,
				activeToken,
				getDirectPathTravelTimeout(character, targetPosition, 0.28)
			)
		end
		if movementMode == "cframe" then
			return cframeCharacterTowards(
				character,
				targetPosition,
				activeToken,
				getDirectPathTravelTimeout(character, targetPosition, 0.45),
				math.max(0.55, WAYPOINT_REACH_DISTANCE * 0.18),
				{
					flatPathFollow = true,
					useUndergroundOffset = false,
				}
			)
		end
		return moveTowardsRecordedPathNode(
			character,
			humanoid or getHumanoid(character),
			targetPosition,
			activeToken
		)
	end)
	pathState.playerAvoiderInternalMove = false
	if not ok then
		pushDebug(tostring(moved))
		return false
	end
	return moved == true
end

function processPathPlayerAvoidance(character, currentTargetPosition, activeToken, forwardDirection, humanoid, pickaxe)
	if not (character and isPathPlayerAvoiderEnabled() and pathState.playbackActive) then
		clearPathPlayerAvoiderDetection()
		return false
	end
	if pathState.playerAvoiderInternalMove == true then
		return false
	end
	if activeToken and activeToken ~= autoMineToken then
		return false
	end

	local rootPart = getRootPart(character)
	if not rootPart then
		clearPathPlayerAvoiderDetection()
		return false
	end

	local blocker = getPathPlayerAvoiderTarget(character, currentTargetPosition)
	if not blocker then
		return false
	end

	local playerName = blocker.player and blocker.player.Name or "Player"
	cancelPathPlayerAvoiderMining(character, pickaxe)
	clearMovementKeys()
	cancelActiveMoveTween()
	setInteractKeyHeld(false)
	if movementMode ~= "cframe" or blocker.action == "wait" then
		endExperimentalCFrameTravel(character)
	end

	if getPathPlayerAvoiderAction() == "Hop" then
		setStatus("Player detected, hopping servers...", Color3.fromRGB(255, 190, 120))
		setLastMoveCommand(Vector3.zero, "PathAvoidHop")
		requestPathServerHop("Player detected, hopping servers...", getPathCycleRestartSlotIndex(), true, "player")
		return true, "hop"
	end

	if blocker.action == "backup" then
		local retreatDirection = typeof(forwardDirection) == "Vector3" and (-Vector3.new(forwardDirection.X, 0, forwardDirection.Z)) or Vector3.zero
		local retreatTarget = getPathPlayerAvoiderRetreatTarget(character, rootPart, currentTargetPosition)
		setStatus("Backing up from " .. playerName, Color3.fromRGB(255, 190, 120))
		setLastMoveCommand(retreatDirection, "PathAvoidBack")
		if retreatTarget then
			movePathPlayerAvoiderTarget(character, humanoid, retreatTarget, activeToken)
		elseif currentTargetPosition then
			local advanceFallback = getPathPlayerAvoiderAdvanceTarget(rootPart, currentTargetPosition)
			if advanceFallback then
				movePathPlayerAvoiderTarget(character, humanoid, advanceFallback, activeToken)
			else
				task.wait(PATH_PLAYER_AVOIDER_WAIT_STEP)
			end
		else
			task.wait(PATH_PLAYER_AVOIDER_WAIT_STEP)
		end
		return true, "backup"
	end

	if blocker.action == "advance" then
		local advanceDirection = typeof(forwardDirection) == "Vector3" and Vector3.new(forwardDirection.X, 0, forwardDirection.Z) or Vector3.zero
		local advanceTarget = getPathPlayerAvoiderAdvanceTarget(rootPart, currentTargetPosition)
		setStatus("Moving ahead from " .. playerName, Color3.fromRGB(255, 190, 120))
		setLastMoveCommand(advanceDirection, "PathAvoidAdvance")
		if advanceTarget then
			movePathPlayerAvoiderTarget(character, humanoid, advanceTarget, activeToken)
		else
			task.wait(PATH_PLAYER_AVOIDER_WAIT_STEP)
		end
		return true, "advance"
	end

	setStatus("Waiting for " .. playerName .. " to move", Color3.fromRGB(255, 190, 120))
	setLastMoveCommand(Vector3.zero, "PathAvoidWait")
	task.wait(PATH_PLAYER_AVOIDER_WAIT_STEP)
	return true, "wait"
end

function findNearestOreInFolders(character, oreFolders)
	local rootPart = getRootPart(character)
	if not rootPart or not isTable(oreFolders) then
		return nil
	end

	local nearestOre = nil
	local nearestDistance = nil

	for _, oreFolder in ipairs(oreFolders) do
		if oreFolder and oreFolder.Parent then
			for _, oreDeposit in ipairs(oreFolder:GetChildren()) do
				local oreRemaining = getOreRemainingValue(oreDeposit)
				local orePosition = getOrePosition(oreDeposit)
				if oreRemaining and oreRemaining.Value > 0 and orePosition then
					local distance = (rootPart.Position - orePosition).Magnitude
					if not nearestDistance or distance < nearestDistance then
						nearestDistance = distance
						nearestOre = oreDeposit
					end
				end
			end
		end
	end

	return nearestOre, nearestDistance
end

function findNearestOre(character)
	if not hasSelectedOres() then
		return nil
	end

	return findNearestOreInFolders(character, getSelectedOreFolders())
end

function findNearestAnyOre(character)
	local oreDeposits = getOreDepositsFolder()
	if not oreDeposits then
		return nil
	end

	local oreFolders = {}
	for _, oreFolder in ipairs(oreDeposits:GetChildren()) do
		if oreFolder and oreFolder:IsA("Folder") then
			table.insert(oreFolders, oreFolder)
		end
	end

	return findNearestOreInFolders(character, oreFolders)
end

function findNearestOreWithinRange(character, preferredOre, maxDistance, allowAnyOreFallback)
	local distanceLimit = maxDistance or MINE_AURA_RANGE
	if not character then
		return nil, nil
	end

	if preferredOre and preferredOre.Parent and isOreNameSelected(getOreGroupName(preferredOre)) and isOreAvailable(preferredOre) then
		local preferredDistance = getDistanceToOre(character, preferredOre)
		if preferredDistance <= distanceLimit then
			return preferredOre, preferredDistance
		end
	end

	local nearestOre, nearestDistance = findNearestOre(character)
	if nearestOre and nearestDistance and nearestDistance <= distanceLimit then
		return nearestOre, nearestDistance
	end

	if allowAnyOreFallback == true then
		local fallbackOre, fallbackDistance = findNearestAnyOre(character)
		if fallbackOre and fallbackDistance and fallbackDistance <= distanceLimit then
			return fallbackOre, fallbackDistance
		end
	end

	return nil, nil
end

function walkHumanoidTowards(character, humanoid, targetPosition, activeToken, timeout)
	local startedAt = os.clock()
	local lastProgressAt = startedAt
	local lastPosition = nil

	while autoMineEnabled and activeToken == autoMineToken and (os.clock() - startedAt) < (timeout or MOVE_TIMEOUT) do
		local rootPart = getRootPart(character)
		if not rootPart then
			break
		end

		local currentPosition = rootPart.Position
		local flatTarget = Vector3.new(targetPosition.X, currentPosition.Y, targetPosition.Z)
		local delta = flatTarget - currentPosition
		local distance = delta.Magnitude

		if distance <= WAYPOINT_REACH_DISTANCE then
			clearMovementKeys()
			setLastMoveCommand(Vector3.zero, "arrived")
			return true
		end

		local direction = delta.Unit
		local basisForward = nil
		local basisRight = nil

		local camera = workspace.CurrentCamera
		if camera then
			local camForward = Vector3.new(camera.CFrame.LookVector.X, 0, camera.CFrame.LookVector.Z)
			if camForward.Magnitude > 0.01 then
				basisForward = camForward.Unit
				local camRight = Vector3.new(camera.CFrame.RightVector.X, 0, camera.CFrame.RightVector.Z)
				if camRight.Magnitude > 0.01 then
					basisRight = camRight.Unit
				end
			end
		end

		if not basisForward or not basisRight then
			local rootForward = Vector3.new(rootPart.CFrame.LookVector.X, 0, rootPart.CFrame.LookVector.Z)
			local rootRight = Vector3.new(rootPart.CFrame.RightVector.X, 0, rootPart.CFrame.RightVector.Z)
			if rootForward.Magnitude > 0.01 and rootRight.Magnitude > 0.01 then
				basisForward = rootForward.Unit
				basisRight = rootRight.Unit
			end
		end

		local forwardDot = basisForward and basisForward:Dot(direction) or 0
		local rightDot = basisRight and basisRight:Dot(direction) or 0

		local pressW = forwardDot > MOVE_DIRECTION_DEADZONE
		local pressS = forwardDot < -MOVE_DIRECTION_DEADZONE
		local pressD = rightDot > MOVE_SIDE_DEADZONE
		local pressA = rightDot < -MOVE_SIDE_DEADZONE

		if not (pressW or pressS or pressA or pressD) then
			pressW = true
		end

		setMovementKey(Enum.KeyCode.W, pressW)
		setMovementKey(Enum.KeyCode.A, pressA)
		setMovementKey(Enum.KeyCode.S, pressS)
		setMovementKey(Enum.KeyCode.D, pressD)
		setMovementKey(Enum.KeyCode.LeftShift, pressW or pressA or pressS or pressD)
		setLastMoveCommand(direction, "InputKeys")

		if lastPosition and (currentPosition - lastPosition).Magnitude > 0.15 then
			lastProgressAt = os.clock()
		end
		lastPosition = currentPosition

		if (os.clock() - lastProgressAt) > WALK_PROGRESS_TIMEOUT then
			pushDebug("walk stalled before reaching waypoint")
			break
		end

		task.wait(0.05)
	end

	clearMovementKeys()
	setLastMoveCommand(Vector3.zero, "stop")
	return false
end

function tweenCharacterTowards(character, targetPosition, activeToken, timeout)
	local rootPart = getRootPart(character)
	if not rootPart then
		return false
	end

	clearMovementKeys()
	cancelActiveMoveTween()

	local safeTarget = getSafeTweenTargetPosition(character, targetPosition)

	local currentPosition = rootPart.Position
	local delta = safeTarget - currentPosition
	local distance = delta.Magnitude
	if distance <= WAYPOINT_REACH_DISTANCE then
		setLastMoveCommand(Vector3.zero, "TweenArrived")
		return true
	end

	local direction = delta.Unit
	setLastMoveCommand(direction, "Tween")

	local moveTime = math.min(timeout or MOVE_TIMEOUT, math.max(0.08, distance / tweenMoveSpeed))
	local flatDirection = Vector3.new(direction.X, 0, direction.Z)
	if flatDirection.Magnitude < 0.001 then
		flatDirection = Vector3.new(0, 0, -1)
	end
	local goalCFrame = CFrame.lookAt(safeTarget, safeTarget + flatDirection)

	local tween = TweenService:Create(
		rootPart,
		TweenInfo.new(moveTime, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
		{ CFrame = goalCFrame }
	)
	activeMoveTween = tween

	local completed = false
	local connection = tween.Completed:Connect(function()
		completed = true
	end)

	tween:Play()

	local startedAt = os.clock()
	while autoMineEnabled and activeToken == autoMineToken and not completed and (os.clock() - startedAt) < (moveTime + 0.35) do
		task.wait(0.03)
	end

	connection:Disconnect()
	if activeMoveTween == tween then
		activeMoveTween = nil
	end
	if not completed then
		pcall(function()
			tween:Cancel()
		end)
	end

	return getDistanceToPosition(character, safeTarget) <= (WAYPOINT_REACH_DISTANCE + 1.5)
end

function cframeCharacterTowards(character, targetPosition, activeToken, timeout, arriveDistance, movementOptions)
	local rootPart = getRootPart(character)
	if not rootPart then
		return false
	end

	clearMovementKeys()
	cancelActiveMoveTween()

	local useFlatPathFollow = type(movementOptions) == "table" and movementOptions.flatPathFollow == true
	local useUndergroundOffset = type(movementOptions) == "table" and movementOptions.useUndergroundOffset == true
	local effectiveUndergroundOffset = 0
	local workingTarget = targetPosition
	if useUndergroundOffset and typeof(targetPosition) == "Vector3" then
		workingTarget = targetPosition - Vector3.new(0, effectiveUndergroundOffset, 0)
	end
	local safeTarget = useUndergroundOffset and workingTarget or getSafeTweenTargetPosition(character, workingTarget)
	local function getFlatPathFollowClearance(activeRootPart)
		local liveClearance = getCharacterGroundClearance(character, activeRootPart or rootPart)
		return math.clamp(liveClearance, 1.85, math.max(2.45, TWEEN_GROUND_CLEARANCE - 0.7))
	end
	if useFlatPathFollow then
		if useUndergroundOffset then
			safeTarget = workingTarget
		else
			local groundedTarget = getGroundAlignedPathPosition(workingTarget, character) or workingTarget
			local clearanceOffset = getFlatPathFollowClearance(rootPart)
			safeTarget = Vector3.new(workingTarget.X, groundedTarget.Y + clearanceOffset, workingTarget.Z)
		end
	end
	local wantedDistance = math.max(0.2, tonumber(arriveDistance) or WAYPOINT_REACH_DISTANCE)
	local startedAt = os.clock()
	local lastStepAt = startedAt
	local cframeTravelSpeed = getEffectiveCFrameSpeed()
	local initialVerticalGap = safeTarget and math.abs((safeTarget.Y or rootPart.Position.Y) - rootPart.Position.Y) or 0
	local undergroundVerticalTolerance = math.max(0.85, math.min(4.4, wantedDistance + 0.85 + (initialVerticalGap * 0.04)))
	local travelTimeout = (timeout or MOVE_TIMEOUT) + (useUndergroundOffset and math.min(2.2, initialVerticalGap * 0.05) or 0)
	local undergroundRejectCount = 0
	local undergroundCooldownUntil = 0
	local lastUndergroundGoalY = nil
	local lastUndergroundBaseFlatDistance = nil
	local lastAppliedUndergroundOffset = 0

	while autoMineEnabled and activeToken == autoMineToken do
		rootPart = getRootPart(character)
		if not rootPart then
			return false
		end

		local loopNow = os.clock()
		if useUndergroundOffset then
			local baseFlatDistance = (Vector3.new(targetPosition.X, 0, targetPosition.Z) - Vector3.new(rootPart.Position.X, 0, rootPart.Position.Z)).Magnitude
			if lastUndergroundGoalY and math.abs(lastAppliedUndergroundOffset) > 0.15 then
				local verticalCorrection = rootPart.Position.Y > (lastUndergroundGoalY + 1.6)
				local distanceCorrection = lastUndergroundBaseFlatDistance and baseFlatDistance > (lastUndergroundBaseFlatDistance + math.max(1.1, wantedDistance * 0.45))
				if verticalCorrection or distanceCorrection then
					undergroundRejectCount += 1
				else
					undergroundRejectCount = math.max(0, undergroundRejectCount - 1)
				end
				if undergroundRejectCount >= 2 then
					undergroundCooldownUntil = loopNow + 0.35
					undergroundRejectCount = 0
				end
			end

			local offsetScale = 1
			if loopNow < undergroundCooldownUntil then
				offsetScale = 0
			end
			local liveUndergroundOffset = effectiveUndergroundOffset * offsetScale
			workingTarget = targetPosition - Vector3.new(0, liveUndergroundOffset, 0)
			safeTarget = workingTarget
			lastUndergroundBaseFlatDistance = baseFlatDistance
			lastAppliedUndergroundOffset = liveUndergroundOffset
		elseif useFlatPathFollow then
			local groundedTarget = getGroundAlignedPathPosition(workingTarget, character) or workingTarget
			local clearanceOffset = getFlatPathFollowClearance(rootPart)
			safeTarget = Vector3.new(workingTarget.X, groundedTarget.Y + clearanceOffset, workingTarget.Z)
		end

		local delta = safeTarget - rootPart.Position
		local flatDelta = Vector3.new(delta.X, 0, delta.Z)
		local distance = delta.Magnitude
		local flatDistance = flatDelta.Magnitude
		local verticalDistance = math.abs(delta.Y)
		local reachedTarget = false
		if useUndergroundOffset then
			reachedTarget = distance <= wantedDistance or (flatDistance <= wantedDistance and verticalDistance <= undergroundVerticalTolerance)
		elseif useFlatPathFollow then
			local flatPathVerticalTolerance = math.max(0.6, math.min(1.4, wantedDistance + 0.35))
			reachedTarget = distance <= wantedDistance or (flatDistance <= wantedDistance and verticalDistance <= flatPathVerticalTolerance)
		else
			reachedTarget = flatDistance <= wantedDistance or distance <= wantedDistance
		end
		if reachedTarget then
			setLastMoveCommand(Vector3.zero, "CFrameArrived")
			return true
		end

		local direction = distance > 0.001 and delta.Unit or Vector3.zero
		local flatDirection = flatDistance > 0.001 and flatDelta.Unit or Vector3.new(direction.X, 0, direction.Z)
		if flatDirection.Magnitude < 0.001 then
			flatDirection = Vector3.new(rootPart.CFrame.LookVector.X, 0, rootPart.CFrame.LookVector.Z)
		end
		if flatDirection.Magnitude < 0.001 then
			flatDirection = Vector3.new(0, 0, -1)
		end

		if useFlatPathFollow then
			local avoidanceHandled = processPathPlayerAvoidance(
				character,
				targetPosition,
				activeToken,
				flatDirection,
				getHumanoid(character)
			)
			if avoidanceHandled then
				lastStepAt = os.clock()
				continue
			end
		end

		local stepTime = math.clamp(loopNow - lastStepAt, 1 / 240, 0.08)
		lastStepAt = loopNow
		local stepDistance = math.clamp(cframeTravelSpeed * stepTime, 0.02, 6.5)
		local safeStepTarget = nil
		if useFlatPathFollow then
			local horizontalStep = math.min(stepDistance, flatDistance)
			local nextXZ = rootPart.Position + (flatDirection * horizontalStep)
			if useUndergroundOffset then
				local verticalDelta = safeTarget.Y - rootPart.Position.Y
				local verticalStep = math.max(
					0.85,
					math.min(
						(stepDistance * 1.2) + (math.abs(effectiveUndergroundOffset) * 0.08) + (verticalDistance * 0.12),
						4.5
					)
				)
				local nextY = rootPart.Position.Y + math.clamp(verticalDelta, -verticalStep, verticalStep)
				safeStepTarget = Vector3.new(nextXZ.X, nextY, nextXZ.Z)
			else
				local steppedGroundTarget = getGroundAlignedPathPosition(Vector3.new(nextXZ.X, safeTarget.Y, nextXZ.Z), character)
				local desiredStepY = safeTarget.Y
				if steppedGroundTarget then
					desiredStepY = steppedGroundTarget.Y + getFlatPathFollowClearance(rootPart)
				end
				local verticalDelta = desiredStepY - rootPart.Position.Y
				local verticalStep = math.max(0.3, math.min((horizontalStep * 1.15) + 0.45, 5.25))
				local nextY = rootPart.Position.Y + math.clamp(verticalDelta, -verticalStep, verticalStep)
				safeStepTarget = Vector3.new(nextXZ.X, nextY, nextXZ.Z)
			end
		else
			local stepTarget = rootPart.Position + (direction * math.min(stepDistance, distance))
			safeStepTarget = useUndergroundOffset and stepTarget or getSafeTweenTargetPosition(character, stepTarget)
		end
		setLastMoveCommand(flatDirection, "CFrame")
		if useUndergroundOffset then
			lastUndergroundGoalY = safeStepTarget and safeStepTarget.Y or nil
		end

		local stepStartPosition = rootPart.Position
		local stepStartFlatDistance = flatDistance
		local rotationOnly = rootPart.CFrame - rootPart.Position
		local goalCFrame = CFrame.new(safeStepTarget) * rotationOnly
		local moved = false
		if EXPERIMENTAL_AUTOMINE_CFRAME then
			moved = applyExperimentalCFrameGoal(character, goalCFrame)
		else
			moved = pcall(function()
				rootPart.CFrame = goalCFrame
			end)
		end
		if not moved then
			break
		end

		if (loopNow - startedAt) >= travelTimeout then
			break
		end

		RunService.Heartbeat:Wait()
		local liveRootPart = getRootPart(character)
		if not liveRootPart then
			return false
		end
		local liveFlatPosition = Vector3.new(liveRootPart.Position.X, 0, liveRootPart.Position.Z)
		local safeFlatTarget = Vector3.new(safeTarget.X, 0, safeTarget.Z)
		local stepStartFlatPosition = Vector3.new(stepStartPosition.X, 0, stepStartPosition.Z)
		local expectedStepFlatPosition = Vector3.new(safeStepTarget.X, 0, safeStepTarget.Z)
		local liveFlatDistance = (safeFlatTarget - liveFlatPosition).Magnitude
		local actualStepFlatDelta = liveFlatPosition - stepStartFlatPosition
		local movedOppositeDirection = actualStepFlatDelta.Magnitude > 0.75
			and actualStepFlatDelta.Unit:Dot(flatDirection) < -0.45
		local deviatedFromStep = (liveFlatPosition - expectedStepFlatPosition).Magnitude > RUBBERBAND_STEP_DEVIATION_DISTANCE
		local snappedBackwards = liveFlatDistance > (stepStartFlatDistance + RUBBERBAND_BACKTRACK_DISTANCE)
		if snappedBackwards or (deviatedFromStep and movedOppositeDirection) then
			clearMovementKeys()
			setLastMoveCommand(Vector3.zero, "CFrameRubberBand")
			pushDebugRateLimited(
				"cframe_rubberband_step",
				string.format("cframe rubber band: %.1f -> %.1f", stepStartFlatDistance, liveFlatDistance),
				0.8
			)
			return false, "rubberband"
		end
	end

	clearMovementKeys()
	setLastMoveCommand(Vector3.zero, "CFrameStop")
	local rootPart = getRootPart(character)
	if rootPart and safeTarget then
		local delta = safeTarget - rootPart.Position
		local flatDistance = Vector3.new(delta.X, 0, delta.Z).Magnitude
		local verticalDistance = math.abs(delta.Y)
		local finishSlack = math.max(0.2, wantedDistance * 0.4)
		if useUndergroundOffset then
			return delta.Magnitude <= (wantedDistance + finishSlack) or (flatDistance <= (wantedDistance + finishSlack) and verticalDistance <= (undergroundVerticalTolerance + finishSlack))
		end
		return flatDistance <= (wantedDistance + finishSlack) or delta.Magnitude <= (wantedDistance + finishSlack)
	end
	return false
end

function moveTowardsPosition(character, humanoid, targetPosition, activeToken, timeout)
	if movementMode == "tween" then
		return tweenCharacterTowards(character, targetPosition, activeToken, timeout)
	end
	if movementMode == "cframe" then
		return cframeCharacterTowards(character, targetPosition, activeToken, timeout, nil, {
			useUndergroundOffset = false,
		})
	end

	return walkHumanoidTowards(character, humanoid, targetPosition, activeToken, timeout)
end

function getSegmentedPathTarget(currentPosition, finalTarget, maxDistance)
	local delta = finalTarget - currentPosition
	local distance = delta.Magnitude
	if distance <= (maxDistance or PATH_SEGMENT_DISTANCE) then
		return finalTarget
	end

	local direction = delta.Unit
	return currentPosition + (direction * (maxDistance or PATH_SEGMENT_DISTANCE))
end

function getDirectPathTravelTimeout(character, targetPosition, minimumTime)
	local rootPart = getRootPart(character)
	if not rootPart or not targetPosition then
		return math.max(tonumber(minimumTime) or 0.35, MOVE_TIMEOUT)
	end

	local flatDistance = (Vector3.new(rootPart.Position.X, 0, rootPart.Position.Z) - Vector3.new(targetPosition.X, 0, targetPosition.Z)).Magnitude
	local travelSpeed = movementMode == "cframe" and getEffectiveCFrameSpeed() or math.max(tonumber(tweenMoveSpeed) or TWEEN_MOVE_SPEED, 1)
	return math.max(tonumber(minimumTime) or 0.35, (flatDistance / travelSpeed) + 0.4)
end

function moveTowardsRecordedPathNode(character, humanoid, targetPosition, activeToken)
	if not targetPosition then
		return false
	end

	local rootPart = getRootPart(character)
	if not rootPart or not autoMineEnabled or activeToken ~= autoMineToken then
		return false
	end

	local pathReachDistance = movementMode == "cframe"
		and math.max(0.55, WAYPOINT_REACH_DISTANCE * 0.18)
		or math.max(1.8, WAYPOINT_REACH_DISTANCE * 0.6)
	local flatDelta = Vector3.new(targetPosition.X - rootPart.Position.X, 0, targetPosition.Z - rootPart.Position.Z)
	local flatDistance = flatDelta.Magnitude
	if flatDistance <= pathReachDistance then
		cancelActiveMoveTween()
		clearMovementKeys()
		setLastMoveCommand(Vector3.zero, "PathArrived")
		return true
	end

	if movementMode == "cframe" then
		clearMovementKeys()
		cancelActiveMoveTween()
		local goalDirection = flatDelta.Magnitude > 0.001 and flatDelta.Unit or Vector3.new(rootPart.CFrame.LookVector.X, 0, rootPart.CFrame.LookVector.Z)
		if goalDirection.Magnitude < 0.001 then
			goalDirection = Vector3.new(0, 0, -1)
		end
		setLastMoveCommand(goalDirection, "PathCFrame")
		return cframeCharacterTowards(
			character,
			targetPosition,
			activeToken,
			getDirectPathTravelTimeout(character, targetPosition, 0.45),
			pathReachDistance,
			{
				flatPathFollow = true,
				useUndergroundOffset = false,
			}
		)
	end

	local groundedTarget = getGroundAlignedPathPosition(targetPosition, character) or targetPosition
	local currentGroundPoint = getCharacterGroundPoint(character, rootPart)
	if currentGroundPoint then
		currentGroundPoint = Vector3.new(rootPart.Position.X, currentGroundPoint.Y, rootPart.Position.Z)
	else
		currentGroundPoint = Vector3.new(rootPart.Position.X, groundedTarget.Y, rootPart.Position.Z)
	end

	local segmentGroundTarget = getSegmentedPathTarget(
		currentGroundPoint,
		groundedTarget,
		math.max(pathReachDistance + 0.9, PATH_RECORD_NODE_SPACING * 0.45)
	)
	local goalDirection = flatDelta.Magnitude > 0.001 and flatDelta.Unit or Vector3.new(rootPart.CFrame.LookVector.X, 0, rootPart.CFrame.LookVector.Z)
	if goalDirection.Magnitude < 0.001 then
		goalDirection = Vector3.new(0, 0, -1)
	end

	local pathGroundClearance = math.max(2.05, math.min(getCharacterGroundClearance(character, rootPart) + 0.12, 3.1))
	local goalY = math.max(segmentGroundTarget.Y + pathGroundClearance, rootPart.Position.Y - 0.4)

	local goalPosition = Vector3.new(
		segmentGroundTarget.X,
		goalY,
		segmentGroundTarget.Z
	)

	clearMovementKeys()
	cancelActiveMoveTween()
	setLastMoveCommand(goalDirection, movementMode == "cframe" and "PathCFrame" or "PathTween")

	local moveDistance = (goalPosition - rootPart.Position).Magnitude
	local moveTime = math.clamp(moveDistance / math.max(tweenMoveSpeed, 10), 0.05, 0.18)
	local tween = TweenService:Create(
		rootPart,
		TweenInfo.new(moveTime, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
		{ CFrame = CFrame.lookAt(goalPosition, goalPosition + goalDirection) }
	)
	activeMoveTween = tween

	local connection = nil
	connection = tween.Completed:Connect(function()
		if connection then
			connection:Disconnect()
		end
		if activeMoveTween == tween then
			activeMoveTween = nil
		end
	end)

	tween:Play()
	return true
end

function moveCharacterNearPosition(character, humanoid, targetPosition, arriveRange, activeToken)
	if not targetPosition then
		return false
	end

	local wantedRange = arriveRange or WAYPOINT_REACH_DISTANCE
	if movementMode == "cframe" then
		return cframeCharacterTowards(
			character,
			targetPosition,
			activeToken,
			getDirectPathTravelTimeout(character, targetPosition, 0.5),
			wantedRange
		)
	end

	local function getPathArrivalDistance()
		local rootPart = getRootPart(character)
		if not rootPart then
			return math.huge
		end
		return (Vector3.new(rootPart.Position.X, 0, rootPart.Position.Z) - Vector3.new(targetPosition.X, 0, targetPosition.Z)).Magnitude
	end

	if getPathArrivalDistance() <= wantedRange then
		return true
	end

	local rootPart = getRootPart(character)
	if not rootPart then
		return false
	end

	local attempts = 0
	while autoMineEnabled and activeToken == autoMineToken and attempts < PATH_REPLAN_ATTEMPTS do
		attempts += 1

		if getPathArrivalDistance() <= wantedRange then
			return true
		end

		rootPart = getRootPart(character)
		if not rootPart then
			return false
		end

		local pathTarget = getSegmentedPathTarget(rootPart.Position, targetPosition, PATH_SEGMENT_DISTANCE)
		local usingSegmentTarget = (pathTarget - targetPosition).Magnitude > 1
		local path = PathfindingService:CreatePath({
			AgentCanJump = true,
			WaypointSpacing = PATH_WAYPOINT_SPACING,
		})

		local computed = pcall(function()
			path:ComputeAsync(rootPart.Position, pathTarget)
		end)

		if computed and path.Status == Enum.PathStatus.Success then
			local waypoints = path:GetWaypoints()
			local points = {}
			for _, waypoint in ipairs(waypoints) do
				table.insert(points, waypoint.Position)
			end
			visualizePathPoints(points, targetPosition)

			local progressed = false
			for _, waypoint in ipairs(waypoints) do
				if not autoMineEnabled or activeToken ~= autoMineToken then
					return false
				end

				if getPathArrivalDistance() <= wantedRange then
					return true
				end

				if waypoint.Action == Enum.PathWaypointAction.Jump then
					humanoid.Jump = true
				end

				local reached = moveTowardsPosition(character, humanoid, waypoint.Position, activeToken, MOVE_TIMEOUT)
				if reached then
					progressed = true
				else
					break
				end
			end

			if progressed and usingSegmentTarget then
				pushDebugRateLimited("path_segment_progress", "pathing seller in segments", 2)
			elseif progressed then
				task.wait(0.05)
			else
				pushDebugRateLimited("path_segment_stall", "path segment stalled", 1.5)
			end
		else
			visualizePathPoints({ rootPart.Position, pathTarget }, targetPosition)

			local fallbackTarget = pathTarget
			if movementMode == "tween" then
				pushDebugRateLimited("tween_path_fallback", "tween fallback -> walk segment", 1.5)
				if not walkHumanoidTowards(character, humanoid, fallbackTarget, activeToken, MOVE_TIMEOUT) then
					break
				end
			else
				if not moveTowardsPosition(character, humanoid, fallbackTarget, activeToken, MOVE_TIMEOUT) then
					break
				end
			end
		end
	end

	return getPathArrivalDistance() <= wantedRange
end

function moveCharacterNearOre(character, humanoid, oreDeposit, activeToken, arriveRange)
	local orePosition = getOrePosition(oreDeposit)
	if not orePosition then
		pushDebug("ore missing position")
		return false
	end

	local desiredRange = arriveRange or MINE_RANGE

	if getDistanceToPosition(character, orePosition) <= desiredRange then
		return true
	end

	local rootPart = getRootPart(character)
	if not rootPart then
		pushDebug("root part not found")
		return false
	end

	setStatus("Walking to " .. getOreDisplayName(oreDeposit), Color3.fromRGB(120, 190, 255))

	local movedNear = moveCharacterNearPosition(character, humanoid, orePosition, desiredRange, activeToken)
	if movedNear and getDistanceToPosition(character, orePosition) <= desiredRange then
		return true
	end

	pushDebug(safeLower(getMovementModeLabel()) .. " move failed")
	return false
end

function tryAutoPickup(character, humanoid, activeToken, options)
	options = type(options) == "table" and options or nil
	if not autoPickupEnabled then
		return false
	end

	local pickupRange = math.max(0.5, tonumber(options and options.arriveRange) or PICKUP_RANGE)
	local pickupReachSlack = math.max(0.55, pickupRange * 0.18)
	local maxPickupDistance = tonumber(options and options.maxDistance) or AUTO_PICKUP_MAX_DISTANCE
	if maxPickupDistance < pickupRange then
		maxPickupDistance = pickupRange
	end
	local requireAlreadyInReach = options and options.requireAlreadyInReach == true
	local dropItem, distance = findNearestDroppedItem(character, maxPickupDistance)
	if not dropItem then
		return false
	end
	if requireAlreadyInReach and (not distance or distance > (pickupRange + pickupReachSlack)) then
		return false
	end

	local dropPart = getDroppedItemBasePart(dropItem)
	if not dropPart then
		return false
	end
	endExperimentalCFrameTravel(character)
	local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
	local actionGroupInstance, networkId = resolveDroppedPickupActionTarget(dropItem, dropPart)

	local dropName = getDropItemTypeName(dropItem)
	local dropLabel = getPickupLabel(dropName)
	pushDebug("pickup target: " .. dropLabel .. " (" .. tostring(math.floor((distance or 0) + 0.5)) .. ")")
	setStatus("Picking up " .. dropLabel, Color3.fromRGB(165, 220, 140))

	local reached = true
	if requireAlreadyInReach then
		reached = getDistanceToPosition(character, dropPart.Position) <= (pickupRange + pickupReachSlack)
	else
		reached = moveCharacterNearPosition(character, humanoid, dropPart.Position, pickupRange, activeToken)
	end
	if not reached then
		pushDebug("pickup move failed")
		return false
	end

	if tryPickupDroppedItemViaNetwork(network, dropItem, dropPart, actionGroupInstance, networkId) then
		pushDebug("picked instantly: " .. dropLabel)
		return true
	end

	if tryPickupDroppedItemViaObjectAction(network, dropItem, dropPart, actionGroupInstance, networkId) then
		pushDebug("picked via object action: " .. dropLabel)
		return true
	end

	facePosition(character, dropPart.Position)
	clearMovementKeys()

	local startedAt = os.clock()
	setInteractKeyHeld(true)
	while autoMineEnabled and activeToken == autoMineToken and dropItem.Parent and (os.clock() - startedAt) < PICKUP_HOLD_SECONDS do
		task.wait(0.05)
	end
	setInteractKeyHeld(false)

	if dropItem.Parent then
		pushDebug("pickup timeout: " .. dropLabel)
	else
		pushDebug("picked: " .. dropLabel)
	end

	return true
end

function getSellTargetBasePart(instance)
	if typeof(instance) ~= "Instance" then
		return nil
	end

	if instance:IsA("Model") then
		return instance.PrimaryPart
			or instance:FindFirstChild("HumanoidRootPart")
			or instance:FindFirstChildWhichIsA("BasePart", true)
	end

	if instance:IsA("Attachment") and instance.Parent and instance.Parent:IsA("BasePart") then
		return instance.Parent
	end

	if instance:IsA("BillboardGui") or instance:IsA("SurfaceGui") then
		local adornee = instance.Adornee
		if adornee and adornee:IsA("BasePart") then
			return adornee
		end

		local parent = instance.Parent
		if parent and parent:IsA("BasePart") then
			return parent
		end
	end

	if instance:IsA("ProximityPrompt") or instance:IsA("ClickDetector") then
		local parent = instance.Parent
		if parent and parent:IsA("BasePart") then
			return parent
		end
	end

	if instance:IsA("BasePart") then
		return instance
	end

	return instance:FindFirstChildWhichIsA("BasePart", true)
end

function textMatchesAutoSellHints(text)
	if type(text) ~= "string" then
		return false
	end

	if AUTO_SELL_SHOP_TYPES[text] then
		return true
	end

	local loweredText = safeLower(text)
	for shopType in pairs(AUTO_SELL_SHOP_TYPES) do
		if string.find(loweredText, safeLower(shopType), 1, true) then
			return true
		end
	end

	for _, hint in ipairs(AUTO_SELL_SHOP_NAME_HINTS) do
		if string.find(loweredText, hint, 1, true) then
			return true
		end
	end

	return false
end

function getAutoSellMatchScore(instance)
	if typeof(instance) ~= "Instance" then
		return 100
	end

	local bestScore = 100
	local function considerText(text)
		if type(text) ~= "string" or text == "" then
			return
		end

		local lowered = safeLower(text)
		if string.find(lowered, "sell loot", 1, true) then
			bestScore = math.min(bestScore, 0)
		elseif AUTO_SELL_SHOP_TYPES[text] then
			bestScore = math.min(bestScore, 0)
		elseif textMatchesAutoSellHints(text) then
			bestScore = math.min(bestScore, 3)
		end
	end

	considerText(instance.Name)
	for _, attributeName in ipairs(AUTO_SELL_SHOP_ATTRIBUTE_NAMES) do
		considerText(instance:GetAttribute(attributeName))
	end
	for attributeName, attributeValue in pairs(instance:GetAttributes()) do
		considerText(attributeName)
		considerText(attributeValue)
	end

	if instance:IsA("ProximityPrompt") then
		considerText(instance.ActionText)
		considerText(instance.ObjectText)
		if safeLower(instance.ActionText) == "sell loot" then
			bestScore = math.min(bestScore, 0)
		end
	end

	for _, child in ipairs(instance:GetChildren()) do
		considerText(child.Name)
		if child:IsA("StringValue") or child:IsA("IntValue") then
			considerText(tostring(child.Value))
		end
	end

	return bestScore
end

function isSellPromptInstance(instance)
	return getAutoSellMatchScore(instance) <= 1
end

function instanceMatchesAutoSellShop(instance)
	if typeof(instance) ~= "Instance" then
		return false
	end

	local directName = instance.Name
	if textMatchesAutoSellHints(directName) then
		return true
	end

	for _, attributeName in ipairs(AUTO_SELL_SHOP_ATTRIBUTE_NAMES) do
		local attributeValue = instance:GetAttribute(attributeName)
		if textMatchesAutoSellHints(attributeValue) then
			return true
		end
	end

	for attributeName, attributeValue in pairs(instance:GetAttributes()) do
		if textMatchesAutoSellHints(attributeName) or textMatchesAutoSellHints(attributeValue) then
			return true
		end
	end

	if instance:IsA("ProximityPrompt") then
		if textMatchesAutoSellHints(instance.ActionText) or textMatchesAutoSellHints(instance.ObjectText) then
			return true
		end
	end

	for _, child in ipairs(instance:GetChildren()) do
		if textMatchesAutoSellHints(child.Name) then
			return true
		end

		if child:IsA("StringValue") or child:IsA("IntValue") then
			if textMatchesAutoSellHints(tostring(child.Value)) then
				return true
			end
		end
	end

	return false
end

function getAutoSellTargetCandidates(forceRefresh)
	local now = os.clock()
	if cachedAutoSellTargets and not forceRefresh then
		if #cachedAutoSellTargets > 0 then
			return cachedAutoSellTargets
		end

		if (now - lastAutoSellTargetScanAt) < 3 then
			return cachedAutoSellTargets
		end
	end

	lastAutoSellTargetScanAt = now
	local candidates = {}
	local seenParts = {}

	local function tryAddTarget(instance, basePart)
		if not basePart or not basePart.Parent or seenParts[basePart] then
			return
		end

		seenParts[basePart] = true
		table.insert(candidates, {
			part = basePart,
			instance = instance or basePart,
			score = getAutoSellMatchScore(instance or basePart),
		})
	end

	local configuredPart, configuredGui = getConfiguredGeneralShopTarget()
	tryAddTarget(configuredGui or configuredPart, configuredPart)

	local searchRoots = {
		workspace:FindFirstChild("WORKSPACE_Geometry"),
		getInteractablesFolder(),
		workspace:FindFirstChild("WORKSPACE_Entities"),
	}

	for _, searchRoot in ipairs(searchRoots) do
		if searchRoot then
			for _, descendant in ipairs(searchRoot:GetDescendants()) do
				if (descendant:IsA("BillboardGui") or descendant:IsA("SurfaceGui") or descendant:IsA("ProximityPrompt"))
					and instanceMatchesAutoSellShop(descendant)
				then
					tryAddTarget(descendant, getSellTargetBasePart(descendant))
				end
			end
		end
	end

	cachedAutoSellTargets = candidates
	return cachedAutoSellTargets
end

function getNodeDistanceToSellTarget(nodePosition, targetPart)
	if not nodePosition or not targetPart or not targetPart:IsA("BasePart") then
		return math.huge
	end

	local localPosition = targetPart.CFrame:PointToObjectSpace(nodePosition)
	local halfSize = targetPart.Size * 0.5
	return Vector3.new(
		math.max(math.abs(localPosition.X) - halfSize.X, 0),
		0,
		math.max(math.abs(localPosition.Z) - halfSize.Z, 0)
	).Magnitude
end

function getBestSellTargetForPath(pathData, preferredNodeIndex, forceRefresh)
	if type(pathData) ~= "table" then
		return nil, nil, nil, nil
	end

	local nodes = pathData.nodes or {}
	if #nodes == 0 then
		return nil, nil, nil, nil
	end

	local normalizedPreferredNodeIndex = normalizePathNodeIndex(preferredNodeIndex, #nodes)
	local candidates = getAutoSellTargetCandidates(forceRefresh)
	local bestPart = nil
	local bestInstance = nil
	local bestNodeIndex = nil
	local bestDistance = nil
	local bestScore = nil
	local bestMetric = nil

	for _, candidate in ipairs(candidates or {}) do
		local candidateNodeIndexes = {}
		if normalizedPreferredNodeIndex then
			candidateNodeIndexes[1] = normalizedPreferredNodeIndex
		else
			for index = 1, #nodes do
				candidateNodeIndexes[index] = index
			end
		end

		for _, nodeIndex in ipairs(candidateNodeIndexes) do
			local node = nodes[nodeIndex]
			local nodePosition = getPathNodePosition(node)
			local distance = nodePosition and getNodeDistanceToSellTarget(nodePosition, candidate.part) or math.huge
			if distance < math.huge then
				local metric = distance + ((candidate.score or 0) * 12)
				if not bestMetric
					or metric < bestMetric
					or (metric == bestMetric and distance < (bestDistance or math.huge))
				then
					bestMetric = metric
					bestDistance = distance
					bestScore = candidate.score
					bestNodeIndex = nodeIndex
					bestPart = candidate.part
					bestInstance = candidate.instance
				end
			end
		end
	end

	return bestPart, bestInstance, bestNodeIndex, bestDistance
end

function getPathSellTarget(pathData, forceRefresh)
	local manualSellNodeIndex = getManualPathSellNodeIndex(pathData)
	local sellerPart, sellerInstance, autoNodeIndex, autoDistance = getBestSellTargetForPath(pathData, manualSellNodeIndex, forceRefresh)
	if not sellerPart and not forceRefresh then
		sellerPart, sellerInstance, autoNodeIndex, autoDistance = getBestSellTargetForPath(pathData, manualSellNodeIndex, true)
	end

	if manualSellNodeIndex then
		return sellerPart, sellerInstance, manualSellNodeIndex, autoDistance
	end

	if autoNodeIndex and autoDistance and autoDistance <= PATH_RECORD_SELL_DISTANCE then
		return sellerPart, sellerInstance, autoNodeIndex, autoDistance
	end

	return sellerPart, sellerInstance, nil, autoDistance
end

function findNearestAutoSellTarget(character, forceRefresh)
	local rootPart = getRootPart(character)
	if not rootPart then
		return nil, nil, nil
	end

	local bestPart = nil
	local bestDistance = nil
	local bestInstance = nil
	local bestScore = nil
	local bestMetric = nil

	for _, candidate in ipairs(getAutoSellTargetCandidates(forceRefresh) or {}) do
		local distance = (rootPart.Position - candidate.part.Position).Magnitude
		local metric = distance + ((candidate.score or 0) * 12)
		if not bestMetric
			or metric < bestMetric
			or (metric == bestMetric and distance < (bestDistance or math.huge))
		then
			bestMetric = metric
			bestScore = candidate.score
			bestDistance = distance
			bestPart = candidate.part
			bestInstance = candidate.instance
		end
	end

	if not bestPart and not forceRefresh then
		return findNearestAutoSellTarget(character, true)
	end

	return bestPart, bestInstance, bestDistance
end

function pushNearbySellPromptHints(character)
	local rootPart = getRootPart(character)
	if not rootPart then
		return
	end

	local nearbyHints = {}
	local searchRoots = {
		getInteractablesFolder(),
		workspace:FindFirstChild("WORKSPACE_Entities"),
		workspace:FindFirstChild("WORKSPACE_Map"),
		workspace:FindFirstChild("Map"),
	}

	for _, searchRoot in ipairs(searchRoots) do
		if searchRoot then
			for _, descendant in ipairs(searchRoot:GetDescendants()) do
				if descendant:IsA("ProximityPrompt") then
					local actionText = tostring(descendant.ActionText or "")
					local objectText = tostring(descendant.ObjectText or "")
					if actionText ~= "" or objectText ~= "" then
						local basePart = getSellTargetBasePart(descendant)
						if basePart then
							local distance = (rootPart.Position - basePart.Position).Magnitude
							if distance <= 90 then
								table.insert(nearbyHints, {
									distance = distance,
									text = string.format("%s / %s", actionText, objectText),
									name = descendant:GetFullName(),
								})
							end
						end
					end
				end
			end
		end
	end

	table.sort(nearbyHints, function(left, right)
		return left.distance < right.distance
	end)

	for index = 1, math.min(#nearbyHints, 3) do
		local hint = nearbyHints[index]
		pushDebugRateLimited(
			"sell_prompt_hint_" .. tostring(index),
			string.format("sell prompt nearby: %d studs | %s | %s", math.floor(hint.distance + 0.5), hint.text, hint.name),
			6
		)
	end
end

function findSellPromptForTarget(targetInstance, targetPart)
	local candidates = {}
	local seen = {}

	local function tryAdd(instance)
		if typeof(instance) ~= "Instance" or seen[instance] then
			return
		end
		seen[instance] = true
		table.insert(candidates, instance)
	end

	local instanceCursor = targetInstance
	for _ = 1, 4 do
		if not instanceCursor then
			break
		end
		tryAdd(instanceCursor)
		instanceCursor = instanceCursor.Parent
	end

	if targetInstance then
		for _, descendant in ipairs(targetInstance:GetDescendants()) do
			tryAdd(descendant)
		end
	end

	if targetPart then
		tryAdd(targetPart)
		for _, descendant in ipairs(targetPart:GetDescendants()) do
			tryAdd(descendant)
		end
	end

	local bestPrompt = nil
	local bestScore = nil
	for _, candidate in ipairs(candidates) do
		if candidate:IsA("ProximityPrompt") then
			local score = getAutoSellMatchScore(candidate)
			if not bestScore or score < bestScore then
				bestScore = score
				bestPrompt = candidate
			end
		end
	end

	return bestPrompt
end

function getSellInteractKey(targetInstance, targetPart)
	local prompt = findSellPromptForTarget(targetInstance, targetPart)
	if prompt then
		local keyCode = prompt.KeyboardKeyCode
		if keyCode and keyCode ~= Enum.KeyCode.Unknown then
			pushDebugRateLimited("sell_prompt_key", "sell prompt key: " .. formatKeyCodeName(keyCode), 4)
			return keyCode, prompt
		end
	end

	return INTERACT_SECONDARY_KEY, prompt
end

function waitForSellInventoryDrop(filledBefore, timeoutSeconds, lootSummaryBefore)
	local timeoutAt = os.clock() + math.max(0.1, tonumber(timeoutSeconds) or 0.8)
	local latestFilled = filledBefore
	local latestLootSummary = nil

	while os.clock() < timeoutAt do
		latestLootSummary = getSellableLootSummary(false)
		if wasSellableLootReduced(lootSummaryBefore, latestLootSummary) then
			local summaryFilledSlots = tonumber(rawget(latestLootSummary, "filledSlots") or latestLootSummary.filledSlots)
			if summaryFilledSlots ~= nil then
				latestFilled = summaryFilledSlots
			else
				latestFilled = select(1, getInventorySlotCounts())
			end
			return true, latestFilled, latestLootSummary
		end

		latestFilled = select(1, getInventorySlotCounts())
		if latestFilled < filledBefore then
			return true, latestFilled, latestLootSummary
		end
		task.wait(0.05)
	end

	latestLootSummary = getSellableLootSummary(false)
	if wasSellableLootReduced(lootSummaryBefore, latestLootSummary) then
		local summaryFilledSlots = tonumber(rawget(latestLootSummary, "filledSlots") or latestLootSummary.filledSlots)
		if summaryFilledSlots ~= nil then
			latestFilled = summaryFilledSlots
		else
			latestFilled = select(1, getInventorySlotCounts())
		end
		return true, latestFilled, latestLootSummary
	end

	latestFilled = select(1, getInventorySlotCounts())
	return latestFilled < filledBefore, latestFilled, latestLootSummary
end

function tryFireSellPromptDirectly(prompt)
	if not (prompt and prompt:IsA("ProximityPrompt")) then
		return false
	end

	if type(fireproximityprompt) == "function" then
		for _, args in ipairs({
			{},
			{ 0 },
			{ 0, true },
			{ tonumber(prompt.HoldDuration) or 0, true },
		}) do
			local ok = pcall(function()
				fireproximityprompt(prompt, unpack(args))
			end)
			if ok then
				return true
			end
		end
	end

	local okBegin = pcall(function()
		prompt:InputHoldBegin()
	end)
	if okBegin then
		task.wait(math.max(0.05, tonumber(prompt.HoldDuration) or 0))
		pcall(function()
			prompt:InputHoldEnd()
		end)
		return true
	end

	return false
end

function getNextAutoSellObjectActionActivationId()
	autoSellObjectActionActivationId = (tonumber(autoSellObjectActionActivationId) or 0) + 1
	if autoSellObjectActionActivationId > 2000000000 then
		autoSellObjectActionActivationId = 1
	end
	return autoSellObjectActionActivationId
end

function getAutoSellObjectActionNetworkId(instance)
	if typeof(instance) ~= "Instance" then
		return nil
	end

	local ok, networkId = pcall(function()
		return instance:GetAttribute("NetworkId")
	end)
	if ok and networkId ~= nil then
		return networkId
	end

	return nil
end

function waitForAutoSellObjectActionNetworkId(instance, timeoutSeconds)
	if typeof(instance) ~= "Instance" then
		return nil
	end

	local networkId = getAutoSellObjectActionNetworkId(instance)
	if networkId ~= nil then
		return networkId
	end

	local timeoutAt = os.clock() + math.max(0.05, tonumber(timeoutSeconds) or AUTO_SELL_OBJECT_ACTION_NETWORK_ID_WAIT)
	while os.clock() < timeoutAt do
		task.wait(0.03)
		networkId = getAutoSellObjectActionNetworkId(instance)
		if networkId ~= nil then
			return networkId
		end
	end

	return getAutoSellObjectActionNetworkId(instance)
end

function findAutoSellActionGroupAncestor(instance)
	local current = typeof(instance) == "Instance" and instance or nil
	local depth = 0

	while current and depth < 8 do
		if AUTO_SELL_SHOP_TYPES[current.Name] then
			return current
		end
		current = current.Parent
		depth = depth + 1
	end

	return nil
end

function resolveAutoSellActionGroupTarget(targetInstance, targetPart)
	local roots = {}
	local seen = {}

	local function pushRoot(instance)
		if typeof(instance) == "Instance" and not seen[instance] then
			seen[instance] = true
			table.insert(roots, instance)
		end
	end

	pushRoot(targetInstance)
	pushRoot(targetPart)

	local current = typeof(targetInstance) == "Instance" and targetInstance or nil
	for _ = 1, 2 do
		current = current and current.Parent or nil
		pushRoot(current)
	end

	current = typeof(targetPart) == "Instance" and targetPart or nil
	for _ = 1, 2 do
		current = current and current.Parent or nil
		pushRoot(current)
	end

	local function tryResolveInstance(instance)
		local actionGroupInstance = findAutoSellActionGroupAncestor(instance)
		if not actionGroupInstance then
			return nil, nil, nil
		end

		local networkId = getAutoSellObjectActionNetworkId(actionGroupInstance)
		if networkId == nil then
			networkId = waitForAutoSellObjectActionNetworkId(actionGroupInstance, AUTO_SELL_OBJECT_ACTION_NETWORK_ID_WAIT)
		end

		if networkId ~= nil then
			return actionGroupInstance, networkId, actionGroupInstance.Name
		end

		return nil, nil, nil
	end

	for _, instance in ipairs(roots) do
		local actionGroupInstance, networkId, actionGroupType = tryResolveInstance(instance)
		if actionGroupInstance then
			return actionGroupInstance, networkId, actionGroupType
		end
	end

	for _, root in ipairs(roots) do
		local okDescendants, descendants = pcall(function()
			return root:GetDescendants()
		end)
		if okDescendants and type(descendants) == "table" then
			for _, descendant in ipairs(descendants) do
				if AUTO_SELL_SHOP_TYPES[descendant.Name] then
					local networkId = getAutoSellObjectActionNetworkId(descendant)
					if networkId == nil then
						networkId = waitForAutoSellObjectActionNetworkId(descendant, AUTO_SELL_OBJECT_ACTION_NETWORK_ID_WAIT)
					end

					if networkId ~= nil then
						return descendant, networkId, descendant.Name
					end
				end
			end
		end
	end

	return nil, nil, nil
end

function trySellLootViaObjectAction(network, targetPart, targetInstance, confirmSellSuccess)
	if not network or type(network.FireServer) ~= "function" then
		return false
	end

	local actionGroupInstance, networkId, actionGroupType = resolveAutoSellActionGroupTarget(targetInstance, targetPart)
	if not actionGroupInstance or networkId == nil then
		pushDebugRateLimited("sell_object_action_missing", "sell object action group not resolved", 2)
		return false
	end

	pushDebugRateLimited(
		"sell_object_action_target",
		string.format("sell object action: %s | %s", tostring(actionGroupType), tostring(actionGroupInstance:GetFullName())),
		4
	)

	local activationId = getNextAutoSellObjectActionActivationId()
	local okStart = pcall(network.FireServer, network, "ObjectActionStarted", networkId, "Secondary", getSyncedNetworkTime(), activationId)
	if not okStart then
		return false
	end

	task.wait(AUTO_SELL_OBJECT_ACTION_DURATION)

	local finishTimestamp = getSyncedNetworkTime()
	local okFinish = pcall(network.FireServer, network, "ObjectActionFinished", activationId, finishTimestamp)
	if not okFinish then
		pcall(network.FireServer, network, "ObjectActionCancelled", activationId, finishTimestamp)
		return false
	end

	return confirmSellSuccess(AUTO_SELL_OBJECT_ACTION_CONFIRM_TIMEOUT)
end

function trySellLootNearTarget(targetPart, targetInstance, character)
	endExperimentalCFrameTravel(character or cachedCharacter)
	local filledBefore, totalBefore = getInventorySlotCounts()
	local lootSummaryBefore = getSellableLootSummary(false) or getSellableLootSummary(true)
	lastInventoryFilledSlots = filledBefore
	lastInventoryTotalSlots = totalBefore
	local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
	local promptKey = nil
	local matchedPrompt = nil

	local function confirmSellSuccess(timeoutSeconds)
		local sold, filledAfter, lootSummaryAfter = waitForSellInventoryDrop(filledBefore, timeoutSeconds, lootSummaryBefore)
		lastInventoryFilledSlots = filledAfter
		if isTable(lootSummaryAfter) then
			local summaryTotalSlots = tonumber(rawget(lootSummaryAfter, "totalSlots") or lootSummaryAfter.totalSlots)
			if summaryTotalSlots ~= nil then
				lastInventoryTotalSlots = summaryTotalSlots
			end
		end
		if sold then
			local soldLootValue = getSoldLootValue(lootSummaryBefore, lootSummaryAfter)
			local soldMessage = soldLootValue > 0
				and ("Sold $" .. formatMoneyAmount(soldLootValue) .. " worth of ores.")
				or "Sold ores."
			if type(spawnNotification) == "function" then
				spawnNotification(soldMessage, "success", true, 3)
			else
				notifyUser("Success", soldMessage, 3)
			end
			pcall(sendDiscordAutomineSellWebhook, soldLootValue)
			return true
		end
		return false
	end

	if trySellLootViaObjectAction(network, targetPart, targetInstance, confirmSellSuccess) then
		return true
	end

	if network and type(network.FireServer) == "function" then
		pcall(network.FireServer, network, "SellAllLoot")
		pcall(network.FireServer, network, "SellAllLoot", targetInstance)
		pcall(network.FireServer, network, "SellAllLoot", targetPart)
	end

	if confirmSellSuccess(0.65) then
		return true
	end

	promptKey, matchedPrompt = getSellInteractKey(targetInstance, targetPart)
	if matchedPrompt and matchedPrompt:IsA("ProximityPrompt") then
		pushDebugRateLimited(
			"sell_prompt_match",
			string.format("sell prompt matched: %s / %s", tostring(matchedPrompt.ActionText or ""), tostring(matchedPrompt.ObjectText or "")),
			4
		)

		if tryFireSellPromptDirectly(matchedPrompt) and confirmSellSuccess(math.max(0.7, (tonumber(matchedPrompt.HoldDuration) or 0) + 0.35)) then
			return true
		end
	end

	local function tryPromptKey(keyCode)
		if not keyCode then
			return false
		end

		local holdSeconds = SELL_HOLD_SECONDS
		if matchedPrompt and matchedPrompt:IsA("ProximityPrompt") then
			holdSeconds = math.max(SELL_HOLD_SECONDS, tonumber(matchedPrompt.HoldDuration) or 0)
		end

		if keyCode == autoFarmKeybind then
			suppressAutoFarmHotkey = true
			ignoreToggleUntil = os.clock() + 0.5
		end

		local pressed = sendKeyState(keyCode, true)
		task.wait(holdSeconds)
		sendKeyState(keyCode, false)
		task.wait(0.35)

		if keyCode == autoFarmKeybind then
			suppressAutoFarmHotkey = false
		end

		if not pressed then
			return false
		end

		return confirmSellSuccess(0.5)
	end

	local activeCharacter = character or cachedCharacter
	if activeCharacter and targetPart then
		facePosition(activeCharacter, targetPart.Position)
	end

	if tryPromptKey(promptKey) then
		return true
	end

	if promptKey ~= INTERACT_KEY and tryPromptKey(INTERACT_KEY) then
		return true
	end

	if promptKey ~= Enum.KeyCode.F and INTERACT_KEY ~= Enum.KeyCode.F and tryPromptKey(Enum.KeyCode.F) then
		return true
	end

	return false
end

function getDroppedItemPickupActionName(instance)
	if typeof(instance) ~= "Instance" then
		return nil
	end

	if instance.Name == "PickupItem" then
		return "PickupItem"
	end

	for _, attributeName in ipairs(AUTO_PICKUP_ACTION_ATTRIBUTE_NAMES) do
		local okAttribute, attributeValue = pcall(function()
			return instance:GetAttribute(attributeName)
		end)
		if okAttribute and type(attributeValue) == "string" and attributeValue == "PickupItem" then
			return "PickupItem"
		end

		local valueObject = instance:FindFirstChild(attributeName)
		if valueObject and valueObject:IsA("StringValue") and valueObject.Value == "PickupItem" then
			return "PickupItem"
		end
	end

	return nil
end

function resolveDroppedPickupActionTarget(dropItem, dropPart)
	local roots = {}
	local seen = {}

	local function pushRoot(instance)
		if typeof(instance) == "Instance" and not seen[instance] then
			seen[instance] = true
			table.insert(roots, instance)
		end
	end

	local pickupItem = typeof(dropItem) == "Instance" and dropItem:FindFirstChild("PickupItem", true) or nil
	pushRoot(dropItem)
	pushRoot(dropPart)
	pushRoot(pickupItem)
	pushRoot(pickupItem and pickupItem.Parent or nil)

	local current = typeof(dropItem) == "Instance" and dropItem or nil
	for _ = 1, 3 do
		current = current and current.Parent or nil
		pushRoot(current)
	end

	current = typeof(dropPart) == "Instance" and dropPart or nil
	for _ = 1, 2 do
		current = current and current.Parent or nil
		pushRoot(current)
	end

	local function tryResolveInstance(instance)
		local currentInstance = typeof(instance) == "Instance" and instance or nil
		local depth = 0
		while currentInstance and depth < 8 do
			if getDroppedItemPickupActionName(currentInstance) == "PickupItem" then
				local networkId = getAutoSellObjectActionNetworkId(currentInstance)
				if networkId == nil then
					networkId = waitForAutoSellObjectActionNetworkId(currentInstance, AUTO_SELL_OBJECT_ACTION_NETWORK_ID_WAIT)
				end
				if networkId ~= nil then
					return currentInstance, networkId
				end
			end
			currentInstance = currentInstance.Parent
			depth += 1
		end
		return nil, nil
	end

	for _, instance in ipairs(roots) do
		local actionGroupInstance, networkId = tryResolveInstance(instance)
		if actionGroupInstance and networkId ~= nil then
			return actionGroupInstance, networkId
		end
	end

	for _, root in ipairs(roots) do
		local okDescendants, descendants = pcall(function()
			return root:GetDescendants()
		end)
		if okDescendants and type(descendants) == "table" then
			for _, descendant in ipairs(descendants) do
				if getDroppedItemPickupActionName(descendant) == "PickupItem" then
					local networkId = getAutoSellObjectActionNetworkId(descendant)
					if networkId == nil then
						networkId = waitForAutoSellObjectActionNetworkId(descendant, AUTO_SELL_OBJECT_ACTION_NETWORK_ID_WAIT)
					end
					if networkId ~= nil then
						return descendant, networkId
					end
				end
			end
		end
	end

	return nil, nil
end

function waitForDroppedItemCollected(dropItem, timeoutSeconds)
	local timeoutAt = os.clock() + math.max(0.05, tonumber(timeoutSeconds) or AUTO_PICKUP_OBJECT_ACTION_CONFIRM_TIMEOUT)
	while os.clock() < timeoutAt do
		if typeof(dropItem) ~= "Instance" or not dropItem.Parent or isDroppedItemDisabled(dropItem) then
			return true
		end
		task.wait(0.03)
	end

	return typeof(dropItem) ~= "Instance" or not dropItem.Parent or isDroppedItemDisabled(dropItem)
end

function playLocalPickupItemFeedback()
	local playerCharacter = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
	if not isTable(playerCharacter) then
		return
	end

	local pickupItemFn = rawget(playerCharacter, "PickupItem") or playerCharacter.PickupItem
	if type(pickupItemFn) == "function" then
		pcall(pickupItemFn, playerCharacter)
	end
end

function tryPickupDroppedItemViaNetwork(network, dropItem, dropPart, actionGroupInstance, networkId)
	if not network or type(network.FireServer) ~= "function" then
		return false
	end

	local pickupItem = typeof(dropItem) == "Instance" and dropItem:FindFirstChild("PickupItem", true) or nil
	local pickupItemId = nil
	if pickupItem then
		local okAttribute, attributeValue = pcall(function()
			return pickupItem:GetAttribute("ItemId")
		end)
		if okAttribute and attributeValue ~= nil then
			pickupItemId = attributeValue
		end
		if pickupItemId == nil then
			local valueObject = pickupItem:FindFirstChild("ItemId")
			if valueObject and valueObject:IsA("StringValue") then
				pickupItemId = valueObject.Value
			end
		end
	end

	for _, payload in ipairs({
		{},
		{ dropItem },
		{ dropPart },
		{ actionGroupInstance },
		{ networkId },
		pickupItemId ~= nil and { pickupItemId } or nil,
	}) do
		if payload then
			pcall(function()
				network:FireServer("PickupItem", unpack(payload))
			end)
			if waitForDroppedItemCollected(dropItem, AUTO_PICKUP_INSTANT_CONFIRM_TIMEOUT) then
				playLocalPickupItemFeedback()
				return true
			end
		end
	end

	return false
end

function tryPickupDroppedItemViaObjectAction(network, dropItem, dropPart, actionGroupInstance, networkId)
	if not network or type(network.FireServer) ~= "function" then
		return false
	end

	if not actionGroupInstance or networkId == nil then
		actionGroupInstance, networkId = resolveDroppedPickupActionTarget(dropItem, dropPart)
	end
	if not actionGroupInstance or networkId == nil then
		pushDebugRateLimited("pickup_object_action_missing", "pickup object action group not resolved", 2)
		return false
	end

	pushDebugRateLimited(
		"pickup_object_action_target",
		string.format("pickup object action: %s | %s", tostring(actionGroupInstance.Name), tostring(actionGroupInstance:GetFullName())),
		4
	)

	local activationId = getNextAutoSellObjectActionActivationId()
	local okStart = pcall(network.FireServer, network, "ObjectActionStarted", networkId, "Primary", getSyncedNetworkTime(), activationId)
	if not okStart then
		return false
	end

	task.wait(AUTO_PICKUP_OBJECT_ACTION_DURATION)

	local finishTimestamp = getSyncedNetworkTime()
	local okFinish = pcall(network.FireServer, network, "ObjectActionFinished", activationId, finishTimestamp)
	if not okFinish then
		pcall(network.FireServer, network, "ObjectActionCancelled", activationId, finishTimestamp)
		return false
	end

	local pickedUp = waitForDroppedItemCollected(dropItem, AUTO_PICKUP_OBJECT_ACTION_CONFIRM_TIMEOUT)
	if pickedUp then
		playLocalPickupItemFeedback()
	end
	return pickedUp
end

function tryAutoSell(character, humanoid, activeToken)
	if pathState.playbackActive then
		return false
	end

	local shouldSell, filledSlots, totalSlots = isAutoSellThresholdReached()
	if not shouldSell then
		return false
	end

	currentTargetOre = nil
	local sellPart, sellInstance = findNearestAutoSellTarget(character)
	if not sellPart then
		setStatus(string.format("Bag %d/%d - no sell shop found", filledSlots, totalSlots), Color3.fromRGB(255, 190, 120))
		pushDebug("auto sell: no sell shop target found")
		pushNearbySellPromptHints(character)
		return false
	end

	pushDebug(string.format("auto sell: %d/%d slots", filledSlots, totalSlots))
	pushDebug(string.format("auto sell target: %d studs | %s", math.floor(getDistanceToPosition(character, sellPart.Position) + 0.5), sellInstance:GetFullName()))
	setStatus(string.format("Selling loot (%d/%d)", filledSlots, totalSlots), Color3.fromRGB(232, 184, 96))

	local reached = moveCharacterNearPosition(character, humanoid, sellPart.Position, 9, activeToken)
	if not reached then
		pushDebug("auto sell: failed to reach vendor")
		return false
	end

	clearMovementKeys()
	setInteractKeyHeld(false)

	if trySellLootNearTarget(sellPart, sellInstance, character) then
		local remainingSlots = select(1, getInventorySlotCounts())
		pushDebug("auto sell complete, filled slots: " .. tostring(remainingSlots))
		return true
	end

	pushDebug("auto sell: sell prompt/network attempt failed")
	return false
end

function setServerHopStatus(text, color)
	serverHopStatusText = tostring(text or "")
	serverHopStatusColor = color or Color3.fromRGB(176, 167, 150)
	if serverHopStatusLabel then
		serverHopStatusLabel.Text = serverHopStatusText
		serverHopStatusLabel.TextColor3 = serverHopStatusColor
	end
end

function getExecutorEnvironmentTables()
	local environments = { _G }
	if type(getgenv) == "function" then
		local ok, environment = pcall(getgenv)
		if ok and type(environment) == "table" then
			table.insert(environments, environment)
		end
	end
	return environments
end

function getTeleportQueueFunction()
	local directQueue = getCallableGlobal("queue_on_teleport") or getCallableGlobal("queueonteleport")
	if directQueue then
		return directQueue
	end

	for _, environment in ipairs(getExecutorEnvironmentTables()) do
		for _, tableName in ipairs({ "syn", "fluxus", "krnl" }) do
			local executorTable = environment[tableName]
			if type(executorTable) == "table" then
				if type(executorTable.queue_on_teleport) == "function" then
					return executorTable.queue_on_teleport
				end
				if type(executorTable.queueonteleport) == "function" then
					return executorTable.queueonteleport
				end
			end
		end
	end

	return nil
end

function getHttpRequestFunction()
	local directRequest = getCallableGlobal("request")
		or getCallableGlobal("http_request")
		or getCallableGlobal("httprequest")
	if directRequest then
		return directRequest
	end

	for _, environment in ipairs(getExecutorEnvironmentTables()) do
		for _, tableName in ipairs({ "syn", "http", "fluxus", "krnl" }) do
			local executorTable = environment[tableName]
			if type(executorTable) == "table" then
				for _, fieldName in ipairs({ "request", "http_request", "httprequest" }) do
					if type(executorTable[fieldName]) == "function" then
						return executorTable[fieldName]
					end
				end
			end
		end
	end

	local gameHttpGet = type(game.HttpGet) == "function" and game.HttpGet or nil
	if gameHttpGet then
		return function(options)
			return {
				Success = true,
				StatusCode = 200,
				Body = game:HttpGet(options.Url),
			}
		end
	end

	return nil
end

function getDiscordAutomineWebhookUrl()
	local rawValue = tostring(S.discordAutomineWebhookUrl or "")
	return (string.match(rawValue, "^%s*(.-)%s*$") or "")
end

function isDiscordAutomineWebhookUrlValid(value)
	local url = value
	if url == nil then
		url = getDiscordAutomineWebhookUrl()
	else
		url = string.match(tostring(url), "^%s*(.-)%s*$") or ""
	end
	if url == "" then
		return false
	end
	if string.match(url, "^https://discord%.com/api/webhooks/%d+/[%w%-%._~]+") then
		return true
	end
	if string.match(url, "^https://discordapp%.com/api/webhooks/%d+/[%w%-%._~]+") then
		return true
	end
	return false
end

function getDiscordAutomineWebhookOptions()
	return cloneAutomineWebhookOptions(S.discordAutomineWebhookOptions)
end

function isDiscordAutomineWebhookTrackOptionEnabled(optionName)
	return type(S.discordAutomineWebhookOptions) == "table" and S.discordAutomineWebhookOptions[tostring(optionName or "")] == true
end

function getDiscordAutomineWebhookTrackingLabel()
	local enabledOptions = {}
	for _, optionName in ipairs(AUTOMINE_WEBHOOK_TRACK_OPTION_ORDER) do
		if isDiscordAutomineWebhookTrackOptionEnabled(optionName) then
			table.insert(enabledOptions, optionName)
		end
	end
	if #enabledOptions == 0 then
		return "Track earnings"
	end
	if #enabledOptions == 1 then
		return enabledOptions[1]
	end
	return enabledOptions[1] .. " +" .. tostring(#enabledOptions - 1)
end

function isDiscordAutomineWebhookTrackingEnabled()
	return S.discordAutomineWebhookTrackingEnabled == true and isDiscordAutomineWebhookUrlValid()
end

function setDiscordAutomineWebhookUrl(value, skipPersistence)
	local nextValue = string.match(tostring(value or ""), "^%s*(.-)%s*$") or ""
	if getDiscordAutomineWebhookUrl() == nextValue then
		return
	end
	S.discordAutomineWebhookUrl = nextValue
	if not isDiscordAutomineWebhookUrlValid(nextValue) and S.discordAutomineWebhookTrackingEnabled then
		S.discordAutomineWebhookTrackingEnabled = false
		if type(spawnNotification) == "function" then
			spawnNotification("Webhook tracking turned off because the webhook is no longer valid.", "warning", true)
		end
	end
	if skipPersistence then
		return
	end
	schedulePersist()
end

function setDiscordAutomineWebhookTrackingEnabled(value, skipPersistence)
	local nextValue = value == true
	if nextValue and not isDiscordAutomineWebhookUrlValid() then
		notifyUser("Webhook", "Enter a valid Discord webhook first.", 4)
		S.discordAutomineWebhookTrackingEnabled = false
		return false
	end
	if S.discordAutomineWebhookTrackingEnabled == nextValue then
		return true
	end
	S.discordAutomineWebhookTrackingEnabled = nextValue
	if skipPersistence then
		return true
	end
	schedulePersist()
	return true
end

function toggleDiscordAutomineWebhookTrackOption(optionName)
	local optionKey = tostring(optionName or "")
	if optionKey == "" then
		return
	end
	local nextOptions = cloneAutomineWebhookOptions(S.discordAutomineWebhookOptions)
	if nextOptions[optionKey] == nil then
		return
	end
	nextOptions[optionKey] = not nextOptions[optionKey]
	if not nextOptions["Track earnings"] and not nextOptions["Track flags"] then
		nextOptions["Track earnings"] = true
	end
	S.discordAutomineWebhookOptions = nextOptions
	schedulePersist()
end

local function formatAutomineWebhookElapsed(seconds)
	local totalSeconds = math.max(0, math.floor((tonumber(seconds) or 0) + 0.5))
	local hours = math.floor(totalSeconds / 3600)
	local minutes = math.floor((totalSeconds % 3600) / 60)
	local remainingSeconds = totalSeconds % 60
	if hours > 0 then
		return string.format("%dh %02dm %02ds", hours, minutes, remainingSeconds)
	end
	return string.format("%dm %02ds", minutes, remainingSeconds)
end

local function getAutomineWebhookTotalEarned()
	automineWebhookState = type(automineWebhookState) == "table" and automineWebhookState or {}
	automineWebhookState.totalEarned = tonumber(automineWebhookState.totalEarned) or 0
	runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE = automineWebhookState
	return automineWebhookState.totalEarned
end

local function setAutomineWebhookTotalEarned(value)
	automineWebhookState = type(automineWebhookState) == "table" and automineWebhookState or {}
	automineWebhookState.totalEarned = math.max(0, math.floor((tonumber(value) or 0) + 0.5))
	runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE = automineWebhookState
	return automineWebhookState.totalEarned
end

local function postDiscordAutomineWebhook(titleText, lines)
	if not isDiscordAutomineWebhookTrackingEnabled() then
		return false, "disabled"
	end
	local requestFunction = getHttpRequestFunction()
	if type(requestFunction) ~= "function" then
		return false, "request unavailable"
	end

	local contentParts = {}
	if titleText and titleText ~= "" then
		table.insert(contentParts, tostring(titleText))
	end
	if type(lines) == "table" then
		for _, line in ipairs(lines) do
			local text = tostring(line or "")
			if text ~= "" then
				table.insert(contentParts, text)
			end
		end
	end
	if #contentParts == 0 then
		return false, "empty payload"
	end

	local okRequest, response = pcall(requestFunction, {
		Url = getDiscordAutomineWebhookUrl(),
		Method = "POST",
		Headers = {
			["Content-Type"] = "application/json",
		},
		Body = HttpService:JSONEncode({
			content = table.concat(contentParts, "\n"),
		}),
	})
	if not okRequest then
		pushDebug("discord webhook error: " .. tostring(response))
		return false, tostring(response)
	end

	local statusCode = tonumber(response and (response.StatusCode or response.Status or response.status_code)) or 0
	local success = response and response.Success
	if success == nil then
		success = statusCode == 0 or (statusCode >= 200 and statusCode < 300)
	elseif success == false and statusCode >= 200 and statusCode < 300 then
		success = true
	end
	if not success then
		pushDebug("discord webhook failed: " .. tostring(statusCode))
		return false, tostring(statusCode)
	end
	return true, response
end

function sendDiscordAutomineSellWebhook(soldLootValue)
	local soldAmount = math.max(0, math.floor((tonumber(soldLootValue) or 0) + 0.5))
	local totalEarned = setAutomineWebhookTotalEarned(getAutomineWebhookTotalEarned() + soldAmount)
	if not (isDiscordAutomineWebhookTrackingEnabled() and isDiscordAutomineWebhookTrackOptionEnabled("Track earnings")) then
		return false
	end
	local serverElapsed = os.clock() - (tonumber(automineWebhookServerStartedAt) or os.clock())
	return postDiscordAutomineWebhook("AutoMine Sell", {
		"Sold: $" .. formatMoneyAmount(soldAmount),
		"Server elapsed: " .. formatAutomineWebhookElapsed(serverElapsed),
		"Total earned: $" .. formatMoneyAmount(totalEarned),
	})
end

function sendDiscordAutomineFlagWebhook(flagType, detailText)
	if not (isDiscordAutomineWebhookTrackingEnabled() and isDiscordAutomineWebhookTrackOptionEnabled("Track flags")) then
		return false
	end
	local prettyFlagType = tostring(flagType or "flag")
	prettyFlagType = prettyFlagType:gsub("^%l", string.upper)
	return postDiscordAutomineWebhook("AutoMine Flag", {
		"Type: " .. prettyFlagType,
		detailText and ("Reason: " .. tostring(detailText)) or nil,
		"Server elapsed: " .. formatAutomineWebhookElapsed(os.clock() - (tonumber(automineWebhookServerStartedAt) or os.clock())),
		"Total earned: $" .. formatMoneyAmount(getAutomineWebhookTotalEarned()),
	})
end

function getCurrentScriptFilePath()
	if type(debug) ~= "table" or type(debug.info) ~= "function" then
		local cachedPath = runtimeEnv.__FOB_LAST_SCRIPT_FILE_PATH
		if type(cachedPath) == "string" and cachedPath ~= "" then
			return cachedPath
		end
		return nil
	end

	for level = 1, 10 do
		local ok, source = pcall(debug.info, level, "s")
		if ok and type(source) == "string" and string.sub(source, 1, 1) == "@" then
			local resolvedPath = string.sub(source, 2)
			runtimeEnv.__FOB_LAST_SCRIPT_FILE_PATH = resolvedPath
			return resolvedPath
		end
	end

	local cachedPath = runtimeEnv.__FOB_LAST_SCRIPT_FILE_PATH
	if type(cachedPath) == "string" and cachedPath ~= "" then
		return cachedPath
	end

	local isfileFunction = getCallableGlobal("isfile")
	local readfileFunction = getCallableGlobal("readfile")
	if type(isfileFunction) == "function" then
		local embeddedFallbackPath = tostring(TELEPORT_SCRIPT_EMBEDDED_FALLBACK_PATH or "")
		local okEmbedded, embeddedExists = pcall(isfileFunction, embeddedFallbackPath)
		if okEmbedded and embeddedExists and embeddedFallbackPath ~= "" then
			runtimeEnv.__FOB_LAST_SCRIPT_FILE_PATH = embeddedFallbackPath
			return embeddedFallbackPath
		end
	end

	if type(isfileFunction) ~= "function" or type(readfileFunction) ~= "function" then
		return nil
	end

	local filePath = string.format(
		"%s\\%s_%s.txt",
		PATH_STORAGE_FOLDER,
		TELEPORT_SCRIPT_PATH_FILE_PREFIX,
		sanitizePathStorageKey(getStorageId())
	)
	local okRead, storedPath = pcall(function()
		if not isfileFunction(filePath) then
			return nil
		end
		return readfileFunction(filePath)
	end)
	if okRead and type(storedPath) == "string" then
		storedPath = string.gsub(storedPath, "^%s+", "")
		storedPath = string.gsub(storedPath, "%s+$", "")
		if storedPath ~= "" then
			runtimeEnv.__FOB_LAST_SCRIPT_FILE_PATH = storedPath
			return storedPath
		end
	end

	return nil
end

function rememberTeleportScriptFilePath(scriptFilePath)
	local normalizedPath = type(scriptFilePath) == "string" and string.gsub(scriptFilePath, "^%s+", "") or ""
	normalizedPath = string.gsub(normalizedPath, "%s+$", "")
	if normalizedPath == "" then
		return false
	end

	runtimeEnv.__FOB_LAST_SCRIPT_FILE_PATH = normalizedPath

	local makefolderFunction = getCallableGlobal("makefolder")
	local writefileFunction = getCallableGlobal("writefile")
	if type(makefolderFunction) == "function" and type(writefileFunction) == "function" then
		pcall(makefolderFunction, PATH_STORAGE_FOLDER)
		pcall(
			writefileFunction,
			string.format(
				"%s\\%s_%s.txt",
				PATH_STORAGE_FOLDER,
				TELEPORT_SCRIPT_PATH_FILE_PREFIX,
				sanitizePathStorageKey(getStorageId())
			),
			normalizedPath
		)
	end

	return true
end

function buildTeleportQueueSource(scriptFilePath)
	local queuedWebhookTotalEarned = math.max(0, math.floor((tonumber(automineWebhookState and automineWebhookState.totalEarned) or 0) + 0.5))
	return string.format([[
pcall(function()
    local scriptPath = %q
    local retryAttempts = %d
    local retryDelay = %s
    local webhookTotalEarned = %s
    local runtimeEnv = (getgenv and getgenv()) or _G
    runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE = runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE or {}
    runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE.totalEarned = math.max(tonumber(runtimeEnv.__FOB_AUTOMINE_WEBHOOK_STATE.totalEarned) or 0, webhookTotalEarned)

    local function createLoader()
        if type(loadfile) == "function" then
            local okLoader, loader = pcall(loadfile, scriptPath)
            if okLoader and type(loader) == "function" then
                return true, loader
            end
        end

        if type(readfile) == "function" and type(loadstring) == "function" then
            local okRead, source = pcall(readfile, scriptPath)
            if okRead and type(source) == "string" and source ~= "" then
                local okChunk, chunk = pcall(loadstring, source)
                if okChunk and type(chunk) == "function" then
                    return true, chunk
                end
            end
        end

        return false
    end

    local function tryLaunch()
        local okLoader, loader = createLoader()
        if not okLoader or type(loader) ~= "function" then
            return false
        end
        local okRun = pcall(loader)
        return okRun == true
    end

    pcall(function()
        if not game:IsLoaded() then
            game.Loaded:Wait()
        end
    end)

    for _ = 1, retryAttempts do
        if tryLaunch() then
            return
        end
        task.wait(retryDelay)
    end

    warn("[FOB] teleport bootstrap failed for " .. tostring(scriptPath))
end)
]], scriptFilePath, TELEPORT_QUEUE_RETRY_ATTEMPTS, tostring(TELEPORT_QUEUE_RETRY_DELAY), tostring(queuedWebhookTotalEarned))
end

function queueCurrentScriptForServerHop()
	local queueFunction = getTeleportQueueFunction()
	if not queueFunction then
		return true, "queue_on_teleport unavailable"
	end

	local scriptFilePath = getCurrentScriptFilePath()
	if not scriptFilePath or scriptFilePath == "" then
		return true, "current script file path unavailable"
	end

	rememberTeleportScriptFilePath(scriptFilePath)

	local queuedSource = buildTeleportQueueSource(scriptFilePath)
	local ok, err = pcall(queueFunction, queuedSource)
	if not ok then
		return false, tostring(err)
	end

	return true
end

function buildPublicServerListUrl(cursor)
	local baseUrl = string.format(
		"https://games.roblox.com/v1/games/%d/servers/Public?sortOrder=Asc&limit=%d&excludeFullGames=true",
		game.PlaceId,
		SERVER_HOP_PAGE_SIZE
	)

	if cursor and cursor ~= "" then
		baseUrl = baseUrl .. "&cursor=" .. HttpService:UrlEncode(cursor)
	end

	return baseUrl
end

function decodeHttpResponse(response)
	if type(response) == "string" then
		return true, 200, response
	end

	if type(response) ~= "table" then
		return false, nil, nil
	end

	local statusCode = tonumber(response.StatusCode or response.Status or response.status)
	local success = response.Success
	if success == nil then
		success = response.success
	end
	if success == nil then
		success = statusCode and statusCode >= 200 and statusCode < 300
	end

	return success == true, statusCode, response.Body or response.body
end

function fetchPublicServerPage(cursor)
	local requestFunction = getHttpRequestFunction()
	if not requestFunction then
		return nil, "executor HTTP request unavailable"
	end

	local ok, response = pcall(requestFunction, {
		Url = buildPublicServerListUrl(cursor),
		Method = "GET",
	})
	if not ok then
		return nil, "server list request errored"
	end
	local success, statusCode, body = decodeHttpResponse(response)
	if not success or type(body) ~= "string" or body == "" then
		return nil, "server list request failed (" .. tostring(statusCode or "?") .. ")"
	end

	local ok, payload = pcall(function()
		return HttpService:JSONDecode(body)
	end)
	if not ok or type(payload) ~= "table" then
		return nil, "server list JSON decode failed"
	end

	return payload, nil
end

function findSmallServerHopTarget()
	local bestServer = nil
	local cursor = nil

	for _ = 1, SERVER_HOP_PAGE_LIMIT do
		local payload, err = fetchPublicServerPage(cursor)
		if not payload then
			return nil, err
		end

		for _, server in ipairs(payload.data or {}) do
			local serverId = tostring(server.id or "")
			local playing = tonumber(server.playing) or math.huge
			local maxPlayers = tonumber(server.maxPlayers) or math.huge
			if serverId ~= "" and serverId ~= game.JobId and playing < maxPlayers then
				if not bestServer
					or playing < (tonumber(bestServer.playing) or math.huge)
					or (playing == (tonumber(bestServer.playing) or math.huge) and maxPlayers < (tonumber(bestServer.maxPlayers) or math.huge))
				then
					bestServer = server
				end
			end
		end

		cursor = payload.nextPageCursor
		if not cursor or cursor == "" then
			break
		end
	end

	if not bestServer then
		return nil, "no suitable public server found"
	end

	return bestServer, nil
end

function teleportToServerInstance(serverId)
	local ok, err = pcall(function()
		local teleportOptions = Instance.new("TeleportOptions")
		teleportOptions.ServerInstanceId = tostring(serverId)
		TeleportService:TeleportAsync(game.PlaceId, { LocalPlayer }, teleportOptions)
	end)
	if ok then
		return true
	end

	ok, err = pcall(function()
		TeleportService:TeleportToPlaceInstance(game.PlaceId, tostring(serverId), LocalPlayer)
	end)
	if ok then
		return true
	end

	ok, err = pcall(function()
		TeleportService:TeleportToPlaceInstance(game.PlaceId, tostring(serverId))
	end)
	if ok then
		return true
	end

	return false, err
end

function tryPathServerHop(reasonText, resumeSlotIndex, forceHop, flagReason)
	if (not forceHop and not serverHopEnabled) or serverHopInProgress then
		serverHopTaskQueued = false
		return false
	end

	local normalizedResumeSlotIndex = normalizePathSlotIndex(resumeSlotIndex)
	serverHopTaskQueued = false
	serverHopInProgress = true
	setStatus("Finding a smaller server...", Color3.fromRGB(120, 190, 255))
	setServerHopStatus(tostring(reasonText or "Finding a smaller public server..."), Color3.fromRGB(120, 190, 255))
	if normalizedResumeSlotIndex then
		setPathSequenceStartSlotIndex(normalizedResumeSlotIndex)
		setPathResumePlaybackPending(true, true)
	else
		setPathResumePlaybackPending(false, true)
	end
	if type(refreshServerHopDisplay) == "function" then
		refreshServerHopDisplay()
	end

	if flagReason then
		pcall(sendDiscordAutomineFlagWebhook, flagReason, reasonText)
	end

	local queuedOk, queueStatus = queueCurrentScriptForServerHop()
	if not queuedOk then
		pushDebug("server hop queue: " .. tostring(queueStatus))
		pushDebug("server hop will rely on auto-execute or rerunning the script")
	end

	local targetServer, findErr = findSmallServerHopTarget()
	if not targetServer then
		serverHopInProgress = false
		serverHopTaskQueued = false
		if normalizedResumeSlotIndex then
			setPathResumePlaybackPending(false, true)
		end
		setServerHopStatus("Hop failed: " .. tostring(findErr), Color3.fromRGB(255, 190, 120))
		pushDebug("server hop failed: " .. tostring(findErr))
		if type(refreshServerHopDisplay) == "function" then
			refreshServerHopDisplay()
		end
		return false
	end

	local playersNow = tonumber(targetServer.playing) or 0
	local maxPlayers = tonumber(targetServer.maxPlayers) or 0
	pushDebug(string.format("server hop target: %s (%d/%d)", tostring(targetServer.id), playersNow, maxPlayers))
	setStatus(string.format("Server hopping (%d/%d)", playersNow, maxPlayers), Color3.fromRGB(120, 190, 255))
	setServerHopStatus(string.format("Joining a %d / %d server...", playersNow, maxPlayers), Color3.fromRGB(120, 190, 255))

	local teleported, teleportErr = teleportToServerInstance(targetServer.id)
	if not teleported then
		serverHopInProgress = false
		serverHopTaskQueued = false
		if normalizedResumeSlotIndex then
			setPathResumePlaybackPending(false, true)
		end
		setServerHopStatus("Hop failed: " .. tostring(teleportErr), Color3.fromRGB(255, 190, 120))
		pushDebug("server hop teleport failed: " .. tostring(teleportErr))
		if type(refreshServerHopDisplay) == "function" then
			refreshServerHopDisplay()
		end
		return false
	end

	if type(refreshServerHopDisplay) == "function" then
		refreshServerHopDisplay()
	end

	return true
end

function remoteMatchesKeywords(remote, keywords)
	local name = safeLower(remote.Name)
	for _, keyword in ipairs(keywords) do
		if string.find(name, safeLower(keyword), 1, true) ~= nil then
			return true
		end
	end
	return false
end

function findRemote(roots, keywords)
	for _, root in ipairs(roots) do
		if root then
			for _, descendant in ipairs(root:GetDescendants()) do
				if (
					descendant:IsA("RemoteEvent")
					or descendant:IsA("RemoteFunction")
					or descendant:IsA("BindableEvent")
					or descendant:IsA("BindableFunction")
				) and remoteMatchesKeywords(descendant, keywords) then
					return descendant
				end
			end
		end
	end

	return nil
end

function findRemotes(roots, keywords, maxCount)
	local results = {}
	local seen = {}

	for _, root in ipairs(roots) do
		if root then
			for _, descendant in ipairs(root:GetDescendants()) do
				local isRemote = descendant:IsA("RemoteEvent")
					or descendant:IsA("RemoteFunction")
					or descendant:IsA("BindableEvent")
					or descendant:IsA("BindableFunction")

				if isRemote and not seen[descendant] and remoteMatchesKeywords(descendant, keywords) then
					seen[descendant] = true
					table.insert(results, descendant)
					if maxCount and #results >= maxCount then
						return results
					end
				end
			end
		end
	end

	return results
end

function getEquipRemote(forceRefresh)
	if cachedEquipRemote and cachedEquipRemote.Parent then
		return cachedEquipRemote
	end

	local now = os.clock()
	if not forceRefresh and (now - lastEquipRemoteScanAt) < RESCAN_INTERVAL then
		return nil
	end

	lastEquipRemoteScanAt = now
	cachedEquipRemote = findRemote(
		{
			LocalPlayer:FindFirstChild("PlayerGui"),
			game:GetService("ReplicatedStorage"),
		},
		{ "equip", "loadout", "pickaxe", "item" }
	)

	return cachedEquipRemote
end

function getMineRemote(forceRefresh, pickaxe, character)
	if cachedMineRemote and cachedMineRemote.Parent then
		return cachedMineRemote
	end

	local now = os.clock()
	if not forceRefresh and (now - lastMineRemoteScanAt) < RESCAN_INTERVAL then
		return cachedMineRemote
	end

	lastMineRemoteScanAt = now
	local roots = {
		pickaxe,
		character,
		LocalPlayer:FindFirstChild("PlayerGui"),
		game:GetService("ReplicatedStorage"),
	}
	cachedMineRemotes = findRemotes(roots, { "mine", "swing", "attack", "hit", "activate", "interact", "network" }, 8)
	cachedMineRemote = cachedMineRemotes[1] or findRemote(roots, { "mine", "swing", "attack", "hit", "activate", "interact" })

	if cachedMineRemote then
		pushDebug("mine remote: " .. cachedMineRemote:GetFullName())
	end

	return cachedMineRemote
end

function getMineRemotes(forceRefresh, pickaxe, character)
	getMineRemote(forceRefresh, pickaxe, character)
	return cachedMineRemotes or {}
end

function triggerRemote(remote, ...)
	if not remote then
		return false
	end

	local packedArgs = table.pack(...)

	local ok = pcall(function()
		if remote:IsA("RemoteEvent") then
			remote:FireServer(table.unpack(packedArgs, 1, packedArgs.n))
		elseif remote:IsA("RemoteFunction") then
			remote:InvokeServer(table.unpack(packedArgs, 1, packedArgs.n))
		elseif remote:IsA("BindableEvent") then
			remote:Fire(table.unpack(packedArgs, 1, packedArgs.n))
		elseif remote:IsA("BindableFunction") then
			remote:Invoke(table.unpack(packedArgs, 1, packedArgs.n))
		end
	end)

	return ok
end

function isToolInfoPickaxe(toolInfo)
	if not isTable(toolInfo) then
		return false
	end

	local directNameHint = rawget(toolInfo, "Name")
		or toolInfo.Name
		or rawget(toolInfo, "ItemName")
		or toolInfo.ItemName
		or rawget(toolInfo, "Type")
		or toolInfo.Type
		or rawget(toolInfo, "ItemType")
		or toolInfo.ItemType
		or rawget(toolInfo, "ClassName")
		or toolInfo.ClassName
	if string.find(safeLower(directNameHint), "pickaxe", 1, true) ~= nil then
		return true
	end

	if rawget(toolInfo, "CanMine") == true or toolInfo.CanMine == true then
		return true
	end

	local model = rawget(toolInfo, "Model") or toolInfo.Model
	if typeof(model) == "Instance" then
		local modelName = safeLower(model.Name)
		return string.find(modelName, "pickaxe", 1, true) ~= nil
	end

	local modelHint = safeLower(model)
	if string.find(modelHint, "pickaxe", 1, true) ~= nil then
		return true
	end

	return string.find(modelHint, "mine", 1, true) ~= nil
end

function getToolInfoCharacter(toolInfo, fallbackCharacter)
	local character = rawget(toolInfo, "Character") or toolInfo.Character
	if typeof(character) == "Instance" and character:IsA("Model") then
		return character
	end

	return fallbackCharacter
end

function getToolInfoHitbox(toolInfo)
	local hitbox = rawget(toolInfo, "Hitbox") or toolInfo.Hitbox
	if typeof(hitbox) == "Instance" and hitbox:IsA("BasePart") then
		return hitbox
	end

	local model = rawget(toolInfo, "Model") or toolInfo.Model
	if typeof(model) == "Instance" then
		local found = model:FindFirstChild("HitBox", true)
			or model:FindFirstChild("Hitbox", true)
			or model:FindFirstChild("Handle", true)
		if found and found:IsA("BasePart") then
			return found
		end
	end

	return nil
end

function isLikelyMiningToolInfo(toolInfo, character)
	if not looksLikeToolInfo(toolInfo) then
		return false
	end

	if isToolInfoPickaxe(toolInfo) then
		return true
	end

	local isEquipped = rawget(toolInfo, "IsEquipped")
	if isEquipped == nil then
		isEquipped = toolInfo.IsEquipped
	end

	local toolCharacter = getToolInfoCharacter(toolInfo, nil)
	local characterMatches = (not character) or (not toolCharacter) or (toolCharacter == character)
	local hasHitbox = getToolInfoHitbox(toolInfo) ~= nil

	return characterMatches and isEquipped ~= false and hasHitbox
end

function scoreToolInfoCandidate(toolInfo, character)
	local score = 0
	if looksLikeToolInfo(toolInfo) then
		score += 2
	end
	if isToolInfoPickaxe(toolInfo) then
		score += 4
	end
	if getToolInfoHitbox(toolInfo) then
		score += 2
	end

	local isEquipped = rawget(toolInfo, "IsEquipped")
	if isEquipped == nil then
		isEquipped = toolInfo.IsEquipped
	end
	if isEquipped == true then
		score += 2
	elseif isEquipped == nil then
		score += 1
	end

	local toolCharacter = getToolInfoCharacter(toolInfo, nil)
	if character and toolCharacter == character then
		score += 3
	elseif toolCharacter then
		score += 1
	end

	return score
end

function resolveToolInfo(forceRefresh, character)
	if cachedDirectToolInfo and isLikelyMiningToolInfo(cachedDirectToolInfo, character) then
		local cachedCharacter = getToolInfoCharacter(cachedDirectToolInfo, nil)
		if not character or not cachedCharacter or cachedCharacter == character then
			return cachedDirectToolInfo
		end
		cachedDirectToolInfo = nil
	end

	local _, ownerToolInfo = resolveToolInfoOwner(forceRefresh)
	if ownerToolInfo and isLikelyMiningToolInfo(ownerToolInfo, character) then
		cachedDirectToolInfo = ownerToolInfo
		return ownerToolInfo
	end

	if type(getgc) ~= "function" then
		return nil
	end

	local now = os.clock()
	if not forceRefresh and (now - lastDirectToolInfoScanAt) < AURA_FORCE_SCAN_INTERVAL then
		return nil
	end
	lastDirectToolInfoScanAt = now

	local ok, gcObjects = pcall(getgc, true)
	if not ok or type(gcObjects) ~= "table" then
		return nil
	end

	local bestMatch = nil
	local bestScore = -math.huge
	local deadline = os.clock() + TOOLINFO_DIRECT_SCAN_BUDGET
	for _, object in ipairs(gcObjects) do
		if os.clock() > deadline then
			break
		end

		if isTable(object) and isLikelyMiningToolInfo(object, character) then
			local isEquipped = rawget(object, "IsEquipped")
			if isEquipped == nil then
				isEquipped = object.IsEquipped
			end

			local objectCharacter = getToolInfoCharacter(object, nil)
			local characterMatches = (not character) or (not objectCharacter) or objectCharacter == character
			local equippedMatches = (isEquipped == nil) or (isEquipped == true)

			if characterMatches and equippedMatches then
				local score = scoreToolInfoCandidate(object, character)
				if score > bestScore then
					bestScore = score
					bestMatch = object
				end
				if score >= 9 then
					cachedDirectToolInfo = object
					return object
				end
			end
		end
	end

	cachedDirectToolInfo = bestMatch
	return bestMatch
end

function callToolNetworkActivate(toolInfo, ...)
	local networkActivate = rawget(toolInfo, "NetworkActivate") or toolInfo.NetworkActivate
	if type(networkActivate) ~= "function" then
		return false
	end

	local packedArgs = table.pack(...)
	return pcall(function()
		toolInfo:NetworkActivate(table.unpack(packedArgs, 1, packedArgs.n))
	end)
end

resolveGlobalNetwork = function(forceRefresh)
	local cache = rawget(_G, "__AutoMineGlobalNetwork")
	if cache and type(cache.FireServer) == "function" then
		return cache
	end

	local lastScan = rawget(_G, "__AutoMineGlobalNetworkLastScan") or 0
	local now = os.clock()
	if not forceRefresh and (now - lastScan) < RESCAN_INTERVAL then
		return nil
	end
	_G.__AutoMineGlobalNetworkLastScan = now

	local sharedModules = ReplicatedStorage:FindFirstChild("SharedModules")
	local globalModuleScript = sharedModules and sharedModules:FindFirstChild("Global")
	if not globalModuleScript or not globalModuleScript:IsA("ModuleScript") then
		return nil
	end

	local okRequire, globalModule = pcall(require, globalModuleScript)
	if not okRequire or not isTable(globalModule) then
		return nil
	end
	_G.__AutoMineGlobalModule = globalModule

	local network = rawget(globalModule, "Network") or globalModule.Network
	if (not network or type(network.FireServer) ~= "function") and type(globalModule.LoadModule) == "function" then
		local okLoad, loadedNetwork = pcall(function()
			return globalModule:LoadModule("Network")
		end)
		if okLoad and isTable(loadedNetwork) and type(loadedNetwork.FireServer) == "function" then
			network = loadedNetwork
			globalModule.Network = loadedNetwork
		end
	end
	if not network or type(network.FireServer) ~= "function" then
		return nil
	end

	_G.__AutoMineGlobalNetwork = network
	return network
end

function resolveGlobalParams(forceRefresh)
    local globalModule = rawget(_G, "__AutoMineGlobalModule")
    if not isTable(globalModule) then
        resolveGlobalNetwork(forceRefresh)
        globalModule = rawget(_G, "__AutoMineGlobalModule")
    end
    if not isTable(globalModule) then
        return nil
    end

    local params = rawget(globalModule, "Params") or globalModule.Params
    if isTable(params) then
        return params
    end

    if type(globalModule.LoadModule) == "function" then
        local okLoad, loadedParams = pcall(function()
            return globalModule:LoadModule("Params")
        end)
        if okLoad and isTable(loadedParams) then
            globalModule.Params = loadedParams
            return loadedParams
        end
    end

    return nil
end

function resolvePlayerCharacterModule(forceRefresh)
	local modulesTable = resolveGameModules(forceRefresh)
	if hasPlrCharacterModule(modulesTable) then
		return rawget(modulesTable, "plrCharacter") or modulesTable.plrCharacter
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return nil
	end

	for _, key in ipairs({ "PlayerCharacter", "playerCharacter", "RepCharHandler", "repCharHandler", "plrCharacter" }) do
		local playerCharacter = rawget(globalModule, key) or globalModule[key]
		if looksLikePlayerCharacterModule(playerCharacter) then
			return playerCharacter
		end
	end

	if type(globalModule.LoadModule) == "function" then
		for _, moduleName in ipairs({ "RepCharHandler", "PlayerCharacter", "SwimController" }) do
			local okLoad, loadedModule = pcall(function()
				return globalModule:LoadModule(moduleName)
			end)
			if okLoad and looksLikePlayerCharacterModule(loadedModule) then
				globalModule[moduleName] = loadedModule
				return loadedModule
			end
		end
	end

	return nil
end

function resolvePlayerDataModule(forceRefresh)
	local cache = rawget(_G, "__AutoMinePlayerDataModule")
	if looksLikePlayerDataModule(cache) then
		return cache
	end

	local modulesTable = resolveGameModules(forceRefresh)
	if isTable(modulesTable) then
		for _, key in ipairs({ "PlayerData", "playerData" }) do
			local playerData = rawget(modulesTable, key) or modulesTable[key]
			if looksLikePlayerDataModule(playerData) then
				_G.__AutoMinePlayerDataModule = playerData
				return playerData
			end
		end
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return nil
	end

	for _, key in ipairs({ "PlayerData", "playerData" }) do
		local playerData = rawget(globalModule, key) or globalModule[key]
		if looksLikePlayerDataModule(playerData) then
			_G.__AutoMinePlayerDataModule = playerData
			return playerData
		end
	end

	if type(globalModule.LoadModule) == "function" then
		local okLoad, loadedModule = pcall(function()
			return globalModule:LoadModule("PlayerData")
		end)
		if okLoad and looksLikePlayerDataModule(loadedModule) then
			globalModule.PlayerData = loadedModule
			_G.__AutoMinePlayerDataModule = loadedModule
			return loadedModule
		end
	end

	return nil
end

function resolveSharedPlayerItems(forceRefresh)
	local cache = rawget(_G, "__AutoMineSharedPlayerItems")
	if isTable(cache) then
		return cache
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return nil
	end

	local sharedData = rawget(globalModule, "SharedData") or globalModule.SharedData
	local playerItems = isTable(sharedData) and (rawget(sharedData, "PlayerItems") or sharedData.PlayerItems) or nil
	if not isTable(playerItems) and type(globalModule.LoadModule) == "function" then
		local okLoad, loadedSharedData = pcall(function()
			return globalModule:LoadModule("SharedData")
		end)
		if okLoad and isTable(loadedSharedData) then
			sharedData = loadedSharedData
			globalModule.SharedData = loadedSharedData
			playerItems = rawget(loadedSharedData, "PlayerItems") or loadedSharedData.PlayerItems
		end
	end

	if isTable(playerItems) then
		_G.__AutoMineSharedPlayerItems = playerItems
		return playerItems
	end

	return nil
end

function looksLikeSafeTeleportHandler(handler)
	if not isTable(handler) then
		return false
	end

	return type(rawget(handler, "_UpdateLocalCharPlatform") or handler._UpdateLocalCharPlatform) == "function"
		or type(rawget(handler, "ReplicateCharacters") or handler.ReplicateCharacters) == "function"
		or isTable(rawget(handler, "Flags") or handler.Flags)
end

function resolveSafeTeleportHandler(forceRefresh)
	local playerCharacter = resolvePlayerCharacterModule(forceRefresh)
	if looksLikeSafeTeleportHandler(playerCharacter) then
		return playerCharacter
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end

	if isTable(globalModule) then
		for _, key in ipairs({ "RepCharHandler", "repCharHandler", "PlayerCharacter", "playerCharacter" }) do
			local candidate = rawget(globalModule, key) or globalModule[key]
			if looksLikeSafeTeleportHandler(candidate) then
				return candidate
			end
		end
	end

	local modulesTable = resolveGameModules(forceRefresh)
	if isTable(modulesTable) then
		for _, key in ipairs({ "RepCharHandler", "repCharHandler", "plrCharacter", "PlayerCharacter", "playerCharacter" }) do
			local candidate = rawget(modulesTable, key) or modulesTable[key]
			if looksLikeSafeTeleportHandler(candidate) then
				return candidate
			end
		end
	end

	for _, root in ipairs(getEnvironmentTables()) do
		for _, key in ipairs({ "RepCharHandler", "repCharHandler", "PlayerCharacter", "playerCharacter", "plrCharacter" }) do
			local candidate = rawget(root, key) or root[key]
			if looksLikeSafeTeleportHandler(candidate) then
				return candidate
			end
		end
	end

	return nil
end

function resolveSafeTeleportRagdollOwner(forceRefresh)
	local playerCharacter = resolvePlayerCharacterModule(forceRefresh)
	if isTable(playerCharacter) and type(rawget(playerCharacter, "Ragdoll") or playerCharacter.Ragdoll) == "function" then
		return playerCharacter
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end

	if isTable(globalModule) then
		for _, key in ipairs({ "PlayerCharacter", "playerCharacter", "RepCharHandler", "repCharHandler" }) do
			local candidate = rawget(globalModule, key) or globalModule[key]
			if isTable(candidate) and type(rawget(candidate, "Ragdoll") or candidate.Ragdoll) == "function" then
				return candidate
			end
		end
	end

	return nil
end

function resolveGlobalReplicatedState(forceRefresh)
	local cache = rawget(_G, "__AutoMineGlobalReplicatedState")
	if isTable(cache) and type(cache.LocalSetState) == "function" then
		return cache
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return nil
	end

	local replicatedState = rawget(globalModule, "ReplicatedState") or globalModule.ReplicatedState
	if (not isTable(replicatedState) or type(replicatedState.LocalSetState) ~= "function") and type(globalModule.LoadModule) == "function" then
		local okLoad, loadedReplicatedState = pcall(function()
			return globalModule:LoadModule("ReplicatedState")
		end)
		if okLoad and isTable(loadedReplicatedState) and type(loadedReplicatedState.LocalSetState) == "function" then
			replicatedState = loadedReplicatedState
			globalModule.ReplicatedState = loadedReplicatedState
		end
	end
	if isTable(replicatedState) and type(replicatedState.LocalSetState) == "function" then
		_G.__AutoMineGlobalReplicatedState = replicatedState
		return replicatedState
	end

	return nil
end

function getExperimentalCFrameTravelState()
	local state = playerState.experimentalCFrameTravel
	if type(state) ~= "table" then
		state = {}
		playerState.experimentalCFrameTravel = state
	end
	return state
end

function shouldDelayExperimentalCFrameEquip()
	return EXPERIMENTAL_AUTOMINE_CFRAME
		and movementMode == "cframe"
		and pathState
		and pathState.playbackActive == true
end

function setCharacterMotorTransforms(character, transformValue)
	if not character then
		return false
	end

	local applied = false
	for _, descendant in ipairs(character:GetDescendants()) do
		if descendant:IsA("Motor6D") then
			pcall(function()
				descendant.Transform = transformValue
			end)
			applied = true
		end
	end

	return applied
end

function setAnimationHandlerJointsEnabled(animationHandler, isEnabled)
	if not isTable(animationHandler) then
		return false
	end

	local setJointsEnabled = rawget(animationHandler, "SetJointsEnabled") or animationHandler.SetJointsEnabled
	if type(setJointsEnabled) ~= "function" then
		return false
	end

	return pcall(setJointsEnabled, animationHandler, isEnabled == true)
end

function resolveRagdollsModule(forceRefresh)
	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return nil
	end

	local ragdolls = rawget(globalModule, "Ragdolls") or globalModule.Ragdolls
	if isTable(ragdolls) then
		return ragdolls
	end

	if type(globalModule.LoadModule) == "function" then
		local okLoad, loadedModule = pcall(function()
			return globalModule:LoadModule("Ragdolls")
		end)
		if okLoad and isTable(loadedModule) then
			globalModule.Ragdolls = loadedModule
			return loadedModule
		end
	end

	return nil
end

function getExperimentalTravelCharacter()
	local state = getExperimentalCFrameTravelState()
	if state and typeof(state.character) == "Instance" then
		return state.character
	end
	return nil
end

function ensureExperimentalSoftRagdollBypass()
	local ragdolls = resolveRagdollsModule(false) or resolveRagdollsModule(true)
	if not isTable(ragdolls) then
		return nil
	end

	if playerState.experimentalSoftRagdollPatched and playerState.experimentalSoftRagdollOwner == ragdolls then
		return ragdolls
	end

	local enterRagdoll = rawget(ragdolls, "EnterRagdoll") or ragdolls.EnterRagdoll
	local exitRagdoll = rawget(ragdolls, "ExitRagdoll") or ragdolls.ExitRagdoll
	if type(enterRagdoll) ~= "function" or type(exitRagdoll) ~= "function" then
		return ragdolls
	end

	playerState.experimentalSoftRagdollOwner = ragdolls
	playerState.experimentalSoftRagdollOriginalEnter = enterRagdoll
	playerState.experimentalSoftRagdollOriginalExit = exitRagdoll

	ragdolls.EnterRagdoll = function(self, character, ...)
		local activeCharacter = getExperimentalTravelCharacter()
		if activeCharacter and character == activeCharacter then
			return
		end
		return playerState.experimentalSoftRagdollOriginalEnter(self, character, ...)
	end

	ragdolls.ExitRagdoll = function(self, character, ...)
		local activeCharacter = getExperimentalTravelCharacter()
		if activeCharacter and character == activeCharacter then
			return
		end
		return playerState.experimentalSoftRagdollOriginalExit(self, character, ...)
	end

	playerState.experimentalSoftRagdollPatched = true
	return ragdolls
end

function enforceExperimentalCFrameFreezeState(character)
	local activeCharacter = character or getExperimentalTravelCharacter()
	if not activeCharacter then
		return false
	end

	local state = getExperimentalCFrameTravelState()
	local repChar = state.repChar or resolveLocalRepChar(false)
	local rootPart = getRootPart(activeCharacter)
	local changed = false

	if isTable(repChar) then
		state.repChar = repChar
		changed = true
	end

	if rootPart then
		if state.originalRootAnchored == nil then
			state.originalRootAnchored = rootPart.Anchored
		end
		pcall(function()
			rootPart.Anchored = true
			rootPart.Velocity = Vector3.zero
			rootPart.RotVelocity = Vector3.zero
		end)
		pcall(function()
			rootPart.AssemblyLinearVelocity = Vector3.zero
			rootPart.AssemblyAngularVelocity = Vector3.zero
		end)
		changed = true
	end

	return changed
end

function resolveLocalRepChar(forceRefresh)
	local handler = resolveSafeTeleportHandler(forceRefresh)
	if isTable(handler) then
		local repChar = rawget(handler, "LocalRepChar") or handler.LocalRepChar
		if isTable(repChar) then
			return repChar
		end
		local getRepChar = rawget(handler, "GetRepChar") or handler.GetRepChar
		if type(getRepChar) == "function" and localPlayer then
			local okGet, result = pcall(getRepChar, handler, localPlayer)
			if okGet and isTable(result) then
				return result
			end
		end
	end

	local playerCharacter = resolvePlayerCharacterModule(forceRefresh)
	if isTable(playerCharacter) then
		local repChar = rawget(playerCharacter, "RepChar") or playerCharacter.RepChar
		if isTable(repChar) then
			return repChar
		end
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if isTable(globalModule) then
		local handlerFromGlobal = rawget(globalModule, "RepCharHandler") or globalModule.RepCharHandler
		if isTable(handlerFromGlobal) then
			local repChar = rawget(handlerFromGlobal, "LocalRepChar") or handlerFromGlobal.LocalRepChar
			if isTable(repChar) then
				return repChar
			end
			local getRepChar = rawget(handlerFromGlobal, "GetRepChar") or handlerFromGlobal.GetRepChar
			if type(getRepChar) == "function" and localPlayer then
				local okGet, result = pcall(getRepChar, handlerFromGlobal, localPlayer)
				if okGet and isTable(result) then
					return result
				end
			end
		end
	end

	return nil
end

function setExperimentalRepFaceMouseDisabled(repChar, isDisabled)
	if not (repChar and repChar.Model) then
		return false
	end

	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(false)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return false
	end

	local faceMouseModule = rawget(globalModule, "FaceMouse") or globalModule.FaceMouse
	if isTable(faceMouseModule) and type(rawget(faceMouseModule, "DisableFor") or faceMouseModule.DisableFor) == "function" then
		return pcall(faceMouseModule.DisableFor, faceMouseModule, repChar.Model, isDisabled == true)
	end

	return false
end

function endExperimentalCFrameTravel(character)
	local state = getExperimentalCFrameTravelState()
	if not state.active then
		return
	end

	local activeCharacter = character or state.character
	local repChar = state.repChar or resolveLocalRepChar(false)
	local playerCharacter = state.playerCharacter or resolvePlayerCharacterModule(false)
	local humanoid = state.humanoid or getHumanoid(activeCharacter)
	local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
	local rootPart = getRootPart(activeCharacter)

	if isTable(repChar) then
		repChar.LocalForceCFrame = nil
	end

	if isTable(playerCharacter) and humanoid and humanoid.Health > 0 then
		if state.usedSoftSelfRagdoll and type(rawget(playerCharacter, "ToggleSelfRagdoll") or playerCharacter.ToggleSelfRagdoll) == "function" then
			pcall(playerCharacter.ToggleSelfRagdoll, playerCharacter, false)
		end
		if state.usedControlLost and type(rawget(playerCharacter, "GainControl") or playerCharacter.GainControl) == "function" then
			pcall(playerCharacter.GainControl, playerCharacter)
		end
	end

	if humanoid then
		pcall(function()
			humanoid.Jump = false
			humanoid:Move(Vector3.zero)
		end)
	end

	if state.usedServerRagdoll and humanoid and humanoid.Health > 0 and network and type(network.InvokeServer) == "function" then
		pcall(function()
			network:InvokeServer("AttemptGetUp")
		end)
	end

	if rootPart and state.originalRootAnchored ~= nil then
		pcall(function()
			rootPart.Anchored = state.originalRootAnchored
		end)
	end

	playerState.experimentalCFrameTravel = {}
end

function beginExperimentalCFrameTravel(character)
	if not (EXPERIMENTAL_AUTOMINE_CFRAME and movementMode == "cframe" and character) then
		return false
	end

	local state = getExperimentalCFrameTravelState()
	if state.active and state.character == character then
		return true
	end
	if state.active then
		endExperimentalCFrameTravel()
		state = getExperimentalCFrameTravelState()
	end

	local playerCharacter = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
	local repChar = resolveLocalRepChar(false) or resolveLocalRepChar(true)
	local humanoid = getHumanoid(character)

	state.active = true
	state.character = character
	state.playerCharacter = playerCharacter
	state.repChar = repChar
	state.humanoid = humanoid

	if isTable(playerCharacter) and type(rawget(playerCharacter, "OnControlLost") or playerCharacter.OnControlLost) == "function" then
		local okControlLost = pcall(playerCharacter.OnControlLost, playerCharacter)
		state.usedControlLost = okControlLost and true or false
	end

	local ragdolls = ensureExperimentalSoftRagdollBypass()
	if ragdolls and isTable(playerCharacter) then
		local ragdollMethod = rawget(playerCharacter, "Ragdoll") or playerCharacter.Ragdoll
		if type(ragdollMethod) == "function" then
			local okServerRagdoll = pcall(ragdollMethod, playerCharacter, nil, true)
			state.usedServerRagdoll = okServerRagdoll and true or false
			state.usedSoftSelfRagdoll = okServerRagdoll and true or false
		end
		if not state.usedSoftSelfRagdoll then
			local toggleSelfRagdoll = rawget(playerCharacter, "ToggleSelfRagdoll") or playerCharacter.ToggleSelfRagdoll
			if type(toggleSelfRagdoll) == "function" then
				local okSoftRagdoll = pcall(toggleSelfRagdoll, playerCharacter, true)
				state.usedSoftSelfRagdoll = okSoftRagdoll and true or false
			end
		end
	end

	if humanoid then
		pcall(function()
			if type(humanoid.UnequipTools) == "function" then
				humanoid:UnequipTools()
			end
			humanoid.Jump = false
			humanoid:Move(Vector3.zero)
		end)
	end

	if isTable(playerCharacter) and not state.usedControlLost then
		if type(rawget(playerCharacter, "EquipItem") or playerCharacter.EquipItem) == "function" then
			pcall(playerCharacter.EquipItem, playerCharacter, nil, true)
		end
		if type(rawget(playerCharacter, "StopAllTracks") or playerCharacter.StopAllTracks) == "function" then
			pcall(playerCharacter.StopAllTracks, playerCharacter)
		end
	end

	enforceExperimentalCFrameFreezeState(character)
	return true
end

function requestPathServerHop(reasonText, resumeSlotIndex, forceHop, flagReason)
	if serverHopInProgress or serverHopTaskQueued then
		return false
	end

	serverHopTaskQueued = true
	task.spawn(function()
		local ok, err = xpcall(function()
			tryPathServerHop(reasonText, resumeSlotIndex, forceHop, flagReason)
		end, debug.traceback)
		if not ok then
			serverHopTaskQueued = false
			serverHopInProgress = false
			pushDebug("server hop async failed: " .. tostring(err))
		end
	end)
	return true
end

function tryServerHopAfterPathSell()
	local resumeSlotIndex = getPathCycleRestartSlotIndex()
	return requestPathServerHop("Finding a smaller public server after sell...", resumeSlotIndex, false)
end

function applyExperimentalCFrameGoal(character, goalCFrame)
	if not (character and typeof(goalCFrame) == "CFrame") then
		return false
	end

	beginExperimentalCFrameTravel(character)

	local state = getExperimentalCFrameTravelState()
	local repChar = state.repChar or resolveLocalRepChar(false)
	local rootPart = getRootPart(character)
	local moved = false

	enforceExperimentalCFrameFreezeState(character)

	if isTable(repChar) then
		repChar.LocalForceCFrame = goalCFrame
	end

	if rootPart then
		moved = pcall(function()
			rootPart.CFrame = goalCFrame
			rootPart.Anchored = true
			rootPart.Velocity = Vector3.zero
			rootPart.RotVelocity = Vector3.zero
		end)
		pcall(function()
			rootPart.AssemblyLinearVelocity = Vector3.zero
			rootPart.AssemblyAngularVelocity = Vector3.zero
		end)
	end

	return moved
end

function getInfiniteStaminaStateTables(forceRefresh)
	local states = {}
	local seen = {}

	local replicatedState = resolveGlobalReplicatedState(forceRefresh)
	if isTable(replicatedState) then
		local state = rawget(replicatedState, "State") or replicatedState.State
		if isTable(state) and not seen[state] then
			seen[state] = true
			table.insert(states, state)
		end
	end

	if #states == 0 then
		local playerCharacter = resolvePlayerCharacterModule(forceRefresh)
		if isTable(playerCharacter) then
			local state = rawget(playerCharacter, "State") or playerCharacter.State
			if isTable(state) and not seen[state] then
				seen[state] = true
				table.insert(states, state)
			end
		end
	end

	return states
end

function installInfiniteStaminaStateHook(stateTable)
	if not isTable(stateTable) then
		return false
	end

	local stateHooks = playerState.stateHooks
	if type(stateHooks) ~= "table" then
		stateHooks = {}
		playerState.stateHooks = stateHooks
	end

	local existingHook = stateHooks[stateTable]
	if existingHook and existingHook.installedMetatable and getmetatable(stateTable) == existingHook.installedMetatable then
		return true
	end

	local originalMetatable = getmetatable(stateTable)
	if originalMetatable ~= nil and type(originalMetatable) ~= "table" then
		return false
	end

	local originalIndex = originalMetatable and originalMetatable.__index or nil
	local originalNewIndex = originalMetatable and originalMetatable.__newindex or nil
	local shadowValue = rawget(stateTable, "Stamina")
	if shadowValue == nil then
		if type(originalIndex) == "function" then
			local ok, value = pcall(originalIndex, stateTable, "Stamina")
			if ok then
				shadowValue = value
			end
		elseif type(originalIndex) == "table" then
			shadowValue = rawget(originalIndex, "Stamina") or originalIndex.Stamina
		end
	end
	if shadowValue == nil then
		shadowValue = playerState.displayStamina or 100
	end

	local hook = {
		shadowValue = shadowValue,
		originalMetatable = originalMetatable,
		originalIndex = originalIndex,
		originalNewIndex = originalNewIndex,
		installedMetatable = nil,
	}

	local installedMetatable = {}
	if originalMetatable then
		for key, value in pairs(originalMetatable) do
			installedMetatable[key] = value
		end
	end

	installedMetatable.__index = function(tableRef, key)
		if key == "Stamina" then
			if playerState.infiniteStaminaEnabled then
				return playerState.displayStamina or 100
			end
			return hook.shadowValue
		end

		local original = hook.originalIndex
		if type(original) == "function" then
			return original(tableRef, key)
		end
		if type(original) == "table" then
			return original[key]
		end
		return nil
	end

	installedMetatable.__newindex = function(tableRef, key, value)
		if key == "Stamina" then
			hook.shadowValue = value
			return
		end

		local original = hook.originalNewIndex
		if type(original) == "function" then
			return original(tableRef, key, value)
		end
		if type(original) == "table" then
			original[key] = value
			return
		end
		rawset(tableRef, key, value)
	end

	hook.installedMetatable = installedMetatable
	stateHooks[stateTable] = hook

	local okSet = pcall(function()
		setmetatable(stateTable, installedMetatable)
	end)
	if not okSet then
		stateHooks[stateTable] = nil
		return false
	end

	pcall(function()
		rawset(stateTable, "Stamina", nil)
	end)

	return true
end

playerState.safeTeleportBlockedRemotes = {
	DamageSelf = true,
	CharUpdate = true,
	CharacterReplicate = true,
	ACDTResponse = true,
}

playerState.safeTeleportBlockedFlags = {
	DamageSelf = true,
	LowerStamina = true,
	CharacterReplicate = true,
}

function hasPlayerStateNetworkHookEnabled()
	return playerState.infiniteStaminaEnabled
        or playerState.safeTeleportEnabled
        or playerState.noFallDamageEnabled
        or S.silentAimEnabled
        or S.fastProjectileEnabled
        or S.noSpreadEnabled
end

function hasPlayerStateFeatureHooksEnabled()
	return hasPlayerStateNetworkHookEnabled()
		or playerState.noJumpCooldownEnabled
		or playerState.noRagdollEnabled
		or playerState.autoBreakFreeEnabled
		or playerState.autoGetUpEnabled
        or playerState.equipWhileSwimmingEnabled
        or hasGunFeatureHooksEnabled()
end

function shouldBlockPlayerStateRemote(remoteName)
	if playerState.infiniteStaminaEnabled and remoteName == "LowerStamina" then
		return true
	end

	if playerState.noFallDamageEnabled and (remoteName == "DamageSelf" or remoteName == "TrainSmack") then
		return true
	end

	return playerState.safeTeleportEnabled and playerState.safeTeleportBlockedRemotes[remoteName] == true
end

function restorePlayerStateNetworkHook()
	if playerState.hookedNetwork
		and playerState.originalNetworkFireServer
		and playerState.hookedNetwork.FireServer == playerState.networkFireServerWrapper then
		playerState.hookedNetwork.FireServer = playerState.originalNetworkFireServer
	end

	playerState.hookedNetwork = nil
	playerState.originalNetworkFireServer = nil
	playerState.networkFireServerWrapper = nil
end

function ensurePlayerStateNetworkHook()
	if not hasPlayerStateNetworkHookEnabled() then
		restorePlayerStateNetworkHook()
		return
	end

	local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
	if not network or type(network.FireServer) ~= "function" then
		return
	end

	if network ~= playerState.hookedNetwork or network.FireServer ~= playerState.networkFireServerWrapper then
		restorePlayerStateNetworkHook()
		playerState.hookedNetwork = network
		playerState.originalNetworkFireServer = network.FireServer
		playerState.networkFireServerWrapper = function(self, remoteName, ...)
			if shouldBlockPlayerStateRemote(remoteName) then
				return nil
			end
            local packedArgs = table.pack(...)
            if shouldSuppressTrackedFastProjectileRemote(remoteName, packedArgs) then
                return nil
            end
            packedArgs = processGunProjectileRemote(remoteName, packedArgs) or packedArgs
			return playerState.originalNetworkFireServer(self, remoteName, table.unpack(packedArgs, 1, packedArgs.n))
		end
		network.FireServer = playerState.networkFireServerWrapper
	end
end

function restoreInfiniteStaminaHooks()
	if type(playerState.stateHooks) == "table" then
		for stateTable, hook in pairs(playerState.stateHooks) do
			if isTable(stateTable) and type(hook) == "table" then
				pcall(function()
					if hook.installedMetatable and getmetatable(stateTable) == hook.installedMetatable then
						setmetatable(stateTable, hook.originalMetatable)
					end
				end)
				pcall(function()
					rawset(stateTable, "Stamina", hook.shadowValue)
				end)
			end
		end
	end

	playerState.stateHooks = {}
end

function ensureInfiniteStaminaHooks()
	if not playerState.infiniteStaminaEnabled then
		return
	end

	local stateTables = getInfiniteStaminaStateTables(false)
	if #stateTables == 0 then
		stateTables = getInfiniteStaminaStateTables(true)
	end
	for _, stateTable in ipairs(stateTables) do
		installInfiniteStaminaStateHook(stateTable)
	end
end

function restoreSafeTeleportHooks()
	local handler = playerState.hookedSafeTeleportHandler
	if isTable(handler) then
		if playerState.originalSafeTeleportPlatformUpdate and handler._UpdateLocalCharPlatform == playerState.safeTeleportPlatformWrapper then
			handler._UpdateLocalCharPlatform = playerState.originalSafeTeleportPlatformUpdate
		end
		if playerState.originalSafeTeleportReplicateCharacters and handler.ReplicateCharacters == playerState.safeTeleportReplicateWrapper then
			handler.ReplicateCharacters = playerState.originalSafeTeleportReplicateCharacters
		end
	end

	local flagsMetatable = playerState.hookedSafeTeleportFlagsMetatable
	if type(flagsMetatable) == "table" and flagsMetatable.__newindex == playerState.safeTeleportFlagsNewIndexWrapper then
		flagsMetatable.__newindex = playerState.originalSafeTeleportFlagsNewIndex
	end

	local ragdollOwner = playerState.hookedSafeTeleportRagdollOwner
	if isTable(ragdollOwner) and playerState.originalSafeTeleportRagdoll and ragdollOwner.Ragdoll == playerState.safeTeleportRagdollWrapper then
		ragdollOwner.Ragdoll = playerState.originalSafeTeleportRagdoll
	end

	playerState.hookedSafeTeleportHandler = nil
	playerState.originalSafeTeleportPlatformUpdate = nil
	playerState.safeTeleportPlatformWrapper = nil
	playerState.originalSafeTeleportReplicateCharacters = nil
	playerState.safeTeleportReplicateWrapper = nil
	playerState.hookedSafeTeleportFlagsMetatable = nil
	playerState.originalSafeTeleportFlagsNewIndex = nil
	playerState.safeTeleportFlagsNewIndexWrapper = nil
	playerState.hookedSafeTeleportRagdollOwner = nil
	playerState.originalSafeTeleportRagdoll = nil
	playerState.safeTeleportRagdollWrapper = nil
end

function ensureSafeTeleportHooks()
	if not (playerState.safeTeleportEnabled or playerState.noRagdollEnabled) then
		return
	end

	local handler = resolveSafeTeleportHandler(false) or resolveSafeTeleportHandler(true)
	if handler and handler ~= playerState.hookedSafeTeleportHandler then
		restoreSafeTeleportHooks()
		playerState.hookedSafeTeleportHandler = handler
	end

	handler = playerState.hookedSafeTeleportHandler
	if isTable(handler) then
		local updateLocalCharPlatform = rawget(handler, "_UpdateLocalCharPlatform") or handler._UpdateLocalCharPlatform
		if type(updateLocalCharPlatform) == "function" and updateLocalCharPlatform ~= playerState.safeTeleportPlatformWrapper then
			playerState.originalSafeTeleportPlatformUpdate = updateLocalCharPlatform
			playerState.safeTeleportPlatformWrapper = function(...)
				if playerState.safeTeleportEnabled then
					return nil
				end
				return playerState.originalSafeTeleportPlatformUpdate(...)
			end
			handler._UpdateLocalCharPlatform = playerState.safeTeleportPlatformWrapper
		end

		local replicateCharacters = rawget(handler, "ReplicateCharacters") or handler.ReplicateCharacters
		if type(replicateCharacters) == "function" and replicateCharacters ~= playerState.safeTeleportReplicateWrapper then
			playerState.originalSafeTeleportReplicateCharacters = replicateCharacters
			playerState.safeTeleportReplicateWrapper = function(self, ...)
				if playerState.safeTeleportEnabled then
					return nil
				end
				return playerState.originalSafeTeleportReplicateCharacters(self, ...)
			end
			handler.ReplicateCharacters = playerState.safeTeleportReplicateWrapper
		end

		local flags = rawget(handler, "Flags") or handler.Flags
		local flagsMetatable = isTable(flags) and getmetatable(flags) or nil
		if type(flagsMetatable) == "table" and type(flagsMetatable.__newindex) == "function"
			and flagsMetatable.__newindex ~= playerState.safeTeleportFlagsNewIndexWrapper then
			local originalNewIndex = flagsMetatable.__newindex
			playerState.hookedSafeTeleportFlagsMetatable = flagsMetatable
			playerState.originalSafeTeleportFlagsNewIndex = originalNewIndex
			playerState.safeTeleportFlagsNewIndexWrapper = function(tableRef, key, value)
				if playerState.safeTeleportEnabled and playerState.safeTeleportBlockedFlags[key] then
					return
				end
				return originalNewIndex(tableRef, key, value)
			end
			flagsMetatable.__newindex = playerState.safeTeleportFlagsNewIndexWrapper
		end
	end

	local ragdollOwner = resolveSafeTeleportRagdollOwner(false) or resolveSafeTeleportRagdollOwner(true)
	if ragdollOwner and ragdollOwner ~= playerState.hookedSafeTeleportRagdollOwner then
		local previousOwner = playerState.hookedSafeTeleportRagdollOwner
		if isTable(previousOwner) and playerState.originalSafeTeleportRagdoll and previousOwner.Ragdoll == playerState.safeTeleportRagdollWrapper then
			previousOwner.Ragdoll = playerState.originalSafeTeleportRagdoll
		end

		local ragdoll = rawget(ragdollOwner, "Ragdoll") or ragdollOwner.Ragdoll
		if type(ragdoll) == "function" then
			playerState.hookedSafeTeleportRagdollOwner = ragdollOwner
			playerState.originalSafeTeleportRagdoll = ragdoll
			playerState.safeTeleportRagdollWrapper = function(...)
				local experimentalState = getExperimentalCFrameTravelState()
				local allowExperimentalRagdoll = type(experimentalState) == "table" and experimentalState.active == true
				if (playerState.safeTeleportEnabled or playerState.noRagdollEnabled) and not allowExperimentalRagdoll then
					return nil
				end
				return playerState.originalSafeTeleportRagdoll(...)
			end
			ragdollOwner.Ragdoll = playerState.safeTeleportRagdollWrapper
		end
	end
end

function restoreNoJumpCooldownHooks()
    if type(playerState.jumpHookConnections) == "table" then
        for _, connection in ipairs(playerState.jumpHookConnections) do
            if connection and type(connection.Enable) == "function" then
                pcall(function()
                    connection:Enable()
                end)
            end
        end
    end

    playerState.jumpHookHumanoid = nil
    playerState.jumpHookConnections = {}
end

function ensureNoJumpCooldownHooks()
    if not playerState.noJumpCooldownEnabled then
        return
    end
    if type(getconnections) ~= "function" then
        return
    end

    local character = getWorldCharacterForPlayer(localPlayer) or localPlayer.Character
    local humanoid = getCharacterHumanoid(character)
    if not humanoid then
        restoreNoJumpCooldownHooks()
        return
    end

    if playerState.jumpHookHumanoid == humanoid and type(playerState.jumpHookConnections) == "table" and #playerState.jumpHookConnections > 0 then
        return
    end

    restoreNoJumpCooldownHooks()
    playerState.jumpHookHumanoid = humanoid
    playerState.jumpHookConnections = {}

    local okConnections, connections = pcall(function()
        return getconnections(humanoid:GetPropertyChangedSignal("Jump"))
    end)
    if not okConnections or type(connections) ~= "table" then
        return
    end

    for _, connection in ipairs(connections) do
        if connection and type(connection.Disable) == "function" then
            local okDisable = pcall(function()
                connection:Disable()
            end)
            if okDisable and type(connection.Enable) == "function" then
                table.insert(playerState.jumpHookConnections, connection)
            end
        end
    end
end

function restoreAutoBreakFreeHooks()
    if playerState.breakFreeOwner and playerState.breakFreeHook then
        local owner = playerState.breakFreeOwner
        local hook = playerState.breakFreeHook
        pcall(function()
            if hook.installedMetatable and getmetatable(owner) == hook.installedMetatable then
                setmetatable(owner, hook.originalMetatable)
            end
        end)
        pcall(function()
            rawset(owner, "BreakFreePerc", hook.shadowValue)
        end)
    end

    playerState.breakFreeOwner = nil
    playerState.breakFreeHook = nil
end

function installAutoBreakFreeHook(owner)
    if not isTable(owner) then
        return false
    end
    if playerState.breakFreeOwner == owner and playerState.breakFreeHook then
        return true
    end

    restoreAutoBreakFreeHooks()

    local originalMetatable = getmetatable(owner)
    if type(originalMetatable) ~= "table" then
        originalMetatable = {}
    end

    local originalIndex = originalMetatable.__index
    local originalNewIndex = originalMetatable.__newindex
    local shadowValue = rawget(owner, "BreakFreePerc")

    if shadowValue == nil then
        if type(originalIndex) == "function" then
            local okIndex, value = pcall(originalIndex, owner, "BreakFreePerc")
            if okIndex then
                shadowValue = value
            end
        elseif type(originalIndex) == "table" then
            shadowValue = rawget(originalIndex, "BreakFreePerc") or originalIndex.BreakFreePerc
        end
    end

    local installedMetatable = {}
    for key, value in pairs(originalMetatable) do
        installedMetatable[key] = value
    end

    installedMetatable.__index = function(tableRef, key)
        if key == "BreakFreePerc" then
            return shadowValue
        end
        if type(originalIndex) == "function" then
            return originalIndex(tableRef, key)
        elseif type(originalIndex) == "table" then
            return originalIndex[key]
        end
        return rawget(tableRef, key)
    end

    installedMetatable.__newindex = function(tableRef, key, value)
        if key == "BreakFreePerc" then
            shadowValue = value
            if playerState.autoBreakFreeEnabled and type(value) == "number" and value > 0 then
                shadowValue = 1
            end
            return
        end
        if type(originalNewIndex) == "function" then
            return originalNewIndex(tableRef, key, value)
        end
        rawset(tableRef, key, value)
    end

    local okSet = pcall(function()
        setmetatable(owner, installedMetatable)
    end)
    if not okSet then
        return false
    end

    playerState.breakFreeOwner = owner
    playerState.breakFreeHook = {
        originalMetatable = originalMetatable,
        installedMetatable = installedMetatable,
        shadowValue = shadowValue,
    }

    pcall(function()
        rawset(owner, "BreakFreePerc", nil)
    end)

    return true
end

function ensureAutoBreakFreeHooks()
    if not playerState.autoBreakFreeEnabled then
        return
    end

    local owner = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
    if installAutoBreakFreeHook(owner) and playerState.breakFreeHook and type(playerState.breakFreeHook.shadowValue) == "number" and playerState.breakFreeHook.shadowValue > 0 then
        playerState.breakFreeHook.shadowValue = 1
    end
end

function runPlayerFeaturePulse()
    local experimentalState = getExperimentalCFrameTravelState()
    local experimentalTravelActive = type(experimentalState) == "table" and experimentalState.active == true
    if not experimentalTravelActive and (playerState.noRagdollEnabled or playerState.autoGetUpEnabled) then
        local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
        local playerCharacter = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
        local characterModel = getWorldCharacterForPlayer(localPlayer) or localPlayer.Character
        local humanoid = getCharacterHumanoid(characterModel)
        if network
            and playerCharacter
            and humanoid
            and humanoid.Health > 0
            and type(playerCharacter.CanGetUp) == "function"
            and playerCharacter:CanGetUp() then
            pcall(function()
                humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end)
            if type(playerCharacter.ToggleSelfRagdoll) == "function" then
                pcall(function()
                    playerCharacter:ToggleSelfRagdoll(false)
                end)
            end
            if type(network.InvokeServer) == "function" then
                pcall(function()
                    network:InvokeServer("AttemptGetUp")
                end)
            end
        end
    end
end

function ensurePlayerFeatureHooks()
	ensurePlayerStateNetworkHook()

	if playerState.infiniteStaminaEnabled then
		ensureInfiniteStaminaHooks()
	else
		restoreInfiniteStaminaHooks()
	end

	if playerState.safeTeleportEnabled or playerState.noRagdollEnabled then
		ensureSafeTeleportHooks()
	else
		restoreSafeTeleportHooks()
	end

    if playerState.noJumpCooldownEnabled then
        ensureNoJumpCooldownHooks()
    else
        restoreNoJumpCooldownHooks()
    end

    if playerState.autoBreakFreeEnabled then
        ensureAutoBreakFreeHooks()
    else
        restoreAutoBreakFreeHooks()
    end

    ensureGunFeatureHooks()

    runPlayerFeaturePulse()
    runGunFeaturePulse()

	if hasPlayerStateFeatureHooksEnabled() and not playerState.hookRefreshActive then
		playerState.hookRefreshActive = true
		task.spawn(function()
			while hasPlayerStateFeatureHooksEnabled() do
				ensurePlayerStateNetworkHook()
				if playerState.infiniteStaminaEnabled then
					ensureInfiniteStaminaHooks()
				end
				if playerState.safeTeleportEnabled or playerState.noRagdollEnabled then
					ensureSafeTeleportHooks()
				end
                if playerState.noJumpCooldownEnabled then
                    ensureNoJumpCooldownHooks()
                end
                if playerState.autoBreakFreeEnabled then
                    ensureAutoBreakFreeHooks()
                end
                ensureGunFeatureHooks()
                runPlayerFeaturePulse()
                runGunFeaturePulse()
				task.wait(0.35)
			end
			restorePlayerStateNetworkHook()
            restoreNoJumpCooldownHooks()
            restoreAutoBreakFreeHooks()
            restoreGunFeatureHooks()
			playerState.hookRefreshActive = false
		end)
	end
end

function resolveGlobalMiningHandler(forceRefresh)
	local globalModule = rawget(_G, "__AutoMineGlobalModule")
	if not isTable(globalModule) then
		resolveGlobalNetwork(forceRefresh)
		globalModule = rawget(_G, "__AutoMineGlobalModule")
	end
	if not isTable(globalModule) then
		return nil
	end

	local handler = rawget(globalModule, "MiningHandler") or globalModule.MiningHandler
	if isTable(handler) and type(handler.HitDeposit) == "function" then
		return handler
	end

	if type(globalModule.LoadModule) == "function" then
		local okLoad, loadedHandler = pcall(function()
			return globalModule:LoadModule("MiningHandler")
		end)
		if okLoad and isTable(loadedHandler) and type(loadedHandler.HitDeposit) == "function" then
			globalModule.MiningHandler = loadedHandler
			return loadedHandler
		end
	end

	return nil
end

function tryMineViaGlobalNetwork(character, targetOre, equippedPickaxe)
	if not mineAuraEnabled or not targetOre then
		return false
	end

	local network = resolveGlobalNetwork(false)
	if not network then
		network = resolveGlobalNetwork(true)
	end
	if not network then
		return false
	end

	local orePart = getOreBasePart(targetOre)
	local orePosition = getOrePosition(targetOre)
	if not orePosition then
		return false
	end
	local depositModel = targetOre
	if orePart and orePart.Parent then
		depositModel = orePart.Parent
	end

	local toolPart = nil
	local activePickaxe = equippedPickaxe or getEquippedPickaxe(character)
	if activePickaxe then
		toolPart = activePickaxe:FindFirstChild("HitBox", true)
			or activePickaxe:FindFirstChild("Hitbox", true)
			or activePickaxe:FindFirstChild("Handle", true)
	end

	local toolPos = orePosition
	local toolVec = Vector3.new(0, -1, 0)
	if toolPart and toolPart:IsA("BasePart") then
		local halfWidth = toolPart.Size.X * 0.5
		toolPos = toolPart.CFrame * Vector3.new(halfWidth, 0, 0)
		toolVec = -toolPart.CFrame.XVector
	end

	local miningHandler = resolveGlobalMiningHandler(false)
	if not miningHandler then
		miningHandler = resolveGlobalMiningHandler(true)
	end

	local localHitAllowed = true
	if miningHandler and depositModel then
		local okHit, hitAllowed = pcall(function()
			return miningHandler:HitDeposit(depositModel, toolPos, toolVec)
		end)
		if okHit then
			localHitAllowed = hitAllowed == true
			if not localHitAllowed then
				pushDebugRateLimited("mine_aura_local_reject", "mine aura local hit rejected", 2)
			end
		else
			localHitAllowed = false
			pushDebugRateLimited("mine_aura_local_error", "mine aura local hit errored", 2)
		end
	end

	if not localHitAllowed then
		return false
	end

	local syncedTime = getSyncedNetworkTime()
	local swingOk = pcall(network.FireServer, network, "MeleeSwing", syncedTime, "Swing")
	local mineOk = pcall(network.FireServer, network, "MineDeposit", syncedTime, depositModel, toolPos, toolVec)
		or pcall(network.FireServer, network, "MineDeposit", syncedTime, targetOre, toolPos, toolVec)
		or pcall(network.FireServer, network, "MineDeposit", syncedTime, orePart, toolPos, toolVec)
		or pcall(network.FireServer, network, "MineDeposit", syncedTime, orePart and orePart.Parent, toolPos, toolVec)
		or pcall(network.FireServer, network, "MineDeposit", syncedTime, depositModel, orePosition, toolVec)
		or pcall(network.FireServer, network, "MineDeposit", syncedTime, targetOre, orePosition, toolVec)
		or pcall(network.FireServer, network, "MineDeposit", syncedTime, orePart, orePosition, toolVec)

	if swingOk or mineOk then
		pushDebugRateLimited("mine_aura_global", "mine aura global network: " .. targetOre.Name, 2)
		return true
	end

	return false
end

function pushMineAuraDebugThrottled(message)
	local now = os.clock()
	if (now - lastMineAuraDebugAt) > 2 then
		lastMineAuraDebugAt = now
		pushDebug(message)
	end
end

function probeToolInfoCandidates(character)
	if type(getgc) ~= "function" then
		return
	end

	local now = os.clock()
	if (now - lastAuraProbeAt) < 8 then
		return
	end
	lastAuraProbeAt = now

	local ok, gcObjects = pcall(getgc, true)
	if not ok or type(gcObjects) ~= "table" then
		return
	end

	local bestSummary = {}
	local bestScores = {}
	local deadline = os.clock() + TOOLINFO_DIRECT_SCAN_BUDGET
	for _, object in ipairs(gcObjects) do
		if os.clock() > deadline then
			break
		end

		if isTable(object) and looksLikeToolInfo(object) then
			local score = scoreToolInfoCandidate(object, character)
			if score >= 4 then
				local index = #bestScores + 1
				bestScores[index] = score
				bestSummary[index] = object
				for i = index, 2, -1 do
					if bestScores[i] > bestScores[i - 1] then
						bestScores[i], bestScores[i - 1] = bestScores[i - 1], bestScores[i]
						bestSummary[i], bestSummary[i - 1] = bestSummary[i - 1], bestSummary[i]
					end
				end
				if #bestScores > 2 then
					table.remove(bestScores)
					table.remove(bestSummary)
				end
			end
		end
	end

	if #bestSummary == 0 then
		pushDebug("aura probe: no candidate toolInfo table")
		return
	end

	local chunks = {}
	for i, candidate in ipairs(bestSummary) do
		local model = rawget(candidate, "Model") or candidate.Model
		local nameHint = rawget(candidate, "Name")
			or candidate.Name
			or rawget(candidate, "ItemName")
			or candidate.ItemName
			or model
		local isEquipped = rawget(candidate, "IsEquipped")
		if isEquipped == nil then
			isEquipped = candidate.IsEquipped
		end
		local hasHitbox = getToolInfoHitbox(candidate) ~= nil
		chunks[i] = string.format(
			"%s eq=%s hb=%s sc=%d",
			tostring(nameHint),
			tostring(isEquipped),
			tostring(hasHitbox),
			bestScores[i]
		)
	end

	pushDebug("aura probe: " .. table.concat(chunks, " | "))
end

function tryMineViaToolInfoNetwork(character, targetOre)
	if not mineAuraEnabled then
		return false
	end

	local toolInfo = resolveToolInfo(false, character)
	if not toolInfo then
		local now = os.clock()
		local lastDirectScan = lastDirectToolInfoScanAt or 0
		if (now - lastAuraForceScanAt) >= AURA_FORCE_SCAN_INTERVAL and (now - lastDirectScan) >= AURA_FORCE_SCAN_INTERVAL then
			lastAuraForceScanAt = now
			toolInfo = resolveToolInfo(true, character)
		end
	end

	if not looksLikeToolInfo(toolInfo) then
		if not auraCapabilitiesReported then
			auraCapabilitiesReported = true
			pushDebug(string.format(
				"aura env getgc=%s getgenv=%s getrenv=%s",
				tostring(type(getgc) == "function"),
				tostring(type(getgenv) == "function"),
				tostring(type(getrenv) == "function")
			))
		end
		probeToolInfoCandidates(character)
		pushMineAuraDebugThrottled("mine aura: tool info not found")
		return false
	end

	if not isLikelyMiningToolInfo(toolInfo, character) then
		return false
	end

	if rawget(toolInfo, "IsEquipped") == false or toolInfo.IsEquipped == false then
		return false
	end

	local toolCharacter = getToolInfoCharacter(toolInfo, character)
	local auraTarget = findNearestOreWithinRange(toolCharacter, targetOre, MINE_AURA_RANGE, true)
	if not auraTarget then
		return false
	end
	if auraTarget ~= targetOre and not isOreNameSelected(getOreGroupName(auraTarget)) then
		pushDebugRateLimited("mine_aura_any_ore_fallback", "mine aura fallback target: " .. tostring(auraTarget.Name), 2)
	end

	local auraTargetPart = getOreBasePart(auraTarget)
	local toolHitbox = getToolInfoHitbox(toolInfo)
	if not auraTargetPart or not toolHitbox then
		pushMineAuraDebugThrottled("mine aura: hitbox/target part missing")
		return false
	end

	local now = tick()
	local cooldown = tonumber(rawget(toolInfo, "Cooldown") or toolInfo.Cooldown) or 1
	if (now - lastMineAuraNetworkHitAt) < cooldown then
		return false
	end

	local syncedTime = getSyncedNetworkTime()
	local halfWidth = toolHitbox.Size.X * 0.5
	local toolPos = toolHitbox.CFrame * Vector3.new(halfWidth, 0, 0)
	local toolVec = -toolHitbox.CFrame.XVector
	local depositModel = auraTarget
	if auraTargetPart and auraTargetPart.Parent then
		depositModel = auraTargetPart.Parent
	end

	local miningHandler = resolveGlobalMiningHandler(false)
	if not miningHandler then
		miningHandler = resolveGlobalMiningHandler(true)
	end
	if miningHandler and depositModel then
		local okHit, hitAllowed = pcall(function()
			return miningHandler:HitDeposit(depositModel, toolPos, toolVec)
		end)
		if not okHit then
			pushMineAuraDebugThrottled("mine aura: local hit errored")
			return false
		end
		if hitAllowed ~= true then
			pushMineAuraDebugThrottled("mine aura: local hit rejected")
			return false
		end
	end

	local swingSent = callToolNetworkActivate(toolInfo, "MeleeSwing", syncedTime, "Swing")
	local mineSent = callToolNetworkActivate(toolInfo, "MineDeposit", syncedTime, depositModel, toolPos, toolVec)
		or callToolNetworkActivate(toolInfo, "MineDeposit", syncedTime, auraTarget, toolPos, toolVec)
		or callToolNetworkActivate(toolInfo, "MineDeposit", syncedTime, auraTargetPart, toolPos, toolVec)
		or callToolNetworkActivate(toolInfo, "MineDeposit", syncedTime, auraTargetPart.Parent, toolPos, toolVec)

	if swingSent or mineSent then
		lastMineAuraNetworkHitAt = now
		pushMineAuraDebugThrottled("mine aura network: " .. auraTarget.Name)
		return true
	end

	return false
end

function tapKey(keyCode)
	if not keyCode or not VirtualInputManager then
		return false
	end

	local ok = pcall(function()
		VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
		task.wait(0.05)
		VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
	end)

	return ok
end

function tryHotbarKeys(character)
	if PICKAXE_HOTBAR_KEY then
		return tapKey(PICKAXE_HOTBAR_KEY)
	end

	for _, keyCode in ipairs(HOTBAR_KEYS_TO_TRY) do
		if tapKey(keyCode) then
			task.wait(0.12)
			local equippedPickaxe = getEquippedPickaxe(character)
			if equippedPickaxe then
				pushDebug("hotbar equip worked with " .. keyCode.Name)
				return true
			end
		end
	end

	return false
end

function clickMouse()
	ignoreToggleUntil = os.clock() + 0.75

	if VirtualInputManager then
		local camera = workspace.CurrentCamera
		if camera then
			local viewport = camera.ViewportSize
			local x = math.floor(viewport.X * 0.5)
			local y = math.floor(viewport.Y * 0.5)

			local ok = pcall(function()
				VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
				task.wait(0.05)
				VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
			end)

			if ok then
				return true
			end
		end
	end

	if type(mouse1press) == "function" and type(mouse1release) == "function" then
		local ok = pcall(function()
			mouse1press()
			task.wait(0.05)
			mouse1release()
		end)
		if ok then
			return true
		end
	end

	if type(mouse1click) == "function" then
		return pcall(mouse1click)
	end

	return false
end

function equipPickaxe(character, humanoid, pickaxe)
	if not (character and humanoid) then
		pushDebug("equip skipped: missing character/humanoid")
		return false
	end
	if humanoid.Health <= 0 then
		return false
	end
	endExperimentalCFrameTravel(character)

	local equippedPickaxe = getEquippedPickaxe(character)
	if equippedPickaxe then
		cachedPickaxe = equippedPickaxe
		lastPickaxeEquipRequestAt = 0
		lastPickaxeEquipRequestId = nil
		lastPickaxeLoadoutRequestAt = 0
		lastPickaxeLoadoutRequestId = nil
		setStatus("Pickaxe equipped", Color3.fromRGB(140, 220, 140))
		return true
	end

	local storedPickaxe = getStoredPickaxe(character)
	if storedPickaxe then
		cachedPickaxe = storedPickaxe
		pushDebugRateLimited("stored_pickaxe_found", "stored pickaxe found: " .. storedPickaxe:GetFullName(), 2)
	elseif pickaxe and pickaxe.Parent then
		cachedPickaxe = pickaxe
	end

	if pickaxe and pickaxe:IsA("Tool") then
		if pickaxe.Parent ~= character then
			pcall(function()
				humanoid:EquipTool(pickaxe)
			end)
			task.wait(0.1)
		end

		return pickaxe.Parent == character
	end

	local playerCharacter = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
	local playerData = resolvePlayerDataModule(false) or resolvePlayerDataModule(true)
	local playerItems = resolveSharedPlayerItems(false) or resolveSharedPlayerItems(true)

	local loadoutItem, loadoutType, loadoutIndex = findPickaxeLoadoutItem(playerData, playerCharacter, playerItems)
	local inventoryItem = findInventoryPickaxeItem(playerData, playerItems, loadoutItem)
	local loadoutItemScore = scorePickaxeItemCandidate(loadoutItem, playerItems)
	local inventoryItemScore = scorePickaxeItemCandidate(inventoryItem, playerItems)
	local preferredPickaxeItem = inventoryItemScore > loadoutItemScore and inventoryItem or loadoutItem
	local activePickaxeItem = preferredPickaxeItem or inventoryItem or loadoutItem
	local visualItem = getVisualEquippedItemData(playerCharacter)

	if isLikelyPickaxeItem(visualItem, playerItems) then
		local switching = isTable(playerCharacter) and ((rawget(playerCharacter, "IsSwitchingItems") or playerCharacter.IsSwitchingItems) == true)
		setStatus(switching and "Switching to pickaxe" or "Waiting for pickaxe model", Color3.fromRGB(255, 190, 120))
		return false
	end

	if inventoryItem and isTable(playerData) and (not loadoutItem or inventoryItemScore > loadoutItemScore) then
		local setLoadout = rawget(playerData, "SetLoadout") or playerData.SetLoadout
		local slotType = getPickaxeEquipmentType(inventoryItem, playerItems, playerData, playerCharacter)
		local slotIndex = loadoutIndex or findPreferredLoadoutSlot(playerData, slotType, rawget(inventoryItem, "Id") or inventoryItem.Id)
		local inventoryItemId = rawget(inventoryItem, "Id") or inventoryItem.Id or rawget(inventoryItem, "Type") or inventoryItem.Type or PICKAXE_NAME

		if type(setLoadout) == "function" and type(slotType) == "string" and slotType ~= "" and slotIndex ~= nil then
			local now = os.clock()
			if lastPickaxeLoadoutRequestId == inventoryItemId and (now - lastPickaxeLoadoutRequestAt) < PICKAXE_LOADOUT_REQUEST_COOLDOWN then
				setStatus("Waiting for pickaxe hotbar slot", Color3.fromRGB(255, 190, 120))
				return false
			end

			local okSetLoadout = pcall(setLoadout, playerData, slotType, slotIndex, inventoryItem)
			if okSetLoadout then
				lastPickaxeLoadoutRequestId = inventoryItemId
				lastPickaxeLoadoutRequestAt = now
				pushDebug(string.format("assigned pickaxe to %s slot %s", slotType, tostring(slotIndex)))
				setStatus("Adding pickaxe to hotbar", Color3.fromRGB(255, 190, 120))
				task.wait(0.08)
				return false
			end
		end
	end

	if loadoutItem and isTable(playerCharacter) and (not inventoryItem or loadoutItemScore >= inventoryItemScore) then
		local currentItem = getCurrentEquippedItemData(playerCharacter)
		local canSwitchToItem = rawget(playerCharacter, "CanSwitchToItem") or playerCharacter.CanSwitchToItem
		local equipItem = rawget(playerCharacter, "EquipItem") or playerCharacter.EquipItem
		local loadoutItemId = rawget(loadoutItem, "Id") or loadoutItem.Id or rawget(loadoutItem, "Type") or loadoutItem.Type or PICKAXE_NAME

		if isLikelyPickaxeItem(currentItem, playerItems) then
			setStatus("Waiting for pickaxe model", Color3.fromRGB(255, 190, 120))
			return false
		end

		if type(canSwitchToItem) == "function" then
			local okCanSwitch, canSwitch = pcall(canSwitchToItem, playerCharacter, loadoutItem, true)
			if okCanSwitch and not canSwitch then
				if hasPendingLoadoutSync(playerData) then
					setStatus("Waiting for loadout sync", Color3.fromRGB(255, 190, 120))
				else
					setStatus("Pickaxe equip blocked", Color3.fromRGB(255, 190, 120))
				end
				return false
			end
		elseif hasPendingLoadoutSync(playerData) then
			setStatus("Waiting for loadout sync", Color3.fromRGB(255, 190, 120))
			return false
		end

		if (rawget(playerCharacter, "IsSwitchingItems") or playerCharacter.IsSwitchingItems)
			or (rawget(playerCharacter, "ItemSwitchDisabled") or playerCharacter.ItemSwitchDisabled) then
			setStatus("Waiting for item switch", Color3.fromRGB(255, 190, 120))
			return false
		end

		if type(equipItem) == "function" then
			local now = os.clock()
			if lastPickaxeEquipRequestId == loadoutItemId and (now - lastPickaxeEquipRequestAt) < PICKAXE_EQUIP_REQUEST_COOLDOWN then
				return false
			end

			local okEquip = pcall(equipItem, playerCharacter, loadoutItem, true, true)
			if okEquip then
				lastPickaxeEquipRequestId = loadoutItemId
				lastPickaxeEquipRequestAt = now
				pushDebug("requested pickaxe equip: " .. tostring(loadoutItemId))
				setStatus("Equipping pickaxe", Color3.fromRGB(255, 190, 120))
				task.wait(0.08)
				equippedPickaxe = getEquippedPickaxe(character)
				if equippedPickaxe then
					cachedPickaxe = equippedPickaxe
					setStatus("Pickaxe equipped", Color3.fromRGB(140, 220, 140))
					return true
				end
				storedPickaxe = getStoredPickaxe(character)
				if storedPickaxe then
					cachedPickaxe = storedPickaxe
				end
				return false
			end
		end
	end

	if not playerCharacter and not playerData then
		if PICKAXE_HOTBAR_KEY and tapKey(PICKAXE_HOTBAR_KEY) then
			task.wait(0.15)
			equippedPickaxe = getEquippedPickaxe(character)
			if equippedPickaxe then
				cachedPickaxe = equippedPickaxe
				setStatus("Equipped via hotbar key", Color3.fromRGB(140, 220, 140))
				pushDebug("equipped using configured hotbar key")
				return true
			end
		end

		if tryHotbarKeys(character) then
			equippedPickaxe = getEquippedPickaxe(character)
			if equippedPickaxe then
				cachedPickaxe = equippedPickaxe
				setStatus("Equipped via hotbar scan", Color3.fromRGB(140, 220, 140))
				return true
			end
		end

		local remote = getEquipRemote(true)
		if remote then
			pushDebug("trying equip remote: " .. remote:GetFullName())
			local fired = false
			local nameHints = collectPickaxeNameHints(
				activePickaxeItem,
				inventoryItem,
				loadoutItem,
				storedPickaxe,
				pickaxe,
				equippedPickaxe,
				PICKAXE_NAME,
				STORED_PICKAXE_NAME
			)
			for _, hint in ipairs(nameHints) do
				local normalizedHint = stripStoredPickaxePrefix(hint) or hint
				fired = triggerRemote(remote, hint)
					or triggerRemote(remote, normalizedHint)
					or triggerRemote(remote, { Item = hint })
					or triggerRemote(remote, { Item = normalizedHint })
					or triggerRemote(remote, { Item = "LoadoutItem/" .. normalizedHint })
				if fired then
					break
				end
			end
			if not fired then
				fired = triggerRemote(remote, storedPickaxe or pickaxe or activePickaxeItem)
			end

			if fired then
				task.wait(0.2)
				equippedPickaxe = getEquippedPickaxe(character)
				if equippedPickaxe then
					cachedPickaxe = equippedPickaxe
					setStatus("Equipped via remote", Color3.fromRGB(140, 220, 140))
					pushDebug("equip remote worked")
					return true
				end
			end
		end
	end

	if not activePickaxeItem and not storedPickaxe and not pickaxe then
		setStatus("Pickaxe not found in loadout/inventory", Color3.fromRGB(255, 190, 120))
		pushDebugRateLimited("equip_pickaxe_missing", "pickaxe item data not found", 2)
		return false
	end

	setStatus("Couldn't equip pickaxe yet", Color3.fromRGB(255, 190, 120))
	pushDebugRateLimited("equip_failed", "equip failed this pass", 1.5)
	return false
end

function tryMineViaNetwork(remotes, equippedPickaxe, targetOre, orePart, orePosition, character)
	if #remotes == 0 then
		return false
	end

	local pickaxeHints = collectPickaxeNameHints(equippedPickaxe, cachedPickaxe, PICKAXE_NAME)
	for _, mineRemote in ipairs(remotes) do
		if mineRemote and mineRemote.Parent then
			local remoteMined = triggerRemote(mineRemote, targetOre)
				or triggerRemote(mineRemote, orePart)
				or triggerRemote(mineRemote, equippedPickaxe, targetOre)
				or triggerRemote(mineRemote, equippedPickaxe, orePart)
				or triggerRemote(mineRemote)
			if not remoteMined then
				for _, pickaxeHint in ipairs(pickaxeHints) do
					remoteMined = triggerRemote(mineRemote, pickaxeHint, targetOre)
						or triggerRemote(mineRemote, pickaxeHint, orePart)
					if remoteMined then
						break
					end
				end
			end

			if not remoteMined and mineAuraEnabled and orePosition then
				remoteMined = triggerRemote(mineRemote, {
					Tool = equippedPickaxe,
					Target = targetOre,
					Hit = orePart,
					Position = orePosition,
					Distance = MAX_MINE_NETWORK_REACH,
					Reach = MAX_MINE_NETWORK_REACH,
					Character = character,
				})
					or triggerRemote(mineRemote, equippedPickaxe, orePart, orePosition, MAX_MINE_NETWORK_REACH)
					or triggerRemote(mineRemote, orePosition, MAX_MINE_NETWORK_REACH)
					or triggerRemote(mineRemote, targetOre, orePosition, MAX_MINE_NETWORK_REACH)
			end

			if remoteMined then
				return true
			end
		end
	end

	return false
end

function swingPickaxe(character, pickaxe, targetOre)
	if not (character and pickaxe) then
		return false
	end
	endExperimentalCFrameTravel(character)

	local equippedPickaxe = getEquippedPickaxe(character)
	if equippedPickaxe then
		cachedPickaxe = equippedPickaxe
	end

	if mineAuraEnabled then
		if tryMineViaToolInfoNetwork(character, targetOre) then
			return true
		end

		if tryMineViaGlobalNetwork(character, targetOre, equippedPickaxe) then
			return true
		end

		if equippedPickaxe then
			local orePart = targetOre and getOreBasePart(targetOre) or nil
			local orePosition = targetOre and getOrePosition(targetOre) or (orePart and orePart.Position) or nil
			local mineRemotes = getMineRemotes(false, equippedPickaxe, character)
			if tryMineViaNetwork(mineRemotes, equippedPickaxe, targetOre, orePart, orePosition, character) then
				return true
			end
		end

		pushMineAuraDebugThrottled("mine aura: network call failed")
		return false
	end

	if pickaxe:IsA("Tool") and pickaxe.Parent == character then
		local activated = pcall(function()
			pickaxe:Activate()
		end)
		if activated then
			return true
		end
	end

	if equippedPickaxe then
		local orePart = targetOre and getOreBasePart(targetOre) or nil
		local orePosition = targetOre and getOrePosition(targetOre) or (orePart and orePart.Position) or nil
		local mineRemotes = getMineRemotes(false, equippedPickaxe, character)
		if tryMineViaNetwork(mineRemotes, equippedPickaxe, targetOre, orePart, orePosition, character) then
			return true
		end

		local clicked = clickMouse()
		if not clicked then
			pushDebug("swing click failed")
		end
		return clicked
	end

	setStatus("Waiting for equip", Color3.fromRGB(255, 190, 120))
	return false
end

function getPathSellNodeIndex(pathData)
	return select(3, getPathSellTarget(pathData))
end

function setPathResumePlaybackPending(isPending, shouldSave)
	pathState.resumePathPlaybackPending = isPending == true
	if shouldSave then
		savePathsToStorage(false)
	end
end

function getPathCycleRestartSlotIndex()
	if pathState.farmPathsEnabled then
		return getPlayablePathSlotIndex(1)
	end
	return getPathSequenceStartSlotIndex()
end

function shouldUsePathPlayback()
	if not (pathState.playLoopEnabled or pathState.alwaysPlayEnabled or pathState.farmPathsEnabled or pathState.resumePathPlaybackPending) then
		return false
	end
	return getPathSequenceStartSlotIndex() ~= nil
end

function clearPathSpawnReadyState()
	pathState.spawnPlaybackReadySlotIndex = nil
	pathState.spawnPlaybackReadyPathName = nil
	pathState.spawnPlaybackReadySpawnName = nil
end

function markPathSpawnPlaybackReady(pathName, spawnName, slotIndex)
	if type(pathName) ~= "string" then
		return false
	end

	pathState.spawnPlaybackPending = false
	pathState.spawnPlaybackAwaitingRespawn = false
	pathState.spawnPlaybackPendingSlotIndex = nil
	pathState.spawnPlaybackReadySlotIndex = normalizePathSlotIndex(slotIndex)
	pathState.spawnPlaybackReadyPathName = pathName
	pathState.spawnPlaybackReadySpawnName = normalizePathSpawnName(spawnName)
	pathState.spawnPlaybackPendingPathName = nil
	pathState.spawnPlaybackPendingSpawnName = nil
	return true
end

function cancelPendingPathSpawnPlayback(clearReadyState)
	pathState.spawnPlaybackSequenceId = (tonumber(pathState.spawnPlaybackSequenceId) or 0) + 1
	pathState.spawnPlaybackPending = false
	pathState.spawnPlaybackAwaitingRespawn = false
	pathState.spawnPlaybackPendingSlotIndex = nil
	pathState.spawnPlaybackPendingPathName = nil
	pathState.spawnPlaybackPendingSpawnName = nil
	if clearReadyState ~= false then
		clearPathSpawnReadyState()
	end
end

function isPathSpawnSequenceActive(sequenceId)
	return tonumber(sequenceId) ~= nil
		and (tonumber(pathState.spawnPlaybackSequenceId) or 0) == tonumber(sequenceId)
		and (pathState.spawnPlaybackPending or pathState.spawnPlaybackAwaitingRespawn)
end

function isPathPlaybackBusy()
	return (autoMineEnabled and activeFarmMode == "path")
		or pathState.spawnPlaybackPending == true
		or pathState.spawnPlaybackAwaitingRespawn == true
		or type(pathState.spawnPlaybackReadyPathName) == "string"
end

function hasPathPlaybackRespawnCompleted(previousCharacter)
	local character = getCustomCharacter() or (localPlayer and localPlayer.Character) or cachedCharacter
	if not character or not character.Parent then
		return false
	end

	if previousCharacter and previousCharacter.Parent and character ~= previousCharacter then
		return true
	end

	return not isPathPlaybackResetReady(character)
end

function tryResumeReadyPathSpawnPlayback(sourceLabel)
	local readyPathName = pathState.spawnPlaybackReadyPathName
	local readySlotIndex = normalizePathSlotIndex(pathState.spawnPlaybackReadySlotIndex)
	if type(readyPathName) ~= "string" or not readySlotIndex then
		return false
	end

	if getPathSlotSelection(readySlotIndex, false) ~= readyPathName then
		cancelPendingPathSpawnPlayback(true)
		return false
	end

	local readySpawnName = normalizePathSpawnName(pathState.spawnPlaybackReadySpawnName)
	if not consumeReadyPathSpawnPlayback(readyPathName, readySpawnName, readySlotIndex) then
		return false
	end

	cancelPendingPathSpawnPlayback(false)
	pathState.playbackSlotIndex = readySlotIndex
	setFocusedPathSlotIndex(readySlotIndex, true)
	activeFarmMode = "path"
	pushDebug("resuming saved spawn playback from " .. tostring(sourceLabel or "unknown"))
	startAutoMine()
	return true
end

function getPathSpawnCooldownRemaining(spawnName)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	if not normalizedSpawnName then
		return 0
	end

	local replicatedState = resolveGlobalReplicatedState(false) or resolveGlobalReplicatedState(true)
	if not isTable(replicatedState) then
		return 0
	end

	local state = rawget(replicatedState, "State") or replicatedState.State
	local cooldowns = isTable(state) and (rawget(state, "SpawnCooldowns") or state.SpawnCooldowns) or nil
	local cooldownEndsAt = isTable(cooldowns) and tonumber(cooldowns[normalizedSpawnName]) or nil
	local syncedTime = getSyncedNetworkTime()
	if cooldownEndsAt and syncedTime < cooldownEndsAt then
		return cooldownEndsAt - syncedTime
	end
	return 0
end

function formatPathCooldownCountdown(seconds)
	local remaining = math.max(0, tonumber(seconds) or 0)
	if remaining >= 10 then
		return tostring(math.ceil(remaining)) .. "s"
	end
	return string.format("%.1fs", math.ceil(remaining * 10) / 10)
end

function isSpawnUnlockedForPathPlayback(spawnName)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	if not normalizedSpawnName then
		return true
	end

	local playerData = resolvePlayerDataModule(false) or resolvePlayerDataModule(true)
	if not isTable(playerData) then
		return true
	end

	local isSpawnUnlocked = rawget(playerData, "IsSpawnUnlocked") or playerData.IsSpawnUnlocked
	if type(isSpawnUnlocked) ~= "function" then
		return true
	end

	local ok, unlocked = pcall(isSpawnUnlocked, playerData, normalizedSpawnName)
	return not ok or unlocked ~= false
end

function waitForPathSpawnCooldown(sequenceId, spawnName)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	if not normalizedSpawnName then
		return true
	end

	local notified = false
	while isPathSpawnSequenceActive(sequenceId) and pathState.spawnPlaybackPending do
		local remaining = getPathSpawnCooldownRemaining(normalizedSpawnName)
		if remaining <= 0 then
			return true
		end

		local message = normalizedSpawnName .. " is on cooldown, now waiting for it to be off cooldown.... (" .. formatPathCooldownCountdown(remaining) .. ")"
		if not notified then
			notifyUser("Path Spawn", function()
				local currentRemaining = getPathSpawnCooldownRemaining(normalizedSpawnName)
				return normalizedSpawnName .. " is on cooldown, now waiting for it to be off cooldown.... (" .. formatPathCooldownCountdown(currentRemaining) .. ")"
			end, math.max(6, math.ceil(remaining) + 1))
			notified = true
		end
		setStatus(message, Color3.fromRGB(255, 190, 120))
		task.wait(math.clamp(remaining, PATH_SPAWN_WAIT_INTERVAL, 1))
	end

	return false
end

function isPathPlaybackResetReady(character)
	local playerCharacter = resolvePlayerCharacterModule(false) or resolvePlayerCharacterModule(true)
	if isTable(playerCharacter) then
		local isDead = rawget(playerCharacter, "IsDead")
		if isDead == nil then
			isDead = playerCharacter.IsDead
		end
		if isDead == true then
			return true
		end
	end

	local replicatedState = resolveGlobalReplicatedState(false) or resolveGlobalReplicatedState(true)
	if isTable(replicatedState) then
		local state = rawget(replicatedState, "State") or replicatedState.State
		local deadState = isTable(state) and (rawget(state, "Dead") or state.Dead) or nil
		if deadState == true then
			return true
		end
	end

	if not character or not character.Parent then
		return true
	end

	local humanoid = getHumanoid(character)
	return humanoid == nil or humanoid.Health <= 0
end

function requestPathPlaybackReset(character)
	local attempted = false

	local humanoid = getHumanoid(character)
	if humanoid then
		pcall(function()
			humanoid.Sit = false
		end)
	end

	local network = playerState.hookedNetwork or resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
	local fireServer = type(playerState.originalNetworkFireServer) == "function" and playerState.originalNetworkFireServer
		or (network and network.FireServer)
	if network and type(fireServer) == "function" then
		local okRespawnTriggered = pcall(fireServer, network, "RespawnTriggered")
		if okRespawnTriggered then
			attempted = true
		end
	end

	if attempted then
		return true
	end

	if humanoid then
		pcall(function()
			humanoid.Health = 0
		end)
		pcall(function()
			humanoid:ChangeState(Enum.HumanoidStateType.Dead)
		end)
		attempted = true
	end
	if character and character.Parent then
		pcall(function()
			character:BreakJoints()
		end)
		attempted = true
	end
	return attempted
end

function waitForPathPlaybackReset(sequenceId)
	local lastResetAttemptAt = 0
	local resetCooldownNotified = false
	while isPathSpawnSequenceActive(sequenceId) and pathState.spawnPlaybackPending do
		if isOnPathSpawnSelectorScreen() then
			return true
		end

		local character = cachedCharacter or getCustomCharacter() or (localPlayer and localPlayer.Character)
		if isPathPlaybackResetReady(character) then
			return true
		end

		local now = os.clock()
		if lastResetAttemptAt == 0 or (now - lastResetAttemptAt) >= PATH_RESET_RETRY_INTERVAL then
			requestPathPlaybackReset(character)
			if not resetCooldownNotified then
				setStatus("Resetting character for saved spawn", Color3.fromRGB(120, 190, 255))
			end
			lastResetAttemptAt = now
		elseif not resetCooldownNotified and (now - lastResetAttemptAt) >= PATH_RESET_COOLDOWN_NOTICE_DELAY then
			local message = "Reset character is on cooldown, now waiting for it to be off cooldown...."
			notifyUser("Path Spawn", message, 6)
			setStatus(message, Color3.fromRGB(255, 190, 120))
			resetCooldownNotified = true
		end

		task.wait(PATH_SPAWN_WAIT_INTERVAL)
	end

	return false
end

function requestSavedPathRespawn(spawnName)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	if not normalizedSpawnName then
		return false, "spawn missing"
	end

	local network = resolveGlobalNetwork(false) or resolveGlobalNetwork(true)
	if not network or type(network.InvokeServer) ~= "function" then
		return false, "respawn network unavailable"
	end

	local okInvoke, err = pcall(function()
		network:InvokeServer("Respawn", normalizedSpawnName)
	end)
	if not okInvoke then
		return false, tostring(err)
	end
	return true
end

function consumeReadyPathSpawnPlayback(pathName, spawnName, slotIndex)
	local readyPathName = pathState.spawnPlaybackReadyPathName
	local readySpawnName = normalizePathSpawnName(pathState.spawnPlaybackReadySpawnName)
	local readySlotIndex = normalizePathSlotIndex(pathState.spawnPlaybackReadySlotIndex)
	local expectedSpawnName = normalizePathSpawnName(spawnName)
	local expectedSlotIndex = normalizePathSlotIndex(slotIndex)
	local matches = type(pathName) == "string"
		and readyPathName == pathName
		and readySpawnName == expectedSpawnName
		and readySlotIndex == expectedSlotIndex
	clearPathSpawnReadyState()
	return matches
end

function queueSelectedPathSpawnPlayback(pathName, spawnName, sourceLabel, slotIndex)
	local normalizedSpawnName = normalizePathSpawnName(spawnName)
	local normalizedSlotIndex = normalizePathSlotIndex(slotIndex)
	if type(pathName) ~= "string" or not normalizedSpawnName or not normalizedSlotIndex then
		return false
	end

	local pendingSpawnName = normalizePathSpawnName(pathState.spawnPlaybackPendingSpawnName)
	if (pathState.spawnPlaybackPending or pathState.spawnPlaybackAwaitingRespawn)
		and pathState.spawnPlaybackPendingSlotIndex == normalizedSlotIndex
		and pathState.spawnPlaybackPendingPathName == pathName
		and pendingSpawnName == normalizedSpawnName then
		setStatus("Preparing saved spawn: " .. normalizedSpawnName, Color3.fromRGB(120, 190, 255))
		return true
	end

	if not isSpawnUnlockedForPathPlayback(normalizedSpawnName) then
		local message = normalizedSpawnName .. " is locked right now."
		notifyUser("Path Spawn", message, 6)
		setStatus(message, Color3.fromRGB(255, 190, 120))
		return false
	end

	pathState.spawnPlaybackSequenceId = (tonumber(pathState.spawnPlaybackSequenceId) or 0) + 1
	local sequenceId = pathState.spawnPlaybackSequenceId
	pathState.spawnPlaybackPending = true
	pathState.spawnPlaybackAwaitingRespawn = false
	pathState.spawnPlaybackPendingSlotIndex = normalizedSlotIndex
	pathState.spawnPlaybackPendingPathName = pathName
	pathState.spawnPlaybackPendingSpawnName = normalizedSpawnName
	clearPathSpawnReadyState()
	activeFarmMode = "path"
	setStatus("Preparing saved spawn: " .. normalizedSpawnName, Color3.fromRGB(120, 190, 255))
	pushDebug("path spawn queued from " .. tostring(sourceLabel or "unknown") .. ": " .. normalizedSpawnName)
	if type(refreshButton) == "function" then
		refreshButton()
	end
	if type(refreshPathManagerButtons) == "function" then
		refreshPathManagerButtons()
	end

	task.spawn(function()
		local ok, err = xpcall(function()
			if not waitForPathSpawnCooldown(sequenceId, normalizedSpawnName) then
				return
			end
			if not waitForPathPlaybackReset(sequenceId) then
				return
			end
			if not isPathSpawnSequenceActive(sequenceId) or not pathState.spawnPlaybackPending then
				return
			end

			pathState.spawnPlaybackPending = false
			pathState.spawnPlaybackAwaitingRespawn = true
			local respawnBaselineCharacter = getCustomCharacter() or (localPlayer and localPlayer.Character) or cachedCharacter

			local nextRespawnRequestAt = 0
			local failedRespawnRequests = 0
			local respawnRequestSucceeded = false
			local spawnSelectorWaitStartedAt = os.clock()
			local spawnSelectorConfirmed = false
			while isPathSpawnSequenceActive(sequenceId) and pathState.spawnPlaybackAwaitingRespawn do
				if respawnRequestSucceeded and hasPathPlaybackRespawnCompleted(respawnBaselineCharacter) then
					if markPathSpawnPlaybackReady(pathName, normalizedSpawnName, normalizedSlotIndex) then
						setStatus("Respawn complete, starting path", Color3.fromRGB(140, 220, 140))
						task.defer(function()
							task.wait(0.35)
							tryResumeReadyPathSpawnPlayback("respawn-detected")
						end)
					end
					return
				end

				local now = os.clock()
				if not respawnRequestSucceeded then
					local onSpawnSelectorScreen = isOnPathSpawnSelectorScreen(respawnBaselineCharacter)
					if onSpawnSelectorScreen then
						if not spawnSelectorConfirmed then
							spawnSelectorConfirmed = true
							setStatus("Spawn selector ready, choosing " .. normalizedSpawnName, Color3.fromRGB(120, 190, 255))
						end
					elseif not spawnSelectorConfirmed and (now - spawnSelectorWaitStartedAt) < PATH_SPAWN_SELECTOR_WAIT_TIMEOUT then
						setStatus("Waiting for spawn selector: " .. normalizedSpawnName, Color3.fromRGB(255, 190, 120))
						task.wait(PATH_SPAWN_WAIT_INTERVAL)
						continue
					end
				end

				if now >= nextRespawnRequestAt then
					local okRespawn, respawnErr = requestSavedPathRespawn(normalizedSpawnName)
					if okRespawn then
						failedRespawnRequests = 0
						respawnRequestSucceeded = true
						setStatus("Respawning at " .. normalizedSpawnName, Color3.fromRGB(120, 190, 255))
						nextRespawnRequestAt = now + PATH_RESPAWN_CONFIRM_TIMEOUT
					else
						failedRespawnRequests += 1
						setStatus("Waiting to respawn at " .. normalizedSpawnName, Color3.fromRGB(255, 190, 120))
						nextRespawnRequestAt = now + PATH_RESPAWN_RETRY_INTERVAL
						if failedRespawnRequests >= 5 then
							error("Respawn request failed: " .. tostring(respawnErr))
						end
					end
				end
				task.wait(PATH_SPAWN_WAIT_INTERVAL)
			end
		end, debug.traceback)

		if not ok and (tonumber(pathState.spawnPlaybackSequenceId) or 0) == sequenceId then
			cancelPendingPathSpawnPlayback(true)
			activeFarmMode = nil
			setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
			pushDebug(err)
			if type(refreshButton) == "function" then
				refreshButton()
			end
			if type(refreshPathManagerButtons) == "function" then
				refreshPathManagerButtons()
			end
		end
	end)

	return true
end

function startPathPlaybackForSlot(slotIndex, sourceLabel)
	if not shouldUsePathPlayback() then
		return false
	end

	local selectedSlotIndex = getPlayablePathSlotIndex(slotIndex or getPathSequenceStartSlotIndex() or 1)
	local selectedPathName = getPathSlotSelection(selectedSlotIndex, false)
	if type(selectedPathName) ~= "string" or pathState.paths[selectedPathName] == nil then
		return false
	end

	pathState.playbackSlotIndex = selectedSlotIndex
	setFocusedPathSlotIndex(selectedSlotIndex, true)
	if tryResumeReadyPathSpawnPlayback(sourceLabel) then
		return true
	end

	local selectedSpawnName = getPathSlotSpawnName(selectedSlotIndex)
	activeFarmMode = "path"
	if selectedSpawnName then
		if consumeReadyPathSpawnPlayback(selectedPathName, selectedSpawnName, selectedSlotIndex) then
			cancelPendingPathSpawnPlayback(false)
			startAutoMine()
			return true
		end
		return queueSelectedPathSpawnPlayback(selectedPathName, selectedSpawnName, sourceLabel, selectedSlotIndex)
	end

	cancelPendingPathSpawnPlayback(true)
	startAutoMine()
	return true
end

function startSelectedPathPlayback(sourceLabel)
	return startPathPlaybackForSlot(getPathSequenceStartSlotIndex(), sourceLabel)
end

function resetPathPlaybackState()
	pathState.playbackActive = false
	pathState.playbackSlotIndex = nil
	pathState.playbackNodeIndex = 1
	pathState.playbackOreIndex = 1
	pathState.playbackPathName = nil
	pathState.currentPath = nil
	pathState.entryPathingPending = false
	pathState.playbackSellPending = false
	clearPathPlayerAvoiderDetection()
end

function beginPathPlayback()
	if not shouldUsePathPlayback() then
		resetPathPlaybackState()
		return false
	end

	local selectedSlotIndex = getPlayablePathSlotIndex(pathState.playbackSlotIndex or getPathSequenceStartSlotIndex() or 1)
	local selectedPathName = getPathSlotSelection(selectedSlotIndex, false)
	if not selectedSlotIndex or type(selectedPathName) ~= "string" or pathState.paths[selectedPathName] == nil then
		resetPathPlaybackState()
		return false
	end

	pathState.playbackActive = true
	pathState.playbackSlotIndex = selectedSlotIndex
	pathState.playbackNodeIndex = 1
	pathState.playbackOreIndex = 1
	pathState.playbackPathName = selectedPathName
	pathState.currentPath = clonePathData(pathState.paths[selectedPathName])
	pathState.entryPathingPending = true
	pathState.playbackSellPending = false
	if pathState.resumePathPlaybackPending then
		setPathResumePlaybackPending(false, true)
	end
	return pathState.currentPath ~= nil
end

function tryAdvanceToNextPathSequence(sourceLabel)
	local nextSlotIndex = getNextPlayablePathSlotIndex(pathState.playbackSlotIndex)
	if not nextSlotIndex then
		if (pathState.playLoopEnabled or pathState.alwaysPlayEnabled or pathState.farmPathsEnabled) and serverHopEnabled then
			return requestPathServerHop("Finding a smaller public server for the next path cycle...", getPathCycleRestartSlotIndex(), false)
		end
		if pathState.farmPathsEnabled then
			nextSlotIndex = getPlayablePathSlotIndex(1)
		end
	end

	if not nextSlotIndex then
		return false
	end

	local nextPathName = getPathSlotSelection(nextSlotIndex, false)
	if type(nextPathName) ~= "string" then
		return false
	end

	local nextSpawnName = getPathSlotSpawnName(nextSlotIndex)
	if nextSpawnName then
		task.defer(function()
			if autoMineEnabled and activeFarmMode == "path" then
				stopAutoMine()
			end
			activeFarmMode = "path"
			local ok, started = xpcall(function()
				return startPathPlaybackForSlot(nextSlotIndex, sourceLabel or "sequence")
			end, debug.traceback)
			if not ok then
				autoMineEnabled = false
				activeFarmMode = nil
				setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
				pushDebug(started)
			elseif not started then
				autoMineEnabled = false
				activeFarmMode = nil
				setStatus("Next path could not start", Color3.fromRGB(255, 190, 120))
			end
			if type(refreshPathManagerButtons) == "function" then
				refreshPathManagerButtons()
			end
		end)
		return true
	end

	pathState.playbackSlotIndex = nextSlotIndex
	setFocusedPathSlotIndex(nextSlotIndex, true)
	if beginPathPlayback() then
		setStatus("Following path " .. tostring(pathState.playbackPathName), Color3.fromRGB(120, 190, 255))
		if type(refreshPathManagerButtons) == "function" then
			refreshPathManagerButtons()
		end
		return true
	end
	return false
end

function getCurrentPathPlaybackNode()
	local currentPath = pathState.currentPath
	if not currentPath or #currentPath.nodes == 0 then
		return nil
	end

	if pathState.playbackNodeIndex < 1 or pathState.playbackNodeIndex > #currentPath.nodes then
		return nil
	end

	return currentPath.nodes[pathState.playbackNodeIndex]
end

function advancePathPlaybackNode()
	local currentPath = pathState.currentPath
	if not currentPath or #currentPath.nodes == 0 then
		pathState.playbackNodeIndex = 1
		return
	end

	pathState.playbackNodeIndex += 1
	if pathState.playbackNodeIndex > #currentPath.nodes then
		pathState.playbackActive = false
	end
end

function isPathPlaybackSellReady()
	local lootSummary = getSellableLootSummary(false)
	if isTable(lootSummary) then
		local filledSlots = tonumber(rawget(lootSummary, "filledSlots") or lootSummary.filledSlots) or 0
		local totalSlots = tonumber(rawget(lootSummary, "totalSlots") or lootSummary.totalSlots) or 0
		lastInventoryFilledSlots = filledSlots
		lastInventoryTotalSlots = totalSlots
		return (rawget(lootSummary, "hasLoot") or lootSummary.hasLoot) == true
	end

	local filledSlots, totalSlots = getInventorySlotCounts()
	lastInventoryFilledSlots = filledSlots
	lastInventoryTotalSlots = totalSlots
	return totalSlots <= 0 or filledSlots > 0
end

function getPlayablePathOreForNode(currentPath, nodeIndex, nodePosition)
	local bestOreTarget = nil
	local bestResolvedOre = nil
	local bestDistance = nil

	for _, oreTarget in ipairs(currentPath.ores or {}) do
		if (oreTarget.nodeIndex or 0) == nodeIndex then
			local resolvedOre = resolveSavedOreTarget(oreTarget)
			if resolvedOre then
				local orePosition = getOrePosition(resolvedOre)
				local oreRemaining = getOreRemainingValue(resolvedOre)
				if orePosition and oreRemaining and oreRemaining.Value > 0 then
					local distance = (orePosition - nodePosition).Magnitude
					if distance <= MINE_AURA_RANGE and (not bestDistance or distance < bestDistance) then
						bestDistance = distance
						bestOreTarget = oreTarget
						bestResolvedOre = resolvedOre
					end
				end
			end
		end
	end

	return bestOreTarget, bestResolvedOre
end

function runPathPlaybackStep(character, humanoid, pickaxe, activeToken)
	if not pathState.playbackActive and not beginPathPlayback() then
		return false
	end
	if humanoid and humanoid.Health <= 0 then
		setInteractKeyHeld(false)
		clearMovementKeys()
		task.wait(0.15)
		return true
	end

	local currentPath = pathState.currentPath
	if not currentPath or #currentPath.nodes == 0 then
		pathState.playbackActive = false
		setStatus("Selected path has no nodes", Color3.fromRGB(255, 190, 120))
		return false
	end

	local entryJoinDistance = movementMode == "cframe"
		and math.max(0.9, WAYPOINT_REACH_DISTANCE * 0.28)
		or WAYPOINT_REACH_DISTANCE
	local playbackReachDistance = movementMode == "cframe"
		and math.max(0.55, WAYPOINT_REACH_DISTANCE * 0.18)
		or math.max(1.8, WAYPOINT_REACH_DISTANCE * 0.6)
	local function getCurrentNodeDistance(position)
		local rootPart = getRootPart(character)
		if not rootPart or not position then
			return math.huge
		end
		return (Vector3.new(rootPart.Position.X, 0, rootPart.Position.Z) - Vector3.new(position.X, 0, position.Z)).Magnitude
	end

	local sellerPart, sellerPrompt, sellNodeIndex = getPathSellTarget(currentPath, false)
	local pathPickupOptions = {
		maxDistance = PICKUP_RANGE + 0.75,
		arriveRange = PICKUP_RANGE,
		requireAlreadyInReach = true,
	}
	local processedNodes = 0
	while processedNodes < #currentPath.nodes do
		local currentNode = getCurrentPathPlaybackNode()
		if not currentNode then
			pathState.playbackActive = false
			return false
		end

		local currentNodeIndex = pathState.playbackNodeIndex
		local currentNodePosition = getPathNodePosition(currentNode)
		if not currentNodePosition then
			advancePathPlaybackNode()
			processedNodes += 1
			continue
		end
		local currentOreTarget, resolvedOre = getPlayablePathOreForNode(currentPath, currentNodeIndex, currentNodePosition)
		local sellReady = isPathPlaybackSellReady()
		if sellReady then
			pathState.playbackSellPending = true
		end

		local isSellNode = sellNodeIndex and currentNodeIndex == sellNodeIndex
		local activeSellerPart = nil
		local activeSellerPrompt = nil
		local movementTargetPosition = currentNodePosition
		local requiredDistance = pathState.entryPathingPending and entryJoinDistance or playbackReachDistance
		if isSellNode then
			currentTargetOre = nil
			clearMovementKeys()
			setInteractKeyHeld(false)
			if pickaxe and pickaxe:IsA("Tool") and pickaxe.Parent == character then
				pcall(function()
					pickaxe:Deactivate()
				end)
			end
		end
		if isSellNode and pathState.playbackSellPending then
			activeSellerPart, activeSellerPrompt = findNearestAutoSellTarget(character, false)
			if not activeSellerPart then
				sellerPart, sellerPrompt, sellNodeIndex = getPathSellTarget(currentPath, true)
				activeSellerPart = sellerPart
				activeSellerPrompt = sellerPrompt
			end
		end

		local shouldSellHere = isSellNode and pathState.playbackSellPending and getCurrentNodeDistance(currentNodePosition) <= requiredDistance
		currentTargetOre = nil

		if getCurrentNodeDistance(movementTargetPosition) > requiredDistance then
			if processPathPlayerAvoidance(character, movementTargetPosition, activeToken, nil, humanoid, pickaxe) then
				return true
			end
			if not (isSellNode and pathState.playbackSellPending) and tryAutoPickup(character, humanoid, activeToken, pathPickupOptions) then
				return true
			end

			if isSellNode and pathState.playbackSellPending then
				setStatus("Moving to sell node", Color3.fromRGB(232, 184, 96))
			else
				setStatus("Following path " .. tostring(pathState.playbackPathName), Color3.fromRGB(120, 190, 255))
			end
			clearPathVisualization()
			if pathState.entryPathingPending then
				local reachedTarget, moveReason = moveCharacterNearPosition(character, humanoid, movementTargetPosition, requiredDistance, activeToken)
				if not reachedTarget then
					if moveReason == "rubberband" then
						handlePathPlaybackRubberBand(character, movementTargetPosition)
						return true
					end
					if getCurrentNodeDistance(movementTargetPosition) > requiredDistance then
						return false
					end
				end
				pathState.entryPathingPending = false
			else
				local movedToNode, moveReason = moveTowardsRecordedPathNode(character, humanoid, movementTargetPosition, activeToken)
				if not movedToNode and moveReason == "rubberband" then
					handlePathPlaybackRubberBand(character, movementTargetPosition)
					return true
				end
				return movedToNode
			end
		else
			pathState.entryPathingPending = false
			clearPathVisualization()

			if shouldSellHere then
				if not activeSellerPart then
					sellerPart, sellerPrompt, sellNodeIndex = getPathSellTarget(currentPath, true)
					activeSellerPart = sellerPart
					activeSellerPrompt = sellerPrompt
				end

				if activeSellerPart then
					setStatus("Selling on path", Color3.fromRGB(232, 184, 96))
					clearMovementKeys()
					setInteractKeyHeld(false)
					if trySellLootNearTarget(activeSellerPart, activeSellerPrompt, character) then
						pathState.playbackSellPending = false
						if tryAdvanceToNextPathSequence("path-sequence") then
							return true
						end
						if tryServerHopAfterPathSell() then
							return true
						end
						advancePathPlaybackNode()
						return true
					end

					pushDebugRateLimited("path_sell_retry", "path sell: seller found but sell did not complete, retrying", 2)
					setStatus("Waiting for sell to complete", Color3.fromRGB(232, 184, 96))
					task.wait(0.25)
					return true
				else
					pushDebugRateLimited("path_sell_target_missing", "path sell node has no matched seller target", 3)
					setStatus("Waiting for sell target", Color3.fromRGB(255, 190, 120))
					task.wait(0.25)
					return true
				end
			end

			if isSellNode then
				if pathState.playbackSellPending then
					setStatus("Holding at sell node", Color3.fromRGB(232, 184, 96))
					task.wait(0.15)
					return true
				end

				advancePathPlaybackNode()
				processedNodes += 1
				continue
			end

			if currentOreTarget and resolvedOre then
				local avoidanceHandled, avoidReason = processPathPlayerAvoidance(
					character,
					currentNodePosition,
					activeToken,
					nil,
					humanoid,
					pickaxe
				)
				if avoidanceHandled then
					if avoidReason == "advance" then
						advancePathPlaybackNode()
					end
					return true
				end
				currentTargetOre = resolvedOre
				if tryAutoPickup(character, humanoid, activeToken, pathPickupOptions) then
					return true
				end
				setStatus(
					"Mining marked " .. getOreLabel(currentOreTarget.groupName) .. " (" .. formatOreHealthText(resolvedOre) .. ")",
					Color3.fromRGB(140, 220, 140)
				)
				local equipped = equipPickaxe(character, humanoid, pickaxe)
				if not equipped then
					setStatus("Equipping pickaxe", Color3.fromRGB(255, 190, 120))
					task.wait(0.08)
					return true
				end
				pickaxe = getEquippedPickaxe(character) or getStoredPickaxe(character) or pickaxe
				if pickaxe then
					cachedPickaxe = pickaxe
				end
				swingPickaxe(character, pickaxe, resolvedOre)
				if tryAutoPickup(character, humanoid, activeToken, pathPickupOptions) then
					return true
				end
				return true
			end

			if tryAutoPickup(character, humanoid, activeToken, pathPickupOptions) then
				return true
			end

			advancePathPlaybackNode()
			processedNodes += 1
		end
	end

	return true
end

function refreshCachedObjects(forceRefresh)
	local now = os.clock()
	if not forceRefresh and (now - lastRescanAt) < RESCAN_INTERVAL then
		return cachedCharacter, cachedHumanoid, cachedPickaxe
	end

	lastRescanAt = now
	cachedCharacter = getCustomCharacter()
	cachedHumanoid = getHumanoid(cachedCharacter)
	cachedPickaxe = findPickaxe(cachedCharacter)
	if cachedCharacter then
		pushDebug("character: " .. cachedCharacter.Name)
	else
		pushDebug("character not found")
	end
	if cachedPickaxe then
		pushDebug("pickaxe: " .. cachedPickaxe:GetFullName())
		pushDebug("pickaxe name: " .. cachedPickaxe.Name)
	else
		pushDebug("pickaxe not found")
	end

	return cachedCharacter, cachedHumanoid, cachedPickaxe
end

stopAutoMine = function()
	autoMineEnabled = false
	mineAuraEnabled = false
	activeFarmMode = nil
	serverHopInProgress = false
	serverHopTaskQueued = false
	autoMineToken += 1
	autoMineThread = nil
	cancelPendingPathSpawnPlayback(true)
	resetPathPlaybackState()
	currentTargetOre = nil
	lastAutoDiscardScanAt = 0
	lastAutoDiscardAttemptAt = 0
	lastAutoDiscardItemKey = nil
	lastToggleAt = 0
	ignoreToggleUntil = 0
	setInteractKeyHeld(false)
	clearMovementKeys()
	cancelActiveMoveTween()
	endExperimentalCFrameTravel(cachedCharacter)
	restoreMineAura()
	releaseAutoMineManualMovementLock()
	setLastMoveCommand(Vector3.zero, "stop")
	clearPathVisualization()
	_G.__AutoMineStopLatched = nil
	if cachedHumanoid and cachedCharacter then
		local rootPart = getRootPart(cachedCharacter)
		if rootPart then
			pcall(function()
				cachedHumanoid:MoveTo(rootPart.Position)
			end)
		end
	end
	pushDebug("auto mine disabled")
	setStatus("Idle", Color3.fromRGB(180, 180, 180))
	if type(refreshButton) == "function" then
		refreshButton()
	end
	if type(refreshServerHopDisplay) == "function" then
		refreshServerHopDisplay()
	end
end

function startAutoMine()
	activeFarmMode = "path"
	autoMineEnabled = true
	mineAuraEnabled = true
	auraCapabilitiesReported = false
	lastMineAuraDebugAt = 0
	lastAuraForceScanAt = 0
	cachedDirectToolInfo = nil
	cachedToolInfoOwner = nil
	autoMineToken += 1
	autoMineThread = nil
	currentTargetOre = nil
	lastAutoDiscardScanAt = 0
	lastAutoDiscardAttemptAt = 0
	lastAutoDiscardItemKey = nil
	setInteractKeyHeld(false)
	clearMovementKeys()
	cancelActiveMoveTween()
	setLastMoveCommand(Vector3.zero, "start")
	if not shouldUsePathPlayback() or not beginPathPlayback() then
		autoMineEnabled = false
		activeFarmMode = nil
		resetPathPlaybackState()
		error("No saved path selected")
	end
	refreshCachedObjects(true)
	ensureAutoMineManualMovementLock(cachedCharacter, cachedHumanoid)
	if type(refreshButton) == "function" then
		refreshButton()
	end
	local activeToken = autoMineToken
	local targetOre = nil
	autoMineThread = task.spawn(function()
		local ok, err = xpcall(function()
			pushDebug("auto mine loop started")
			while autoMineEnabled and activeToken == autoMineToken do
				local character = cachedCharacter
				local humanoid = cachedHumanoid
				local pickaxe = cachedPickaxe

				if serverHopInProgress then
					task.wait(0.25)
					continue
				end

				if not character or not character.Parent then
					character, humanoid, pickaxe = refreshCachedObjects(true)
				elseif not pickaxe or not pickaxe.Parent then
					character, humanoid, pickaxe = refreshCachedObjects(true)
				end

				if character and humanoid then
					ensureAutoMineManualMovementLock(character, humanoid)
					if tryAutoDiscardLastSlotOre(character, activeToken) then
						task.wait(0.08)
						continue
					end

					if humanoid.Health <= 0 then
						currentTargetOre = nil
						setInteractKeyHeld(false)
						clearMovementKeys()
						task.wait(0.2)
						continue
					end

					local delayEquipForTravel = shouldDelayExperimentalCFrameEquip()
					if not delayEquipForTravel then
						local equipped = equipPickaxe(character, humanoid, pickaxe)
						if not equipped then
							task.wait(AUTO_MINE_INTERVAL)
							continue
						end
					end

					pickaxe = getEquippedPickaxe(character) or getStoredPickaxe(character) or pickaxe
					if pickaxe then
						cachedPickaxe = pickaxe
					end

					local auraPickaxe = getEquippedPickaxe(character) or getStoredPickaxe(character) or pickaxe
					if mineAuraEnabled then
						applyMineAuraToPickaxe(auraPickaxe)
					else
						restoreMineAura()
					end

					if pathState.playbackActive then
						runPathPlaybackStep(character, humanoid, pickaxe, activeToken)
						task.wait(0.03)
						continue
					end

					if tryAdvanceToNextPathSequence("path-finished") then
						task.wait(0.03)
						continue
					end

					pathState.playLoopEnabled = false
					savePathsToStorage(true)
					stopAutoMine()
					setStatus("Path finished", Color3.fromRGB(140, 220, 140))
					refreshPathManagerButtons()
					return
				else
					currentTargetOre = nil
					setStatus("Character/humanoid missing", Color3.fromRGB(255, 190, 120))
				end

				task.wait(AUTO_MINE_INTERVAL)
			end
		end, debug.traceback)

		if not ok and activeToken == autoMineToken then
			autoMineEnabled = false
			mineAuraEnabled = false
			serverHopInProgress = false
			serverHopTaskQueued = false
			resetPathPlaybackState()
			currentTargetOre = nil
			setInteractKeyHeld(false)
			clearMovementKeys()
			cancelActiveMoveTween()
			endExperimentalCFrameTravel(cachedCharacter)
			restoreMineAura()
			releaseAutoMineManualMovementLock()
			setLastMoveCommand(Vector3.zero, "error")
			setStatus("Script error, see debug", Color3.fromRGB(255, 130, 130))
			pushDebug(err)
		end

		if activeToken == autoMineToken or not autoMineEnabled then
			autoMineThread = nil
		end
	end)
end

UI_COLORS = {
	shell = Color3.fromRGB(12, 12, 12),
	sidebar = Color3.fromRGB(16, 16, 16),
	sidebarButton = Color3.fromRGB(26, 26, 26),
	panel = Color3.fromRGB(15, 15, 15),
	card = Color3.fromRGB(18, 18, 18),
	surface = Color3.fromRGB(24, 24, 24),
	border = Color3.fromRGB(44, 44, 44),
	accent = Color3.fromRGB(0, 150, 255),
	accentMuted = Color3.fromRGB(18, 48, 76),
	success = Color3.fromRGB(0, 255, 100),
	successMuted = Color3.fromRGB(22, 62, 38),
	warning = Color3.fromRGB(208, 145, 78),
	text = Color3.fromRGB(245, 245, 245),
	muted = Color3.fromRGB(176, 176, 176),
	subtle = Color3.fromRGB(118, 118, 118),
	debug = Color3.fromRGB(126, 198, 255),
}

function addCorner(instance, radius)
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, radius)
	corner.Parent = instance
	return corner
end

function addStroke(instance, color, thickness, transparency)
	local stroke = Instance.new("UIStroke")
	stroke.Color = color
	stroke.Thickness = thickness or 1
	stroke.Transparency = transparency or 0
	stroke.Parent = instance
	return stroke
end

function colorDistance(colorA, colorB)
	return math.abs(colorA.R - colorB.R) + math.abs(colorA.G - colorB.G) + math.abs(colorA.B - colorB.B)
end

function splitFobButtonText(rawText)
	local textValue = tostring(rawText or "")
	local leftText, rightText = string.match(textValue, "^(.-)%s+|%s+(.+)$")
	if leftText and rightText then
		return leftText, rightText
	end

	leftText, rightText = string.match(textValue, "^(.-):%s*(.+)$")
	if leftText and rightText and #rightText <= 22 then
		return leftText, rightText
	end

	return textValue, nil
end

function getFobValueColor(valueText)
	local loweredValue = string.lower(tostring(valueText or ""))
	if loweredValue == "" then
		return UI_COLORS.muted
	end
	if loweredValue == "on" or loweredValue == "working" then
		return UI_COLORS.success
	end
	if loweredValue == "off" or loweredValue == "none" then
		return UI_COLORS.muted
	end
	if loweredValue == "tween" then
		return UI_COLORS.accent
	end
	if loweredValue == "cframe" then
		return UI_COLORS.warning
	end
	if loweredValue == "legit" then
		return UI_COLORS.text
	end
	return UI_COLORS.text
end

function refreshFobButtonChrome(button)
	local chrome = button:FindFirstChild("FobChrome")
	if not chrome then
		return
	end

	local titleLabel = chrome:FindFirstChild("MainLabel")
	local valueLabel = chrome:FindFirstChild("ValueLabel")
	local icon = chrome:FindFirstChild("Icon")
	local diamond = chrome:FindFirstChild("Diamond")
	local topLine = chrome:FindFirstChild("TopLine")
	local bottomLine = chrome:FindFirstChild("BottomLine")
	if not (titleLabel and valueLabel and icon and diamond and topLine and bottomLine) then
		return
	end

	local titleText, valueText = splitFobButtonText(button.Text)
	local hovering = button:GetAttribute("FobHovering") == true
	local isRaised = colorDistance(button.BackgroundColor3, UI_COLORS.surface) > 0.03
	local isHot = hovering or isRaised

	titleLabel.Text = titleText
	titleLabel.TextColor3 = isHot and Color3.new(1, 1, 1) or UI_COLORS.muted
	valueLabel.Text = valueText or ""
	valueLabel.Visible = valueText ~= nil and valueText ~= ""
	valueLabel.TextColor3 = getFobValueColor(valueText)
	icon.BackgroundColor3 = isHot and Color3.new(1, 1, 1) or UI_COLORS.accent
	diamond.Visible = isHot
	topLine.Size = isHot and UDim2.new(1, 0, 0, 2) or UDim2.new(0, 0, 0, 2)
	bottomLine.Size = isHot and UDim2.new(1, 0, 0, 2) or UDim2.new(0, 0, 0, 2)
end

function applyFobButtonChrome(button)
	if button:FindFirstChild("FobChrome") then
		refreshFobButtonChrome(button)
		return
	end

	local chrome = Instance.new("Frame")
	chrome.Name = "FobChrome"
	chrome.Size = UDim2.fromScale(1, 1)
	chrome.BackgroundTransparency = 1
	chrome.Active = false
	chrome.Parent = button

	local topLine = Instance.new("Frame")
	topLine.Name = "TopLine"
	topLine.Size = UDim2.new(0, 0, 0, 2)
	topLine.BackgroundColor3 = Color3.new(1, 1, 1)
	topLine.BorderSizePixel = 0
	topLine.Parent = chrome

	local bottomLine = Instance.new("Frame")
	bottomLine.Name = "BottomLine"
	bottomLine.Size = UDim2.new(0, 0, 0, 2)
	bottomLine.Position = UDim2.new(0, 0, 1, -2)
	bottomLine.BackgroundColor3 = Color3.new(1, 1, 1)
	bottomLine.BorderSizePixel = 0
	bottomLine.Parent = chrome

	local diamond = Instance.new("Frame")
	diamond.Name = "Diamond"
	diamond.Size = UDim2.fromOffset(6, 6)
	diamond.Position = UDim2.new(0, 10, 0.5, 0)
	diamond.AnchorPoint = Vector2.new(0.5, 0.5)
	diamond.Rotation = 45
	diamond.BackgroundColor3 = Color3.new(1, 1, 1)
	diamond.BorderSizePixel = 0
	diamond.Visible = false
	diamond.Parent = chrome

	local icon = Instance.new("Frame")
	icon.Name = "Icon"
	icon.Size = UDim2.fromOffset(14, 14)
	icon.Position = UDim2.new(0, 24, 0.5, -7)
	icon.BackgroundColor3 = UI_COLORS.accent
	icon.BorderSizePixel = 0
	icon.Parent = chrome
	addCorner(icon, 2)

	local mainLabel = Instance.new("TextLabel")
	mainLabel.Name = "MainLabel"
	mainLabel.Size = UDim2.new(1, -130, 1, 0)
	mainLabel.Position = UDim2.new(0, 50, 0, 0)
	mainLabel.BackgroundTransparency = 1
	mainLabel.Font = Enum.Font.GothamMedium
	mainLabel.TextSize = button.TextSize
	mainLabel.TextColor3 = UI_COLORS.muted
	mainLabel.TextXAlignment = Enum.TextXAlignment.Left
	mainLabel.TextTruncate = Enum.TextTruncate.AtEnd
	mainLabel.Parent = chrome

	local valueLabel = Instance.new("TextLabel")
	valueLabel.Name = "ValueLabel"
	valueLabel.Size = UDim2.new(0, 112, 1, 0)
	valueLabel.Position = UDim2.new(1, -124, 0, 0)
	valueLabel.BackgroundTransparency = 1
	valueLabel.Font = Enum.Font.GothamMedium
	valueLabel.TextSize = math.max(11, button.TextSize - 1)
	valueLabel.TextColor3 = UI_COLORS.text
	valueLabel.TextXAlignment = Enum.TextXAlignment.Right
	valueLabel.TextTruncate = Enum.TextTruncate.AtEnd
	valueLabel.Parent = chrome

	button.TextTransparency = 1
	button.MouseEnter:Connect(function()
		button:SetAttribute("FobHovering", true)
		refreshFobButtonChrome(button)
	end)
	button.MouseLeave:Connect(function()
		button:SetAttribute("FobHovering", false)
		refreshFobButtonChrome(button)
	end)
	button:GetPropertyChangedSignal("Text"):Connect(function()
		refreshFobButtonChrome(button)
	end)
	button:GetPropertyChangedSignal("BackgroundColor3"):Connect(function()
		refreshFobButtonChrome(button)
	end)
	refreshFobButtonChrome(button)
end

function styleSubTabButton(button, textSize)
	button.BorderSizePixel = 0
	button.AutoButtonColor = false
	button.BackgroundTransparency = 1
	button.TextColor3 = UI_COLORS.muted
	button.TextSize = textSize or 12
	button.Font = Enum.Font.GothamMedium
	button.TextXAlignment = Enum.TextXAlignment.Left
	_UTILS._applySoundsToButton(button)

	local padding = Instance.new("UIPadding")
	padding.PaddingLeft = UDim.new(0, 10)
	padding.PaddingRight = UDim.new(0, 10)
	padding.Parent = button

	local underline = Instance.new("Frame")
	underline.Name = "TabUnderline"
	underline.Size = UDim2.new(0, 0, 0, 2)
	underline.Position = UDim2.new(0, 10, 1, -2)
	underline.BackgroundColor3 = UI_COLORS.accent
	underline.BorderSizePixel = 0
	underline.Parent = button

	return button
end

function styleButton(button, textSize)
	button.BorderSizePixel = 0
	if button:IsA("TextButton") then
		button.AutoButtonColor = false
		button.TextXAlignment = Enum.TextXAlignment.Left
		_UTILS._applySoundsToButton(button)
	end
	button.TextColor3 = UI_COLORS.text
	button.TextSize = textSize or 13
	button.Font = Enum.Font.GothamMedium
	button.BackgroundColor3 = UI_COLORS.surface
	addCorner(button, 8)
	addStroke(button, UI_COLORS.border, 1, 0.45)
	if button:IsA("TextButton") then
		applyFobButtonChrome(button)
	end
	return button
end

function styleCard(card, radius)
	card.BorderSizePixel = 0
	card.BackgroundColor3 = UI_COLORS.card
	addCorner(card, radius or 12)
	addStroke(card, UI_COLORS.border, 1, 0.62)
	return card
end

function configureTextLabel(label, font, textSize, color)
	label.BackgroundTransparency = 1
	label.TextColor3 = color
	label.TextSize = textSize
	label.Font = font
	label.TextXAlignment = Enum.TextXAlignment.Left
	return label
end

function bindCanvasToLayout(scrollingFrame, layout, padding)
	local extraPadding = padding or 0
	local function refreshCanvas()
		scrollingFrame.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + extraPadding)
	end

	layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(refreshCanvas)
	refreshCanvas()
end

function isPointInsideGuiObject(guiObject, point)
	if not guiObject or not guiObject.Visible or not point then
		return false
	end

	local absolutePosition = guiObject.AbsolutePosition
	local absoluteSize = guiObject.AbsoluteSize
	return point.X >= absolutePosition.X
		and point.X <= (absolutePosition.X + absoluteSize.X)
		and point.Y >= absolutePosition.Y
		and point.Y <= (absolutePosition.Y + absoluteSize.Y)
end

function attachFloatingDrag(frame, interact)
	local dragging = false
	local dragInput = nil
	local dragStart = nil
	local startPos = nil

	interact.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position
		end
	end)

	_UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			dragInput = input
		end
	end)

	_UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)

	_RunService.RenderStepped:Connect(function(dt)
		if not frame.Visible or not dragging or not dragInput or not dragStart or not startPos then
			return
		end

		local delta = dragInput.Position - dragStart
		local destination = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
		frame.Position = frame.Position:Lerp(destination, math.min(dt * 20, 1))
	end)
end

function setSettingsHint(text, color)
	if not settingsHintLabel then
		return
	end

	settingsHintLabel.Text = text
	settingsHintLabel.TextColor3 = color or UI_COLORS.muted
end

function refreshSettingsHint()
	if pendingKeybindAction == "gui" then
		setSettingsHint("Press a key to set the GUI toggle. Press Escape to cancel.", UI_COLORS.accent)
	elseif pendingKeybindAction == "autofarm" then
		setSettingsHint("Press a key to set the path play toggle. Press Escape to cancel.", UI_COLORS.accent)
	else
		setSettingsHint("Click a button, then press any keyboard key.", UI_COLORS.muted)
	end
end

function setActiveTab(tabName)
	activeTabName = tabName

	for name, page in pairs(tabPages) do
		page.Visible = name == tabName
	end

	for name, button in pairs(tabButtons) do
		local active = name == tabName
		button.BackgroundColor3 = active and UI_COLORS.accentMuted or UI_COLORS.sidebarButton
		local stroke = button:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Color = active and UI_COLORS.accent or UI_COLORS.border
		end
		local label = button:FindFirstChild("TabText")
		if label and label:IsA("TextLabel") then
			label.TextColor3 = active and UI_COLORS.text or UI_COLORS.muted
		end
	end
end

function setOreDropdownOpen(isOpen)
	oreDropdownOpen = isOpen and true or false

	if oreDropdownFrame then
		oreDropdownFrame.Visible = oreDropdownOpen
	end
end

function setPickupWhitelistDropdownOpen(isOpen)
	pickupWhitelistDropdownOpen = isOpen and true or false

	if pickupWhitelistDropdownFrame then
		pickupWhitelistDropdownFrame.Visible = pickupWhitelistDropdownOpen
	end
end

function setMovementModeDropdownOpen(isOpen)
	movementModeDropdownOpen = isOpen and true or false

	if movementModeDropdownFrame then
		movementModeDropdownFrame.Visible = movementModeDropdownOpen
	end
end

function getMovementModeLabel()
	if movementMode == "tween" then
		return "Tween"
	end
	if movementMode == "cframe" then
		return "CFrame"
	end
	return "Legit"
end

function normalizeMovementMode(value)
	if value == true then
		return "tween"
	end
	if value == false or value == nil then
		return "legit"
	end

	local loweredValue = safeLower(value)
	if loweredValue == "tween" or loweredValue == "cframe" then
		return loweredValue
	end

	return "legit"
end

function setMovementMode(nextMode, debugSource, skipPersistence)
	local nextValue = normalizeMovementMode(nextMode)
	if movementMode == nextValue then
		refreshTweenButton()
		return
	end

	if movementMode == "cframe" or nextValue ~= "cframe" then
		endExperimentalCFrameTravel(cachedCharacter)
	end
	movementMode = nextValue
	tweenModeEnabled = nextValue == "tween"
	cancelActiveMoveTween()
	clearMovementKeys()
	setLastMoveCommand(Vector3.zero, nextValue == "cframe" and "CFrameIdle" or (tweenModeEnabled and "TweenIdle" or "LegitIdle"))
	if autoMineEnabled then
		ensureAutoMineManualMovementLock(cachedCharacter, cachedHumanoid)
	end
	pushDebug("movement mode: " .. getMovementModeLabel() .. (debugSource and (" [" .. tostring(debugSource) .. "]") or ""))
	refreshTweenButton()
	if not skipPersistence and type(schedulePathSettingsPersist) == "function" then
		schedulePathSettingsPersist()
	end
end

function setInfiniteStaminaEnabled(isEnabled)
	local nextValue = isEnabled == true
	if playerState.infiniteStaminaEnabled == nextValue then
		refreshInfiniteStaminaButton()
		return
	end

	playerState.infiniteStaminaEnabled = nextValue
    S.infiniteStaminaEnabled = nextValue
	ensurePlayerFeatureHooks()
    schedulePersist()

	pushDebug("infinite stamina: " .. (nextValue and "on" or "off"))
	refreshInfiniteStaminaButton()
end

function setSafeTeleportEnabled(isEnabled)
	local nextValue = isEnabled == true
	if playerState.safeTeleportEnabled == nextValue then
		refreshSafeTeleportButton()
		return
	end

	playerState.safeTeleportEnabled = nextValue
    S.safeTeleportEnabled = nextValue
	ensurePlayerFeatureHooks()
    schedulePersist()

	pushDebug("safe teleport: " .. (nextValue and "on" or "off"))
	refreshSafeTeleportButton()
end

function setNoJumpCooldownEnabled(isEnabled)
    local nextValue = isEnabled == true
    if playerState.noJumpCooldownEnabled == nextValue then
        return
    end

    playerState.noJumpCooldownEnabled = nextValue
    S.noJumpCooldownEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("no jump cooldown: " .. (nextValue and "on" or "off"))
end

function setNoFallDamageEnabled(isEnabled)
    local nextValue = isEnabled == true
    if playerState.noFallDamageEnabled == nextValue then
        return
    end

    playerState.noFallDamageEnabled = nextValue
    S.noFallDamageEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("no fall damage: " .. (nextValue and "on" or "off"))
end

function setNoRagdollEnabled(isEnabled)
    local nextValue = isEnabled == true
    if playerState.noRagdollEnabled == nextValue then
        return
    end

    playerState.noRagdollEnabled = nextValue
    S.noRagdollEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("no ragdoll: " .. (nextValue and "on" or "off"))
end

function setAutoBreakFreeEnabled(isEnabled)
    local nextValue = isEnabled == true
    if playerState.autoBreakFreeEnabled == nextValue then
        return
    end

    playerState.autoBreakFreeEnabled = nextValue
    S.autoBreakFreeEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("auto break free: " .. (nextValue and "on" or "off"))
end

function setAutoGetUpEnabled(isEnabled)
    local nextValue = isEnabled == true
    if playerState.autoGetUpEnabled == nextValue then
        return
    end

    playerState.autoGetUpEnabled = nextValue
    S.autoGetUpEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("auto get up: " .. (nextValue and "on" or "off"))
end

function setEquipWhileSwimmingEnabled(isEnabled)
    local nextValue = isEnabled == true
    if playerState.equipWhileSwimmingEnabled == nextValue then
        return
    end

    playerState.equipWhileSwimmingEnabled = nextValue
    S.equipWhileSwimmingEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("equip while swimming: " .. (nextValue and "on" or "off"))
end

function setSilentAimEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.silentAimEnabled == nextValue then
        return
    end

    S.silentAimEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("silent aim: " .. (nextValue and "on" or "off"))
end

function setSilentAimBodyTarget(value)
    local nextValue = tostring(value or "Head")
    if nextValue ~= "Torso" then
        nextValue = "Head"
    end
    if S.silentAimBodyTarget == nextValue then
        return
    end

    S.silentAimBodyTarget = nextValue
    schedulePersist()
    pushDebug("silent aim body target: " .. nextValue)
end

function setSilentAimMode(value)
    local nextValue = tostring(value or "FOV")
    if not table.find(GUN_AIM_MODE_ORDER, nextValue) then
        nextValue = "FOV"
    end
    if S.silentAimMode == nextValue then
        return
    end

    S.silentAimMode = nextValue
    schedulePersist()
    pushDebug("silent aim mode: " .. nextValue)
end

function setSilentAimVisibilityCheck(isEnabled)
    local nextValue = isEnabled == true
    if S.silentAimVisibilityCheck == nextValue then
        return
    end

    S.silentAimVisibilityCheck = nextValue
    schedulePersist()
    pushDebug("silent aim visibility check: " .. (nextValue and "on" or "off"))
end

function setSilentAimAccuracy(value)
    local nextValue = math.clamp(math.floor((tonumber(value) or 50) + 0.5), 1, 100)
    if S.silentAimAccuracy == nextValue then
        return
    end

    S.silentAimAccuracy = nextValue
    schedulePersist()
end

function setSilentAimDistanceLimit(value)
    local nextValue = math.clamp(math.floor((tonumber(value) or 1000) + 0.5), 10, 20000)
    if S.silentAimDistanceLimit == nextValue then
        return
    end

    S.silentAimDistanceLimit = nextValue
    schedulePersist()
end

function setGunFovEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.gunFovEnabled == nextValue then
        return
    end

    S.gunFovEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
end

function setGunFovShape(value)
    local nextValue = tostring(value or "Circle")
    if not table.find(GUN_FOV_SHAPE_ORDER, nextValue) then
        nextValue = "Circle"
    end
    if S.gunFovShape == nextValue then
        return
    end

    S.gunFovShape = nextValue
    schedulePersist()
end

function setGunFovRadius(value)
    local nextValue = math.clamp(math.floor((tonumber(value) or 200) + 0.5), 10, 2000)
    if S.gunFovRadius == nextValue then
        return
    end

    S.gunFovRadius = nextValue
    schedulePersist()
end

function setGunSnaplineEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.gunSnaplineEnabled == nextValue then
        return
    end

    S.gunSnaplineEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
end

function setFastProjectileEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.fastProjectileEnabled == nextValue then
        return
    end

    S.fastProjectileEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("fast projectile: " .. (nextValue and "on" or "off"))
end

function setWallBangEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.wallBangEnabled == nextValue then
        return
    end

    S.wallBangEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("wall bang: " .. (nextValue and "on" or "off"))
end

function setNoSpreadEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.noSpreadEnabled == nextValue then
        return
    end

    S.noSpreadEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("no spread: " .. (nextValue and "on" or "off"))
end

function setNoRecoilEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.noRecoilEnabled == nextValue then
        return
    end

    S.noRecoilEnabled = nextValue
    setNoRecoilGuardState(nextValue)
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("no recoil: " .. (nextValue and "on" or "off"))
end

function setNoFireCooldownEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.noFireCooldownEnabled == nextValue then
        return
    end

    S.noFireCooldownEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("no fire cooldown: " .. (nextValue and "on" or "off"))
end

function setFastReloadEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.fastReloadEnabled == nextValue then
        return
    end

    S.fastReloadEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("fast reload: " .. (nextValue and "on" or "off"))
end

function setInstantLassoChargeEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.instantLassoChargeEnabled == nextValue then
        return
    end

    S.instantLassoChargeEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("lasso instant charge: " .. (nextValue and "on" or "off"))
end

function setLassoSilentAimEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.lassoSilentAimEnabled == nextValue then
        return
    end

    S.lassoSilentAimEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("lasso silent aim: " .. (nextValue and "on" or "off"))
end

function setLassoSnaplineEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.lassoSnaplineEnabled == nextValue then
        return
    end

    S.lassoSnaplineEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("lasso snapline: " .. (nextValue and "on" or "off"))
end

function setLassoNoRopeLimitEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.lassoNoRopeLimitEnabled == nextValue then
        return
    end

    S.lassoNoRopeLimitEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("lasso no rope limit: " .. (nextValue and "on" or "off"))
end

function setFasterLassoEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.fasterLassoEnabled == nextValue then
        return
    end

    S.fasterLassoEnabled = nextValue
    ensurePlayerFeatureHooks()
    schedulePersist()
    pushDebug("faster lasso: " .. (nextValue and "on" or "off"))
end

function setPlayerEspEnabled(isEnabled)
    local nextValue = isEnabled == true
    if S.playerEspEnabled == nextValue then
        return
    end

    S.playerEspEnabled = nextValue
    if nextValue then
        ensurePlayerEspRenderLoop()
    else
        for _, entry in pairs(playerEspState.entries) do
            hidePlayerEspEntry(entry)
        end
    end
    schedulePersist()
end

function updateAutofarmLayout()
    if pathUi.useFobRows then
        local subTabOffset = 46
        local mainHeight = pathUi.mainRowsLayout and pathUi.mainRowsLayout.AbsoluteContentSize.Y or 0
        local pathsHeight = pathUi.pathRowsLayout and pathUi.pathRowsLayout.AbsoluteContentSize.Y or 0
        local activeHeight = pathState.subTab == "Paths" and pathsHeight or mainHeight

        if pathUi.mainRowsPage then
            pathUi.mainRowsPage.Position = UDim2.fromOffset(0, subTabOffset)
            pathUi.mainRowsPage.Size = UDim2.new(1, 0, 0, mainHeight)
        end

        if pathUi.pathRowsPage then
            pathUi.pathRowsPage.Position = UDim2.fromOffset(0, subTabOffset)
            pathUi.pathRowsPage.Size = UDim2.new(1, 0, 0, pathsHeight)
        end

        local contentHeight = subTabOffset + activeHeight + 18
        if automineCanvas then
            automineCanvas.Size = UDim2.new(1, -4, 0, math.max(contentHeight, 180))
        end

        if autofarmPageRef then
            autofarmPageRef.CanvasSize = UDim2.new(0, 0, 0, math.max(contentHeight + 8, 220))
        end
        return
    end

    local subTabOffset = 46
    local autoFarmHeight = oreDropdownOpen and 284 or 156
    local pickupHeight = pickupWhitelistDropdownOpen and 296 or 156
    local movementHeight = movementModeDropdownOpen and 172 or 104
    local autoSellHeight = 186
    local liveFeedHeight = 126
    local movementY = subTabOffset + autoFarmHeight + 12
    local liveFeedY = movementY + movementHeight + 12
    local autoSellY = subTabOffset + pickupHeight + 12
    local settingsHeight = math.max(liveFeedY + liveFeedHeight, autoSellY + autoSellHeight)
    local pathsHeight = subTabOffset + ((pathUi.pathsPage and pathUi.pathsPage.AbsoluteSize.Y > 0) and math.max(pathUi.pathsPage.AbsoluteSize.Y, pathUi.managerCard and pathUi.managerCard.AbsoluteSize.Y or 238) or 238)
    local contentHeight = pathState.subTab == "Paths" and pathsHeight or settingsHeight

    if autoFarmCardFrame then
        autoFarmCardFrame.Size = UDim2.new(0.5, -6, 0, autoFarmHeight)
        autoFarmCardFrame.Position = UDim2.new(0, 0, 0, subTabOffset)
    end

    if movementCardFrame then
        movementCardFrame.Size = UDim2.new(0.5, -6, 0, movementHeight)
        movementCardFrame.Position = UDim2.new(0, 0, 0, movementY)
    end

    if pickupCardFrame then
        pickupCardFrame.Size = UDim2.new(0.5, -6, 0, pickupHeight)
        pickupCardFrame.Position = UDim2.new(0.5, 6, 0, subTabOffset)
    end

    if autoSellCardFrame then
        autoSellCardFrame.Size = UDim2.new(0.5, -6, 0, autoSellHeight)
        autoSellCardFrame.Position = UDim2.new(0.5, 6, 0, autoSellY)
    end

    if liveFeedCardFrame then
        liveFeedCardFrame.Size = UDim2.new(0.5, -6, 0, liveFeedHeight)
        liveFeedCardFrame.Position = UDim2.new(0, 0, 0, liveFeedY)
    end

    if pathUi.pathsPage then
        pathUi.pathsPage.Position = UDim2.fromOffset(0, subTabOffset)
    end

    if automineCanvas then
        automineCanvas.Size = UDim2.new(1, -4, 0, contentHeight)
    end

    if autofarmPageRef then
        autofarmPageRef.CanvasSize = UDim2.new(0, 0, 0, contentHeight + 8)
    end
end


function refreshButton()
	pcall(function()
		if pathUi.useFobRows then
			for _, slotUi in ipairs(pathUi.pathSlotControls or {}) do
				if slotUi.pathPlaySync then
					slotUi.pathPlaySync()
				end
			end
			return
		end

		if toggleButton then
			local pathActive = isPathPlaybackBusy()
			toggleButton.Text = pathActive and "Stop Path Playback" or "Play Selected Path"
			toggleButton.BackgroundColor3 = pathActive and UI_COLORS.successMuted or UI_COLORS.surface
			local stroke = toggleButton:FindFirstChildOfClass("UIStroke")
			if stroke then
				stroke.Color = pathActive and UI_COLORS.success or UI_COLORS.border
			end
		end

	end)
end

function refreshTweenButton()
	pcall(function()
		if pathUi.useFobRows then
			if pathUi.movementModeSync then
				pathUi.movementModeSync()
			end

			local showSpeed = movementMode == "tween" or movementMode == "cframe"
			if pathUi.movementSpeedContainer then
				pathUi.movementSpeedContainer.Size = UDim2.new(1, 0, 0, showSpeed and 38 or 0)
			end
			if pathUi.movementSpeedRow then
				pathUi.movementSpeedRow.Visible = showSpeed
			end
			if pathUi.movementSpeedLabel then
				pathUi.movementSpeedLabel.Text = movementMode == "cframe" and "CFrame Speed" or "Tween Speed"
			end
			updateAutofarmLayout()
			return
		end

		if not tweenButton then
			return
		end

		local selectedModeLabel = getMovementModeLabel()
		local buttonPrefix = movementModeDropdownOpen and "Hide Modes" or "Movement Mode"
		local selectedFill = movementMode == "tween" and UI_COLORS.accentMuted or (movementMode == "cframe" and UI_COLORS.warning or UI_COLORS.surface)
		local selectedStrokeColor = movementMode == "tween" and UI_COLORS.accent or (movementMode == "cframe" and UI_COLORS.warning or UI_COLORS.border)
		tweenButton.Text = buttonPrefix .. "  |  " .. selectedModeLabel
		tweenButton.BackgroundColor3 = selectedFill

		local stroke = tweenButton:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Color = selectedStrokeColor
		end

		if movementModeDropdownFrame then
			movementModeDropdownFrame.Visible = movementModeDropdownOpen
		end

		if movementModeLegitButton then
			movementModeLegitButton.Text = "Legit  |  Walk normally"
			movementModeLegitButton.BackgroundColor3 = movementMode == "legit" and UI_COLORS.successMuted or UI_COLORS.surface
			local legitStroke = movementModeLegitButton:FindFirstChildOfClass("UIStroke")
			if legitStroke then
				legitStroke.Color = movementMode == "legit" and UI_COLORS.success or UI_COLORS.border
			end
		end

		if movementModeTweenButton then
			movementModeTweenButton.Text = "Tween  |  Smooth direct pathing"
			movementModeTweenButton.BackgroundColor3 = movementMode == "tween" and UI_COLORS.accentMuted or UI_COLORS.surface
			local tweenStroke = movementModeTweenButton:FindFirstChildOfClass("UIStroke")
			if tweenStroke then
				tweenStroke.Color = movementMode == "tween" and UI_COLORS.accent or UI_COLORS.border
			end
		end

		if movementModeCFrameButton then
			movementModeCFrameButton.Text = "CFrame  |  Faster experimental stepping"
			movementModeCFrameButton.BackgroundColor3 = movementMode == "cframe" and UI_COLORS.warning or UI_COLORS.surface
			local cframeStroke = movementModeCFrameButton:FindFirstChildOfClass("UIStroke")
			if cframeStroke then
				cframeStroke.Color = movementMode == "cframe" and UI_COLORS.warning or UI_COLORS.border
			end
		end
	end)
end

function refreshInfiniteStaminaButton()
	pcall(function()
		if playerUi.infiniteStaminaSync then
			playerUi.infiniteStaminaSync()
			return
		end

		if not playerUi.infiniteStaminaButton then
			return
		end

		local enabled = playerState.infiniteStaminaEnabled
		playerUi.infiniteStaminaButton.Text = enabled and "Infinite stamina: ON" or "Infinite stamina: OFF"
		playerUi.infiniteStaminaButton.BackgroundColor3 = enabled and UI_COLORS.successMuted or UI_COLORS.surface

		local stroke = playerUi.infiniteStaminaButton:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Color = enabled and UI_COLORS.success or UI_COLORS.border
		end
	end)
end

function refreshSafeTeleportButton()
	pcall(function()
		if playerUi.safeTeleportSync then
			playerUi.safeTeleportSync()
			return
		end

		if not playerUi.safeTeleportButton then
			return
		end

		local enabled = playerState.safeTeleportEnabled
		playerUi.safeTeleportButton.Text = enabled and "Safe Teleport: ON" or "Safe Teleport: OFF"
		playerUi.safeTeleportButton.BackgroundColor3 = enabled and UI_COLORS.successMuted or UI_COLORS.surface

		local stroke = playerUi.safeTeleportButton:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Color = enabled and UI_COLORS.success or UI_COLORS.border
		end
	end)
end

function refreshAuraButton()
	pcall(function()
		if not mineAuraButton then
			return
		end

		if mineAuraEnabled then
			mineAuraButton.Text = "Mine Aura: ON"
			mineAuraButton.BackgroundColor3 = UI_COLORS.accentMuted
		else
			mineAuraButton.Text = "Mine Aura: OFF"
			mineAuraButton.BackgroundColor3 = UI_COLORS.surface
		end

		local stroke = mineAuraButton and mineAuraButton:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Color = mineAuraEnabled and UI_COLORS.accent or UI_COLORS.border
		end
	end)
end

function refreshAutoPickupButton()
	pcall(function()
		if pathUi.useFobRows then
			if pathUi.autoPickupSync then
				pathUi.autoPickupSync()
			end
			return
		end

		if autoPickupEnabled then
			autoPickupButton.Text = "Auto Pickup: ON"
			autoPickupButton.BackgroundColor3 = UI_COLORS.successMuted
		else
			autoPickupButton.Text = "Auto Pickup: OFF"
			autoPickupButton.BackgroundColor3 = UI_COLORS.surface
		end

		local stroke = autoPickupButton and autoPickupButton:FindFirstChildOfClass("UIStroke")
		if stroke then
			stroke.Color = autoPickupEnabled and UI_COLORS.success or UI_COLORS.border
		end
	end)
end

function refreshAutoSellDisplay()
	pcall(function()
		if autoSellButton then
			autoSellButton.Visible = false
		end
		if autoSellThresholdLabel then
			autoSellThresholdLabel.Visible = false
		end
		if autoSellInventoryLabel then
			autoSellInventoryLabel.Visible = false
		end
		if autoSellSliderFill then
			autoSellSliderFill.Visible = false
		end
		if autoSellSliderKnob then
			autoSellSliderKnob.Visible = false
		end
		if autoSellSliderHitbox then
			autoSellSliderHitbox.Visible = false
		end
	end)
end

function refreshServerHopDisplay()
	pcall(function()
		if pathUi.useFobRows then
			if pathUi.serverHopSync then
				pathUi.serverHopSync()
			end
			if pathUi.staffHopSync then
				pathUi.staffHopSync()
			end
			if pathUi.farmPathsSync then
				pathUi.farmPathsSync()
			end
			return
		end

		if serverHopButton then
			if serverHopInProgress then
				serverHopButton.Text = "Path Server Hop: WORKING"
				serverHopButton.BackgroundColor3 = UI_COLORS.accentMuted
			else
				serverHopButton.Text = serverHopEnabled and "Path Server Hop: ON" or "Path Server Hop: OFF"
				serverHopButton.BackgroundColor3 = serverHopEnabled and UI_COLORS.accentMuted or UI_COLORS.surface
			end

			local stroke = serverHopButton:FindFirstChildOfClass("UIStroke")
			if stroke then
				stroke.Color = (serverHopEnabled or serverHopInProgress) and UI_COLORS.accent or UI_COLORS.border
			end
		end

		if serverHopStatusLabel then
			serverHopStatusLabel.Text = serverHopStatusText ~= "" and serverHopStatusText
				or (serverHopEnabled and "After a path sell, it will try to hop into a smaller public server."
					or "Off. Path sells stay in the current server.")
			serverHopStatusLabel.TextColor3 = serverHopStatusColor or (serverHopEnabled and UI_COLORS.subtle or UI_COLORS.muted)
		end
	end)
end

function refreshPickupWhitelistDisplay()
	pcall(function()
		if pathUi.useFobRows then
			if pathUi.pickupWhitelistSync then
				pathUi.pickupWhitelistSync()
			end
			if pathUi.pickupFilterSync then
				pathUi.pickupFilterSync()
			end
			return
		end

		if pickupWhitelistButton then
			pickupWhitelistButton.Text = autoPickupWhitelistEnabled and "Pickup Whitelist: ON" or "Pickup Whitelist: OFF"
			pickupWhitelistButton.BackgroundColor3 = autoPickupWhitelistEnabled and UI_COLORS.accentMuted or UI_COLORS.surface
			local buttonStroke = pickupWhitelistButton:FindFirstChildOfClass("UIStroke")
			if buttonStroke then
				buttonStroke.Color = autoPickupWhitelistEnabled and UI_COLORS.accent or UI_COLORS.border
			end
		end

		if pickupWhitelistDropdownButton then
			local prefix = pickupWhitelistDropdownOpen and "Hide Pickup List" or "Pickup Filter"
			pickupWhitelistDropdownButton.Text = prefix .. "  |  " .. getSelectedPickupSummary()
		end

		for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
			local optionButton = pickupWhitelistOptionButtons[pickupOption.id]
			if optionButton then
				local selected = selectedPickupNames[pickupOption.id] == true
				optionButton.Text = string.format("%s %s", selected and "[x]" or "[ ]", pickupOption.label)
				optionButton.BackgroundColor3 = selected and UI_COLORS.successMuted or UI_COLORS.surface
				local optionStroke = optionButton:FindFirstChildOfClass("UIStroke")
				if optionStroke then
					optionStroke.Color = selected and UI_COLORS.success or UI_COLORS.border
				end
			end
		end
	end)
end

function refreshOreSelectionDisplay()
	pcall(function()
		if headerSubtitleLabel then
			headerSubtitleLabel.Text = "Saved Path: " .. tostring(getPathSlotSelection(pathState.focusedPathSlotIndex, false) or "None")
		end

		if pathUi.useFobRows then
			return
		end

		if oreDropdownButton then
			local prefix = oreDropdownOpen and "Hide Ore List" or "Pick Ores"
			oreDropdownButton.Text = prefix .. "  |  " .. getSelectedOreSummary()
		end

		for _, oreOption in ipairs(ORE_OPTIONS) do
			local oreButton = oreOptionButtons[oreOption.id]
			if oreButton then
				local selected = isOreNameSelected(oreOption.id)
				oreButton.Text = string.format("%s %s", selected and "[x]" or "[ ]", oreOption.label)
				oreButton.BackgroundColor3 = selected and UI_COLORS.successMuted or UI_COLORS.surface
				local stroke = oreButton:FindFirstChildOfClass("UIStroke")
				if stroke then
					stroke.Color = selected and UI_COLORS.success or UI_COLORS.border
				end
			end
		end
	end)
end

function refreshKeybindButtons()
	pcall(function()
		if guiKeybindButton then
			guiKeybindButton.Text = pendingKeybindAction == "gui" and "Press a key..." or ("GUI Toggle: " .. formatKeyCodeName(guiToggleKeybind))
			guiKeybindButton.BackgroundColor3 = pendingKeybindAction == "gui" and UI_COLORS.accentMuted or UI_COLORS.surface
		end

		if autoFarmKeybindButton then
			autoFarmKeybindButton.Text = pendingKeybindAction == "autofarm" and "Press a key..." or ("Path Play Toggle: " .. formatKeyCodeName(autoFarmKeybind))
			autoFarmKeybindButton.BackgroundColor3 = pendingKeybindAction == "autofarm" and UI_COLORS.accentMuted or UI_COLORS.surface
		end

		refreshSettingsHint()
	end)
end

function setPathEditorStatus(text, color)
	if pathUi.editorStatusLabel then
		pathUi.editorStatusLabel.Text = tostring(text or "")
		pathUi.editorStatusLabel.TextColor3 = color or UI_COLORS.muted
	end
end

function setPathEditorIdleStatus()
	if not pathState.editorVisible then
		setPathEditorStatus("")
		return
	end

	if pathState.deleteMode then
		setPathEditorStatus("Delete mode is on. Click a node to delete it and the route ahead of it.", UI_COLORS.warning)
		return
	end

	if pathState.sellNodePickMode then
		setPathEditorStatus("Sell-node picker is on. Click a node to set it as the sell point. Clicking the current sell node clears it.", UI_COLORS.accent)
		return
	end

	if pathState.editNodePlacementEnabled then
		if pathState.recording then
			setPathEditorStatus("Recording trail live. Edit node placement is on, so you can click nodes or handles and move them around.", UI_COLORS.success)
		else
			setPathEditorStatus("Recording paused. Edit node placement is on, so you can move nodes, then save the route when you're done.", UI_COLORS.accent)
		end
		return
	end

	if pathState.recording then
		setPathEditorStatus("Recording trail live. Turn on Edit node placement if you want to drag nodes, use sell-node pick for manual sell spots, and press the mark key again over a marked ore to unmark it.", UI_COLORS.success)
	else
		setPathEditorStatus("Recording paused. Turn on Edit node placement to move nodes, or use the other controls to mark ores, set a sell node, and save the route.", UI_COLORS.muted)
	end
end

refreshPathManagerButtons = nil
setSavedPathDropdownOpen = nil

function getGroundAlignedPathPosition(targetPosition, character)
	if not targetPosition then
		return nil
	end

	local ignoreInstances = {
		getRecordedPathFolder(),
		getVisualizerFolder(),
		findPathPlayerAvoiderVisualizerFolder(),
	}
	if character then
		table.insert(ignoreInstances, character)
	end

	local function castToFloor(upwardOffset, downwardDistance)
		local raycastParams = RaycastParams.new()
		raycastParams.FilterType = Enum.RaycastFilterType.Exclude
		raycastParams.FilterDescendantsInstances = ignoreInstances

		local scanOrigin = Vector3.new(targetPosition.X, targetPosition.Y + upwardOffset, targetPosition.Z)
		local ray = workspace:Raycast(scanOrigin, Vector3.new(0, -downwardDistance, 0), raycastParams)
		if ray and ray.Position and ray.Normal and ray.Normal.Y >= 0.25 then
			return Vector3.new(targetPosition.X, ray.Position.Y + 0.08, targetPosition.Z)
		end

		return nil
	end

	local alignedPosition = castToFloor(1.5, 12) or castToFloor(3, 20)
	if alignedPosition then
		return alignedPosition
	end

	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.FilterDescendantsInstances = ignoreInstances

	local scanOrigin = Vector3.new(targetPosition.X, targetPosition.Y + TWEEN_GROUND_SCAN_HEIGHT, targetPosition.Z)
	local ray = workspace:Raycast(scanOrigin, Vector3.new(0, -(TWEEN_GROUND_SCAN_HEIGHT * 2), 0), raycastParams)
	if ray and ray.Position then
		return Vector3.new(targetPosition.X, ray.Position.Y + 0.08, targetPosition.Z)
	end

	return Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z)
end

function getEditorMouseGroundPosition(character)
	local camera = workspace.CurrentCamera
	if not camera then
		return nil
	end

	local mouseLocation = UserInputService:GetMouseLocation()
	local ray = camera:ViewportPointToRay(mouseLocation.X, mouseLocation.Y)
	local raycastParams = RaycastParams.new()
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.FilterDescendantsInstances = {
		character,
		getRecordedPathFolder(),
		getVisualizerFolder(),
		findPathPlayerAvoiderVisualizerFolder(),
	}

	local hit = workspace:Raycast(ray.Origin, ray.Direction * 1500, raycastParams)
	if hit and hit.Position then
		return getGroundAlignedPathPosition(hit.Position, character)
	end

	return nil
end

function updatePathEditorActiveNodeVisuals()
	local selectedNodeIndex = pathState.selectedNodeIndex
	if not selectedNodeIndex then
		return
	end

	local nodePosition = getPathNodePosition(pathState.workingPath.nodes[selectedNodeIndex])
	local nodePart = pathUi.editorNodeParts[selectedNodeIndex]
	if nodePosition and nodePart and nodePart.Parent then
		nodePart.Position = nodePosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
	end

	local handleDefinitions = {
		Vector3.new(-1.15, 0, 0),
		Vector3.new(1.15, 0, 0),
		Vector3.new(0, 1.15, 0),
		Vector3.new(0, -1.15, 0),
		Vector3.new(0, 0, 1.15),
		Vector3.new(0, 0, -1.15),
	}

	for index, offset in ipairs(handleDefinitions) do
		local handle = pathUi.editorNodeHandles[index]
		if handle and handle.Parent and nodePosition then
			handle.Position = nodePosition + offset + Vector3.new(0, VISUALIZER_HEIGHT, 0)
		end
	end
end

refreshPathVisuals = function()
	clearRecordedPathVisuals()
	local visualPathData = nil
	local visualSellNodeIndex = nil
	local visualSelectedNodeIndex = nil
	local showNodeHandles = false
	local showDepositLabels = false
	local showSelectedPathPreview = (activeTabName == "Autofarm" or activeTabName == "AUTOMINE")
		and pathState.subTab == "Paths"
		and type(pathState.selectedPathName) == "string"
		and pathState.paths[pathState.selectedPathName] ~= nil

	if pathState.editorVisible then
		local shouldRetrySellNodeAutoDetect = not pathState.sellNodeDirty
			and not pathState.sellNodeIndex
			and not getManualPathSellNodeIndex(pathState.workingPath)
			and #(pathState.workingPath.nodes or {}) > 0
			and (not cachedAutoSellTargets or (#cachedAutoSellTargets == 0 and (os.clock() - lastAutoSellTargetScanAt) >= 3))
		if pathState.sellNodeDirty or shouldRetrySellNodeAutoDetect then
			recomputeWorkingPathSellNode()
		end
		visualPathData = pathState.workingPath
		visualSellNodeIndex = pathState.sellNodeIndex
		visualSelectedNodeIndex = pathState.selectedNodeIndex
		showNodeHandles = pathState.editNodePlacementEnabled == true
		showDepositLabels = pathState.recording
	elseif pathState.playbackActive and pathState.currentPath then
		visualPathData = pathState.currentPath
		visualSellNodeIndex = getPathSellNodeIndex(visualPathData)
	elseif showSelectedPathPreview then
		visualPathData = pathState.paths[pathState.selectedPathName]
		visualSellNodeIndex = getPathSellNodeIndex(visualPathData)
	else
		return
	end

	if not visualPathData or #(visualPathData.nodes or {}) == 0 then
		return
	end

	local previousNodePosition = nil
	local markedOreLookup = {}

	local function getDepositLabelText(groupName, order)
		local oreLabel = getOreLabel(groupName)
		local isVein = string.find(string.lower(oreLabel), "vein", 1, true) ~= nil
		if order then
			return string.format("%d, %s", order, isVein and oreLabel or (oreLabel .. " Ore"))
		end
		return isVein and oreLabel or (oreLabel .. " Deposit")
	end

	local function createOreBillboard(orePart, text, textColor)
		local oreLabel = Instance.new("BillboardGui")
		oreLabel.Name = "OreOrderLabel"
		oreLabel.Size = UDim2.fromOffset(112, 24)
		oreLabel.StudsOffset = Vector3.new(0, 2.65, 0)
		oreLabel.AlwaysOnTop = true
		oreLabel.Parent = orePart

		local oreText = Instance.new("TextLabel")
		oreText.Size = UDim2.new(1, 0, 1, 0)
		oreText.BackgroundTransparency = 1
		oreText.Font = Enum.Font.GothamBold
		oreText.TextSize = 12
		oreText.TextColor3 = textColor
		oreText.TextStrokeTransparency = 0.2
		oreText.Text = text
		oreText.Parent = oreLabel

		table.insert(pathUi.editorOreMarkers, oreLabel)
	end

	local function addMarkedOreHighlights(oreDeposit)
		for _, orePart in ipairs(getOreRenderParts(oreDeposit)) do
			local highlight = Instance.new("Highlight")
			highlight.Name = "MarkedOre"
			highlight.FillTransparency = 0.55
			highlight.OutlineTransparency = 0
			highlight.FillColor = Color3.fromRGB(110, 255, 140)
			highlight.OutlineColor = Color3.fromRGB(255, 220, 90)
			highlight.Adornee = orePart
			highlight.Parent = getRecordedPathFolder()
			table.insert(pathUi.editorOreMarkers, highlight)
		end
	end

	for order, oreTarget in ipairs(visualPathData.ores or {}) do
		local resolvedOre = resolveSavedOreTarget(oreTarget)
		if resolvedOre then
			markedOreLookup[resolvedOre] = {
				order = order,
				groupName = oreTarget.groupName,
			}
		end
	end

	for index, node in ipairs(visualPathData.nodes or {}) do
		local nodePosition = getPathNodePosition(node)
		if not nodePosition then
			continue
		end
		if previousNodePosition then
			createRecordedPathSegment(previousNodePosition, nodePosition, Color3.fromRGB(214, 159, 92))
		end

		local nodePart = Instance.new("Part")
		nodePart.Name = "RecordedNode"
		nodePart.Shape = Enum.PartType.Ball
		nodePart.Size = PATH_RECORD_NODE_SIZE
		nodePart.Anchored = true
		nodePart.CanCollide = false
		nodePart.CanQuery = true
		nodePart.CanTouch = false
		nodePart.Material = Enum.Material.Neon
		nodePart.Transparency = 0.34
		nodePart.Color = index == visualSelectedNodeIndex
			and Color3.fromRGB(255, 214, 117)
			or ((pathState.playbackActive and not pathState.editorVisible and index == pathState.playbackNodeIndex) and Color3.fromRGB(130, 220, 255)
				or (index == visualSellNodeIndex and Color3.fromRGB(105, 220, 130) or Color3.fromRGB(223, 157, 82)))
		nodePart.Position = nodePosition + Vector3.new(0, VISUALIZER_HEIGHT, 0)
		nodePart.Parent = getRecordedPathFolder()
		nodePart:SetAttribute("PathNodeIndex", index)
		pathUi.editorNodeParts[index] = nodePart
		previousNodePosition = nodePosition

		if showNodeHandles and index == visualSelectedNodeIndex then
			local handleDefinitions = {
				{ axis = "X", direction = -1, color = Color3.fromRGB(255, 198, 92), size = Vector3.new(0.72, 0.2, 0.2), offset = Vector3.new(-1.15, 0, 0) },
				{ axis = "X", direction = 1, color = Color3.fromRGB(255, 198, 92), size = Vector3.new(0.72, 0.2, 0.2), offset = Vector3.new(1.15, 0, 0) },
				{ axis = "Y", direction = 1, color = Color3.fromRGB(120, 215, 255), size = Vector3.new(0.2, 0.72, 0.2), offset = Vector3.new(0, 1.15, 0) },
				{ axis = "Y", direction = -1, color = Color3.fromRGB(120, 215, 255), size = Vector3.new(0.2, 0.72, 0.2), offset = Vector3.new(0, -1.15, 0) },
				{ axis = "Z", direction = 1, color = Color3.fromRGB(160, 255, 163), size = Vector3.new(0.2, 0.2, 0.72), offset = Vector3.new(0, 0, 1.15) },
				{ axis = "Z", direction = -1, color = Color3.fromRGB(160, 255, 163), size = Vector3.new(0.2, 0.2, 0.72), offset = Vector3.new(0, 0, -1.15) },
			}

			for _, handleDefinition in ipairs(handleDefinitions) do
				local handle = Instance.new("Part")
				handle.Name = "PathArrow"
				handle.Anchored = true
				handle.CanCollide = false
				handle.CanQuery = true
				handle.CanTouch = false
				handle.Material = Enum.Material.Neon
				handle.Transparency = 0.18
				handle.Color = handleDefinition.color
				handle.Size = handleDefinition.size
				handle.Position = nodePosition + handleDefinition.offset + Vector3.new(0, VISUALIZER_HEIGHT, 0)
				handle.Parent = getRecordedPathFolder()
				handle:SetAttribute("PathNodeIndex", index)
				handle:SetAttribute("PathNodeAxis", handleDefinition.axis)
				handle:SetAttribute("PathNodeDirection", handleDefinition.direction)
				table.insert(pathUi.editorNodeHandles, handle)
			end
		end
	end

	if showDepositLabels then
		local oreDeposits = getOreDepositsFolder()
		if oreDeposits then
			for _, oreFolder in ipairs(oreDeposits:GetChildren()) do
				for _, oreDeposit in ipairs(oreFolder:GetChildren()) do
					local orePart = getOreBasePart(oreDeposit)
					if orePart then
						local markInfo = markedOreLookup[oreDeposit]
						if markInfo then
							addMarkedOreHighlights(oreDeposit)
							createOreBillboard(orePart, getDepositLabelText(markInfo.groupName, markInfo.order), Color3.fromRGB(255, 220, 90))
						else
							createOreBillboard(orePart, getDepositLabelText(oreFolder.Name), Color3.fromRGB(224, 211, 186))
						end
					end
				end
			end
		end
	else
		for resolvedOre, markInfo in pairs(markedOreLookup) do
			local orePart = getOreBasePart(resolvedOre)
			if orePart then
				addMarkedOreHighlights(resolvedOre)
				createOreBillboard(orePart, getDepositLabelText(markInfo.groupName, markInfo.order), Color3.fromRGB(255, 220, 90))
			end
		end
	end
end

_G.refreshPathVisuals = refreshPathVisuals

function refreshPathDropdownButtons()
	for _, button in pairs(pathUi.savedPathButtons) do
		if button and button.Parent then
			button:Destroy()
		end
	end
	pathUi.savedPathButtons = {}

	for _, pathName in ipairs(getSavedPathNamesOrdered()) do
		local button = Instance.new("TextButton")
		button.Name = pathName .. "PathButton"
		button.Size = UDim2.new(1, -2, 0, 28)
		button.Text = pathName
		button.Parent = pathUi.savedPathList
		styleButton(button, 12)
		button.MouseButton1Click:Connect(function()
			pathState.selectedPathName = pathName
			setSavedPathDropdownOpen(false)
			savePathsToStorage(false)
			refreshPathManagerButtons()
			refreshPathVisuals()
		end)
		pathUi.savedPathButtons[pathName] = button
	end
end

function refreshAutofarmSubTabs()
	if pathUi.useFobRows then
		for subTabName, button in pairs(pathUi.subTabButtons) do
			local active = pathState.subTab == subTabName
			button.TextColor3 = active and UI_COLORS.text or UI_COLORS.muted
			local underline = button:FindFirstChild("TabUnderline")
			if underline then
				underline.Size = active and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 0, 0, 2)
				underline.BackgroundColor3 = UI_COLORS.accent
			end
		end

		if autoFarmCardFrame then autoFarmCardFrame.Visible = false end
		if movementCardFrame then movementCardFrame.Visible = false end
		if pickupCardFrame then pickupCardFrame.Visible = false end
		if autoSellCardFrame then autoSellCardFrame.Visible = false end
		if liveFeedCardFrame then liveFeedCardFrame.Visible = false end
		if pathUi.pathsPage then pathUi.pathsPage.Visible = false end
		if pathUi.mainRowsPage then pathUi.mainRowsPage.Visible = pathState.subTab == "AutofarmSettings" end
		if pathUi.pathRowsPage then pathUi.pathRowsPage.Visible = pathState.subTab == "Paths" end
		return
	end

	for subTabName, button in pairs(pathUi.subTabButtons) do
		local active = pathState.subTab == subTabName
		button.TextColor3 = active and UI_COLORS.text or UI_COLORS.muted
		local underline = button:FindFirstChild("TabUnderline")
		if underline then
			underline.Size = active and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 0, 0, 2)
			underline.BackgroundColor3 = UI_COLORS.accent
		end
	end

	local showSettings = pathState.subTab == "AutofarmSettings"
	if autoFarmCardFrame then autoFarmCardFrame.Visible = showSettings end
	if movementCardFrame then movementCardFrame.Visible = showSettings end
	if pickupCardFrame then pickupCardFrame.Visible = showSettings end
	if autoSellCardFrame then autoSellCardFrame.Visible = showSettings end
	if liveFeedCardFrame then liveFeedCardFrame.Visible = showSettings end
	if pathUi.pathsPage then pathUi.pathsPage.Visible = not showSettings end
end

function refreshPathEditorButtons()
	if not pathUi.editorFrame then
		return
	end

	local manualSellNodeIndex = getManualPathSellNodeIndex(pathState.workingPath)
	local selectedNodeIndex = normalizePathNodeIndex(pathState.selectedNodeIndex, #(pathState.workingPath.nodes or {}))

	pathUi.editorFrame.Visible = pathState.editorVisible
	if pathUi.layoutEditorControls then
		pathUi.layoutEditorControls()
	end
	if pathUi.editorRecordLabel then
		pathUi.editorRecordLabel.Text = pathState.recording and "Stop Recording" or "Resume Recording"
	end
	if pathUi.editorRecordSync then
		pathUi.editorRecordSync(true)
	end
	if pathUi.editorSpawnSync then
		pathUi.editorSpawnSync()
	end
	if pathUi.editorNodeSpacingSync then
		local currentNodeSpacing = (type(getPathRecordNodeSpacing) == "function" and getPathRecordNodeSpacing())
			or math.clamp(tonumber(S.pathRecordNodeSpacing) or PATH_RECORD_NODE_SPACING or 9, 1, 10)
		pathUi.editorNodeSpacingSync(currentNodeSpacing, true)
	end
	if pathUi.editorEditNodePlacementLabel then
		pathUi.editorEditNodePlacementLabel.Text = "Edit Node Placement"
	end
	if pathUi.editorEditNodePlacementSync then
		pathUi.editorEditNodePlacementSync(true)
	end
	if pathState.editingSavedPath then
		pathUi.editorNameBox.Visible = false
		pathUi.editorSaveConfirmButton.Visible = false
	end
	if pathUi.editorSaveLabel then
		pathUi.editorSaveLabel.Text = pathState.editingSavedPath and "Update Path" or "Save Path"
	end
	if pathUi.editorSaveConfirmLabel then
		pathUi.editorSaveConfirmLabel.Text = "Confirm Save"
	end
	if pathUi.editorMarkKeyLabel then
		pathUi.editorMarkKeyLabel.Text = pendingKeybindAction == "markore"
			and "Mark Ore Key | Press..."
			or ("Mark Ore Key | " .. formatKeyCodeName(pathState.markOreKey))
	end
	if pathUi.editorDeleteModeLabel then
		pathUi.editorDeleteModeLabel.Text = "Delete Mode"
	end
	if pathUi.editorDeleteModeSync then
		pathUi.editorDeleteModeSync(true)
	end
	if pathUi.editorSellNodeLabel then
		local currentSellText = manualSellNodeIndex and ("Node #" .. tostring(manualSellNodeIndex)) or "Auto"
		if pathState.sellNodePickMode then
			pathUi.editorSellNodeLabel.Text = "Pick Sell Node | Click a node"
		elseif selectedNodeIndex and manualSellNodeIndex == selectedNodeIndex then
			pathUi.editorSellNodeLabel.Text = "Pick Sell Node | " .. currentSellText
		else
			pathUi.editorSellNodeLabel.Text = "Pick Sell Node | " .. currentSellText
		end
	end
	pathUi.editorTitle.Text = pathState.editingSavedPath and "Edit Path" or "Record Path"
end

function refreshPathPlayerAvoiderRowVisibility(instant)
	if type(setAnimatedRowVisible) ~= "function" then
		return
	end
	local avoiderEnabled = isPathPlayerAvoiderEnabled()
	local methodName = getPathPlayerAvoiderMethod()
	local showCorridorRows = avoiderEnabled and methodName == "Corridor"
	local showRadiusRows = avoiderEnabled and methodName == "Radius Field"
	if pathUi.playerAvoiderMethodRow then
		setAnimatedRowVisible(pathUi.playerAvoiderMethodRow, avoiderEnabled, 38, instant)
	end
	if pathUi.playerAvoiderActionRow then
		setAnimatedRowVisible(pathUi.playerAvoiderActionRow, avoiderEnabled, 38, instant)
	end
	if pathUi.playerAvoiderDistanceRow then
		setAnimatedRowVisible(pathUi.playerAvoiderDistanceRow, showCorridorRows, 38, instant)
	end
	if pathUi.playerAvoiderShowCorridorRow then
		setAnimatedRowVisible(pathUi.playerAvoiderShowCorridorRow, showCorridorRows, 38, instant)
	end
	if pathUi.playerAvoiderRadiusRow then
		setAnimatedRowVisible(pathUi.playerAvoiderRadiusRow, showRadiusRows, 38, instant)
	end
	if pathUi.playerAvoiderShowRadiusRow then
		setAnimatedRowVisible(pathUi.playerAvoiderShowRadiusRow, showRadiusRows, 38, instant)
	end
end

function refreshDiscordAutomineWebhookRowVisibility(instant)
	if type(setAnimatedRowVisible) ~= "function" then
		return
	end
	local trackingEnabled = isDiscordAutomineWebhookTrackingEnabled()
	if settingsUi.discordAutomineWebhookOptionsRow then
		setAnimatedRowVisible(settingsUi.discordAutomineWebhookOptionsRow, trackingEnabled, 38, instant)
	end
end

refreshPathManagerButtons = function()
	if pathUi.useFobRows then
		ensurePathSlotState()
		local focusedSlotIndex = normalizePathSlotIndex(pathState.focusedPathSlotIndex) or 1
		local selectedPath = getPathSlotSelection(focusedSlotIndex, false)
		local selectedExists = type(selectedPath) == "string" and pathState.paths[selectedPath] ~= nil
		if headerSubtitleLabel then
			headerSubtitleLabel.Text = "Saved Path: " .. tostring(selectedExists and selectedPath or "None")
		end
		if pathUi.serverHopSync then
			pathUi.serverHopSync()
		end
		if pathUi.staffHopSync then
			pathUi.staffHopSync()
		end
		if pathUi.farmPathsSync then
			pathUi.farmPathsSync()
		end
		if pathUi.hopOnDeathSync then
			pathUi.hopOnDeathSync()
		end
		if pathUi.playerAvoiderSync then
			pathUi.playerAvoiderSync()
		end
		if pathUi.playerAvoiderMethodSync then
			pathUi.playerAvoiderMethodSync()
		end
		if pathUi.playerAvoiderActionSync then
			pathUi.playerAvoiderActionSync()
		end
		if pathUi.playerAvoiderDistanceSync then
			pathUi.playerAvoiderDistanceSync()
		end
		if pathUi.playerAvoiderShowCorridorSync then
			pathUi.playerAvoiderShowCorridorSync()
		end
		if pathUi.playerAvoiderRadiusSync then
			pathUi.playerAvoiderRadiusSync()
		end
		if pathUi.playerAvoiderShowRadiusSync then
			pathUi.playerAvoiderShowRadiusSync()
		end
		refreshPathPlayerAvoiderRowVisibility(true)
		if type(refreshPathDeathHopConnection) == "function" then
			refreshPathDeathHopConnection()
		end
		for slotIndex, slotUi in ipairs(pathUi.pathSlotControls or {}) do
			local slotVisible = slotIndex <= pathState.pathSectionCount
			if slotUi.host then
				slotUi.host.AutomaticSize = slotVisible and Enum.AutomaticSize.Y or Enum.AutomaticSize.None
				slotUi.host.Size = UDim2.new(1, 0, 0, 0)
				slotUi.host.Visible = slotVisible
			end
			if slotUi.headerLabel then
				slotUi.headerLabel.Text = "Path " .. tostring(slotIndex)
			end
			if slotUi.removeSectionButton then
				slotUi.removeSectionButton.Visible = slotVisible and pathState.pathSectionCount > 1
			end
			if slotVisible then
				if slotUi.savedPathSync then
					slotUi.savedPathSync()
				end
				if slotUi.pathPlaySync then
					slotUi.pathPlaySync()
				end
				if slotUi.alwaysPlaySync then
					slotUi.alwaysPlaySync()
				end
				if slotUi.editPathSync then
					slotUi.editPathSync(true)
				end
				if slotUi.recordLabel then
					slotUi.recordLabel.Text = (pathState.editorVisible and not pathState.editingSavedPath and focusedSlotIndex == slotIndex)
						and "Close Recorder" or "Record Path"
				end
				if slotUi.deleteLabel then
					slotUi.deleteLabel.Text = isPathDeleteConfirmArmed(slotIndex) and "Confirm Delete" or "Delete Path"
				end
			end
		end
		if pathUi.addPathButton then
			local showAddButton = pathState.pathSectionCount < MAX_PATH_PLAYBACK_SLOTS
			pathUi.addPathButton.Visible = showAddButton
			pathUi.addPathButton.Size = UDim2.new(1, 0, 0, showAddButton and 38 or 0)
		end
		return
	end

	if not pathUi.managerCard then
		return
	end

	refreshPathDropdownButtons()
	local selectedPath = pathState.selectedPathName
	local selectedExists = type(selectedPath) == "string" and pathState.paths[selectedPath] ~= nil
	local selectedCount = #getSavedPathNamesOrdered()
	local dropdownHeight = pathState.pathDropdownOpen and math.min(140, math.max(34, selectedCount * 34)) or 0
	if headerSubtitleLabel then
		headerSubtitleLabel.Text = "Saved Path: " .. tostring(selectedExists and selectedPath or "None")
	end

	pathUi.savedPathDropdownFrame.Visible = pathState.pathDropdownOpen
	if pathUi.savedPathDropdownFrame.Visible then
		pathUi.savedPathDropdownFrame.Size = UDim2.new(1, -24, 0, dropdownHeight)
	end

	pathUi.savedPathDropdownButton.Text = selectedExists and ("Selected Path  |  " .. selectedPath) or "Choose Saved Path"
	pathUi.recordPathButton.Text = (pathState.editorVisible and not pathState.editingSavedPath) and "Close Recorder" or "Record Path"
	pathUi.loopPlayButton.Visible = selectedExists
	pathUi.alwaysPlayButton.Visible = selectedExists
	pathUi.editPathButton.Visible = selectedExists
	pathUi.deletePathButton.Visible = selectedExists

	local loopY = 124 + dropdownHeight
	local alwaysY = loopY + (selectedExists and 36 or 0)
	local actionsY = alwaysY + (selectedExists and 36 or 0)
	pathUi.loopPlayButton.Position = UDim2.fromOffset(12, loopY)
	pathUi.alwaysPlayButton.Position = UDim2.fromOffset(12, alwaysY)
	pathUi.editPathButton.Position = UDim2.fromOffset(12, actionsY)
	pathUi.deletePathButton.Position = UDim2.new(0.5, 6, 0, actionsY)

	local pathActive = isPathPlaybackBusy()
	pathUi.loopPlayButton.Text = pathActive and "Play Path: ON" or "Play Path: OFF"
	pathUi.loopPlayButton.BackgroundColor3 = pathActive and UI_COLORS.successMuted or UI_COLORS.surface
	pathUi.alwaysPlayButton.Text = pathState.alwaysPlayEnabled and "Always Play Path: ON" or "Always Play Path: OFF"
	pathUi.alwaysPlayButton.BackgroundColor3 = pathState.alwaysPlayEnabled and UI_COLORS.accentMuted or UI_COLORS.surface
	pathUi.deletePathButton.Text = pathState.deleteConfirmArmed and "Confirm Delete" or "Delete Path"

	local managerHeight = selectedExists and (actionsY + 42) or (124 + dropdownHeight + 12)
	pathUi.managerCard.Size = UDim2.new(1, 0, 0, managerHeight)
	pathUi.pathsPage.Size = UDim2.new(1, 0, 0, managerHeight)
end

_G.refreshPathManagerButtons = refreshPathManagerButtons

function setAutofarmSubTab(subTabName)
	pathState.subTab = subTabName
	if subTabName ~= "AutofarmSettings" then
		setMovementModeDropdownOpen(false)
	end
	if subTabName == "Paths" then
		ensurePathStorageSupport(true)
	end
	refreshAutofarmSubTabs()
	refreshPathVisuals()
	updateAutofarmLayout()
end

setSavedPathDropdownOpen = function(isOpen)
	pathState.pathDropdownOpen = isOpen and true or false
	refreshPathManagerButtons()
	updateAutofarmLayout()
end

function openPathEditor(isEditingSavedPath)
	pathState.editorVisible = true
	pathState.editingSavedPath = isEditingSavedPath == true
	pathState.deleteMode = false
	pathState.editNodePlacementEnabled = false
	pathState.sellNodePickMode = false
	pathState.selectedNodeIndex = nil
	pathState.draggingNodeIndex = nil
	pathState.draggingNodeAxis = nil
	pathState.dragLastPointerPosition = nil
	pathUi.editorNameBox.Visible = false
	pathUi.editorSaveConfirmButton.Visible = false
	refreshPathEditorButtons()
	refreshPathVisuals()
	setPathEditorIdleStatus()
	refreshPathManagerButtons()
end

function closePathEditor()
	pathState.editorVisible = false
	pathState.recording = false
	pathState.deleteMode = false
	pathState.editNodePlacementEnabled = false
	pathState.sellNodePickMode = false
	pathState.editingSavedPath = false
	pathState.selectedNodeIndex = nil
	pathState.draggingNodeIndex = nil
	pathState.draggingNodeAxis = nil
	pathState.dragLastPointerPosition = nil
	if pendingKeybindAction == "markore" then
		pendingKeybindAction = nil
	end
	pathUi.editorNameBox.Visible = false
	pathUi.editorSaveConfirmButton.Visible = false
	refreshPathEditorButtons()
	refreshPathVisuals()
	setPathEditorIdleStatus()
	refreshPathManagerButtons()
end

ensureWorkingPathStartNode = nil

function beginNewPathRecording(slotIndex)
	ensurePathStorageSupport(true)
	setFocusedPathSlotIndex(slotIndex or pathState.focusedPathSlotIndex or 1, false)

	if autoMineEnabled then
		stopAutoMine()
	end

	pathState.selectedPathName = nil
	setPathDeleteConfirmSlot(nil)
	pathState.pathDropdownOpen = false
	pathState.workingPath = {
		nodes = {},
		ores = {},
		manualSellNodeIndex = nil,
		spawnName = nil,
	}
	pathState.sellNodeDirty = true
	pathState.sellNodeIndex = nil
	pathState.lastRecordedNodePosition = nil
	pathState.recording = true
	openPathEditor(false)
	if ensureWorkingPathStartNode() then
		refreshPathVisuals()
	else
		setPathEditorStatus("Recording started. Move a little if the first node does not appear right away.", UI_COLORS.muted)
	end
end

function beginPathEditing(pathName, slotIndex)
	if not pathName or not pathState.paths[pathName] then
		return
	end

	ensurePathStorageSupport(true)
	setFocusedPathSlotIndex(slotIndex or pathState.focusedPathSlotIndex or 1, true)

	if autoMineEnabled then
		stopAutoMine()
	end

	pathState.workingPath = clonePathData(pathState.paths[pathName])
	pathState.sellNodeDirty = true
	pathState.selectedPathName = pathName
	pathState.lastRecordedNodePosition = nil
	pathState.recording = false
	openPathEditor(true)
end

function setEditNodePlacementEnabled(isEnabled)
	local nextValue = isEnabled == true
	if pathState.editNodePlacementEnabled == nextValue then
		refreshPathEditorButtons()
		return
	end

	pathState.editNodePlacementEnabled = nextValue
	if nextValue then
		pathState.deleteMode = false
		pathState.sellNodePickMode = false
	else
		pathState.selectedNodeIndex = nil
		pathState.draggingNodeIndex = nil
		pathState.draggingNodeAxis = nil
		pathState.dragLastPointerPosition = nil
	end

	refreshPathEditorButtons()
	refreshPathVisuals()
	setPathEditorIdleStatus()
end

function addWorkingPathNode(position)
	if not position then
		return
	end

	position = getGroundAlignedPathPosition(position, cachedCharacter or getCustomCharacter()) or position

	table.insert(pathState.workingPath.nodes, {
		position = position,
	})
	pathState.lastRecordedNodePosition = position
	pathState.sellNodeDirty = true
	recomputeWorkingPathSellNode()
	refreshPathVisuals()
end

ensureWorkingPathStartNode = function()
	if #pathState.workingPath.nodes > 0 then
		return true
	end

	local character = cachedCharacter or getCustomCharacter() or (localPlayer and localPlayer.Character)
	local rootPart = getRootPart(character)
	if not rootPart and character then
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		rootPart = (humanoid and humanoid.RootPart) or character.PrimaryPart
	end
	if rootPart then
		addWorkingPathNode(rootPart.Position)
		return true
	end
	return false
end

function toggleSelectedWorkingPathSellNode()
	if not pathState.editorVisible then
		return false
	end

	pathState.sellNodePickMode = not pathState.sellNodePickMode
	pathState.editNodePlacementEnabled = false
	pathState.deleteMode = false
	pathState.draggingNodeIndex = nil
	pathState.draggingNodeAxis = nil
	pathState.dragLastPointerPosition = nil
	refreshPathEditorButtons()
	if pathState.sellNodePickMode then
		setPathEditorStatus("Click a node to set the sell point. Clicking the current sell node clears it.", UI_COLORS.accent)
	else
		setPathEditorIdleStatus()
	end
	return true
end

function applyWorkingPathSellNodeSelection(nodeIndex)
	local normalizedNodeIndex = normalizePathNodeIndex(nodeIndex, #(pathState.workingPath.nodes or {}))
	if not normalizedNodeIndex then
		return false
	end

	local manualSellNodeIndex = getManualPathSellNodeIndex(pathState.workingPath)
	pathState.selectedNodeIndex = normalizedNodeIndex
	pathState.sellNodePickMode = false
	pathState.draggingNodeIndex = nil
	pathState.draggingNodeAxis = nil
	pathState.dragLastPointerPosition = nil

	if manualSellNodeIndex == normalizedNodeIndex then
		pathState.workingPath.manualSellNodeIndex = nil
		pathState.sellNodeDirty = true
		recomputeWorkingPathSellNode()
		refreshPathVisuals()
		refreshPathEditorButtons()
		setPathEditorStatus("Sell node reset to auto detection.", UI_COLORS.warning)
		return true
	end

	pathState.workingPath.manualSellNodeIndex = normalizedNodeIndex
	pathState.sellNodeDirty = true
	recomputeWorkingPathSellNode()
	refreshPathVisuals()
	refreshPathEditorButtons()
	setPathEditorStatus("Manual sell node set to #" .. tostring(normalizedNodeIndex) .. ".", UI_COLORS.success)
	return true
end

function saveWorkingPath(pathName)
	local trimmedName = string.gsub(tostring(pathName or ""), "^%s+", "")
	trimmedName = string.gsub(trimmedName, "%s+$", "")
	if trimmedName == "" then
		setPathEditorStatus("Enter a path name before saving.", UI_COLORS.warning)
		return false
	end

	if #pathState.workingPath.nodes == 0 then
		setPathEditorStatus("You need at least one node before saving.", UI_COLORS.warning)
		return false
	end
	if not getWorkingPathSpawnName() then
		setPathEditorStatus("Pick a spawn before saving this path.", UI_COLORS.warning)
		return false
	end

	pathState.paths[trimmedName] = clonePathData(pathState.workingPath)
	setPathSlotSelection(pathState.focusedPathSlotIndex, trimmedName, {syncSelectedPath = true})
	setPathDeleteConfirmSlot(nil)
	pathState.pendingPathNameCapture = false
	pathUi.editorNameBox.Visible = false
	pathUi.editorSaveConfirmButton.Visible = false
	pathUi.editorNameBox.Text = trimmedName
	savePathsToStorage(true)
	refreshPathManagerButtons()
	updateAutofarmLayout()
	setPathEditorStatus("Saved path: " .. trimmedName, UI_COLORS.success)
	return true
end

function updateWorkingPath()
	local selectedPathName = tostring(pathState.selectedPathName or "")
	if selectedPathName == "" or not pathState.paths[selectedPathName] then
		setPathEditorStatus("Pick a saved path before updating.", UI_COLORS.warning)
		return false
	end

	if #pathState.workingPath.nodes == 0 then
		setPathEditorStatus("You need at least one node before updating.", UI_COLORS.warning)
		return false
	end
	if not getWorkingPathSpawnName() then
		setPathEditorStatus("Pick a spawn before updating this path.", UI_COLORS.warning)
		return false
	end

	pathState.paths[selectedPathName] = clonePathData(pathState.workingPath)
	setPathSlotSelection(pathState.focusedPathSlotIndex, selectedPathName, {syncSelectedPath = true})
	setPathDeleteConfirmSlot(nil)
	pathUi.editorNameBox.Visible = false
	pathUi.editorSaveConfirmButton.Visible = false
	savePathsToStorage(true)
	refreshPathManagerButtons()
	refreshPathEditorButtons()
	refreshPathVisuals()
	updateAutofarmLayout()
	setPathEditorStatus("Path updated.", UI_COLORS.success)
	spawnNotification("Path updated", "success", true)
	return true
end

function deleteSelectedPath(slotIndex)
	local targetSlotIndex = normalizePathSlotIndex(slotIndex) or pathState.focusedPathSlotIndex or 1
	local selectedName = getPathSlotSelection(targetSlotIndex, false)
	if not selectedName or not pathState.paths[selectedName] then
		return false
	end

	pathState.paths[selectedName] = nil
	clearPathReferences(selectedName)
	if pathState.playbackPathName == selectedName
		or pathState.spawnPlaybackPendingPathName == selectedName
		or pathState.spawnPlaybackReadyPathName == selectedName then
		pathState.playLoopEnabled = false
		pathState.alwaysPlayEnabled = false
		pathState.resumePathPlaybackPending = false
		resetPathPlaybackState()
		cancelPendingPathSpawnPlayback(true)
	end
	if pathState.editorVisible and pathState.editingSavedPath then
		pathState.editorVisible = false
		clearRecordedPathVisuals()
	end
	setFocusedPathSlotIndex(targetSlotIndex, true)
	setPathDeleteConfirmSlot(nil)
	savePathsToStorage(true)
	refreshPathManagerButtons()
	refreshPathEditorButtons()
	refreshPathVisuals()
	updateAutofarmLayout()
	notifyUser("Paths", "Deleted path: " .. selectedName, 4)
	return true
end

function tryMarkHoveredOre()
	if not pathState.editorVisible then
		return false
	end

	ensureWorkingPathStartNode()
	if #pathState.workingPath.nodes == 0 then
		setPathEditorStatus("Record at least one node before marking ores.", UI_COLORS.warning)
		return false
	end

	local mouse = LocalPlayer:GetMouse()
	local oreDeposit = mouse and getOreDepositFromInstance(mouse.Target)
	if not oreDeposit then
		setPathEditorStatus("Hover an ore deposit, then press the mark key.", UI_COLORS.warning)
		return false
	end

	local orePosition = getOrePosition(oreDeposit)
	local groupName = getOreGroupName(oreDeposit)
	if not orePosition then
		setPathEditorStatus("That ore could not be resolved right now.", UI_COLORS.warning)
		return false
	end
	if not groupName then
		setPathEditorStatus("That ore type could not be identified right now.", UI_COLORS.warning)
		return false
	end

	for order, existingOre in ipairs(pathState.workingPath.ores) do
		if existingOre.groupName == groupName and existingOre.position and (existingOre.position - orePosition).Magnitude <= 6 then
			table.remove(pathState.workingPath.ores, order)
			refreshPathVisuals()
			setPathEditorStatus("Unmarked " .. getOreLabel(groupName) .. ".", UI_COLORS.warning)
			return false
		end
	end

	local nodeIndex, nodeDistance = getClosestPathNodeIndex(orePosition)
	if not nodeIndex or nodeDistance > MINE_AURA_RANGE then
		setPathEditorStatus("Move a sphere closer to that ore before marking it.", UI_COLORS.warning)
		return false
	end

	table.insert(pathState.workingPath.ores, {
		groupName = groupName,
		position = orePosition,
		nodeIndex = nodeIndex,
	})
	refreshPathVisuals()
	setPathEditorStatus("Marked ore #" .. tostring(#pathState.workingPath.ores) .. ": " .. getOreLabel(groupName), UI_COLORS.success)
	return true
end

function toggleAutoMineFromControl(sourceLabel)
	return togglePathLoopFromControl(sourceLabel)
end

function togglePathLoopFromControl(sourceLabel, slotIndex)
	if os.clock() < ignoreToggleUntil then
		pushDebug("path play toggle ignored from " .. sourceLabel)
		return
	end

	local now = os.clock()
	if (now - lastToggleAt) < TOGGLE_DEBOUNCE then
		pushDebug("path play toggle ignored by debounce")
		return
	end
	lastToggleAt = now

	local requestedStartSlot = getPlayablePathSlotIndex(slotIndex or pathState.sequenceStartSlotIndex or 1)
	if not requestedStartSlot then
		pushDebug("path play start blocked: no path selected")
		setStatus("Pick a saved path first", Color3.fromRGB(255, 190, 120))
		return
	end
	setPathSequenceStartSlotIndex(requestedStartSlot)
	setFocusedPathSlotIndex(requestedStartSlot, true)

	local pathActive = isPathPlaybackBusy()
	if pathActive then
		pathState.playLoopEnabled = false
		if autoMineEnabled and activeFarmMode == "path" then
			stopAutoMine()
		else
			cancelPendingPathSpawnPlayback(true)
			activeFarmMode = nil
			setStatus("Idle", Color3.fromRGB(180, 180, 180))
		end
	else
		pathState.playLoopEnabled = true
		if autoMineEnabled then
			stopAutoMine()
		end
		activeFarmMode = "path"
		local ok, started = xpcall(function()
			return startPathPlaybackForSlot(requestedStartSlot, sourceLabel)
		end, debug.traceback)
		if not ok then
			autoMineEnabled = false
			pathState.playLoopEnabled = false
			activeFarmMode = nil
			setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
			pushDebug(started)
		elseif not started then
			autoMineEnabled = false
			pathState.playLoopEnabled = false
			activeFarmMode = nil
		end
	end

	savePathsToStorage(true)
	refreshButton()
	refreshPathManagerButtons()
	refreshServerHopDisplay()
end

end

dashboardLabels = {}
function countAssignedKeybinds()
    local count = 0
    for _ in pairs(S.keybinds) do
        count = count + 1
    end
    return count
end

function refreshDashboard()
    if dashboardLabels.notifications then
        dashboardLabels.notifications.Text = "notifications: " .. (S.notificationsEnabled and "enabled" or "disabled")
    end
    if dashboardLabels.guiKey then
        dashboardLabels.guiKey.Text = "gui key: " .. guiToggleKeyName
    end
    if dashboardLabels.panicKey then
        dashboardLabels.panicKey.Text = "panic key: " .. panicKeyName
    end
    if dashboardLabels.fpsPing then
        dashboardLabels.fpsPing.Text = "fps & ping stats: " .. (S.fpsPingStatsEnabled and "enabled" or "disabled")
    end
    if dashboardLabels.transparency then
        dashboardLabels.transparency.Text = "transparency: " .. tostring(S.guiTransparency) .. "%"
    end
    if dashboardLabels.storage then
        if storageEnabled then
            local selectedName = getSelectedConfigName()
            local autoLoadName = getAutoLoadConfigName()
            if selectedName == CONFIG_SYSTEM.AUTOSAVE_NAME then
                dashboardLabels.storage.Text = autoLoadName == selectedName and "configs: autosave from last session [auto load]" or "configs: autosave from last session"
            elseif type(selectedName) == "string" and selectedName ~= "" then
                dashboardLabels.storage.Text = autoLoadName == selectedName and ("configs: " .. selectedName .. " [auto load]") or ("configs: " .. selectedName)
            elseif type(autoLoadName) == "string" and autoLoadName ~= "" then
                dashboardLabels.storage.Text = "configs: auto load -> " .. autoLoadName
            elseif storageLoaded then
                dashboardLabels.storage.Text = "configs: ready"
            else
                dashboardLabels.storage.Text = "configs: none selected"
            end
        else
            dashboardLabels.storage.Text = "configs: session only"
        end
    end
    hubMain.GroupTransparency = S.guiTransparency / 100
end

function syncAllUi()
    for _, syncFn in ipairs(_allUiSyncs) do
        pcall(syncFn)
    end
    refreshDashboard()
end

function createCompactActionButton(parent, name, text, color, layoutOrder, onClickFn)
    local button = Instance.new("TextButton")
    button.Name = name
    button.Size = UDim2.new(0.5, -6, 0, 34)
    button.LayoutOrder = layoutOrder
    button.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    button.BorderSizePixel = 0
    button.Text = text
    button.TextColor3 = color
    button.Font = Enum.Font.Code
    button.TextSize = 12
    button.AutoButtonColor = false
    button.Parent = parent
    Instance.new("UICorner", button).CornerRadius = UDim.new(0, 4)

    local stroke = Instance.new("UIStroke")
    stroke.Color = color
    stroke.Thickness = 1
    stroke.Transparency = 0.4
    stroke.Parent = button

    _UTILS._applySoundsToButton(button)

    button.MouseEnter:Connect(function()
        if destroyed then
            return
        end
        _TweenService:Create(button, TweenInfo.new(0.15), {
            BackgroundColor3 = Color3.fromRGB(35, 35, 35),
        }):Play()
    end)

    button.MouseLeave:Connect(function()
        if destroyed then
            return
        end
        _TweenService:Create(button, TweenInfo.new(0.15), {
            BackgroundColor3 = Color3.fromRGB(25, 25, 25),
        }):Play()
    end)

    button.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(button)
        onClickFn()
    end)

    return button
end

function createKeyCaptureRow(name, parentList, accentColor, getDisplayNameFn, setKeyFn, helpText)
    local row, icon, label, topLine, bottomLine, diamond = createRow(name)
    row.Parent = parentList
    applyHoverState(row, icon, label, topLine, bottomLine, diamond, false)
    icon.BackgroundColor3 = accentColor

    local keyBoxBg = Instance.new("Frame")
    keyBoxBg.Size = UDim2.new(0, 90, 0, 22)
    keyBoxBg.Position = UDim2.new(1, -105, 0.5, -11)
    keyBoxBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    keyBoxBg.BorderSizePixel = 1
    keyBoxBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
    keyBoxBg.Parent = row
    Instance.new("UICorner", keyBoxBg).CornerRadius = UDim.new(0, 4)

    local keyLabel = Instance.new("TextLabel")
    keyLabel.Size = UDim2.new(1, 0, 1, 0)
    keyLabel.BackgroundTransparency = 1
    keyLabel.TextColor3 = Color3.new(1, 1, 1)
    keyLabel.Text = getDisplayNameFn()
    keyLabel.Font = Enum.Font.GothamMedium
    keyLabel.TextSize = 12
    keyLabel.Parent = keyBoxBg

    local keyButton = Instance.new("TextButton")
    keyButton.Size = UDim2.new(1, 0, 1, 0)
    keyButton.BackgroundTransparency = 1
    keyButton.Text = ""
    keyButton.ZIndex = 3
    keyButton.Parent = keyBoxBg
    _UTILS._applySoundsToButton(keyButton)

    local listening = false
    local listenConnection = nil

    local function stopListening(resetText)
        if not listening then
            return
        end
        listening = false
        endKeyCapture()
        if listenConnection then
            listenConnection:Disconnect()
            listenConnection = nil
        end
        keyBoxBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
        keyLabel.TextColor3 = Color3.new(1, 1, 1)
        if resetText then
            keyLabel.Text = getDisplayNameFn()
        end
    end

    keyButton.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(keyBoxBg)
        if listening then
            stopListening(true)
            return
        end
        listening = true
        beginKeyCapture()
        keyLabel.Text = ". . ."
        keyBoxBg.BorderColor3 = accentColor
        keyLabel.TextColor3 = accentColor
        if helpText then
            spawnNotification(helpText, "info", true)
        end
        listenConnection = _UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if destroyed or not listening or gameProcessed then
                return
            end
            if input.UserInputType ~= Enum.UserInputType.Keyboard then
                return
            end
            if input.KeyCode == Enum.KeyCode.Escape or input.KeyCode == Enum.KeyCode.Backspace then
                stopListening(true)
                return
            end
            if input.KeyCode == Enum.KeyCode.Delete then
                setKeyFn(nil)
                stopListening(true)
                return
            end
            if input.KeyCode ~= Enum.KeyCode.Unknown then
                setKeyFn(input.KeyCode)
                stopListening(true)
            end
        end)
    end)

    registerUiSync(_allTextSyncs, function()
        keyLabel.Text = getDisplayNameFn()
    end)

    return row
end

function setAnimatedRowVisible(row, visible, expandedHeight, instant)
    if not row then
        return
    end

    local rowState = S._rowTweenState[row]
    if not rowState then
        rowState = {}
        S._rowTweenState[row] = rowState
    end

    local targetHeight = tonumber(expandedHeight) or 38
    local targetSize = visible and UDim2.new(1, 0, 0, targetHeight) or UDim2.new(1, 0, 0, 0)
    row.ClipsDescendants = true
    row.AutoButtonColor = visible == true
    row.Active = visible == true
    rowState.targetVisible = visible == true
    rowState.targetHeight = targetHeight

    if rowState.tween then
        pcall(function()
            rowState.tween:Cancel()
        end)
        rowState.tween = nil
    end

    if instant then
        row.Visible = visible == true
        row.Size = targetSize
        return
    end

    if visible then
        if not row.Visible then
            row.Visible = true
            row.Size = UDim2.new(1, 0, 0, 0)
        end
    elseif not row.Visible then
        row.Size = targetSize
        return
    end

    local tween = _TweenService:Create(row, TweenInfo.new(0.22, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        Size = targetSize,
    })
    rowState.tween = tween
    tween.Completed:Connect(function()
        if S._rowTweenState[row] ~= rowState or rowState.tween ~= tween then
            return
        end
        rowState.tween = nil
        if rowState.targetVisible then
            row.Visible = true
            row.Size = UDim2.new(1, 0, 0, rowState.targetHeight or targetHeight)
        else
            row.Visible = false
            row.Size = UDim2.new(1, 0, 0, 0)
        end
    end)
    tween:Play()
end

function createToggleKeybindRow(name, parentList, accentColor, getStateFn, onClickFn, getKeyNameFn, setKeyFn, helpText)
    local row, _, syncLabels = createToggleRow(name, parentList, getStateFn, onClickFn)

    local keyBoxBg = Instance.new("Frame")
    keyBoxBg.Size = UDim2.new(0, 76, 0, 22)
    keyBoxBg.Position = UDim2.new(1, -150, 0.5, -11)
    keyBoxBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    keyBoxBg.BorderSizePixel = 1
    keyBoxBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
    keyBoxBg.Parent = row
    Instance.new("UICorner", keyBoxBg).CornerRadius = UDim.new(0, 4)

    local keyLabel = Instance.new("TextLabel")
    keyLabel.Size = UDim2.new(1, 0, 1, 0)
    keyLabel.BackgroundTransparency = 1
    keyLabel.TextColor3 = Color3.new(1, 1, 1)
    keyLabel.Text = getKeyNameFn()
    keyLabel.Font = Enum.Font.GothamMedium
    keyLabel.TextSize = 12
    keyLabel.Parent = keyBoxBg

    local keyButton = Instance.new("TextButton")
    keyButton.Size = UDim2.new(1, 0, 1, 0)
    keyButton.BackgroundTransparency = 1
    keyButton.Text = ""
    keyButton.ZIndex = 3
    keyButton.Parent = keyBoxBg
    _UTILS._applySoundsToButton(keyButton)

    local listening = false
    local listenConnection = nil

    local function stopListening(resetText)
        if not listening then
            return
        end
        listening = false
        endKeyCapture()
        if listenConnection then
            listenConnection:Disconnect()
            listenConnection = nil
        end
        keyBoxBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
        keyLabel.TextColor3 = Color3.new(1, 1, 1)
        if resetText then
            keyLabel.Text = getKeyNameFn()
        end
    end

    keyButton.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(keyBoxBg)
        if listening then
            stopListening(true)
            return
        end
        listening = true
        beginKeyCapture()
        keyLabel.Text = ". . ."
        keyBoxBg.BorderColor3 = accentColor
        keyLabel.TextColor3 = accentColor
        if helpText then
            spawnNotification(helpText, "info", true)
        end
        listenConnection = _UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if destroyed or not listening or gameProcessed then
                return
            end
            if input.UserInputType ~= Enum.UserInputType.Keyboard then
                return
            end
            if input.KeyCode == Enum.KeyCode.Escape or input.KeyCode == Enum.KeyCode.Backspace then
                stopListening(true)
                return
            end
            if input.KeyCode == Enum.KeyCode.Delete then
                setKeyFn(nil)
                stopListening(true)
                pcall(syncAllUi)
                return
            end
            if input.KeyCode ~= Enum.KeyCode.Unknown then
                setKeyFn(input.KeyCode)
                stopListening(true)
                pcall(syncAllUi)
            end
        end)
    end)

    registerUiSync(_allTextSyncs, function()
        if not listening then
            keyLabel.Text = getKeyNameFn()
        end
    end)

    return row, syncLabels, keyLabel
end

function initConfigSettingsSection(elementsList)
    local cfgOpenLabel = nil
    local syncConfigRows = nil

    local function getConfigOptionNames()
        local options = {"None", CONFIG_SYSTEM.AUTOSAVE_NAME}
        for _, cfgName in ipairs(listConfigs()) do
            if cfgName ~= CONFIG_SYSTEM.AUTOSAVE_NAME then
                table.insert(options, cfgName)
            end
        end
        return options
    end

    local function getConfigSelectionLabel()
        return getSelectedConfigName() or "None"
    end

    local function syncConfigSelectionLabel()
        if cfgOpenLabel then
            cfgOpenLabel.Text = "≡ " .. getConfigSelectionLabel()
        end
    end

    local function persistConfigSelection()
        if storageEnabled then
            storageLoaded = savePersistentState() or storageLoaded
        end
        refreshDashboard()
    end

    local function setSelectedConfigName(name)
        local trimmed = trimConfigName(name)
        if trimmed == "" or trimmed == "None" then
            CONFIG_SYSTEM.selectedConfigName = nil
        else
            CONFIG_SYSTEM.selectedConfigName = trimmed
        end
        if getSelectedConfigName() == CONFIG_SYSTEM.AUTOSAVE_NAME then
            saveConfigState(CONFIG_SYSTEM.AUTOSAVE_NAME)
        end
        persistConfigSelection()
        syncConfigSelectionLabel()
        if syncConfigRows then
            syncConfigRows()
        end
    end

    local function loadSelectedConfig()
        local selectedName = getSelectedConfigName()
        if not selectedName then
            spawnNotification("No config selected.", "error", true)
            return false
        end
        if not loadConfigState(selectedName, true) then
            if selectedName == CONFIG_SYSTEM.AUTOSAVE_NAME then
                spawnNotification("No autosave from last session was found.", "warning", true)
            else
                spawnNotification("Could not load config: " .. selectedName, "error", true)
            end
            return false
        end
        applyStoredStateToRuntime()
        ensurePlayerFeatureHooks()
        syncAllUi()
        if selectedName == CONFIG_SYSTEM.AUTOSAVE_NAME then
            spawnNotification("Autosave from last session loaded.", "success", true)
        else
            spawnNotification("Loaded config: " .. selectedName, "success", true)
        end
        return true
    end

    local showCreateOverlay = nil
    local createConfigRow = createButtonRow("Create Config", elementsList, function()
        if showCreateOverlay then
            showCreateOverlay()
        end
    end)
    local createOverlay = Instance.new("Frame")
    createOverlay.Size = UDim2.new(1, 0, 1, 0)
    createOverlay.BackgroundColor3 = Color3.new(0, 0, 0)
    createOverlay.BackgroundTransparency = 0.35
    createOverlay.BorderSizePixel = 0
    createOverlay.Visible = false
    createOverlay.ZIndex = 240
    createOverlay.Active = true
    createOverlay.Parent = overlayRoot

    local createBox = Instance.new("Frame")
    createBox.Size = UDim2.new(0, 300, 0, 126)
    createBox.Position = UDim2.new(0.5, 0, 0.5, 0)
    createBox.AnchorPoint = Vector2.new(0.5, 0.5)
    createBox.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
    createBox.BorderSizePixel = 0
    createBox.ZIndex = 241
    createBox.Parent = createOverlay
    Instance.new("UICorner", createBox).CornerRadius = UDim.new(0, 8)

    local createStroke = Instance.new("UIStroke")
    createStroke.Color = Color3.fromRGB(0, 150, 255)
    createStroke.Thickness = 1
    createStroke.Parent = createBox

    local createHeader = Instance.new("TextLabel")
    createHeader.Size = UDim2.new(1, -24, 0, 24)
    createHeader.Position = UDim2.new(0, 12, 0, 10)
    createHeader.BackgroundTransparency = 1
    createHeader.TextColor3 = Color3.new(1, 1, 1)
    createHeader.Text = "Name your config"
    createHeader.Font = Enum.Font.GothamBold
    createHeader.TextSize = 15
    createHeader.ZIndex = 242
    createHeader.Parent = createBox

    local createInput = Instance.new("TextBox")
    createInput.Size = UDim2.new(1, -24, 0, 32)
    createInput.Position = UDim2.new(0, 12, 0, 44)
    createInput.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    createInput.BorderSizePixel = 0
    createInput.TextColor3 = Color3.new(1, 1, 1)
    createInput.PlaceholderText = "Config name..."
    createInput.PlaceholderColor3 = Color3.fromRGB(120, 120, 120)
    createInput.Font = Enum.Font.Gotham
    createInput.TextSize = 14
    createInput.Text = ""
    createInput.ClearTextOnFocus = false
    createInput.ZIndex = 242
    createInput.Parent = createBox
    Instance.new("UICorner", createInput).CornerRadius = UDim.new(0, 4)

    local createHint = Instance.new("TextLabel")
    createHint.Size = UDim2.new(1, -24, 0, 30)
    createHint.Position = UDim2.new(0, 12, 0, 84)
    createHint.BackgroundTransparency = 1
    createHint.TextColor3 = Color3.fromRGB(120, 120, 120)
    createHint.Text = "Press Enter to save, or Escape to cancel."
    createHint.TextWrapped = true
    createHint.Font = Enum.Font.Gotham
    createHint.TextSize = 11
    createHint.ZIndex = 242
    createHint.Parent = createBox

    local createOverlayConnection = nil
    local function hideCreateOverlay()
        createOverlay.Visible = false
        if createOverlayConnection then
            createOverlayConnection:Disconnect()
            createOverlayConnection = nil
        end
    end

    showCreateOverlay = function()
        if not storageEnabled then
            spawnNotification("Config files are unavailable in this executor.", "error", true)
            return
        end
        createInput.Text = ""
        createOverlay.Visible = true
        if createOverlayConnection then
            createOverlayConnection:Disconnect()
            createOverlayConnection = nil
        end
        createOverlayConnection = _UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if gameProcessed then
                return
            end
            if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.Escape then
                hideCreateOverlay()
            end
        end)
        task.defer(function()
            if createInput.Parent and createOverlay.Visible then
                createInput:CaptureFocus()
            end
        end)
    end

    createInput.FocusLost:Connect(function(enterPressed)
        if not createOverlay.Visible then
            return
        end
        if enterPressed then
            local trimmed = trimConfigName(createInput.Text)
            if trimmed == "" then
                spawnNotification("Enter a config name first.", "warning", true)
            elseif trimmed == CONFIG_SYSTEM.AUTOSAVE_NAME or trimmed == "None" then
                spawnNotification("That config name is reserved.", "warning", true)
            elseif saveConfigState(trimmed) then
                local nextNames = normalizeSavedConfigNames(CONFIG_SYSTEM.savedConfigs)
                if not table.find(nextNames, trimmed) then
                    table.insert(nextNames, trimmed)
                end
                CONFIG_SYSTEM.savedConfigs = normalizeSavedConfigNames(nextNames)
                persistConfigSelection()
                syncConfigSelectionLabel()
                spawnNotification("Config saved: " .. trimmed, "success", true)
            else
                spawnNotification("Could not save config: " .. trimmed, "error", true)
            end
        end
        hideCreateOverlay()
    end)

    local configSelectRow, cfgSelIcon, cfgSelLabel, cfgSelTop, cfgSelBot, cfgSelDia = createRow("Select Config")
    configSelectRow.Parent = elementsList
    applyHoverState(configSelectRow, cfgSelIcon, cfgSelLabel, cfgSelTop, cfgSelBot, cfgSelDia, false)

    local cfgOpenBg = Instance.new("Frame")
    cfgOpenBg.Size = UDim2.new(0, 168, 0, 22)
    cfgOpenBg.Position = UDim2.new(1, -183, 0.5, -11)
    cfgOpenBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    cfgOpenBg.BorderSizePixel = 0
    cfgOpenBg.Parent = configSelectRow
    Instance.new("UICorner", cfgOpenBg).CornerRadius = UDim.new(0, 4)

    cfgOpenLabel = Instance.new("TextLabel")
    cfgOpenLabel.Size = UDim2.new(1, 0, 1, 0)
    cfgOpenLabel.BackgroundTransparency = 1
    cfgOpenLabel.TextColor3 = Color3.new(1, 1, 1)
    cfgOpenLabel.TextSize = 11
    cfgOpenLabel.Font = Enum.Font.GothamMedium
    cfgOpenLabel.TextTruncate = Enum.TextTruncate.AtEnd
    cfgOpenLabel.Parent = cfgOpenBg
    syncConfigSelectionLabel()
    registerUiSync(_allDropdownSyncs, syncConfigSelectionLabel)

    local _, cfgDropList, _, showCfgDrop, hideCfgDrop = createDraggableDropdown("Configs")
    local function refreshConfigDropdown()
        for _, child in ipairs(cfgDropList:GetChildren()) do
            if child:IsA("TextButton") then
                child:Destroy()
            end
        end

        local selectedName = getSelectedConfigName()
        for index, optionName in ipairs(getConfigOptionNames()) do
            local isSelected = (selectedName == nil and optionName == "None") or selectedName == optionName
            local button = Instance.new("TextButton")
            button.Name = optionName
            button:SetAttribute("SearchText", optionName)
            button.Size = UDim2.new(1, 0, 0, 24)
            button.LayoutOrder = index
            button.BackgroundColor3 = isSelected and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(24, 24, 24)
            button.BorderSizePixel = 0
            button.Text = isSelected and ("  ✓ " .. optionName) or ("  " .. optionName)
            button.TextColor3 = Color3.new(1, 1, 1)
            button.Font = Enum.Font.Gotham
            button.TextSize = 12
            button.TextXAlignment = Enum.TextXAlignment.Left
            button.AutoButtonColor = false
            button.ZIndex = 102
            button.Parent = cfgDropList
            Instance.new("UICorner", button).CornerRadius = UDim.new(0, 2)
            _UTILS._applySoundsToButton(button)

            button.MouseButton1Click:Connect(function()
                if destroyed then
                    return
                end
                _UTILS._createParticleBurst(button)
                setSelectedConfigName(optionName)
                if optionName == CONFIG_SYSTEM.AUTOSAVE_NAME then
                    spawnNotification("Autosave from last session selected.", "success", true)
                elseif optionName == "None" then
                    spawnNotification("No config selected.", "info", true)
                else
                    spawnNotification("Selected config: " .. optionName, "info", true)
                end
                hideCfgDrop()
            end)
        end
    end

    if _currentRegistryTab then
        registerButtonEntry({
            name = "Select Config",
            tab = _currentRegistryTab,
            type = "special",
            onClickFn = function()
                refreshConfigDropdown()
                showCfgDrop()
            end,
        })
    end

    configSelectRow.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(configSelectRow)
        refreshConfigDropdown()
        showCfgDrop()
    end)

    local loadConfigRow = createButtonRow("Load Config", elementsList, function()
        loadSelectedConfig()
    end)

    local deleteOverlay = Instance.new("Frame")
    deleteOverlay.Size = UDim2.new(1, 0, 1, 0)
    deleteOverlay.BackgroundColor3 = Color3.new(0, 0, 0)
    deleteOverlay.BackgroundTransparency = 0.35
    deleteOverlay.BorderSizePixel = 0
    deleteOverlay.Visible = false
    deleteOverlay.ZIndex = 240
    deleteOverlay.Active = true
    deleteOverlay.Parent = overlayRoot

    local deleteBox = Instance.new("Frame")
    deleteBox.Size = UDim2.new(0, 320, 0, 134)
    deleteBox.Position = UDim2.new(0.5, 0, 0.5, 0)
    deleteBox.AnchorPoint = Vector2.new(0.5, 0.5)
    deleteBox.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
    deleteBox.BorderSizePixel = 0
    deleteBox.ZIndex = 241
    deleteBox.Parent = deleteOverlay
    Instance.new("UICorner", deleteBox).CornerRadius = UDim.new(0, 8)

    local deleteStroke = Instance.new("UIStroke")
    deleteStroke.Color = Color3.fromRGB(255, 90, 90)
    deleteStroke.Thickness = 1
    deleteStroke.Parent = deleteBox

    local deleteHeader = Instance.new("TextLabel")
    deleteHeader.Size = UDim2.new(1, -24, 0, 56)
    deleteHeader.Position = UDim2.new(0, 12, 0, 12)
    deleteHeader.BackgroundTransparency = 1
    deleteHeader.TextColor3 = Color3.new(1, 1, 1)
    deleteHeader.TextWrapped = true
    deleteHeader.Text = "Delete selected config?"
    deleteHeader.Font = Enum.Font.GothamBold
    deleteHeader.TextSize = 14
    deleteHeader.ZIndex = 242
    deleteHeader.Parent = deleteBox

    local confirmDeleteButton = Instance.new("TextButton")
    confirmDeleteButton.Size = UDim2.new(0, 122, 0, 32)
    confirmDeleteButton.Position = UDim2.new(0, 20, 1, -48)
    confirmDeleteButton.BackgroundColor3 = Color3.fromRGB(255, 72, 72)
    confirmDeleteButton.BorderSizePixel = 0
    confirmDeleteButton.TextColor3 = Color3.new(1, 1, 1)
    confirmDeleteButton.Text = "Delete"
    confirmDeleteButton.Font = Enum.Font.GothamBold
    confirmDeleteButton.TextSize = 13
    confirmDeleteButton.AutoButtonColor = false
    confirmDeleteButton.ZIndex = 242
    confirmDeleteButton.Parent = deleteBox
    Instance.new("UICorner", confirmDeleteButton).CornerRadius = UDim.new(0, 4)
    _UTILS._applySoundsToButton(confirmDeleteButton)

    local cancelDeleteButton = Instance.new("TextButton")
    cancelDeleteButton.Size = UDim2.new(0, 122, 0, 32)
    cancelDeleteButton.Position = UDim2.new(1, -142, 1, -48)
    cancelDeleteButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    cancelDeleteButton.BorderSizePixel = 0
    cancelDeleteButton.TextColor3 = Color3.new(1, 1, 1)
    cancelDeleteButton.Text = "Cancel"
    cancelDeleteButton.Font = Enum.Font.GothamBold
    cancelDeleteButton.TextSize = 13
    cancelDeleteButton.AutoButtonColor = false
    cancelDeleteButton.ZIndex = 242
    cancelDeleteButton.Parent = deleteBox
    Instance.new("UICorner", cancelDeleteButton).CornerRadius = UDim.new(0, 4)
    _UTILS._applySoundsToButton(cancelDeleteButton)

    local function hideDeleteOverlay()
        deleteOverlay.Visible = false
    end

    local function showDeleteOverlay()
        local selectedName = getSelectedConfigName()
        if not selectedName then
            spawnNotification("No config selected.", "error", true)
            return
        end
        if selectedName == CONFIG_SYSTEM.AUTOSAVE_NAME then
            deleteHeader.Text = "Clear the autosave from last session snapshot?"
        else
            deleteHeader.Text = "Delete config '" .. selectedName .. "'?"
        end
        deleteOverlay.Visible = true
    end

    cancelDeleteButton.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(cancelDeleteButton)
        hideDeleteOverlay()
    end)

    confirmDeleteButton.MouseButton1Click:Connect(function()
        if destroyed then
            return
        end
        _UTILS._createParticleBurst(confirmDeleteButton)
        local selectedName = getSelectedConfigName()
        if not selectedName then
            hideDeleteOverlay()
            return
        end

        local okDelete = storageDeleteAvailable and storageReadAvailable
        if okDelete then
            okDelete = pcall(function()
                local filePath = getConfigFilePath(selectedName)
                if isfile(filePath) then
                    delfile(filePath)
                end
            end)
        end

        if okDelete then
            if selectedName ~= CONFIG_SYSTEM.AUTOSAVE_NAME then
                local nextNames = {}
                for _, cfgName in ipairs(normalizeSavedConfigNames(CONFIG_SYSTEM.savedConfigs)) do
                    if cfgName ~= selectedName then
                        table.insert(nextNames, cfgName)
                    end
                end
                CONFIG_SYSTEM.savedConfigs = normalizeSavedConfigNames(nextNames)
            end
            if getAutoLoadConfigName() == selectedName then
                CONFIG_SYSTEM.autoLoadConfigName = nil
            end
            CONFIG_SYSTEM.selectedConfigName = nil
            persistConfigSelection()
            syncConfigSelectionLabel()
            if syncConfigRows then
                syncConfigRows()
            end
            if selectedName == CONFIG_SYSTEM.AUTOSAVE_NAME then
                spawnNotification("Cleared autosave from last session.", "success", true)
            else
                spawnNotification("Deleted config: " .. selectedName, "success", true)
            end
        else
            spawnNotification("Could not delete config: " .. selectedName, "error", true)
        end

        hideDeleteOverlay()
    end)

    local deleteConfigRow = createButtonRow("Delete Config", elementsList, function()
        showDeleteOverlay()
    end, {
        iconColor = Color3.fromRGB(255, 120, 120),
        labelColor = Color3.fromRGB(255, 170, 170),
    })

    local autoLoadConfigRow, _, autoLoadConfigSync = createToggleRow("Auto Load Config", elementsList, function()
        local selectedName = getSelectedConfigName()
        return selectedName ~= nil and getAutoLoadConfigName() == selectedName
    end, function()
        local selectedName = getSelectedConfigName()
        if not selectedName then
            spawnNotification("No config selected.", "error", true)
            return
        end
        if getAutoLoadConfigName() == selectedName then
            CONFIG_SYSTEM.autoLoadConfigName = nil
            spawnNotification("Auto-load disabled for " .. selectedName .. ".", "info", true)
        else
            CONFIG_SYSTEM.autoLoadConfigName = selectedName
            spawnNotification("Auto-load set to " .. selectedName .. ".", "success", true)
        end
        persistConfigSelection()
        if syncConfigRows then
            syncConfigRows()
        end
    end)

    local configRowsInitialized = false
    syncConfigRows = function()
        local hasSelection = getSelectedConfigName() ~= nil
        setAnimatedRowVisible(loadConfigRow, hasSelection, 38, not configRowsInitialized)
        setAnimatedRowVisible(deleteConfigRow, hasSelection, 38, not configRowsInitialized)
        setAnimatedRowVisible(autoLoadConfigRow, hasSelection, 38, not configRowsInitialized)
        pcall(autoLoadConfigSync)
        configRowsInitialized = true
    end
    table.insert(_allUiSyncs, syncConfigRows)
    syncConfigRows()
end

homeContent = createContentArea()
tabButtons["Home"] = {btn = hubLogo, indicator = homeIcon, label = homeLblText}
tabContents["Home"] = homeContent
createTabBackground(homeContent)
do
    local title, elementsList = createTabLayout(homeContent)
    tabTitles["Home"] = title

    local function formatCompactDuration(sec)
        sec = math.max(0, math.floor(sec + 0.5))
        local h = math.floor(sec / 3600)
        local m = math.floor((sec % 3600) / 60)
        local s = sec % 60
        return string.format("%dh %dm %ds", h, m, s)
    end

    local function createLineSectionHeader(parent, yOffset, word, halfGap)
        halfGap = halfGap or 40
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -16, 0, 14)
        row.Position = UDim2.new(0, 8, 0, yOffset)
        row.BackgroundTransparency = 1
        row.Parent = parent

        local lineColor = Color3.fromRGB(88, 90, 98)
        local leftLine = Instance.new("Frame")
        leftLine.BorderSizePixel = 0
        leftLine.BackgroundColor3 = lineColor
        leftLine.Size = UDim2.new(0.5, -halfGap, 0, 1)
        leftLine.AnchorPoint = Vector2.new(0, 0.5)
        leftLine.Position = UDim2.new(0, 0, 0.5, 0)
        leftLine.Parent = row

        local rightLine = Instance.new("Frame")
        rightLine.BorderSizePixel = 0
        rightLine.BackgroundColor3 = lineColor
        rightLine.Size = UDim2.new(0.5, -halfGap, 0, 1)
        rightLine.AnchorPoint = Vector2.new(1, 0.5)
        rightLine.Position = UDim2.new(1, 0, 0.5, 0)
        rightLine.Parent = row

        local mid = Instance.new("TextLabel")
        mid.BackgroundTransparency = 1
        mid.Text = word
        mid.Font = Enum.Font.Code
        mid.TextSize = 11
        mid.TextColor3 = Color3.fromRGB(200, 200, 205)
        mid.TextXAlignment = Enum.TextXAlignment.Center
        mid.Size = UDim2.new(0, 0, 1, 0)
        mid.AutomaticSize = Enum.AutomaticSize.X
        mid.AnchorPoint = Vector2.new(0.5, 0.5)
        mid.Position = UDim2.new(0.5, 0, 0.5, 0)
        mid.Parent = row
    end

    local layoutContainer = Instance.new("Frame")
    layoutContainer.Size = UDim2.new(1, 0, 0, 318)
    layoutContainer.BackgroundTransparency = 1
    layoutContainer.LayoutOrder = 1
    layoutContainer.Parent = elementsList

    local leftPanel = Instance.new("Frame")
    leftPanel.Size = UDim2.new(0, 205, 0, 318)
    leftPanel.Position = UDim2.new(0, 0, 0, 0)
    leftPanel.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    leftPanel.Parent = layoutContainer
    Instance.new("UICorner", leftPanel).CornerRadius = UDim.new(0, 6)
    local leftStroke = Instance.new("UIStroke")
    leftStroke.Color = Color3.fromRGB(35, 35, 35)
    leftStroke.Thickness = 1
    leftStroke.Parent = leftPanel

    local greetingLbl = Instance.new("TextLabel")
    greetingLbl.Size = UDim2.new(1, 0, 0, 34)
    greetingLbl.Position = UDim2.new(0, 0, 0, 10)
    greetingLbl.BackgroundTransparency = 1
    greetingLbl.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    local hour = tonumber(os.date("%H")) or 12
    local greeting = "Morning"
    if hour >= 12 and hour < 17 then
        greeting = "Afternoon"
    elseif hour >= 17 then
        greeting = "Night"
    end
    greetingLbl.Text = greeting .. ", Anonymous"
    greetingLbl.Font = Enum.Font.Code
    greetingLbl.TextSize = 16
    greetingLbl.Parent = leftPanel

    local avatarBorder = Instance.new("Frame")
    avatarBorder.Size = UDim2.new(0, 134, 0, 134)
    avatarBorder.Position = UDim2.new(0.5, -67, 0, 50)
    avatarBorder.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
    avatarBorder.Parent = leftPanel
    Instance.new("UICorner", avatarBorder).CornerRadius = UDim.new(0, 8)
    local avatarStroke = Instance.new("UIStroke")
    avatarStroke.Color = Color3.fromRGB(45, 45, 45)
    avatarStroke.Thickness = 1
    avatarStroke.Parent = avatarBorder

    local avatarImg = Instance.new("ImageLabel")
    avatarImg.Size = UDim2.new(1, -4, 1, -4)
    avatarImg.Position = UDim2.new(0, 2, 0, 2)
    avatarImg.BackgroundTransparency = 1
    if localPlayer then
        avatarImg.Image = "rbxthumb://type=AvatarHeadShot&id=" .. localPlayer.UserId .. "&w=420&h=420"
    end
    avatarImg.ScaleType = Enum.ScaleType.Fit
    avatarImg.Parent = avatarBorder
    Instance.new("UICorner", avatarImg).CornerRadius = UDim.new(0, 6)

    createLineSectionHeader(leftPanel, 190, "info", 44)

    local function createInfoLabel(key, yPosition)
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -16, 0, 14)
        label.Position = UDim2.new(0, 8, 0, yPosition)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.Code
        label.TextSize = 11
        label.TextColor3 = Color3.fromRGB(175, 175, 180)
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = leftPanel
        dashboardLabels[key] = label
        return label
    end

    createInfoLabel("notifications", 208)
    createInfoLabel("session", 224)
    createInfoLabel("guiKey", 240)
    createInfoLabel("panicKey", 256)

    createLineSectionHeader(leftPanel, 272, "bug report", 58)
    local openBugReportModalBtn = Instance.new("TextButton")
    openBugReportModalBtn.Size = UDim2.new(1, -16, 0, 26)
    openBugReportModalBtn.Position = UDim2.new(0, 8, 0, 288)
    openBugReportModalBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    openBugReportModalBtn.TextColor3 = Color3.new(0.75, 0.75, 0.75)
    openBugReportModalBtn.Text = "Send Bug Report"
    openBugReportModalBtn.Font = Enum.Font.Code
    openBugReportModalBtn.TextSize = 11
    openBugReportModalBtn.AutoButtonColor = false
    openBugReportModalBtn.Parent = leftPanel
    Instance.new("UICorner", openBugReportModalBtn).CornerRadius = UDim.new(0, 4)
    local bugStroke = Instance.new("UIStroke")
    bugStroke.Color = Color3.fromRGB(45, 45, 45)
    bugStroke.Thickness = 1
    bugStroke.Parent = openBugReportModalBtn
    _UTILS._applySoundsToButton(openBugReportModalBtn)
    openBugReportModalBtn.MouseEnter:Connect(function()
        _TweenService:Create(openBugReportModalBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(35, 35, 35)}):Play()
    end)
    openBugReportModalBtn.MouseLeave:Connect(function()
        _TweenService:Create(openBugReportModalBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(25, 25, 25)}):Play()
    end)
    openBugReportModalBtn.MouseButton1Click:Connect(function()
        _UTILS._createParticleBurst(openBugReportModalBtn)
        spawnNotification("GUI-only build does not include bug-report webhooks.", "info", true)
    end)

    local rightPanel = Instance.new("Frame")
    rightPanel.Size = UDim2.new(1, -220, 1, 0)
    rightPanel.Position = UDim2.new(0, 220, 0, 0)
    rightPanel.BackgroundTransparency = 1
    rightPanel.Parent = layoutContainer

    local settingsBox = Instance.new("Frame")
    settingsBox.Size = UDim2.new(1, 0, 0, 65)
    settingsBox.Position = UDim2.new(0, 0, 0, 0)
    settingsBox.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    settingsBox.BorderSizePixel = 0
    settingsBox.Parent = rightPanel
    Instance.new("UICorner", settingsBox).CornerRadius = UDim.new(0, 6)
    local settingsStroke = Instance.new("UIStroke")
    settingsStroke.Color = Color3.fromRGB(35, 35, 35)
    settingsStroke.Thickness = 1
    settingsStroke.Parent = settingsBox

    local settingsHeaderBtn = Instance.new("TextButton")
    settingsHeaderBtn.Size = UDim2.new(1, 0, 0, 32)
    settingsHeaderBtn.Position = UDim2.new(0, 0, 0, 0)
    settingsHeaderBtn.BackgroundTransparency = 1
    settingsHeaderBtn.Text = ""
    settingsHeaderBtn.AutoButtonColor = false
    settingsHeaderBtn.ZIndex = 2
    settingsHeaderBtn.Parent = settingsBox
    _UTILS._applySoundsToButton(settingsHeaderBtn)

    local settingsIcon = Instance.new("ImageLabel")
    settingsIcon.Size = UDim2.new(0, 18, 0, 18)
    settingsIcon.Position = UDim2.new(0, 10, 0, 7)
    settingsIcon.BackgroundTransparency = 1
    settingsIcon.Image = "rbxassetid://6022668898"
    settingsIcon.ScaleType = Enum.ScaleType.Fit
    settingsIcon.ImageColor3 = Color3.fromRGB(0, 150, 255)
    settingsIcon.ZIndex = 2
    settingsIcon.Parent = settingsHeaderBtn

    local settingsLbl = Instance.new("TextLabel")
    settingsLbl.Size = UDim2.new(1, -34, 1, 0)
    settingsLbl.Position = UDim2.new(0, 34, 0, 0)
    settingsLbl.BackgroundTransparency = 1
    settingsLbl.TextColor3 = Color3.new(0.8, 0.8, 0.8)
    settingsLbl.Text = "Settings"
    settingsLbl.Font = Enum.Font.Code
    settingsLbl.TextSize = 13
    settingsLbl.TextXAlignment = Enum.TextXAlignment.Left
    settingsLbl.ZIndex = 2
    settingsLbl.Parent = settingsHeaderBtn

    local line1 = Instance.new("Frame")
    line1.Size = UDim2.new(1, 0, 0, 1)
    line1.Position = UDim2.new(0, 0, 0, 32)
    line1.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    line1.BorderSizePixel = 0
    line1.Parent = settingsBox

    local fpsPingRow = Instance.new("TextButton")
    fpsPingRow.Size = UDim2.new(1, 0, 0, 32)
    fpsPingRow.Position = UDim2.new(0, 0, 0, 33)
    fpsPingRow.BackgroundTransparency = 1
    fpsPingRow.Text = ""
    fpsPingRow.AutoButtonColor = false
    fpsPingRow.ZIndex = 2
    fpsPingRow.Parent = settingsBox
    _UTILS._applySoundsToButton(fpsPingRow)

    local fpsLbl = Instance.new("TextLabel")
    fpsLbl.Size = UDim2.new(1, -75, 1, 0)
    fpsLbl.Position = UDim2.new(0, 10, 0, 0)
    fpsLbl.BackgroundTransparency = 1
    fpsLbl.TextColor3 = Color3.new(0.6, 0.6, 0.6)
    fpsLbl.Text = "FPS & Ping Stats"
    fpsLbl.Font = Enum.Font.Code
    fpsLbl.TextSize = 12
    fpsLbl.TextXAlignment = Enum.TextXAlignment.Left
    fpsLbl.Parent = fpsPingRow

    local fpTextContainer = Instance.new("Frame")
    fpTextContainer.Size = UDim2.new(0, 50, 1, 0)
    fpTextContainer.Position = UDim2.new(1, -65, 0, 0)
    fpTextContainer.BackgroundTransparency = 1
    fpTextContainer.ClipsDescendants = true
    fpTextContainer.Parent = fpsPingRow

    local fpOffLbl = Instance.new("TextLabel")
    fpOffLbl.Size = UDim2.new(1, 0, 1, 0)
    fpOffLbl.BackgroundTransparency = 1
    fpOffLbl.TextColor3 = Color3.fromRGB(120, 120, 120)
    fpOffLbl.Text = "OFF"
    fpOffLbl.Font = Enum.Font.GothamMedium
    fpOffLbl.TextSize = 13
    fpOffLbl.TextXAlignment = Enum.TextXAlignment.Right
    fpOffLbl.Parent = fpTextContainer

    local fpOnLbl = Instance.new("TextLabel")
    fpOnLbl.Size = UDim2.new(1, 0, 1, 0)
    fpOnLbl.Position = UDim2.new(1, 0, 0, 0)
    fpOnLbl.BackgroundTransparency = 1
    fpOnLbl.TextColor3 = Color3.fromRGB(0, 255, 100)
    fpOnLbl.Text = "ON"
    fpOnLbl.Font = Enum.Font.GothamMedium
    fpOnLbl.TextSize = 13
    fpOnLbl.TextXAlignment = Enum.TextXAlignment.Right
    fpOnLbl.Parent = fpTextContainer

    local function syncFpPingLabels()
        if S.fpsPingStatsEnabled then
            fpOffLbl.Position = UDim2.new(-1, 0, 0, 0)
            fpOnLbl.Position = UDim2.new(0, 0, 0, 0)
        else
            fpOffLbl.Position = UDim2.new(0, 0, 0, 0)
            fpOnLbl.Position = UDim2.new(1, 0, 0, 0)
        end
    end

    local fpsPingOverlay = Instance.new("ScreenGui")
    fpsPingOverlay.Name = "FobGuiOnlyFpsPingOverlay"
    fpsPingOverlay.ResetOnSpawn = false
    fpsPingOverlay.IgnoreGuiInset = true
    fpsPingOverlay.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    fpsPingOverlay.DisplayOrder = 100
    fpsPingOverlay.Enabled = false
    do
        local okOverlay = pcall(function()
            fpsPingOverlay.Parent = _CoreGui
        end)
        if not okOverlay and localPlayer then
            fpsPingOverlay.Parent = localPlayer:WaitForChild("PlayerGui")
        end
    end
    table.insert(_extraCleanupGuis, fpsPingOverlay)

    local fpBg = Instance.new("Frame")
    fpBg.Size = UDim2.new(0, 220, 0, 28)
    fpBg.Position = UDim2.new(0, 10, 0, 10)
    fpBg.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    fpBg.BorderSizePixel = 0
    fpBg.Parent = fpsPingOverlay
    Instance.new("UICorner", fpBg).CornerRadius = UDim.new(0, 6)
    local fpOverlayStroke = Instance.new("UIStroke")
    fpOverlayStroke.Color = Color3.fromRGB(40, 40, 40)
    fpOverlayStroke.Thickness = 1
    fpOverlayStroke.Parent = fpBg

    local fpText = Instance.new("TextLabel")
    fpText.Size = UDim2.new(1, -12, 1, 0)
    fpText.Position = UDim2.new(0, 6, 0, 0)
    fpText.BackgroundTransparency = 1
    fpText.TextColor3 = Color3.new(0.85, 0.85, 0.85)
    fpText.Font = Enum.Font.Code
    fpText.TextSize = 14
    fpText.TextXAlignment = Enum.TextXAlignment.Left
    fpText.Text = "FPS: --  |  PING: -- ms"
    fpText.Parent = fpBg

    local fpStatsConn = nil
    local function setFpsPingOverlay(on)
        S.fpsPingStatsEnabled = on
        fpsPingOverlay.Enabled = on
        if fpStatsConn then
            fpStatsConn:Disconnect()
            fpStatsConn = nil
        end
        if on then
            local statsAccumulator = 0
            fpStatsConn = _RunService.RenderStepped:Connect(function(dt)
                if destroyed then
                    return
                end
                statsAccumulator = statsAccumulator + (tonumber(dt) or 0.016)
                if statsAccumulator < 0.25 then
                    return
                end
                statsAccumulator = 0
                local fps = math.floor(workspace:GetRealPhysicsFPS() + 0.5)
                local ping = 0
                local okPing, value = pcall(function()
                    return localPlayer:GetNetworkPing()
                end)
                if okPing and type(value) == "number" then
                    ping = math.floor(value * 1000 + 0.5)
                else
                    pcall(function()
                        local statsItem = game:GetService("Stats").Network.ServerStatsItem["Data Ping"]
                        if statsItem then
                            ping = math.floor(statsItem:GetValue())
                        end
                    end)
                end
                fpText.Text = string.format("FPS: %d  |  PING: %d ms", fps, ping)
            end)
        end
        syncFpPingLabels()
        refreshDashboard()
    end

    registerUiSync(_allToggleSyncs, syncFpPingLabels)
    registerUiSync(_allTextSyncs, function()
        if S.fpsPingStatsEnabled ~= fpsPingOverlay.Enabled then
            setFpsPingOverlay(S.fpsPingStatsEnabled)
        else
            syncFpPingLabels()
        end
    end)
    syncFpPingLabels()
    setFpsPingOverlay(S.fpsPingStatsEnabled)

    fpsPingRow.MouseButton1Click:Connect(function()
        _UTILS._createParticleBurst(fpsPingRow)
        setFpsPingOverlay(not S.fpsPingStatsEnabled)
        schedulePersist()
    end)

    settingsHeaderBtn.MouseButton1Click:Connect(function()
        _UTILS._createParticleBurst(settingsHeaderBtn)
        switchTab("SETTINGS")
        animateCurrentTitle("SETTINGS")
    end)

    local fbBox = Instance.new("Frame")
    fbBox.Size = UDim2.new(1, 0, 0, 112)
    fbBox.Position = UDim2.new(0, 0, 0, 75)
    fbBox.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    fbBox.BorderSizePixel = 0
    fbBox.Parent = rightPanel
    Instance.new("UICorner", fbBox).CornerRadius = UDim.new(0, 6)
    local fbStroke = Instance.new("UIStroke")
    fbStroke.Color = Color3.fromRGB(35, 35, 35)
    fbStroke.Thickness = 1
    fbStroke.Parent = fbBox

    local fIcon = Instance.new("ImageLabel")
    fIcon.Size = UDim2.new(0, 18, 0, 18)
    fIcon.Position = UDim2.new(0, 10, 0, 7)
    fIcon.BackgroundTransparency = 1
    fIcon.Image = "rbxthumb://type=Asset&id=140019549515607&w=420&h=420"
    fIcon.ScaleType = Enum.ScaleType.Fit
    fIcon.Parent = fbBox

    local fbLbl = Instance.new("TextLabel")
    fbLbl.Size = UDim2.new(1, -34, 0, 32)
    fbLbl.Position = UDim2.new(0, 34, 0, 0)
    fbLbl.BackgroundTransparency = 1
    fbLbl.TextColor3 = Color3.new(0.8, 0.8, 0.8)
    fbLbl.Text = "Feedback"
    fbLbl.Font = Enum.Font.Code
    fbLbl.TextSize = 13
    fbLbl.TextXAlignment = Enum.TextXAlignment.Left
    fbLbl.Parent = fbBox

    local line2 = Instance.new("Frame")
    line2.Size = UDim2.new(1, 0, 0, 1)
    line2.Position = UDim2.new(0, 0, 0, 32)
    line2.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    line2.BorderSizePixel = 0
    line2.Parent = fbBox

    local openFeedbackModalBtn = Instance.new("TextButton")
    openFeedbackModalBtn.Size = UDim2.new(1, -20, 0, 28)
    openFeedbackModalBtn.Position = UDim2.new(0, 10, 0, 38)
    openFeedbackModalBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    openFeedbackModalBtn.TextColor3 = Color3.new(0.75, 0.75, 0.75)
    openFeedbackModalBtn.Text = "Send Feedback Message"
    openFeedbackModalBtn.Font = Enum.Font.Code
    openFeedbackModalBtn.TextSize = 12
    openFeedbackModalBtn.AutoButtonColor = false
    openFeedbackModalBtn.Parent = fbBox
    Instance.new("UICorner", openFeedbackModalBtn).CornerRadius = UDim.new(0, 4)
    local feedbackButtonStroke = Instance.new("UIStroke")
    feedbackButtonStroke.Color = Color3.fromRGB(45, 45, 45)
    feedbackButtonStroke.Thickness = 1
    feedbackButtonStroke.Parent = openFeedbackModalBtn
    _UTILS._applySoundsToButton(openFeedbackModalBtn)
    openFeedbackModalBtn.MouseEnter:Connect(function()
        _TweenService:Create(openFeedbackModalBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(35, 35, 35)}):Play()
    end)
    openFeedbackModalBtn.MouseLeave:Connect(function()
        _TweenService:Create(openFeedbackModalBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(25, 25, 25)}):Play()
    end)
    openFeedbackModalBtn.MouseButton1Click:Connect(function()
        _UTILS._createParticleBurst(openFeedbackModalBtn)
        spawnNotification("GUI-only build does not include feedback webhooks.", "info", true)
    end)

    local resetFeedbackCdBtn = Instance.new("TextButton")
    resetFeedbackCdBtn.Size = UDim2.new(1, -20, 0, 22)
    resetFeedbackCdBtn.Position = UDim2.new(0, 10, 0, 72)
    resetFeedbackCdBtn.BackgroundColor3 = Color3.fromRGB(35, 28, 22)
    resetFeedbackCdBtn.TextColor3 = Color3.fromRGB(200, 160, 120)
    resetFeedbackCdBtn.Text = "Reset cooldown (testing)"
    resetFeedbackCdBtn.Font = Enum.Font.Code
    resetFeedbackCdBtn.TextSize = 10
    resetFeedbackCdBtn.AutoButtonColor = false
    resetFeedbackCdBtn.Parent = fbBox
    Instance.new("UICorner", resetFeedbackCdBtn).CornerRadius = UDim.new(0, 4)
    local resetCdStroke = Instance.new("UIStroke")
    resetCdStroke.Color = Color3.fromRGB(80, 55, 40)
    resetCdStroke.Thickness = 1
    resetCdStroke.Parent = resetFeedbackCdBtn
    _UTILS._applySoundsToButton(resetFeedbackCdBtn)
    resetFeedbackCdBtn.MouseEnter:Connect(function()
        _TweenService:Create(resetFeedbackCdBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(52, 38, 30)}):Play()
    end)
    resetFeedbackCdBtn.MouseLeave:Connect(function()
        _TweenService:Create(resetFeedbackCdBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(35, 28, 22)}):Play()
    end)
    resetFeedbackCdBtn.MouseButton1Click:Connect(function()
        _UTILS._createParticleBurst(resetFeedbackCdBtn)
        spawnNotification("There is no feedback cooldown in GUI-only mode.", "warning", true)
    end)

    dashboardLabels._homeInfoLastTick = 0
    _RunService.Heartbeat:Connect(function()
        if destroyed or not isSessionActive() then
            return
        end
        local now = tick()
        if now - (dashboardLabels._homeInfoLastTick or 0) < 0.3 then
            return
        end
        dashboardLabels._homeInfoLastTick = now
        if dashboardLabels.session then
            dashboardLabels.session.Text = "current session: " .. formatCompactDuration(now - _SCRIPT_SESSION_START_TICK)
        end
    end)

    refreshDashboard()
end

AUTOMINEBtn, AUTOMINEIndicator, AUTOMINELabel = createTabButton("AUTOMINE", 10)
AUTOMINEContent = createContentArea()
tabButtons["AUTOMINE"] = {btn = AUTOMINEBtn, indicator = AUTOMINEIndicator, label = AUTOMINELabel}
tabContents["AUTOMINE"] = AUTOMINEContent
createTabBackground(AUTOMINEContent)
do
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -40, 0, 34)
    title.Position = UDim2.new(0, 25, 0, 15)
    title.BackgroundTransparency = 1
    title.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    title.Text = "Automine"
    title.Font = Enum.Font.GothamBold
    title.TextSize = 24
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.ZIndex = 50
    title.Parent = AUTOMINEContent
    tabTitles["AUTOMINE"] = title

    headerSubtitleLabel = Instance.new("TextLabel")
    headerSubtitleLabel.Size = UDim2.new(1, -40, 0, 18)
    headerSubtitleLabel.Position = UDim2.new(0, 25, 0, 46)
    headerSubtitleLabel.BackgroundTransparency = 1
    headerSubtitleLabel.TextColor3 = UI_COLORS.muted
    headerSubtitleLabel.Text = "Saved Path: None"
    headerSubtitleLabel.Font = Enum.Font.Gotham
    headerSubtitleLabel.TextSize = 12
    headerSubtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    headerSubtitleLabel.ZIndex = 50
    headerSubtitleLabel.Parent = AUTOMINEContent

    local automineScroller = Instance.new("ScrollingFrame")
    automineScroller.Name = "AutomineScroller"
    automineScroller.Size = UDim2.new(1, -50, 1, -96)
    automineScroller.Position = UDim2.new(0, 25, 0, 72)
    automineScroller.BackgroundTransparency = 1
    automineScroller.BorderSizePixel = 0
    automineScroller.ScrollBarThickness = 4
    automineScroller.ScrollBarImageColor3 = UI_COLORS.accent
    automineScroller.CanvasSize = UDim2.new(0, 0, 0, 760)
    automineScroller.Parent = AUTOMINEContent
    autofarmPageRef = automineScroller

    automineCanvas = Instance.new("Frame")
    automineCanvas.Name = "AutomineCanvas"
    automineCanvas.Size = UDim2.new(1, -4, 0, 760)
    automineCanvas.BackgroundTransparency = 1
    automineCanvas.BorderSizePixel = 0
    automineCanvas.Parent = automineScroller

    pathUi.subTabBar = Instance.new("Frame")
    pathUi.subTabBar.Name = "AutomineSubTabBar"
    pathUi.subTabBar.Size = UDim2.new(1, 0, 0, 34)
    pathUi.subTabBar.BackgroundTransparency = 1
    pathUi.subTabBar.Parent = automineCanvas

    pathUi.subTabButtons.AutofarmSettings = Instance.new("TextButton")
    pathUi.subTabButtons.AutofarmSettings.Name = "AutofarmSettingsSubTab"
    pathUi.subTabButtons.AutofarmSettings.Size = UDim2.new(0.5, -6, 0, 32)
    pathUi.subTabButtons.AutofarmSettings.Position = UDim2.new(0, 0, 0, 0)
    pathUi.subTabButtons.AutofarmSettings.Text = "Automine"
    pathUi.subTabButtons.AutofarmSettings.Parent = pathUi.subTabBar
    styleSubTabButton(pathUi.subTabButtons.AutofarmSettings, 12)

    pathUi.subTabButtons.Paths = Instance.new("TextButton")
    pathUi.subTabButtons.Paths.Name = "PathsSubTab"
    pathUi.subTabButtons.Paths.Size = UDim2.new(0.5, -6, 0, 32)
    pathUi.subTabButtons.Paths.Position = UDim2.new(0.5, 6, 0, 0)
    pathUi.subTabButtons.Paths.Text = "Paths"
    pathUi.subTabButtons.Paths.Parent = pathUi.subTabBar
    styleSubTabButton(pathUi.subTabButtons.Paths, 12)

    autoFarmCardFrame = Instance.new("Frame")
    autoFarmCardFrame.Name = "AutoMineCard"
    autoFarmCardFrame.Size = UDim2.new(0.5, -6, 0, 156)
    autoFarmCardFrame.Position = UDim2.new(0, 0, 0, 46)
    autoFarmCardFrame.Parent = automineCanvas
    styleCard(autoFarmCardFrame, 14)

    local autoFarmTitle = Instance.new("TextLabel")
    autoFarmTitle.Size = UDim2.new(1, -22, 0, 18)
    autoFarmTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(autoFarmTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    autoFarmTitle.Text = "Automine Section"
    autoFarmTitle.Parent = autoFarmCardFrame

    local autoFarmSubtitle = Instance.new("TextLabel")
    autoFarmSubtitle.Size = UDim2.new(1, -22, 0, 16)
    autoFarmSubtitle.Position = UDim2.fromOffset(12, 28)
    configureTextLabel(autoFarmSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    autoFarmSubtitle.Text = "Play saved paths and choose which ores the route is allowed to mine."
    autoFarmSubtitle.Parent = autoFarmCardFrame

    toggleButton = Instance.new("TextButton")
    toggleButton.Name = "AutomineToggle"
    toggleButton.Size = UDim2.new(1, -24, 0, 36)
    toggleButton.Position = UDim2.fromOffset(12, 46)
    toggleButton.Text = "Play Selected Path"
    toggleButton.Parent = autoFarmCardFrame
    styleButton(toggleButton, 14)

    local oreHint = Instance.new("TextLabel")
    oreHint.Size = UDim2.new(1, -24, 0, 14)
    oreHint.Position = UDim2.fromOffset(12, 88)
    configureTextLabel(oreHint, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    oreHint.Text = "Selected ores"
    oreHint.Parent = autoFarmCardFrame

    oreDropdownButton = Instance.new("TextButton")
    oreDropdownButton.Name = "OreDropdownButton"
    oreDropdownButton.Size = UDim2.new(1, -24, 0, 32)
    oreDropdownButton.Position = UDim2.fromOffset(12, 104)
    oreDropdownButton.Text = ""
    oreDropdownButton.Parent = autoFarmCardFrame
    styleButton(oreDropdownButton, 12)

    oreDropdownFrame = Instance.new("Frame")
    oreDropdownFrame.Name = "OreDropdown"
    oreDropdownFrame.Size = UDim2.new(1, -24, 0, 132)
    oreDropdownFrame.Position = UDim2.fromOffset(12, 142)
    oreDropdownFrame.Visible = false
    oreDropdownFrame.Parent = autoFarmCardFrame
    styleCard(oreDropdownFrame, 10)
    oreDropdownFrame.BackgroundColor3 = UI_COLORS.panel

    local oreList = Instance.new("ScrollingFrame")
    oreList.Name = "OreList"
    oreList.Size = UDim2.new(1, -12, 1, -12)
    oreList.Position = UDim2.fromOffset(6, 6)
    oreList.BackgroundTransparency = 1
    oreList.BorderSizePixel = 0
    oreList.ScrollBarThickness = 4
    oreList.ScrollBarImageColor3 = UI_COLORS.accent
    oreList.CanvasSize = UDim2.new(0, 0, 0, 0)
    oreList.Parent = oreDropdownFrame

    local oreListLayout = Instance.new("UIListLayout")
    oreListLayout.Padding = UDim.new(0, 6)
    oreListLayout.Parent = oreList
    bindCanvasToLayout(oreList, oreListLayout, 2)

    for _, oreOption in ipairs(ORE_OPTIONS) do
        local oreButton = Instance.new("TextButton")
        oreButton.Name = oreOption.id .. "Button"
        oreButton.Size = UDim2.new(1, -2, 0, 28)
        oreButton.Text = ""
        oreButton.Parent = oreList
        styleButton(oreButton, 12)
        oreOptionButtons[oreOption.id] = oreButton
    end

    movementCardFrame = Instance.new("Frame")
    movementCardFrame.Name = "MovementCard"
    movementCardFrame.Size = UDim2.new(0.5, -6, 0, 104)
    movementCardFrame.Position = UDim2.new(0, 0, 0, 214)
    movementCardFrame.Parent = automineCanvas
    styleCard(movementCardFrame, 14)

    local movementTitle = Instance.new("TextLabel")
    movementTitle.Size = UDim2.new(1, -22, 0, 18)
    movementTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(movementTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    movementTitle.Text = "Movement Section"
    movementTitle.Parent = movementCardFrame

    local movementSubtitle = Instance.new("TextLabel")
    movementSubtitle.Size = UDim2.new(1, -22, 0, 16)
    movementSubtitle.Position = UDim2.fromOffset(12, 28)
    configureTextLabel(movementSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    movementSubtitle.Text = "Legit walks, Tween is smooth, and CFrame is the faster experimental mode."
    movementSubtitle.Parent = movementCardFrame

    tweenButton = Instance.new("TextButton")
    tweenButton.Name = "MovementMode"
    tweenButton.Size = UDim2.new(0.62, -6, 0, 34)
    tweenButton.Position = UDim2.fromOffset(12, 52)
    tweenButton.Text = ""
    tweenButton.Parent = movementCardFrame
    styleButton(tweenButton, 13)

    movementModeDropdownFrame = Instance.new("Frame")
    movementModeDropdownFrame.Name = "MovementModeDropdown"
    movementModeDropdownFrame.Size = UDim2.new(0.62, -6, 0, 74)
    movementModeDropdownFrame.Position = UDim2.fromOffset(12, 90)
    movementModeDropdownFrame.Visible = false
    movementModeDropdownFrame.Parent = movementCardFrame
    styleCard(movementModeDropdownFrame, 10)
    movementModeDropdownFrame.BackgroundColor3 = UI_COLORS.panel

    movementModeLegitButton = Instance.new("TextButton")
    movementModeLegitButton.Size = UDim2.new(1, -12, 0, 20)
    movementModeLegitButton.Position = UDim2.fromOffset(6, 6)
    movementModeLegitButton.Text = ""
    movementModeLegitButton.Parent = movementModeDropdownFrame
    styleButton(movementModeLegitButton, 11)

    movementModeTweenButton = Instance.new("TextButton")
    movementModeTweenButton.Size = UDim2.new(1, -12, 0, 20)
    movementModeTweenButton.Position = UDim2.fromOffset(6, 28)
    movementModeTweenButton.Text = ""
    movementModeTweenButton.Parent = movementModeDropdownFrame
    styleButton(movementModeTweenButton, 11)

    movementModeCFrameButton = Instance.new("TextButton")
    movementModeCFrameButton.Size = UDim2.new(1, -12, 0, 20)
    movementModeCFrameButton.Position = UDim2.fromOffset(6, 50)
    movementModeCFrameButton.Text = ""
    movementModeCFrameButton.Parent = movementModeDropdownFrame
    styleButton(movementModeCFrameButton, 11)

    tweenSpeedLabel = Instance.new("TextLabel")
    tweenSpeedLabel.Size = UDim2.new(0.3, 0, 0, 14)
    tweenSpeedLabel.Position = UDim2.new(0.68, 0, 0, 54)
    configureTextLabel(tweenSpeedLabel, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    tweenSpeedLabel.Text = "Move speed"
    tweenSpeedLabel.Parent = movementCardFrame

    tweenSpeedBox = Instance.new("TextBox")
    tweenSpeedBox.Size = UDim2.new(0.3, 0, 0, 34)
    tweenSpeedBox.Position = UDim2.new(0.68, 0, 0, 68)
    tweenSpeedBox.Text = tostring(math.floor(tweenMoveSpeed + 0.5))
    tweenSpeedBox.ClearTextOnFocus = false
    tweenSpeedBox.Parent = movementCardFrame
    styleButton(tweenSpeedBox, 13)

    pickupCardFrame = Instance.new("Frame")
    pickupCardFrame.Name = "PickupCard"
    pickupCardFrame.Size = UDim2.new(0.5, -6, 0, 156)
    pickupCardFrame.Position = UDim2.new(0.5, 6, 0, 46)
    pickupCardFrame.Parent = automineCanvas
    styleCard(pickupCardFrame, 14)

    local pickupTitle = Instance.new("TextLabel")
    pickupTitle.Size = UDim2.new(1, -22, 0, 18)
    pickupTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(pickupTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    pickupTitle.Text = "Auto Pickup Section"
    pickupTitle.Parent = pickupCardFrame

    local pickupSubtitle = Instance.new("TextLabel")
    pickupSubtitle.Size = UDim2.new(1, -22, 0, 16)
    pickupSubtitle.Position = UDim2.fromOffset(12, 28)
    configureTextLabel(pickupSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    pickupSubtitle.Text = "Filter pickup targets so the route only grabs the drops you want."
    pickupSubtitle.Parent = pickupCardFrame

    autoPickupButton = Instance.new("TextButton")
    autoPickupButton.Size = UDim2.new(1, -24, 0, 30)
    autoPickupButton.Position = UDim2.fromOffset(12, 52)
    autoPickupButton.Text = ""
    autoPickupButton.Parent = pickupCardFrame
    styleButton(autoPickupButton, 13)

    pickupWhitelistButton = Instance.new("TextButton")
    pickupWhitelistButton.Size = UDim2.new(1, -24, 0, 30)
    pickupWhitelistButton.Position = UDim2.fromOffset(12, 88)
    pickupWhitelistButton.Text = ""
    pickupWhitelistButton.Parent = pickupCardFrame
    styleButton(pickupWhitelistButton, 13)

    pickupWhitelistDropdownButton = Instance.new("TextButton")
    pickupWhitelistDropdownButton.Size = UDim2.new(1, -24, 0, 30)
    pickupWhitelistDropdownButton.Position = UDim2.fromOffset(12, 124)
    pickupWhitelistDropdownButton.Text = ""
    pickupWhitelistDropdownButton.Parent = pickupCardFrame
    styleButton(pickupWhitelistDropdownButton, 12)

    pickupWhitelistDropdownFrame = Instance.new("Frame")
    pickupWhitelistDropdownFrame.Name = "PickupWhitelistDropdown"
    pickupWhitelistDropdownFrame.Size = UDim2.new(1, -24, 0, 132)
    pickupWhitelistDropdownFrame.Position = UDim2.fromOffset(12, 160)
    pickupWhitelistDropdownFrame.Visible = false
    pickupWhitelistDropdownFrame.Parent = pickupCardFrame
    styleCard(pickupWhitelistDropdownFrame, 10)
    pickupWhitelistDropdownFrame.BackgroundColor3 = UI_COLORS.panel

    local pickupWhitelistList = Instance.new("ScrollingFrame")
    pickupWhitelistList.Size = UDim2.new(1, -12, 1, -12)
    pickupWhitelistList.Position = UDim2.fromOffset(6, 6)
    pickupWhitelistList.BackgroundTransparency = 1
    pickupWhitelistList.BorderSizePixel = 0
    pickupWhitelistList.ScrollBarThickness = 4
    pickupWhitelistList.ScrollBarImageColor3 = UI_COLORS.accent
    pickupWhitelistList.CanvasSize = UDim2.new(0, 0, 0, 0)
    pickupWhitelistList.Parent = pickupWhitelistDropdownFrame

    local pickupWhitelistLayout = Instance.new("UIListLayout")
    pickupWhitelistLayout.Padding = UDim.new(0, 6)
    pickupWhitelistLayout.Parent = pickupWhitelistList
    bindCanvasToLayout(pickupWhitelistList, pickupWhitelistLayout, 2)

    for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
        local pickupOptionButton = Instance.new("TextButton")
        pickupOptionButton.Name = pickupOption.id .. "PickupButton"
        pickupOptionButton.Size = UDim2.new(1, -2, 0, 28)
        pickupOptionButton.Text = ""
        pickupOptionButton.Parent = pickupWhitelistList
        styleButton(pickupOptionButton, 12)
        pickupWhitelistOptionButtons[pickupOption.id] = pickupOptionButton
    end

    autoSellCardFrame = Instance.new("Frame")
    autoSellCardFrame.Name = "ServerHopCard"
    autoSellCardFrame.Size = UDim2.new(0.5, -6, 0, 186)
    autoSellCardFrame.Position = UDim2.new(0.5, 6, 0, 214)
    autoSellCardFrame.Parent = automineCanvas
    styleCard(autoSellCardFrame, 14)

    local autoSellTitle = Instance.new("TextLabel")
    autoSellTitle.Size = UDim2.new(1, -22, 0, 18)
    autoSellTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(autoSellTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    autoSellTitle.Text = "Path Server Hop Section"
    autoSellTitle.Parent = autoSellCardFrame

    local autoSellSubtitle = Instance.new("TextLabel")
    autoSellSubtitle.Size = UDim2.new(1, -22, 0, 28)
    autoSellSubtitle.Position = UDim2.fromOffset(12, 28)
    configureTextLabel(autoSellSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    autoSellSubtitle.TextWrapped = true
    autoSellSubtitle.TextYAlignment = Enum.TextYAlignment.Top
    autoSellSubtitle.Text = "Saved paths handle their sell node. Server hop can join a smaller public server after a sell and try to resume the saved path."
    autoSellSubtitle.Parent = autoSellCardFrame

    local serverHopTitle = Instance.new("TextLabel")
    serverHopTitle.Size = UDim2.new(1, -24, 0, 16)
    serverHopTitle.Position = UDim2.fromOffset(12, 68)
    configureTextLabel(serverHopTitle, Enum.Font.GothamBold, 12, UI_COLORS.text)
    serverHopTitle.Text = "Path Server Hop"
    serverHopTitle.Parent = autoSellCardFrame

    local serverHopHint = Instance.new("TextLabel")
    serverHopHint.Size = UDim2.new(1, -24, 0, 28)
    serverHopHint.Position = UDim2.fromOffset(12, 86)
    configureTextLabel(serverHopHint, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    serverHopHint.TextWrapped = true
    serverHopHint.TextYAlignment = Enum.TextYAlignment.Top
    serverHopHint.Text = "Path-only. After a successful sell, it tries to join a smaller public server and resume the saved path."
    serverHopHint.Parent = autoSellCardFrame

    serverHopButton = Instance.new("TextButton")
    serverHopButton.Size = UDim2.new(1, -24, 0, 30)
    serverHopButton.Position = UDim2.fromOffset(12, 118)
    serverHopButton.Text = ""
    serverHopButton.Parent = autoSellCardFrame
    styleButton(serverHopButton, 13)

    serverHopStatusLabel = Instance.new("TextLabel")
    serverHopStatusLabel.Size = UDim2.new(1, -24, 0, 32)
    serverHopStatusLabel.Position = UDim2.fromOffset(12, 152)
    configureTextLabel(serverHopStatusLabel, Enum.Font.Gotham, 10, UI_COLORS.muted)
    serverHopStatusLabel.TextWrapped = true
    serverHopStatusLabel.TextYAlignment = Enum.TextYAlignment.Top
    serverHopStatusLabel.Text = ""
    serverHopStatusLabel.Parent = autoSellCardFrame

    liveFeedCardFrame = Instance.new("Frame")
    liveFeedCardFrame.Name = "LiveFeedCard"
    liveFeedCardFrame.Size = UDim2.new(0.5, -6, 0, 126)
    liveFeedCardFrame.Position = UDim2.new(0, 0, 0, 330)
    liveFeedCardFrame.Parent = automineCanvas
    styleCard(liveFeedCardFrame, 14)

    local liveFeedTitle = Instance.new("TextLabel")
    liveFeedTitle.Size = UDim2.new(1, -22, 0, 18)
    liveFeedTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(liveFeedTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    liveFeedTitle.Text = "Live Feed Section"
    liveFeedTitle.Parent = liveFeedCardFrame

    local liveFeedSubtitle = Instance.new("TextLabel")
    liveFeedSubtitle.Size = UDim2.new(1, -22, 0, 16)
    liveFeedSubtitle.Position = UDim2.fromOffset(12, 28)
    configureTextLabel(liveFeedSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    liveFeedSubtitle.Text = "Status, debug lines, and movement telemetry."
    liveFeedSubtitle.Parent = liveFeedCardFrame

    statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, -24, 0, 16)
    statusLabel.Position = UDim2.fromOffset(12, 48)
    configureTextLabel(statusLabel, Enum.Font.GothamSemibold, 12, Color3.fromRGB(180, 180, 180))
    statusLabel.Text = "Idle"
    statusLabel.Parent = liveFeedCardFrame

    debugLabel = Instance.new("TextLabel")
    debugLabel.Size = UDim2.new(1, -24, 0, 22)
    debugLabel.Position = UDim2.fromOffset(12, 68)
    configureTextLabel(debugLabel, Enum.Font.Code, 11, UI_COLORS.muted)
    debugLabel.Text = "Ready"
    debugLabel.TextWrapped = true
    debugLabel.TextYAlignment = Enum.TextYAlignment.Top
    debugLabel.Parent = liveFeedCardFrame

    movementDebugLabel = Instance.new("TextLabel")
    movementDebugLabel.Size = UDim2.new(1, -24, 0, 22)
    movementDebugLabel.Position = UDim2.fromOffset(12, 92)
    configureTextLabel(movementDebugLabel, Enum.Font.Code, 11, UI_COLORS.debug)
    movementDebugLabel.Text = "move debug loading..."
    movementDebugLabel.TextWrapped = true
    movementDebugLabel.TextYAlignment = Enum.TextYAlignment.Top
    movementDebugLabel.Parent = liveFeedCardFrame

    pathUi.pathsPage = Instance.new("Frame")
    pathUi.pathsPage.Name = "PathsPage"
    pathUi.pathsPage.Size = UDim2.new(1, 0, 0, 238)
    pathUi.pathsPage.Position = UDim2.fromOffset(0, 46)
    pathUi.pathsPage.BackgroundTransparency = 1
    pathUi.pathsPage.Visible = false
    pathUi.pathsPage.Parent = automineCanvas

    pathUi.managerCard = Instance.new("Frame")
    pathUi.managerCard.Name = "PathsManagerCard"
    pathUi.managerCard.Size = UDim2.new(1, 0, 0, 238)
    pathUi.managerCard.Parent = pathUi.pathsPage
    styleCard(pathUi.managerCard, 14)

    pathUi.managerTitle = Instance.new("TextLabel")
    pathUi.managerTitle.Size = UDim2.new(1, -22, 0, 18)
    pathUi.managerTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(pathUi.managerTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    pathUi.managerTitle.Text = "Paths"
    pathUi.managerTitle.Parent = pathUi.managerCard

    pathUi.managerSubtitle = Instance.new("TextLabel")
    pathUi.managerSubtitle.Size = UDim2.new(1, -22, 0, 16)
    pathUi.managerSubtitle.Position = UDim2.fromOffset(12, 28)
    configureTextLabel(pathUi.managerSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    pathUi.managerSubtitle.Text = "Record, save, edit, and loop a mining route with marked ores only."
    pathUi.managerSubtitle.Parent = pathUi.managerCard

    pathUi.recordPathButton = Instance.new("TextButton")
    pathUi.recordPathButton.Size = UDim2.new(1, -24, 0, 30)
    pathUi.recordPathButton.Position = UDim2.fromOffset(12, 52)
    pathUi.recordPathButton.Text = "Record Path"
    pathUi.recordPathButton.Parent = pathUi.managerCard
    styleButton(pathUi.recordPathButton, 13)

    pathUi.savedPathDropdownButton = Instance.new("TextButton")
    pathUi.savedPathDropdownButton.Size = UDim2.new(1, -24, 0, 30)
    pathUi.savedPathDropdownButton.Position = UDim2.fromOffset(12, 88)
    pathUi.savedPathDropdownButton.Text = "Choose Saved Path"
    pathUi.savedPathDropdownButton.Parent = pathUi.managerCard
    styleButton(pathUi.savedPathDropdownButton, 12)

    pathUi.savedPathDropdownFrame = Instance.new("Frame")
    pathUi.savedPathDropdownFrame.Size = UDim2.new(1, -24, 0, 112)
    pathUi.savedPathDropdownFrame.Position = UDim2.fromOffset(12, 124)
    pathUi.savedPathDropdownFrame.Visible = false
    pathUi.savedPathDropdownFrame.Parent = pathUi.managerCard
    styleCard(pathUi.savedPathDropdownFrame, 10)
    pathUi.savedPathDropdownFrame.BackgroundColor3 = UI_COLORS.panel

    pathUi.savedPathList = Instance.new("ScrollingFrame")
    pathUi.savedPathList.Size = UDim2.new(1, -12, 1, -12)
    pathUi.savedPathList.Position = UDim2.fromOffset(6, 6)
    pathUi.savedPathList.BackgroundTransparency = 1
    pathUi.savedPathList.BorderSizePixel = 0
    pathUi.savedPathList.ScrollBarThickness = 4
    pathUi.savedPathList.ScrollBarImageColor3 = UI_COLORS.accent
    pathUi.savedPathList.CanvasSize = UDim2.new(0, 0, 0, 0)
    pathUi.savedPathList.Parent = pathUi.savedPathDropdownFrame

    pathUi.savedPathListLayout = Instance.new("UIListLayout")
    pathUi.savedPathListLayout.Padding = UDim.new(0, 6)
    pathUi.savedPathListLayout.Parent = pathUi.savedPathList
    bindCanvasToLayout(pathUi.savedPathList, pathUi.savedPathListLayout, 2)

    pathUi.loopPlayButton = Instance.new("TextButton")
    pathUi.loopPlayButton.Size = UDim2.new(1, -24, 0, 30)
    pathUi.loopPlayButton.Position = UDim2.fromOffset(12, 124)
    pathUi.loopPlayButton.Text = "Play Path"
    pathUi.loopPlayButton.Parent = pathUi.managerCard
    styleButton(pathUi.loopPlayButton, 13)

    pathUi.alwaysPlayButton = Instance.new("TextButton")
    pathUi.alwaysPlayButton.Size = UDim2.new(1, -24, 0, 30)
    pathUi.alwaysPlayButton.Position = UDim2.fromOffset(12, 160)
    pathUi.alwaysPlayButton.Text = "Always Play Path"
    pathUi.alwaysPlayButton.Parent = pathUi.managerCard
    styleButton(pathUi.alwaysPlayButton, 13)

    pathUi.editPathButton = Instance.new("TextButton")
    pathUi.editPathButton.Size = UDim2.new(0.5, -18, 0, 30)
    pathUi.editPathButton.Position = UDim2.fromOffset(12, 196)
    pathUi.editPathButton.Text = "Edit Path"
    pathUi.editPathButton.Parent = pathUi.managerCard
    styleButton(pathUi.editPathButton, 13)

    pathUi.deletePathButton = Instance.new("TextButton")
    pathUi.deletePathButton.Size = UDim2.new(0.5, -18, 0, 30)
    pathUi.deletePathButton.Position = UDim2.new(0.5, 6, 0, 196)
    pathUi.deletePathButton.Text = "Delete Path"
    pathUi.deletePathButton.Parent = pathUi.managerCard
    styleButton(pathUi.deletePathButton, 13)

    pathUi.editorFrame = Instance.new("Frame")
    pathUi.editorFrame.Name = "PathEditor"
    pathUi.editorFrame.Size = UDim2.fromOffset(336, 474)
    pathUi.editorFrame.Position = UDim2.new(1, -352, 0.5, -237)
    pathUi.editorFrame.BackgroundColor3 = UI_COLORS.panel
    pathUi.editorFrame.BorderSizePixel = 0
    pathUi.editorFrame.Active = true
    pathUi.editorFrame.Visible = false
    -- Keep the recorder editor outside the clipped main panel and outside the
    -- draggable hub wrapper so it stays put when the main GUI moves.
    pathUi.editorFrame.Parent = overlayRoot
    addCorner(pathUi.editorFrame, 16)
    addStroke(pathUi.editorFrame, UI_COLORS.border, 1)

    pathUi.editorDragHandle = Instance.new("Frame")
    pathUi.editorDragHandle.Name = "DragHandle"
    pathUi.editorDragHandle.Size = UDim2.new(1, 0, 0, 42)
    pathUi.editorDragHandle.Active = true
    pathUi.editorDragHandle.BackgroundTransparency = 1
    pathUi.editorDragHandle.Parent = pathUi.editorFrame

    pathUi.editorTitle = Instance.new("TextLabel")
    pathUi.editorTitle.Size = UDim2.new(1, -24, 0, 18)
    pathUi.editorTitle.Position = UDim2.fromOffset(12, 10)
    configureTextLabel(pathUi.editorTitle, Enum.Font.GothamBold, 14, UI_COLORS.text)
    pathUi.editorTitle.Text = "Path Editor"
    pathUi.editorTitle.Parent = pathUi.editorFrame

    pathUi.editorSubtitle = Instance.new("TextLabel")
    pathUi.editorSubtitle.Size = UDim2.new(1, -24, 0, 32)
    pathUi.editorSubtitle.Position = UDim2.fromOffset(12, 30)
    configureTextLabel(pathUi.editorSubtitle, Enum.Font.Gotham, 11, UI_COLORS.subtle)
    pathUi.editorSubtitle.TextWrapped = true
    pathUi.editorSubtitle.Text = "Record nodes, pick the spawn this path should use, tune spacing, then turn on Edit node placement if you want to drag nodes around."
    pathUi.editorSubtitle.Parent = pathUi.editorFrame

    local function positionEditorControl(control, yOffset)
        control.Size = UDim2.new(1, -24, 0, 38)
        control.Position = UDim2.fromOffset(12, yOffset)
        control.Active = true
        control.ClipsDescendants = true
        return control
    end

    pathUi.editorRecordButton, _, pathUi.editorRecordSync, pathUi.editorRecordLabel = createToggleRow(
        "Resume Recording",
        pathUi.editorFrame,
        function()
            return pathState.recording
        end,
        function()
            if not pathState.editorVisible then
                return
            end
            if pathState.recording then
                pathState.recording = false
            else
                pathState.recording = true
                ensureWorkingPathStartNode()
            end
            refreshPathEditorButtons()
            refreshPathVisuals()
            setPathEditorIdleStatus()
        end
    )
    positionEditorControl(pathUi.editorRecordButton, 70)

    pathUi.editorSaveButton, _, pathUi.editorSaveLabel = createButtonRow(
        "Save Path",
        pathUi.editorFrame,
        function()
            if not pathState.editorVisible then
                return
            end
            if pathState.editingSavedPath then
                updateWorkingPath()
                return
            end
            pathUi.editorNameBox.Visible = true
            pathUi.editorSaveConfirmButton.Visible = true
            pathUi.editorNameBox.Text = pathState.selectedPathName or ""
            refreshPathEditorButtons()
            pathUi.editorNameBox:CaptureFocus()
            setPathEditorStatus("Type a name, then confirm save.", UI_COLORS.muted)
        end,
        {skipRegistry = true}
    )
    positionEditorControl(pathUi.editorSaveButton, 116)
    if pathUi.editorSaveLabel then
        pathUi.editorSaveLabel.Size = UDim2.new(1, -20, 1, 0)
    end

    pathUi.editorNameBox = Instance.new("TextBox")
    pathUi.editorNameBox.Size = UDim2.new(1, -24, 0, 30)
    pathUi.editorNameBox.Position = UDim2.fromOffset(12, 162)
    pathUi.editorNameBox.PlaceholderText = "Path name"
    pathUi.editorNameBox.Text = ""
    pathUi.editorNameBox.ClearTextOnFocus = false
    pathUi.editorNameBox.Visible = false
    pathUi.editorNameBox.Parent = pathUi.editorFrame
    styleButton(pathUi.editorNameBox, 13)

    pathUi.editorSaveConfirmButton, _, pathUi.editorSaveConfirmLabel = createButtonRow(
        "Confirm Save",
        pathUi.editorFrame,
        function()
            saveWorkingPath(pathUi.editorNameBox.Text)
            refreshPathManagerButtons()
            refreshPathEditorButtons()
        end,
        {skipRegistry = true}
    )
    positionEditorControl(pathUi.editorSaveConfirmButton, 198)
    if pathUi.editorSaveConfirmLabel then
        pathUi.editorSaveConfirmLabel.Size = UDim2.new(1, -20, 1, 0)
    end
    pathUi.editorSaveConfirmButton.Visible = false

    pathUi.editorSpawnRow, pathUi.editorSpawnSync = createDropdownRow(
        "Spawn Location",
        pathUi.editorFrame,
        getPathSpawnOptions,
        function()
            return getWorkingPathSpawnDisplayName()
        end,
        function(value)
            setWorkingPathSpawnName(value)
        end
    )
    positionEditorControl(pathUi.editorSpawnRow, 162)

    local initialNodeSpacing = (type(getPathRecordNodeSpacing) == "function" and getPathRecordNodeSpacing())
        or math.clamp(tonumber(S.pathRecordNodeSpacing) or PATH_RECORD_NODE_SPACING or 9, 1, 10)

    pathUi.editorNodeSpacingRow, pathUi.editorNodeSpacingSync, _, pathUi.editorNodeSpacingLabel, pathUi.editorNodeSpacingBox = createSliderRow(
        "Node Spacing",
        pathUi.editorFrame,
        initialNodeSpacing,
        function(value)
            if type(setPathRecordNodeSpacing) == "function" then
                setPathRecordNodeSpacing(value)
            end
        end,
        function()
            if type(getPathRecordNodeSpacing) == "function" then
                return getPathRecordNodeSpacing()
            end
            return math.clamp(tonumber(S.pathRecordNodeSpacing) or PATH_RECORD_NODE_SPACING or initialNodeSpacing, 1, 10)
        end,
        1,
        10,
        {
            decimals = 1,
            step = 0.1,
            textBoxWidth = 42,
            labelWidth = 96,
            trackWidth = 118,
            trackRightInset = 130,
        }
    )
    positionEditorControl(pathUi.editorNodeSpacingRow, 208)

    pathUi.editorEditNodePlacementButton, _, pathUi.editorEditNodePlacementSync, pathUi.editorEditNodePlacementLabel = createToggleRow(
        "Edit Node Placement",
        pathUi.editorFrame,
        function()
            return pathState.editNodePlacementEnabled
        end,
        function()
            if not pathState.editorVisible then
                return
            end
            setEditNodePlacementEnabled(not pathState.editNodePlacementEnabled)
        end
    )
    positionEditorControl(pathUi.editorEditNodePlacementButton, 254)

    pathUi.editorMarkKeyButton, _, pathUi.editorMarkKeyLabel = createButtonRow(
        "Mark Ore Key",
        pathUi.editorFrame,
        function()
            pendingKeybindAction = "markore"
            refreshPathEditorButtons()
        end,
        {skipRegistry = true}
    )
    positionEditorControl(pathUi.editorMarkKeyButton, 300)
    if pathUi.editorMarkKeyLabel then
        pathUi.editorMarkKeyLabel.Size = UDim2.new(1, -20, 1, 0)
    end

    pathUi.editorDeleteModeButton, _, pathUi.editorDeleteModeSync, pathUi.editorDeleteModeLabel = createToggleRow(
        "Delete Mode",
        pathUi.editorFrame,
        function()
            return pathState.deleteMode
        end,
        function()
            pathState.deleteMode = not pathState.deleteMode
            if pathState.deleteMode then
                pathState.sellNodePickMode = false
                pathState.editNodePlacementEnabled = false
                pathState.selectedNodeIndex = nil
                pathState.draggingNodeIndex = nil
                pathState.draggingNodeAxis = nil
                pathState.dragLastPointerPosition = nil
            end
            refreshPathEditorButtons()
            refreshPathVisuals()
            setPathEditorIdleStatus()
        end
    )
    positionEditorControl(pathUi.editorDeleteModeButton, 346)

    pathUi.editorSellNodeButton, _, pathUi.editorSellNodeLabel = createButtonRow(
        "Pick Sell Node",
        pathUi.editorFrame,
        function()
            toggleSelectedWorkingPathSellNode()
        end,
        {skipRegistry = true}
    )
    positionEditorControl(pathUi.editorSellNodeButton, 392)
    if pathUi.editorSellNodeLabel then
        pathUi.editorSellNodeLabel.Size = UDim2.new(1, -20, 1, 0)
    end

    pathUi.editorStatusLabel = Instance.new("TextLabel")
    pathUi.editorStatusLabel.Size = UDim2.new(1, -24, 0, 44)
    pathUi.editorStatusLabel.Position = UDim2.fromOffset(12, 392)
    configureTextLabel(pathUi.editorStatusLabel, Enum.Font.Gotham, 10, UI_COLORS.muted)
    pathUi.editorStatusLabel.TextWrapped = true
    pathUi.editorStatusLabel.TextYAlignment = Enum.TextYAlignment.Top
    pathUi.editorStatusLabel.Text = ""
    pathUi.editorStatusLabel.Parent = pathUi.editorFrame

    pathUi.layoutEditorControls = function()
        local yOffset = 70
        local rowGap = 8

        if pathUi.editorRecordButton then
            positionEditorControl(pathUi.editorRecordButton, yOffset)
            yOffset = yOffset + 38 + rowGap
        end

        if pathUi.editorSaveButton then
            positionEditorControl(pathUi.editorSaveButton, yOffset)
            yOffset = yOffset + 38 + rowGap
        end

        local savePromptVisible = (pathUi.editorNameBox and pathUi.editorNameBox.Visible)
            or (pathUi.editorSaveConfirmButton and pathUi.editorSaveConfirmButton.Visible)
        if pathUi.editorNameBox then
            pathUi.editorNameBox.Position = UDim2.fromOffset(12, yOffset)
            pathUi.editorNameBox.Size = UDim2.new(1, -24, 0, 30)
        end
        if savePromptVisible then
            yOffset = yOffset + 30 + rowGap
            if pathUi.editorSaveConfirmButton then
                positionEditorControl(pathUi.editorSaveConfirmButton, yOffset)
                yOffset = yOffset + 38 + rowGap
            end
        end

        if pathUi.editorSpawnRow then
            positionEditorControl(pathUi.editorSpawnRow, yOffset)
            yOffset = yOffset + 38 + rowGap
        end
        if pathUi.editorNodeSpacingRow then
            positionEditorControl(pathUi.editorNodeSpacingRow, yOffset)
            yOffset = yOffset + 38 + rowGap
        end
        if pathUi.editorEditNodePlacementButton then
            positionEditorControl(pathUi.editorEditNodePlacementButton, yOffset)
            yOffset = yOffset + 38 + rowGap
        end
        if pathUi.editorMarkKeyButton then
            positionEditorControl(pathUi.editorMarkKeyButton, yOffset)
            yOffset = yOffset + 38 + rowGap
        end
        if pathUi.editorDeleteModeButton then
            positionEditorControl(pathUi.editorDeleteModeButton, yOffset)
            yOffset = yOffset + 38 + rowGap
        end
        if pathUi.editorSellNodeButton then
            positionEditorControl(pathUi.editorSellNodeButton, yOffset)
            yOffset = yOffset + 38 + rowGap
        end
        if pathUi.editorStatusLabel then
            pathUi.editorStatusLabel.Position = UDim2.fromOffset(12, yOffset)
        end

        local targetHeight = math.max(474, yOffset + 44 + 12)
        pathUi.editorFrame.Size = UDim2.fromOffset(336, targetHeight)
    end
    pathUi.layoutEditorControls()

    local function applyEditorZ(root, baseZ)
        if root:IsA("GuiObject") then
            root.ZIndex = baseZ
        end
        for _, descendant in ipairs(root:GetDescendants()) do
            if descendant:IsA("GuiObject") then
                descendant.ZIndex = baseZ + 1
            end
        end
    end
    applyEditorZ(pathUi.editorFrame, 220)
    attachFloatingDrag(pathUi.editorFrame, pathUi.editorDragHandle)

    registerButtonEntry({
        name = "Play Selected Path",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            toggleAutoMineFromControl("keybind")
        end,
    })
    registerButtonEntry({
        name = "Auto Pickup",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            autoPickupEnabled = not autoPickupEnabled
            setInteractKeyHeld(false)
            refreshAutoPickupButton()
        end,
    })
    registerButtonEntry({
        name = "Pickup Whitelist",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            autoPickupWhitelistEnabled = not autoPickupWhitelistEnabled
            refreshPickupWhitelistDisplay()
        end,
    })
    registerButtonEntry({
        name = "Path Server Hop",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            if serverHopInProgress then
                return
            end
            serverHopEnabled = not serverHopEnabled
            if not serverHopEnabled then
                setPathResumePlaybackPending(false, false)
                setServerHopStatus("Off. Path sells stay in the current server.", Color3.fromRGB(176, 167, 150))
            else
                setServerHopStatus("After a path sell, it will try to hop into a smaller public server.", Color3.fromRGB(176, 167, 150))
            end
            savePathsToStorage(false)
            refreshServerHopDisplay()
        end,
    })
    registerButtonEntry({
        name = "Record Path",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            if pathState.editorVisible and not pathState.editingSavedPath then
                closePathEditor()
            else
                beginNewPathRecording()
                refreshPathEditorButtons()
                refreshPathManagerButtons()
            end
        end,
    })
    registerButtonEntry({
        name = "Play Path",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            togglePathLoopFromControl("keybind")
        end,
    })
    registerButtonEntry({
        name = "Always Play Path",
        tab = "AUTOMINE",
        type = "button",
        onClickFn = function()
            local playableSlotIndex = getPathSequenceStartSlotIndex()
            if not playableSlotIndex then
                return
            end
            setPathSequenceStartSlotIndex(playableSlotIndex)
            setFocusedPathSlotIndex(playableSlotIndex, true)
	pathState.alwaysPlayEnabled = not pathState.alwaysPlayEnabled
	if pathState.alwaysPlayEnabled and not autoMineEnabled then
		activeFarmMode = "path"
		local ok, started = xpcall(function()
			return startPathPlaybackForSlot(playableSlotIndex, "alwaysplay")
		end, debug.traceback)
		if not ok then
			autoMineEnabled = false
			activeFarmMode = nil
			setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
			pushDebug(started)
		elseif not started then
			autoMineEnabled = false
			activeFarmMode = nil
		end
	end
            savePathsToStorage(true)
            refreshPathManagerButtons()
        end,
    })

    for _, oreOption in ipairs(ORE_OPTIONS) do
        local oreButton = oreOptionButtons[oreOption.id]
        if oreButton then
            oreButton.MouseButton1Click:Connect(function()
                if selectedOreNames[oreOption.id] then
                    selectedOreNames[oreOption.id] = nil
                else
                    selectedOreNames[oreOption.id] = true
                end
                if currentTargetOre and not isOreNameSelected(getOreGroupName(currentTargetOre)) then
                    currentTargetOre = nil
                end
                if not hasSelectedOres() then
                    setStatus("Select at least one ore", Color3.fromRGB(255, 190, 120))
                end
                refreshOreSelectionDisplay()
            end)
        end
    end

    oreDropdownButton.MouseButton1Click:Connect(function()
        setOreDropdownOpen(not oreDropdownOpen)
        updateAutofarmLayout()
        refreshOreSelectionDisplay()
    end)

    tweenButton.MouseButton1Click:Connect(function()
        setMovementModeDropdownOpen(not movementModeDropdownOpen)
        updateAutofarmLayout()
        refreshTweenButton()
    end)

    movementModeLegitButton.MouseButton1Click:Connect(function()
        setMovementMode("legit", "selector")
        setMovementModeDropdownOpen(false)
        updateAutofarmLayout()
        refreshTweenButton()
    end)

    movementModeTweenButton.MouseButton1Click:Connect(function()
        setMovementMode("tween", "selector")
        setMovementModeDropdownOpen(false)
        updateAutofarmLayout()
        refreshTweenButton()
    end)

    movementModeCFrameButton.MouseButton1Click:Connect(function()
        setMovementMode("cframe", "selector")
        setMovementModeDropdownOpen(false)
        updateAutofarmLayout()
        refreshTweenButton()
    end)

    tweenSpeedBox.FocusLost:Connect(function(enterPressed)
        local typed = tonumber(tweenSpeedBox.Text)
        if typed then
            local oldValue = tweenMoveSpeed
            setTweenSpeed(typed)
            if math.abs(oldValue - tweenMoveSpeed) > 0.05 and enterPressed then
                pushDebug("move speed: " .. tostring(math.floor(tweenMoveSpeed + 0.5)))
                if tweenMoveSpeed > 40 then
                    pushDebugRateLimited("tween_speed_pushback", "move speeds above ~40 may get server pushback", 2.5)
                end
            end
        else
            setTweenSpeed(tweenMoveSpeed)
        end
    end)

    toggleButton.MouseButton1Click:Connect(function()
        toggleAutoMineFromControl("button")
    end)

    autoPickupButton.MouseButton1Click:Connect(function()
        autoPickupEnabled = not autoPickupEnabled
        setInteractKeyHeld(false)
        pushDebug("auto pickup: " .. (autoPickupEnabled and "ON" or "OFF"))
        refreshAutoPickupButton()
    end)

    serverHopButton.MouseButton1Click:Connect(function()
        if serverHopInProgress then
            return
        end
        serverHopEnabled = not serverHopEnabled
        if not serverHopEnabled then
            setPathResumePlaybackPending(false, false)
            setServerHopStatus("Off. Path sells stay in the current server.", Color3.fromRGB(176, 167, 150))
        else
            setServerHopStatus("After a path sell, it will try to hop into a smaller public server.", Color3.fromRGB(176, 167, 150))
        end
        savePathsToStorage(false)
        pushDebug("path server hop: " .. (serverHopEnabled and "ON" or "OFF"))
        refreshServerHopDisplay()
    end)

    pickupWhitelistButton.MouseButton1Click:Connect(function()
        autoPickupWhitelistEnabled = not autoPickupWhitelistEnabled
        pushDebug("pickup whitelist: " .. (autoPickupWhitelistEnabled and "ON" or "OFF"))
        refreshPickupWhitelistDisplay()
    end)

    pickupWhitelistDropdownButton.MouseButton1Click:Connect(function()
        setPickupWhitelistDropdownOpen(not pickupWhitelistDropdownOpen)
        updateAutofarmLayout()
        refreshPickupWhitelistDisplay()
    end)

    for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
        local optionButton = pickupWhitelistOptionButtons[pickupOption.id]
        if optionButton then
            optionButton.MouseButton1Click:Connect(function()
                if selectedPickupNames[pickupOption.id] then
                    selectedPickupNames[pickupOption.id] = nil
                else
                    selectedPickupNames[pickupOption.id] = true
                end
                refreshPickupWhitelistDisplay()
            end)
        end
    end

    pathUi.subTabButtons.AutofarmSettings.MouseButton1Click:Connect(function()
        setAutofarmSubTab("AutofarmSettings")
    end)

    pathUi.subTabButtons.Paths.MouseButton1Click:Connect(function()
        setAutofarmSubTab("Paths")
    end)

    pathUi.recordPathButton.MouseButton1Click:Connect(function()
        if pathState.editorVisible and not pathState.editingSavedPath then
            closePathEditor()
            return
        end
        beginNewPathRecording()
        refreshPathEditorButtons()
        refreshPathManagerButtons()
    end)

    pathUi.savedPathDropdownButton.MouseButton1Click:Connect(function()
        setSavedPathDropdownOpen(not pathState.pathDropdownOpen)
    end)

    pathUi.loopPlayButton.MouseButton1Click:Connect(function()
        togglePathLoopFromControl("button")
    end)

    pathUi.alwaysPlayButton.MouseButton1Click:Connect(function()
        if not pathState.selectedPathName or not pathState.paths[pathState.selectedPathName] then
            notifyUser("Paths", "Pick a saved path first.", 4)
            return
        end
        pathState.alwaysPlayEnabled = not pathState.alwaysPlayEnabled
        if pathState.alwaysPlayEnabled and not autoMineEnabled then
            activeFarmMode = "path"
            local ok, started = xpcall(function()
                return startSelectedPathPlayback("alwaysplay")
            end, debug.traceback)
            if not ok then
                autoMineEnabled = false
                activeFarmMode = nil
                setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
                pushDebug(started)
            elseif not started then
                autoMineEnabled = false
                activeFarmMode = nil
            end
        end
        savePathsToStorage(true)
        refreshPathManagerButtons()
    end)

    pathUi.editPathButton.MouseButton1Click:Connect(function()
        if pathState.selectedPathName then
            beginPathEditing(pathState.selectedPathName)
            refreshPathEditorButtons()
            refreshPathManagerButtons()
        end
    end)

    pathUi.deletePathButton.MouseButton1Click:Connect(function()
        if not pathState.selectedPathName then
            return
        end
        if pathState.deleteConfirmArmed then
            pathState.deleteConfirmArmed = false
            deleteSelectedPath()
            pathUi.deletePathButton.Text = "Delete Path"
            return
        end
        pathState.deleteConfirmArmed = true
        pathUi.deletePathButton.Text = "Confirm Delete"
        task.delay(3, function()
            if pathUi.deletePathButton and pathState.deleteConfirmArmed then
                pathState.deleteConfirmArmed = false
                pathUi.deletePathButton.Text = "Delete Path"
            end
        end)
    end)

    do
        local previousRegistryTab = _currentRegistryTab
        _currentRegistryTab = nil
        pathUi.useFobRows = true

        if autoFarmCardFrame then autoFarmCardFrame.Visible = false end
        if movementCardFrame then movementCardFrame.Visible = false end
        if pickupCardFrame then pickupCardFrame.Visible = false end
        if autoSellCardFrame then autoSellCardFrame.Visible = false end
        if liveFeedCardFrame then liveFeedCardFrame.Visible = false end
        if pathUi.pathsPage then pathUi.pathsPage.Visible = false end
        if pathUi.managerCard then pathUi.managerCard.Visible = false end

        local function nextCustomOrder(guiObject)
            _rowLayoutCounter = _rowLayoutCounter + 1
            guiObject.LayoutOrder = _rowLayoutCounter
            return guiObject
        end

        local function clearSelectionMap(targetMap)
            for key in pairs(targetMap) do
                targetMap[key] = nil
            end
        end

        local function getPickupOptionIdFromValue(value)
            local loweredValue = string.lower(tostring(value or ""))
            for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
                if loweredValue == string.lower(pickupOption.id) or loweredValue == string.lower(pickupOption.label) then
                    return pickupOption.id
                end
            end
            return nil
        end

        pathUi.mainRowsPage = Instance.new("Frame")
        pathUi.mainRowsPage.Name = "AutomineRowsPage"
        pathUi.mainRowsPage.Size = UDim2.new(1, 0, 0, 0)
        pathUi.mainRowsPage.Position = UDim2.fromOffset(0, 46)
        pathUi.mainRowsPage.BackgroundTransparency = 1
        pathUi.mainRowsPage.BorderSizePixel = 0
        pathUi.mainRowsPage.Parent = automineCanvas

        pathUi.mainRowsLayout = Instance.new("UIListLayout")
        pathUi.mainRowsLayout.Padding = UDim.new(0, 8)
        pathUi.mainRowsLayout.SortOrder = Enum.SortOrder.LayoutOrder
        pathUi.mainRowsLayout.Parent = pathUi.mainRowsPage

        pathUi.pathRowsPage = Instance.new("Frame")
        pathUi.pathRowsPage.Name = "PathsRowsPage"
        pathUi.pathRowsPage.Size = UDim2.new(1, 0, 0, 0)
        pathUi.pathRowsPage.Position = UDim2.fromOffset(0, 46)
        pathUi.pathRowsPage.BackgroundTransparency = 1
        pathUi.pathRowsPage.BorderSizePixel = 0
        pathUi.pathRowsPage.Visible = false
        pathUi.pathRowsPage.Parent = automineCanvas

        pathUi.pathRowsLayout = Instance.new("UIListLayout")
        pathUi.pathRowsLayout.Padding = UDim.new(0, 8)
        pathUi.pathRowsLayout.SortOrder = Enum.SortOrder.LayoutOrder
        pathUi.pathRowsLayout.Parent = pathUi.pathRowsPage

        local _, movementModeSync = createDropdownRow(
            "Movement Type",
            pathUi.mainRowsPage,
            function()
                return {"Legit", "Tween", "CFrame"}
            end,
            function()
                return getMovementModeLabel()
            end,
            function(value)
                setMovementMode(value, "selector")
                refreshTweenButton()
            end
        )
        pathUi.movementModeSync = movementModeSync

        pathUi.movementSpeedContainer = nextCustomOrder(Instance.new("Frame"))
        pathUi.movementSpeedContainer.Name = "MovementSpeedContainer"
        pathUi.movementSpeedContainer.Size = UDim2.new(1, 0, 0, 0)
        pathUi.movementSpeedContainer.BackgroundTransparency = 1
        pathUi.movementSpeedContainer.BorderSizePixel = 0
        pathUi.movementSpeedContainer.ClipsDescendants = true
        pathUi.movementSpeedContainer.Parent = pathUi.mainRowsPage

        pathUi.movementSpeedRow, _, _, pathUi.movementSpeedLabel = createSliderRow(
            "Tween Speed",
            pathUi.movementSpeedContainer,
            tweenMoveSpeed,
            function(value)
                local oldValue = tweenMoveSpeed
                setTweenSpeed(value)
                if math.abs(oldValue - tweenMoveSpeed) > 0.05 then
                    pushDebug("move speed: " .. tostring(math.floor(tweenMoveSpeed + 0.5)))
                    if tweenMoveSpeed > 40 then
                        pushDebugRateLimited("tween_speed_pushback", "move speeds above ~40 may get server pushback", 2.5)
                    end
                end
            end,
            function()
                return math.floor(tweenMoveSpeed + 0.5)
            end,
            1,
            100
        )
        pathUi.movementSpeedRow.Visible = false

        _, _, pathUi.autoPickupSync = createToggleRow(
            "Auto Pickup",
            pathUi.mainRowsPage,
            function()
                return autoPickupEnabled
            end,
            function()
                autoPickupEnabled = not autoPickupEnabled
                setInteractKeyHeld(false)
                pushDebug("auto pickup: " .. (autoPickupEnabled and "ON" or "OFF"))
                refreshAutoPickupButton()
            end
        )

        _, _, pathUi.pickupWhitelistSync = createToggleRow(
            "Pickup Filter",
            pathUi.mainRowsPage,
            function()
                return autoPickupWhitelistEnabled
            end,
            function()
                autoPickupWhitelistEnabled = not autoPickupWhitelistEnabled
                pushDebug("pickup whitelist: " .. (autoPickupWhitelistEnabled and "ON" or "OFF"))
                refreshPickupWhitelistDisplay()
            end
        )

        _, pathUi.pickupFilterSync = createDropdownRow(
            "Pickup Type",
            pathUi.mainRowsPage,
            function()
                local options = {"none"}
                for _, pickupOption in ipairs(AUTO_PICKUP_OPTIONS) do
                    table.insert(options, pickupOption.label)
                end
                return options
            end,
            function()
                return getSelectedPickupSummary()
            end,
            function(value)
                clearSelectionMap(selectedPickupNames)
                local pickupId = getPickupOptionIdFromValue(value)
                if pickupId then
                    selectedPickupNames[pickupId] = true
                end
                refreshPickupWhitelistDisplay()
            end
        )

        pathUi.statusFrame = nextCustomOrder(Instance.new("Frame"))
        pathUi.statusFrame.Name = "AutomineStatusFrame"
        pathUi.statusFrame.Size = UDim2.new(1, 0, 0, 76)
        pathUi.statusFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
        pathUi.statusFrame.BorderSizePixel = 0
        pathUi.statusFrame.Parent = pathUi.mainRowsPage
        Instance.new("UICorner", pathUi.statusFrame).CornerRadius = UDim.new(0, 8)
        pathUi.statusStroke = Instance.new("UIStroke")
        pathUi.statusStroke.Color = Color3.fromRGB(34, 34, 34)
        pathUi.statusStroke.Transparency = 0.55
        pathUi.statusStroke.Parent = pathUi.statusFrame

        statusLabel = Instance.new("TextLabel")
        statusLabel.Size = UDim2.new(1, -16, 0, 16)
        statusLabel.Position = UDim2.fromOffset(8, 8)
        statusLabel.BackgroundTransparency = 1
        statusLabel.Text = "Idle"
        statusLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
        statusLabel.Font = Enum.Font.GothamSemibold
        statusLabel.TextSize = 12
        statusLabel.TextXAlignment = Enum.TextXAlignment.Left
        statusLabel.Parent = pathUi.statusFrame

        debugLabel = Instance.new("TextLabel")
        debugLabel.Size = UDim2.new(1, -16, 0, 20)
        debugLabel.Position = UDim2.fromOffset(8, 28)
        debugLabel.BackgroundTransparency = 1
        debugLabel.Text = "Ready"
        debugLabel.TextColor3 = UI_COLORS.muted
        debugLabel.Font = Enum.Font.Code
        debugLabel.TextSize = 11
        debugLabel.TextWrapped = true
        debugLabel.TextXAlignment = Enum.TextXAlignment.Left
        debugLabel.TextYAlignment = Enum.TextYAlignment.Top
        debugLabel.Parent = pathUi.statusFrame

        movementDebugLabel = Instance.new("TextLabel")
        movementDebugLabel.Size = UDim2.new(1, -16, 0, 20)
        movementDebugLabel.Position = UDim2.fromOffset(8, 48)
        movementDebugLabel.BackgroundTransparency = 1
        movementDebugLabel.Text = "move debug loading..."
        movementDebugLabel.TextColor3 = UI_COLORS.debug
        movementDebugLabel.Font = Enum.Font.Code
        movementDebugLabel.TextSize = 11
        movementDebugLabel.TextWrapped = true
        movementDebugLabel.TextXAlignment = Enum.TextXAlignment.Left
        movementDebugLabel.TextYAlignment = Enum.TextYAlignment.Top
        movementDebugLabel.Parent = pathUi.statusFrame

        _, _, pathUi.serverHopSync = createToggleRow(
            "Path Server Hop",
            pathUi.pathRowsPage,
            function()
                return serverHopEnabled or serverHopInProgress
            end,
            function()
                if serverHopInProgress then
                    return
                end
                serverHopEnabled = not serverHopEnabled
                if not serverHopEnabled then
                    setPathResumePlaybackPending(false, false)
                    setServerHopStatus("Off. Path sells stay in the current server.", Color3.fromRGB(176, 167, 150))
                else
                    setServerHopStatus("After a path sell, it will try to hop into a smaller public server.", Color3.fromRGB(176, 167, 150))
                end
                savePathsToStorage(false)
                pushDebug("path server hop: " .. (serverHopEnabled and "ON" or "OFF"))
                refreshServerHopDisplay()
            end
        )

        _, _, pathUi.staffHopSync = createToggleRow(
            "Hop on staff join",
            pathUi.pathRowsPage,
            function()
                return pathState.staffHopEnabled
            end,
            function()
                pathState.staffHopEnabled = not pathState.staffHopEnabled
                savePathsToStorage(false)
                if pathState.staffHopEnabled then
                    scanCurrentTwwStaff("notify")
                end
                refreshPathManagerButtons()
            end
        )

        _, _, pathUi.farmPathsSync = createToggleRow(
            "Farm paths",
            pathUi.pathRowsPage,
            function()
                return pathState.farmPathsEnabled
            end,
            function()
                local firstPlayableSlotIndex = getPlayablePathSlotIndex(1)
                if not pathState.farmPathsEnabled and not firstPlayableSlotIndex then
                    notifyUser("Paths", "Pick a saved path first.", 4)
                    return
                end

                local nextEnabled = not pathState.farmPathsEnabled
                pathState.farmPathsEnabled = nextEnabled
                if nextEnabled then
                    pathState.playLoopEnabled = true
                    setPathSequenceStartSlotIndex(firstPlayableSlotIndex or 1)
                    setFocusedPathSlotIndex(firstPlayableSlotIndex or 1, true)
                    if not autoMineEnabled then
                        activeFarmMode = "path"
                        local ok, started = xpcall(function()
                            return startPathPlaybackForSlot(firstPlayableSlotIndex, "farmpaths")
                        end, debug.traceback)
                        if not ok then
                            autoMineEnabled = false
                            pathState.playLoopEnabled = false
                            pathState.farmPathsEnabled = false
                            activeFarmMode = nil
                            setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
                            pushDebug(started)
                        elseif not started then
                            autoMineEnabled = false
                            pathState.playLoopEnabled = false
                            pathState.farmPathsEnabled = false
                            activeFarmMode = nil
                        end
                    end
                else
                    pathState.playLoopEnabled = false
                    pathState.resumePathPlaybackPending = false
                    if autoMineEnabled and activeFarmMode == "path" then
                        stopAutoMine()
                    end
                end
                savePathsToStorage(true)
                refreshPathManagerButtons()
                refreshButton()
                refreshServerHopDisplay()
            end
        )

        _, _, pathUi.hopOnDeathSync = createToggleRow(
            "Hop on death",
            pathUi.pathRowsPage,
            function()
                return isPathHopOnDeathEnabled()
            end,
            function()
                setPathHopOnDeathEnabled(not isPathHopOnDeathEnabled())
                refreshPathManagerButtons()
            end
        )

        do
            _, _, pathUi.playerAvoiderSync = createToggleRow(
                "Player Avoider",
                pathUi.pathRowsPage,
                function()
                    return isPathPlayerAvoiderEnabled()
                end,
                function()
                    local nextValue = not isPathPlayerAvoiderEnabled()
                    setPathPlayerAvoiderEnabled(nextValue)
                    refreshPathPlayerAvoiderRowVisibility(false)
                    refreshPathManagerButtons()
                    updateAutofarmLayout()
                end
            )

            pathUi.playerAvoiderMethodRow, pathUi.playerAvoiderMethodSync = createDropdownRow(
                "Avoid Method",
                pathUi.pathRowsPage,
                PATH_PLAYER_AVOIDER_METHOD_ORDER,
                function()
                    return getPathPlayerAvoiderMethod()
                end,
                function(value)
                    setPathPlayerAvoiderMethod(value)
                    refreshPathPlayerAvoiderRowVisibility(false)
                    refreshPathManagerButtons()
                    updateAutofarmLayout()
                end
            )

            pathUi.playerAvoiderActionRow, pathUi.playerAvoiderActionSync = createDropdownRow(
                "Avoid Action",
                pathUi.pathRowsPage,
                PATH_PLAYER_AVOIDER_ACTION_ORDER,
                function()
                    return getPathPlayerAvoiderAction()
                end,
                function(value)
                    setPathPlayerAvoiderAction(value)
                    refreshPathManagerButtons()
                end
            )

            pathUi.playerAvoiderDistanceRow, pathUi.playerAvoiderDistanceUpdate = createSliderRow(
                "Detect Distance",
                pathUi.pathRowsPage,
                getPathPlayerAvoiderDistance(),
                function(value)
                    setPathPlayerAvoiderDistance(value)
                end,
                function()
                    return getPathPlayerAvoiderDistance()
                end,
                1,
                200
            )
            pathUi.playerAvoiderDistanceSync = function()
                if pathUi.playerAvoiderDistanceUpdate then
                    pathUi.playerAvoiderDistanceUpdate(getPathPlayerAvoiderDistance(), true)
                end
            end

            pathUi.playerAvoiderShowCorridorRow, _, pathUi.playerAvoiderShowCorridorSync = createToggleRow(
                "Visualize Corridor",
                pathUi.pathRowsPage,
                function()
                    return isPathPlayerAvoiderShowCorridorEnabled()
                end,
                function()
                    setPathPlayerAvoiderShowCorridorEnabled(not isPathPlayerAvoiderShowCorridorEnabled())
                    refreshPathManagerButtons()
                    updateAutofarmLayout()
                end
            )

            pathUi.playerAvoiderRadiusRow, pathUi.playerAvoiderRadiusUpdate = createSliderRow(
                "Radius Distance",
                pathUi.pathRowsPage,
                getPathPlayerAvoiderRadius(),
                function(value)
                    setPathPlayerAvoiderRadius(value)
                end,
                function()
                    return getPathPlayerAvoiderRadius()
                end,
                5,
                200
            )
            pathUi.playerAvoiderRadiusSync = function()
                if pathUi.playerAvoiderRadiusUpdate then
                    pathUi.playerAvoiderRadiusUpdate(getPathPlayerAvoiderRadius(), true)
                end
            end

            pathUi.playerAvoiderShowRadiusRow, _, pathUi.playerAvoiderShowRadiusSync = createToggleRow(
                "Visualize Radius",
                pathUi.pathRowsPage,
                function()
                    return isPathPlayerAvoiderShowRadiusEnabled()
                end,
                function()
                    setPathPlayerAvoiderShowRadiusEnabled(not isPathPlayerAvoiderShowRadiusEnabled())
                    refreshPathManagerButtons()
                    updateAutofarmLayout()
                end
            )

            refreshPathPlayerAvoiderRowVisibility(true)
        end

        pathUi.serverHopStatusFrame = nextCustomOrder(Instance.new("Frame"))
        pathUi.serverHopStatusFrame.Name = "PathStatusFrame"
        pathUi.serverHopStatusFrame.Size = UDim2.new(1, 0, 0, 0)
        pathUi.serverHopStatusFrame.BackgroundTransparency = 1
        pathUi.serverHopStatusFrame.BorderSizePixel = 0
        pathUi.serverHopStatusFrame.Visible = false
        pathUi.serverHopStatusFrame.Parent = pathUi.pathRowsPage

        serverHopStatusLabel = Instance.new("TextLabel")
        serverHopStatusLabel.Size = UDim2.new(1, -4, 1, 0)
        serverHopStatusLabel.Position = UDim2.fromOffset(0, 0)
        serverHopStatusLabel.BackgroundTransparency = 1
        serverHopStatusLabel.Text = ""
        serverHopStatusLabel.TextColor3 = UI_COLORS.muted
        serverHopStatusLabel.Font = Enum.Font.Gotham
        serverHopStatusLabel.TextSize = 11
        serverHopStatusLabel.TextWrapped = true
        serverHopStatusLabel.TextXAlignment = Enum.TextXAlignment.Left
        serverHopStatusLabel.TextYAlignment = Enum.TextYAlignment.Top
        serverHopStatusLabel.Visible = false
        serverHopStatusLabel.Parent = pathUi.serverHopStatusFrame

        pathUi.pathSlotControls = {}
        pathUi.maxPathSlots = tonumber(MAX_PATH_PLAYBACK_SLOTS) or 4
        for slotIndex = 1, pathUi.maxPathSlots do
            pathUi.pathSlotControls[slotIndex] = createPathSlotSectionUi(pathUi.pathRowsPage, slotIndex, nextCustomOrder)
        end

        pathUi.addPathButton, _, pathUi.addPathLabel = createButtonRow("+", pathUi.pathRowsPage, function()
            if pathState.pathSectionCount >= (pathUi.maxPathSlots or tonumber(MAX_PATH_PLAYBACK_SLOTS) or 4) then
                return
            end
            setPathSectionCount(pathState.pathSectionCount + 1)
            savePathsToStorage(false)
            refreshPathManagerButtons()
            updateAutofarmLayout()
        end, {skipRegistry = true})

        pathUi.mainRowsLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(updateAutofarmLayout)
        pathUi.pathRowsLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(updateAutofarmLayout)

        _currentRegistryTab = previousRegistryTab
    end

    pathUi.editorNameBox.FocusLost:Connect(function(enterPressed)
        if enterPressed and pathUi.editorNameBox.Visible then
            saveWorkingPath(pathUi.editorNameBox.Text)
            refreshPathManagerButtons()
            refreshPathEditorButtons()
        end
    end)

    _UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType ~= Enum.UserInputType.Keyboard or input.KeyCode == Enum.KeyCode.Unknown then
            return
        end
        if pendingKeybindAction == "markore" then
            if input.KeyCode == Enum.KeyCode.Escape then
                pendingKeybindAction = nil
                refreshPathEditorButtons()
                setPathEditorStatus("Mark key capture cancelled.", UI_COLORS.muted)
                return
            end
            if input.KeyCode == guiToggleKeyCode then
                pendingKeybindAction = nil
                refreshPathEditorButtons()
                setPathEditorStatus("That key is already used by the GUI toggle.", UI_COLORS.warning)
                return
            end
            pathState.markOreKey = input.KeyCode
            pendingKeybindAction = nil
            savePathsToStorage(false)
            pushDebug("mark ore key: " .. formatKeyCodeName(pathState.markOreKey))
            refreshPathEditorButtons()
            return
        end
        if _UserInputService:GetFocusedTextBox() then
            return
        end
        if pathState.editorVisible and input.KeyCode == pathState.markOreKey then
            tryMarkHoveredOre()
            return
        end
        if gameProcessed then
            return
        end
    end)

    _UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed or not pathState.editorVisible then
            return
        end
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            local mouse = localPlayer:GetMouse()
            local target = mouse and mouse.Target
            local nodeIndex = target and target:GetAttribute("PathNodeIndex")
            local axisName = target and target:GetAttribute("PathNodeAxis")
            if nodeIndex then
                nodeIndex = tonumber(nodeIndex)
                if pathState.sellNodePickMode then
                    applyWorkingPathSellNodeSelection(nodeIndex)
                elseif pathState.deleteMode then
                    pathState.selectedNodeIndex = nil
                    pathState.draggingNodeIndex = nil
                    pathState.draggingNodeAxis = nil
                    pathState.dragLastPointerPosition = nil
                    truncateWorkingPathFromIndex(nodeIndex)
                    refreshPathVisuals()
                    refreshPathEditorButtons()
                    setPathEditorStatus("Deleted nodes from #" .. tostring(nodeIndex) .. " onward.", UI_COLORS.warning)
                else
                    pathState.selectedNodeIndex = nodeIndex
                    if not pathState.editNodePlacementEnabled then
                        pathState.draggingNodeIndex = nil
                        pathState.draggingNodeAxis = nil
                        pathState.dragLastPointerPosition = nil
                        refreshPathVisuals()
                        refreshPathEditorButtons()
                        setPathEditorStatus("Turn on Edit node placement to move nodes.", UI_COLORS.warning)
                        return
                    end
                    pathState.selectedNodeIndex = nodeIndex
                    pathState.draggingNodeIndex = nodeIndex
                    pathState.draggingNodeAxis = axisName and tostring(axisName) or "FREE"
                    pathState.dragLastPointerPosition = input.Position
                    refreshPathVisuals()
                    refreshPathEditorButtons()
                    if axisName then
                        setPathEditorStatus("Adjusting node #" .. tostring(nodeIndex) .. " on the " .. tostring(axisName) .. " axis.", UI_COLORS.accent)
                    else
                        setPathEditorStatus("Dragging node #" .. tostring(nodeIndex), UI_COLORS.accent)
                    end
                end
            elseif pathState.sellNodePickMode then
                setPathEditorStatus("Sell-node picker is on. Click one of the route nodes.", UI_COLORS.warning)
            elseif pathState.selectedNodeIndex then
                pathState.selectedNodeIndex = nil
                pathState.draggingNodeIndex = nil
                pathState.draggingNodeAxis = nil
                pathState.dragLastPointerPosition = nil
                refreshPathVisuals()
                refreshPathEditorButtons()
                setPathEditorStatus("Node edit cleared.", UI_COLORS.muted)
            end
        end
    end)

    _UserInputService.InputChanged:Connect(function(input)
        if not pathState.editNodePlacementEnabled then
            return
        end
        if pathState.draggingNodeIndex and input.UserInputType == Enum.UserInputType.MouseMovement then
            local nodePosition = getPathNodePosition(pathState.workingPath.nodes[pathState.draggingNodeIndex])
            if nodePosition then
                if pathState.draggingNodeAxis == "FREE" then
                    local movedPosition = getEditorMouseGroundPosition(cachedCharacter or getCustomCharacter())
                    if movedPosition then
                        setPathNodePosition(pathState.workingPath, pathState.draggingNodeIndex, movedPosition)
                    end
                elseif pathState.dragLastPointerPosition then
                    local delta = input.Position - pathState.dragLastPointerPosition
                    local sensitivity = 0.045
                    if pathState.draggingNodeAxis == "X" then
                        setPathNodePosition(pathState.workingPath, pathState.draggingNodeIndex, nodePosition + Vector3.new(delta.X * sensitivity, 0, 0))
                    elseif pathState.draggingNodeAxis == "Y" then
                        setPathNodePosition(pathState.workingPath, pathState.draggingNodeIndex, nodePosition + Vector3.new(0, -delta.Y * sensitivity, 0))
                    elseif pathState.draggingNodeAxis == "Z" then
                        setPathNodePosition(pathState.workingPath, pathState.draggingNodeIndex, nodePosition + Vector3.new(0, 0, -delta.Y * sensitivity))
                    end
                    pathState.dragLastPointerPosition = input.Position
                end
                pathState.sellNodeDirty = true
                recomputeWorkingPathSellNode()
                updatePathEditorActiveNodeVisuals()
                for index, part in pairs(pathUi.editorNodeParts) do
                    if part and part.Parent then
                        part.Color = index == pathState.selectedNodeIndex
                            and Color3.fromRGB(255, 214, 117)
                            or (index == pathState.sellNodeIndex and Color3.fromRGB(105, 220, 130) or Color3.fromRGB(223, 157, 82))
                    end
                end
            end
        end
    end)

    _UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            autoSellSliderDragging = false
            if pathState.draggingNodeIndex then
                pathState.draggingNodeIndex = nil
                pathState.draggingNodeAxis = nil
                pathState.dragLastPointerPosition = nil
                refreshPathVisuals()
                setPathEditorStatus("Node moved.", UI_COLORS.success)
            end
        end
    end)

    if localPlayer and localPlayer:FindFirstChild("PlayerGui") then
        localPlayer.PlayerGui.DescendantAdded:Connect(function(descendant)
            if descendant.Name == "InventoryUI" or descendant.Name == "ScrollingFrame" or descendant.Name == "SlotTemplate" or descendant == cachedInventorySlotsFrame then
                cachedInventorySlotsFrame = nil
                refreshAutoSellDisplay()
            end
        end)

        localPlayer.PlayerGui.DescendantRemoving:Connect(function(descendant)
            if descendant.Name == "InventoryUI" or descendant.Name == "ScrollingFrame" or descendant.Name == "SlotTemplate" or descendant == cachedInventorySlotsFrame then
                cachedInventorySlotsFrame = nil
            end
        end)
    end

    localPlayer.CharacterAdded:Connect(function(character)
        cachedCharacter = character
        cachedHumanoid = getHumanoid(character)
        cachedPickaxe = findPickaxe(character)
        cachedInventorySlotsFrame = nil
        task.defer(function()
            task.wait(0.35)
            if type(refreshPathDeathHopConnection) == "function" then
                refreshPathDeathHopConnection()
            end
        end)
        local shouldResumeSavedSpawn = false
        local pendingSpawnSlotIndex = normalizePathSlotIndex(pathState.spawnPlaybackPendingSlotIndex)
        if pathState.spawnPlaybackAwaitingRespawn and type(pathState.spawnPlaybackPendingPathName) == "string" and pendingSpawnSlotIndex then
            if getPathSlotSelection(pendingSpawnSlotIndex, false) == pathState.spawnPlaybackPendingPathName then
                shouldResumeSavedSpawn = markPathSpawnPlaybackReady(
                    pathState.spawnPlaybackPendingPathName,
                    pathState.spawnPlaybackPendingSpawnName,
                    pendingSpawnSlotIndex
                )
            else
                cancelPendingPathSpawnPlayback(true)
            end
        end
        if autoMineEnabled or hasPlayerStateFeatureHooksEnabled() or shouldResumeSavedSpawn or type(pathState.spawnPlaybackReadyPathName) == "string" then
            task.wait(1)
            if shouldResumeSavedSpawn or type(pathState.spawnPlaybackReadyPathName) == "string" then
                activeFarmMode = "path"
                local ok, started = xpcall(function()
                    return tryResumeReadyPathSpawnPlayback("respawn")
                end, debug.traceback)
                if not ok then
                    autoMineEnabled = false
                    activeFarmMode = nil
                    setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
                    pushDebug(started)
                elseif not started then
                    if not (autoMineEnabled and activeFarmMode == "path") then
                        autoMineEnabled = false
                        activeFarmMode = nil
                    end
                end
            elseif autoMineEnabled then
                startAutoMine()
            end
            if hasPlayerStateFeatureHooksEnabled() then
                ensurePlayerFeatureHooks()
                syncAllUi()
            end
        end
    end)

    task.spawn(function()
        while task.wait(1) do
            if type(refreshPathDeathHopConnection) == "function" and (isPathHopOnDeathEnabled() or pathDeathHopDiedConnection) then
                pcall(refreshPathDeathHopConnection)
            end
        end
    end)

    pcall(function()
        game:GetService("TeleportService").TeleportInitFailed:Connect(function(player, teleportResult, errorMessage)
            if player ~= localPlayer then
                return
            end
            serverHopInProgress = false
            setPathResumePlaybackPending(false, true)
            setServerHopStatus(
                "Hop failed: " .. tostring(errorMessage or teleportResult or "unknown"),
                Color3.fromRGB(255, 190, 120)
            )
            pushDebug("server hop teleport init failed: " .. tostring(errorMessage or teleportResult or "unknown"))
            refreshServerHopDisplay()
        end)
    end)

    loadPathsFromStorage()
    if serverHopEnabled then
        setServerHopStatus("After a path sell, it will try to hop into a smaller public server.", Color3.fromRGB(176, 167, 150))
    else
        setServerHopStatus("Off. Path sells stay in the current server.", Color3.fromRGB(176, 167, 150))
    end
    setTweenSpeed(tweenMoveSpeed)
    if type(refreshPathDeathHopConnection) == "function" then
        refreshPathDeathHopConnection()
    end
    setAutofarmSubTab("AutofarmSettings")
    setOreDropdownOpen(false)
    setMovementModeDropdownOpen(false)
    setPickupWhitelistDropdownOpen(false)
    updateAutofarmLayout()
    refreshButton()
    refreshTweenButton()
    refreshAuraButton()
    refreshAutoPickupButton()
    refreshAutoSellDisplay()
    refreshServerHopDisplay()
    refreshPickupWhitelistDisplay()
    refreshOreSelectionDisplay()
    refreshPathManagerButtons()
    refreshPathEditorButtons()
    refreshPathVisuals()
    pushDebug("automine tab loaded")

    if (pathState.alwaysPlayEnabled or pathState.farmPathsEnabled or pathState.resumePathPlaybackPending) and shouldUsePathPlayback() and not autoMineEnabled then
        activeFarmMode = "path"
        local ok, started = xpcall(function()
            return startSelectedPathPlayback("autoload")
        end, debug.traceback)
        if not ok then
            autoMineEnabled = false
            activeFarmMode = nil
            setStatus("Path start failed", Color3.fromRGB(255, 130, 130))
            pushDebug(started)
        elseif not started then
            autoMineEnabled = false
            activeFarmMode = nil
        end
    end

    task.spawn(function()
        while hubGui.Parent and not destroyed and isSessionActive() do
            local character = cachedCharacter or getCustomCharacter()
            local humanoid = cachedHumanoid or getHumanoid(character)
            local rootPart = getRootPart(character)
            local now = os.clock()
            local horizontalSpeed = 0
            local displacementSpeed = 0
            local anchored = "?"
            local stateName = "nil"
            local walkSpeed = 0
            local moveDirectionMagnitude = 0
            local targetDistance = -1
            local oreHealth = "nil"

            if rootPart then
                anchored = tostring(rootPart.Anchored)
                horizontalSpeed = math.floor(Vector3.new(rootPart.AssemblyLinearVelocity.X, 0, rootPart.AssemblyLinearVelocity.Z).Magnitude * 10 + 0.5) / 10
                if lastRootSamplePosition and lastRootSampleAt > 0 then
                    local dt = now - lastRootSampleAt
                    if dt > 0 then
                        displacementSpeed = math.floor(((rootPart.Position - lastRootSamplePosition).Magnitude / dt) * 10 + 0.5) / 10
                    end
                end
                lastRootSamplePosition = rootPart.Position
                lastRootSampleAt = now
            else
                lastRootSamplePosition = nil
                lastRootSampleAt = 0
            end

            if humanoid then
                walkSpeed = humanoid.WalkSpeed
                moveDirectionMagnitude = math.floor(humanoid.MoveDirection.Magnitude * 100 + 0.5) / 100
                local ok, state = pcall(function()
                    return humanoid:GetState()
                end)
                if ok and state then
                    stateName = state.Name
                end
            end

            if character and currentTargetOre then
                targetDistance = math.floor(getDistanceToOre(character, currentTargetOre) * 10 + 0.5) / 10
                oreHealth = formatOreHealthText(currentTargetOre, false)
            end

            if movementDebugLabel then
                pcall(function()
                    movementDebugLabel.Text = table.concat({
                        string.format("mode=%s aura=%s pickup=%s path=%s range=%.0f sprint=%s infstam=%s safetp=%s ts=%.0f", getMovementModeLabel(), tostring(mineAuraEnabled), tostring(autoPickupEnabled), tostring(pathState.playbackActive), mineAuraEnabled and MINE_AURA_RANGE or MINE_RANGE, tostring(movementKeyState[Enum.KeyCode.LeftShift]), tostring(playerState.infiniteStaminaEnabled), tostring(playerState.safeTeleportEnabled), tweenMoveSpeed),
                        string.format("state=%s anchored=%s", stateName, anchored),
                        string.format("walkspeed=%.2f movedir=%.2f", walkSpeed, moveDirectionMagnitude),
                        string.format("vel=%.1f studs/s delta=%.1f", horizontalSpeed, displacementSpeed),
                        string.format("cmd=%s %.2f, %.2f, %.2f", lastMoveCommandSource, lastMoveCommand.X, lastMoveCommand.Y, lastMoveCommand.Z),
                        string.format("targetDist=%s oreHp=%s", targetDistance >= 0 and tostring(targetDistance) or "nil", oreHealth),
                    }, "\n")
                end)
            end

            if pathState.recording and pathState.editorVisible and character then
                local currentRoot = getRootPart(character)
                if currentRoot and (not pathState.lastRecordedNodePosition or (currentRoot.Position - pathState.lastRecordedNodePosition).Magnitude >= PATH_RECORD_NODE_SPACING) then
                    addWorkingPathNode(currentRoot.Position)
                end
            end

            refreshAutoSellDisplay()
            refreshPathEditorButtons()
            task.wait(0.1)
        end
    end)
end

function initWeaponsGunsSection(elementsList)
    local gunList, gunHeaderLabel = createCollapsibleSection("Guns", elementsList)
    gunHeaderLabel.RichText = true
    gunHeaderLabel.Text = '<font color="#d9d9d9">Guns</font>'

    local silentAimRow, _, silentAimSync = createToggleRow("Silent aim", gunList, function()
        return S.silentAimEnabled
    end, function()
        setSilentAimEnabled(not S.silentAimEnabled)
    end)

    local bodyTargetRow, bodyTargetSync = createDropdownRow("Body target", gunList, {"Head", "Torso"}, function()
        return S.silentAimBodyTarget
    end, function(value)
        setSilentAimBodyTarget(value)
    end)

    local aimModeRow, aimModeSync = createDropdownRow("Aim Mode", gunList, GUN_AIM_MODE_ORDER, function()
        return S.silentAimMode
    end, function(value)
        setSilentAimMode(value)
    end)

    local targetPriorityRow, targetPrioritySync = createMultiSelectDropdownRow("Target Priority", gunList, GUN_TARGET_PRIORITY_ORDER, function()
        return cloneGunTargetPriority(S.silentAimTargetPriority)
    end, function(priorityName)
        toggleGunTargetPriority(priorityName)
        schedulePersist()
    end, getGunTargetPriorityLabel)

    local visibilityCheckRow, _, visibilityCheckSync = createToggleRow("Visibility Check", gunList, function()
        return S.silentAimVisibilityCheck
    end, function()
        setSilentAimVisibilityCheck(not S.silentAimVisibilityCheck)
    end)

    local hitAccuracyRow = createSliderRow("Hit accuracy", gunList, S.silentAimAccuracy, function(value)
        setSilentAimAccuracy(value)
    end, function()
        return S.silentAimAccuracy
    end, 1, 100)

    local distanceLimitRow = createSliderRow("Distance limit", gunList, S.silentAimDistanceLimit, function(value)
        setSilentAimDistanceLimit(value)
    end, function()
        return S.silentAimDistanceLimit
    end, 10, 20000)

    local fovRow, _, fovSync = createToggleRow("Fov", gunList, function()
        return S.gunFovEnabled
    end, function()
        setGunFovEnabled(not S.gunFovEnabled)
    end)

    local snaplineRow, _, snaplineSync = createToggleRow("Snapline", gunList, function()
        return S.gunSnaplineEnabled
    end, function()
        setGunSnaplineEnabled(not S.gunSnaplineEnabled)
    end)

    local fovShapeRow, fovShapeSync = createDropdownRow("FOV Shape", gunList, GUN_FOV_SHAPE_ORDER, function()
        return S.gunFovShape
    end, function(value)
        setGunFovShape(value)
    end)

    local fovRadiusRow = createSliderRow("FOV Radius", gunList, S.gunFovRadius, function(value)
        setGunFovRadius(value)
    end, function()
        return S.gunFovRadius
    end, 10, 2000)

    createToggleRow("Fast projectile", gunList, function()
        return S.fastProjectileEnabled
    end, function()
        setFastProjectileEnabled(not S.fastProjectileEnabled)
    end)

    createToggleRow("Wall bang", gunList, function()
        return S.wallBangEnabled
    end, function()
        setWallBangEnabled(not S.wallBangEnabled)
    end)

    createToggleRow("No spread", gunList, function()
        return S.noSpreadEnabled
    end, function()
        setNoSpreadEnabled(not S.noSpreadEnabled)
    end)

    createToggleRow("No recoil", gunList, function()
        return S.noRecoilEnabled
    end, function()
        setNoRecoilEnabled(not S.noRecoilEnabled)
    end)

    createToggleRow("No fire cooldown", gunList, function()
        return S.noFireCooldownEnabled
    end, function()
        setNoFireCooldownEnabled(not S.noFireCooldownEnabled)
    end)

    createToggleRow("Fast reload", gunList, function()
        return S.fastReloadEnabled
    end, function()
        setFastReloadEnabled(not S.fastReloadEnabled)
    end)

    local gunRowsInitialized = false

    local function syncGunRows()
        local showSilentAimRows = S.silentAimEnabled == true
        local showFovExtras = showSilentAimRows and S.gunFovEnabled == true
        local showFovRadius = showSilentAimRows and (S.gunFovEnabled == true or tostring(S.silentAimMode or "FOV") == "FOV")
        setAnimatedRowVisible(bodyTargetRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(aimModeRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(targetPriorityRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(visibilityCheckRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(hitAccuracyRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(distanceLimitRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(fovRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(snaplineRow, showSilentAimRows, 38, not gunRowsInitialized)
        setAnimatedRowVisible(fovShapeRow, showFovExtras, 38, not gunRowsInitialized)
        setAnimatedRowVisible(fovRadiusRow, showFovRadius, 38, not gunRowsInitialized)
        pcall(silentAimSync)
        pcall(bodyTargetSync)
        pcall(aimModeSync)
        pcall(targetPrioritySync)
        pcall(visibilityCheckSync)
        pcall(fovSync)
        pcall(snaplineSync)
        pcall(fovShapeSync)
        gunRowsInitialized = true
    end

    table.insert(_allUiSyncs, syncGunRows)
    syncGunRows()
end

function initWeaponsLassoSection(elementsList)
    local lassoList, lassoHeaderLabel = createCollapsibleSection("Lasso", elementsList)
    lassoHeaderLabel.RichText = true
    lassoHeaderLabel.Text = '<font color="#d9d9d9">Lasso</font>'

    local silentAimRow, _, silentAimSync = createToggleRow("Silent aim", lassoList, function()
        return S.lassoSilentAimEnabled
    end, function()
        setLassoSilentAimEnabled(not S.lassoSilentAimEnabled)
    end)

    local snaplineRow, _, snaplineSync = createToggleRow("Snapline", lassoList, function()
        return S.lassoSnaplineEnabled
    end, function()
        setLassoSnaplineEnabled(not S.lassoSnaplineEnabled)
    end)

    createToggleRow("No rope limit", lassoList, function()
        return S.lassoNoRopeLimitEnabled
    end, function()
        setLassoNoRopeLimitEnabled(not S.lassoNoRopeLimitEnabled)
    end)

    createToggleRow("Faster lasso", lassoList, function()
        return S.fasterLassoEnabled
    end, function()
        setFasterLassoEnabled(not S.fasterLassoEnabled)
    end)

    createToggleRow("Instant charge", lassoList, function()
        return S.instantLassoChargeEnabled
    end, function()
        setInstantLassoChargeEnabled(not S.instantLassoChargeEnabled)
    end)

    local lassoRowsInitialized = false

    local function syncLassoRows()
        local showSnapline = S.lassoSilentAimEnabled == true
        setAnimatedRowVisible(snaplineRow, showSnapline, 38, not lassoRowsInitialized)
        pcall(silentAimSync)
        pcall(snaplineSync)
        lassoRowsInitialized = true
    end

    table.insert(_allUiSyncs, syncLassoRows)
    syncLassoRows()
end

weaponsBtn, weaponsIndicator, weaponsLabel = createTabButton("WEAPONS", 50)
weaponsContent = createContentArea()
tabButtons["WEAPONS"] = {btn = weaponsBtn, indicator = weaponsIndicator, label = weaponsLabel}
tabContents["WEAPONS"] = weaponsContent
createTabBackground(weaponsContent)
do
    local title, elementsList = createTabLayout(weaponsContent)
    tabTitles["WEAPONS"] = title
    _currentRegistryTab = "WEAPONS"

    local dynamiteList, dynamiteHeaderLabel = createCollapsibleSection("Dynamite misc", elementsList)
    dynamiteHeaderLabel.RichText = true
    dynamiteHeaderLabel.Text = '<font color="#ffb64a">Dynamite misc</font>'

    local tpDynamiteRow, _, tpDynamiteSync = createToggleRow("TP Dynamite", dynamiteList, function()
        return S.dynamiteTpEnabled
    end, function()
        S.dynamiteTpEnabled = not S.dynamiteTpEnabled
        schedulePersist()
        spawnNotification("TP Dynamite " .. (S.dynamiteTpEnabled and "enabled." or "disabled."), S.dynamiteTpEnabled and "success" or "warning", true)
    end)

    local tpStyleRow, tpStyleSync = createDropdownRow("TP Style", dynamiteList, {"Blatant", "Silent"}, function()
        return S.dynamiteStyle
    end, function(value)
        S.dynamiteStyle = tostring(value or "Blatant")
        schedulePersist()
        spawnNotification("Dynamite style set to " .. S.dynamiteStyle .. ".", "info", true)
    end)

    local tpTargetRow, tpTargetSync = createDropdownRow("TP Target", dynamiteList, {"Vault", "Tunnel", "Player"}, function()
        return S.dynamiteTarget
    end, function(value)
        S.dynamiteTarget = tostring(value or "Vault")
        ensureValidDynamiteSelectedPlayer()
        if S.dynamiteTarget ~= "Player" then
            clearAllDynamiteMarkedPlayers()
            refreshDynamiteMarkVisuals()
            restoreDynamiteSpectateCamera()
        end
        schedulePersist()
        spawnNotification("Dynamite target set to " .. S.dynamiteTarget .. ".", "info", true)
    end)

    local spectateRow, _, spectateSync = createToggleRow("Spectate", dynamiteList, function()
        return S.dynamiteSpectateEnabled
    end, function()
        S.dynamiteSpectateEnabled = not S.dynamiteSpectateEnabled
        if S.dynamiteSpectateEnabled then
            updateDynamiteSpectateCamera()
        else
            restoreDynamiteSpectateCamera()
        end
        schedulePersist()
        spawnNotification("Dynamite spectate " .. (S.dynamiteSpectateEnabled and "enabled." or "disabled."), S.dynamiteSpectateEnabled and "success" or "warning", true)
    end)

    local selectPlayerRow, selectPlayerSync = createDropdownRow("Select Player", dynamiteList, function()
        return getDynamitePlayerOptions()
    end, function()
        return ensureValidDynamiteSelectedPlayer()
    end, function(value)
        S.dynamiteSelectedPlayer = tostring(value or "No Players")
        ensureValidDynamiteSelectedPlayer()
        schedulePersist()
        spawnNotification("Dynamite player target set to " .. tostring(S.dynamiteSelectedPlayer) .. ".", "info", true)
    end)
    local markPlayersRow, markPlayersSync = createToggleKeybindRow("Mark Players", dynamiteList, Color3.fromRGB(255, 85, 85), function()
        return S.dynamiteMarkPlayersEnabled
    end, function()
        S.dynamiteMarkPlayersEnabled = not S.dynamiteMarkPlayersEnabled
        if S.dynamiteMarkPlayersEnabled then
            spawnNotification("Hover your mouse and press " .. formatKeyCodeName(S.keybinds.dynamiteMark or Enum.KeyCode.F) .. " over a player to mark them for the next time you throw a dynamite.", "info", true)
        else
            clearAllDynamiteMarkedPlayers()
            refreshDynamiteMarkVisuals()
            spawnNotification("Dynamite player marking disabled.", "warning", true)
        end
        schedulePersist()
    end, function()
        return formatKeyCodeName(S.keybinds.dynamiteMark or Enum.KeyCode.F)
    end, function(keyCode)
        S.keybinds.dynamiteMark = keyCode or Enum.KeyCode.F
        schedulePersist()
        spawnNotification("Dynamite mark key set to " .. formatKeyCodeName(S.keybinds.dynamiteMark) .. ".", "success", true)
    end, "Press a key. Esc cancels, Delete resets to F.")

    local dynamiteRowsInitialized = false

    local function syncDynamiteRows()
        local isPlayerTarget = S.dynamiteTarget == "Player"
        ensureValidDynamiteSelectedPlayer()
        setAnimatedRowVisible(spectateRow, isPlayerTarget, 38, not dynamiteRowsInitialized)
        setAnimatedRowVisible(selectPlayerRow, isPlayerTarget, 38, not dynamiteRowsInitialized)
        setAnimatedRowVisible(markPlayersRow, isPlayerTarget, 38, not dynamiteRowsInitialized)
        pcall(tpDynamiteSync)
        pcall(tpStyleSync)
        pcall(tpTargetSync)
        pcall(spectateSync)
        pcall(selectPlayerSync)
        pcall(markPlayersSync)
        dynamiteRowsInitialized = true
    end

    table.insert(_allUiSyncs, syncDynamiteRows)
    syncDynamiteRows()

    initWeaponsGunsSection(elementsList)
    initWeaponsLassoSection(elementsList)

    _currentRegistryTab = nil
end
playerBtn, playerIndicator, playerLabel = createTabButton("PLAYER", 90)
playerContent = createContentArea()
tabButtons["PLAYER"] = {btn = playerBtn, indicator = playerIndicator, label = playerLabel}
tabContents["PLAYER"] = playerContent
createTabBackground(playerContent)
do
    local title, elementsList = createTabLayout(playerContent)
    tabTitles["PLAYER"] = title
    _currentRegistryTab = "PLAYER"

    local playerToggleRow, _, infiniteStaminaSync = createToggleRow("Infinite Stamina", elementsList, function()
        return playerState.infiniteStaminaEnabled
    end, function()
        setInfiniteStaminaEnabled(not playerState.infiniteStaminaEnabled)
    end)
    playerUi.infiniteStaminaButton = playerToggleRow
    playerUi.infiniteStaminaSync = infiniteStaminaSync

    createToggleRow("Infinite Jump", elementsList, function()
        return playerState.noJumpCooldownEnabled
    end, function()
        setNoJumpCooldownEnabled(not playerState.noJumpCooldownEnabled)
    end)

    createToggleRow("No Fall Damage", elementsList, function()
        return playerState.noFallDamageEnabled
    end, function()
        setNoFallDamageEnabled(not playerState.noFallDamageEnabled)
    end)

    createToggleRow("No Ragdoll", elementsList, function()
        return playerState.noRagdollEnabled
    end, function()
        setNoRagdollEnabled(not playerState.noRagdollEnabled)
    end)

    createToggleRow("Auto Break Lasso", elementsList, function()
        return playerState.autoBreakFreeEnabled
    end, function()
        setAutoBreakFreeEnabled(not playerState.autoBreakFreeEnabled)
    end)

    createToggleRow("Auto Get Up", elementsList, function()
        return playerState.autoGetUpEnabled
    end, function()
        setAutoGetUpEnabled(not playerState.autoGetUpEnabled)
    end)

    createToggleRow("Equip while swimming", elementsList, function()
        return playerState.equipWhileSwimmingEnabled
    end, function()
        setEquipWhileSwimmingEnabled(not playerState.equipWhileSwimmingEnabled)
    end)

    _currentRegistryTab = nil
end

visualsBtn, visualsIndicator, visualsLabel = createTabButton("VISUALS", 130)
visualsContent = createContentArea()
tabButtons["VISUALS"] = {btn = visualsBtn, indicator = visualsIndicator, label = visualsLabel}
tabContents["VISUALS"] = visualsContent
createTabBackground(visualsContent)
do
    local title, elementsList = createTabLayout(visualsContent)
    tabTitles["VISUALS"] = title
    _currentRegistryTab = "VISUALS"

    local playerEspRow, _, playerEspSync = createToggleRow("Player ESP", elementsList, function()
        return S.playerEspEnabled
    end, function()
        local nextValue = not S.playerEspEnabled
        setPlayerEspEnabled(nextValue)
        spawnNotification("Player ESP " .. (nextValue and "enabled." or "disabled."), nextValue and "success" or "warning", true)
    end)

    local espMethodRow, espMethodSync = createMultiSelectDropdownRow("ESP Method", elementsList, PLAYER_ESP_METHOD_ORDER, function()
        return clonePlayerEspMethods(S.playerEspMethods)
    end, function(methodName)
        togglePlayerEspMethod(methodName)
        schedulePersist()
        if S.playerEspEnabled then
            ensurePlayerEspRenderLoop()
        end
    end, getPlayerEspMethodLabel)

    local visualRowsInitialized = false

    local function syncVisualRows()
        setAnimatedRowVisible(espMethodRow, S.playerEspEnabled, 38, not visualRowsInitialized)
        pcall(playerEspSync)
        pcall(espMethodSync)
        visualRowsInitialized = true
    end

    table.insert(_allUiSyncs, syncVisualRows)
    syncVisualRows()

    _currentRegistryTab = nil
end

gameBtn, gameIndicator, gameLabel = createTabButton("GAME", 170)
gameContent = createContentArea()
tabButtons["GAME"] = {btn = gameBtn, indicator = gameIndicator, label = gameLabel}
tabContents["GAME"] = gameContent
createTabBackground(gameContent)
do
    local title, elementsList = createTabLayout(gameContent)
    tabTitles["GAME"] = title
    _currentRegistryTab = "GAME"

    local safeTeleportRow, _, safeTeleportSync = createToggleRow("Safe Teleport", elementsList, function()
        return playerState.safeTeleportEnabled
    end, function()
        setSafeTeleportEnabled(not playerState.safeTeleportEnabled)
    end)
    playerUi.safeTeleportButton = safeTeleportRow
    playerUi.safeTeleportSync = safeTeleportSync

    createToggleRow("Notify if Staff Present", elementsList, function()
        return S.staffNotifyEnabled
    end, function()
        S.staffNotifyEnabled = not S.staffNotifyEnabled
        schedulePersist()
        if S.staffNotifyEnabled then
            spawnNotification("Staff alerts enabled.", "success", true)
            scanCurrentTwwStaff("notify")
        else
            spawnNotification("Staff alerts disabled.", "warning", true)
        end
    end)

    createToggleRow("Kick if Staff Joins", elementsList, function()
        return S.staffKickEnabled
    end, function()
        S.staffKickEnabled = not S.staffKickEnabled
        schedulePersist()
        if S.staffKickEnabled then
            spawnNotification("Auto-kick on staff detection enabled.", "warning", true)
            scanCurrentTwwStaff("kick")
        else
            spawnNotification("Auto-kick on staff detection disabled.", "info", true)
        end
    end)

    _currentRegistryTab = nil
end

settingsBtn, settingsIndicator, settingsLabel = createTabButton("SETTINGS", 210)
settingsContent = createContentArea()
tabButtons["SETTINGS"] = {btn = settingsBtn, indicator = settingsIndicator, label = settingsLabel}
tabContents["SETTINGS"] = settingsContent
createTabBackground(settingsContent)
do
    local title, elementsList = createTabLayout(settingsContent)
    tabTitles["SETTINGS"] = title
    _currentRegistryTab = "SETTINGS"

    createKeyCaptureRow(
        "Keybind for hiding GUI",
        elementsList,
        Color3.fromRGB(0, 150, 255),
        function()
            return guiToggleKeyName
        end,
        function(keyCode)
            if keyCode then
                guiToggleKeyCode = keyCode
                guiToggleKeyName = keyCode.Name
                spawnNotification("GUI toggle key set to " .. guiToggleKeyName .. ".", "success", true)
            end
            schedulePersist()
            refreshDashboard()
        end,
        "Press a key. Esc cancels."
    )

    createKeyCaptureRow(
        "Panic Button",
        elementsList,
        Color3.fromRGB(255, 60, 60),
        function()
            return panicKeyName
        end,
        function(keyCode)
            if keyCode then
                panicKeyCode = keyCode
                panicKeyName = keyCode.Name
                spawnNotification("Panic key set to " .. panicKeyName .. ".", "warning", true)
            else
                panicKeyCode = nil
                panicKeyName = "none"
                spawnNotification("Panic key cleared.", "info", true)
            end
            schedulePersist()
            refreshDashboard()
        end,
        "Press a key. Esc cancels, Delete clears."
    )

    createSliderRow("Transparency", elementsList, S.guiTransparency, function(value)
        S.guiTransparency = value
        hubMain.GroupTransparency = value / 100
        schedulePersist()
        refreshDashboard()
    end, function()
        return S.guiTransparency
    end, 0, 100)

    createToggleRow("Notifications", elementsList, function()
        return S.notificationsEnabled
    end, function()
        S.notificationsEnabled = not S.notificationsEnabled
        schedulePersist()
        refreshDashboard()
        if S.notificationsEnabled then
            spawnNotification("Notifications enabled.", "success", true)
        end
    end)

    settingsUi.discordAutomineWebhookRow, _, settingsUi.discordAutomineWebhookSync = createToggleRow(
        "Discord automine webhook tracking",
        elementsList,
        function()
            return isDiscordAutomineWebhookTrackingEnabled()
        end,
        function()
            if setDiscordAutomineWebhookTrackingEnabled(not isDiscordAutomineWebhookTrackingEnabled()) then
                refreshDiscordAutomineWebhookRowVisibility(false)
                refreshDashboard()
            end
        end
    )

    settingsUi.discordAutomineWebhookUrlRow = createInputRow(
        "Discord webhook",
        elementsList,
        function()
            return getDiscordAutomineWebhookUrl()
        end,
        function(value)
            setDiscordAutomineWebhookUrl(value)
            refreshDiscordAutomineWebhookRowVisibility(false)
            pcall(syncAllUi)
        end,
        "https://discord.com/api/webhooks/..."
    )

    settingsUi.discordAutomineWebhookOptionsRow, settingsUi.discordAutomineWebhookOptionsSync = createMultiSelectDropdownRow(
        "Webhook tracking",
        elementsList,
        AUTOMINE_WEBHOOK_TRACK_OPTION_ORDER,
        function()
            return getDiscordAutomineWebhookOptions()
        end,
        function(optionName)
            toggleDiscordAutomineWebhookTrackOption(optionName)
        end,
        getDiscordAutomineWebhookTrackingLabel
    )

    local function syncDiscordWebhookSettingsRows()
        if settingsUi.discordAutomineWebhookSync then
            settingsUi.discordAutomineWebhookSync()
        end
        if settingsUi.discordAutomineWebhookOptionsSync then
            settingsUi.discordAutomineWebhookOptionsSync()
        end
        refreshDiscordAutomineWebhookRowVisibility(true)
    end

    table.insert(_allUiSyncs, syncDiscordWebhookSettingsRows)
    syncDiscordWebhookSettingsRows()

    initConfigSettingsSection(elementsList)

    _currentRegistryTab = nil
end

keybindsBtn, keybindsIndicator, keybindsLabel = createTabButton("KEYBINDS", 250)
keybindsContent = createContentArea()
tabButtons["KEYBINDS"] = {btn = keybindsBtn, indicator = keybindsIndicator, label = keybindsLabel}
tabContents["KEYBINDS"] = keybindsContent
createTabBackground(keybindsContent)
do
    local title, elementsList = createTabLayout(keybindsContent)
    tabTitles["KEYBINDS"] = title

    local keybindListening = false
    local keybindListeningBtn = nil
    local keybindListenConn = nil
    local keybindRows = {}

    local function getKeyDisplayName(keyCode)
        if not keyCode then
            return "none"
        end
        return keyCode.Name
    end

    local function stopKeybindListening()
        if not keybindListening then
            return
        end
        keybindListening = false
        endKeyCapture()
        if keybindListenConn then
            keybindListenConn:Disconnect()
            keybindListenConn = nil
        end
        if keybindListeningBtn then
            local rowData
            for _, data in pairs(keybindRows) do
                if data.keybindBtn == keybindListeningBtn then
                    rowData = data
                    break
                end
            end
            if rowData then
                rowData.keybindLabel.Text = getKeyDisplayName(S.keybinds[rowData.registryName])
                rowData.keybindBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
                rowData.keybindLabel.TextColor3 = Color3.new(1, 1, 1)
            end
            keybindListeningBtn = nil
        end
    end

    local searchBarBg = Instance.new("Frame")
    searchBarBg.Size = UDim2.new(1, 0, 0, 32)
    searchBarBg.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    searchBarBg.BorderSizePixel = 1
    searchBarBg.BorderColor3 = Color3.fromRGB(50, 50, 50)
    searchBarBg.LayoutOrder = 0
    searchBarBg.Parent = elementsList
    Instance.new("UICorner", searchBarBg).CornerRadius = UDim.new(0, 6)

    local searchBox = Instance.new("TextBox")
    searchBox.Size = UDim2.new(1, -20, 1, 0)
    searchBox.Position = UDim2.new(0, 10, 0, 0)
    searchBox.BackgroundTransparency = 1
    searchBox.TextColor3 = Color3.new(1, 1, 1)
    searchBox.PlaceholderText = "Search keybinds..."
    searchBox.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
    searchBox.Text = ""
    searchBox.Font = Enum.Font.GothamMedium
    searchBox.TextSize = 13
    searchBox.TextXAlignment = Enum.TextXAlignment.Left
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = searchBarBg

    local kbLayoutOrder = 0
    local function nextKbOrder()
        kbLayoutOrder = kbLayoutOrder + 1
        return kbLayoutOrder
    end

    local function createKeybindSectionHeader(text)
        local header = Instance.new("TextLabel")
        header.Size = UDim2.new(1, 0, 0, 28)
        header.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
        header.BorderSizePixel = 0
        header.TextColor3 = Color3.fromRGB(0, 150, 255)
        header.Text = "  " .. text
        header.Font = Enum.Font.GothamBold
        header.TextSize = 13
        header.TextXAlignment = Enum.TextXAlignment.Left
        header.LayoutOrder = nextKbOrder()
        header.Parent = elementsList
        Instance.new("UICorner", header).CornerRadius = UDim.new(0, 4)
        return header
    end

    local function createKeybindRow(regEntry)
        local rowName = regEntry.name
        local rowKey = regEntry.tab .. ":" .. regEntry.name

        local row = Instance.new("TextButton")
        row.Name = "KB_" .. rowKey
        row.Size = UDim2.new(1, 0, 0, 38)
        row.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
        row.BorderSizePixel = 0
        row.Text = ""
        row.AutoButtonColor = false
        row.LayoutOrder = nextKbOrder()
        row.Parent = elementsList

        local diamond = Instance.new("Frame")
        diamond.Size = UDim2.new(0, 6, 0, 6)
        diamond.Position = UDim2.new(0, 10, 0.5, 0)
        diamond.AnchorPoint = Vector2.new(0.5, 0.5)
        diamond.Rotation = 45
        diamond.BackgroundColor3 = Color3.new(1, 1, 1)
        diamond.BorderSizePixel = 0
        diamond.Visible = false
        diamond.Parent = row

        local icon = Instance.new("Frame")
        icon.Size = UDim2.new(0, 14, 0, 14)
        icon.Position = UDim2.new(0, 24, 0.5, -7)
        icon.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
        icon.BorderSizePixel = 0
        icon.Parent = row
        Instance.new("UICorner", icon).CornerRadius = UDim.new(0, 2)

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(0, 180, 1, 0)
        label.Position = UDim2.new(0, 50, 0, 0)
        label.BackgroundTransparency = 1
        label.TextColor3 = Color3.new(0.8, 0.8, 0.8)
        label.Text = rowName
        label.Font = Enum.Font.GothamMedium
        label.TextSize = 13
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextTruncate = Enum.TextTruncate.AtEnd
        label.Parent = row

        local topLine = Instance.new("Frame")
        topLine.Size = UDim2.new(0, 0, 0, 2)
        topLine.BackgroundColor3 = Color3.new(1, 1, 1)
        topLine.BorderSizePixel = 0
        topLine.Parent = row

        local bottomLine = Instance.new("Frame")
        bottomLine.Size = UDim2.new(0, 0, 0, 2)
        bottomLine.Position = UDim2.new(0, 0, 1, -2)
        bottomLine.BackgroundColor3 = Color3.new(1, 1, 1)
        bottomLine.BorderSizePixel = 0
        bottomLine.Parent = row

        local resetBg = Instance.new("TextButton")
        resetBg.Size = UDim2.new(0, 45, 0, 22)
        resetBg.Position = UDim2.new(1, -55, 0.5, -11)
        resetBg.BackgroundColor3 = Color3.fromRGB(50, 30, 30)
        resetBg.BorderSizePixel = 0
        resetBg.Text = "Reset"
        resetBg.TextColor3 = Color3.fromRGB(255, 120, 120)
        resetBg.Font = Enum.Font.GothamMedium
        resetBg.TextSize = 10
        resetBg.AutoButtonColor = false
        resetBg.ZIndex = 3
        resetBg.Parent = row
        Instance.new("UICorner", resetBg).CornerRadius = UDim.new(0, 4)
        _UTILS._applySoundsToButton(resetBg)

        local keybindBg = Instance.new("Frame")
        keybindBg.Size = UDim2.new(0, 75, 0, 22)
        keybindBg.Position = UDim2.new(1, -140, 0.5, -11)
        keybindBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        keybindBg.BorderSizePixel = 1
        keybindBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
        keybindBg.Parent = row
        Instance.new("UICorner", keybindBg).CornerRadius = UDim.new(0, 4)

        local keybindLabel = Instance.new("TextLabel")
        keybindLabel.Size = UDim2.new(1, 0, 1, 0)
        keybindLabel.BackgroundTransparency = 1
        keybindLabel.TextColor3 = Color3.new(1, 1, 1)
        keybindLabel.Text = getKeyDisplayName(S.keybinds[rowKey])
        keybindLabel.Font = Enum.Font.GothamMedium
        keybindLabel.TextSize = 11
        keybindLabel.Parent = keybindBg

        local keybindBtn = Instance.new("TextButton")
        keybindBtn.Size = UDim2.new(1, 0, 1, 0)
        keybindBtn.BackgroundTransparency = 1
        keybindBtn.Text = ""
        keybindBtn.ZIndex = 3
        keybindBtn.Parent = keybindBg
        _UTILS._applySoundsToButton(keybindBtn)

        row.MouseEnter:Connect(function()
            if destroyed then
                return
            end
            _TweenService:Create(row, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 35, 35)}):Play()
            _TweenService:Create(icon, TweenInfo.new(0.2), {BackgroundColor3 = Color3.new(1, 1, 1)}):Play()
            _TweenService:Create(label, TweenInfo.new(0.2), {TextColor3 = Color3.new(1, 1, 1)}):Play()
            local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
            _TweenService:Create(topLine, tweenInfo, {Size = UDim2.new(1, 0, 0, 2)}):Play()
            _TweenService:Create(bottomLine, tweenInfo, {Size = UDim2.new(1, 0, 0, 2)}):Play()
            diamond.Visible = true
        end)

        row.MouseLeave:Connect(function()
            if destroyed then
                return
            end
            _TweenService:Create(row, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(24, 24, 24)}):Play()
            _TweenService:Create(icon, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 150, 255)}):Play()
            _TweenService:Create(label, TweenInfo.new(0.2), {TextColor3 = Color3.new(0.8, 0.8, 0.8)}):Play()
            local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
            _TweenService:Create(topLine, tweenInfo, {Size = UDim2.new(0, 0, 0, 2)}):Play()
            _TweenService:Create(bottomLine, tweenInfo, {Size = UDim2.new(0, 0, 0, 2)}):Play()
            diamond.Visible = false
        end)

        resetBg.MouseEnter:Connect(function()
            _TweenService:Create(resetBg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(80, 30, 30)}):Play()
        end)

        resetBg.MouseLeave:Connect(function()
            _TweenService:Create(resetBg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 30, 30)}):Play()
        end)

        local rowData = {
            registryName = rowKey,
            displayName = rowName,
            row = row,
            keybindLabel = keybindLabel,
            keybindBtn = keybindBtn,
            keybindBg = keybindBg,
            resetBtn = resetBg,
            regEntry = regEntry,
        }
        keybindRows[rowKey] = rowData

        keybindBtn.MouseButton1Click:Connect(function()
            if destroyed then
                return
            end
            _UTILS._createParticleBurst(keybindBg)
            if keybindListening and keybindListeningBtn == keybindBtn then
                stopKeybindListening()
                return
            end
            if keybindListening then
                stopKeybindListening()
            end
            keybindListening = true
            beginKeyCapture()
            keybindListeningBtn = keybindBtn
            keybindLabel.Text = ". . ."
            keybindBg.BorderColor3 = Color3.fromRGB(0, 150, 255)
            keybindLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
            spawnNotification("Press Esc or Backspace to cancel.", "info", true)

            keybindListenConn = _UserInputService.InputBegan:Connect(function(input, gameProcessed)
                if destroyed or not keybindListening or gameProcessed then
                    return
                end
                if input.UserInputType ~= Enum.UserInputType.Keyboard then
                    return
                end
                if input.KeyCode == Enum.KeyCode.Escape or input.KeyCode == Enum.KeyCode.Backspace then
                    stopKeybindListening()
                    return
                end
                if input.KeyCode ~= Enum.KeyCode.Unknown then
                    S.keybinds[rowKey] = input.KeyCode
                    keybindLabel.Text = input.KeyCode.Name
                    keybindBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
                    keybindLabel.TextColor3 = Color3.new(1, 1, 1)
                    keybindListening = false
                    endKeyCapture()
                    if keybindListenConn then
                        keybindListenConn:Disconnect()
                        keybindListenConn = nil
                    end
                    keybindListeningBtn = nil
                    schedulePersist()
                    refreshDashboard()
                    spawnNotification(rowName .. " has been keybound to " .. input.KeyCode.Name .. ".", "success", true)
                end
            end)
        end)

        resetBg.MouseButton1Click:Connect(function()
            if destroyed then
                return
            end
            _UTILS._createParticleBurst(resetBg)
            if keybindListening and keybindListeningBtn == keybindBtn then
                stopKeybindListening()
            end
            S.keybinds[rowKey] = nil
            keybindLabel.Text = "none"
            keybindBg.BorderColor3 = Color3.fromRGB(60, 60, 60)
            keybindLabel.TextColor3 = Color3.new(1, 1, 1)
            schedulePersist()
            refreshDashboard()
        end)

        return row
    end

    local allKbElements = {}
    local tabOrder = {"Home", "AUTOMINE", "WEAPONS", "PLAYER", "VISUALS", "GAME", "SETTINGS"}

    for _, tabName in ipairs(tabOrder) do
        local tabEntries = {}
        for _, regEntry in ipairs(_buttonRegistry) do
            if regEntry.tab == tabName and regEntry.type ~= "slider" then
                table.insert(tabEntries, regEntry)
            end
        end
        if #tabEntries > 0 then
            local header = createKeybindSectionHeader(tabName)
            table.insert(allKbElements, {element = header, names = {}})
            for _, regEntry in ipairs(tabEntries) do
                local row = createKeybindRow(regEntry)
                table.insert(allKbElements, {element = row, names = {regEntry.name:lower()}})
            end
        end
    end

    searchBox:GetPropertyChangedSignal("Text"):Connect(function()
        local query = searchBox.Text:lower()
        if query == "" then
            for _, item in ipairs(allKbElements) do
                item.element.Visible = true
            end
            return
        end
        local currentHeader = nil
        local headerHasMatch = false
        for _, item in ipairs(allKbElements) do
            if #item.names == 0 then
                if currentHeader then
                    currentHeader.Visible = headerHasMatch
                end
                currentHeader = item.element
                headerHasMatch = false
            else
                local matches = false
                for _, name in ipairs(item.names) do
                    if name:find(query, 1, true) then
                        matches = true
                        break
                    end
                end
                item.element.Visible = matches
                if matches then
                    headerHasMatch = true
                end
            end
        end
        if currentHeader then
            currentHeader.Visible = headerHasMatch
        end
    end)

    registerUiSync(_allTextSyncs, function()
        for bindName, rowData in pairs(keybindRows) do
            rowData.keybindLabel.Text = getKeyDisplayName(S.keybinds[bindName])
        end
    end)
end

function onTabClicked(tabName)
    switchTab(tabName)
    animateCurrentTitle(tabName)
    activeTabName = (tabName == "AUTOMINE") and "Autofarm" or tabName
    if type(refreshPathVisuals) == "function" then
        refreshPathVisuals()
    end
end

hubLogo.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("Home")
end)
_UTILS._applySoundsToButton(hubLogo)

AUTOMINEBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("AUTOMINE")
end)

weaponsBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("WEAPONS")
end)

playerBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("PLAYER")
end)

visualsBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("VISUALS")
end)

gameBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("GAME")
end)

settingsBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("SETTINGS")
end)

keybindsBtn.MouseButton1Click:Connect(function()
    if destroyed then
        return
    end
    onTabClicked("KEYBINDS")
end)

function applyAutomineOnlyModeUi()
    local autoMineOnly = AUTOMINE_ONLY_MODE == true

    if hubLogo then
        hubLogo.Visible = not autoMineOnly
    end

    for tabName, data in pairs(tabButtons) do
        if data and data.btn then
            data.btn.Visible = (not autoMineOnly) or tabName == "AUTOMINE"
        end
    end

    if autoMineOnly then
        for tabName, content in pairs(tabContents) do
            content.Visible = (tabName == "AUTOMINE")
        end
    end
end

applyAutomineOnlyModeUi()
sidebarScroll.CanvasSize = UDim2.new(0, 0, 0, AUTOMINE_ONLY_MODE and 70 or 330)

do
    local dragging = false
    local dragInput
    local dragStart
    local startPos
    local hubTargetRot = 0
    local hubCurrentRot = 0
    local lastMouseX = nil

    hubMain.InputBegan:Connect(function(input)
        if destroyed then
            return
        end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            if pathState.editorVisible and isPointInsideGuiObject(pathUi.editorFrame, input.Position) then
                return
            end
            dragging = true
            dragStart = input.Position
            startPos = hubWrapper.Position
            hideAllDropdowns()
        end
    end)

    _UserInputService.InputChanged:Connect(function(input)
        if destroyed then
            return
        end
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            dragInput = input
        end
    end)

    _UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)

    _RunService.RenderStepped:Connect(function(dt)
        if destroyed or not hubWrapper.Visible then
            return
        end
        if dragging and dragInput then
            local delta = dragInput.Position - dragStart
            local destination = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
            hubWrapper.Position = hubWrapper.Position:Lerp(destination, math.min(dt * 20, 1))
            if lastMouseX then
                hubTargetRot = math.clamp(-(dragInput.Position.X - lastMouseX) * 0.7, -15, 15)
            end
            lastMouseX = dragInput.Position.X
        else
            lastMouseX = nil
            hubTargetRot = 0
        end
        hubCurrentRot = hubCurrentRot + (hubTargetRot - hubCurrentRot) * math.min(dt * 10, 1)
        hubWrapper.Rotation = hubCurrentRot
    end)
end

do
    _UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if destroyed or not isSessionActive() or gameProcessed then
            return
        end
        if isCapturingKey() or _UserInputService:GetFocusedTextBox() then
            return
        end
        if panicKeyCode and input.KeyCode == panicKeyCode then
            destroyed = true
            runtimeEnv._FOB_GUI_ONLY_ROOT = nil
            runtimeEnv._FOB_GUI_ONLY_TOKEN = nil
            pcall(function()
                if type(stopAutoMine) == "function" then
                    stopAutoMine()
                end
            end)
            pcall(function()
                if type(clearRecordedPathVisuals) == "function" then
                    clearRecordedPathVisuals()
                end
            end)
            pcall(function()
                if type(clearPathVisualization) == "function" then
                    clearPathVisualization()
                end
            end)
            pcall(function()
                if type(restoreDynamiteSpectateCamera) == "function" then
                    restoreDynamiteSpectateCamera()
                end
            end)
            for _, gui in ipairs(_extraCleanupGuis) do
                pcall(function()
                    gui:Destroy()
                end)
            end
            pcall(function()
                hubGui:Destroy()
            end)
        end
    end)
end

do
    local isToggling = false
    local function toggleVisibility()
        if isToggling or destroyed then
            return
        end
        isToggling = true
        if hubWrapper.Visible then
            hideAllDropdowns()
            _TweenService:Create(hubWrapper, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
                Size = UDim2.new(0, 0, 0, 380),
            }):Play()
            task.delay(0.5, function()
                if hubWrapper.Parent then
                    hubWrapper.Visible = false
                end
                if type(refreshPathEditorButtons) == "function" then
                    refreshPathEditorButtons()
                end
                isToggling = false
            end)
        else
            hubWrapper.Size = UDim2.new(0, 0, 0, 380)
            hubWrapper.Visible = true
            if type(refreshPathEditorButtons) == "function" then
                refreshPathEditorButtons()
            end
            _TweenService:Create(hubWrapper, TweenInfo.new(0.8, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
                Size = UDim2.new(0, 600, 0, 380),
            }):Play()
            task.delay(0.8, function()
                isToggling = false
            end)
        end
    end

    _UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if destroyed or not isSessionActive() or gameProcessed then
            return
        end
        if isCapturingKey() or _UserInputService:GetFocusedTextBox() then
            return
        end
        if input.KeyCode == guiToggleKeyCode then
            toggleVisibility()
        end
    end)
end

do
    _UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if destroyed or not isSessionActive() or gameProcessed then
            return
        end
        if isCapturingKey() or _UserInputService:GetFocusedTextBox() then
            return
        end
        if input.UserInputType ~= Enum.UserInputType.Keyboard then
            return
        end
        local keyCode = input.KeyCode
        if keyCode == Enum.KeyCode.Unknown then
            return
        end
        if keyCode == (S.keybinds.dynamiteMark or Enum.KeyCode.F) then
            toggleHoveredDynamitePlayerMark()
            if S.dynamiteTarget == "Player" and S.dynamiteMarkPlayersEnabled then
                return
            end
        end
        for bindKey, boundKey in pairs(S.keybinds) do
            if boundKey == keyCode then
                local bindTab, bindName = bindKey:match("^(.+):(.+)$")
                if bindTab and bindName then
                    for _, regEntry in ipairs(_buttonRegistry) do
                        if regEntry.name == bindName and regEntry.tab == bindTab then
                            if (regEntry.type == "toggle" or regEntry.type == "button" or regEntry.type == "special") and regEntry.onClickFn then
                                regEntry.onClickFn()
                                syncAllUi()
                            end
                            break
                        end
                    end
                end
            end
        end
    end)
end

function showHub()
    hubWrapper.Size = UDim2.new(0, 0, 0, 380)
    hubWrapper.Visible = true
    if type(refreshPathEditorButtons) == "function" then
        refreshPathEditorButtons()
    end
    _TweenService:Create(hubWrapper, TweenInfo.new(0.8, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 600, 0, 380),
    }):Play()
    local initialTab = AUTOMINE_ONLY_MODE and "AUTOMINE" or "Home"
    switchTab(initialTab)
    activeTabName = initialTab == "AUTOMINE" and "Autofarm" or initialTab
    task.spawn(function()
        task.wait(0.2)
        animateCurrentTitle(initialTab)
    end)
end

applyStoredStateToRuntime()
if hasPlayerStateFeatureHooksEnabled() then
    ensurePlayerFeatureHooks()
end

syncAllUi()
showHub()
if CONFIG_SYSTEM.autoLoadLoadedOnStartup then
    spawnNotification("Auto-load config loaded: " .. tostring(getAutoLoadConfigName() or "config") .. ".", "success", true)
elseif CONFIG_SYSTEM.autosaveLoadedOnStartup then
    spawnNotification("Autosave from last session loaded.", "success", true)
else
    spawnNotification("Press " .. guiToggleKeyName .. " to hide GUI.", "info", true)
end
